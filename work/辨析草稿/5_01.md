# 辨析卡草稿 · 批次 5 切片 28（5 组 / worklist_idx 136–140）

> 小结：切片 `work/compare_slices/slice28.json` 的 5 组（worklist_idx 136–140）全部写完；门禁 `python3 tools/check_compare_draft.py work/辨析草稿/5_01.md` 结果见本行末：PASSED（0 条查不到）。宽度全部用 `tools/width_rule.py` 那把尺实测，标称值逐组见各组小标题。
> 提请拍板 3 处：**组 3**（assess / gauge）共享义项「评估」只在 assess 一侧成立（1397 的 gauge 是名词「量规」，gauge 的动词档本书 0 处）；**组 4**（compute / estimate）「估算」这一档本书两处译文都没给 compute（1398 译「计算」、卡 ex 译「算出」），只在 estimate 侧成立；**组 5**（estimate / evaluate）「估价」在这一段两侧都不成立（1399 译「估算」、1400 译「评价」），evaluate 的估价档只在卡 ex 那块旧手表上。另有组 1、组 2 属"义项在卡上成立、在课文那处换了中文"，按口径 6 就地处理，不必拍板。
> 未动任何产品代码 / 数据文件（`shadow/data/vocab.json`、`shadow/data/sections.json` 只读未改，章3 坏主题栏未碰），未碰别人的草稿与 `work/辨析审核/`，未跑 `tools/land_compare.py`，未做 git 写操作。

---

## 组1 · device / equipment —— 设备 ｜ `all_synonyms`: false ｜ 段 [4,19] ｜ 句 1394 / 1395

**共现位置**：1 段 2 句 —— 第 4 章（历史与发明）段 19（全局 1390–1395），成员句 1394（device）与 1395（equipment）**相邻两句、说的是同一批院子里的家伙**：前一句是那台连上厨房电器的节水机器，后一句是院子免费摊开来晚上共用的那一堆。**主锚点 = 章4 段19**（`paraZh`：老海员定下水域方向，基金支持下学生在院子里开工。`subheads[4][18]`：下篇·发明比赛）。卡挂段 19 末；同段的 domain / realm（worklist_idx 135，1390 那句）已由批次 4 的 4_09 组 2 占用，本组只讲"那台机器 vs 那一堆家伙"，不回去讲水域（口径 9）。
主锚点 = 章4 段19

**这一段在讲什么**（只按 1392–1395 四句说）：他们中午集中、用学校后面的雨水罐（1392），日常用水量简单、害羞的测试员每次试验后记笔记（1393），1394 给出第一台装置——它连上一件厨房电器来给植物节水，1395 说学校院子免费提供场地、晚上共用设备干活。段主题栏写「老海员定下水域方向，基金支持下学生在院子里开工」（段主题栏，本段句子撑得住的是"院子里开工"那半，"老海员""基金"在 1390、1391，本组不引）。

**该段原文（章4 段19，成员两句 + 前 2 句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1392 | They would **concentrate** each noon and **utilise** rain jars behind the school. | 他们每天中午集中，用学校后面的雨水罐。 |
| 1393 | Daily **usage** was simple, so a shy **tester** wrote notes after each **trial**. | 日常用法简单，害羞的测试员每次试验后记笔记。 |
| 1394 | The **first** **device** joined a **kitchen** **appliance** to save water for plants. | 第一个装置连着厨房电器，为植物节水。 |
| 1395 | The school yard gave free **facilities** and shared **equipment** for evening work. | 学校院子免费提供场地，晚上共用设备干活。 |

- device 全书带 `[[词头:…]]` 标记 1 处（1394）、未打标记 0 处（按 device/devices 扫全书 1833 句，只有这一句）。
- equipment 全书带标记 1 处（1395）、未打标记 0 处（equipment 在课文只这一句）。

**证据强度**：两个词都只有 1 处书证（口径 7：常态），但各自的**卡上文本**不薄——device 卡 `ex` + 词伙 3 条 + 别的卡 3 条，equipment 卡 `ex` + 词伙 3 条 + 别的卡 2 条。两卡 `note` 都**只有词伙段、没有同义词栏**（设备这一档卡上没给近义提示），worklist 标 `all_synonyms: false` 有据。

### 表头一句（title）

> 同一批院子里的家伙，课文先叫它 device（1394 那台还得连上一件厨房电器）、紧接着改口 equipment（1395 摊开来晚上共用的一堆）——worklist 给的共同义项是「设备」，本书译文只把设备这三个字给了 equipment，device 那处译成了「装置」：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| device | /dɪˈvaɪs/ | n. | 设备；装置；手段 | 一台台数得清的那件 | 院子里第一台、连着厨房电器节水；卡上是新买回来拍照的那台；键盘那种外围的 | `The first device joined a kitchen appliance`（课文 1394） |
| equipment | /ɪˈkwɪpmənt/ | n. | 设备；装备 | 摊开来共用的一整套 | 学校院子晚上共用那堆；健身房新添的；实验室要测无菌的那套 | `shared equipment for evening work`（课文 1395） |

### 默认那一行（实测宽度 35.5 / ≤40）

```
device = 连着厨房电器那台，equipment = 晚上共用那堆（shared equipment）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段它站哪一边 | device | `The first device`（1394）、`joined a kitchen appliance`（1394，它得连上一件电器才干活）、`to save water for plants`（1394） | 说"造出来的那台东西"，有名字有功能 | 它在本段是一件具体的、被学生做出来的东西；主语就是它自己 |
| 本段它站哪一边 | equipment | `shared equipment for evening work`（1395）、`The school yard gave free facilities`（同句前半，场地和设备一块儿给） | 说"这一摊摊家当"，谁都能拿来用 | 它在本段不是某一台，是晚上大家共用的一堆；句子里没给它任何数量 |
| 卡上词伙把它带往哪一行 | device | `digital devices`、`electronic devices`、`portable device`（卡词伙；后两条本书课文未用到）；旁证 `The keyboard is a peripheral device.`（peripheral 卡 ex）、`mobile devices`（mobile 卡词伙）、`portable device`（portable 卡词伙，与设备卡同串） | 电子、能拿在手里的那一类东西 | 本书凡是给 device 找伴儿，伴儿全是"电"那一挂（digital / electronic / portable / mobile / peripheral） |
| 卡上词伙把它带往哪一行 | equipment | `fitness equipment`、`household equipment`、`medical equipment`（卡词伙，三条本书课文均未用到；household 卡词伙有同一条 `household equipment`）；旁证 `The gym has new exercise equipment.`（卡 ex）、`The lab tested the sterility of the equipment.`（sterility 卡 ex） | 按场合成套配齐的那一摊：健身房 / 家里 / 医院 / 实验室 | 给 equipment 找伴儿的是**场所**（哪一行用），给 device 找伴儿的是**性质**（带电不带电） |
| 本书这几处它前面站什么 | device | `The first device`（1394）、`a new device`（卡 ex）、`a peripheral device`（peripheral 卡 ex）、`portable device`（卡词伙） | 单数、前面挂 a / the | 本书 4 处 device 写法全是单数，其中 3 处前面明明白白挂着 a / the |
| 本书这几处它前面站什么 | equipment | `shared equipment`（1395）、`new exercise equipment`（卡 ex）、`the equipment`（sterility 卡 ex） | 前面是修饰语或 the，没有 a、没有数目 | 本书 6 处 equipment 里没有任何一处是"一个 equipment"这种形状，也没出现加 s 的写法（`equipments` 两份数据各 0 处，已 grep）；这是本书这几处的分布，不是语法规则 |
| 中文落点 | 两个词 | device：1394「装置」／卡 ex「新设备」；equipment：1395「设备」／卡 ex「健身设备」（`sentZh`、`exZh`） | 汉译英时先问中文给的是"一台机器"还是"一摊家当" | 本书译文自己就没把这两个词并成同一个中文词：「装置」给了 device，「设备」给了 equipment |

### 总结句（实测宽度 21.5 / ≤30）

> device 是一台台数得清的，equipment 是一堆。

### 出处清单

- `The first device joined a kitchen appliance to save water for plants`、`The first device`、`first device`、`joined a kitchen appliance`、`a kitchen appliance`、`to save water for plants`、译文「第一个装置连着厨房电器，为植物节水」 —— 课文第 1394 句（章4 段19 第 4 句）及 `sentZh`。device 侧全书唯一的课文书证。
- `The school yard gave free facilities and shared equipment for evening work`、`shared equipment`、`for evening work`、`The school yard`、`free facilities`、译文「学校院子免费提供场地，晚上共用设备干活」 —— 课文第 1395 句（章4 段19 第 5 句）及 `sentZh`。equipment 侧全书唯一的课文书证。
- `He bought a new device to take photos.` / 他买了一个新设备来拍照。 —— device 卡 `ex` / `exZh`（表里写作 `a new device`，为其连续子串）。
- `digital devices`、`electronic devices`、`portable device` —— device 卡 `note` 词伙段（本书课文 0 处用到，已 grep；`portable device` 与 portable 卡 `note` 词伙段同串，按口径 8 算 1 条文本）。
- `The gym has new exercise equipment.` / 健身房有新的健身设备。 —— equipment 卡 `ex` / `exZh`（表里写作 `new exercise equipment`，为其连续子串）。
- `fitness equipment`、`household equipment`、`medical equipment` —— equipment 卡 `note` 词伙段；其中 `household equipment` 同见 household 卡 `note` 词伙段（跨卡同串，算 1 条）。三条在本书课文 0 处。
- `The keyboard is a peripheral device.` —— peripheral 卡 `ex`（表里写作 `a peripheral device`，为其连续子串）；`mobile devices` —— mobile 卡 `note` 词伙段。两条只作"device 被别的卡怎么用"的旁证，不进成员。
- `The lab tested the sterility of the equipment.` —— sterility 卡 `ex`（表里写作 `the equipment`，为其连续子串）；只作 equipment 侧"被别的卡怎么用"的旁证。
- `They would concentrate each noon and utilise rain jars behind the school`、`Daily usage was simple, so a shy tester wrote notes after each trial` —— 课文第 1392 / 1393 句（本段其余有卡的词，只作表头背景，不进成员）。
- 义项「设备；装置；手段」「设备；装备」 —— 两卡 `m`；「老海员定下水域方向，基金支持下学生在院子里开工」—— 段 19 `paraZh`（本组只引用它被 1394/1395 撑住的那半）；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，「设备」这档在课文那处换了中文

- 两卡 `note` 都只有词伙段、无同义词栏，互不列对方 → worklist 标 `all_synonyms: false` 有据；本组属"中文义项撞车"型，区别落在**一台 vs 一堆 + 词伙各行 + 本书冠词分布**三格。
- **口径 6 处理**：共享义项「设备」来自两卡 `m` 的交集，但本书 1394 那处 device 译的是「装置」，所以标题与默认行只说"这一段各领哪样东西"、「设备」留在 `sense` 槽。它不算名不副实到底：device 卡自己的 `ex` 就译成「新设备」（`He bought a new device to take photos.`），"设备"这档在卡上是有中文落点的，只是课文那处没这么译。
- **同源合并（口径 8）**：device 卡词伙 `portable device` = portable 卡词伙、equipment 卡词伙 `household equipment` = household 卡词伙，各算 1 条；两侧与课文都**不同源**（课文那两句的词伙卡上一个都没写），所以独立文本 device 侧 5 条（课文 1 + 卡 ex 1 + 卡词伙 3）、equipment 侧 5 条（课文 1 + 卡 ex 1 + 卡词伙 3，加 peripheral/mobile/sterility 旁证另计）。是本片厚薄最对称的一组。
- **计数结论、不是禁令**：第 5、6 行两格来自本书 device 4 处 / equipment 6 处的分布，样本小；"equipment 不加 s"这条只写到"本书没出现这种写法"为止，不写成语法判决。
- **不写的**：「device 比 equipment 小／更聪明」「equipment 专指重型机械」（两卡 `note` 无语域标注，本书给不出证据）、device 卡 `m` 第三档「手段」（本书 0 处书证，只报备不造句，`by all devices` 一类本书没有）、`a piece of equipment`、`laboratory equipment`、`safety device` 这类两份数据各 0 处的写法一律不列。
- 无需拍板：义项在卡上有落点（见上），本组按现表落地即可。

---

## 组2 · instrument / tool —— 工具 ｜ `all_synonyms`: false ｜ 段 [4,20] ｜ 句 1396

**共现位置**：1 段 1 句 —— 第 4 章段 20（全局 1396–1401），两个词挤在同句 1396 的前半、由 `and` 并列，后半共用一个 `stayed in a locked wooden box`。**主锚点 = 章4 段20**（`paraZh`：工具量具齐备，学生测量计算，请老师评估改进。`subheads[4][18]`：下篇·发明比赛）。卡挂段 20 末；同段末还要挂组 3（assess / gauge）、组 4（compute / estimate）、组 5（estimate / evaluate）三张，本组只讲"箱里那件 vs 手里那把"（口径 9）。
主锚点 = 章4 段20

**这一段在讲什么**（只按 1396–1398 三句说）：1396 说每件锋利的 instrument 和 hand tool 都关在上锁的木箱里，1397 说那把黄铜 gauge 帮学生每天稳稳地量浑水，1398 说他们晚饭后在纸上算省下的水滴和费用。段主题栏写「工具量具齐备，学生测量计算，请老师评估改进」（段主题栏，其中「改进」二字本段句子不含此事，本组不引）。

**该段原文（章4 段20，成员句 + 后 2 句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1396 | Each sharp **instrument** and hand **tool** stayed in a locked **wooden** box. | 每件锋利的仪器和手工具都放在上锁的木箱里。 |
| 1397 | A **brass** **gauge** helped pupils **measure** **muddy** water with **steady** hands daily. | 那把黄铜量规，帮学生们每天都稳稳地测那些浑水。 |
| 1398 | They would **calculate** drops saved and **compute** costs on **paper** after **supper**. | 他们晚饭后在纸上计算省下的水滴和费用。 |

- instrument 全书带 `[[词头:…]]` 标记 1 处（1396）、未打标记 0 处（按 instrument(s) 扫全书 1833 句，只有这一句）。
- tool 全书带标记 1 处（1396）、**未打标记另有 2 处**：课文 397（`fixed with tools`，译「又用工具把弯了的车架修好了」）、课文 760（`bright steel tools`，译「亮闪闪的钢制工具」）—— 按 §五.13 两档分开写。

**证据强度**：instrument 侧独立文本 4 条（课文 1 + 卡 ex 1 + 卡词伙 2），全书只露面 1 次，是本片最薄的一侧之一；它的 `note` 有词伙段、**没有同义词栏**。tool 侧独立文本 12 条上下（课文标 1 + 未标 2 + 卡 ex 1 + 卡词伙 3 + 别的卡 ex 5），`note` 同样只有词伙段、没有同义词栏。

### 表头一句（title）

> 同一个上锁的木箱，本书给里面两样东西各配了一个修饰语：sharp（锋利的）给了 instrument、hand（手上的）给了 tool——worklist 说共同义项是「工具」，可本段真译成「工具」的只有 tool 那一侧（instrument 译的是「仪器」）：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| instrument | /ˈɪnstrəmənt/ | n. | 仪器；工具 | 锋利到要上锁那件 | 木箱里那件锋利的；卡上是那把盖房子用的锤子；词伙里还要被人 play | `Each sharp instrument`（课文 1396） |
| tool | /tuːl/ | n. | 工具；刀具；器具 | 手里那把干活的 | 同一只木箱里那把 hand 的；修自行车架的；王叔那批钢制的；卡上是修东西那把锤子 | `hand tool`（课文 1396） |

### 默认那一行（实测宽度 35.5 / ≤40）

```
instrument = 锋利那件（sharp instrument），tool = 手里那把（hand tool）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 同句里它各领哪个修饰语 | instrument | `Each sharp instrument`（1396）、`stayed in a locked wooden box`（1396，两样共用后半） | 说"磨得锋利、要锁起来的那件" | 本书唯一一处给它的是 sharp（锋利），跟"精密度"没关系——是句子自己分的 |
| 同句里它各领哪个修饰语 | tool | `hand tool`（1396，配的是 hand 而不是 sharp）、同句后半 `a locked wooden box` | 说"握在手里那把" | 同一句里 tool 领到的是 hand——两只手的东西一起上锁，本书就这么把它们并排放 |
| 卡 ex 给它哪桩活儿 | instrument | `A hammer is a useful instrument for building.`（卡 ex，for building）、`a useful instrument`（连续子串） | 盖东西那桩 | 两卡 ex 是同一句式只换了词头、连主角都是同一把 hammer（见「数据边界」）——这是本书自己给的"能换"物证 |
| 卡 ex 给它哪桩活儿 | tool | `A hammer is a useful tool for fixing things.`（卡 ex，for fixing things） | 修东西那桩 | 它的目的状语是 fixing things（修东西），跟课文 397 修车架、electronics 卡修电器一路：tool 的活儿是修 |
| 卡上词伙把它带往哪一行 | instrument | `play an instrument`、`musical instruments`（卡词伙；两条本书课文均未用到，已 grep）；`musical instruments` 同见 musical 卡词伙段（同串，算 1 条） | 乐器那一行 | instrument 卡上另挂着"乐器"这一档（play 的对象），tool 卡上一个字都没提乐器——这是两卡最硬的一处不对称 |
| 卡上词伙把它带往哪一行 | tool | `online tools`、`digital tools`、`communication tools`（卡词伙，三条本书课文均未用到）；旁证 `steel tools`（steel 卡词伙 = 课文 760 同源）、`The school uses multimedia tools to teach students.`（multimedia 卡 ex） | 线上 / 屏幕里那一行，加手边那把 | tool 卡上另开的这一档是"看不见也用得着"的东西，而 instrument 那条词伙走的是"演奏"，两词各往一个完全不同的行业跑 |
| 中文落点 | 两个词 | instrument：1396「仪器」／卡 ex「工具」（`exZh`）；tool：1396「手工具」／397「工具」／760「钢制工具」／卡 ex「工具」 | 汉译英时先问中文是"仪器"还是"手里那把" | 本书译文只在卡 ex 那一句让 instrument 说成「工具」，课文那处给的是「仪器」；tool 四处译文全带「工具」两个字 |

### 总结句（实测宽度 23.0 / ≤30）

> instrument 另有一档乐器，tool 另有一档线上的。

### 出处清单

- `Each sharp instrument and hand tool stayed in a locked wooden box`、`Each sharp instrument`、`sharp instrument`、`hand tool`、`stayed in a locked wooden box`、`a locked wooden box`、译文「每件锋利的仪器和手工具都放在上锁的木箱里」 —— 课文第 1396 句（章4 段20 第 0 句）及 `sentZh`。两词在本书唯一一处同段同句书证。
- `A hammer is a useful instrument for building.` / 锤子是建造时有用的工具。 —— instrument 卡 `ex` / `exZh`（表里写作 `for building`、`a useful instrument`，均为其连续子串）。
- `A brass gauge helped pupils measure muddy water with steady hands daily` —— 课文第 1397 句（本组只作背景，gauge 归组 3）。
- `They would calculate drops saved and compute costs on paper after supper` —— 课文第 1398 句（本组只作背景，calculate / compute 不与本组并卡）。
- `A hammer is a useful tool for fixing things.` / 锤子是修理东西的有用工具。 —— tool 卡 `ex` / `exZh`（表里写作 `for fixing things`、`a useful tool`，均为其连续子串）。
- `play an instrument`、`musical instruments` —— instrument 卡 `note` 词伙段（本书课文 0 处用到，已 grep）；`musical instruments` 同见 musical 卡 `note` 词伙段（跨卡同串，按口径 8 算 1 条）。
- `online tools`、`digital tools`、`communication tools` —— tool 卡 `note` 词伙段（本书课文 0 处用到，已 grep）。
- `The bike repair manual was read and the bent frame fixed with tools`、`fixed with tools`、译文「他读完了自行车修理手册，又用工具把弯了的车架修好了」 —— 课文第 397 句（章1 段9；该句 tools 未打标记，按 §五.13 单列）。
- `Uncle Wang brought bright steel tools, ensuring strength and long stability for shelves`、`bright steel tools`、`steel tools`、译文「王叔带来了亮闪闪的钢制工具，确保架子结实、能用很多年」 —— 课文第 760 句（章2 段18；该句 tools 未打标记）；`steel tools` 同见 steel 卡 `note` 词伙段（与 760 **同源**）。
- `The new tool enables him to finish work faster.`（enable 卡 ex）、`He fixed the broken electronics with a small tool.`（electronics 卡 ex）、`This tool is practical for everyday use.`（practical 卡 ex）、`We can utilise this tool to fix the problem.`（utilise 卡 ex）、`The school uses multimedia tools to teach students.`（multimedia 卡 ex） —— 五张别的卡的 `ex`，只作"tool 被别的卡怎么用"的旁证，不进成员。
- 义项「仪器；工具」「工具；刀具；器具」 —— 两卡 `m`；「工具量具齐备，学生测量计算，请老师评估改进」—— 段 20 `paraZh`（只引用被 1396–1398 撑住的部分）；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，但 instrument 侧全书只有 1 处（薄，不退回）

- 两卡 `note` 都只有词伙段、**连同义词栏都没有**，谈不上互列 → worklist 标 `all_synonyms: false` 有据。同时两卡 `ex` 是同一句式只换词头（`A hammer is a useful instrument for building.` ↔ `A hammer is a useful tool for fixing things.`，主角同一把 hammer）：按口径 14，**同形这一事实照写**（本书最强的"可换"物证），`false` 也照写（两栏没互列），这两句**各算一侧书证**、不并成一条。
- **口径 6 处理**：共享义项「工具」取自两卡 `m` 的交集，但本段 1396 的 instrument 译「仪器」、tool 译「手工具」，"工具"这三个字在本段只落在 tool 那侧。它不算名不副实到底：instrument 卡的 `exZh` 自己就把 instrument 译成了「工具」（锤子是建造时有用的工具），所以"instrument 也能叫工具"在卡上有中文落点，只是课文那处没这么译。标题与默认行因此只说"各领哪个修饰语"，「工具」留在 `sense` 槽。
- **同源合并（口径 8）**：steel 卡词伙 `steel tools` 就是课文 760，算 1 条；instrument 卡词伙 `musical instruments` 与 musical 卡词伙同串，算 1 条。两侧与课文都不同源（卡上两条词伙本书课文均未用到）。
- **tool 卡上第二、三档（「刀具；器具」）本书没这样用过**：本书 3 处 tool 的译文是「工具」「钢制工具」「手工具」，没有一处落在这两档（已 grep 译文）。另两处中文写「刀具」的句子（课文 1317、1367）英文都是 knives；写「工具箱」「用具」的课文 655 用的是 `repair kit` 与 `chrome utensil`。读到 tool 别往那三处套。
- **计数结论、不是禁令**："tool 是修东西的、instrument 不修"来自本书 tool 侧 12 条 vs instrument 侧 4 条的分布，instrument 侧样本只有 1 句课文，措辞已按"本书这几处"落地。
- **不写的**：「instrument 比 tool 精密／正式／书面」（两卡 `note` 无语域标注，本书给不出证据）、「instrument 还能指法律手段／工具手段」（卡 `m` 没这一档，`an instrument of the law` 本书 0 处）、`power tools`、`garden tools`、`measuring instrument`、`toolbox`、`precision instrument`（五串两份数据各 0 处，已 grep）一律不列。
- 无需拍板：本组虽薄，但有一处真区别可落地 —— **instrument 卡上带"乐器"这一档（play an instrument / musical instruments），tool 卡上带"线上"这一档（online / digital / communication tools）**，两串词伙都在卡上、可 grep，是本组唯一一处"一边有一边没有"的硬货。

---

## 组3 · assess / gauge —— 评估 ｜ `all_synonyms`: false ｜ 段 [4,20] ｜ 句 1397 / 1400

**共现位置**：1 段 2 句 —— 第 4 章段 20（全局 1396–1401），成员句 1397（gauge）与 1400（assess）**隔两句、同一套活儿的头和尾**：1397 是学生手上那把黄铜量具，1400 是老师来看作品的那一眼。**主锚点 = 章4 段20**（`paraZh`：工具量具齐备，学生测量计算，请老师评估改进。`subheads[4][18]`：下篇·发明比赛）。卡挂段 20 末；组 2（instrument / tool，1396）、组 4（compute / estimate，1398/1399）、组 5（estimate / evaluate，1399/1400）同挂这一段，本组只讲"器具 vs 动作"，别再回去讲木箱和账（口径 9）。
主锚点 = 章4 段20

**这一段在讲什么**（只按 1397、1399、1400 三句说）：1397 那把 brass gauge 帮 pupils 每天稳稳地量 muddy water；1399 他们数清空的罐子、估算整条街的节水量；1400 老师们 come to assess the build、带着微笑 evaluate drawings。段主题栏那句「请老师评估改进」里的"评估"正对上 1400（段主题栏，"改进"二字本段句子不含此事，不取）。

**该段原文（章4 段20，成员两句 + 中间一句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1397 | A **brass** **gauge** helped pupils **measure** **muddy** water with **steady** hands daily. | 那把黄铜量规，帮学生们每天都稳稳地测那些浑水。 |
| 1399 | They could **count** jars emptied and **estimate** savings for the whole street. | 他们数清空的罐子，估算整条街的节水量。 |
| 1400 | Teachers came to **assess** the build and **evaluate** drawings with kind smiles. | 老师们来评估作品，带着微笑评价图纸。 |

- gauge 全书带 `[[词头:…]]` 标记 1 处（1397）、未打标记 0 处（按 gauge/gauges/gauged 扫全书 1833 句只这一句）。
- assess 全书带标记 1 处（1400）、未打标记 0 处（按 assess/assessed/assessment 扫全书只这一句；`assessment` 这个词形两份数据里只出现在 measure 卡的同义词栏，课文 0 处）。

**证据强度**：两侧各 2 条独立文本（assess = 课文 1 + 卡 ex 1；gauge = 课文 1 + 卡 ex 1），都薄（口径 7：常态）。**assess 卡的 `note` 只有「同义词：」栏、没有词伙段** —— 所以它一栏搭配都给不出，表里 assess 的搭配一律取自课文与卡 ex；gauge 的词伙 `brass gauge` 与课文 1397 **同源**，不另计。

### 表头一句（title）

> 同一段，gauge 是学生手上那把黄铜器具（1397）、assess 是老师过来的那一下（1400）——worklist 说共同义项是「评估」，可本书两处译文里「评估」这两个字只出现在 assess 那侧，gauge 被译成「量规」、卡上译成「测量仪器」：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| gauge | /ɡeɪdʒ/ | n. | 测量仪器；规格；评估 | 手上那把带刻度的 | 学生每天稳稳量浑水的那把黄铜的；机修工查轮胎气压用的那把 | `A brass gauge helped pupils measure muddy water`（课文 1397） |
| assess | /əˈses/ | v. | 评估 | 老师过来看那一眼 | 老师们来看这件作品；老师明天看你的作业 | `Teachers came to assess the build`（课文 1400） |

### 默认那一行（实测宽度 30.0 / ≤40）

```
gauge = 那把黄铜量规（brass gauge），assess = 老师来看那一眼
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 它是手里那件还是那个动作 | gauge | 卡 `m` 三档全是 n.（测量仪器；规格；评估）；1397 它当主语 `A brass gauge helped pupils`、卡 ex 它当宾语 `used a gauge to check the tire pressure` | 说桌上那件器具，它自己"帮"人干活 | 本书 gauge 露面 2 次，两次都是名词、两次都是手里那件东西 |
| 它是手里那件还是那个动作 | assess | 卡 `m` 只 v. 一档；1400 `came to assess the build`（不定式）、卡 ex `will assess your homework tomorrow`（将来时） | 说人做的一个动作，往后要接宾语 | 本书 assess 露面 2 次，两次都是动词、后面都接一份"要被打分的东西" |
| 谁用它、对谁用 | gauge | `helped pupils measure muddy water`（1397，用它的是学生，量的是浑水）、`The mechanic used a gauge to check the tire pressure.`（卡 ex，用它的是机修工，查的是轮胎气压；本书课文 1384 真请了一位 kind mechanic 来指导学生） | 用量具去测一个看得见的量 | 它这边永远有一个能拿在手里的东西、加一个被测的物理量（水深、气压） |
| 谁用它、对谁用 | assess | `Teachers came to assess the build`（1400，主语是老师们，宾语是他们做出来的作品）、`The teacher will assess your homework tomorrow.`（卡 ex，主语是老师，宾语是作业） | 打分的人来看一份成品 | 它这边永远是老师、对象永远是"人做出来的东西"（作品、作业）——本书两处没有一个宾语是物理量 |
| 卡上还给了哪一档、用上了吗 | gauge | 词伙 `brass gauge`（= 课文 1397，同源）；`m` 的「规格」档与「评估」档本书均 0 处（已 grep 全书 gauge 词形只有 1397 一处） | 想说"一种规格"或"去估一估"时本书给不出例子 | 本组的坑就在 gauge 卡上：它自己挂着"评估"那一档，可本书没有把它当动词用过（`gauge the pressure` 这类写法本书没有，已 grep 两份数据各 0 处） |
| 卡上还给了哪一档、用上了吗 | assess | 卡 `m` 只有「评估」一档；`note` 只有同义词栏 `calculate, measure, evaluate`、**没有词伙段** | 只管这一个动作 | assess 侧没有词伙可依，表里它的两条搭配全来自课文与卡 ex，落地时别替它补搭配 |
| 中文落点 | 两个词 | gauge：1397「量规」／卡 ex「测量仪器」；assess：1400「评估」／卡 ex「评估」（`sentZh`、`exZh`） | 汉译英时先问中文给的是"一把器具"还是"一个动作" | 本书译文里「评估」两个字只跟着 assess 出现过（两处），gauge 两处译文都是一种器具 |

### 总结句（实测宽度 22.0 / ≤30）

> 「评估」这两个字，本书只给了 assess 的两句。

### 出处清单

- `A brass gauge helped pupils measure muddy water with steady hands daily`、`A brass gauge helped pupils measure muddy water`、`A brass gauge`、`brass gauge`、`helped pupils measure`、`measure muddy water`、`muddy water`、`with steady hands daily`、`steady hands`、译文「那把黄铜量规，帮学生们每天都稳稳地测那些浑水」 —— 课文第 1397 句（章4 段20 第 1 句）及 `sentZh`；`brass gauge` 同见 gauge 卡 `note` 词伙段（与 1397 **同源**，算 1 条）；`brass` 另见 brass 卡。gauge 侧全书唯一课文书证。
- `Teachers came to assess the build and evaluate drawings with kind smiles`、`Teachers came to assess the build`、`came to assess`、`assess the build`、`the build`、`evaluate drawings`、`with kind smiles`、译文「老师们来评估作品，带着微笑评价图纸」 —— 课文第 1400 句（章4 段20 第 4 句）及 `sentZh`。assess 侧全书唯一课文书证；同句的 evaluate 归组 5。
- `They could count jars emptied and estimate savings for the whole street`、`count jars emptied`、`estimate savings for the whole street` —— 课文第 1399 句（本组只作中间背景，归组 4 / 组 5）。
- `The mechanic used a gauge to check the tire pressure.` / 机械师用一个测量仪器来检查轮胎气压。 —— gauge 卡 `ex` / `exZh`（表里写作 `used a gauge to check the tire pressure`、`the tire pressure`，均为其连续子串；`tire pressure` 两份数据只此一处，课文 0 处）。
- `The teacher will assess your homework tomorrow.` / 老师明天会评估你的作业。 —— assess 卡 `ex` / `exZh`（表里写作 `will assess your homework tomorrow`、`assess your homework`，均为其连续子串）。
- 同义词：estimate（gauge 卡 `note`，栏里**没有** assess）／同义词：calculate, measure, evaluate（assess 卡 `note`，栏里**没有** gauge；此栏之后**没有词伙段**） —— 两卡 `note` 同义词栏，互不列对方，worklist 标 `all_synonyms: false` 有据。
- `Our small town polytechnic asked a kind mechanic to guide school pupils today`、`a kind mechanic`、译文「镇上小理工学院请了一位和善的机修工来指导学生」 —— 课文第 1384 句（章4 段18）；只作"机修工 + 量具"这一行的旁证（gauge 卡 ex 的主语就是 mechanic），不进成员。
- 义项「测量仪器；规格；评估」「评估」 —— 两卡 `m`；「工具量具齐备，学生测量计算，请老师评估改进」—— 段 20 `paraZh`（只取被 1397/1400 撑住的"量具"与"评估"两层）；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，但「评估」只在 assess 一侧（**要拍板**）

- **口径 6 处理（本组提请拍板）**：worklist 的「评估」是两卡 `m` 的交集（gauge 卡第三档就写着"评估"），但共现两处走的完全不是同一件事：1397 的 gauge 是名词"量规"、1400 的 assess 是动词"评估"。本书**没有**任何一处把 gauge 当"评估"用过（全书 gauge 词形只 1397 一处，已 grep），也没给 gauge 写过 `gauge the pressure` 这类动词句（`gauge the` 两份数据 0 处，已 grep）。所以标题与默认行只说"这一段各领哪样东西"、「评估」留在 `sense` 槽。
- 还有一种**同段异档**要写清：gauge 卡上「规格」那一档本书也 0 处，本书给 gauge 的全部文本就是"器具"这一档。这不是"本书没这个词形"，别升级成那种说法。
- **同源合并（口径 8）**：gauge 词伙 `brass gauge` = 课文 1397；assess 侧无词伙可同源。两侧独立文本各 2 条（课文 1 + 卡 ex 1），是本片最对称的两条薄证。
- **assess 无词伙段是本组的真实缺口**：表里 assess 的"搭配"两格只能挂课文与卡 ex 的原句，落地时不要把 assess 那格写成空着的搭配清单，也不要现编 `assess the situation` 之类（这种写法本书没有，已 grep 两份数据各 0 处）。
- **计数结论、不是禁令**：gauge 只有 1 处课文书证，"gauge 在本书是名词"是这一处 + 卡 ex 一处的分布，不是普适规则（英语里 gauge 作动词很常见，本书没用到就是没用到）。
- **不写的**：「assess 比 evaluate 更正式」「gauge 强调凭经验估」（两卡 `note` 无语域标注，本书给不出证据）、`an assessment of`、`assess the situation`、`gauge the speed`、`tire gauge`、`pressure gauge`（后三串两份数据各 0 处，已 grep）一律不列；gauge 卡 `m` 的「规格」档只报备不造句。
- **提请拍板点**：本组唯一可争议的格子是"能不能在表里让 gauge 沾『评估』这个义项"。我的判断：不沾 —— 表里从头到尾把 gauge 当器具写，只在「数据边界」说明卡上那一档本书没用过。若他要求"共享义项必须在共现段成立"，本组属名不副实型，需他点头才按现表落地。

---

## 组4 · compute / estimate —— 估算 ｜ `all_synonyms`: false ｜ 段 [4,20] ｜ 句 1398 / 1399

**共现位置**：1 段 2 句 —— 第 4 章段 20（全局 1396–1401），成员句 1398（compute）与 1399（estimate）**相邻两句、句式还是一个模子**（`They would … and compute costs …` / `They could … and estimate savings …`）。**主锚点 = 章4 段20**（`paraZh`：工具量具齐备，学生测量计算，请老师评估改进。`subheads[4][18]`：下篇·发明比赛）。卡挂段 20 末；同段末的组 2（1396）、组 3（1397/1400）、组 5（1399/1400）各讲各的，本组只讲"已经花掉的那笔账 vs 还没省下来的那个数"（口径 9）。
主锚点 = 章4 段20

**这一段在讲什么**（只按 1398、1399 两句说）：1398 说他们会 calculate drops saved、然后 compute costs，时间地点给得很死——on paper、after supper；1399 说他们能 count jars emptied，再 estimate savings for the whole street。两句都是"数得清的那一件 + 算不出来的那一件"配成一对。段主题栏「学生测量计算」（段主题栏，本段句子撑得住）只到"算"为止，不涉及"估算"这个中文词。

**该段原文（章4 段20，成员两句 + 前 1 句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1397 | A **brass** **gauge** helped pupils **measure** **muddy** water with **steady** hands daily. | 那把黄铜量规，帮学生们每天都稳稳地测那些浑水。 |
| 1398 | They would **calculate** drops saved and **compute** costs on **paper** after **supper**. | 他们晚饭后在纸上计算省下的水滴和费用。 |
| 1399 | They could **count** jars emptied and **estimate** savings for the whole street. | 他们数清空的罐子，估算整条街的节水量。 |

- compute 全书带 `[[词头:…]]` 标记 1 处（1398）、未打标记 0 处（按 compute/computed/computing 扫全书 1833 句只这一句；`computer` 在课文 0 处，只在 10 张别的卡的 `ex` 里出现，属另一个词，已 grep）。
- estimate 全书带标记 1 处（1399）、未打标记 0 处（课文里 `estimat` 词形另有 498、499 两句，那是 `underestimating` / `overestimate`，各自有独立卡，不算 estimate 的书证）。

**证据强度**：两侧各 2 条独立文本（compute = 课文 1 + 卡 ex 1，estimate = 课文 1 + 卡 ex 1），卡上词伙 `compute costs`、`estimate savings` 与两句课文**一一同源**（口径 8），所以真正确实独立的只有卡 ex 那两句。compute 的 `note` **只有词伙段、没有同义词栏**；estimate 的 `note` 两栏齐（同义词：approximate, calculate, gauge；词伙：estimate savings）。

### 表头一句（title）

> 挨着的两句都在算数、连句式都一样，算的东西却不是一回事：1398 的 compute 算晚饭后纸上那笔费用，1399 的 estimate 猜整条街还没省下来的水——worklist 给的共同义项「估算」，本书两处译文只把"估"这个字给了后面那句：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| compute | /kəmˈpjuːt/ | v. | 估算；计算 | 纸上算出的那笔账 | 晚饭后在纸上算的费用；卡上那笔被很快算出来的总账 | `compute costs on paper after supper`（课文 1398） |
| estimate | /ˈestɪmeɪt/ | v. / n. | 估算；估价 | 还没发生的那个数 | 整条街省下的水还只是预期；卡上问的是"要多久" | `estimate savings for the whole street`（课文 1399） |

### 默认那一行（实测宽度 38.0 / ≤40）

```
compute = 纸上的账（compute costs），estimate = 还没省的（estimate savings）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本句它的宾语是什么 | compute | `compute costs`（1398，卡词伙同条、同源）、`on paper after supper`（1398：纸上手算、晚饭后）、同句并列的 `calculate drops saved`（calculate 卡词伙 `calculate drops` 与同句同源） | 算一笔已经存在的账 | 它接的是 costs（已经花出去/要花的钱），同句被它并列的那个 calculate 接的也是实数（省下的水滴） |
| 本句它的宾语是什么 | estimate | `estimate savings for the whole street`（1399，卡词伙 `estimate savings` 与之同源）、同句并列的 `count jars emptied`（1399，数得清的才用 count） | 猜一个还没到手的量 | 它接的是 savings（整条街往后能省多少），而句中"数得清的"那半交给了 count——本书自己就分了"数得清 / 只能估" |
| 它算的东西到没到场 | compute | 卡 ex `He computes the total cost quickly.`（the total cost 是现成的账，动笔就有答案） | 有数可查，算的是结果 | 本书 compute 的 2 处宾语（costs / the total cost）全是"账面类"的东西，都落在钱上 |
| 它算的东西到没到场 | estimate | 卡 ex `Can you estimate how long it will take?`（how long it will take——还没发生，而且整句是个问句） | 没数可查，先给个准头 | estimate 的卡 ex 问的是将来的时间，本书那处估的是往后一整条街的省头：两处都不是"已经摆在那儿的数" |
| 卡上另一档用上了吗 | compute | 卡 `m` 两档「估算；计算」；本书 1398 译「计算」、卡 ex 译「算出」 | 想说"算出来"它最稳 | 「估算」这一档在 compute 一侧的两处译文里都没落成"估"字——它的名义项与实译之间差一档，表里因此不写"compute = 估" |
| 卡上另一档用上了吗 | estimate | 卡 `m` 给 v. 估算 + n. 估价；本书唯一那处（1399）走 v. 档；`note` 同义词栏 `approximate, calculate, gauge` | 名词那一档本书没用过 | 它的 n.「估价」档本书 0 处（已 grep 全书 estimate 词形只有 1399 一处）；同义词栏里还挂着本片的 gauge（归组 3）和 calculate（本句的邻居） |
| 中文落点 | 两个词 | compute：1398「计算」／卡 ex「算出」；estimate：1399「估算」／卡 ex「估算一下」（`sentZh`、`exZh`） | 汉译英时先问中文给的是"算出来"还是"猜个准头" | 本书译文把 1398 的 compute 和 calculate 一起并成「计算」，把 1399 的 estimate 单独立成「估算」——「估」这个字在段里只出现一次，跟着 estimate |

### 总结句（实测宽度 21.5 / ≤30）

> compute 算已经花掉的，estimate 猜还没省的。

### 出处清单

- `They would calculate drops saved and compute costs on paper after supper`、`compute costs`、`compute costs on paper after supper`、`on paper after supper`、`calculate drops saved`、译文「他们晚饭后在纸上计算省下的水滴和费用」 —— 课文第 1398 句（章4 段20 第 2 句）及 `sentZh`；`compute costs` 同见 compute 卡 `note` 词伙段（与 1398 **同源**）；`calculate drops` 见 calculate 卡词伙段（同句同源）。compute 侧全书唯一课文书证。
- `They could count jars emptied and estimate savings for the whole street`、`estimate savings`、`estimate savings for the whole street`、`count jars emptied`、`for the whole street`、译文「他们数清空的罐子，估算整条街的节水量」 —— 课文第 1399 句（章4 段20 第 3 句）及 `sentZh`；`estimate savings` 同见 estimate 卡 `note` 词伙段（与 1399 **同源**）。estimate 侧全书唯一课文书证。
- `A brass gauge helped pupils measure muddy water with steady hands daily` —— 课文第 1397 句（本组只作前一句背景，gauge 归组 3）。
- `He computes the total cost quickly.` / 他很快算出总成本。 —— compute 卡 `ex` / `exZh`（表里写作 `the total cost`、`computes`，均为其连续子串）。
- `Can you estimate how long it will take?` / 你能估算一下需要多长时间吗？ —— estimate 卡 `ex` / `exZh`（表里写作 `how long it will take`，为其连续子串）。
- 同义词：approximate, calculate, gauge ／词伙：estimate savings —— estimate 卡 `note`；compute 卡 `note` 只有词伙段 `compute costs`、**没有同义词栏**。两栏都没把对方列进去（compute 栏整个不存在，estimate 栏里没有 compute），worklist 标 `all_synonyms: false` 有据。
- `Honest daily feedback stopped them from ever underestimating slow steady development`（课文 498）／`They learned never to overestimate talent and treasured warm club fellowship deeply`（课文 499） —— 两句用的是 `underestimating` / `overestimate` 各自的卡（underestimate、overestimate 两词头在 `vocab.json` 里都有独立卡，已查），不算 estimate 的书证，只报备"estimate 家族在本书还有这两处"。
- 义项「估算；计算」「v. 估算；n. 估价」 —— 两卡 `m`；「工具量具齐备，学生测量计算，请老师评估改进」—— 段 20 `paraZh`（只取"测量计算"那层）；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡；「估算」这一档本书没给 compute（**要拍板**）

- **口径 6 处理（本组提请拍板）**：worklist 的「估算」来自两卡 `m` 的交集（compute 卡第一档就写"估算"），但本书 compute 的两处译文分别是「计算」（1398）与「算出」（卡 ex），**没有一处把 compute 译成"估"**；"估"字在段里只跟着 1399 的 estimate。所以标题与默认行只说"账 vs 还没省的"，「估算」留在 `sense` 槽，表里第 5 行已把这条写成"卡上档 vs 实译差一档"。
- **不许升级**：这不等于"compute 不能表估算"（它卡上第一档明明写着），只等于"本书没这么译过"。落地措辞请停在"本书这两处"。
- **同源合并（口径 8）**：两条词伙 `compute costs` / `estimate savings` 就是 1398 / 1399 那两句里的原话，两侧真实独立文本各只有 2 条（课文 + 卡 ex），是本片最薄的一组；但两句课文挨着、句式相同，是本组最强的一条对照证据。
- **与别组的交界**：1398 同句的 calculate、1399 同句的 count 都是本书目标词，但 worklist 把它们并进了另一组（worklist_idx 209 `calculate / compute / count`，属别的切片），按硬约束 2 本组不进成员，只在表里当"句中并列的那个动作"用；estimate 在组 5 还会再挂一次本段末，那张讲"钱与图纸"，本张讲"账与省头"（口径 9）。
- **计数结论、不是禁令**："compute 的宾语落在钱上"来自本书 2 处（costs / the total cost）都带 cost 这一事实；样本只有 2 条，落地别写成"compute 只能算钱"。
- **不写的**：「compute 偏计算机／精确运算，estimate 偏粗略」（两卡 `note` 无语域标注；`computer` 在本书只出现在别的卡的 `ex` 里、课文 0 处，与 compute 不同词，不引为证据）、`compute by hand`、`roughly computed`、`estimated cost`、`make an estimate`（四串两份数据各 0 处，已 grep）一律不列；estimate 的名词「估价」档只报备不造句（本组第 6 行）。
- **提请拍板点**：本组的共享义项「估算」只在 estimate 一侧有译文落点，compute 那两处走的是"计算/算出"。我建议按现表落地——两词的分工（已经存在的账 vs 还没发生的量）不依赖那个义项名，且证据是挨着的两句同句式课文。

---

## 组5 · estimate / evaluate —— 估价 ｜ `all_synonyms`: false ｜ 段 [4,20] ｜ 句 1399 / 1400

**共现位置**：1 段 2 句 —— 第 4 章段 20（全局 1396–1401），成员句 1399（estimate）与 1400（evaluate）**相邻两句**：前一句是学生在纸上往下猜的省头，后一句是老师带着微笑来看图纸。**主锚点 = 章4 段20**（`paraZh`：工具量具齐备，学生测量计算，请老师评估改进。`subheads[4][18]`：下篇·发明比赛）。卡挂段 20 末；组 2 / 组 3 / 组 4 同挂这里，组 4 讲的是"账 vs 还没省的"（也用了 1399），本组换一头：讲"猜数量的人 vs 评东西的人"，以及「估价」这一档在本书到底落在哪儿（口径 9）。
主锚点 = 章4 段20

**这一段在讲什么**（只按 1399、1400 两句说）：1399 他们 count jars emptied、estimate savings for the whole street；1400 Teachers came to assess the build、并 evaluate drawings with kind smiles。两句一头是学生、一头是老师；一个对着还没发生的量，一个对着已经画出来的图纸。段主题栏「请老师评估改进」对得上 1400 的"老师来评"（段主题栏，"改进"二字本段句子不含此事，不取）。

**该段原文（章4 段20，成员两句 + 后 1 句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1399 | They could **count** jars emptied and **estimate** savings for the whole street. | 他们数清空的罐子，估算整条街的节水量。 |
| 1400 | Teachers came to **assess** the build and **evaluate** drawings with kind smiles. | 老师们来评估作品，带着微笑评价图纸。 |
| 1401 | A **spare** **accessory** came free, with a clean **byproduct** for garden soil. | 有个备用的配件是白送的，还会留下一样干净的副产品，可以拌进花园的土里。 |

- estimate 全书带 `[[词头:…]]` 标记 1 处（1399）、未打标记 0 处（课文 `estimat` 词形另见 498、499 的 `underestimating` / `overestimate`，两词各有独立卡，不算它的书证）。
- evaluate 全书带标记 1 处（1400）、未打标记 0 处（按 evaluate/evaluated/evaluating/evaluation 扫全书 1833 句只这一句；`evaluation` 这个词形课文 0 处，只出现在 measure、procedure、rigorous、undergo 四张卡的 `note` 里）。

**证据强度**：两侧各 2 条独立文本（estimate = 课文 1 + 卡 ex 1；evaluate = 课文 1 + 卡 ex 1），词伙 `estimate savings`、`evaluate risks` 中前者与课文 1399 **同源**、后者本书课文未用到。两卡 `note` 都有词伙段与同义词栏：evaluate 的栏里写着 assess（assess 栏里也回列 evaluate，两卡互列），**但两卡都没把对方列进去** → worklist 标 `all_synonyms: false` 有据。

### 表头一句（title）

> 挨着的两句，一句是学生往下猜省多少（1399），一句是老师看图说好坏（1400）——worklist 给的共同义项是「估价」，可这一段两处都没在估价：1399 译「估算」、1400 译「评价」，本书唯一真译成「估价」的那句在 evaluate 卡上（店主那块旧手表）：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| estimate | /ˈestɪmeɪt/ | v. / n. | 估算；估价 | 先给个没发生的数 | 整条街往后能省的水；卡上问"要多久" | `estimate savings for the whole street`（课文 1399） |
| evaluate | /ɪˈvæljueɪt/ | v. | 估价；评价 | 有人来看值不值 | 老师们带着微笑看那叠图纸；卡上是店主看那块旧手表 | `evaluate drawings with kind smiles`（课文 1400） |

### 默认那一行（实测宽度 33.0 / ≤40）

```
estimate = 猜省多少（estimate savings），evaluate = 店主看那块旧手表
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段它接哪个宾语 | estimate | `estimate savings for the whole street`（1399）、同句前半 `count jars emptied`（数得出的才用 count） | 对一个还没到手的量 | 它的宾语 savings 是"往后才发生的事"，主语是学生自己 |
| 本段它接哪个宾语 | evaluate | `evaluate drawings with kind smiles`（1400）、同句并列的 `came to assess the build`（1400，评估作品那半归组 3） | 对一份已经做出来的东西下判断 | 它的宾语 drawings 已经画好摊在那儿，主语是外来的老师，还带着微笑——它是"被评"的那一场 |
| 卡 ex 让它碰什么 | estimate | `Can you estimate how long it will take?`（卡 ex；how long it will take 是时间，整句是问句） | 碰时间、数量 | estimate 在卡上被叫去猜"要多久"，压根没提钱 |
| 卡 ex 让它碰什么 | evaluate | `The shopkeeper will evaluate the old watch.`（卡 ex；店主 + 旧手表） | 碰一件旧东西值多少 | 本组唯一一处真正的"估价"书证就在这句：译文是「店主将估价这块旧手表」——钱、旧货、掌眼的行家 |
| 卡上词伙与近义圈 | estimate | 词伙 `estimate savings`（= 课文 1399，同源）；同义词栏 `approximate, calculate, gauge`（**没有 evaluate**） | 它站在"算"那一堆词里 | 它的近义圈全是算数的词（approximate / calculate / gauge），跟"评好坏"不沾 |
| 卡上词伙与近义圈 | evaluate | 词伙 `evaluate risks`（risk 卡词伙段有同一条，跨卡同串；本书课文未用到，已 grep）；同义词栏 `assess`（assess 栏亦回列 evaluate，两卡互列） | 它站在"评"那一堆词里 | 它的近义圈是 assess（同句 1400 的另一半），本组真正的近义关系在 assess↔evaluate，不在 estimate↔evaluate |
| 中文落点 | 两个词 | estimate：1399「估算」／卡 ex「估算一下」；evaluate：1400「评价」／卡 ex「估价」（`sentZh`、`exZh`） | 汉译英时先问中文是"猜个数"还是"说它好不好／值多少" | 本书课文 1833 句的译文里没有出现过「估价」这两个字（已 grep），它只在 evaluate 卡那句 `exZh` 上落地 |

### 总结句（实测宽度 21.0 / ≤30）

> estimate 猜省多少，evaluate 看图纸评好坏。

### 出处清单

- `They could count jars emptied and estimate savings for the whole street`、`estimate savings`、`estimate savings for the whole street`、`count jars emptied`、`for the whole street`、译文「他们数清空的罐子，估算整条街的节水量」 —— 课文第 1399 句（章4 段20 第 3 句）及 `sentZh`；`estimate savings` 同见 estimate 卡 `note` 词伙段（与 1399 **同源**，按口径 8 算 1 条）。estimate 侧全书唯一课文书证。
- `Teachers came to assess the build and evaluate drawings with kind smiles`、`evaluate drawings`、`evaluate drawings with kind smiles`、`with kind smiles`、`came to assess`、`the build`、译文「老师们来评估作品，带着微笑评价图纸」 —— 课文第 1400 句（章4 段20 第 4 句）及 `sentZh`。evaluate 侧全书唯一课文书证；同句的 assess 归组 3。
- `A spare accessory came free, with a clean byproduct for garden soil` —— 课文第 1401 句（本组只作后一句背景）。
- `Can you estimate how long it will take?` / 你能估算一下需要多长时间吗？ —— estimate 卡 `ex` / `exZh`（表里写作 `how long it will take`，为其连续子串）。
- `The shopkeeper will evaluate the old watch.` / 店主将估价这块旧手表。 —— evaluate 卡 `ex` / `exZh`（表里写作 `the old watch`、`will evaluate the old watch`，均为其连续子串）。这是本组唯一一处中文落成「估价」的文本。
- `evaluate risks` —— evaluate 卡 `note` 词伙段；risk 卡 `note` 词伙段同条（跨卡同串，算 1 条）。本书课文 `evaluate risks` 0 处（已 grep）。
- 同义词：approximate, calculate, gauge（estimate 卡 `note`，栏里没有 evaluate）／同义词：assess（evaluate 卡 `note`，栏里没有 estimate；assess 卡 `note` 栏 `calculate, measure, evaluate` 回列 evaluate，两卡互列） —— worklist 标 `all_synonyms: false` 有据。
- `Honest daily feedback stopped them from ever underestimating slow steady development`（课文 498）、`They learned never to overestimate talent and treasured warm club fellowship deeply`（课文 499） —— 报备：estimate 家族在本书另有这两处，用的是 underestimate / overestimate 两个独立词头（都有卡），不并入本组证据。
- 义项「v. 估算；n. 估价」「v. 估价；评价」 —— 两卡 `m`；「工具量具齐备，学生测量计算，请老师评估改进」—— 段 20 `paraZh`（只取"请老师评估"那层）；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，但「估价」这一档本段两侧都不在（**要拍板**）

- **口径 6 处理（本组提请拍板）**：worklist 的「估价」来自两卡 `m` 的交集，可共现两处走的都不是它：1399 的 estimate 是动词"估算"（数量），1400 的 evaluate 是"评价"（好坏）。本书译文里「估价」这两个字在课文 1833 句 0 处（已 grep），唯一落点是 evaluate 卡 `exZh`「店主将估价这块旧手表」。所以标题直接否定那个义项、默认行换成两侧各一个画面（estimate 侧取 1399 那个还没发生的省头，evaluate 侧取卡 ex 那位店主与那块旧表——这一处书证在卡上、不在本段），「估价」只待在 `sense` 槽。
- 还有一个**同族档位错**要写清：estimate 卡上「估价」是**名词档**（n. 估价），本书 estimate 唯一那处（1399）走动词档；evaluate 卡上「估价」是**动词档**，本书书证在卡 ex、不在本段。**两档都不是"本书没有这个词形"**，落地时别说成"estimate 不能指估价"，只说"本书没这样用过"。
- **同源合并（口径 8）**：`estimate savings` = 课文 1399（同源，不另计）；`evaluate risks` 与 risk 卡词伙同串（跨卡同串，算 1 条），本书课文未用。两侧独立文本各 2 条（课文 + 卡 ex），是本片最薄的一组之一，但两句课文相邻、一头学生一头老师，对照关系是实的。
- **与别组的交界**：1400 同句的 assess（归组 3）、1399 同句的 count 与 1398 的 calculate/compute（归组 4 与别切片的 worklist_idx 209）都不是本组成员；本组表里提 assess 只为说明 evaluate 的近义圈在哪。按硬约束 2 一律不进成员。
- **计数结论、不是禁令**："evaluate 的对象是已经做出来的东西"来自本书 2 处（drawings / the old watch），estimate 侧也是 2 处（savings / how long it will take），四条而已，别写成通用搭配规则。
- **不写的**：「evaluate 比 estimate 正式」（两卡 `note` 无语域标注，本书给不出证据）、estimate 的名词「估价」档造句（本书 0 处，只报备）、`evaluate the performance`、`self-assessment`、`market estimate`（三串两份数据各 0 处，已 grep）一律不列。
- **提请拍板点**：本组是这片唯一"共现两处都偏离共享义项"的组（组 3、组 4 至少一侧还在义项上）。我的判断：值得出卡，因为两词分工在段里极干净（猜还没发生的量 vs 评已经做好的东西），且 evaluate 侧那条"估价"书证（店主 + 旧手表）可在表里当第二档交代；若他要求辨析必须扣住义项，本组需他点头改判。

---

## 附：本切片复核索引（给门禁 / 抽查用）

| 组 | worklist_idx | words | senses | `all_synonyms` | paras | sents | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 136 | device / equipment | 设备 | false | [4,19] | 1394,1395 | 出卡：1394 的 device 译「装置」（义项在卡 ex「新设备」有落点，不必拍板）；硬区别 = 一台台数得清 vs 晚上共用一堆 + 词伙一行（电子那挂 vs 场所那挂） |
| 2 | 137 | instrument / tool | 工具 | false | [4,20] | 1396 | 出卡：同句各领一个修饰语（sharp / hand）；两卡 `ex` 同一句式只换词头（同一把 hammer，口径 14 已按现表出卡）；硬区别 = instrument 词伙带乐器、tool 词伙带线上；instrument 全书只 1 处（薄，不退回） |
| 3 | 138 | assess / gauge | 评估 | false | [4,20] | 1397,1400 | 出卡但**「评估」只在 assess 侧**（1397 的 gauge 是名词「量规」、gauge 的动词档与「规格」档本书均 0 处）；assess 卡**无词伙段**已明说；**要拍板** |
| 4 | 139 | compute / estimate | 估算 | false | [4,20] | 1398,1399 | 出卡但**「估算」本书没给 compute**（1398 译「计算」、卡 ex 译「算出」）；硬区别 = 已存在的账（costs / the total cost）vs 还没发生的量（savings / how long it will take）；**要拍板** |
| 5 | 140 | estimate / evaluate | 估价 | false | [4,20] | 1399,1400 | 出卡但**本段两侧都不在估价**（1399 估算、1400 评价；「估价」二字课文译文 0 处，只在 evaluate 卡 exZh）；硬区别 = 猜没发生的量 vs 评已做好的东西；**要拍板** |

- 全局句号口径按章偏移 `[0, 339, 651, 883, 1277, 1492]`（全书 1833 句）自算：章4 段19 起 1390（六句 → 1390–1395）、段20 起 1396（1396–1401）—— 5 组去重后 7 个句号（1394、1395、1396、1397、1398、1399、1400）与切片包各组 `sents` 逐个对得上，无一需要改用别的编号。
- 挂段位置：组 1 → 章4 段19 末；组 2、组 3、组 4、组 5 → 章4 段20 末（同段四张，切入角分别是"箱里那件 vs 手里那把""器具 vs 动作""账 vs 省头""猜数量 vs 评东西"，不重复，口径 9）。批次 4 的 4_09 组 2（domain / realm）挂段 19 末，本文件组 1 与它不撞角。
- 本文件 5 组的宽度标称值全部用 `tools/width_rule.py` 的同一把尺量出（汉字与全角标点各 1、其余 0.5）：默认行 35.5 / 35.5 / 30.0 / 38.0 / 33.0，总结句 21.5 / 23.0 / 22.0 / 21.5 / 21.0，无一条超上限（默认行 ≤40、总结句 ≤30）。标称值取的是落地器真正会取的那串（代码围栏里的本体与 `>` 后那句），已用 `tools/land_compare.py` 的同一套取值路径复量，只读、未跑任何写盘模式。
- 出现次数一律按 §五.13 分两档写（带标记 N 处 + 未打标记 M 处）：本片 9 个成员词里只有 tool 有未打标记书证（2 处：397、760），其余 8 词均"标 1 处、未标 0 处"；卡上词伙与课文同源的四条（`compute costs`、`estimate savings`、`brass gauge`、`calculate drops`）已在各组「数据边界」点名合并（口径 8）。
- 章3 坏主题栏与本片无关（本片全在章4）；段主题栏只在逐字引用并标明"本段句子不含此事"时才用（组 2 的「改进」、组 3 的「改进」、组 1 的「老海员／基金」三处已按此处理）。
- 本文件不引用任何在 `shadow/data/vocab.json`、`shadow/data/sections.json` 里查不到的英文串；门禁终值：`python3 tools/check_compare_draft.py work/辨析草稿/5_01.md` → `OK 5_01.md` / `门禁 1: PASSED（全部英文串可回溯）`（0 条查不到）。
- 落地成卡对象时：`title` = 各组「表头一句」，`summary` 的「简单记」= 各组「默认那一行」，`diff` 各行 `label` = 「维度」列，键名用成员词头。
