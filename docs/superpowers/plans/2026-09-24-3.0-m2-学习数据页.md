# 3.0 M2（学习数据页）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `/app/` 交出一屏「学习数据」一级页（`#/stats`）：有计划时按 §5.3–5.8 渲四问四块 + 单词掌握 + 随身听（M2 空态占位）+ 待加强；没计划时只给一句话 + 一个打开「我的」浮窗的按钮。

**Architecture:** 只动 3.0 外壳（`app/app.js` + `app/index.html`）。数据全部从既有 `ROOT2.state` / 引擎派生（§9.4），**不新增存储字段**；文章熟练度直接复用 M1 的 `APP3.articleStat(a)`（首页卡片与数据页共用同一个函数 = 同源）。`#/stats` 路由 M1 已存在（当时是占位屏），M2 把 `route()` 里 stats 分支接到 `APP3.renderStats`。`/`、`/shadow/`、`shadow/js/plan-engine.js` 一行不动。

**Tech Stack:** 零依赖原生 HTML/CSS/JS（无框架、无图表库）+ Playwright E2E。

**Spec:** `docs/superpowers/specs/2026-09-24-3.0-article-first-PRD.md`（§5 / §9.4 / §10.1 / §10.2 / §10.4 / §14.2）

## Global Constraints

- **允许改**：`app/**`、`playwright-tests/**`、`docs/**`。**不许进 diff**：`index.html`、`shadow/**`、`shadow/js/plan-engine.js`、`config.js`。
- **不新增存储字段**：数据页所有数字从 `TASK.state()` / `TASK.todayStats()` / `TASK.countStages()` / `TASK.planConfig()` / `APP3.articleStat()` 现算（PRD §9.4）。不写 `localStorage`。
- **不引图表库**：§5.6 趋势图手绘（CSS 柱 + 一条 SVG 折线）。
- **不新增浮层/页面**（§2.4 护栏）：入口只有侧栏「学习数据」一项；点文章小卡只是回首页 + 高亮，不新开屏。
- **随身听 M2 = 空态占位**，文案「随身听还没用过 → 去试试」，**不显示任何伪造指标**（§5.7 原话）。
- **未开始态**（§5.2，2026-09-24 用户改口径）：一句话 + 一个按钮 → 打开「我的」浮窗，**不内嵌计划表单**（计划三旋钮只活在「我的」里）。
- 视觉**继承**既有令牌（`--card` / `--border` / `--accent-soft` / `--accent-text` / `--grad` / `--note`…），**不新造色**；一级页面用主站首页薄荷配色 + 白底卡片；**深色全覆盖**（`body.dark`）。
- 无障碍（§10.4）：语义化 `h1`/`h2`、原生可聚焦按钮、触摸目标 ≥44px、趋势图给文本替代（`role="img"` + `aria-label`）。
- 每个任务收尾：`cd playwright-tests && npx playwright test journeys/app3/stats.spec.ts` 绿。收口任务再跑全量。
- 分支纪律：已在 `agent/opencode-3.0-m2-0924`，**不 push、不改历史**。

---

## File Structure

| 文件 | 职责 | 新建/改动 |
|------|------|----------|
| `app/app.js` | 学习数据页的渲染与派生（`renderStats` / `statsOverview` / `statsWords` / `statsTips` / `statsDays` / `renderStatsTrend` / `openHomeHighlight`）+ `route()` 接 stats 分支 + 事件代理 | 改 |
| `app/index.html` | 数据页 CSS（含深色覆盖、响应式）+ `renderHome` 的高亮钩子 | 改 |
| `playwright-tests/journeys/app3/stats.spec.ts` | M2 的 E2E 锁 | 新建 |
| `docs/superpowers/specs/2026-09-24-3.0-article-first-PRD.md` | §5.2 改口径 + §5.7 标注 M2 空态 | 改 |
| `scripts/build_site.mjs` | **不动**（M2 只改已有的 `app/index.html` + `app/app.js`，都已在白名单） | 不动 |

**为什么全部塞进 `app/app.js` 而不新建文件**：数据页是纯派生 + 渲染，没有独立子系统；`app/app.js` 现 393 行，M2 追加约 200 行仍在可读范围。新建文件会碰构建白名单，收益为零。

---

## Task 1: 路由接入 + 未开始态（§5.2）

**Files:**
- Modify: `app/app.js`（`route()` 加 stats 分支；新增 `renderStats` 的未开始分支）
- Modify: `app/index.html`（数据页 CSS：`.stats-page` / `.st-block` / `.st-empty-start`）
- Test: `playwright-tests/journeys/app3/stats.spec.ts`（新建，含夹具）

**Interfaces:**
- Consumes: `TASK.hasPlan`、`APP3.openMePop()`（M1 已有）、`dataReady`、`SECTIONS`
- Produces: `window.APP3.renderStats(view)`

- [ ] **Step 1: 写失败测试**（新建 `stats.spec.ts`）

```ts
// playwright-tests/journeys/app3/stats.spec.ts
// 3.0 M2：学习数据页（PRD §5）。服务器归 global-setup.ts 起停（仓库根 8932）；
// /app/ 在仓库根，所以和 shell.spec.ts / home.spec.ts 一样用 E2E_ROOT_URL，不走 baseURL。
import { test, expect } from '../../fixtures';

declare const TASK: {
  hasPlan: boolean;
  resetV2(): void;
  initPlan(minutes: number): void;
  readDone(i: number): void;
  seedDailyForTest(back: number): void;
  seedArticleForTest(a: number, o: { quizOk?: number; quizNo?: number; reps?: number; pass2?: boolean }): void;
  state(): {
    daily: Record<string, { sentDone?: number; quizDone?: number; minutes?: number }>;
    words: Record<string, unknown>;
    sents: Record<number, { lastReadAt: number }>;
  };
  todayStats(): { streak: number; graduated: number; targetWords: number };
  countStages(): { graduated: number; leech: number };
};
declare const ShadowPlan: {
  articleScope(sections: unknown, a: number): Set<number>;
  newWord(): Record<string, unknown>;
  dayKey(ts: number, b: number): string;
  DAY_MS: number;
};
declare const SECTIONS: unknown[];
declare const APP3: {
  articleStat(a: number): { progress: number; stage: string; ever: number; total: number };
  route(): void;
};

const rootUrl = process.env.E2E_ROOT_URL || '';

/* 6 篇小壳（与 home.spec.ts 同款）：第 0 篇 24 句、30 个不同目标词，其余 2 句。
   既让数据页拿到 6 篇，又不再多渲一页 1833 句 + 3242 词（并行时那份额外负载会压出 shadow 偶发红）。 */
const SIX: Record<string, string> = (() => {
  const mk = (title: string, n: number, extra: number) => ({
    title, zh: title, subheads: [''],
    paragraphs: [Array.from({ length: n }, (_, i) =>
      `Sentence ${i} about [[w${i}:w${i}]].${i < extra ? ` Plus [[x${i}:x${i}]].` : ''}`)],
    sentZh: [Array.from({ length: n }, (_, i) => `第 ${i} 句。`)],
    paraZh: [''],
  });
  const titles = ['地球与生命', '校园与文化', '衣食住行', '社会与规则', '历史与发明', '身体与时间'];
  return {
    'sections.json': JSON.stringify(titles.map((t, i) => mk(t, i === 0 ? 24 : 2, i === 0 ? 6 : 0))),
    'vocab.json': '{}',
    'chapters.json': '[]',
  };
})();

async function stubData(page: import('@playwright/test').Page, payloads: Record<string, string>) {
  for (const [name, body] of Object.entries(payloads)) {
    await page.route(`**/shadow/data/${name}*`, (r) =>
      r.fulfill({ status: 200, contentType: 'application/json', body }));
  }
}

test.describe('3.0 学习数据页（M2，PRD §5）', () => {
  test('未开始：一句话 + 一个按钮，点了打开「我的」浮窗，不内嵌计划表单', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => localStorage.removeItem('ielts.shadow.v2'));
    await page.reload();

    const start = page.locator('.st-empty-start');
    await expect(start).toBeVisible();
    await expect(start.locator('h1')).toHaveText('开始你的学习计划');
    // 「一句话」：有正文，不是光标题
    expect((await start.locator('p').innerText()).length, '未开始态要有一句说明').toBeGreaterThan(0);
    // 不内嵌计划表单：数据页里不得出现 renderSetup 的 .ps-start
    expect(await page.locator('#appView .ps-start').count(), '未开始态不得内嵌计划表单').toBe(0);

    // 按钮 → 打开「我的」浮窗（不是跳设置屏）
    await page.locator('.st-open-me').click();
    await expect(page.locator('#mePop')).toBeVisible();
    await expect(page.locator('#mePop [data-me-theme]'), '「我的」浮窗真的开了').toBeVisible();
    expect(await page.locator('#appView .ps-start').count(), '点按钮也不许内嵌表单').toBe(0);
  });
});
```

- [ ] **Step 2: 跑测试确认红**

Run: `cd playwright-tests && npx playwright test journeys/app3/stats.spec.ts`
Expected: FAIL —— `.st-empty-start` 不存在（当前 `#/stats` 是 `.app-todo` 占位屏）

- [ ] **Step 3: 实现**

在 `app/app.js` 顶部 `let cur = 'home';` 之后加高亮状态：

```js
  let _hlArticle = null;   // 数据页点文章小卡 → 回首页要高亮的那一篇（§5.4）
```

在 `route()` 里，`if (cur === 'home') { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }` 之后、`const meta = TODO_META[cur];` 之前插入：

```js
    if (cur === 'stats' && window.APP3 && window.APP3.renderStats) return window.APP3.renderStats(view);
    if (cur === 'stats') { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }
```

在 `window.APP3 = Object.assign(window.APP3, { renderHome, articleStat });` 之后追加数据页实现：

```js
  /* ---- 3.0 学习数据页（M2，PRD §5）----
     四问四块（每块标题即问题）+ 单词掌握 + 随身听（M2 空态占位，M4 接真数据）+ 待加强。
     所有数字从既有 ROOT2 state 派生（§9.4），不新增存储字段。 */
  function renderStats(view) {
    if (typeof dataReady === 'undefined' || !dataReady) { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }
    const hasPlan = !!(typeof TASK !== 'undefined' && TASK.hasPlan);
    if (!hasPlan) {
      // §5.2（2026-09-24 用户改口径）：一句话 + 一个按钮，点了打开「我的」浮窗；不内嵌计划表单。
      view.innerHTML = `<div class="st-empty-start">
        <h1>开始你的学习计划</h1>
        <p>学习数据会在你建立计划后出现在这里。每天读多久、几点换一天、新词开关都在「我的」里。</p>
        <button class="st-open-me" type="button">打开「我的」</button>
      </div>`;
      return;
    }
    view.innerHTML = '<div class="stats-page"><h1>学习数据</h1></div>';   // Task 2–6 逐步填块
  }
  window.APP3 = Object.assign(window.APP3, { renderStats });
  // 数据页的点击（按钮是每次重渲的，走事件代理，只绑一次）。Task 3 会把 .st-art/.st-go/.tip-go 并进来。
  document.addEventListener('click', (e) => {
    if (e.target.closest('.st-open-me')) { openMePop(); return; }
  });
```

> ⚠️ 还有一处**必须一起改**，否则点 `.st-open-me` 会「开了又被自己关掉」：`app/app.js` 底部那条
> 「点浮窗外面收掉」的监听会把 `.st-open-me` 当成「点外面」。在它的白名单里加 `.st-open-me`
> （与已有的 `#meCard` 并列）：
> ```js
> if (e.target.closest('#mePop') || e.target.closest('#meCard') || e.target.closest('.st-open-me')) return;
> ```
> 实施时先漏了这条，测试红在 `#mePop` 不可见 —— 单跑「打开浮窗」很容易看出来的那种红。

在 `app/index.html` 的 `<style>` 里（`.app-todo` 那段之后）加 CSS：

```css
  /* ============ 3.0 学习数据页（M2，PRD §5） ============ */
  .stats-page { max-width: 880px; }
  .stats-page > h1 { font-size: 22px; font-weight: 800; color: var(--text); margin: 2px 0 16px; }
  .st-block { margin-bottom: 22px; }
  .st-block > h2 { font-size: 15px; font-weight: 700; color: var(--text); margin: 0 0 10px; }
  .st-empty-start { max-width: 420px; margin: 12vh auto 0; text-align: center; padding: 0 20px; }
  .st-empty-start h1 { font-size: 22px; font-weight: 800; margin: 0 0 10px; color: var(--text); }
  .st-empty-start p { font-size: 13.5px; color: var(--muted); line-height: 1.7; margin: 0 0 18px; }
  .st-empty-start .st-open-me { width: 100%; min-height: 46px; border: none; border-radius: 999px;
    background: var(--grad); color: var(--cta-ink); font-size: 15px; font-weight: 700; cursor: pointer;
    box-shadow: var(--grad-shadow); }
```

- [ ] **Step 4: 跑测试确认绿** → PASS

Run: `cd playwright-tests && npx playwright test journeys/app3/stats.spec.ts`

- [ ] **Step 5: 提交**

```bash
git add app/app.js app/index.html playwright-tests/journeys/app3/stats.spec.ts
git commit -m "feat(app3): 学习数据页路由 + 未开始态（一句话 + 打开「我的」浮窗）"
```

---

## Task 2: 学习总览 5 个数（§5.3）

**Files:**
- Modify: `app/app.js`（`statsOverview` + `renderStats` 的 overview 块）
- Modify: `app/index.html`（`.st-nums` / `.st-num`）
- Test: `stats.spec.ts`（追加）

**Interfaces:**
- Produces: `window.APP3.statsOverview()` → `{days, minutes, streak, arts, acts}`

- [ ] **Step 1: 写失败测试**（追加到 `stats.spec.ts` 的 describe 内）

```ts
  test('进行中：学习总览 5 个数与 state 同源', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const s = ShadowPlan.articleScope(SECTIONS, 0);
      Array.from(s).slice(0, 5).forEach((i) => TASK.readDone(i));  // 今天读 5 句
      TASK.seedDailyForTest(1);                                   // 昨天：sentDone 5、minutes 15
      /* ⚠️ seedDailyForTest 必须放在 readDone 之后：readDone → push2 → recompute2 会从事件流
         重放 daily，把直接写进 state 的「昨天」抹掉（实施时踩到，测试因此红在 days<2 那条守卫）。 */
    });
    await page.reload();

    await expect(page.locator('.st-block[data-block="overview"]')).toBeVisible();
    const expected = await page.evaluate(() => {
      const st = TASK.state();
      const daily = st.daily || {};
      const keys = Object.keys(daily);
      let minutes = 0, acts = 0;
      keys.forEach((k) => { const d = daily[k] || {}; minutes += d.minutes || 0; acts += (d.sentDone || 0) + (d.quizDone || 0); });
      let arts = 0;
      for (let a = 0; a < SECTIONS.length; a++) { const s = APP3.articleStat(a).stage; if (s === 'done' || s === 'pro') arts++; }
      return { days: keys.length, minutes, streak: TASK.todayStats().streak, arts, acts };
    });
    const num = (k: string) => page.locator(`.st-num[data-k="${k}"] b`).innerText().then((t) => Number(t.replace('%', '')));
    expect(await num('days')).toBe(expected.days);
    expect(await num('minutes')).toBe(expected.minutes);
    expect(await num('streak')).toBe(expected.streak);
    expect(await num('arts')).toBe(expected.arts);
    expect(await num('acts')).toBe(expected.acts);
    expect(expected.days, '夹具要真造出日账，否则这条什么都没测').toBeGreaterThanOrEqual(2);
    expect(expected.acts, '夹具要真造出学习次数').toBeGreaterThan(0);
  });
```

- [ ] **Step 2: 跑测试确认红** → `.st-num` 不存在

- [ ] **Step 3: 实现**

`app/app.js` 追加：

```js
  function statsOverview() {
    const st = (typeof TASK !== 'undefined' && TASK.state) ? TASK.state() : null;
    const daily = (st && st.daily) || {};
    const keys = Object.keys(daily);
    let minutes = 0, acts = 0;
    keys.forEach((k) => { const d = daily[k] || {}; minutes += d.minutes || 0; acts += (d.sentDone || 0) + (d.quizDone || 0); });
    const ts = (typeof TASK !== 'undefined' && TASK.todayStats) ? TASK.todayStats() : { streak: 0 };
    let arts = 0;
    for (let a = 0; a < SECTIONS.length; a++) { const s = articleStat(a).stage; if (s === 'done' || s === 'pro') arts++; }
    return { days: keys.length, minutes: minutes, streak: ts.streak, arts: arts, acts: acts };
  }
  window.APP3 = Object.assign(window.APP3, { statsOverview });
```

把 `renderStats` 里那句占位 `view.innerHTML = '<div class="stats-page"><h1>学习数据</h1></div>';` 换成（先只放 overview 块）：

```js
    const ov = statsOverview();
    view.innerHTML = `<div class="stats-page">
      <h1>学习数据</h1>
      <section class="st-block" data-block="overview" aria-labelledby="stH1">
        <h2 id="stH1">我最近学得怎么样？</h2>
        <div class="st-nums" data-cols="5">
          <div class="st-num" data-k="days"><b>${ov.days}</b><span>累计学习天数</span></div>
          <div class="st-num" data-k="minutes"><b>${ov.minutes}</b><span>累计学习时长（分钟）</span></div>
          <div class="st-num" data-k="streak"><b>${ov.streak}</b><span>连续学习天数</span></div>
          <div class="st-num" data-k="arts"><b>${ov.arts}</b><span>完成文章数</span></div>
          <div class="st-num" data-k="acts"><b>${ov.acts}</b><span>总学习次数</span></div>
        </div>
      </section>
    </div>`;
```

CSS：

```css
  .st-nums { display: grid; gap: 10px; }
  .st-nums[data-cols="5"] { grid-template-columns: repeat(5, 1fr); }
  .st-nums[data-cols="4"] { grid-template-columns: repeat(4, 1fr); }
  .st-num { padding: 12px 10px; border: 1px solid var(--border); border-radius: 14px; background: var(--card);
    text-align: center; box-shadow: var(--shadow); }
  .st-num b { display: block; font-size: 20px; font-weight: 800; color: var(--accent-text); font-variant-numeric: tabular-nums; }
  .st-num span { font-size: 11px; color: var(--muted); }
  @media (max-width: 1023px) { .st-nums[data-cols="5"] { grid-template-columns: repeat(3, 1fr); } }
  @media (max-width: 700px) { .st-nums[data-cols="5"], .st-nums[data-cols="4"] { grid-template-columns: repeat(2, 1fr); } }
```

- [ ] **Step 4: 跑测试确认绿**
- [ ] **Step 5: 提交** `feat(app3): 学习数据页 · 学习总览 5 个数（与 state 同源）`

---

## Task 3: 文章学习 6 篇小卡 + 回首页高亮（§5.4）

**Files:**
- Modify: `app/app.js`（`renderStats` 的 articles 块 + `openHomeHighlight` + 事件代理）
- Modify: `app/index.html`（`renderHome` 高亮钩子 + `.st-art` / `.art-card.hl` CSS）
- Test: `stats.spec.ts`（追加）

**Interfaces:**
- Consumes: `APP3.articleStat(a)`（M1，同源）
- Produces: `window.APP3.openHomeHighlight(a)`

- [ ] **Step 1: 写失败测试**

```ts
  test('文章学习：6 篇小卡数字与首页卡片同源；点小卡 → 回首页并高亮', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const s = ShadowPlan.articleScope(SECTIONS, 0);
      Array.from(s).slice(0, 20).forEach((i) => TASK.readDone(i));
    });
    await page.reload();

    await expect(page.locator('.st-art')).toHaveCount(6);
    // 与 APP3.articleStat 同源（真断言，不写死字面量）
    const stats = await page.evaluate(() => Array.from({ length: SECTIONS.length }, (_, a) => APP3.articleStat(a).progress));
    for (let a = 0; a < 6; a++) {
      await expect(page.locator(`.st-art[data-a="${a}"] .sa-pct`), `第 ${a} 篇小卡百分比`).toHaveText(stats[a] + '%');
    }
    // 首页卡片同一个数：切到首页比对
    await page.goto(`${rootUrl}/app/index.html#/home`);
    await expect(page.locator('.art-card[data-a="0"] .a-pct')).toHaveText(stats[0] + '%');

    // 点第 0 篇小卡 → 回首页且该卡片被高亮
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.locator('.st-art[data-a="0"]').click();
    await expect(page).toHaveURL(/#\/home/);
    await expect(page.locator('.art-card[data-a="0"]')).toHaveClass(/hl/);
  });
```

- [ ] **Step 2: 跑测试确认红** → `.st-art` 不存在 / `.hl` 不存在

- [ ] **Step 3: 实现**

`app/app.js`：

1. `route()` 开头，`cur` 定下来之后加：`if (cur !== 'home') _hlArticle = null;`
2. `renderHome` 末尾（`if (window.APP3.renderBanner) …` 之后）加高亮钩子：

```js
    if (_hlArticle != null) {
      const card = view.querySelector('.art-card[data-a="' + _hlArticle + '"]');
      if (card) { card.classList.add('hl'); try { card.scrollIntoView({ block: 'center' }); } catch (e) {} }
    }
```

3. 追加 `openHomeHighlight` 与事件代理（放在数据页实现附近）：

```js
  function openHomeHighlight(a) {
    _hlArticle = a;
    if (location.hash === '#/home' || !location.hash) window.APP3.route();
    else location.hash = '#/home';
  }
  window.APP3 = Object.assign(window.APP3, { openHomeHighlight });
  // 数据页的点击（按钮是每次重渲的，走事件代理，只绑一次）
  document.addEventListener('click', (e) => {
    if (e.target.closest('.st-open-me')) { openMePop(); return; }
    const art = e.target.closest('.st-art');
    if (art && art.dataset.a != null) { openHomeHighlight(Number(art.dataset.a)); return; }
    const go = e.target.closest('.st-go, .tip-go');
    if (go) {
      const tip = go.closest('.st-tip');
      const kind = tip ? tip.dataset.tip : go.dataset.go;
      if (kind === 'leech' || kind === 'words') { location.hash = '#/words'; return; }
      if (kind === 'listen') { location.hash = '#/listen'; return; }
      const a = tip && tip.dataset.a != null ? Number(tip.dataset.a) : 0;
      openHomeHighlight(a);
    }
  });
```

4. `renderStats` 的 overview 块后追加 articles 块：

```js
      <section class="st-block" data-block="articles" aria-labelledby="stH2">
        <h2 id="stH2">我的文章掌握到了什么程度？</h2>
        <div class="st-arts">${SECTIONS.map((s, a) => {
          const x = articleStat(a);
          return `<button class="st-art" data-a="${a}" type="button" aria-label="《${esc(s.title)}》熟练度 ${x.progress}%，回首页看这张卡片">
            <span class="sa-head"><span class="sa-title">${esc(s.title)}</span><span class="sa-pct">${x.progress}%</span></span>
            <span class="sa-bar"><i style="width:${x.progress}%"></i></span>
            <span class="sa-meta">${STAGE_LABEL[x.stage] || '未开始'}</span>
          </button>`;
        }).join('')}</div>
      </section>
```

CSS：

```css
  .st-arts { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }
  .st-art { display: block; width: 100%; min-height: 44px; text-align: left; padding: 10px 12px;
    border: 1px solid var(--border); border-radius: 14px; background: var(--card); cursor: pointer;
    font: inherit; color: inherit; box-shadow: var(--shadow); }
  .st-art:hover { border-color: var(--accent-line); }
  .st-art .sa-head { display: flex; justify-content: space-between; align-items: center; gap: 8px; }
  .st-art .sa-title { font-size: 13.5px; font-weight: 700; }
  .st-art .sa-pct { font-size: 13px; font-weight: 800; color: var(--accent-text); }
  .st-art .sa-bar { display: block; height: 5px; margin: 7px 0 5px; border-radius: 3px; background: rgba(11,84,64,.14); overflow: hidden; }
  .st-art .sa-bar i { display: block; height: 100%; background: var(--grad); }
  .st-art .sa-meta { font-size: 10.5px; color: var(--muted); }
  .art-card.hl { outline: 2px solid var(--accent); outline-offset: 2px; }
  @media (max-width: 1023px) { .st-arts { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 700px) { .st-arts { grid-template-columns: 1fr; } }
```

- [ ] **Step 4: 跑测试确认绿**
- [ ] **Step 5: 提交** `feat(app3): 学习数据页 · 文章学习 6 小卡 + 点卡回首页高亮`

---

## Task 4: 单词掌握 4 个数（§5.5）

**Files:**
- Modify: `app/app.js`（`statsWords` + `renderStats` 的 words 块）
- Test: `stats.spec.ts`（追加）

**Interfaces:**
- Produces: `window.APP3.statsWords()` → `{learned, grad, leech, rate, total}`

- [ ] **Step 1: 写失败测试**

```ts
  test('单词掌握：4 个数与 countStages/state 同源', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const s = ShadowPlan.articleScope(SECTIONS, 0);
      Array.from(s).slice(0, 8).forEach((i) => TASK.readDone(i));
      TASK.seedArticleForTest(0, { quizOk: 2 });     // 造点词状态，别让 4 个数全 0
    });
    await page.reload();

    const expected = await page.evaluate(() => {
      const c = TASK.countStages();
      const st = TASK.state();
      const learned = Object.keys(st.words || {}).length;
      const total = TASK.todayStats().targetWords;
      return { learned, grad: c.graduated, leech: c.leech, rate: total ? Math.round((c.graduated / total) * 100) : 0 };
    });
    const num = (k: string) => page.locator(`.st-num[data-k="${k}"] b`).innerText().then((t) => Number(t.replace('%', '')));
    expect(await num('learned')).toBe(expected.learned);
    expect(await num('grad')).toBe(expected.grad);
    expect(await num('leech')).toBe(expected.leech);
    expect(await num('rate')).toBe(expected.rate);
    expect(expected.learned, '夹具要真造出词状态').toBeGreaterThan(0);
  });
```

- [ ] **Step 2: 跑测试确认红** → `.st-num[data-k="learned"]` 不存在

- [ ] **Step 3: 实现**

```js
  function statsWords() {
    const c = (typeof TASK !== 'undefined' && TASK.countStages) ? TASK.countStages() : { graduated: 0, leech: 0 };
    const st = (typeof TASK !== 'undefined' && TASK.state) ? TASK.state() : null;
    const learned = (st && st.words) ? Object.keys(st.words).length : 0;
    const total = (typeof TASK !== 'undefined' && TASK.todayStats) ? TASK.todayStats().targetWords : 0;
    return { learned: learned, grad: c.graduated, leech: c.leech, rate: total ? Math.round(c.graduated / total * 100) : 0, total: total };
  }
  window.APP3 = Object.assign(window.APP3, { statsWords });
```

`renderStats` 里 articles 块后追加：

```js
      <section class="st-block" data-block="words" aria-labelledby="stH3">
        <h2 id="stH3">我的单词掌握到了什么程度？</h2>
        <div class="st-nums" data-cols="4">
          <div class="st-num" data-k="learned"><b>${wd.learned}</b><span>已学习单词</span></div>
          <div class="st-num" data-k="grad"><b>${wd.grad}</b><span>已掌握（已毕业）</span></div>
          <div class="st-num" data-k="leech"><b>${wd.leech}</b><span>待巩固（重点词）</span></div>
          <div class="st-num" data-k="rate"><b>${wd.rate}%</b><span>掌握率</span></div>
        </div>
      </section>
```

并在 `renderStats` 里 `const ov = statsOverview();` 旁加 `const wd = statsWords();`。

- [ ] **Step 4: 跑测试确认绿**
- [ ] **Step 5: 提交** `feat(app3): 学习数据页 · 单词掌握 4 个数`

---

## Task 5: 学习趋势（14 天柱状 + SVG 折线，无图表库）（§5.6）

**Files:**
- Modify: `app/app.js`（`statsDays` + `renderStatsTrend` + `renderStats` 的 trend 块）
- Modify: `app/index.html`（`.tr-*` CSS）
- Test: `stats.spec.ts`（追加）

**Interfaces:**
- Produces: `window.APP3.statsDays()` → `Array<{key, sentDone, quizDone, minutes}>`（近 14 天，升序）

- [ ] **Step 1: 写失败测试**

```ts
  test('学习趋势：14 天柱状 + SVG 折线，有文本替代，无图表库', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const s = ShadowPlan.articleScope(SECTIONS, 0);
      Array.from(s).slice(0, 5).forEach((i) => TASK.readDone(i));
      TASK.seedDailyForTest(1); TASK.seedDailyForTest(2);   // 造出跨天日账（必须在 readDone 之后），柱/线才有形状
    });
    await page.reload();

    const wrap = page.locator('.tr-wrap');
    await expect(wrap).toBeVisible();
    expect(await page.locator('.tr-bar').count(), '近 14 天 = 14 根柱').toBe(14);
    expect(await page.locator('.tr-wrap svg.tr-line polyline').count(), '一条折线').toBe(1);
    // 文本替代：role=img + aria-label
    await expect(wrap).toHaveAttribute('role', 'img');
    expect(await wrap.getAttribute('aria-label')).toContain('近 14 天');
    // 无图表库（回归锁：有人引 Chart.js / ECharts 就红）
    expect(await page.evaluate(() => typeof (window as unknown as { Chart?: unknown }).Chart)).toBe('undefined');
    expect(await page.evaluate(() => typeof (window as unknown as { echarts?: unknown }).echarts)).toBe('undefined');
  });
```

- [ ] **Step 2: 跑测试确认红** → `.tr-wrap` 不存在

- [ ] **Step 3: 实现**

```js
  function statsDays() {
    const st = (typeof TASK !== 'undefined' && TASK.state) ? TASK.state() : null;
    const cfg = (typeof TASK !== 'undefined' && TASK.planConfig) ? TASK.planConfig() : null;
    const b = (cfg && Number.isInteger(cfg.boundary)) ? cfg.boundary : 4;
    const daily = (st && st.daily) || {};
    const out = [];
    for (let k = 13; k >= 0; k--) {
      const key = window.ShadowPlan.dayKey(Date.now() - k * window.ShadowPlan.DAY_MS, b);
      const d = daily[key] || {};
      out.push({ key: key, sentDone: d.sentDone || 0, quizDone: d.quizDone || 0, minutes: d.minutes || 0 });
    }
    return out;
  }
  function renderStatsTrend() {
    const days = statsDays();
    const maxAct = Math.max(1, days.reduce((m, d) => Math.max(m, d.sentDone + d.quizDone), 0));
    const maxMin = Math.max(1, days.reduce((m, d) => Math.max(m, d.minutes), 0));
    const bars = days.map((d) => {
      const v = d.sentDone + d.quizDone;
      const h = v ? Math.max(4, Math.round(v / maxAct * 100)) : 0;
      return `<span class="tr-bar${v ? ' has' : ''}" style="height:${h}%" data-day="${d.key}" title="${d.key}：${v} 次"></span>`;
    }).join('');
    const W = 280, H = 60, n = days.length;
    const pt = (i) => ({ x: Math.round(i / (n - 1) * W), y: Math.round(H - days[i].minutes / maxMin * H) });
    const pts = days.map((_, i) => { const p = pt(i); return p.x + ',' + p.y; }).join(' ');
    const a0 = pt(0), a1 = pt(n - 1);
    const label = '近 14 天学习趋势：' + days.map((d) => `${d.key.slice(5)} 学 ${d.sentDone + d.quizDone} 次、${d.minutes} 分钟`).join('；');
    return `<div class="tr-wrap" role="img" aria-label="${esc(label)}">
      <div class="tr-bars">${bars}</div>
      <svg class="tr-line" viewBox="0 0 ${W} ${H}" preserveAspectRatio="none" aria-hidden="true">
        <polyline points="${pts}" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/>
        <circle cx="${a0.x}" cy="${a0.y}" r="3" fill="currentColor"/>
        <circle cx="${a1.x}" cy="${a1.y}" r="3" fill="currentColor"/>
      </svg>
      <div class="tr-legend"><span>柱 = 每日学习次数</span><span>线 = 每日学习时长</span></div>
    </div>`;
  }
  window.APP3 = Object.assign(window.APP3, { statsDays });
```

`renderStats` 里 words 块后追加：

```js
      <section class="st-block" data-block="trend" aria-labelledby="stH4">
        <h2 id="stH4">我的学习是否持续？</h2>
        ${renderStatsTrend()}
      </section>
```

CSS：

```css
  .tr-wrap { padding: 12px; border: 1px solid var(--border); border-radius: 14px; background: var(--card); box-shadow: var(--shadow); }
  .tr-bars { display: flex; align-items: flex-end; gap: 3px; height: 72px; }
  .tr-bar { flex: 1; min-height: 2px; background: var(--accent-soft); border-radius: 2px 2px 0 0; }
  .tr-bar.has { background: var(--accent); }
  .tr-line { display: block; width: 100%; height: 44px; margin-top: 8px; color: var(--note); }
  .tr-legend { display: flex; justify-content: space-between; gap: 8px; margin-top: 6px; font-size: 10.5px; color: var(--muted); }
```

- [ ] **Step 4: 跑测试确认绿**
- [ ] **Step 5: 提交** `feat(app3): 学习数据页 · 学习趋势（14 天柱 + SVG 折线，无图表库）`

---

## Task 6: 随身听空态占位（§5.7）+ 待加强（§5.8）

**Files:**
- Modify: `app/app.js`（`statsTips` + `renderStats` 的 listen / focus 块）
- Modify: `app/index.html`（`.st-empty` / `.st-tips` / `.st-tip` / `.st-go` CSS）
- Test: `stats.spec.ts`（追加）

**Interfaces:**
- Produces: `window.APP3.statsTips()` → `Array<{kind, a?, text, go}>`（≤3 条）

- [ ] **Step 1: 写失败测试**

```ts
  test('随身听空态占位：文案正确且不含伪造数字', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => { TASK.resetV2(); TASK.initPlan(15); });
    await page.reload();
    const box = page.locator('.st-empty[data-empty="listen"]');
    await expect(box).toBeVisible();
    await expect(box.locator('p')).toHaveText('随身听还没用过 → 去试试');
    expect(await box.innerText(), '随身听 M2 不得显示伪造指标（§5.7）').not.toMatch(/\d/);
  });

  test('待加强：2–3 条建议，每条按钮落到正确入口', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const arr = Array.from(ShadowPlan.articleScope(SECTIONS, 0));
      arr.slice(0, 3).forEach((i) => TASK.readDone(i));      // 已开始但没读完 → continue
      const st = TASK.state();
      const old = Date.now() - 3 * 864e5;
      arr.slice(0, 3).forEach((i) => { st.sents[i].lastReadAt = old; });   // 3 天没学 → stale
      st.words['__leech__'] = Object.assign(ShadowPlan.newWord(), { stage: 'seen', reps: 1, leech: true, due: Date.now() - 1000 });  // 重点词到期 → leech
      APP3.route();
    });

    await expect(page.locator('.st-tip')).toHaveCount(3);
    await expect(page.locator('.st-tip[data-tip="continue"]')).toContainText('还差');
    await expect(page.locator('.st-tip[data-tip="leech"]')).toContainText('重点词');
    await expect(page.locator('.st-tip[data-tip="stale"]')).toContainText('天没学');

    // leech → 单词本
    await page.locator('.st-tip[data-tip="leech"] .tip-go').click();
    await expect(page).toHaveURL(/#\/words/);
    // continue → 回首页并高亮第 0 篇
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.locator('.st-tip[data-tip="continue"] .tip-go').click();
    await expect(page).toHaveURL(/#\/home/);
    await expect(page.locator('.art-card[data-a="0"]')).toHaveClass(/hl/);
    // stale → 回首页并高亮第 0 篇
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.locator('.st-tip[data-tip="stale"] .tip-go').click();
    await expect(page).toHaveURL(/#\/home/);
    await expect(page.locator('.art-card[data-a="0"]')).toHaveClass(/hl/);
  });
```

- [ ] **Step 2: 跑测试确认红** → `.st-empty[data-empty="listen"]` / `.st-tip` 不存在

- [ ] **Step 3: 实现**

```js
  function statsTips() {
    const tips = [];
    const now = Date.now();
    // A) 已开始但没读完的篇目 → 继续
    for (let a = 0; a < SECTIONS.length; a++) {
      const x = articleStat(a);
      if (x.ever > 0 && x.ever < x.total) {
        tips.push({ kind: 'continue', a: a, text: `《${esc(SECTIONS[a].title)}》还差 ${x.total - x.ever} 句读完`, go: '继续' });
        break;
      }
    }
    // B) 重点词今天到期 → 去复习（单词本筛选，M3 交付；M2 先落到 #/words）
    const st = (typeof TASK !== 'undefined' && TASK.state) ? TASK.state() : null;
    let leechDue = 0;
    if (st && st.words) Object.keys(st.words).forEach((k) => { const w = st.words[k]; if (w && w.leech && (w.due || 0) <= now) leechDue++; });
    if (leechDue > 0) tips.push({ kind: 'leech', text: `有 ${leechDue} 个重点词今天到期`, go: '去复习' });
    // C) 久没学的篇目 → 回去看看
    let stale = null;
    for (let a = 0; a < SECTIONS.length; a++) {
      const x = articleStat(a);
      if (x.lastAt > 0) {
        const d = Math.floor((now - x.lastAt) / 864e5);
        if (d >= 2 && (!stale || x.lastAt < stale.lastAt)) stale = { a: a, days: d, lastAt: x.lastAt };
      }
    }
    if (stale) tips.push({ kind: 'stale', a: stale.a, text: `已经 ${stale.days} 天没学《${esc(SECTIONS[stale.a].title)}》了`, go: '回去看看' });
    // 兜底：不足 2 条时补「没开始的篇目」/「单词本」，保证底部卡片总有 2–3 条可点建议（§5.8）
    if (tips.length < 2) {
      let firstNew = -1, nNew = 0;
      for (let a = 0; a < SECTIONS.length; a++) { if (articleStat(a).ever === 0) { nNew++; if (firstNew < 0) firstNew = a; } }
      if (nNew > 0) tips.push({ kind: 'start', a: firstNew, text: `还有 ${nNew} 篇没开始`, go: '去首页' });
      if (tips.length < 2) tips.push({ kind: 'words', text: '去单词本按文章复习单词', go: '看词本' });
    }
    return tips.slice(0, 3);
  }
  window.APP3 = Object.assign(window.APP3, { statsTips });
```

`renderStats` 里 trend 块后追加：

```js
      <section class="st-block" data-block="listen" aria-labelledby="stH5">
        <h2 id="stH5">随身听</h2>
        <div class="st-empty" data-empty="listen">
          <p>随身听还没用过 → 去试试</p>
          <button class="st-go" type="button" data-go="listen">去随身听</button>
        </div>
      </section>
      <section class="st-block" data-block="focus" aria-labelledby="stH6">
        <h2 id="stH6">哪些内容还需要加强？</h2>
        <ul class="st-tips">${tips.map((t) => `<li class="st-tip" data-tip="${t.kind}"${t.a != null ? ` data-a="${t.a}"` : ''}>
          <span class="tip-text">${t.text}</span>
          <button class="tip-go" type="button">${t.go}</button></li>`).join('')}</ul>
      </section>
```

并在 `renderStats` 里加 `const tips = statsTips();`。

CSS：

```css
  .st-empty { padding: 14px 16px; border: 1px dashed var(--border); border-radius: 14px; background: var(--card);
    display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; }
  .st-empty p { margin: 0; font-size: 13px; color: var(--muted); }
  .st-tips { list-style: none; display: flex; flex-direction: column; gap: 8px; }
  .st-tip { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 10px 14px;
    border: 1px solid var(--border); border-radius: 14px; background: var(--card); box-shadow: var(--shadow); }
  .st-tip .tip-text { font-size: 13px; color: var(--text); }
  .st-go, .st-tip .tip-go { flex: none; min-height: 44px; padding: 0 16px; border: none; border-radius: 999px;
    background: var(--grad); color: var(--cta-ink); font-size: 13px; font-weight: 700; cursor: pointer;
    box-shadow: var(--grad-shadow); }
```

- [ ] **Step 4: 跑测试确认绿**
- [ ] **Step 5: 提交** `feat(app3): 学习数据页 · 随身听空态占位 + 待加强（行动导向）`

---

## Task 7: 收口（深色 + 无障碍 + PRD §5.2 + 全量回归）

**Files:**
- Modify: `app/index.html`（数据页深色覆盖 + 响应式补全）
- Modify: `docs/superpowers/specs/2026-09-24-3.0-article-first-PRD.md`（§5.2 改口径；§5.7 标注 M2 空态）
- Test: `stats.spec.ts`（追加深色 + a11y）

**Interfaces:** 无新增（纯收口）

- [ ] **Step 1: 写失败测试**

```ts
  test('深色模式：数据页关键块不是浅色那套（PRD §10.1）', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const s = ShadowPlan.articleScope(SECTIONS, 0);
      Array.from(s).slice(0, 5).forEach((i) => TASK.readDone(i));
      TASK.seedDailyForTest(1);
    });
    await page.reload();
    await expect(page.locator('.st-num').first()).toBeVisible();

    const light = await page.locator('.st-num').first().evaluate((el) => getComputedStyle(el).backgroundColor);
    await page.evaluate(() => document.body.classList.add('dark'));
    const dark = await page.locator('.st-num').first().evaluate((el) => getComputedStyle(el).backgroundColor);
    expect(dark, '深色下卡片底色必须变').not.toBe(light);
    expect(dark, '深色下不能还是白底').not.toBe('rgb(255, 255, 255)');
    /* ⚠️ 上面两条其实在**没写任何深色覆盖**时也会绿：.st-num 用 var(--card)，body.dark 一改 token 就跟着变。
       真正会漏的是**写死的浅色值** —— .sa-bar 的 rgba(11,84,64,.14)。实施时正是这条才逼出深色覆盖。 */
    const track = await page.locator('.st-art .sa-bar').first().evaluate((el) => getComputedStyle(el).backgroundColor);
    expect(track, '深色下进度条轨道不能还是浅色那档 rgba(11,84,64,.14)').not.toBe('rgba(11, 84, 64, 0.14)');
  });

  test('无障碍：语义标题层级 + 触摸目标 ≥44px', async ({ page }) => {
    await stubData(page, SIX);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const s = ShadowPlan.articleScope(SECTIONS, 0);
      Array.from(s).slice(0, 5).forEach((i) => TASK.readDone(i));
    });
    await page.reload();
    await expect(page.locator('.stats-page > h1')).toHaveCount(1);
    await expect(page.locator('.st-block > h2')).toHaveCount(6);
    const h = await page.locator('.st-tip .tip-go').first().evaluate((el) => el.getBoundingClientRect().height);
    expect(h, '触摸目标 ≥44px').toBeGreaterThanOrEqual(44);
  });
```

- [ ] **Step 2: 跑测试确认红** → 深色那条应红（无 `body.dark` 覆盖时 `.st-num` 仍是 `var(--card)` = 白）；a11y 那条应已绿（Task 1–6 已用 h1/h2 与 44px 按钮）——**若 a11y 已绿属正常，深色必须先红**。

- [ ] **Step 3: 实现**

`app/index.html` CSS 追加深色覆盖：

```css
  body.dark .st-num, body.dark .st-art, body.dark .tr-wrap, body.dark .st-empty, body.dark .st-tip {
    background: var(--card); border-color: var(--border); }
  body.dark .st-art .sa-bar { background: var(--hl); }
  body.dark .tr-bar { background: rgba(255,255,255,.06); }
  body.dark .tr-bar.has { background: var(--accent); }
```

PRD §5.2 表格里「未开始」那行改成：

```markdown
| **未开始** | `ROOT2.plan` 为空 | 大标题「开始你的学习计划」+ 一句话 + 一个按钮（打开「我的」浮窗）→ 计划三旋钮收在「我的」里，**不内嵌计划表单**（2026-09-24 用户改口径，见 §8.1） |
```

PRD §5.7 末尾补一句：

```markdown
> **交付分期**：M2（2026-09-24）这一块先放**空态占位**（「随身听还没用过 → 去试试」），
> M4 随身听落地后再接真数据 —— 空态期间不显示任何伪造指标。
```

- [ ] **Step 4: 跑测试确认绿 + 全量回归**

Run: `cd playwright-tests && npx playwright test journeys/app3/stats.spec.ts` → PASS
Run: `cd playwright-tests && npm test` → 全绿（基线 215 + M2 新增）
Run: `python3 scripts/validate_data.py` → `Validation PASSED`
Run: `node scripts/build_site.mjs` → 成功
Run: `git diff --name-only` → 不得出现 `index.html`、`shadow/**`、`shadow/js/plan-engine.js`

- [ ] **Step 5: 提交** `feat(app3): 学习数据页收口（深色全覆盖 + 无障碍）+ PRD §5.2/§5.7 同步`

---

## Self-Review

**1. Spec coverage（M2 范围内的 PRD 条款）**

| PRD 条款 | 落在哪个 Task |
|---------|-------------|
| §5.1 四问四块（标题即问题） | T2–T6 |
| §5.2 未开始态（一句话 + 按钮开「我的」） | T1 |
| §5.3 学习总览 5 个数 | T2 |
| §5.4 文章学习 6 小卡 + 点卡回首页高亮 | T3 |
| §5.5 单词掌握 4 个数 | T4 |
| §5.6 学习趋势（14 天柱 + SVG 折线，无图表库） | T5 |
| §5.7 随身听（M2 空态占位，不伪造指标） | T6 |
| §5.8 待加强（2–3 条可点建议） | T6 |
| §9.4 全从 ROOT2 派生、不新增存储字段 | T2–T6（每个 `stats*` 都只读 state） |
| §10.1 令牌继承 / 深色 | T1–T7 |
| §10.2 断点 | T2/T3/T7 |
| §10.4 无障碍 | T5（文本替代）+ T7（标题/44px） |
| §2.4 不新增浮层/页面 | 全局约束（点小卡只回首页高亮） |
| §14.2 M2 那行 | 本 plan 整体 |

**2. Placeholder scan**：全文无 TBD/TODO；每个代码步骤给了可粘贴的代码与精确锚点。

**3. Type consistency**：
- `APP3.renderStats(view)` / `statsOverview()` / `statsWords()` / `statsDays()` / `statsTips()` / `openHomeHighlight(a)` —— T1–T6 逐处定义并使用，名字一致。
- 选择器：`.st-empty-start` / `.st-open-me` / `.st-num[data-k]` / `.st-art[data-a]` / `.tr-wrap` / `.tr-bar` / `.st-empty[data-empty="listen"]` / `.st-tip[data-tip]` —— 测试与实现一致。
- 事件代理里 `kind` 取值 `continue|start|stale|leech|words|listen` 与 `statsTips()` 产出、`data-go="listen"` 一致。

**4. 依赖顺序**：T1（路由 + 骨架）→ T2/T3/T4/T5 各自独立可验证 → T6（依赖 T3 的高亮 + T1 的事件代理）→ T7（收口，依赖全部）。每个任务只加自己那一块，不依赖后续任务。

**已知取舍（写进报告）**：
- §5.1 说「四块」，但 §5.3–5.8 实际列了 6 段（学习总览 / 文章学习 / 单词掌握 / 学习趋势 / 随身听 / 待加强）。本 plan 按 §5.3–5.8 渲 6 段，其中四段标题用 §5.1 的四个问题，单词掌握/随身听各给一个对应标题。
- 待加强的「去复习」在 M2 落到 `#/words`（单词本是 M3 才交付的占位页），URL 是「正确入口」，页面内容待 M3。
