# 修复记录 · 批次 5 `5_03.md` + `5_04.md`（依据 `work/辨析审核/audit_b_b5.md`）· 2026-09-21

执行人：修复代理（FIX_5_b）。本轮**只改了两份草稿** `work/辨析草稿/5_03.md`、`work/辨析草稿/5_04.md` 与本文件；
未碰任何数据 / 代码 / 别人的草稿与 `work/辨析审核/` 里的审核报告；**未跑 `tools/land_compare.py` 的任何模式（含 `--dry-run` / `--check`），
`work/辨析审核/land_preview.json` 时间戳未变**；无任何 git 写操作（只跑了 `git status` 只读）。
**单列声明**：报告「专题二」的 curse-swear slug 静默覆盖风险、以及 glance / glimpse 两卡被线上旧 look-verbs 表占位的载体冲突，
我按用户与主控的指示**一律没有触碰** —— 没改 `shadow/data/vocab.json`（也没改任何数据文件）、没把组 150 的标题成员顺序换成 `swear / curse` 去绕过 slug 检测、
没动组 148 / 150 的成员顺序，只改草稿文字；这两件事留给真人拍板、由主控代理写进给用户的报告。

改前对数据的自查（报告建议措辞不当事实源用）：1498 `sentZh` 逐字是「低矮的阳光有些刺眼，但利奥很快**瞥见**了公交车」、
1499 是「利奥飞快地**看了一眼**手表…」（按偏移 `[0,339,651,883,1277,1492]` 自算 [5,1] 段第 0、1 句）；
`glance.cmp` / `glimpse.cmp` / `peep.cmp` 三份逐字段 diff（唯一差异 = peep 多 `at:[5,1]`，glance / glimpse 连 `at` 键都没有）；
`vocab.json` 键序 glimpse 1636 < glance 1637 < peep 1638 < oath 1640、curse 1657 < swear 1658；
`swear.ex` = `He swore he didn't steal the money.`、`exZh` = 「他发誓他没有偷钱。」（swore 后接从句、句中无 to）；
`swear to` 在 `sections.json`（三种标记拆法）0 处；1509 逐字含 `curse the bad luck` / `swear at the heavy traffic`；
bet 卡 `m` = 「v. 打赌；n. 赌注」、gamble 卡 `m` = 「v. 赌博；冒险；n. 打赌」（两组新 core 均不复述这些字面，也不含"赌／冒险"）。

| # | 报告编号 | 组 | 改前 | 改后 | 改后宽度 | 过不过 |
|---|---|---|---|---|---|---|
| 1 | 组 148 · MISMATCH①（汇总表首行） | 148（`5_03.md` 组3，glance / glimpse） | 表头（将原样写成 `cmp.title`）：「挨着的两句，两个词前面挂的都是 `a quick`，**中文也都是"看一眼"**：1498 是阳光刺眼却**撞见**了公交，1499 是他自己低头**去看**手表——区别不在快慢，在谁先动手：」 | 「挨着的两句，两个词前面挂的都是 `a quick`，**可本书中文给它们的落点并不一样：1498 印「瞥见」、1499 印「看了一眼」**——一个是阳光刺眼时**撞见**了公交，一个是自己低头**去看**手表，区别不在快慢，在谁先动手：」——与 1498 / 1499 的 `sentZh` 逐字一致，也与本组「中文落点」行（瞥见 / 看了一眼）不再自相矛盾；未用「这一句／这句」（`validate_data.py` #16(b) 正则 `这一?句` 我扫过，10 张 title 全不触） | 表头不受宽度限；默认行 / 总结句一字未动，仍 37.0 / 17.5 | 过 |
| 2 | 组 148 · F 项"事实不全"（汇总表首行后半） | 148（`5_03.md` 组3） | 「数据边界」只写了 glance / glimpse 两份拷贝，没报第三张卡 | 新增一条：「**那份旧表的拷贝其实挂在三张卡上（补报）**：带 look-verbs 那份 `cmp` 的不只是两张，**peep 卡上也带着一份同表**……**三份拷贝唯一的差别就是 peep 那份额外带 `at` = [5, 1]**，glance 与 glimpse 那两份都没有 `at` 字段、其余内容三张全等」+ 该表实际取位路径（glimpse 键序最前且无 at → 退回"第一个 ≥2 个成员被标出的段"= 章5 段1；peep 的 [5,1] 指同一段）+「要不要给新表补 at 属卡数据改动，不在本草稿权限内，只报备」 | 未动任何带宽度的行 | 过 |
| 3 | 组 150 · MISMATCH①（数字错且不可解） | 150（`5_03.md` 组5，curse / swear） | 表头：「……主语一样、`will not` 一样，**差别全在中间那三个字母**：curse 后面直接跟"坏运气"，swear 要拐一个 `at` 才接到"堵车"……」 | 「……主语一样、`will not` 一样，**差别全在动词后面那个 at**：curse 后面直接跟"坏运气"，swear 要拐一个 `at` 才接到"堵车"……」（后半句照旧，1509 两处 `curse the bad luck` / `swear at the heavy traffic` 可 grep，成员顺序与组标题未动） | 表头不受宽度限；默认行 / 总结句未动，仍 35.0 / 21.5 | 过 |
| 4 | 组 150 · C 项第 8 条混合写法 | 150（`5_03.md` 组5） | 「数据边界」旧表回查行：「……卡 ex 走的又是 ``He swore + 从句``」（英文 + 中文语法词混排，不是书证串，下游会照抄） | 改成纯中文描述：「卡 ex 那句走的是另一条路：swore 后面直接跟他说出来的那件事（宾语从句，exZh 逐字印「他发誓他没有偷钱」），一个 to 都没有」——与 `swear.ex` / `exZh` 逐字对齐 | 未动带宽度的行 | 过 |
| 5 | 组 153 · E 项触线（硬约束 4） | 153（`5_04.md` 组3，bet / gamble） | gamble 那格 `core` = 「**押上去的冒险**」（「冒险」正是 gamble 卡 `m` 的字面档） | 「**押上去、输不起**」（报告给的两条候选之一，7 字 ≤12；不含 赌博／冒险／打赌 任一 m 字面，钩子落在 1532 `gamble with safety`） | `core` 无宽度门禁（≤12 字，实测 7 字）；默认行 34.0 / 总结句 16.5 未动 | 过 |
| 6 | 组 153 · E 项同批（bet 那格） | 153（`5_04.md` 组3） | bet 那格 `core` = 「**押明价码的赌**」（复用了 bet 卡 `m` 的「打赌／赌注」字头） | 「**押明码实价的钱**」（7 字 ≤12；具体词"钱"取自 1531 `bet money on the game`，与 gamble 那格成"明码 vs 输不起"的对子） | 同上 | 过 |

## 未改项及理由

- **专题二 curse-swear 覆盖 / 专题一 glance-glimpse 载体冲突**：用户已定由真人拍，我按指示没碰数据、没碰成员顺序（见开头单列声明）。报告给的"方案 (a) 先清旧表再落 / (b) 改顺序"两条我都没执行。
- **`5_03.md` 第 219 行出处清单**「两卡上是同一份内容的两份拷贝」、**文件头第 4 行小结**「两卡上已经带着一份 `cmp` 四词表」：报告点名的补报位置是「数据边界」，已在第 2 条改项写全"三张卡 + at 差异"；这两处对 glance / glimpse 两份本身并不假，按最小清单不动。
- **组 148 的 B 项"备注"**（导引句把组 4 的表格范围说成成员范围、1503 是 pledge）：报告自标"非落地字段、不扣"，未动。
- **组 151 两处建议／备注**（544「语言角」标取自段主题栏；"跟卡 `m` 的落点一致"措辞可再软半格）：报告标"建议／备注（不改不阻断）"，未动。
- **组 152「默认行实测 40.0 贴顶」**：本轮未给该行加一字（改的是组 153 的 `core` 格），实测仍 40.0。
- **组 153「我敢说」那一格**：报告判"不必拍板、照现表留"，未删未改。
- **组 155 介词行的建议补句**（"for／to 各只 1 处、是计数结论"）：报告标"建议（不扣级）"，未动。
- **判 OK 的 7 组（146 / 147 / 149 / 151 / 152 / 154 / 155）正文**：一字未动。

## 我对报告的一处复核意见（不影响照办）

报告专题一写「旧表现在确实就钉在章5 段1 末（我按 `cmpIndex()` 的逻辑跑了一遍：**peep 走 at 命中 [5,1]**；glance/glimpse 两份无 at，退回……）」。
我自己读了 `shadow/index.html:1724-1753` 并复算：同一个 `group` 只会被处理一次（`done` 去重、按 `Object.keys(VOCAB)` 顺序取第一份），
而词表键序里 **glimpse（1636）先于 glance（1637）先于 peep（1638）** → 实际生效的那一份是**无 at 的 glimpse 拷贝**，
走的是"第一个 ≥2 个成员被标出的段"回退路径；我按回退条件实扫全书，第一个满足的段确实也是 [5, 1]（1498–1500 连着标了 glimpse / glance / peep / peer）。
所以报告结论（挂段1 末）成立、成因不是 peep 那份 at —— 草稿里我按成因写，没照抄报告那句话。

## 终检

- 门禁 1（真跑，原样输出）：

```
$ python3 tools/check_compare_draft.py work/辨析草稿/5_03.md work/辨析草稿/5_04.md
OK   5_03.md
OK   5_04.md

门禁 1: PASSED（全部英文串可回溯）
```

- 范围内 10 条默认行 + 10 条总结句，标称 vs `tools/width_rule.width` 实测（含未改的组，逐条真量）：

| 组 | 默认行标称 / 实测 | 过 | 总结句标称 / 实测 | 过 |
|---|---|---|---|---|
| 146 individual / single | 38.0 / 38.0 | ≤40 ✓ | 14.0 / 14.0 | ≤30 ✓ |
| 147 committee / council | 35.0 / 35.0 | ✓ | 15.0 / 15.0 | ✓ |
| 148 glance / glimpse | 37.0 / 37.0 | ✓ | 17.5 / 17.5 | ✓ |
| 149 oath / vow | 34.0 / 34.0 | ✓ | 16.5 / 16.5 | ✓ |
| 150 curse / swear | 35.0 / 35.0 | ✓ | 21.5 / 21.5 | ✓ |
| 151 applaud / clap | 36.5 / 36.5 | ✓ | 18.5 / 18.5 | ✓ |
| 152 manipulate / steer | **40.0 / 40.0**（贴顶，本轮未加字） | ✓ | 20.5 / 20.5 | ✓ |
| 153 bet / gamble | 34.0 / 34.0 | ✓ | 16.5 / 16.5 | ✓ |
| 154 allure / tempt | 36.5 / 36.5 | ✓ | 21.5 / 21.5 | ✓ |
| 155 assign / distribute | 39.0 / 39.0 | ✓ | 19.0 / 19.0 | ✓ |

- 形状自检：20 个小标题仍是 `### 默认那一行（实测宽度 X / ≤40）` + ``` 围栏、`### 总结句（实测宽度 X / ≤30）` + 紧跟一行的机检形状；
  10 个组标题的编号与成员顺序、`主锚点 = 章N 段M` 字段名一字未动。
- 未跑 `validate_data.py`（本轮无数据改动，第 14 / 15 条不涉及）；未跑 `tools/land_compare.py` 任何模式。
