# 修复记录 · 批次 6 `6_07.md` + `6_08.md` + `6_09.md`（依据 `work/辨析审核/audit_d_b6.md`）· 2026-09-21

执行人：修复代理（FIX_6_d，作者侧）。**只碰了三份草稿 `work/辨析草稿/6_07.md`、`6_08.md`、`6_09.md` 与本文件。**

- **未写任何数据**：`shadow/data/vocab.json`、`shadow/data/sections.json`、`shadow/index.html` mtime 与报告「边界自证」所记基线一字不差（见本文末「边界自证」）。**没有为了消重去动已落地的旧表 `decrease-reduce`／`amplify-augment`／`alleviate-relieve`** —— 撞钩子的三条全部改在我这几份新草稿里。
- **未碰** `scripts/*`、`tools/*`、`index.html`、别人的草稿（`6_01..6_06`、`5_*.md`、`4_*.md`）、任何 `audit_*.md`（本报告只读 `audit_d_b6.md`）。
- **没有跑 `tools/land_compare.py` 任何模式**（`--dry-run`／`--check` 也没跑；`work/辨析审核/land_preview.json` 未被本代理改动）。草稿自检只跑了 `tools/check_compare_draft.py` 与 `tools/width_rule.py`（都是只读）。
- **无任何 git 写操作**：只跑过 `git status --short` 与 `git diff`（读）。
- 报告范围 MISMATCH 3（BLOCK 0）＝组 213 / 215 / 218，落成 **22 处编辑**（6_08 12 处、6_09 10 处，含 1 处新增条目）；**`6_07.md` 一字未动**（报告判组 208–212 全通过，最小清单没有一条落在它身上），其 mtime 仍是报告基线的 Sep 21 09:13:25。
- 内容骨架未动：12 组的组标题编号与成员顺序原样（决定 slug）、12 处 `主锚点 = 章N 段M` 字段全在（脚本数到 5＋5＋2）、items/diff/原文表列数与表头逐张相等（含 217／218／219 三个四成员组）。载体分配（compute/amplify/alleviate/postulate/decrease 五张旧表、12 个新 slug 与线上无一同名）未改一字。

## 改动清单

| # | 报告编号 | 组 | 改前 | 改后 | 改后宽度 | 过不过 |
|---|---|---|---|---|---|---|
| 1 | 组213·F MISMATCH（必改清单①） | 213（`6_08` 组1）默认行 | `amplify = 话筒（soft voices），enlarge = 家教的小步子，magnify = 显微镜下的细节` | `amplify = 扬声器（the sound），enlarge = 家教的小步子，magnify = 显微镜下的细节` —— 钩子两个成分都取自**同一处书证** amplify 卡 `ex`（`The speaker will amplify the sound…`）＋ `exZh`（「扬声器会放大声音，让每个人都能听到。」），不是拼的；括号里不再用课文 1578 的 `soft voices` | 39.5（≤40；小标题标称值不变） | 过 |
| 2 | 组213·自报不实（必改清单①后半） | 213 共现位置段 | 「……**不回去复述那张表的两条结论**（口径 9）。」 | 「……本组换一条线——三个词各接哪样东西、句子里谁在动手（口径 9）。**amplify 接的是声音这一条是两表共享的**，因为本书给它的书证就那两处（课文 1578 与卡 `ex`），换不掉；旧表把这条写进了它的默认行，本组不复述它"领声音／领加料"的那组对照，默认行也不用它那句话里的钩子……」 | —（正文，不落地） | 过 |
| 3 | 组213·自报不实（必改清单①后半） | 213 数据边界第一条 | 「**本组内容不引用那张表的结论**，只把 augment 挡在成员外（硬约束 2）。」 | 「本组只把 augment 挡在成员外（硬约束 2）。**与那张表的分工要说准（不是"完全不引用"）**：『它接声音』这一条两表必然共享……diff 里凡是它已经用过的那种措辞（音量那一格、声音类宾语那一格、"靠什么干成"那一格）本组一律换成自己的说法，或改成直引数据原文。」 | — | 过 |
| 4 | 组213·连带引用（默认行的来源） | 213 出处清单 | 「（表里写作 `amplify the sound`，为其连续子串；**默认行括号里的 `soft voices` 出自课文 1578**，不在这句里）」 | 「（……**默认行括号里的 `the sound` 与"扬声器"这两个钩子都取自这一对 `ex`／`exZh`**，同一处书证、不是拼的；课文 1578 里的 `soft voices` 不在这句里，本组默认行也不再用它）」 | — | 过 |
| 5 | 组213·自查（旧表逐字串，同族改字） | 213 diff「接住哪样东西｜amplify」 | 用在哪「**听得到的音量**」（＝旧表 `diff[2]/amplify` 原字）；区别「本书给它 2 处，**宾语全是声音类的东西**，一个例外都没有」（＝旧表 `diff[0]/amplify` 原字，报告点过名） | 用在哪「说"声音够不够大"那一头」；区别「本书给它 2 处，接住的都是**听得见的那一类**，一个例外都没有」 | — | 过 |
| 6 | 组213·自查（同上） | 213 diff「谁在动手｜amplify」 | 用在哪「**靠设备**」；区别「本书 2 处**主语都是机器**（话筒 / 扬声器）」（旧表为「本书两处的主语都是机器」「靠设备（话筒 / 扬声器）」） | 用在哪「靠一台会发声的东西」；区别「本书给它那 2 处，句子开头站着的都是发声器本身（`Microphones` / `The speaker`）；另两词在课文里的主语一位是家教、一批是图片」 | — | 过 |
| 7 | 组213·自查（同上） | 213 items「amplify」`scene` | 「几只**话筒把轻声**送出去（1578）；**扬声器让每个人都听到声音**（卡 ex）」（后 10 字＝旧表 `items[0]/scene` 原字） | 「几只话筒接住的是**轻柔的声音**（1578）；卡 `ex` 那台扬声器，`exZh` 写作「**让每个人都能听到**」；卡上词伙另挂"痛苦"那一头（课文未用到）」——改成直引数据原文 | — | 过 |
| 8 | 组213·自查（同上） | 213「这一段在讲什么」1577 分句 | 「1577 图片能**给枯燥课文加料**、把小细节送到疲惫的双眼前」（7 字＝旧表 `items[1]/scene` 的原话，且 augment 非本组成员） | 「1577 图片能给枯燥的课文**添一层可读性**、把小细节送到疲惫的双眼前」（贴 `sentZh`「图片能增加枯燥课文的可读性」） | — | 过 |
| 9 | 组213·附录同步 | 213 附录复核索引 组1 行 | 结尾只到「落地需换载体到 enlarge 或 magnify」 | 追加「；本组默认行 amplify 一侧的钩子取自卡 `ex`／`exZh` 那台扬声器（`the sound`），不重用它默认行那半句，"amplify 接声音"这条两表共享已在组内「共现位置」「数据边界」写明」 | — | 过 |
| 10 | 组213·附录同步 | `6_08` 附录「挂段位置」行 | 「……本切片各换了切入角，**未复述其结论**。」 | 追加「**段14 那一处要说准**：本书给 amplify 的书证就是课文 1578 与卡 `ex` 两处，"它接声音"这条两表共享、装看不见没意义；本组换的是切入角与措辞，默认行 amplify 一侧另起钩子……不复用旧表默认行那半句，diff 里也不用它原来的措辞」 | — | 过 |
| 11 | 组215·F MISMATCH（必改清单②） | 215（`6_08` 组3）计数行 | 「**三张卡的词伙里只有 alleviate 那两条本书课文没用过**；`sweet relief`（relief 卡词伙）与课文 1702 同源……」 | 「三张卡 `note` 里的词伙一共 **3 条**（alleviate 2 条 ＋ ease 1 条 `ease tensions`），**本书课文一条都没用过**（逐条回 `sections.json` 扫过：`boredom` 全书 0 处、`health problems` 0 处、tension 只有 1327 一处且那句没有 ease）；**relieve 卡的 `note` 整条为空，连词伙段都没有**，它一侧的词伙只能靠 relief 那张卡：`sweet relief`（relief 卡词伙）与课文 1702 同源……」 | —（不涉默认行／总结句） | 过（与数据一致） |
| 12 | 组215·连带引用（§五.15 清单） | `6_08` 附录「存在性断言」行 | 结尾只到「ease 未打标记 3 处 = 752／1757／1764，带标记 1 处 = 1704。」 | 追加「；组3 三张卡的 **3 条词伙**（`alleviate boredom`／`alleviate health problems`／`ease tensions`）课文 0 处——`boredom` 与 `health problems` 两个词在课文里各 0 处（`boredom` 只出现在 alleviate 卡与 reduce 卡的词伙栏），`tension` 词形课文只 1327 一处（1662 那个 `hypertension` 是别的词），relieve 卡 `note` 整条为空」 | — | 过 |
| 13 | 组218·F MISMATCH（必改清单③前半） | 218（`6_09` 组1）默认行 | `decrease = 那截数，diminish = 噪音变弱，dwindle = 硬币一天天少，reduce = 他动手` —— 末段与旧表「简单记」**逐字相同**，首段与旧表「落下去那截」同形 | `decrease = 生意那笔，diminish = 噪音变弱，dwindle = 硬币天天少，reduce = 压浪费` —— 四格全落在本组新轴（掉的是哪类东西／带不带过程）；`生意`←1163 `sentZh`「生意的下滑」、`噪音`←diminish 卡 `exZh`「午夜后噪音会减少」、`硬币天天少`←1162「存下的硬币一天天变少」、`浪费`←1165「他努力减少后屋的浪费」 | 39.5（≤40；小标题标称值不变。四成员名吃掉 20.5 当量＋三个全角逗号 3.0 → 钩子总预算只有 16 个汉字，报告举的例子 42.5 装不下，见 #17） | 过 |
| 14 | 组218·F MISMATCH（必改清单③前半） | 218 总结句 | `decrease 记那个数，reduce 记那双手，其余两个记怎么没的。`（「那双手」＝旧表辨析句同义复述） | `decrease 记的是生意，reduce 记的是浪费，另两个一实一虚。`（「一实一虚」＝本组 diff 两格：dwindle 带实物硬币／塘水、diminish 接摸不着的客流／噪音／希望） | 28.0（≤30；标称不变） | 过 |
| 15 | 组218·自报不实（必改清单③后半） | 218 共现位置 blockquote | 「……旧表四格讲完了 —— 本组不复述这条线……」 | 追加「**共享哪几条要说准**（本组不自称"完全不引用"）：① decrease / reduce 两个成员在本组照样各占一格，而本书给它们的书证就是旧表用的那两句（1163 / 1165），换不掉；② "reduce 那一句里有人在动手"这件事两表都会看到——本组只是不再拿它当区别线。本组换的是**看哪一格**与**用哪套措辞**……」 | — | 过 |
| 16 | 组218·自报不实（连带） | 218 数据边界「同段已有三张表」条 | 「……decrease / reduce 两员在本表里**只以新角出现**。」 | 逐个点名换到什么：默认行四钩子、总结句、`core`（能记账的那笔／他要压下去的浪费）、`scene`（他要撑的那笔生意）都不重字；并就地注明表内**唯一一处沿用**旧表结论（reduce 不站在 the / a 后面当那个数，见「计数结论」条） | — | 过 |
| 17 | 组218·新增条目（宽度账） | 218 数据边界（新增一条） | 无 | 「**默认行的余量本来就窄（不是本组写长了）**：四个成员名吃掉 14.5 当量、四个 ` = ` 记 6.0（合计 20.5，与本文件附录"14.5"那句同口径）＋三个全角逗号 3.0 ＝ 23.5，上限 40 只给四个钩子留 16.5（整数 16 字）……把四侧都写成 4–5 字（`生意那笔`＋`噪音变弱`＋`硬币一天天少`＋`后屋的浪费`）实测 **42.5**，超线，故 dwindle 收成 `硬币天天少`、reduce 收成 `压浪费`」 | 42.5（反例，未采用）／采用版 39.5 | 过 |
| 18 | 组218·自查（旧表逐字串，同族改字） | 218 items「decrease」 | `core`「**账上落下来的那截**」（旧表 `core` = 「落下来的那一截」）、`scene` 首格「**生意那一段往下走**（1163）」（旧表 `items[0]/scene` 原字 8 字） | `core`「能记账的那笔」（贴本组 diff「落在生意、降雨这类能记账的东西上」）、`scene`「他要撑的那笔生意（1163）」（`撑` ← `sentZh`「为了撑过生意的下滑」） | core 6 字（≤12） | 过 |
| 19 | 组218·自查（同上） | 218 items「reduce」 | `core`「**他伸手压下去的那样东西**」（与旧表 `core`「他动手把那样东西弄小」同一句话的换词写法） | `core`「他要压下去的浪费」（换到"哪类东西"这一格，与默认行 `压浪费` 同轴） | core 8 字（≤12） | 过 |
| 20 | 组218·自查（同上） | 218 表头一句 | 「1163 是**账上落下来的那一截**（它还是个名词）」（7 字＝旧表 `title`／`core` 原字） | 「1163 是**账上记下来的那笔生意**（它还是个名词）」 | —（表头不设宽度上限） | 过 |
| 21 | 组218·自查（同上） | 218 diff「掉下去的是哪类东西｜reduce」 | 「它是本组唯一一条**动词后面直接拖宾语**的」（＝旧表 `diff[2]/reduce`「后面直接拖东西」） | 「它是本组唯一一条**宾语直接挂在动词后头**的」 | — | 过 |
| 22 | 组218·自查（同上）＋附录 | 218 出处清单／附录复核索引 | 「它是 decrease 卡上**唯一的动词框架**」（7 字＝旧表 `diff[4]/decrease` 原话）；附录组1 行结尾只到「本组须落 diminish / dwindle / reduce」 | 「它卡上只有这一条是"动词 + 宾语"形状的写法」；附录补「**与同段旧表的字面分工**：默认行四钩子、总结句、`core`、`scene` 已全部换到本组两格，不重用它默认行与 `core` 里的说法；共享的只有两句书证（1163 / 1165）与 reduce 不站在 the / a 后那一格（组内已注明沿用）」 | — | 过 |

## 与旧表逐字不重的证据

旧表原文（`shadow/data/vocab.json`，本代理只读未改，逐字打印）：

- `decrease` 卡 `cmp`（`group: decrease-reduce`、`at: [3,47]`）`summary` 全文：
  > `辨析：decrease 说落了多少，reduce 说他动了手。 简单记：decrease = 落下去那截（decrease in business），reduce = 他动手（reduce waste）`
  「简单记」那半句 = **`decrease = 落下去那截（decrease in business），reduce = 他动手（reduce waste）`**；`items[0].core` = **`落下来的那一截`**；`items[1].core` = **`他动手把那样东西弄小`**；`items[0].scene` 首格 = **`生意那一段往下走（1163）`**；`title` 含「说的是**生意落下来的那一截**」；`diff[4]/decrease` 含「卡词伙 decrease life satisfaction 是它**唯一的动词框架**」。
- `amplify` 卡 `cmp`（`group: amplify-augment`、`at: [5,14]`）`summary` 全文：
  > `辨析：amplify 把小的变大，augment 往不够的加码。 简单记：amplify = 话筒（soft voices），augment = 课文加料（augment dull texts）`
  「简单记」那半句 = **`amplify = 话筒（soft voices），augment = 课文加料（augment dull texts）`**；`diff[0]/amplify` = 「本书 amplify 的 2 处**宾语全是声音类的东西**——同一份内容，只是更大声」；`diff[2]/amplify`（用在哪）= **`听得到的音量`**；`diff[3]/amplify` = 「本书两处的**主语都是机器**」；`diff[5]/amplify`（用在哪）= **`靠设备（话筒 / 扬声器）`**；`items[0].core` = **`把音量拧大的那台机器`**；`items[0].scene` = 「话筒放大轻柔的说话声；**扬声器让每个人都听到**（卡 ex）」。
- `alleviate` 卡 `cmp`（`at: [5,35]`）「简单记」= `alleviate = 泡走腿上的疼（leg ache），relieve = 唱走心里的事（her worry）` —— 本组默认行（未改）为 `alleviate = 暖浴在动手，relieve = 几首歌在动手，ease = 不用谁动手（had eased）`，报告判它「默认行已换"动手"新轴，段末不会重复」，本轮只按必改清单动了计数行。

**逐字对照实跑结果**（`python3` 现场跑，取旧表「简单记」那半句与新默认行做最长公共子串 ＋ 公共中文词表）：

| 段末并排的两张表 | 旧表「简单记」 | 新草稿默认行 | 最长公共子串 | 公共中文词 |
|---|---|---|---|---|
| [5,14] `amplify-augment` ↔ 本组新表 | `amplify = 话筒（soft voices），augment = 课文加料（augment dull texts）` | `amplify = 扬声器（the sound），enlarge = 家教的小步子，magnify = 显微镜下的细节` | `'amplify = '`（成员名＋等号，删不掉） | **`[]`（0 个）** |
| [3,47] `decrease-reduce` ↔ 本组新表 | `decrease = 落下去那截（decrease in business），reduce = 他动手（reduce waste）` | `decrease = 生意那笔，diminish = 噪音变弱，dwindle = 硬币天天少，reduce = 压浪费` | `'decrease = '` | **`[]`（0 个）** |
| [5,35] `alleviate-relieve` ↔ 本组新表（未改） | `alleviate = 泡走腿上的疼（leg ache），relieve = 唱走心里的事（her worry）` | `alleviate = 暖浴在动手，relieve = 几首歌在动手，ease = 不用谁动手（had eased）` | `'alleviate = '` | **`[]`（0 个）** |

**全字段扫描**（把旧表 `cmp` 的 title/items/diff/summary 摊平，取所有 ≥5 字连续中文串，回扫新草稿该组全文）改后再跑一遍的结果：

- 6_08 组1 ↔ `amplify-augment`：剩下的 ≥5 字命中只有 ①`让每个人都`／`放大轻柔的`（**直引卡 `exZh`、课文 `sentZh` 原文**，属 §五.16 允许的原样引用）；②`的共同义项`／`课文未用到`／`档本书没有`／`有同义词栏`／`没有词伙段`（术语脚手架）；③`靠什么干成`（出现在本组「数据边界」里，是**指名旧表那一格**的引用性说法）。**作者原创的画面句与结论句 0 命中**（改前有 6 条：`话筒把轻声`／`听得到的音量`／`宾语全是声音类的东西`／`主语都是机器`／`扬声器让每个人都听到声音`／`给枯燥课文加料`，见 #5–#8）。
- 6_09 组1 ↔ `decrease-reduce`：剩下的 ≥5 字命中只有 ①`他努力减少后屋的浪费`／`为了撑过生意的下滑`／`上个月降雨量`／`后屋的浪费`／`生意的下滑`（**直引 `sentZh`／卡 `exZh`**）；②`句子里有没有人动手`（本组 blockquote 里**点名旧表四格**的引用）；③`同义词栏里互列`（§五.14 的判定式原话）；④`后面当那个数`／`本书没有一处`（「计数结论」条，就地标了"旧表已声明过这条，本表沿用不另证"）。**改前的 6 处原创重复** `那截数`／`他动手`／`落下来的那一截`／`生意那一段往下走`／`唯一的动词框架`／`动词后面直接拖宾语` 已全部改字（#13／#18／#20／#21／#22）。
- 6_09 组1 总结句 ↔ 旧表全字段：最长公共中文串 `'的是生意'`（4 字，来自旧表 `title`「说的是生意落下来的那一截」里的语法词＋本书词「生意」）；**旧表的画面词 `落下去那截`／`那双手`／`动了手` 在新总结句里 0 命中**。
- 6_08 组1 总结句（未改，报告判 OK）↔ 旧表全字段：最长公共中文串 `'台机器'`（3 字，旧表 `core`「把音量拧大的那台机器」／本组「不归同一台机器」），未达逐字重复线，见「未改项」第 4 条。

**新写进卡面的英文串自查**（§五.1／§五.15：自己回 `sections.json`＋`vocab.json` 查，标记按「表面形式」与「词头 表面形式」两变体拆开后扫）：`the sound` → 只在 `amplify` 卡 `ex`（课文 1833 句 0 命中，故出处清单注明它不来自 1578）；`Microphones` → 课文 1578；`The speaker` → `amplify` 卡 `ex`；`ease tensions` → `ease` 卡 `note`（课文 0 处）；`alleviate boredom`／`alleviate health problems` → `alleviate` 卡 `note`（课文 0 处）；`boredom`／`health problems` 单词形 → 课文各 0 处（`boredom` 只在 alleviate 卡与 reduce 卡的词伙栏）；`tension` → 课文 1327 一处（1662 是 `hypertension`，别的词）。中文侧新断言（「全书译作…的只有」「`note` 整条为空」「课文 0 处」）逐条 grep 过。

## 未改项及理由

**报告标「建议／nit／提请拍板」的一律未动**（照报告办，不自己发挥）：

1. **215 的「建议（不计入清单）」**：items 两处 `core`（暖浴按着的那处疼／几首歌松开的那件事）与旧表「简单记」泡走腿上的疼／唱走心里的事语义同源 —— 报告自标"若求全，落地前……"，且明写「默认行已换"动手"新轴，段末不会重复」→ **未动**。（自查补一句给下一位：本组「这一段在讲什么」里 1703 的转述「几首轻柔的歌松开她的担忧」与旧表 `items[1].scene` 同字，但它是对**同一句课文**（1703）的转述、报告判在段末不重复，本代理不擅自扩大改动面。）
2. **三条 nit**：6_07 组3「turned … into」概括形状（1743／1813 实为 `turning`）、6_08 组2「引过 1676 整句」实为前半、6_09 组2 回落行的弯引号写成直引号 —— 报告均标"不改判"，未动。
3. **6_09 组1「落在 decrease 卡上就成了"一段末两张表挤在同一张卡"」** —— 报告 nit 说应写"顶掉旧表"。方向结论（换载体、落地器自动先挑 diminish）与报告一致，措辞属载体层，**未动**；真要改就一行的事，请下一位或真人顺手定。
4. **6_08 组1 `core`「话筒那台机器」**（旧表 `core` =「把音量拧大的那台机器」）：公共串只有 `那台机器` 4 字，未达逐字重复线；`core` 在卡页里、不在段末并排那一行 → 未动。**6_09 组1 表头/diff 里残留的 `那一截`**（3 字，段 58「那一截已经掉完了」）同理未动。
5. **文件级第 1 条（`cmpIndex` 同段多表按载体 VOCAB 键序排）**：属渲染侧口径，报告列为"要他拍"，本代理未碰 `shadow/index.html`，也未改任何草稿的载体建议（草稿里"建议换 XX 或 YY"的原话原样保留）。
6. **报告「需要他拍的最多 3 条」第 1 条本身**（改法 A＝作者改新表钩子 ／ 改法 B＝旧表整体下线重写）：本轮按**改法 A** 执行（改字、不动结构、不碰数据）；如果真人最终拍 B，本报告 #1／#5–#8／#13／#18–#22 这 11 处改字会被整表重写取代，#2／#3／#10／#15／#16 这几条自报文字则两法都要留。
7. **判定"通过"的 9 组**（208–212、214、216、217、219）：一字未动 —— `6_07.md` 全文件 0 改动，`6_08.md` 组2／4／5、`6_09.md` 组2 只出现在本文的终检表里。

**我认为报告可以说得更准／下一位要注意的两处（都按报告执行了，只是备案）**：

1. 报告 #1／#3 给的示例串**宽度都超线**，不能照抄：`amplify = 机器送声（amplify the sound），enlarge …，magnify …` 实测 **44.5**、`decrease = 生意那截，…，reduce = 后屋的浪费` 实测 **42.5**（上限 40）。四成员那一行的硬账是：成员名 14.5 ＋ ` = ` 6.0 ＋ 逗号 3.0 ＝ 23.5，钩子只剩 16 个汉字。所以我按报告的**轴**（哪类东西／带不带过程）自己压了措辞，并把这笔账写进 #17，免得下一位再撞一次。
2. 报告说本组「diff 首行又把旧表结论原样重讲」，但最小清单只要求改自报。**我把那几格 diff 的措辞一起改了**（#5／#6／#21），理由是"改字即可、不动骨架"，且这样自报里那句"不重用它原来的措辞"才是真话。真人若认为 diff 措辞不该动，回退 #5／#6／#21 三处即可，不影响 #1／#13／#14 的判定。

## 终检

1. 门禁 1（唯一被允许跑的自检工具，**原样输出**；未跑落地器）：

```
$ python3 tools/check_compare_draft.py work/辨析草稿/6_07.md work/辨析草稿/6_08.md work/辨析草稿/6_09.md
OK   6_07.md
OK   6_08.md
OK   6_09.md

门禁 1: PASSED（全部英文串可回溯）
```

2. **12 条默认行 ＋ 12 条总结句：标称（组内小标题）／实测（`tools/width_rule.width`，落盘后用 `width_rule.extract` 读回）逐条对照，含未改的 9 组**（24 个标称值全部＝实测，全部 ≤40／≤30；改过的只有 213 默认行、218 默认行与总结句三条，且**宽度标称值一条都没变**：213＝39.5、218＝39.5／28.0）：

| 组 | 默认行标称 | 默认行实测 | ≤40 | 总结句标称 | 总结句实测 | ≤30 |
|---|---|---|---|---|---|---|
| 208 `6_07` 组1 | 39.5 | 39.5 | 过（贴顶） | 26.0 | 26.0 | 过 |
| 209 `6_07` 组2 | 38.5 | 38.5 | 过 | 23.5 | 23.5 | 过 |
| 210 `6_07` 组3 | 38.0 | 38.0 | 过 | 26.5 | 26.5 | 过 |
| 211 `6_07` 组4 | 37.5 | 37.5 | 过 | 22.0 | 22.0 | 过 |
| 212 `6_07` 组5 | 36.5 | 36.5 | 过 | 21.0 | 21.0 | 过 |
| 213 `6_08` 组1 | 39.5 | **39.5** | 过（贴顶，已换文字未加字） | 24.0 | 24.0 | 过（未动） |
| 214 `6_08` 组2 | 37.5 | 37.5 | 过 | 23.0 | 23.0 | 过 |
| 215 `6_08` 组3 | 39.0 | 39.0 | 过（未动） | 21.0 | 21.0 | 过（未动） |
| 216 `6_08` 组4 | 37.5 | 37.5 | 过 | 20.0 | 20.0 | 过 |
| 217 `6_08` 组5 | 39.5 | 39.5 | 过（贴顶，**一个字没加**） | 21.0 | 21.0 | 过 |
| 218 `6_09` 组1 | 39.5 | **39.5** | 过（贴顶，四成员名吃 20.5、钩子 16 字） | 28.0 | **28.0** | 过 |
| 219 `6_09` 组2 | 38.0 | 38.0 | 过 | 29.0 | 29.0 | 过（未动） |

3. **附录复述清单 ↔ 组内小标题 ↔ 实测 三方逐个相等**（脚本比对，三份文件都跑）：
   - `6_07.md`：附录「默认行 39.5 / 38.5 / 38.0 / 37.5 / 36.5，总结句 26.0 / 23.5 / 26.5 / 22.0 / 21.0」＝组内标称＝实测 ✓
   - `6_08.md`：附录「默认行 **39.5 / 37.5 / 39.0 / 37.5 / 39.5**、总结句 **24.0 / 23.0 / 21.0 / 20.0 / 21.0**」＝组内标称＝实测 ✓（文件头小结"默认行最大 39.5、总结句最大 24.0"仍成立）
   - `6_09.md`：附录与文件头小结「组 1 默认行 39.5、总结句 28.0；组 2 默认行 38.0、总结句 29.0」＝组内标称＝实测 ✓；附录"组 1 四个成员名吃 14.5 当量／组 2 吃 12.0、钩子一律压到 3–6 字"与新钩子（4／4／5／3）逐个相符 ✓
4. 组标题编号与成员顺序：`6_07` 组1–5、`6_08` 组1–5、`6_09` 组1–2 未增删未换位（worklist_idx 208–219 仍一一对应，slug 不变）；12 组 `主锚点 = 章N 段M` 字段名与值原样（脚本数到 5＋5＋2 行）。
5. 表列数：三份文件 **40 张 markdown 表**（16＋17＋7）逐张数 `|`，列数不齐 0 张；改过的 items 行（amplify／decrease／reduce）仍是 7 列、`eg` 槽非空。`core` 宽度实测最大 11（≤12），改动的两格（decrease 6／reduce 8）全过。
6. §五.4 义项词包含检测（core／默认行／总结句，含未改组）：213 组「放大／增强」0 命中、215 组「缓解／减轻」0 命中、218 组「减少／降低／削弱／减损／缩小／减量」0 命中 —— 「放大」仍只待在 `sense` 槽与表头引用位。
7. 改动面自查：`git status --short` 里属于本代理的只有 `6_08.md`、`6_09.md` 两份 ` M`（`6_07.md` 未列入＝0 改动）＋本文件 `??`；`git diff` 里无 `config.js`、无 Supabase key、无 `<<<<<<<` 冲突标记。

**边界自证（写盘后 mtime，与 `audit_d_b6.md` 文末基线对照）**：`shadow/data/vocab.json` Sep 21 03:25:15（未变）、`shadow/data/sections.json` Sep 19 22:12:02（未变）、`shadow/index.html` Sep 21 03:25:15（未变）、`work/辨析草稿/6_07.md` Sep 21 09:13:25（未变）；被本代理改过的只有 `work/辨析草稿/6_08.md`（10:37:31）、`work/辨析草稿/6_09.md`（10:41:58）。
