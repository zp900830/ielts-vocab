# 待办：主按钮配色回退（用户 2026-09-24 指定，排在 M2 之后）

## 用户原话

> 「M2完成后，把全局的主按钮配色调回到 主站"填充+黑色 icon"之前那一个版本。」

## 已查明的确切目标版本

「填充 + 黑色 icon」= commit `8ce9db7`（2026-09-22）「绿底白字主按钮**换深墨字**（1.90 → 5.85）」。

它**之前**那一版（= 用户要回退到的目标）在主站 `index.html` 是：

```css
--iel-grad: linear-gradient(135deg, #2bd4a4, #0fae7e);   /* 轻薄荷渐变 */
--iel-grad-shadow: 0 3px 10px rgba(16,180,135, .25);      /* 通透光晕 */
/* 主按钮 */ background: var(--iel-grad); color: #fff;   /* 白字 + 白 icon */
```

## 要做的

1. 三站（`index.html`、`shadow/index.html`、`app/index.html`）主按钮统一回退到上面这套：
   - `--grad` = `linear-gradient(135deg,#2bd4a4,#0fae7e)`
   - 光晕 = `0 3px 10px rgba(16,180,135,.25)`（保留"通透感"）
   - `--cta-ink` = `#fff`（白字/白 icon）
2. **对比度门禁改造**：从"对比度底线"改成**锁定色值的回归锁**（防止有人手滑改浅/改深），并**诚实标注**：这是用户选定的品牌色，实测 **1.90:1**，**已知低于 WCAG AA**，用户知情接受。
   - 上一轮加的"合成回归（太浅会红）"要相应调整或删除 —— 否则它会和用户的新选择直接冲突。
3. **保留**：`--accent` 底色的非主按钮控件（选中的胶囊等）**维持深墨字**（`--cta-ink-dark`），那是另一回事，用户没要求动。
4. PRD §10.1 + `docs/PRD.md` 同步：记录新 token 值 + 已知取舍。

## 风险（已告知用户）

`#2bd4a4` 上的白字实测 **1.90:1**，比用户自己定的 3:1 底线还低，实际阅读会偏糊（阳光/老花更明显）。用户已明确接受。

## 关联

- 上一轮（`bffa8d2`）把主按钮调到了 `#2e9c76`（3.42:1），本轮是把它**再往回退**到 `#2bd4a4`（1.90:1）。
- 门禁文件：`playwright-tests/journeys/shadow/cta-and-popups.spec.ts` + `playwright-tests/fixtures/cta-gate-selfcheck.css` + `playwright-tests/journeys/app3/palette.spec.ts`。
