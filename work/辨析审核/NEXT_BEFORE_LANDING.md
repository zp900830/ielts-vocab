# 落地前剩余清单（52 组：批次 1 十组 + 批次 2 四十二组）

批次 2 现状（2026-09-20 我复跑，非代理自报）：42 组宽度机检 **0 条超限**、
`check_compare_draft.py` 九份 **PASSED**。三路修复代理各自另交 3 条分歧（见各 FIX 文件末节）。

按顺序做，做完一步勾一步：

1. **长表→宽表**（唯一的硬阻塞）。批次 2 的差异表是 `| 维度 | 词 | 常见搭配 | 用在哪 | 一句话区别 |`
   长表，落地器只吃批次 1A 那种 `| 维度 | 词1 | 词2 |` 宽表。
   `tools/normalize_draft_shape.py` 的这一步**有 bug 不许 --write**：它就地改行却按旧跨度遍历，
   索引漂移，会把「出处清单」误认成差异表。修法＝倒序处理各组 + 只在「差异维度表」小节内找表；
   改完必须先看 dry-run 报告里「组数 N → N」和每组行数对不对，再 --write。
2. **4 处表头「这一句」**残留，门禁 3 第 16 条会拦（卡挂段末、没有"这一句"可指）：
   `2_04` 2 处、`2_08` 1 处、`2_09` 1 处。改成「这一段」或「NNN 同句里」。
   （批次 2 另 6 处已由修复代理清掉。）
3. **回退一处越界改动**：`2_06` 组 37 门禁 2 判 OK，修复代理为留余量把括号例子换成
   `(at the end)`。审核说 OK 就不该动 —— 只回这一处，见 `FIX_2_05_07.md` 分歧 1。
4. 批次 1（1A/1B）补测与 FIX_batch1.md —— 代理在跑，落地前必须齐。
5. `python3 tools/land_compare.py`（dry）→ 看冲突清单（`cite` 一词条带两张表）→ `--write`
   → `scripts/validate_data.py` → `cd playwright-tests && npm test`。
6. 新增一批机检：把"每个组都被量到"写死（打印 量到 N / 应有 M）。
   这一轮两次踩到同一个坑：1B 和 2_04 用 `## 3.` 编号，找组的脚本静默跳过，
   于是"全绿"里有 3 组从没被量过。**漏测比超限危险，因为它不报错。**


## 2026-09-20 23:00 落地后补记（52 组已进 vocab.json，cmp 字段）

已完成：落地器 4 处形状修复 → 52 组 0 错误 → `--write` → validate PASSED（57 张卡）→ 82 E2E 全绿
→ 390px 真机视口看过渲染（维度小标题成块、单侧维度整行合并、无 markdown 死字、无空列）。

**一处要他拍板的实测发现（我没动内容）**：段末默认行**全部渲染成两行**。
容器实测宽 334px、字号 13px、行高 22.1px → 一行只装得下约 25 个「汉字当量」，
而我们一路校准的上限是 40。也就是说 ≤40 这条尺从批次 1A 起就没对应到"一行读完"，
52 条 + 旧 5 条无一例外都是两行。三选一：
① 接受两行（把 PRD §7.2 那句"一行"改成"两行内"，什么都不用重做）；
② 上限压到 ~25，52 条默认行重压一轮（一轮代理活，内容要删）；
③ 折中：字号/行高不动，把默认行改成"词 = 钩子"两行版式（第一行一个词），
   等于承认两行但让它断得整齐。
我倾向 ①（他现在每天看的就是这个高度，两行没挡住读），但这条是口味，不替他定。

**顺手记两条口径决定**（写进台账，别再逐组上报）：
· 2_06 组 3（worklist 37）那处"报告判 OK、修复代理为留余量换了括号例子"**不回退**：
  两个写法（morning summary / at the end）都是同一条卡 ex 里的原文，宽度 40.0 压线是事实，
  换完 37.5 有余量、语义没变，且报告原话就是"建议减字留余量"。已在 FIX_2_05_07.md 点名。
· 1A 组 2 表头与 1B 组 6/7 的引号**不再改回**：改回去只统一了标点、不动任何一条默认行，
  收益是零、风险是把门禁 2 判过 E BLOCK 的"照抄义项"形状再摆回标题。
  1A 组 1/组 3 那两处按 §五.11 改回是另一回事（那里有"当场否定"）。
