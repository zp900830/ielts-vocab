# 雅思背单词项目 · 全量排查报告

**排查日期**: 2026-09-15  
**排查范围**: `shadow/index.html`（影子跟读）、`index.html`（情境阅读）、`admin/index.html`（管理后台）、`data/`、构建脚本、Playwright 测试、CI 工作流  
**排查方法**: a11y 源碼审计、性能源碼审计、 thatsweird 浏览器边缘 case 扫描、人工代码走查

---

## 一、总体结论

项目功能已相对完整，但存在 **3 类必须尽快处理的风险**:

1. **安全/凭证泄露**: 两个前端文件直接硬编码了 Supabase anon key 与 project URL。
2. **CI 无法通过**: GitHub Actions 工作流未跟踪到 git，且配置会导致测试直接失败。
3. **影子跟读主应用体积过大**: `shadow/index.html` 单文件 ~1.3 MB，首屏载荷高，且无压缩/缓存。

其余问题多为 **功能瑕疵、可访问性缺口、UI  polish、数据/测试基建不足**，可按优先级分阶段修复。

---

## 二、问题总览（按严重程度）

| 级别 | 数量 | 说明 |
|------|------|------|
| **P0 严重** | 12 | 崩溃、数据错误、安全/凭证、CI 不可用、核心功能阻断 |
| **P1 高** | 22 | 明显 bug、性能瓶颈、可访问性阻断、用户体验受损 |
| **P2 中** | 18 | 可优化点、 polish、偶发问题、代码异味 |
| **P3 低** | 12 | 细节优化、建议项、文档/配置补全 |

---

## 三、P0 严重问题

### 1. 凭证硬编码（安全）
- **位置**: `shadow/index.html:2161-2163`、`admin/index.html:310`
- **问题**: Supabase project URL 与 anon key 以明文写入前端 HTML。
- **影响**: 任何人查看页面源码即可拿到 key，可直接调用 auth / edge functions；即使 RLS 正确，也增大了被滥用、爆破、配额盗用的风险。
- **建议**: 改为构建时注入或走自有后端代理；至少轮换当前 key 并限制调用来源。

### 2. CI 工作流未入版本控制且配置错误
- **位置**: `.github/workflows/playwright.yml`（未跟踪 `??`）
- **问题**:
  - `timeout-minutes: 5` 远小于单条测试 `120s * 6 = 12min` 的超时。
  - `channel: 'chrome'` 需要系统安装 Google Chrome，而 CI 只装了 Chromium。
  - 直接 `npx playwright test` 跳过了 `package.json` 的 `pretest`（`ensure-server.cjs`）。
- **影响**: PR/Push 到 main 时测试必然失败或超时。
- **建议**: 提交该 workflow；改为 `channel: 'chromium'`；超时改为 30min；运行 `npm test`。

### 3. `shadow/index.html` 首屏体积过大
- **位置**: `shadow/index.html`
- **问题**: 单文件 ~1.3 MB，内联 JS ~1.23 MB、CSS ~64 KB，再加 121 KB 外部图标字体；无压缩、无缓存头。
- **影响**: 移动端/弱网首次加载极慢；每次重载重复下载全部数据。
- **建议**: 将 `SECTIONS`/`VOCAB`/`CHAPTERS` 数据拆出为独立 JSON 懒加载；开启 gzip/brotli；加长期缓存头。

### 4. 云端音频缓存可能撤销正在播放的 URL
- **位置**: `shadow/index.html:2297-2303`
- **问题**: `_cacheSet` 在达到 120 条时直接 `URL.revokeObjectURL()` 最旧项，若最旧项正是 `this._au.src`，会中断当前播放。
- **影响**: 长文连播到后期可能突然静音或降级。
- **建议**: 记录当前/下一个正在播放的 key，跳过这些 key 的回收。

### 5. 云端 TTS fetch 无超时/取消
- **位置**: `shadow/index.html:2348-2372`、`index.html:2323`
- **问题**: `fetch` 没有 `AbortController`，用户暂停/切句时请求仍在后台挂起。
- **影响**: 弱网时卡住，资源浪费；串行队列可能被一个挂起请求阻塞。
- **建议**: 加 `AbortController` + 超时；在 `stop()` / 切句时 `abort()`。

### 6. 移动端词库抽屉下滑关闭与列表滚动冲突
- **位置**: `shadow/index.html:3720-3739`
- **问题**: 在可滚动的词库列表内下滑也会触发抽屉关闭。
- **影响**: 用户想滚动词库时频繁误关抽屉。
- **建议**: 仅当触摸起点在抽屉头部/手柄，或列表已滚动到顶部时才允许下滑关闭。

### 7. index.html 注册/登录不迁移访客数据
- **位置**: `index.html:1333-1338`、`1238-1254`
- **问题**: 按钮文案写“注册（带入当前数据）”，实际注册后只加载账号数据，访客数据丢失。
- **影响**: 用户注册后之前的学习记录、书签等被清空。
- **建议**: 检测到新邮箱且存在访客 `S` 时，先合并访客数据再上传。

### 8. index.html Service Worker 作用域与缓存名错误
- **位置**: `index.html:4226、4254`
- **问题**: `DATA_VER` 在 SW 线程中 undefined，缓存名恒为 `ielts-vocab-v1`；`scope:"/"` 在 GitHub Pages 子路径部署时会控制整个 origin。
- **影响**: 新版本可能长期命中旧缓存；子路径部署会污染同域名其他项目。
- **建议**: 将 `DATA_VER` 注入 SW 代码；使用 `location.pathname` 派生 scope；缓存路径应为应用路径。

### 9. index.html `resetAll()` 未清除阅读位置
- **位置**: `index.html:3938-3945`
- **问题**: 重置数据后 `POS_KEY` 仍在，下次加载仍会打开旧文章。
- **影响**: 与“清空数据”的期望不符。
- **建议**: `resetAll()` 中 `localStorage.removeItem(POS_KEY)`。

### 10. index.html 云 TTS 检查状态可能永久卡住
- **位置**: `index.html:2242`
- **问题**: 无 session 时直接 `return`，未清 `_checking` 标志，也未触发 `_fireReady()`。
- **影响**: 未登录用户音频条一直显示“正在检测云端语音…”，且 `_whenReady` 回调永不执行。
- **建议**: 无 session 时也要清标志并触发 ready（状态为不可用）。

### 11. index.html 单词弹窗无最大高度/不可滚动
- **位置**: `index.html:359、2130-2135`
- **问题**: 长释义会超出视口且无法滚动。
- **影响**: 释义/例句过长时看不到下方内容。
- **建议**: `#wpop` 加 `max-height` + `overflow-y:auto` + `overscroll-behavior:contain`。

### 12. admin 导出后立即撤销 blob URL 可能导致下载失败
- **位置**: `admin/index.html:463`
- **问题**: `URL.revokeObjectURL(a.href)` 紧跟 `a.click()`，浏览器可能还没开始下载。
- **影响**: 大文件导出可能失败或损坏。
- **建议**: `setTimeout(()=>URL.revokeObjectURL(url), 1000)` 后再释放。

---

## 四、P1 高优先级问题

### 影子跟读 `shadow/index.html`

1. **键盘空格冲突**: 聚焦句子/单词时按空格会同时触发元素播放与全局播放/暂停。
2. **暗色模式 `body.dark html` 选择器无效**: 根背景在暗色下仍是浅色，导致 iOS 橡皮筋/加载时闪白。
3. **播放倍速/循环次数未持久化**: 刷新后恢复默认值。
4. **中英文/内联辨析显示状态未持久化**: 刷新后恢复默认显示。
5. **暗色模式切换按钮初始化状态不对**: 从 localStorage 恢复暗色后，按钮图标仍是月亮。
6. **A-B 循环与单句循环叠加时行为异常**: 第一轮后单句重复失效。
7. **云端音频“暂停过久”同时恢复+降级**: 3s 恢复与 5s 降级逻辑耦合，可能同时触发。
8. **iOS 5s 保活无条件 pause/resume**: 可能导致吞字、卡顿。
9. **可点击 `<span>` 无键盘/ARIA 支持**: 句子、单词、进度条标题等键盘不可达。
10. **弹窗无 focus trap / 焦点返回**: 词弹窗、跳转、书签、云登录弹窗打开后 Tab 会跑到背后。
11. **`theme-color` 不会随暗色模式更新**: 暗色下地址栏仍闪浅色。
12. **多处硬编码颜色未覆盖暗色**: `.sec h2`、`.t-en`、`.gl` 等。
13. **弹窗可堆叠**: 打开新弹窗时旧弹窗未关闭。
14. **菜单与抽屉 z-index 层级问题**: 菜单可能被抽屉盖住。

### 情境阅读 `index.html`

1. **无暗色模式**: 只有浅色主题，夜间使用刺眼。
2. **键盘快捷键在按钮/链接上仍触发**: 空格会同时点击按钮并切换播放。
3. **Esc 不能关闭单词弹窗**: 键盘用户无法退出弹窗。
4. **云音频恢复失败静默吞错**: blob URL 被回收后 resume 失败无降级。
5. **2s 语音卡死计时器过短**: 慢设备/长句可能误判。
6. **备份/导入未包含阅读滚动位置**: `POS_KEY` 不在备份中。
7. **push/pull 同步竞态**: `pull()` 可能在正在 push 时触发，导致新状态未上传。
8. **启动时先渲染访客态再刷新**: 登录用户会闪现游客横幅。
9. **章节选择器在数据就绪前渲染**: 初始显示空/错计数。
10. **学习统计包含非目标词**: 效率计算被非章节词污染。
11. **阅读器未在 tab 切回时自动恢复**: 只有随身听做了恢复。

### 管理后台 `admin/index.html`

1. **`:deep()` 用于普通 `<style>`**: 规则被浏览器丢弃。
2. **`loadData()` 错误后无限 loading**: `dInfo` 保持 null。
3. **图表未 dispose**: 内存泄漏。
4. **保存 TTS 配置会清空 key**: `loadTts()` 把 `tts.key` 清空，保存时若未重新输入可能删除 key。
5. **`exportAll()` 未使用 `api()` 辅助**: 401 时不会跳转登录。
6. **审计日志与批量操作共用 endpoint**: 语义不清，权限脆弱。
7. **admin/pending 角色标签同色**: 视觉上无法区分。
8. **提升/降级管理员无二次确认**: 误操作风险高。
9. **日期格式化未校验输入**: 可能输出 NaN。

### 数据 / 构建 / 测试

1. **`build_ielts.py` 源码路径不存在且目标错误**: `SRC` 指向不存在的 Downloads 目录；`OUT` 指向 legacy `雅思影子跟读.html` 而非 `shadow/index.html`。
2. **`data/vocab.json` 顶层结构与数据不一致**: 930 个顶层词只有 `note`，没有 `p`/`m`；实际音标释义在 `chapters` 里，且 `vocab-map.json` 未跟踪。
3. **`data/book.json` 跨章重复词**: 48 个词出现多次，可能导致章节词表不一致。
4. **Playwright 测试未覆盖主应用**: 19 条测试全针对 shadow，index.html 零覆盖。
5. **测试导入不一致**: 部分用 `@playwright/test`，部分用 `../../fixtures`。
6. **`tsconfig.json` 中 `@fixtures` 别名无人使用**: 死配置。

---

## 五、P2 中优先级问题

### 通用 / 可访问性
- 多个页面缺少 `h1`、跳至内容链接、`label` 关联。
- 图标按钮、空链接、新标签页链接无提示。
- 表单仅靠 placeholder，无 label。
- Toast / 动态状态无 `aria-live`。
- 动画已有 `prefers-reduced-motion` 的页面：`shadow/index.html`、`index.html`；`admin/index.html` 缺失。

### 性能
- 根目录 19.7 MB PNG 截图/备份（含 14 MB 超高全页截图），未引用但占用仓库/部署体积。
- `admin/index.html` 阻塞加载 ~2.5 MB 第三方 JS/CSS（Vue、Element Plus、ECharts）。
- Supabase SDK 使用浮动 `@2` 标签，可能静默升级。
- 无 `preconnect` 到 CDN 域名。
- 未压缩、无缓存头。

### thatsweird 边缘 case
- `index.html`、`admin/index.html` 缺少 `color-scheme` meta/声明。
- `admin/index.html` 缺少 `theme-color`。
- `admin/index.html` 输入框无 16px 字体，iOS 可能自动缩放。

---

## 六、P3 低优先级 / 建议

- 代码中存在多处 `try/catch` 空吞错，用户完全无感知（如 localStorage 写满）。
- 多处滚动监听未合并，维护成本高。
- `content-visibility: auto` 可能影响远距离 scrollIntoView 精度。
- `resolveVoice()` 用 `vs.includes(k)` 做对象引用比较，不可靠。
- `cloudPullMarks()` 可能在登录时被调用两次。
- admin 视图未同步 URL，刷新回默认页。
- admin 表格无空态、无分页。
- 依赖版本未锁定（`^1.55.0`）。
- `.gitignore` 未包含 `__pycache__`、`test-results/`。

---

## 七、自动化审计摘要

### 可访问性
- **评分**: 58/100（D 级）
- **主要缺口**: 键盘不可达的点击元素、弹窗无 dialog 角色/焦点管理、表单无 label、标题层级缺失。

### 性能
- **主要缺口**: 单文件过大、未优化 PNG、admin 阻塞第三方资源、无压缩缓存。
- **最大收益**: 拆分 shadow 数据、清理截图、admin 脚本加 defer/async。

### /thatsweird
- `shadow/index.html` 较完整；`index.html`、`admin/index.html` 需补 `color-scheme`、`theme-color`、iOS 输入字体、 reduced-motion。

---

## 八、分阶段修复计划

### Phase 1 — 止血（P0，建议 1-2 天）

| # | 任务 | 目标文件 |
|---|------|----------|
| 1.1 | 修复 CI workflow：提交 `.github/workflows/playwright.yml`，改 `chrome`→`chromium`，超时 30min，运行 `npm test` | `.github/workflows/playwright.yml` |
| 1.2 | 修复 Supabase 凭证泄露： shadow/admin 改构建时注入；如不能立即改后端，至少先轮换 key 并加 referrer/RLP 限制 | `shadow/index.html`、`admin/index.html`、构建流程 |
| 1.3 | 拆分 `shadow/index.html` 数据：把 `SECTIONS`/`VOCAB`/`CHAPTERS` 拆成独立 JSON，异步加载 | `shadow/index.html`、`data/` |
| 1.4 | 修复缓存回收撤销当前播放 URL | `shadow/index.html` |
| 1.5 | 给云端 TTS fetch 加 AbortController + 超时 | `shadow/index.html`、`index.html` |
| 1.6 | 修复 index.html Service Worker scope 与缓存名 | `index.html` |
| 1.7 | 修复注册/登录不迁移访客数据 | `index.html` |
| 1.8 | 修复 `resetAll()` 未清阅读位置 | `index.html` |
| 1.9 | 修复 admin 导出 blob 被提前释放 | `admin/index.html` |

### Phase 2 — 核心体验（P1，建议 2-3 天）

| # | 任务 | 目标文件 |
|---|------|----------|
| 2.1 | 修复移动端词库抽屉下滑误关闭 | `shadow/index.html` |
| 2.2 | 修复 shadow 键盘空格冲突 + 句子/单词可访问性 | `shadow/index.html` |
| 2.3 | shadow 暗色模式根背景/ theme-color 联动 | `shadow/index.html` |
| 2.4 | shadow 播放偏好持久化（倍速、循环、显示设置） | `shadow/index.html` |
| 2.5 | shadow 弹窗 focus trap + 焦点返回 + 互斥关闭 | `shadow/index.html` |
| 2.6 | 修复 A-B 循环与单句循环叠加 | `shadow/index.html` |
| 2.7 | 修复 index.html 暗色模式缺失 | `index.html` |
| 2.8 | 修复 index.html 键盘快捷键冲突、Esc 关闭弹窗 | `index.html` |
| 2.9 | 修复 index.html 云音频 resume 失败降级 | `index.html` |
| 2.10 | 修复 admin `:deep()`、图表 dispose、loading 错误态 | `admin/index.html` |
| 2.11 | admin 提升/降级加确认、TTS 保存不丢 key | `admin/index.html` |
| 2.12 | 清理根目录 19.7 MB 无用截图/备份 | 仓库根目录 |
| 2.13 | admin 第三方脚本加 `defer`/`async` | `admin/index.html` |

### Phase 3 — 可访问性与 polish（P2，建议 2 天）

| # | 任务 | 目标文件 |
|---|------|----------|
| 3.1 | 补 h1、跳至内容链接、label、aria-live、图标按钮 label | 全部 HTML |
| 3.2 | 弹窗加 `role="dialog"`、`aria-modal="true"` | 全部 HTML |
| 3.3 | `index.html`、`admin/index.html` 补 `color-scheme`、`theme-color`、reduced-motion | `index.html`、`admin/index.html` |
| 3.4 | 修复 admin iOS 输入缩放、视图同步 URL、空态/分页 | `admin/index.html` |
| 3.5 | 性能：开启 gzip/brotli、加缓存头、图片懒加载/现代格式 | 部署配置 |
| 3.6 | 统一 Playwright fixtures 导入、为主应用加 smoke 测试 | `playwright-tests/` |

### Phase 4 — 数据与基建（P3，建议 3-5 天）

| # | 任务 | 目标文件 |
|---|------|----------|
| 4.1 | 修复 `build_ielts.py`：接受参数/环境变量、目标指向 `shadow/index.html`、原子写入、备份 | `build_ielts.py` |
| 4.2 | 统一数据 schema：合并 `vocab.json` 顶层与 `vocab-map.json`，消除漂移 | `data/` |
| 4.3 | 添加数据校验测试：story 占位符存在性、词汇总数一致性 | `playwright-tests/` 或 Python tests |
| 4.4 | 锁定依赖版本、完善 `.gitignore`、压缩/指纹资源 | 配置 |
| 4.5 | 补全错误处理与用户反馈（localStorage 满、网络失败等） | 全部 JS |

---

## 九、下一步

1. 请确认上述报告范围与分级是否符合你的预期。
2. 确认 Phase 1 的优先级与取舍（尤其是凭证处理方案：构建时注入 vs 后端代理）。
3. 我将按确认的优先级逐阶段执行，每阶段完成后提交变更并运行回归测试。

---

## 十、附录：待验证问题

以下问题需要真机/特定环境验证，未纳入立即修复：

1. `content-visibility: auto` 对远距离 `scrollIntoView` 的影响。
2. `resolveVoice()` 中 `vs.includes(k)` 引用比较是否会导致降级。
3. `cloudPullMarks()` 登录时是否被调用两次。
4. 折叠音频条点击是否误从第 1 句播放。
5. Service Worker 在 GitHub Pages 子路径下的实际 scope 行为。
6. 反复打开/关闭文章是否导致 `document.fonts.ready` 闭包泄漏。
