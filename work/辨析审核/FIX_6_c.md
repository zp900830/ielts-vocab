# 修复记录 · 批次 6 `6_05.md` + `6_06.md`（依据 `work/辨析审核/audit_c_b6.md`）· 2026-09-21

执行人：修复代理（FIX_6_c，作者侧）。**本轮只改了两份被审草稿 `work/辨析草稿/6_05.md`、`work/辨析草稿/6_06.md` 与本文件**；未写 `shadow/data/*`（`sections.json`、`vocab.json` 只读）、未碰 `scripts/*`／`tools/*`／`index.html`／`shadow/index.html`／别人的草稿（本批 `6_01..6_04`、`6_07..6_09`、批次 5 全部）／任何 `audit_*.md`（`audit_c_b6.md` 一字未动）；**没有跑 `tools/land_compare.py` 的任何模式**（连 `--dry-run`／`--check` 都没跑，`work/辨析审核/land_preview.json` 未被我碰过）；**无任何 git 写操作**（只跑过 `git status --porcelain`、`git branch --show-current`、`git log --oneline -3`、`git diff --stat` 四条只读命令，分支仍是 `agent/qoder-compare-batches2-6-0920`）。

本轮处理报告 4 条 MISMATCH（组 198 / 201 / 202 / 207，BLOCK 0），共改 **17 个位置**：报告必修 **10 处**（`6_05` 8 处＝组 198 两格 ＋ 附录句号 12→11 ＋ 组 201 三处（1147 补档、6→7 处、旁证 0 次改口径）＋ 组 202 两处（`in-house` 不再称"词头"）；`6_06` 2 处＝组 207 的 `requirements` 1→2 处两个引用点）＋ 载体分配写清 **5 处**（`6_05` 文件头、组 200 两处、组 201 两处）＋ 修复轮自证行 **2 处**（两文件各一行）。判定"通过"的 6 组（199／200／203／204／205／206）卡面内容一字未动。**10 条默认行与 10 条总结句、30 个 `core` 全部未动**——报告点名的 6_05 组 2（adorn）、组 3（delicious）默认行**实测正好 40.0 贴顶**，本轮一个字都没往上加（终检第 2 条逐条复量）。

被修文件 mtime／体积（自证改了哪一版；报告审的是 09:15:48 / 85 879 B 与 09:09:04 / 91 327 B）：`6_05.md` 2026-09-21 10:39:16 · 89 618 B、`6_06.md` 10:44:38 · 92 384 B（最后一次改动是 #10 那格的措辞校正，改后门禁 1 与 20 条宽度**重跑过**，见终检 1／2）；`work/辨析审核/audit_c_b6.md` 仍是 10:18:55 · 34 578 B（我没动过它）；`work/辨析审核/land_preview.json` 仍是 **09-20 23:24:49**（落地器没被跑过的直接证据）。`git diff --stat -- shadow/data scripts tools index.html shadow/index.html` 输出为空。另：本工作区在我开工前就不干净（`6_01..6_04`、`6_08`、`6_09` 由并行的修复代理在改，`FIX_6_a.md`／`FIX_6_b.md` 已存在），那六份文件我一个字没动，只在我自己那两份里写了修复轮自证行。

## 改动清单

| # | 报告编号 | 组 | 改前 | 改后 | 改后宽度 | 过不过 |
|---|---|---|---|---|---|---|
| 1 | 组198·F-1（必改清单①） | 198（`6_05` L61，diff「句子的力气花在哪」/leap） | 「**本书唯一**给跳法配了程度词的就是 leap high」（与本文件 L82 自己引的 kangaroo 卡 ex 相矛盾） | 「**课文**里给跳法配了程度词的只有 637 的 `leap high`（已 grep：课文没有 `jump high`／`jumps high`／`hop high`）；**卡侧**另有 kangaroo 卡 ex `The kangaroo jumps high in the Australian wild.` 一处拿 high 配 jump，那是卡不是课文，别说成"全书唯一"。over 那半句在卡 ex，课文没用」——范围点明到"课文 vs 卡侧"，反例就地承认 | —（diff 格，不受 ≤40／≤30 约束） | 过。自查：`jump high`／`hop high` 两份数据 **0 命中**（反向 grep 证实"没有"为真）；`jumps high` 只住 vocab（kangaroo 卡 ex）；把两份数据全部卡 `ex`/`exZh`/`note` 扫了一遍，含跳法词＋high 的卡侧文本恰好只有 kangaroo ex 与 leap 卡词伙 `leap high`（后者与 637 同源），无第三条 |
| 2 | 组198·F-2（必改清单①） | 198（`6_05` L65，diff「中文落点」/三个词） | 「**本书译文从没**把三词并成同一个中文词」（说过头：同一行「常见搭配」栏自己引的 jump「跳过了水坑」与 leap「跳过了篱笆」动词部分相同） | 「**本段**三处译文各给一个中文（跳上跳下／蹦得老高／单脚跳），没并成同一个词；范围一放到全书就不成立：卡 `exZh` 里 jump「跳过了水坑」与 leap「跳过了篱笆」动词部分完全相同（那是卡侧），课文侧 302 的 hop 译「在扶栏边跳」与 921 的 jump 译「跳下来」也共用「跳」（那是别的段）」＋原「蹦」全 2 处那句保留 | —（diff 格） | 过。三处 `exZh`／`sentZh` 逐字回数据验过：jump 卡 `exZh`＝「男孩跳过了水坑。」、leap 卡 `exZh`＝「猫跳过了篱笆。」、302 `sentZh`＝「…乌鸦在扶栏边跳…」、921 `sentZh`＝「…从卡车上跳下来…」；「蹦」全书课文译文实测 2 处＝637、638 ✓ |
| 3 | 组198·文件头计数 | 附录（`6_05` L449） | 「5 组 **12 个**句号与切片包 `sents` 逐个对得上」 | 「5 组 **11 个**句号（组 1 三句 636／637／638 ＋ 组 2–5 各两句，切片包 `sents` 条目共 11 个、无跨组重复：636、637、638、718、719、852、853、878、879、880、881）」——按 `slice41.json` 的 5 组 `sents` 摊平重算：3＋2＋2＋2＋2＝11、去重后仍 11 | — | 过 |
| 4 | 组201·F-1 前半（必改清单②） | 201（`6_05` L317，diff「它在句子里站哪一格」/outside） | 「**后面什么都不接**」清单只列 1080／1095／1110／1151／1496／1758（6 处），1147 两格都没进 | 补进 `the wooden door outside`（1147，outside 落在 door 后面当后置修饰，自己后面没有名词），两桶各标称数：「带名词（13 处里的 6 处）」「**后面什么都不接**（13 处里的 7 处）」，「一句话区别」格同时补「6 处带名词 ＋ 7 处不接 ＝ 带标记 13 处」 | —（diff 格） | 过。我用脚本按 `[[outside:…]]` 定位后取其后 40 字符逐个分类：紧跟句末／to 的＝1080、1095、1110、1147、1151、1496、1758 共 **7**；后接名词的＝878、942、976、1092、1270、1521 共 **6**；6＋7＝13 与带标记 13 处自洽 |
| 5 | 组201·F-1 后半（连带引用点） | 201（`6_05` L349，数据边界「计数结论」） | 「（1080 / 1095 / 1110 / 1151 / 1496 / 1758 **共 6 处都不接**）」 | 「（1080 / 1095 / 1110 / **1147** / 1151 / 1496 / 1758 **共 7 处都不接**，另 6 处带名词：878 / 942 / 976 / 1092 / 1270 / 1521）」 | — | 过（与 #4 同为一个数） |
| 6 | 组201·F-2（措辞） | 201（`6_05` L291，计数段旁证） | 「exterior、external 在别的卡 `ex` / `note` 里各 0 次」——`note` 里确有同义词栏，这句话与下文 `all_synonyms: true` 判据打架 | 「exterior、external 在别的卡的 `ex`、`exZh` 与**词伙段**里各 0 次（external 只被 event 卡词伙 `external events` 引了一次，与它自己那条同串，所以准确说法是"0 条新文本"而非"0 次出现"）。**这个 0 次不含同义词栏**：`exterior` 被 external、outside 两卡各列一次，`external` 被 exterior、outside 两卡各列一次（已 grep 全部词头卡的 `note`）——栏内互列正是下文 `all_synonyms: true` 的判据，不是旁证书证；已落地的 `exterior-outward` 那张表里对这两个词的引用同理属辨析表自身文本」 | — | 过。**这里我没照抄报告的建议措辞**：报告建议改成"在别的卡的 `ex` 与词伙段里各 0 次"，但 event 卡词伙正是 `external events`，那样写会变成一条新的虚报（见「报告建议 ≠ 事实来源」段）。实测：exterior 只出现在 external、outside 两卡的同义词栏；external 出现在 exterior、outside 两卡同义词栏 ＋ event 卡词伙；两者的 `ex`／`exZh`／`m`／词伙段全部 0 命中 |
| 7 | 组202·F-1 前半（必改清单③） | 202（`6_05` L408，diff「中文落点」/三个词） | 「一处给 `in-house`（406，**别的词头**）」 | 「另一处落的是 406 里**未打标记、也没有卡**的 `in-house`（那句只标了 `accounting`、`audit` 两个词头，`vocab.json` 里查不到 in-house 这个键，所以它不是词头、别按"别的词头"去找它的卡）」。「内部」只 2 处那条断言原样保留（复验为真） | —（diff 格） | 过。实测：406 = `An [[accounting:accounting]] buddy … an in-house cash [[audit:audit]] at noon.`，`MARK.findall` 只有 accounting、audit；`'in-house' in vocab.json 键集合` → **False**；「内部」全书课文译文 2 处＝406、1634 ✓ |
| 8 | 组202·F-1 后半（连带引用点） | 202（`6_05` L422，出处清单 406 行） | 「……落的是 `in-house` **这个别的词头**"这一条断言（§五.15），不进成员。」 | 「……落的是 406 里 `in-house` 这个词"这一条断言（§五.15）。**它不是词头**：406 那句打的标记只有 `accounting` 与 `audit` 两个，`in-house` 既没在课文里被标出来、`vocab.json` 里也没有它的卡，所以既不进成员，下游也不必去 vocab 找它的词条。」 | — | 过 |
| 9 | 组207·F-1 前半（必改清单④） | 207（`6_06` L389，计数段词形侧） | 「requirement / requirements 全书**只在 criteria 卡的同义词栏里躺着**（课文与 require 卡都没有）」 | 「requirements 全书 **2 处**、都在卡片侧：criteria 卡 `note` 同义词栏（`standards, benchmarks, requirements`）与 appearance 卡 `note` 词伙 `meet appearance requirements`，课文 0 处、require 卡自己也没有；**单数 requirement 全书 0 处**」 | — | 过。逐卡扫 `requirement`：命中的卡片只有 criteria（同义词栏）与 appearance（词伙），共 2 处；两份数据课文侧 0 命中；`\brequirement\b`（不带 s）在两文件 0 命中 ✓ |
| 10 | 组207·F-1 后半（连带引用点） | 207（`6_06` L443，出处清单 1221 行） | 「只用来报备 `requirements` 这个词形**全书唯一一处**在 criteria 卡的同义词栏里（课文没有）」 | 「只用来报备 `requirements` 这个词形在本书的**两处所在**：criteria 卡 `note` 同义词栏（`standards, benchmarks, requirements`）＋ appearance 卡 `note` 词伙 `meet appearance requirements`（已 grep：课文 0 处 …）；appearance 另有卡、不属本组（硬约束 2）」 | — | 过（与 #9 同为一个数） |
| 11 | 载体分配（任务书要求"写清"，非报告必修） | 200（`6_05` L4 文件头 ＋ L188 ＋ L265） | 文件头写「提请拍板 **0 组**」、组内写「载体大概率换到 `delicious` 或 `yummy`」——读起来像不用拍 | 文件头改成「提请拍板 **内容 0 组 ＋ 载体分配 1 项（非内容）**」并点名两问：组 200 挂 `delicious` 还是 `yummy`、组 201 挂 `external` 还是 `outside`；L188 补「`cmp` 里的 `group` 与 `at` 我另跑脚本复验：`tasty-yummy`、`at` 实测 [2, 34]」＋建议载体与「段 34 末本组落地后共挂 2 张」；L265 同步成「建议 `delicious`（备选 `yummy`）……这一个分配要真人拍」 | —（正文行，不受上限约束） | 过。实测：tasty 卡 `cmp.group`=`tasty-yummy`、`at`=[2,34]；yummy、delicious 两卡 `cmp` 均为空 ✓；`at`=[2,34] 的落地表全书只有 1 张（tasty）→ 加本组共 2 张 ✓ |
| 12 | 载体分配（同上） | 201（`6_05` L274 ＋ L351） | 「exterior 卡已带一份表、大概率换到 `external` 或 `outside`」（未记 `at` 实测、未点明要拍） | L274 补两张已落地表的 `at` 实测值（`entrance-entry` [2,39]、`exterior-outward` [2,39]）＋建议载体改「`external`（备选 `outside`）……挂哪一个要真人拍」；L351 补「段 39 末现有 2 张（脚本实测 `at` 都是 [2, 39]），加上本组与组 5 落地后共 4 张，属 §五.9 渲染层事项、不是内容问题」 | — | 过。实测：`at`=[2,39] 的落地表恰为 entrance（entrance-entry）、exterior（exterior-outward）两张；external、outside、inner、interior、internal 五卡 `cmp` 均为空 → 组 202 不涉及换载体（草稿 L360 那句原样成立）✓ |
| 13 | 修复轮自证 | `6_05` L6、`6_06` L6 | 文件头只有起草那一轮的「未碰 `work/辨析审核/`」声明，修复后读起来像假话 | 两份文件各加一行「本文件已经过门禁 2 修复轮（`work/辨析审核/FIX_6_c.md`，依据 `work/辨析审核/audit_c_b6.md`）……」（照 `6_03`／`6_04` 那两份同轮修复的写法），并列明本轮改了什么、默认行与总结句未动 | — | 过 |
| 14 | 内容骨架 | 全部 10 组 | — | **未动**：组标题编号与成员顺序（决定 slug）、`主锚点 = 章N 段M` 字段行、items 表成员行顺序与 7 列形状、`core`、默认行、总结句、表头一句 | 10 条默认行／10 条总结句实测与标称一字不差（见终检第 2 条） | 过 |

## 连带引用点清单（我改的每个数字，在这两份文件里出现过的行）

行号为**改后**当前行号（`6_05`／`6_06` 文件头各加了一行，报告里的旧行号＝本表行号 −1）。

| 改动的数 | 出现位置（已逐处一起改／逐处确认不用改） | 确认结果 |
|---|---|---|
| 句号 **12 → 11** | `6_05` L449（附录唯一一处，已改）；`6_05` L3／L4 文件头不含句号计数；`6_06` L3、L468 写的是「5 组 **10 个**句号」（另一个文件、另一个数） | 11 ✓（`slice41.json` 五组 `sents` 摊平＝11、去重＝11）；`6_06` 的 10 实测 1200/1201/1206/1207/1218/1219/1259/1260/1274/1275＝10 ✓ 未动 |
| 不接桶 **6 → 7**（带名词 6） | `6_05` L317（diff 格：两桶标称 ＋「6 ＋ 7 ＝ 13」）、L349（数据边界：7 处 ＋ 另 6 处带名词）——两处一起改 | 6＋7＝13 ✓；`6_05` 其余 13 的引用点 L274（"那 13 处书证"）、L291（标 13 处清单）、L347（课文 13）、L446（附录判定行"13 处"）**均为 13，不需改** ✓；L337 出处清单「其余 12 处带标记书证」是"13 减 878"，含 1147 ✓ 未动 |
| **1147** | `6_05` L6（本轮新加的修复说明行）、L291（13 处清单里，原本就有）、L317（本轮补进不接桶）、L337（出处清单原句已列）、L349（本轮补进） | 五处一致：1147 属"不接"那一桶 ✓ |
| 「本书唯一 → 课文唯一」 | `6_05` L61（已改）；组 198 区间（L9–96）其余"唯一"字样只有 L63（"是本组三词里唯一一处**课文里的**名词用法"，已自带"课文"限定）与 L81（"jump 侧名词档的**课文**唯一书证"）——都不含"给跳法配程度词"这一命题 | 原句「本书唯一给跳法配了程度词」在全文 **0 命中** ✓；`grep 本书唯一` 只剩两处：L6（本轮自证行里带引号复述旧措辞）与 L447（组 202 附录行"interior 是本书唯一当名词用的"，报告判 OK，我复验属实：inner 5 处、internal 2 处全贴名词前，只有 interior 两处是名词） |
| 「本书译文从没并成同一个中文词 → 本段」 | `6_05` L65（已改）；同文件另三处「本书课文译文从没把…」属**别的组**（L154/L175 adorn 组、L239 delicious 组、L316/L295 外部组），报告逐组判为"引用＋否定且验实为真" | 只改 198 那一处 ✓；`grep '本书译文从没把三词并成同一个中文词' 6_05.md` → 0 命中 |
| `in-house` 的「别的词头」 | `6_05` L408（已改）、L422（已改）、L430（数据边界那句只写「另一处给 `in-house`（406）」，**没称词头**）→ 不用改 | `grep '别的词头' `：L408／L422 内已无「in-house…别的词头」搭配（同词另出现在 L73/L82 等处，说的是 spring／dive／hip-hop，属实、未动） |
| `requirements` **1 → 2 处** | `6_06` L389（已改）、L443（已改）、L452（"本书没写过 … `requirements`（课文）"＝课文 0 处，与 2 处并存 ✓ 不改）、L453（"`requirement` 派生词形（课文 0 处）" ✓ 不改）、L6（本轮新加的修复说明行，写的就是 2 处） | 四处口径统一为"卡片侧 2 处、课文 0 处、单数 0 处" ✓ |
| 载体：200 → `delicious`、201 → `external` | `6_05` L4（文件头）、L188／L265（组 200）、L274／L351（组 201）、L445／L446（附录 组3／组4 行的载体报备，写的是"已带 `tasty-yummy`／`exterior-outward`（同挂 [2,34]／[2,39]）"，实测为真）→ 不改 | 与实测一致 ✓ |

## 报告建议 ≠ 事实来源（我自查后偏离报告的地方，共 3 处）

1. **#6（组 201 L291）**：报告建议改成"在别的卡的 `ex` 与**词伙段**里各 0 次"——但 event 卡词伙段里就有一条 `external events`（报告自己同一句里也写了这条），照抄会造出一条新的虚报。我按实测改成「0 条新文本」＋"这个 0 次不含同义词栏"，并把 exterior／external 各自被哪两卡列了、列了几次写清。
2. **#1（组 198 L61）**：报告建议写"本书**课文**唯一给跳法配程度词的是 637"。我把"课文唯一"这句话自己回数据验了两遍（`jump high`／`jumps high`／`hop high` 在剥标记后的三变体课文里 0 命中；卡侧含跳法＋high 的只有 kangaroo 卡 ex 与 leap 卡词伙），确认后才写，并顺手把反例的出处（kangaroo 卡 ex）就地承认，免得同文再自相矛盾。
3. **#11／#12（载体）**：报告说"草稿建议前者、内容不需要为它改一个字"，但草稿原文只写"大概率换到 A 或 B"，没点明"这一格要真人拍"，文件头还写着"提请拍板 0 组"。我只把话说清（建议载体＋待拍板＋实测 `at` 与张数），成员顺序、切入角、diff 格一律没重排。

## 未改项及理由

**报告标"建议级"的，一律未动**（照报告办、不自己发挥；FIX_5_d 同规则）：

- **组 198·建议级**（L83 引 hip-hop 卡 `m` 写成「n. 街舞；嘻哈文化」，卡上实为「n. 街舞；嘻哈文化；adj. 嘻哈的」，第三档被吞）：报告标"建议级"、未进必改清单 → 未动。事实我核过（卡上确为三档），要补就在门禁 3 一并补。
- **组 199·建议级**（L154「两张 `exZh`」与 L175「三卡 `exZh`」不一致，报告建议按后者统一）：该组判定"通过"，未动。
- **组 202·建议级 2 处**（L408／L432 的「都落『里面的』」报告建议收成「都落『里面』」，因 881 译文是「里面每一个角落」；L375「interior、internal 在别的卡 ex/note 里 0 次」与组 201 #6 同族措辞问题）：都标在"附带两处建议级"档，未动。
- **组 204·建议级**（L158 引 dismiss 卡 `exZh` 截半，缺「，走开了。」）、**组 205·建议级**（L247 agreement 卡 `exZh` 起头省略「他们签署了一份…」）、**组 206·建议级**（L354 说 1262「小聪明」是 `clever`，实测是 clever ＋ trick 两词头一起译）：三处均在"通过"组、报告标建议级 → 未动。
- **组 207·建议级**（L401 `demand` 的 `scene` 写"卡 ex 是干净的水人人都缺"，报告建议改成"干净水的缺口在涨"）：`scene` 是 landing 槽、报告明列"建议级"，且改它会牵动 items 表内容骨架 → 未动。（顺手实测：卡 `exZh` 原文＝「对清洁水的需求正在增加。」，"人人都缺"确实是外加画面，下一位要改就照报告那句换。）
- **6_06 那 3 条"提请拍板"（组 203／204／205）**：报告建议"全部撤下、按批次 5 判据属照写不再上报"，但撤不撤是作者的报备话术、报告也自认"不必返工"→ 文件头小结、各组「提请拍板点」、附录复核索引三处**原样保留**，一字未删。
- **载体分配本身**：`tasty` 已带 `tasty-yummy`、`exterior` 已带 `exterior-outward` 且 `at` 与新组同段——**这是真人要拍的**，我只把草稿写清（#11／#12），没有替它挑载体、没有重排成员、没有改任何内容骨架。
- **判定"通过"的 6 组（199／200／203／204／205／206）卡面内容**：一字未动（199、200 的默认行实测正好 40.0，加任何一个字就 41.0＝BLOCK，本轮压根没碰那两行）。

**报告没点名、我读的时候发现的一处存疑（备案，未动）**：`6_05` L93（组 198 数据边界「计数结论」）写「hop 3 处 / jump **6+6** 处 / leap 2 处」，而同文件 L92 写的是 jump 侧「自身 7 条 ＋ 别的卡 ex 旁证 7 条」——"6+6" 与两种口径（课文 6＋卡侧 8？自身 7＋旁证 7？）都对不上。报告 B/C 两行没扫这一格，我也没动（不属本轮范围，且我不确定作者原意）。建议下一位按实测写：课文 6 处（636、1536、53、290、306、921）＋ 自身卡 ex 1 ＋ 别的卡 ex 7。复核方法：`MARK` 扫 `[[jump…]]` ＋ 逐卡扫 `ex`。

## 终检

1. **门禁 1（唯一被允许跑的自检工具，原样输出；未跑落地器）**：

```
$ cd "/Users/zhoupeng/Library/Mobile Documents/com~apple~CloudDocs/雅思背单词项目" && python3 tools/check_compare_draft.py work/辨析草稿/6_05.md work/辨析草稿/6_06.md
OK   6_05.md
OK   6_06.md

门禁 1: PASSED（全部英文串可回溯）
EXIT=0
```

2. **10 条默认行 ＋ 10 条总结句：小标题标称／`tools/width_rule.width` 实测逐条列（含未改的 6 组）**——本轮一个字都没往这些行上加，20/20 标称＝实测：

| 组 | 文件·行号 | 默认行标称 | 默认行实测 | ≤40 | 总结句标称 | 总结句实测 | ≤30 |
|---|---|---|---|---|---|---|---|
| 198 hop/jump/leap | `6_05` 46／67 | 37.0 | 37.0 | 过 | 25.0 | 25.0 | 过 |
| 199 adorn/embellish/ornament | `6_05` 135／156 | **40.0** | **40.0** | 过（贴顶，本轮未加一字） | 28.0 | 28.0 | 过 |
| 200 delicious/tasty/yummy | `6_05` 220／241 | **40.0** | **40.0** | 过（贴顶，本轮未加一字） | 28.5 | 28.5 | 过 |
| 201 exterior/external/outside | `6_05` 305／326 | 39.0 | 39.0 | 过 | 27.0 | 27.0 | 过 |
| 202 inner/interior/internal | `6_05` 389／410 | 36.5 | 36.5 | 过 | 27.0 | 27.0 | 过 |
| 203 occupation/profession/vocation | `6_06` 50／71 | 38.5 | 38.5 | 过 | 25.0 | 25.0 | 过 |
| 204 dismiss/fire/layoff | `6_06` 136／160 | 38.5 | 38.5 | 过 | 22.5 | 22.5 | 过 |
| 205 agreement/protocol/treaty | `6_06` 225／249 | 39.0 | 39.0 | 过 | 23.0 | 23.0 | 过 |
| 206 cheat/deceive/fraud | `6_06` 317／341 | 38.5 | 38.5 | 过 | 20.0 | 20.0 | 过 |
| 207 demand/request/require | `6_06` 405／429 | 39.0 | 39.0 | 过 | 23.0 | 23.0 | 过 |

  另：`core` 30 格、附录复述行（`6_05` L452、`6_06` L473）本轮均未改，复述值与上表逐位相等（37.0/40.0/40.0/39.0/36.5 ＋ 25.0/28.0/28.5/27.0/27.0；38.5/38.5/39.0/38.5/39.0 ＋ 25.0/22.5/23.0/20.0/23.0）✓。

3. **新写英文串逐条回数据（三变体子串：原样／剥标记取表面形式／剥标记取"词头 表面形式"）**：本轮往卡面新加的 7 条串全部 HIT —— `The kangaroo jumps high in the Australian wild.`、`the wooden door outside`、`meet appearance requirements`、`standards, benchmarks, requirements`、`exterior-outward`、`tasty-yummy`、`in-house`／`accounting`／`audit`；另有 2 条**有意写的反例**（`jump high`、`hop high`）确认两份数据 0 命中，所在行各带"没有／查不到"字样，属 §五.15 放行形状。
4. **"不跳过否定行"严格复查**（自己按 `check_compare_draft.py` 的 units 逻辑重跑、不按 NEG 跳行）：两份文件共 **47** 条 code span 查不到（`6_05` 18 ＋ `6_06` 29），与改动前逐条同一批（本轮新增的只有 #1 那两条反例 `jump high`／`hop high`）——全是「不写的」清单与"本书 0 处"式句子里有意举的反例；反向 grep 逐条确认为 0 命中，未新增任何"正证据查不到"的串。按默认口径（跳过 NEG 行）复跑：`6_05` 缺 0、`6_06` 缺 0 ✓。命令：把 `tools/check_compare_draft.py` 的 `haystack()`／`units()`／`NEG` 原样 import 后逐行扫两遍（一遍跳 NEG、一遍不跳）。
5. **结构自检**：`^## 组` 计数 5＋5、编号与成员顺序未增删未换位（slug 不变）；`^主锚点 = ` 恰 10 行（每文件 5 行）；10 张 items 表 30 行成员列数＝表头 7 列、diff 表 5 列、原文表 3 列（脚本逐行数 `|`：两文件"列数与表头不齐"命中数 0）。
6. **改动条数自查（逐条 `count()` 过，不是估的）**：旧字样「本书唯一给跳法配了程度词」「本书译文从没把三词并成同一个中文词」「共 6 处都不接」「12 个句号」「in-house\`（406，别的词头」「全书唯一一处在 criteria 卡」在两文件里全部 **0 命中**；新字样「课文\*\*里给跳法配了程度词」「本段\*\*三处译文」「共 7 处都不接」「\*\*11 个\*\*」「未打标记、也没有卡」「requirements 全书 \*\*2 处\*\*」「两处所在」各恰好 **1 次**。（`grep 本书唯一` 仍剩 2 处：`6_05` L6 是本轮自证行里带引号复述旧措辞、L447 是组 202 附录行"interior 是本书唯一当名词用的"——报告判 OK、我复验属实；`grep 这个别的词头` 剩 1 处是组 199 的 `decorate`，那是**真有卡**的词头，报告判通过。）
7. **范围自证**：`git status --porcelain` 共 15 行——8 份 `M`（`6_01..6_06`、`6_08`、`6_09`，其中除我这俩之外都是并行修复代理改的，`6_07` 不在改动列表里）＋ 7 行未跟踪（`audit_a..d_b6.md` 四份与 `FIX_6_a/b/c.md`）；我只写了 `6_05.md`、`6_06.md`、`work/辨析审核/FIX_6_c.md` 三个文件。`git diff --stat -- shadow/data scripts tools index.html shadow/index.html` 输出为**空**；`work/辨析审核/land_preview.json` mtime 仍是 09-20 23:24:49（落地器任何模式都没跑）；无任何 git 写操作（分支仍 `agent/qoder-compare-batches2-6-0920`，`git log --oneline -3` 顶一条仍是作者起草那次 `507f26f`）。
