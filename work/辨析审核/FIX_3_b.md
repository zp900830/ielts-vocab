# 批次 3 · 切片 3_03 + 3_04 辨析卡草稿 —— 门禁 2 修复台账（b 线）

对应审核报告：`work/辨析审核/audit_b_b3.md`（10 组：BLOCK 0 / MISMATCH 3（组内共 5 个必改点）/ 通过 7）。
本轮是**作者侧照报告改稿**，不是再审。改动文件只有两份草稿：`work/辨析草稿/3_03.md`、`work/辨析草稿/3_04.md`，加本文件。
未碰 `shadow/data/*`、`shadow/index.html`、`scripts/`、`tools/`、别人的草稿与审核报告原文；未做任何 git 写操作；**未跑 `tools/land_compare.py`**（含 `--dry-run`/`--check`）。
宽度一律用 `python3 -c "...from width_rule import width..."` 复量（未手算、未 `--bless`，避免改 `tools/width_samples.json`）。

## 一、5 个必改点（报告点名，全部改毕）

| 报告编号 / 组 | 改前写法 | 改后写法 | 改后宽度（默认行 / 总结句，实测） | 这条现在过不过 |
|---|---|---|---|---|
| ③类·附录计数算错 —— `3_03 组5`（idx 66）F 行 + 文末复核索引：「8 条词伙中 6 条与课文同源」 | 附录：「10 个成员词全书各只 1 现（对每词扫全 6 章的 `[[词头:` 标记计数所得）；**8 条词伙中 6 条与课文同源**（列了 8 项，其中 makeup 只列 1 条）」 | 附录：改为「按口径 13 分两档 —— 带 `[[词头:…]]` 标记各 1 处 ＋ 未打标记 0 处（逐词扫全 6 章所得；encouragement 内嵌的 gem 属子串撞词不计）」＋「词伙共 **9 条**、**9 条全部与课文同源**，**自列加数**：small fridge=691、old refrigerator=690、blue gem=717、bright jewel=717、dance costume=723、gentle grace=725、simple elegance=725、light soft makeup=727、**soft makeup=727**（加数 1×7＋2＝9；garment、cosmetics 两卡 `note` 为空各 0 条）」。加数已对 `shadow/data/vocab.json` 逐卡复核：9 条词伙、9 条同源，与自列加数逐项吻合 | 组 5 默认行 **40.0**／总结句 **23.0**（本条不涉两行文本，标称未变、复量相符） | **过**（原「8/6」连自己列出的 8 项都对不上，现自列 9 项＝9 条，计数自洽） |
| ②类·旁证不精确 —— `3_03 组5`（idx 66）C 行：本书另有 tropics、**cattle** 等卡也这样标（复数词形） | 「（本书另有 tropics、cattle 等卡也这样标，非孤例）」 | 「（本书另有 tropics、**bacteria** 两张卡也这样标，非孤例；**cattle 卡标的是「牛（复数名词）」、用词不同，不算这一档**）」——照报告给的替换方案执行，并把 cattle 为何不算写明。已核 `vocab.json`：bacteria `m`＝「n. 细菌；复数词形」、tropics `m`＝「n. 热带地区；复数词形」、cattle `m`＝「n. 牛；牛（复数名词）」 | 同上（组 5 40.0／23.0，未涉） | **过**（"非孤例"结论仍成立，tropics＋bacteria 两张卡足撑） |
| ②类·虚报"本书没有" —— `3_04 组2`（idx 68）C 行误报：`seat belt` 被列进"本书没出现过的串" | 「`seat belt`、`shoulder strap`、`belt bag`、`punch a hole` 这类本书没出现过的串一律不写」 | 「`shoulder strap`、`belt bag`、`punch a hole` 这类本书没出现过的串一律不写；**`seat belt` 单独报备**（审核报告点名的订正）：本组两张卡上没有、课文里 seat belt 0 处，**但语料里确实出现过 —— compel 卡与 oblige 卡的 `ex` 各写着一句安全带（「法律强迫司机系安全带。」「法律迫使司机系安全带。」），英文都是 `wear seat belts`**，所以不能算"本书没有"；它说的是车上那条带子、与本组两词卡无关，故仍不进表」 | 组 2 默认行 **37.5**／总结句 **17.5**（未动两行本体） | **过**（改后每一串都可 grep：`wear seat belts` 实测在 compel/oblige 两卡 `ex`；`shoulder strap`、`belt bag`、`punch a hole` 复核仍 0 处） |
| ④报告组 2 建议②（belt 卡 `m` 档位） —— `3_04 组2`（idx 68） | `sense` 槽写「腰带；皮带；传送带」，未交代卡上原文是分两个 `n.` 档 | 在「义项那格要说清」段末补一条给落地用的说明：「belt 卡 `m` 原文是两个词性档 —— `n.` 腰带；皮带 ＋ `n.` 传送带；本组 `sense` 槽合并写成了「腰带；皮带；传送带」，落地时要保留档位的划分，别让"传送带"被读成第三个中文近义词」。已核 `vocab.json`：belt `m`＝「n. 腰带；皮带；n. 传送带」 | 组 2 37.5／17.5（未涉） | **过**（正文表格未动，只补护栏） |
| ①类·过度概括（被自家下文证伪） —— `3_04 组5`（idx 71）C①：diff「本书里它被谁怎样」hut 行「本书 hut 侧 **3 条全在『破 + 有人住』**这条线上」 | 「本书 hut 侧 3 条全在"破 + 有人住"这条线上，谁都没扫过它」 | 「本书 hut 侧 3 条**各说一样、合不成一条线**：769 只说它破、卡 ex 只说是谁搭的、crude 卡 ex 才同时给了"破 + 有人住"（**这条线只有 1 处书证**）；三条里谁都没扫过它」——按报告给的最小清单降级为按条写，并与同组「用在哪」格（说它破／说谁搭它／说谁住在里头）、文件头小结（其余 2 条只说了"破"和"孩子搭的"）不再自相矛盾 | 组 5 默认行 **38.0**／总结句 **23.0**（未动两行本体） | **过**（同行矛盾已消；"住人"线明写只 1 处，未升级为禁令） |
| ①类·过度概括 —— `3_04 组5`（idx 71）C②：表头「本书另外 3 处**全在打扫、通风、借人**」（三词枚举漏了 1444） | 「（`the leaning shed`，本书另外 3 处全在打扫、通风、借人）」 | 「（`the leaning shed`，本书另外 3 处**也全在人进进出出：打扫、通风、借人、跟着帮手往里走**）」—— umbrella 换成"人进进出出"，四件事对上 1418／1457／1444 三条（1457 同时给借出与打扫），补回原先漏掉的 1444「跟着每个少年帮手去棚里」 | 组 5 38.0／23.0（表头不受宽度门禁，默认行/总结句实测未变） | **过** |

### 顺带 1 处（报告未点名，与上表 C② 同因；不想扩大改动可一键回退）

| 位置 | 改前 | 改后 | 宽度 | 判断 |
|---|---|---|---|---|
| `3_04 组5`「不写的」行（举例句里复述那句计数结论） | 所以只能写成"本书 shed 侧 4 条全在推门、打扫、通风、借人这条线上"这种计数结论 | 所以只能写成"本书 shed 侧 4 条全在人进进出出这条线上（推门、打扫、通风、借人、跟着帮手往里走）"这种计数结论 | 组 5 38.0／23.0（未涉两行本体） | 与已改表头同源的另一句"全在…线上"，同一枚举也漏 1444；补全后表内三处 shed 概括（表头／diff／不写的）措辞一致。**报告 F 行只点名 C 行两处，故此项标注为顺带，可回退。** |

## 二、未改的（逐条给理由）

| 报告条目 | 为什么不改 |
|---|---|
| `3_03 组1`（idx 62）备注：段 6 `paraZh` 引文漏句末「。」 | 报告判 OK 且注明「全片同类引用一致，属体例」，非事实错误。 |
| `3_03 组2/3/4`（idx 63/64/65） | 报告判 OK、明写「无需作者改」。组 3 备注（`scene`「假面舞会刚过去」属画面加工，报告称"不违 A–F，落地前可改"）与组 4 提请拍板项（"分工一半靠卡 ex"报告核为已如实标注、建议接受现表）均属落地/拍板层，按硬约束不在改稿范围，未动。 |
| `3_03 组4`（idx 65）E 行「表头出现『优雅』」 | 报告核为「该段确实撞（725 两词同句、`sentZh` 自己并译）」，放行；不动。 |
| `3_04 组1`（idx 67）两条建议（① `eg` 缩成 `a small buttoned purse`；②「修饰词说哪样」两行对照收紧） | 报告标「建议（非必改）」且判定 OK；硬约束要求 OK 组不顺手改。 |
| `3_04 组3`（idx 69） | 报告判 OK（本片最薄，按口径 7 不退），无改点。 |
| `3_04 组4`（idx 70）「它前面**只会**站 the / a / this 这类限定词」建议 | 报告判定 OK、该条列为「1 处建议」；属"该退不退/措辞"类，交由作者侧之外的裁决线处理，本轮未动（若要采纳，最小改法：收成"本书 2 处前面都站限定词，this 本书未出现"）。 |
| 作者提请拍板的 7 处（`3_03` 组 1/4/5、`3_04` 组 2/3/4/5） | 报告结论是**一组都不该退**，故全部**保持出卡**，未新增任何退回门槛、未删卡、未降级。 |
| 全部 10 组的默认行与总结句 | 报告 D 项核「20 个标称值与实测逐位相符、无超限」，本轮 5 个改点全在表头／diff／出处／附录／不写的 五类位置，两行本体一字未动；复量后仍全绿（见下）。 |

## 三、门禁复跑（改稿后）

```
python3 tools/check_compare_draft.py work/辨析草稿/3_03.md   → OK 3_03.md ／ 门禁 1: PASSED（exit 0）
python3 tools/check_compare_draft.py work/辨析草稿/3_04.md   → OK 3_04.md ／ 门禁 1: PASSED（exit 0）
```

- 另按"关掉否定行跳过"的严格模式复跑否定行（同一把 `check_compare_draft.py` 的 SPAN/units/haystack，只是不跳 NEG 行）：`3_03` 查无 6 条、`3_04` 查无 15 条，与改稿前**逐条同一批**，全部是各组「不写的」里明写的反例；`seat belt` 改后仍可回溯（`wear seat belts` 在 compel／oblige 卡 `ex`），15 这个数未变，文件头小结那句"15 条"仍成立。
- 宽度复量（`width_rule.width`，逐组 20 个读数：10 默认行 + 10 总结句）：标称与实测 **20/20 逐位相符**，默认行实测区间 33.0–40.0（上限 40，3_03 组 1、组 5 压线 40.0 未加字）、总结句 17.5–26.0（上限 30），**超限 0 条**。
- 两份草稿文件头各加 1 行改稿溯源（写明按 `audit_b_b3.md` 改了哪几处、指向本台账），卡片正文表格未因此变动。
