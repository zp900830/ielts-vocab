# 修复记录 · 批次 5 前半 `5_01.md` + `5_02.md`（依据 `work/辨析审核/audit_a_b5.md`）· 2026-09-21

执行人：修复代理（FIX_5_a）。**只改了两份草稿 `work/辨析草稿/5_01.md`、`work/辨析草稿/5_02.md` 与本文件**；未碰 `shadow/data/*`、`scripts/*`、`tools/*`、`index.html`、`shadow/index.html`、别人的草稿（`5_03..5_09`、`4_*.md`）与任何 `audit_*.md`；**未跑 `tools/land_compare.py` 的任何模式**（含 `--dry-run` / `--check`，`work/辨析审核/land_preview.json` 未被本次触碰）；无 git 写操作（只跑过 `git status` 确认工作区）。宽度一律用 `tools/width_rule.py`；英文串与存在性断言自己回 `shadow/data/sections.json`、`shadow/data/vocab.json` 复grep 后才落地。

本次范围：BLOCK 1（组140）＋ MISMATCH 2（组141、组142）＋ 片级附带 `subheads[4][17]` → `[18]` 共 10 处。组标题编号与顺序、`主锚点 = 章N 段M` 字段名一字未动。

| # | 报告编号 | 组 | 改前 | 改后 | 改后宽度 | 过不过 |
|---|---|---|---|---|---|---|
| 1 | 组140·E 行 **BLOCK**（本批唯一 BLOCK） | 140（5_01 组5 estimate / evaluate） | 默认行 `estimate = 猜省多少（estimate savings），evaluate = 给旧表估价` —— 「估价」正是 worklist 共享义项、也是两卡 `m` 字面（estimate `m`：v. 估算；n. 估价 ／ evaluate `m`：v. 估价；评价），违 §五.4 且落在落地唯一露出的那一行 | 换成具体画面钩子（挪位置不算改）：`estimate = 猜省多少（estimate savings），evaluate = 店主看那块旧表`。事实自查：evaluate 卡 `ex` = `The shopkeeper will evaluate the old watch.`、`exZh` = 「店主将估价这块旧手表。」（`shopkeeper` sections 0 处 / vocab 1 处、`old watch` 0 与 1 —— 与草稿原「只在这张卡上」的归属一致，钩子取卡上那件真东西，不脑补） | **33.0**（≤40；小标题标称已由 31.0 改成本次实测 33.0） | 过 |
| 2 | 组140·随 #1 的连带更正（报告未列，但被我这条改动改成失实的一句） | 140（5_01 组5） | 「数据边界」原句：「默认行改成**段里真在发生的事**」 | 改为「默认行换成两侧各一个画面（estimate 侧取 1399 那个还没发生的省头，evaluate 侧取卡 ex 那位店主与那块旧表——**这一处书证在卡上、不在本段**）」——1400 段里没有店主也没有旧表（`shopkeeper` 课文 0 处，已 grep），留着原话就成了新的虚报 | —（非落地字段） | 过 |
| 3 | 组140·随 #1 的标称值同步 | 140（5_01 附录） | 文末复核索引行「默认行 35.5 / 35.5 / 30.0 / 38.0 / **31.0**」 | 「… / 38.0 / **33.0**」——避免附录与组内小标题打架 | — | 过 |
| 4 | 组141·片级附带 | 141（5_02 组1 fault / flaw） | 「共现位置」与「出处清单」两处 `subheads[4][17]`：下篇·发明比赛 | `subheads[4][18]` | — | 过（我自己读 `sections.json` 章4 `subheads`：共 36 项，`[17]` = `''` 空串、`[18]` = `下篇·发明比赛`；`5_01.md` 同片写 `[18]`，确为笔误非口径） |
| 5 | 组142·MISMATCH（E 行义项照抄） | 142（5_02 组2 juvenile / teenage） | 成员表 `core` 给 teenage 写「**十来岁那几年**」——teenage 卡 `m` = 「adj. 青少年的；十来岁的」的字面复述，违 §五.4 | 改「**扫巷的那批小辈**」（juvenile 侧「还没成年那拨」不动，对比关系不丢）。事实自查：课文 1446 = `No noisy [[gang:gang]] blocked the path; [[teenage:teenage]] volunteers swept the [[lane:lane]] daily.`、`sentZh` = 「没有喧闹的帮伙挡路，青少年志愿者每天扫巷。」——「扫巷」是译文原词、「swept the lane」是课文原串（标记切断处按 §门禁口径拆「表面形式／词头 表面形式」两种变体查得过）；「小辈」不是任何卡的 `m` 字面 | core 7 字（≤12） | 过 |
| 6 | 组142·片级附带 | 142（5_02 组2） | 2 处 `subheads[4][17]` | `subheads[4][18]` | — | 过 |
| 7 | 组143·片级附带 | 143（5_02 组3 kin / relative） | 2 处 `subheads[4][17]` | `subheads[4][18]` | — | 过 |
| 8 | 组144·片级附带 | 144（5_02 组4 hostess / mistress） | 2 处 `subheads[4][17]` | `subheads[4][18]` | — | 过 |
| 9 | 组145·片级附带 | 145（5_02 组5 client / customer） | 2 处 `subheads[4][17]` | `subheads[4][18]` | — | 过 |

片级附带合计 **10 处全改**：改后 `grep -c 'subheads\[4\]\[17\]' work/辨析草稿/5_02.md` = **0**，`[18]` = **10**；`5_01.md` 的 `[18]` = 10（未动，本来就是对的）。

## 未改项及理由

- **组144 表头那句「共同义项本书译文两处都照写了」**（报告"需要他拍的第 2 条"）：**未动**。我自己回数据核了报告用作放行依据的那句话，属实：章4 段29 `sentZh` = 「笑盈盈的女主人倒茶，严厉的女房东洗杯。」／「院子的女主人和忙碌的家庭主妇一起拖地。」——1452、1453 两处译文**都**写「女主人」，属"真撞"，不是 §五.11 拦的"宣称而该段实际不撞"；`core`、默认行、总结句都不含「女主人」（复量见下表）。等他把"真撞即可写"钉死，后面几批照办即可，我不替他改判。
- **「卡上另一档 0 处」这一格的体例（报告拍板第 3 条 + 体例建议①②③）**：**未动**（报告自标"建议"、不在最小清单）。顺手把报告的纠正核了一遍，站得住：这一格确实不止作者说的"三组"——5_02 组1（「卡上三档各用上了吗」＝「缺陷」档）、组2（「卡上还给了哪一档、用上了吗」＝「幼稚的／n. 少年」档）、组4（「卡上另一档用上了吗」＝女招待／情妇／女教师），加 5_01 组3（规格／评估档）、组4（estimate 的 n.「估价」档），**共 5 组**在按这个形状用；`5_02.md` 头记与组4 提请拍板点仍写"三组一起改"，按字面低估了。**留待他拍板后一并改，本轮不动**（若定"全部挪进数据边界"，牵动的是这 5 组，不只是 5_02）。
- **6 条拍板请求（报告结论：全部"不退"）**：内容一字未动，只做了报告点名的措辞/索引修正。
- **组140 evaluate 的 `core`「有人来看值不值」、diff「碰一件旧东西值多少」**：报告判 E 行"本组唯一必改点"是默认行，未列 `core`，**未动**。我复核过它的边界：「值不值」不是两卡 `m` 的字面词（字面是「估价／评价」），与 5_01 组2 总结句「乐器」那条被放行的"引用卡上词伙、非 `m` 字面"同一档。
- **组142 总结句「本书两处都站在名词前，juvenile 卡上还兼着幼稚」**：报告未列，**未动**。**我认为这是本批唯一报告可能判轻了的地方**（§五.4 若按字面推到总结句，「幼稚」正是 juvenile `m` 的字面「幼稚的」）；我按报告自己在 5_01 组2 用的判据复核为可留——这一句是"卡上还兼着"的**带归属引用**（明示出处是卡，不是拿义项当钩子），与同批被放行的「instrument 另有一档乐器」同形。已在本节报备，等他一句话说死要不要连总结句也一律换掉。
- **判可落地的 7 组（组136–139、141、143–145）除片级附带外的内容、全部 diff 表、出处清单、数据边界、"不写的"清单**：一字未动（报告逐条给了正面复算结论）。
- **全局句号编号**：报告明示本审核不改编号（切片包 `sents` 是 0 基，与批次 4 已落地写法一致），**未动**。
- **5_01 附录那句"已用 `tools/land_compare.py` 的同一套取值路径复量，只读、未跑任何写盘模式"**：前一轮作者留下的文字，报告未质疑，我也**没跑**那个脚本（任何模式），故既不删也不背书，原样保留。

## 终检

1. 门禁 1（原样输出，改稿后跑）：

```
$ python3 tools/check_compare_draft.py work/辨析草稿/5_01.md work/辨析草稿/5_02.md
OK   5_01.md
OK   5_02.md

门禁 1: PASSED（全部英文串可回溯）
```

2. 范围内 10 组默认行 / 总结句全量复量（`tools/width_rule.py`；标称＝实测，无一条超 40 / 30；含未改的组，确认没人手滑改坏）：

| 组 | 默认行那一行本体 | 标称 | 实测 | 总结句那一行 | 标称 | 实测 |
|---|---|---|---|---|---|---|
| 136 device/equipment | `device = 连着厨房电器那台，equipment = 晚上共用那堆（shared equipment）` | 35.5 | 35.5 | device 是一台台数得清的，equipment 是一堆。 | 21.5 | 21.5 |
| 137 instrument/tool | `instrument = 锋利那件（sharp instrument），tool = 手里那把（hand tool）` | 35.5 | 35.5 | instrument 另有一档乐器，tool 另有一档线上的。 | 23.0 | 23.0 |
| 138 assess/gauge | `gauge = 那把黄铜量规（brass gauge），assess = 老师来看那一眼` | 30.0 | 30.0 | 「评估」这两个字，本书只给了 assess 的两句。 | 22.0 | 22.0 |
| 139 compute/estimate | `compute = 纸上的账（compute costs），estimate = 还没省的（estimate savings）` | 38.0 | 38.0 | compute 算已经花掉的，estimate 猜还没省的。 | 21.5 | 21.5 |
| 140 estimate/evaluate（**本次改**） | `estimate = 猜省多少（estimate savings），evaluate = 店主看那块旧表` | 33.0 | 33.0 | estimate 猜省多少，evaluate 看图纸评好坏。 | 21.0 | 21.0 |
| 141 fault/flaw | `fault = 算到谁头上（his fault），flaw = 东西上那道纹（a thin flaw）` | 33.5 | 33.5 | fault 说这事怪谁，flaw 说东西上有道纹。 | 19.5 | 19.5 |
| 142 juvenile/teenage | `juvenile = 小狗跟着的帮手，teenage = 扫巷的志愿者（teenage volunteers）` | 35.5 | 35.5 | 本书两处都站在名词前，juvenile 卡上还兼着幼稚。 | 23.5 | 23.5 |
| 143 kin/relative | `kin = 近旁那一片（close kin），relative = 远方那一位（one far relative）` | 36.0 | 36.0 | kin 是一大片人，relative 数得出一位。 | 18.5 | 18.5 |
| 144 hostess/mistress | `hostess = 端茶迎客那位（poured tea），mistress = 这院子归她管（of the yard）` | 38.0 | 38.0 | hostess 迎上门的客，mistress 管这一家。 | 19.5 | 19.5 |
| 145 client/customer | `client = 上门问事那位（a shy client），customer = 进店买东西（shop customer）` | 38.5 | 38.5 | customer 在柜台付钱，client 找律师办事。 | 20.0 | 20.0 |

3. 义项原词泄漏复查（两份草稿的 `core`／默认行／总结句三个槽）：「估价」「十来岁」在默认行与 `core` 已全部为 0（`grep '给旧表估价\|十来岁那几年'` → 无残留），其余出现处均为 `sense` 槽、被否定的表头、或"卡上第几档本书 0 处"式引用。10 个 `core` 逐条列宽：9/9/8/7/8/8/8/8/8/7（5_01）、5/6/6/**7**/7/7/8/6/6/7（5_02），全部 ≤12 字。
4. `git status --porcelain`：只有 `work/辨析草稿/5_0*.md`、`work/辨析审核/audit_*_b5.md` 等未跟踪项，**无数据 / 代码 / 测试文件被改**；未做任何 git 写操作。
