# 修复记录 · 批次 2 切片 04 / 08 / 09（依 `work/辨析审核/audit_d.md`）

修复人：内容落地前校稿代理（非草稿作者）。改的文件只有三份草稿：
`work/辨析草稿/2_04.md`、`work/辨析草稿/2_08.md`、`work/辨析草稿/2_09.md`。

未动 `shadow/data/*`、`shadow/index.html`、测试目录、`BRIEF.md`、`AUDIT_BRIEF.md`、别人的草稿、审核报告原文；
未做任何 git 写操作（只跑 `git status` / `git log` / `git diff` 只读命令）。

内容结论（表头、钩子、差异维度、出处判定）**一处未改**；改的是记账值、落地字段格式、以及已被 BRIEF 定死的口径复述。

---

## 一、审核点名的 2 处 MISMATCH

| # | 位置 | 报告原话摘要 | 改法 |
|---|---|---|---|
| 1 | 组 50（`2_09.md` 成员表 expect 行）· D 宽度记账 | core「等着谁或哪样东西到位」标称 `（9）`，实为 10 个汉字，"落地时应把该行改成（10）" | 已把该行标称值改成 `（10）`，中文钩子一字未动。同组另三条 core（8 / 8 / 9）复算无误，未动 |
| 2 | 组 51（`2_09.md`「提请拍板」第 2 条）· 标签口径判断错 | "false 该改 true"的建议不成立：`all_synonyms` 按**双向互点**算，本组是单向指点，false 是算法正确输出 | 已**撤回**该建议，改写为："本组为单向同义指点，all_synonyms 按互点口径为 false 是正确的"；并补一句给落地侧 —— 真正的选项闸门是 `confusable_groups.py` 里那条单向 OR 同义词黑名单（pastime → recreation 单向就已命中），下游不要拿 all_synonyms 当选项闸门。正文「证据强度」里那句"两卡却互点同义词（单向：pastime → recreation），这一处口径要请你确认"同步改成"false 没有标错"的陈述 |

组 51 的 diff 表、出处清单、默认行、总结句按报告结论**未改**（报告本身写明"内容层面草稿并未因此写错任何一行"）。

---

## 二、12 组主锚点（落地用 `[章, 段]`，一行一组）

取自各节草稿自己那张「该段原文」表所在段（= 切片 `paras` 第一个共现段），未重新推算。
另用 `shadow/data/sections.json` 独立复核过一遍：12 个锚点段**全部把该组成员词都标记在段内**，号段与 `audit_d.md` 自算结果逐组一致。

```
组 25  eminent / outstanding      主锚点 = 章1 段3    [1, 3]     全局句 357–362   成员句 359 / 361
组 26  headmaster / principal     主锚点 = 章1 段4    [1, 4]     全局句 363–368   成员句 365（同句）
组 27  professor / tutor          主锚点 = 章1 段4    [1, 4]     全局句 363–368   成员句 367 / 368
组 28  canteen / dining hall      主锚点 = 章1 段7    [1, 7]     全局句 381–386   成员句 383（同句）
组 29  story / tale               主锚点 = 章1 段8    [1, 8]     全局句 387–392   成员句 388 / 389
组 45  empire / imperial          主锚点 = 章1 段30   [1, 30]    全局句 521–526   成员句 525 / 526
组 46  prosperity / thrive        主锚点 = 章1 段32   [1, 32]    全局句 533–538   成员句 535（同句）
组 47  oral / verbal              主锚点 = 章1 段33   [1, 33]    全局句 539–544   成员句 543 / 544
组 48  narrate / tale             主锚点 = 章1 段36   [1, 36]    全局句 557–562   成员句 558 / 560
组 49  interpret / paraphrase     主锚点 = 章1 段36   [1, 36]    全局句 557–562   成员句 559 / 560
组 50  anticipate / expect        主锚点 = 章1 段38   [1, 38]    全局句 569–574   成员句 574（同句）
组 51  pastime / recreation       主锚点 = 章1 段39   [1, 39]    全局句 575–580   成员句 575 / 576
```

主锚点写法现状：
- `2_09.md`：原有「主锚点段 = 章 1 段 38 / 39」两行，统一成 `主锚点 = 章1 段38` / `主锚点 = 章1 段39`（与 §5 落地口径同名同形）。
- `2_08.md`：五组各有「**共现位置 / 主锚点**」一行但没写章段值，补成 `**主锚点 = 章1 段30**`（32 / 33 / 36 / 36 同理）。
- `2_04.md`：五组原本**完全没有**这一行（只有小节标题里的 `段 [1,3]`），每组新增一行「**共现位置 / 主锚点**」，含 `主锚点 = 章1 段N`、全局号段、成员句、卡挂哪一段末。

段内排序（BRIEF §5.9）：段 [1,4] = 组 26 → 组 27；段 [1,36] = 组 48 → 组 49；段 [1,8] = 组 29 → 切片 05 的 idx 30（diary / journal）。三处均按 worklist_idx 升序，已在正文与索引里各写一句陈述。

---

## 三、格式对齐（让门禁 3 的机检能量到，不算改内容）

1. `2_04.md` 五个小节标题从 `## 1.`～`## 5.` 改成 `## 组 25 ·`～`## 组 29 ·`（带 worklist_idx）。
   原因：`tools/audit_ledger.py` 认组要求标题形如 `## 组 N ·…`，旧写法**整份文件解析出 0 组**（既不报 no-line 也不报超限，等于漏检）。
   连带把正文里指自己切片的三处交叉引用改掉：组 3 → 组 27（组 26 节内）、组 2 → 组 26（组 27 节内两处）、口径第 9 条「组 2、组 4」→「组 26、组 28」。
   「批次 1A 组 1 / 组 3 / 组 4」是引别的切片，未动。
2. `2_09.md` 两个小节标题 `## 组 1 ·` / `## 组 2 ·` → `## 组 50 ·` / `## 组 51 ·`；口径里的"组 1 = 574、组 2 = 575/576"同步改成组 50 / 组 51。
3. 「默认那一行」标题统一为 `### 默认那一行（实测宽度 X / ≤40）` + 代码围栏：`2_04.md` 五条原为 `### 默认显示的那一行（…）`；`2_09.md` 两条去掉标题里的「summary「简单记」」定语（该映射改写在片头口径里，信息不丢）。`2_08.md` 五条本来就是这一形，未动。
4. 「总结句」标题统一为 `### 总结句（实测宽度 X / ≤30）` + 一行引用：`2_04.md` 五条原为 `### 总结句（≤30 字，实测 X）`（无"宽度"二字 → 标称值取不到）；`2_08.md` 五条原为 `### 总结句（≤30｜实测 X）`；`2_09.md` 两条原为 `### 总结句（实测 X / ≤30）`。引用行本体一字未动。
5. 片头「3 行小结」各补一句门禁 2 结论与本次校订范围（保持 3 行结构不变），文末索引表加一列「主锚点（落地用 `[章, 段]`）」。
6. 另外修掉两处**让 `tools/land_compare.py` 认错小节的标题碰撞**（只是把引用语／表caption 换个说法，句子内容与出处一字未动）：
   - 组 25 段内括注原写"……见**差异维度表**最后一行"，落地器的 `find('差异维度表')` 会先撞上这句括注、然后撞到下一个 `###` 就报"找不到差异表" → 改成"见本节末尾差异表最后一行"。
   - 组 27 的成员句表 caption 原是 `**成员句原文**`，被落地器的 `^\*\*成员` 当成「成员（items）」表头，于是报了"找不到成员表" → 改成 `**该段原文（章1 段4 的两个成员句；整段 363–368 的表见组 26，同段不重复贴）**`（顺带把该组的原文表名补齐，主锚点口径不变）。
   两处改完后 `--check` 模式对这两组不再报错（见第八节实跑原文）。

---

## 四、把"要你定口径"改成陈述（BRIEF §5.9 / §5.6 / §5.7 已定死）

| 位置 | 原文 | 改后 |
|---|---|---|
| `2_04.md` 小结第 3 条 ⑤ | "段 [1,4]、段 [1,8] 各被两组同时当锚点，挂段末会撞位（详见文末索引）" | 降为"属 BRIEF §5.9 的常态，见文末「同段多卡」一行" |
| `2_04.md` 文末「撞位报备（要产品侧决定…）」三条 | "需要在落地阶段决定'一段一张'还是'段末叠放'" | 整节改名「同段多卡（BRIEF §5.9 已定口径，本文件不再提请拍板）」，逐段写明挂几张、按 worklist_idx 升序、各自切入角、渲染由落地侧做 |
| `2_04.md` 组 26 / 组 29 段内括注 | "卡挂段末会撞位，见文末报备" | 改为陈述（同段 2 张是常态，本组只讲哪个切入角） |
| `2_04.md` 组 26 / 组 27 节内「要请人拍板的一点」 | "若他要求'辨析必须撞同一义项'，这组应退回" / "建议连组 2 一起由人定去留" | 改成「报备一点（不提请拍板）」+ 引 §5.6 的处置与门禁 2 的核实结论；判定句不再请求去留 |
| `2_04.md` 组 25 / 组 28「证据偏薄，请人过目」「要请人拍板的一点（本组唯一的结构性问题）」 | 同上 | 改「报备」并引 §5.7（实测 216/220 组同档，薄不是退回理由） |
| `2_08.md` 小结 + 文末「待拍板 1（组 48 与组 49 抢同一个位置）」 | "要合并还是挪走一张，请他定"，并给出"合并成 4 词大表 / 挪到前一段"两个方案 | 关掉：改为「同段多卡（§5.9 已定口径，不再提请拍板）」的陈述 —— 本文件不做合并、也不挪挂段，两张各写各的切入角。原「待拍板 2 / 3」降为「报备 1 / 2」（§5.6 / §5.7） |
| `2_09.md` 组 50「提请拍板」1 | "**要不要保留这组**请你定" | 改「报备（不提请拍板）」+ 引 §5.7 与门禁 2 的"可落地"结论；第 2 条（给 anticipate 补词伙）明确划到落地侧走正常流程，不在草稿里动数据 |
| `2_09.md` 组 51「提请拍板」 | 见第一节 MISMATCH 2 | 同表处理 |

---

## 五、分歧（不认同的审核结论：先按它改了，这里说明）

1. **报告"全局事项 3"的 E 项字面严判**：报告自己承认"若按 AUDIT_BRIEF E 的字面，12 组全 BLOCK"，并采用口味基线判全过。我按报告的宽松结论走（未动 12 条表头），但提醒：这 8 组表头里带引号的共享义项词（如"这一段'杰出'出现了两次"）与 BRIEF §5.11 的放行理由**只覆盖了"引用+当场否掉"**，而组 25 的标题是"这一段'杰出'出现了两次：一次夸…一次夸…"——它没有否定，只是把义项词当**叙述起点**。我倾向维持通过（默认行与 core 确实 0 处照抄），但如果他要点的是"标题里一个义项字都不许出现"，这 8 条需要单独摘掉引号词，请一句话定。
2. **MISMATCH 1 的量级**：报告说 core 标称（9）与实测 10 差 1、"未超 1.0 → 记 MISMATCH 非 BLOCK"。我照改成（10），但记一句：core 的 ≤12 是硬门、标称值只是给人看的记账，落地器读的是文本本身，所以"下游照标称值做排版预算会偏"的风险其实只发生在**人工誊写**环节；真要防，应该是机检里加一条 core 汉字数复算，而不是要求草稿手标常对。属代码/口径问题，不在本次改动范围。
3. **BRIEF §5.9 的落地口径**：报告把"组 48 / 组 49 同段"按指派排除在 BLOCK 之外，我只把"请他定"改成陈述、**没有**改两张表的挂段（两张都仍写段 36 末）。若以后定下"一个词只带一张表"这类新规则，这两张里必有一张要挪段甚至砍掉 —— 那时改的是挂段与内容取舍，不是本次的格式对齐。

---

## 六、门禁 1 实跑原文（三份各一次，最后再合跑一次）

```
$ python3 tools/check_compare_draft.py work/辨析草稿/2_04.md
OK   2_04.md

门禁 1: PASSED（全部英文串可回溯）
$ python3 tools/check_compare_draft.py work/辨析草稿/2_08.md
OK   2_08.md

门禁 1: PASSED（全部英文串可回溯）
$ python3 tools/check_compare_draft.py work/辨析草稿/2_09.md
OK   2_09.md

门禁 1: PASSED（全部英文串可回溯）
```

```
$ python3 tools/check_compare_draft.py work/辨析草稿/2_04.md work/辨析草稿/2_08.md work/辨析草稿/2_09.md
OK   2_04.md
OK   2_08.md
OK   2_09.md

门禁 1: PASSED（全部英文串可回溯）
```

---

## 七、宽度机检实跑原文（`tools/audit_ledger.py`，口径 = `validate_data.py._width`：汉字＋全角标点 1，其余 0.5）

```
$ python3 tools/audit_ledger.py work/辨析草稿/2_04.md work/辨析草稿/2_08.md work/辨析草稿/2_09.md
扫描 3 份草稿 / 12 组
按当前机检口径（汉字+全角标点=1）超限的组数：0


--- 明细（超限行原文）---

--- 标称 vs 现口径 差值最大的 12 组（默认行）---
       2_04.md 组 25 · eminent / outstanding   标称  35.5 → 现  35.5 (Δ+0.0)
       2_04.md 组 26 · headmaster / principa   标称  36.0 → 现  36.0 (Δ+0.0)
       2_04.md 组 27 · professor / tutor ——    标称  34.5 → 现  34.5 (Δ+0.0)
       2_04.md 组 28 · canteen / dining hall   标称  34.0 → 现  34.0 (Δ+0.0)
       2_04.md 组 29 · story / tale —— 故事      标称  33.0 → 现  33.0 (Δ+0.0)
       2_08.md 组 45 · empire / imperial（共享义   标称  38.5 → 现  38.5 (Δ+0.0)
       2_08.md 组 46 · prosperity / thrive（共   标称  39.5 → 现  39.5 (Δ+0.0)
       2_08.md 组 47 · oral / verbal（共享义项：口头   标称  37.5 → 现  37.5 (Δ+0.0)
       2_08.md 组 48 · narrate / tale（共享义项：叙   标称  39.5 → 现  39.5 (Δ+0.0)
       2_08.md 组 49 · interpret / paraphras   标称  39.5 → 现  39.5 (Δ+0.0)
       2_09.md 组 50 · anticipate / expect —   标称  39.5 → 现  39.5 (Δ+0.0)
       2_09.md 组 51 · pastime / recreation    标称  39.0 → 现  39.0 (Δ+0.0)
exit=0
```

12 组 0 条 `line>40` / `sum>30` / `no-line` / `no-summary`，标称与现口径 Δ 全为 0（与 `audit_d.md` D 项复算的 24 条数值逐条一致）。

`audit_ledger.py` 只打默认行的标称差，总结句另用同一份解析器逐组量了一遍（只读，未新建脚本文件，跑的是 `python3 -c`）：

```
组 25  line 35.5 (claim 35.5) | sum 29.0 (claim 29.0)
组 26  line 36.0 (claim 36.0) | sum 23.5 (claim 23.5)
组 27  line 34.5 (claim 34.5) | sum 19.0 (claim 19.0)
组 28  line 34.0 (claim 34.0) | sum 27.0 (claim 27.0)
组 29  line 33.0 (claim 33.0) | sum 27.5 (claim 27.5)
组 45  line 38.5 (claim 38.5) | sum 24.0 (claim 24.0)
组 46  line 39.5 (claim 39.5) | sum 25.0 (claim 25.0)
组 47  line 37.5 (claim 37.5) | sum 18.0 (claim 18.0)
组 48  line 39.5 (claim 39.5) | sum 23.5 (claim 23.5)
组 49  line 39.5 (claim 39.5) | sum 26.5 (claim 26.5)
组 50  line 39.5 (claim 39.5) | sum 23.0 (claim 23.0)
组 51  line 39.0 (claim 39.0) | sum 23.5 (claim 23.5)
```

贴线提醒（不超限，但落地时别再往括号里加第二个英文例句）：组 46 / 48 / 49 / 50 默认行都是 39.5，余量 0.5。

---

## 八、落地前还剩什么（顺手跑了 `tools/land_compare.py --check`，只读、不写文件）

```
$ python3 tools/land_compare.py --check work/辨析草稿/2_04.md work/辨析草稿/2_08.md work/辨析草稿/2_09.md
解析 3 份草稿 / 11 组
载体分配：11 组可挂，冲突 0 组

错误 12 条 / 提醒 0 条
  2_04.md 组25: 差异表是长表（第 2 列是「词」），先转成 `| 维度 | 词1 | 词2 |` 宽表再落地
  2_04.md 组26: 差异表是长表（…）
  2_04.md 组27: 差异表是长表（…）
  2_04.md 组28: 成员数 <2
  2_04.md 组29: 差异表是长表（…）
  2_08.md 组45–49: 差异表是长表（…）（5 条）
  2_09.md 组50, 组51: 差异表是长表（…）（2 条）

未落地。
```

（上面把重复的"长表"那 11 条折成省略号，其余原文照抄。）

三点结论：

1. **主锚点被落地器全部认出来了** —— 两次跑（未改工具的 11 组、内存放开片语成员后的 12 组）都没有一条"找不到主锚点"，也没有一条"锚点段里成员没到齐"，说明本次补的 `主锚点 = 章N 段M` 与 `land_compare.py` 第 228 行的正则对得上；该工具用 `sections.json` 独立复核了能解析出的 11 个锚点段（组 28 见第 3 条），与第二节我手工复核 12/12 的结果一致。
2. **剩下的唯一硬错误类是"长表 → 宽表"**，批次 2 全部 42 组同一形状（工具文档里写明只吃宽表），属落地侧的一次性转换，不是这三份草稿的缺陷 —— 我没有为过机检去改差异表的任何一格内容。
3. **`2_04.md` 组 28（canteen / dining hall）报"成员数 <2"是工具的小坑，不是草稿错**：第 137 行的成员过滤器 `re.fullmatch(r'[a-z][a-z\-]*', x)` 不允许空格，把片语词头 `dining hall` 滤掉了，于是成员只剩 1 个。全 220 组只有 2 组带片语成员（idx 28 与 idx 73 `living room / parlour`）。我在内存里把该过滤器放开空格重跑一次（**未改 `tools/land_compare.py`**），12 组全部解析成功、`载体分配：12 组可挂，冲突 0 组`，除长表外 0 条错误。建议落地侧把第 137 行改成 `[a-z][a-z\- ]*` 并给 `dining hall` 这类词头做载体择一 —— 这属代码变更，不在本次草稿范围内。

