# 修复记录 · 批次 2 切片 05 / 06 / 07（依 `work/辨析审核/audit_c.md`）

修复人：作者侧校稿代理（非原审核人）。**只改了这三份草稿**：
`work/辨析草稿/2_05.md`、`work/辨析草稿/2_06.md`、`work/辨析草稿/2_07.md`。

未动 `shadow/data/*`、`shadow/index.html`、测试目录、`BRIEF.md`、`AUDIT_BRIEF.md`、别人的草稿、审核报告原文；
未做任何 git 写操作（只跑 `git status` / `git diff` 只读命令）。

**口径先记账**：宽度只有一把尺 = `scripts/validate_data.py._width`（2026-09-20 拍定，
汉字与全角标点各记 1、其余记 0.5），报告要你拍的那条按此执行 —— 组 40–44 的 D-BLOCK 全按这把尺改，
2_05 / 2_06 的 10 条标称值复量后 Δ=0.0（它们本来用的就是这把尺），未发生报告担心的"集体反转成虚报 2.5"。

修复量：**8 个 BLOCK 组 / 13 条 BLOCK 判定**（E 义项 4 条、D 宽度 8 条、A 原文 1 条）
＋ **16 条 MISMATCH**（F 同源计数 8、D 总结句标称 4、语义越界 3、边界索引误报 1）。
15 组逐条状态见下；**所有修法只删不造**：没有引入任何数据里查不到的英文串（门禁 1 见文末实跑）。

---

## 一、13 条 BLOCK 逐条：报告条目 → 改成什么样 → 现在实测宽度

| # | 报告条目（组 / 项） | 报告原话要点 | 改成什么样 | 现在实测宽度 |
|---|---|---|---|---|
| 1 | 组 30 · E-BLOCK ① | 表头「同一个"日记"，谁在看这本子差得最远」宣称同指，与本组 diff「中文落点」行自打脸 | 表头改成「这一段两本本子挨着出现：…两本本子各给谁翻，差得最远（本书 391 把 journal 译成「班级日志」，没译成「日记」）」；义项「日记」只留在 `sense` 槽 | 默认行 37.0（未动文本）；表头不受宽度闸 |
| 2 | 组 30 · E-BLOCK ② | 「本书用的却是第三档『日记』」读错了档 | 顶部「提请拍板①」、组内新增「档位核实」条、diff 表 journal 行「用在哪」格、附表索引行 —— 四处统一改成：**三档顺序 期刊(1)／日志(2)／日记(3)，本书 391 走第二档「日志」，第三档「日记」零书证**；并在「证据强度」补「本书没把 journal 当"日记"用过」 | —（非宽度项） |
| 3 | 组 32 · E-BLOCK | 表头「同一个"比率"」宣称同指，与「中文落点」行矛盾 | 表头改成「这一段在算两笔账：429 那笔看的是涨得多快、有多高，430 那笔只关心两样东西怎么配对」；「证据强度」补一条 BRIEF 口径 6 的义项报备（429 译「挂科率」、430 译「学习配比」）；「这一段在讲什么」里「两个"比率"」同步改成不宣称同指 | 默认行 39.0（未动） |
| 4 | 组 38 · E-BLOCK | 默认行「detect = **测出**升上来的」照抄 detect 卡 `m` 第一档原词 | 默认行改成 `detect = 早早查出来的（rising stress），perceive = 看出小差别（small changes）`（「早早」取自 476 的 `early` 与 `sentZh`「早早察觉」；「查出」对齐卡同义词族 find/discover/notice）；组 4 表头里同一处「早一步测出来」也改成「早一步查出来」 | **39.0**（标称同步为 39.0） |
| 5 | 组 40 · D-BLOCK 默认行 | 标称 38.0 / 实测 40.5 破上限 | 按报告给的两条路里取"删中文虚字"：`deduce = 照提示推（deduce answers），infer = 从哈欠看出累（from her yawns）`（中文钩子 7 字删到 4 字，「答案」由括号里的卡词伙 `deduce answers` 带出来） | **37.5** = 标称 37.5 |
| 6 | 组 40 · D-BLOCK 总结句 | 标称 24.0 / 实测 25.5 虚报 1.5 | 标称改成实测值，文本未动 | **25.5** = 标称 25.5（≤30） |
| 7 | 组 41 · D-BLOCK 默认行 | 标称 35.0 / 实测 37.5 虚报 2.5（未破 40） | 只重标数字，文本一字未动（报告另建议落地保留两卡 ex 最小对 —— 仍在 diff 表「考试里怎么用」行） | **37.5** = 标称 37.5 |
| 8 | 组 42 · A-BLOCK | 503 行把 `renaissance` 照词头抄了，数据表面形式是 `Renaissance` | 按数据改表：该行英文改成 `… and the **Renaissance** in the big hall.`（加粗保留），中文列未动 | —（非宽度项） |
| 9 | 组 42 · D-BLOCK 默认行 | 标称 39.0 / 实测 41.5 破上限 | 按 BRIEF §10「宁可只留一侧的例句」：删掉 moral 侧括号，并把 ethical 侧括号升级成可整块背的卡词伙 → `ethical = 定规矩的那条线（uphold ethical principles），moral = 心里那点良知`（中文钩子一字未删） | **37.5** = 标称 37.5 |
| 10 | 组 42 · D-BLOCK 总结句 | 标称 19.5 / 实测 20.0 虚报 0.5 | 只重标数字 | **20.0** = 标称 20.0 |
| 11 | 组 43 · D-BLOCK 默认行 | 标称 39.5 / 实测 42.0，15 组里最超的一条 | 取报告明示的那条路"去掉一个括号内容"：删 alien 侧（felt alien）→ `foreigner = 当面那位外宾（asked for directions），alien = 心里那点见外`；`felt alien` 仍在总结句与 diff 表里 | **35.0** = 标称 35.0 |
| 12 | 组 43 · D-BLOCK 总结句 | 标称 22.0 / 实测 22.5 虚报 0.5 | 只重标数字 | **22.5** = 标称 22.5 |
| 13 | 组 44 · D-BLOCK 默认行 + 总结句 | 标称 33.0 / 实测 35.5、标称 23.0 / 实测 24.0；总结句另有 F 越界 | 默认行文本未动、只重标 35.5；总结句换成书证级表述 `说地方时互换，本书各给一处：sacred room、holy chime`，并在「证据强度」写明前一版「只有」为何降级（sacred 仅 518 一处、holy 仅 519 一处） | 默认行 **35.5**、总结句 **25.5**（均 = 标称） |

---

## 二、16 条 MISMATCH 逐条

| # | 报告条目 | 改成什么样 | 现在实测 |
|---|---|---|---|
| 1 | 组 31 · F 同源 | subtract 侧「3 条独立文本」→ **2 条**（卡词伙＝课文 418 同一处，按口径 8 合并），minus 侧原本就写对未动 | — |
| 2 | 组 33 · F 同源 | 「每侧 3–4 条独立文本」→ **每侧 2 条**（卡词伙＝课文 452 同一处） | — |
| 3 | 组 34 | 报告判 OK，未动（数据侧 thesis 卡待拍板 2 保持原样，不改数据） | — |
| 4 | 组 35 · F 同源 | 两侧总账各 3 条 → **各 2 条**，并写明「课文 468＝卡词伙（`reference book` / `cite sources`）」＋一句"前一版把同一处数了两遍" | — |
| 5 | 组 36 · F 同源 | 「独立文本各 3 条」→ **各 2 条**；顺带撤回「比组 1 稍厚」这句随之失效的比较 | — |
| 6 | 组 37 · F 同源 | summary 侧「四条文本方向一致」→ **2 条**（`brief morning summary`＝课文 471、`morning summary` 是它的前缀，三者一处），并说明"收尾"那一格靠的是卡 ex 那条 `at the end`，不依赖虚报条数 | — |
| 7 | 组 37 · D 零余量（报告"建议"） | 默认行 40.0 → 37.5（把（morning summary）换成同一条卡 ex 里的（at the end）） | 37.5 |
| 8 | 组 38 · F 同源（轻） | detect 侧条数 → **3 条**并点名同源；line 229「书里另一处 detect」→「课文里 detect 只有 476 一处；另一次主语是机器，在 sensor 卡 ex（不是课文）」 | — |
| 9 | 组 39 · F 同源 ① | postulate 侧「课文 1 句 + 卡词伙 1 处 + 卡 ex 1 句」→ **2 条**（两条卡词伙都指 474）；presume 侧同步写明 2 条 | — |
| 10 | 组 39 · F 边界索引 ② | 撤回「切片另有组 195 / 217 收它们」对 `speculate` / `predict` / `assume` 的误报。我另开 `compare_groups_worklist.json` 复算过：**195 = deem / reckon / suppose、217 = hypothesis / postulate / presume / suppose（与报告一致）；speculate、predict、assume 全 220 组一组都没收**。表中把"已被别的组收走"与"没人收"两种归属分开写，结论（不并进来）不变 | — |
| 11 | 组 40 · F 同源 | 「独立文本共 6 条」→ **4 条**（`deduce answers`＝483、`safely infer`＝482），附表索引行「证据 6 条」同步改成「同源合并后证据 4 条」 | — |
| 12 | 组 41 · D 总结句 | 标称 19.5 → **20.0** | 20.0 |
| 13 | 组 42 · F 语义越界（同义词框架行） | 「阅读里碰到任一都译"道德的"，别当新信息」→「中文都可能落到「道德」这一族，读到别当新信息；**但本书 507 自己就没让它们撞：ethical 译「道德上的」、moral 译「向善的」心**」，与同组 148 行不再矛盾 | — |
| 14 | 组 42 · F 出处截断（小疵） | 1107 那行加省略号并补整句：`Our family ethic is short enough to say in one sentence…` + 「整句是 `… in one sentence: work hard and be kind.`」 | — |
| 15 | 组 43 · D 总结句 | 标称 22.0 → **22.5** | 22.5 |
| 16 | 组 44 · F 语义越界（总结句两个「只有」） | 见上表 #13；"优先"的口气只留在 diff 表「考试里怎么用」那一行（那里本来就写的是「优先」不是「只有」） | 25.5 |

---

## 三、报告没点名、我顺手改的（自报，供你回退）

1. **表头「这一句」指代**：`scripts/validate_data.py` 第 8 条会拦"表头用「这一句」指代、卡却挂段末"。
   本批 4 处已改成落地可用的写法：
   2_05 组 4「452 这一句」→「452 同句里」；2_06 组 4「这一句里"眼尖的人"」→「476 同句里"眼尖的人"」；
   2_07 组 42「老师这一句把两个"道德"装进同一句」→「507 同句里两个"道德"各修一样东西」；
   2_07 组 43「"外"在那句是一个人，在这句是一种感觉」→「在 510 那边是一个人，在 511 那边是一种感觉」。
   已核对：本批 15 组表头现在没有一处含 `这(一)?句`。
2. **同一条事实错误在 2_06 出现了三次**，报告只点了组 39 那处：组 1 的「`elicit`（469）、`extract`（470）、`assume`（471）各属别的辨析组」与组 4 的「`speculate` / `predict` 各属别的组」同样不成立，已按同口径改成"worklist 全 220 组里一组都没收它们"。
3. **三份文件头的宽度口径复述**统一到同一句话（`= validate_data.py._width`），2_07 那句错的（"CJK 记 1、其余 0.5"）已改掉，避免下一个代理照抄错尺。
4. **组 40 表头一处中文病句**：原写「导师查出薄弱环节、**往出**一条学习路线（482）」，"往出"是漏字，按 482 `sentZh`「再据此推出一条更合适的学习路线」补正为「再推出一条更合适的学习路线」。纯中文，不涉及英文串。

---

## 四、15 组宽度终表（唯一一把尺实测；标称 = 实测，无一条超限）

| 组 | 成员 | 默认行 标称 → 实测 | 总结句 标称 → 实测 |
|---|---|---|---|
| 30 | diary / journal | 37.0 → 37.0 | 22.0 → 22.0 |
| 31 | minus / subtract | 39.0 → 39.0 | 24.5 → 24.5 |
| 32 | rate / ratio | 39.0 → 39.0 | 15.5 → 15.5 |
| 33 | elementary / rudimentary | 38.0 → 38.0 | 23.5 → 23.5 |
| 34 | dissertation / thesis | 35.5 → 35.5 | 25.0 → 25.0 |
| 35 | cite / reference | 38.5 → 38.5 | 24.5 → 24.5 |
| 36 | cite / quote | 39.0 → 39.0 | 23.5 → 23.5 |
| 37 | abstract / summary | **37.5 → 37.5**（原 40.0） | 24.5 → 24.5 |
| 38 | detect / perceive | **39.0 → 39.0**（换过钩子） | 23.0 → 23.0 |
| 39 | postulate / presume | 39.5 → 39.5 | 23.0 → 23.0 |
| 40 | deduce / infer | **37.5 → 37.5**（原实测 40.5） | **25.5 → 25.5**（原标称 24.0） |
| 41 | inquire / query | **37.5 → 37.5**（原标称 35.0） | **20.0 → 20.0**（原标称 19.5） |
| 42 | ethical / moral | **37.5 → 37.5**（原实测 41.5） | **20.0 → 20.0**（原标称 19.5） |
| 43 | alien / foreigner | **35.0 → 35.0**（原实测 42.0） | **22.5 → 22.5**（原标称 22.0） |
| 44 | holy / sacred | **35.5 → 35.5**（原标称 33.0） | **25.5 → 25.5**（改写后，原实测 24.0） |

> 说明：`tools/audit_ledger.py` 只认「实测宽度 X」这种写法，量出来 15 组 Δ 全 0.0、超限 0 条；
> 2_05 / 2_07 的总结句标成「≤30 字｜实测 X」或「（≤30，实测 X）」，该行不在它的差值表里，上表这一列由我用同一个 `_width` 逐条复量核对过。

---

## 五、门禁实跑结果原文

### 门禁 1（`python3 tools/check_compare_draft.py <三份各一次>`）

```
OK   2_05.md

门禁 1: PASSED（全部英文串可回溯）
OK   2_06.md

门禁 1: PASSED（全部英文串可回溯）
OK   2_07.md

门禁 1: PASSED（全部英文串可回溯）
```

### `python3 tools/audit_ledger.py work/辨析草稿/2_05.md work/辨析草稿/2_06.md work/辨析草稿/2_07.md`

```
扫描 3 份草稿 / 15 组
按当前机检口径（汉字+全角标点=1）超限的组数：0


--- 明细（超限行原文）---

--- 标称 vs 现口径 差值最大的 12 组（默认行）---
       2_05.md 组 1 · diary / journal —— 日记    标称  37.0 → 现  37.0 (Δ+0.0)
       2_05.md 组 2 · minus / subtract —— 减去   标称  39.0 → 现  39.0 (Δ+0.0)
       2_05.md 组 3 · rate / ratio —— 比率       标称  39.0 → 现  39.0 (Δ+0.0)
       2_05.md 组 4 · elementary / rudimenta   标称  38.0 → 现  38.0 (Δ+0.0)
       2_05.md 组 5 · dissertation / thesis    标称  35.5 → 现  35.5 (Δ+0.0)
       2_06.md 组 1 · cite / reference —— 提及   标称  38.5 → 现  38.5 (Δ+0.0)
       2_06.md 组 2 · cite / quote —— 引用       标称  39.0 → 现  39.0 (Δ+0.0)
       2_06.md 组 3 · abstract / summary ——    标称  37.5 → 现  37.5 (Δ+0.0)
       2_06.md 组 4 · detect / perceive —— 察   标称  39.0 → 现  39.0 (Δ+0.0)
       2_06.md 组 5 · postulate / presume ——   标称  39.5 → 现  39.5 (Δ+0.0)
       2_07.md 组 40 · deduce / infer —— 共享义   标称  37.5 → 现  37.5 (Δ+0.0)
       2_07.md 组 41 · inquire / query —— 共享   标称  37.5 → 现  37.5 (Δ+0.0)
```

（差值表只列前 12 行；组 42 / 43 / 44 三条同样 Δ+0.0，实测值见上表。）

---

## 六、请你复核的 3 条分歧（都不影响门禁通过）

1. **组 37（2_06 组 3）我改了一件报告判"OK"的事**：报告 D 项写的是「40.0 恰好压线，不超但零余量，建议减 2 字留余量」——
   我照建议把默认行括号从（morning summary）换成同一条卡 ex 里的（at the end）。
   严格说 D-OK 组被动了文本。若你的意思是"压线就原样落地、余量不归草稿管"，回退只需把这一处括号换回去（宽度回到 40.0，仍不超限）。
2. **组 42 / 43 删的是哪一侧括号，我做了取舍**：两条超线行我都选择"删一侧英文括号、保住中文钩子"（BRIEF §10 给的第一条路），
   并且各自留下被删那一侧的英文在别处（组 42 的 `moral heart` 在原文表 / diff / 出处清单，组 43 的 `felt alien` 在总结句 / diff / 出处清单）。
   如果你更希望默认行留住"框架"而不是"搭配对象"，反向删另一侧可行：组 42 → `ethical = 定规矩的那条线，moral = 心里那点良知（moral heart）`（34.0）、
   组 43 → `foreigner = 当面那位外宾，alien = 心里那点见外（felt alien）`（31.0）。我这边不坚持。
3. **表头「这一句」是全批次问题，不止我这批**：`validate_data.py` 第 8 条会拦挂段末卡片的「这一句」指代，
   我在 2_05 / 2_06 / 2_07 里清了 4 处（报告未点名）。刚才用 `^> …这(一)?句` 复扫别的切片，写这份记录时还剩
   **2_04 两处（组「headmaster / principal」「canteen / dining hall」）、2_08 一处、2_09 一处**
   （2_02 / 2_03 原本也有，已被同时段修那三片的代理改掉 —— 说明这条已有人在处理，别重复动）。
   建议要么由对应切片的修复代理顺手清掉，要么落地器统一改写成「这一段 / NNN 同句里」—— 别等到门禁 3 才发现。
