# 修复记录 · 批次 5 `5_07.md` + `5_08.md` + `5_09.md`（依据 `work/辨析审核/audit_d_b5.md`）· 2026-09-21

执行人：修复代理（FIX_5_d）。**只改了三份草稿 `work/辨析草稿/5_07.md`、`5_08.md`、`5_09.md` 与本文件**；未写 `shadow/data/*`、未碰 `scripts/*`／`tools/*`／`index.html`／`shadow/index.html`／别人的草稿（`5_01..5_06`、`4_*.md`）／任何 `audit_*.md`；**没有跑 `tools/land_compare.py` 的任何模式**（连 `--dry-run`／`--check` 都没跑，`work/辨析审核/land_preview.json` 时间戳仍是 09-20 23:24 未被我动过）；无任何 git 写操作（只跑过 `git status` / `git branch --show-current` / `git log --oneline`）。本轮共处理报告 4 条 MISMATCH + 3 条文件级，落成 13 处改动（含 4 处"同一条计数的连带引用"）。`5_09.md` 报告两组全通过、无文件级条目，**一字未动**。

## 改动清单

| # | 报告编号 | 组 | 改前 | 改后 | 改后宽度 | 过不过 |
|---|---|---|---|---|---|---|
| 1 | 组167·F MISMATCH（必改清单①） | 167（`5_07` 组2） | 数据边界：「death 侧独立文本 **7 条**（课文 1 + 卡 ex + 卡词伙 2 + 别的卡 ex 4，其中 `the death penalty` 与 penalty 卡同串）」——四项相加是 8 | 「death 侧独立文本 **8 条**（课文 1 + 卡 ex 1 + 卡词伙 2 + 别的卡 ex 4 = 8，两条词伙本书课文 0 处、不与课文同源故各算一条；其中 `the death penalty` 与 penalty 卡同串，跨卡同串只算 1 条）」——我按报告口径重数：课文 1676 + `death.ex`（`His death made everyone very sad.`）+ 卡词伙 `the death penalty`／`prevent unnecessary deaths`（两条我在 sections 三种变体里各扫过，课文均 0 命中）+ heaven／hell／revenge／lament 四卡 `ex`（四条 `ex` 我逐条读过，每条都含 death）= **8** | —（未动默认行／总结句） | 过（加得起来了） |
| 2 | 组167·连带引用（同一条计数） | 167 | 数据边界下一行：「"demise 本书 2 处都带限定词、death 有零冠词用法"是 2 处 vs **7 处**的分布」 | 「…是 2 处 vs **8 处**的分布」 | — | 过 |
| 3 | 组167·连带引用 | 167 | diff「冠词与介词框架｜demise」格：「但只是 2 处 vs **数处**的分布」（模糊，下游会自己脑补一个数） | 「但只是 2 处 vs **8 处**（death 侧独立文本 8 条）的分布」 | — | 过 |
| 4 | 组167·连带引用 | 167 | 附录复核索引 组2 行：只写「demise 侧独立文本 2 条（薄，按口径 7 如实写）」 | 「独立文本 demise 侧 2 条、death 侧 8 条（薄的那侧按口径 7 如实写）」 | — | 过（三处死亡侧条数现在同为一个数） |
| 5 | 组173·F MISMATCH（本片最严重一条，必改清单②） | 173（`5_08` 组3） | diff「谁在高兴｜rejoice」格：「本段给它的是复数主语——两个 `ex` 与**两处课文**都是"一群人 + 一个动作"」 | 「本段给它的是复数主语——两个 `ex` 都是复数主语；课文侧只有 1713 一处是群人，1711 是一颗疲惫的心（那句主语是那碗汤，delight 被端进去）」——数据侧我自查：1711 = `Then Grandma's warm soup brought soft … delight to his tired heart.`（主语 warm soup、落点 his tired heart）、1713 = `…his whole family seemed to rejoice…`；两卡 `ex` 主语分别 `The children`／`They` | —（默认行／总结句一字未动，仍是 35.5／20.5） | 过（与数据同向） |
| 6 | 组173·连带（数据边界） | 173 | 「—— **4 条文本方向一致**，但样本只有 4 条，落地措辞须停在"本书这几处"」 | 「—— 4 条文本里 **3 条**（1713 + 两卡 `ex`）主语是复数、方向一致；1711 是反例：主语是那碗汤，delight 落在 `his tired heart` 上，不是"一群人 + 一个动作"。样本只有 4 条、且只 3 条同向，落地措辞须停在"本书这几处"」 | — | 过 |
| 7 | 组173·省略号拼接（门禁 1 放行但落地会失真） | 173 | diff「谁在高兴｜delight」格引卡 ex 写成 `The children ... when they saw the toys`（两半之间被省略号缝合；`check_compare_draft.py` 见到 `...` 就跳过整串，所以它一直是"假绿"） | 换成连续子串 `The children felt delight`（`delight.ex` = `The children felt delight when they saw the toys.` 的前 4 词，我按连续子串比过）；同格顺手补「（delight 仍在 felt 的宾语位）」 | — | 过 |
| 8 | 组174·F MISMATCH（唯一性断言虚报，必改清单③） | 174（`5_08` 组4） | 共现位置段：「两词的标记书证是**同一处**——这是本切片**唯一一组**"两个词只共用一句"的组」 | 「两词的标记书证是**同一处**——与本切片组 1（alleviate / relieve）同形状：两词的标记书证都只落在同一句（那里是 1703，本组是 1713，两处都由 `and` 串起前半后半）」——我自己按标记扫过：`[[alleviate:alleviate]]` 全书 1 处＝1703、`[[relieve:relieve]]` 全书 1 处＝1703、`[[joy]]`／`[[rejoice]]` 各 1 处＝1713；1703 与 1713 两句都含 `and` | — | 过（§五.15 意义上的"改口为可查事实"） |
| 9 | 组175·F MISMATCH（「明写」过头，必改清单④） | 175（`5_08` 组5） | diff「这层喜欢是怎么动起来的｜favour」格：「1719 的后半句**明写**这是别人看得见的偏心（给了林意外之喜），fond 那一句没有这个旁观者」 | 「本书 1719 的 `sentZh` 写作「偏爱」，句子里 `to slow learners` 点出接受者是谁（一群慢学的孩子，fond 侧两处宾语都是物）——**这是本书这几处这么写，两卡 `note` 无语域标注**，别读成 favour 词义自带偏心」（报告要求的自我限定同格保留）——出处自查：1719 `sentZh` = 「她**偏爱**慢学的孩子，这在灰色日子里给了林意外之喜。」、句面含 `to slow learners`；`favour.note` 只有「词伙：special favour」、`fond.note` = `""`，确无语域栏 | —（默认行 39.0／总结句 19.0 未动） | 过（措辞降级到本书，格未删） |
| 10 | 组175·A 行粗体噪音（必改清单④后半） | 175 | 原文表 1719 行：`She showed special **favour** to slow learners, which gave Lin a **surprising** lift on grey days.` | 只留 `**favour**` 一个粗体（`**` 的约定＝该句挂了词头，成员表下方只该点成员词） | — | 过 |
| 11 | 文件级①（`5_08` 附录宽度复述行，报告记为文件级 MISMATCH） | 附录（`5_08` 第 397 行） | 「总结句 22.0 / 22.0 / 20.5 / 26.0 / **21.0**」——组 5 小标题与实测都是 19.0，差 2.0 | 「总结句 22.0 / 22.0 / 20.5 / 26.0 / **19.0**（逐条与各组小标题的标称值一一对应）」——**只改附录数字，卡面文本一个字没动** | 附录＝实测 19.0（≤30） | 过 |
| 12 | 文件级②（`5_08` 附录句号计数行） | 附录（`5_08` 第 395 行） | 「8 个句号（去重 **6 个**：1703、1711、1713、1718、1719）」——括号里只有 5 个数 | 「8 个句号（条目 8 个，去重 **5 个**：1703、1711、1713、1718、1719——1711 与 1713 各被组 2／组 3 重复列一次、1713 另在组 4 再列一次）」——我按 `slice35.json` 的 5 组 `sents` 摊平重算：条目 8、去重 {1703,1711,1713,1718,1719}＝5 | — | 过 |
| 13 | 文件级③（`5_07` 附录句号写法） | 附录（`5_07` 第 391 行） | 「5 组 8 个句号与切片包 `sents` 逐个对得上」（读起来像条目数；条目实为 9） | 「5 组句号去重 8 个（切片包 `sents` 条目共 9 个，1689 被组 3／组 4 各列一次）：1671、1676、1677、1687、1688、1689、1691、1692，与切片包逐个对得上」——按 `slice34.json` 重算：条目 9、去重 8 ✓ | — | 过 |

## 未改项及理由

**报告标"建议／nit／口味"的，一律未动**（照报告办，不自己发挥）：

- **组 166 无项**；**组 167·E 建议**：`core`「绕着说」那层语域判断要挂到 1677 译文「离世」这一个出处上——报告自标"建议，不改判"，未动。
- **组 168·nit**：diff「本书 2 处都是修饰身份」宜改成"都是说明身份、从不自己当那个'事'"（1689 的 curative 是 be 后表语）——nit，未动。
- **组 168／170·截半引用建议**：1793 译文缺后半「，路过偷热奶的狡猾小猫。」、1763 译文缺后半「他不再为错过的春游大怒」，报告建议补后半或加省略号——建议级，未动（注：这与 #7 那条英文省略号拼接不是一回事，那条是"缝合两处"、报告点名为落地前必换）。
- **组 171·建议 1 处**：`core`「把心里那件事挪开」改带范围措辞——报告明示"不必返工"，未动。
- **组 172·建议 2 处**：共现位置行的「第一次」（数据不支持，1713 的 `again` 反倒说明不是第一次）与「本切片最对称的一组」（与组 5「最对称的一组之一」相冲）——报告标建议、判"通过"，未动。
- **组 174·并卡问题（待拍板）**：报告结论"并不得，按现表出两张"，但拍板文字属作者上报项，文件头小结、组内「提请拍板点」、附录复核索引三处原样保留，一字未删（#8 只动了那句虚报的唯一性，没动拍板论据本身）。
- **组 175·待拍板文字**：文件头小结与数据边界「**一处语义判断，请过目**」两段（含"删掉那一行即可，剩下三格都有直接出处"）原样保留——报告已判"不删格、改措辞"，格已按 #9 改，删格分支不再适用但报备话术留着给他看。
- **组 175·粗体建议的另一半**：1716 行 `**grew**`／`**keen**`、1717 行 `**zeal**`／`**lively**`、1718 行 `**lovely**`／`**grew**` 也加了粗，报告把这条记成"建议只粗成员词"、必改清单只点名 1719 行 → 只改了 1719。数据事实我核过（复核方法见下），**真正的"排版噪音"只有两处 `grew`**：`MARK.findall` 结果为 1716＝progress/desirable/keen、1717＝zeal/lively、1718＝lovely/fond、1719＝favour/surprising —— `grew` 三处都没挂词头。要不要一起摘掉，请下一位拍板（一行 `sed` 的事，不牵宽度）。
- **判定"通过"的其余 8 组（166／168／169／170／171／172／176／177）**：一字未动。

**我认为报告可以说得更准／需要下一位注意的两处（都按报告执行了，只是备案）**：

1. #10 摘掉的 `**surprising**` 其实**是**挂了词头的（1719 标记 = `[[favour:favour]]` + `[[surprising:surprising]]`），按报告自己引的"`**`＝该句挂了词头"约定它本可保留；而真正违反该约定的 `**grew**`（1716／1718，数据里 `grew` 无标记）反而只在"建议"档。我按必改清单执行（改 1719、留下 grew），但这会让 1719 行比 1716／1718 行更"干净"、方向有点反。复核方法：
   ```bash
   python3 -c "
   import json,re
   MARK=re.compile(r'\[\[([^\]:\"\\n]+):([^\]\"]+)\]\]')
   s=json.load(open('shadow/data/sections.json',encoding='utf-8'))
   flat=[]
   for ci,ch in enumerate(s):
       for para in ch['paragraphs']:
           for x in para: flat.append(x)
   for g in (1716,1717,1718,1719): print(g, MARK.findall(flat[g]))"
   # 实跑输出：1716 = progress/desirable/keen ｜ 1717 = zeal/lively ｜ 1718 = lovely/fond ｜ 1719 = favour/surprising
   ```
2. **`5_08.md` 组 4 数据边界仍有一句"是本切片最薄的一组"**（报告未点名、我没动）。它按"可引文本总条数"算成立（组4＝1713 + 两卡 `ex` + `find joy` 共 4 条；组1＝1703 + 两卡 `ex` + alleviate 词伙 2 条 + acupuncture `ex` 共 7 条），但报告这次要我改掉的正是同组"两个词只共用一句＝唯一"那种说法，而**组 1 与共它同形（两词标记书证都只有 1703 一句）**——若下一位按"共现句数"读"最薄"，这句会立刻变成第二条虚报唯一性。建议措辞：「共现证据与组 1 同为一句，可引文本 4 条为本片最少」。复核方法：把 `slice35.json` 各组 `sents` 摊平去重，再按标记扫 `[[alleviate`／`[[relieve`／`[[joy`／`[[rejoice`。

## 终检

1. 门禁 1（唯一被允许跑的自检工具，原样输出，未跑落地器）：

```
$ python3 tools/check_compare_draft.py work/辨析草稿/5_07.md work/辨析草稿/5_08.md work/辨析草稿/5_09.md
OK   5_07.md
OK   5_08.md
OK   5_09.md

门禁 1: PASSED（全部英文串可回溯）
```

2. **12 条默认行 + 12 条总结句：标称（小标题）／实测（`tools/width_rule.py` 的 `width()`）逐条对照，含未改的 8 组**——本轮没动任何默认行与总结句的文字，标称值全部原样成立：

| 组 | 默认行标称 | 默认行实测 | ≤40 | 总结句标称 | 总结句实测 | ≤30 |
|---|---|---|---|---|---|---|
| 166 `5_07` 组1 | 39.0 | 39.0 | 过 | 23.0 | 23.0 | 过 |
| 167 `5_07` 组2 | 39.5 | 39.5 | 过 | 20.5 | 20.5 | 过 |
| 168 `5_07` 组3 | 40.0 | 40.0 | 过（压线） | 20.5 | 20.5 | 过 |
| 169 `5_07` 组4 | 39.5 | 39.5 | 过 | 22.5 | 22.5 | 过 |
| 170 `5_07` 组5 | 40.0 | 40.0 | 过（压线） | 20.0 | 20.0 | 过 |
| 171 `5_08` 组1 | 36.5 | 36.5 | 过 | 22.0 | 22.0 | 过 |
| 172 `5_08` 组2 | 37.5 | 37.5 | 过 | 22.0 | 22.0 | 过 |
| 173 `5_08` 组3 | 35.5 | 35.5 | 过 | 20.5 | 20.5 | 过 |
| 174 `5_08` 组4 | 36.0 | 36.0 | 过 | 26.0 | 26.0 | 过 |
| 175 `5_08` 组5 | 39.0 | 39.0 | 过 | **19.0** | 19.0 | 过（附录已同步） |
| 176 `5_09` 组1 | 38.5 | 38.5 | 过 | 27.5 | 27.5 | 过 |
| 177 `5_09` 组2 | 38.0 | 38.0 | 过 | 26.0 | 26.0 | 过 |

3. **附录复述清单 ↔ 组内小标题 ↔ 实测 三方逐个相等**（脚本比对：`extract()` 取文本、`width()` 量、正则取小标题标称、正则取附录那一行，逐位比）：
   - `5_07.md`：附录「默认行 39.0 / 39.5 / 40.0 / 39.5 / 40.0，总结句 23.0 / 20.5 / 20.5 / 22.5 / 20.0」＝组内标称＝实测 ✓
   - `5_08.md`：附录「默认行 36.5 / 37.5 / 35.5 / 36.0 / 39.0，总结句 22.0 / 22.0 / 20.5 / 26.0 / 19.0」＝组内标称＝实测 ✓（本轮改的就是最后一个数）
   - `5_09.md`：附录「默认行 38.5 / 38.0，总结句 27.5 / 26.0」＝组内标称＝实测 ✓
4. 组标题编号与顺序：`5_07` 组1–5 / `5_08` 组1–5 / `5_09` 组1–2 未增删未换位（worklist_idx 166–177 仍一一对应）；每组 `主锚点 = 章N 段M` 字段名保留（12 组均在）。
5. 本轮新写进卡面的每一个反引号英文串（`The children felt delight`、`his tired heart`、`to slow learners`、`showed special favour`、`which gave Lin a surprising lift on grey days`、`the death penalty`、`prevent unnecessary deaths`）我另跑了一次"三种标记变体"子串回扫，全部 HIT；#8 那句"那里是 1703、本组是 1713、两处都由 and 串起"按 `sections.json` 原句逐字看过 ✓。
6. 改动条数一致性自查：`grep -n "7 条\|7 处" 5_07.md` 只剩组 1 的 injury 侧 7 条（另一组、报告判 OK，未动）；`grep -n "唯一一组\|两处课文\|4 条文本方向一致\|明写这是\|21.0\|去重 6" 5_08.md` 已 0 命中。
