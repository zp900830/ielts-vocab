# 雅思背单词 3.0 · 产品需求文档（PRD）

| 字段 | 内容 |
|------|------|
| 产品名称 | 雅思背单词 3.0（IELTS Vocab · Article-First Learning） |
| 版本 | v3.0.0（草案 · 2026-09-24 起稿） |
| 状态 | **Confirmed · 开发中**（2026-09-24 拍板 14 项，见 §14.1；交付顺序见 §14.2，先做 M1 最小闭环） |
| 前置版本 | v2.0.0（tag `v2.0`，2026-09-24；本文所有「继承」均指这个版本里已上线的能力） |
| 线上地址 | **3.0 = `https://ielts-vocab.bond/app/`**（已定，§14.1 第 10 条）。现有 `https://ielts-vocab.bond/`（情景训练）与 `https://ielts-vocab.bond/shadow/`（影子跟读）**原样保留、照常可访**（§11.3） |
| 维护模式 | 单人维护 · 多 AI Agent 协作 |

---

## 目录

1. [产品定位](#1-产品定位)
2. [信息架构与页面框架](#2-信息架构与页面框架)
3. [首页 · 六篇文章卡片](#3-首页--六篇文章卡片)
4. [文章任务模式（进入卡片后）](#4-文章任务模式进入卡片后)
5. [学习数据页](#5-学习数据页)
6. [单词本页](#6-单词本页)
7. [随身听](#7-随身听)
8. [我的](#8-我的)
9. [数据模型与进度体系](#9-数据模型与进度体系)
10. [视觉与交互规格](#10-视觉与交互规格)
11. [不做什么](#11-不做什么)
12. [迁移与兼容](#12-迁移与兼容)
13. [验收清单](#13-验收清单)
14. [决议记录与待确认](#14-决议记录与待确认)

---

## 1. 产品定位

### 1.1 一句话

3.0 把 v2.0 里分散在两个应用（情景训练 + 影子跟读）里的学习行为**合并到一个产品**里，核心路径只有一条：

> **阅读文章 → 理解文章 → 跟读/朗读 → 反复熟悉文章 → 在不同语境中掌握文章中的单词 → 真正掌握单词**

### 1.2 设计总纲

| 原则 | 含义 |
|------|------|
| **文章是学习主线** | 六篇大文章是全部学习行为的入口和容器，不是「阅读功能」 |
| **单词是学习结果** | 单词从文章学习过程中自然沉淀出来，不作为一级入口 |
| **信息层级** | 文章 > 学习任务 > 学习进度 > 单词 > 数据 |
| **产品形态** | 「以文章为载体的英语学习系统」，不是「背单词 App + 一个文章功能」 |
| **复用不重造** | 视觉体系继承情景训练（`index.html` 现有 tokens），播放交互继承影子跟读（`shadow/index.html` 现有逻辑），任务模式逻辑继承 v2.0 的两步制引擎 |

### 1.3 核心学习路径

```
首页选文章 → 进入文章任务模式
                        ↓
              ① 通读（一步一停 · 逐句跟读）
                        ↓
              ② 挖空选词（语境中检验）
                        ↓
              日常：随身听反复磨耳 → 全屏阅读精读 → 单词本复习
                        ↓
              文章熟练度 ↑ → 单词掌握数 ↑ → 下篇文章
```

### 1.4 学习目标

| 目标 | 量化口径 |
|------|---------|
| 持续学习 6 篇核心文章 | 全部进入「已学完」态：**每句至少读过一次 + ② 答过一遍**（判据见 §9.4，注意不是「通读遍数」—— 遍数没有记账） |
| 熟悉度 / 理解度 / 朗读熟练度 | 每篇文章一个 0–100% **熟练度**分数（由通读次数 + ② 正确率 + 跟读次数合成，见 §9.2） |
| 真正掌握单词 | 词状态机（已见面 → 文中可辨 → 义项直连 → 已毕业）沿用 v2.0 引擎，全 3245 词 |
| 看到自己的进度和掌握情况 | 首页卡片 + 学习数据页 + 单词本，三个层级都有实时数字 |

---

## 2. 信息架构与页面框架

### 2.1 整体布局

**左侧固定导航 + 右侧内容区域**，桌面 / 平板 / 手机三档：

```
┌─────┬──────────────────────────┐
│     │                          │
│ 导  │      内容区域             │
│ 航  │   （当前一级页面）        │
│     │                          │
│ 4项 │                          │
└─────┴──────────────────────────┘
桌面 ≥ 1024px：左侧固定侧栏 220px（4 项，图标 + 文字；底部一条分割线 + 「我的」一行）
平板 701–1023px：左侧收窄侧栏 180px（图标 + 文字收窄）
手机 ≤ 700px：底部 TabBar（4 项 + 「我的」，共 5 格）
```

### 2.2 一级导航（4 项 + 底部「我的」一行；不加）

| # | 名称 | 图标 | 路由 | 页面职责 |
|---|------|------|------|---------|
| 1 | **首页** | 🏠 `ri-home-5-line` | `#/home` | 六篇文章卡片 + 「今天该做什么」 |
| 2 | **随身听** | 🎧 `ri-headphone-line` | `#/listen` | 播放卡片 + 展开全屏阅读 |
| 3 | **单词本** | 📖 `ri-book-2-line` | `#/words` | 按文章关联的单词列表 + 详情 |
| 4 | **学习数据** | 📊 `ri-bar-chart-2-line` | `#/stats` | 学习总览 / 文章 / 单词 / 趋势四块 |

**「我的」不再是一级导航**（2026-09-24 用户改口径）：改成**侧栏底部一条分割线 + 下面一行**（未登录显示「登录」按钮；
已登录显示头像 + 账号），点它**向上弹「我的」浮窗**（账号 / 学习摘要 / 当前状态 / 数据管理 / 主题 / 口音 / 音色 / 计划设置，见 §8.1），
不再是切页。手机端这一行就是底部 TabBar 的**第 5 格**。所以导航是 **4 项 + 1 行**。

**明确不加的页面**：使用方法、性能与错误、帮助中心、关于产品、设置大杂烩 —— 与核心学习流程无关的任何入口。设置里能留的只有：每日分钟数、新词开关（2026-09-25 起**日界固定凌晨 4 点、不再可调**，那个旋钮已删；两者收进「我的」浮窗里，不再单独占一级页面）。

### 2.3 首次进入（还没有计划）

**2026-09-24 定**：没计划**也能进任务模式** —— 页面给一个空状态 + 跳配置的入口，既不堵门、也不装死。

| 场景 | 行为 |
|------|------|
| 打开首页 | 照常可浏览六张卡片（全「未开始」态），不拿设置屏堵门 |
| 点卡片 | **照常进入该篇任务模式**（正文可读、可自由跟读）；**任务条那一格换成空状态**：「还没有学习计划 · 去设置每天读多久」→ 点了跳配置（v2.0 的设置屏）。⚠️ v2.0 的 `enterTaskMode()` 在 `!plan` 时**直接 return**，3.0 要把这条改成「照常渲染 + 空状态」 |
| 进「学习数据」 | 没计划时是一句话 + 一个按钮（打开「我的」浮窗，§5.2），**不内嵌设置屏** |
| 进「随身听」 | 不要求有计划，可以直接听（六篇任选） |

### 2.4 一级页面之外的全部入口（穷举，防止悄悄长出新页面）

| 入口 | 形态 | 来自哪 |
|------|------|--------|
| 词库抽屉 | 正文右侧边标签 → 抽屉 | 影子跟读 `#panel` / `#edgeTab` |
| 单词释义卡 | 点正文里的词 → 浮窗 | 影子跟读 `openPop` / `#pop` |
| 快速跳转 | 点标题胶囊 → 选卷 + 输句号 | 影子跟读 `#jumpPop` |
| 今日面板 | …… **3.0 不再需要**：它承载的「今天/计划/词本」三屏已被拆成一页首页 + 一页学习数据 + 一页单词本 | — |

> 这条是给实施时的护栏：**除上表之外不再新增浮层/页面**。

> **M2 落实**（2026-09-24）：学习数据页是 §2.2 的一级页，入口**只有侧栏「学习数据」一项**；
> 页内「点文章小卡 → 回首页高亮那张卡片」是同一屏内的状态迁移（`#/home` + 高亮 class），
> **不是新浮层、也不是新页面**，所以本表无需扩列。
>
> **M3 落实**（2026-09-24）：单词本是 §2.2 的一级页，入口**只有侧栏「单词本」一项**（`#/words`，
> 可带筛选段 `#/words/todo|learning|mastered` 做深链）；词详情是**行内展开**（`aria-expanded`），
> 「▶ 播放这句」复用既有的文章任务模式（回首页 + 进该篇 + 定位该句播放），
> **不是新浮层、也不是新页面**，所以本表无需扩列。
>
> **M4 落实**（2026-09-24）：随身听是 §2.2 的一级页，入口**只有侧栏「随身听」一项**（`#/listen`）。
> 「点击展开全文阅读」是**同一屏的状态迁移**（`body.listen-mode` 复用任务模式那套 `.layout` 正文 +
> 底部 `.audiobar`），**不是新浮层、也不是新页面**；播放控制复用影子跟读既有播放链，未新增浮层，本表无需扩列。

### 2.5 技术底座

| 项 | 决策 | 理由 |
|---|------|------|
| **部署路径** | **3.0 做新站点，不动现有两个应用**：`/app/`（新目录，草案）承载 3.0；`/`（情景训练）与 `/shadow/`（影子跟读）**原样保留、照常可访** | 2026-09-24 定：旧版内容是资产，不做替换、不做「丢掉」（§11.3）。⚠️ 是否改用**独立域名**（如 `3-0.ielts-vocab.bond`）见 §14 |
| 旧应用保留 | `/` 与 `/shadow/` 一行代码不动 | 3.0 与它们**并存**，互不依赖 |
| 单页 vs 多页 | 仍然是单页 HTML + hash 路由（`#/home` 等），不引入框架 | 沿用现有架构（`index.html` 4636 行 / `shadow/index.html` 6725 行，都是零依赖单文件）。**3.0 主要是把影子跟读那套运行时（6725 行）搬过来 + 加外壳与两个新页面，预计 7000–9000 行**；真实体量以实施为准，若超 10000 行则拆 `app.js` / `app.css` 两个外部文件（同 `shadow/js/plan-engine.js` 的做法，加进构建白名单） |
| 数据层 | 沿用 `ielts.shadow.v2`（`ROOT2` 事件流 + 词状态机引擎 `plan-engine.js`） | v2.0 的间隔引擎、词状态机、事件流重放**原封不动搬过来**，只换驱动方式。⚠️ **localStorage 是同一浏览器、同源共享的**：3.0 在 `/app/` 下的 `localStorage` 与 `/shadow/` **不是同一份**（路径不同不影响 origin，同源就是同一份！）—— 见下方注释 |
| 发布白名单 | 新文件（`app/index.html` 等）必须同时加进 `scripts/build_site.mjs` 的 `FILES` | 该文件是**显式白名单**，没列进去的文件公网不可见、且构建自检会直接失败（见 AGENTS.md） |
| PWA | 把主站的 service worker + manifest 搬过来 | ⚠️ **影子跟读目前没有 service worker**（`grep serviceWorker shadow/index.html` = 0 命中），主站有 —— 3.0 要继承的是主站那一套 |

> **关于同源共享（重要，别踩）**：`ielts-vocab.bond/app/` 与 `ielts-vocab.bond/shadow/` **同源**，
> 所以 3.0 和影子跟读会**共用同一份 `localStorage`**。好处是老用户的进度直接续上（§12.1 的无缝升级成立）；
> 风险是两边同时在跑时，后写的会覆盖先写的。
> 处置：3.0 **继续用 `ielts.shadow.v2` 这个 key、不改 schema**（读写都兼容），
> 并在 `VERSION`/PRD 里写明「3.0 与影子跟读共用进度，同时开两个标签页可能互相覆盖」。

---

## 3. 首页 · 六篇文章卡片

### 3.1 页面结构

```
┌──────────────────────────────────────────┐
│  顶部问候区（Hi / 今天读多久 / 继续/开始按钮）│
├──────────────────────────────────────────┤
│  ┌────────┐ ┌────────┐ ┌────────┐        │
│  │ 文章 1  │ │ 文章 2  │ │ 文章 3  │       │  ← 6 张卡片
│  └────────┘ └────────┘ └────────┘        │     桌面 3 列
│  ┌────────┐ ┌────────┐ ┌────────┐        │     平板 2 列
│  │ 文章 4  │ │ 文章 5  │ │ 文章 6  │       │     手机 1 列
│  └────────┘ └────────┘ └────────┘        │
└──────────────────────────────────────────┘
```

### 3.2 文章卡片数据（每张 4–5 个数，不堆砌）

按「帮用户判断这篇文章学得怎么样了」挑，每组只留最能说话的：

| 数据 | 来源 | 说明 |
|------|------|------|
| **文章标题**（中 + 英） | `SECTIONS[n].title` / `TIT_EN` | 一行中文 + 一行小字英文 |
| **熟练度** | §9.2 公式 | 一根 0–100% 的进度条 + 百分比数字，是卡片**视觉最重**的元素 |
| **当前阶段** | 派生（§9.4） | 一枚状态胶囊：`未开始` / `通读中` / `② 可答题` / `已学完` / `熟练` |
| **单词掌握** | **需新增** `countStagesOf(article)` | `已毕业 X / 全篇 Y 词`。⚠️ 现有 `countStages()` **只统计全局、没有按篇能力**（`for (const k in ROOT2.state.words)` 一把梭），要新建一张「词 → 篇章」索引，见 §9.5 |
| **最近学习** | `state.sents` 最后接触时间 | `3 天前` / `今天`，相对时间 |

> **不做的**：学习次数、跟读次数、已学习天数 —— 这些在「学习数据」页里看，卡片上放不下也不该放。

### 3.3 卡片状态视觉（4 档）

| 状态 | 判据 | 视觉 |
|------|------|------|
| **未开始** | `sentDone = 0` 且 `quizDone = 0` | 灰底 + 虚线进度条 + 半透明标题 |
| **学习中**（通读 / ②） | 有进度但未满 | 正常卡片 + 薄荷进度条 + 状态胶囊 |
| **已学完** | 通读 ≥1 遍 + ② ≥1 遍 | 进度条满 + 绿色胶囊 `已学完` |
| **熟练** | 熟练度 ≥ 80% | 进度条换金色渐变 + `熟练` 徽章 |

### 3.4 点击行为

点卡片 → **进入该文章的任务模式页面**（§4），不是阅读详情页。

### 3.5 顶部「今天该做什么」

首页顶部一条窄横幅（不是弹窗），内容：

- 还没建计划 → 「设置你每天的学习时间」按钮 → 展开设置屏（复用 v2.0 的 `renderSetup`）
- 有计划 & 今天没做完 → 「继续学《XX》· 还剩 N 句」 + 开始按钮
- 有计划 & 今天做完了 → 「今天的量读完了 · 多读了 N 句」（沿用 v2.0 的如实口径）
- 有计划 & 连续 N 天 → 「连续 N 天 · 已毕业 M / 3245 词」

---

## 4. 文章任务模式（进入卡片后）

### 4.1 整体结构（参考情景训练阅读器 + 影子跟读任务模式）

```
┌──────────────────────────────────────────┐
│ ← 返回首页 │ 文章标题 │ 卷/段筛选        │  ← 顶栏
├──────────────────────────────────────────┤
│                                          │
│       当前学习任务内容区                  │
│       ① 通读态 = 正文 + 高亮当前句        │
│       ② 挖空态 = 正文里挖空 + 浮窗选择    │
│                                          │
├──────────────────────────────────────────┤
│  任务条（一条浮空玻璃卡）                  │  ← 底部固定
│  进度 n/N + 位置条 + 放这一句/下一句/…   │
└──────────────────────────────────────────┘
```

- **顶部**：文章信息（标题 + 卷名）+ 筛选（按卷/段快速跳，沿用 `#jumpPop`）
- **中间**：任务内容（① 通读 / ② 挖空，两态切换）
- **底部**：任务条（v2.0 的 `#taskBar` 一条通栏、三态换内容、不藏控件）
  - **① 辅行**（2026-09-25 用户加）：`今天 N/M 句 · 覆盖 K 词`。K = 今天这批句子去重后的目标词数，
    与今日面板「还剩 N 句 · 覆盖 K 个词」同一份派生（`todayPlan().words`，`assemble` 去重后的目标词集合），
    不各算一套。标签继续写在脸上（主行/气泡=本篇、辅行=今天，不破坏 2026-09-24 的口径）。

### 4.2 复用 v2.0 任务模式的完整逻辑

以下**全部继承 v2.0 已上线的实现，不重写**：

| 能力 | v2.0 实现 | 3.0 改动 |
|------|-----------|---------|
| 一步一停播放 | `playFrom` 跳过预取 + `taskSentenceFinished` 完成计数 | 无改动 |
| 那颗键担两义 | 「放这一句」→「下一句」+ 点过整趟不变 | 无改动 |
| 放完不自动挪高亮 | `taskSentenceFinished` 只记完成、不 `goToNextTask` | 无改动 |
| **② 挖空答题** | `buildQuiz` 四层闸门 + 辨析干扰项 + 补考一次 | **引擎不变，UI 重做**：从「底部题卡」改成「**文内挖空 + 浮窗选择**」（§4.4） |
| 遍间小结（①→②） | `renderPassSummary` 三个数字 | 保留，它就是 ② 的**入口提示**（§4.4「入口」） |
| 收工态 | 「今天完成 · N 句 · M 词晋升」 + 看词本 | 按钮改为「回首页」+「看词本」（跳单词本页） |
| 续读 | 刷新后任务条变续读条「今天 N 句 / 接着做」 | 无改动 |
| 队列排程 | `ShadowPlan.assemble`（三池 + 分钟预算 + 保温） | **驱动从「全局队列」改为「按篇队列」**：进入第 N 篇时 assemble 只排这一篇的句（§9.3） |

### 4.3 ① 通读态（正文阅读）

- 正文布局沿用影子跟读的 `#art`：段（`.para`）→ 句（`.sent`），行内 `[[词:形式]]` 标记 + 点词弹释义卡（`openPop`）
- 译文开关 / 行内词义开关 / 目标词数 → **文章头部那一条**（不是另起第二排）。
  **2026-09-25 用户（G）定左右**：`目标词` pill 在左侧、紧跟「第 N/6 篇」；`全句译文` + `行内词义`
  两个 iOS 拨杆在右侧、紧在「上一篇」之前；整条随头部 `.shrunk`（滚过 40px）**等比例缩放**，缩了不错位。
  （口音/音色不在这里，已收进「我的」浮窗，§8.1。）锁见 `playwright-tests/journeys/app3/header.spec.ts`。
- 当前句高亮 `.playing` + 自动滚动跟随（`scrollIntoView block:center`）
- 播放条 = **任务条那一条**（不是底部另一个 audiobar），v2.0 的做法

### 4.4 ② 挖空答题（文内挖空 + 浮窗选择）

> **2026-09-24 改口径**：② 不再是「底部弹出一张题卡」，改成**挖空直接长在正文里、选择用浮窗**。
> v2.0 的底部题卡（`#taskCard` + `renderQuiz`）形态作废；出题引擎（`buildQuiz` 四层闸门 / 四选一 /
> 补考一次 / 辨析干扰项）**原封不动**，只换承载它的 UI。

```
① 通读完成
    ↓  （遍间小结卡提示「进入答题」）
② 挖空态：正文里把本遍的目标词挖成空
    ↓  （点空 / 点任务条的「下一题」）
浮窗答题卡（四选一，锚在那个空上）
    ↓  （选完）
空被填上：对=绿字 / 错=红字＋正确答案，错的再摊辨析表
    ↓  自动推进（停留：对 ~1s / 错 ~2.2s；也可点「下一题」立即推进）
定位到下一个还没答的空 → 弹浮窗 …… 答完 → 小结 → 收工
```

| 项 | 规格 |
|----|------|
| **挖空位置** | 正文对应句里的**目标词位置**（`.sent` 内联 `<span class="qz-blank">`），不另起容器 |
| **空的样子** | 下划线 + 按被遮词形给宽度（沿用 `maskSplit` 口径）；三态：未答 / 答对 / 答错 |
| **点空** | 弹出**浮窗答题卡**，锚在该空附近（复用 `.prog-tip` 那套玻璃浮层），里面 4 个选项 + 「这题有问题」 |
| **选完** | 浮窗收掉 → 空里填上所选词；答错就地摊开辨析表（原来那套 `revealNote`） |
| **答完自动下一题（2026-09-24）** | 选完**先显示对/错**（`#quizLive` 同步播报），停留后**自动**滚到下一个未答的空并弹浮窗；最后一题答完 → 直接进小结/收工。停留 = `max(对 1s / 错 2.2s, 播报文本长度 × 90ms)`，答错更久（看答案 + 辨析）。「下一题」按钮**保留**（键盘 / 想多看一眼 / 自动化），手动与自动共用一条 `nextBlank()` 且互斥（不跳两题）。自动推进**不抢焦点**（避免截断 polite 播报），reduced-motion 下滚动改 `instant`。锁见 `task.spec.ts` 的 `② 答完自动下一题`。 |
| **「下一题」** | 底部任务条那颗键：滚到**下一个未答的空**（居中）+ 自动弹浮窗；没有下一个 = 进小结 |
| **「这题有问题」** | 从题卡头部移进浮窗 |
| **「回看句子」** | **去掉** —— 句子就在眼前，不需要"回看" |
| **进度** | 任务条仍报 `② 挖空答题 3/24`（同一个 n/N 口径，见 §10.3 数字唯一性） |
| **键盘** | 浮窗打开时 1–4 选择、Esc 关浮窗；`→` = 下一题 |

**入口（两条，都要做）**

| # | 入口 | 行为 |
|---|------|------|
| 1 | **今天额度读满即提示** | **触发判据（2026-09-24 修正）**：`todayQuotaDone()` = 今天这一本账读满（`daily.sentDone ≥ plannedDay`，当天快照定的 N 句），**与这一篇读没读完无关**；另外「本篇队列一句不剩」照旧触发（M1 既有路径，两条路都在）。触发时 → 遍间小结卡（沿用 `renderPassSummary`）→「开始答题」按钮 → 进 ② 态，正文立刻挖空。任务条那颗推进键在额度读满时从「下一句」改成「开始答题」，点了直接进 ② —— 不许再把人往「多读」上引。 |
| 2 | **首页卡片直达** | 满足任一即出「答题」：① 通读已完成（`everRead = 全篇句数`，§9.4 的 `read`/`quiz` 态）；② **今天额度读满且这一篇正是今天读的那篇（还没读完）**。点了直达该篇 ② 态。数据页「今天」块与「待加强」的今日建议指向同一入口。 |

> **为什么入口 1 的判据要改**：队列是按篇装配、且排程滚动；一篇 339 句、当天额度 180 句时，
> 读完今天的 180 句队列里还剩一大截没有读 → 旧的「本篇队列走完」判据永远不成立，小结不出现、
> 主按钮还写「下一句」，用户按计划做完了今天的量却拿不到今天的题（2026-09-24 实测，337/339 与
> 180/180）。新的额度口径只认「今天该读的句读完了」，把学习闭环接回来。
>
> 入口 2 的意义：今天只想做题、不想通读，也不该被挡在门外。直达 ② 时批次用当天 `assemble` 的
> items 现建（与"① 后进 ②"同源），**不要求先跑一遍 ①**。

### 4.5 任务模式里点句 = 一步一停（2026-09-24 定）

**点正文里任意一句 = 只放那一句**（`playFrom`，高亮跟过去），依旧是「放完不自动往下走」——
和「放这一句」那颗键同一套口径，**不切换成连播**。

> 早前草稿写过「点句 = 在当前位置改为自由阅读（自动连播）」，**2026-09-24 用户否掉了**：
> 任务模式里就保持一步一停，最稳、不会意外连播。
> 想连播精读整篇 → 去「随身听」（§7），那是它唯一的职责。
>
> **键盘快捷键**（2026-09-25 用户点名保留，与影子跟读同一套全局 handler）：`空格` = 播放/暂停当前高亮句，
> `←` = 上一句，`→` = 下一句（任务模式里 `→` 走 `TASK.next()`，高亮与队列位置一起带走）。
> 输入框获得焦点、或焦点在句/词上时不抢键。锁见 `playwright-tests/journeys/app3/keyboard.spec.ts`。

---

## 5. 学习数据页

### 5.1 页面定位

回答四个问题（页面按这四问分四块，**每块标题就是问题本身**）：

> **我最近学得怎么样？** → 学习总览
> **我的文章掌握到了什么程度？** → 文章学习
> **我的学习是否持续？** → 学习趋势
> **哪些内容还需要加强？** → 待加强（行动导向）

> 页面另有两块：**单词掌握**（§5.5，回答「我的单词掌握到了什么程度？」）与
> **随身听**（§5.7，独立记账、M2 先空态占位）。四问四块是主线，这两块是补充。

### 5.2 双阶段形态（未开始 vs 进行中）

| 阶段 | 判据 | 页面形态 |
|------|------|---------|
| **未开始** | `ROOT2.plan` 为空 | 大标题「开始你的学习计划」+ 一句话 + 一个按钮（打开「我的」浮窗）→ 计划三旋钮收在「我的」里，**不内嵌计划表单**（2026-09-24 用户改口径，见 §8.1） |
| **进行中** | 有计划 | 四块数据（§5.3–5.6） |

### 5.3 学习总览（5 个数）

| 数据 | 来源 |
|------|------|
| 累计学习天数 | `Object.keys(state.daily).length` |
| 累计学习时长 | `daily[*].minutes` 求和 |
| 连续学习天数 | `streak2()` |
| 完成文章数 | 六篇中已学完的数量 |
| 总学习次数 | `daily[*].sentDone` + `quizDone` 求和 |

### 5.4 文章学习（6 篇横排小卡）

每篇一行：标题 + 熟练度百分比 + 微缩进度条（复用首页卡片的计算，只是更紧凑）。点击 → 跳首页并高亮该卡片。

### 5.5 单词掌握（4 个数）

| 数据 | 来源 |
|------|------|
| 已学习单词 | `Object.keys(state.words).length` |
| 已掌握（已毕业） | `countStages().graduated` |
| 待巩固（重点词） | `countStages().leech` |
| 掌握率 | 已毕业 / 3245，百分比 |

### 5.6 学习趋势（简单折线，不引图表库）

- 近 14 天每日 `sentDone + quizDone` 的条形图（纯 CSS 柱状，高度按比例）
- 近 14 天每日学习时长的折线（SVG 一条线 + 两个端点，约 20 行代码）
- **不引入 Chart.js / ECharts** —— 数据量 14 点，手绘够了

### 5.7 随身听（独立一块，与精读分开）

「随身听」的数据**单独算数**（§7.6），所以这里给它一块自己的：

| 数据 | 来源 |
|------|------|
| 收听句数 | 随身听路径记的收听事件条数（该篇 / 全部） |
| 收听时长 | 随身听会话时长累计（按 `daily[*].minutes` 拆出来的那份） |
| 最近收听 | 每篇最后一次随身听的相对时间 |

> 不显示「收听掌握度」这类伪造指标 —— 听得多不等于会，掌握看 §5.5 的词状态。

> **交付分期**：M2（2026-09-24）这一块先放**空态占位**（「随身听还没用过 → 去试试」），
> M4 随身听落地后再接真数据 —— 空态期间不显示任何伪造指标。
>
> **M4 落地**（2026-09-24）：这一块已接**真数据**（播放卡片 + 展开全屏阅读 + 独立收听账，见 §7）。
> 数据来自随身听自己的收听账（`LS_ARTICLE.listen`，§7.6 与精读分开），显示**收听句数 / 收听时长 / 最近收听**
> （含每篇一行）；M2 那句「随身听还没用过」的空态文案已去掉。§5.7 的原则不变：**不显示「收听掌握度」这类伪造指标**。

### 5.8 「待加强」（行动导向，不是数据堆砌）

页面底部一个卡片，列 2–3 条**具体可点**的建议：

- 「《XX》还差 N 句读完 → 继续」
- 「有 M 个重点词今天到期 → 去复习」
- 「已经 N 天没学《XX》了 → 回去看看」

每条带一个按钮，点了直接跳到对应入口（首页卡片 / 单词本筛选 / 随身听切篇）。

---

## 6. 单词本页

### 6.1 核心理念

> 不要让用户脱离文章孤立背单词 —— 单词始终跟文章语境绑在一起展示。

### 6.2 列表结构

```
┌──────────────────────────────────────┐
│  筛选栏：全部 / 待掌握 / 学习中 / 已掌握 │
├──────────────────────────────────────┤
│  atmosphere                    已毕业 │
│  地球与生命 · 第 1 卷 · 出现 3 次     │
│  ─────────────────────────────────   │
│  oxygen                        文中可辨│
│  地球与生命 · 第 3 卷 · 出现 2 次     │
│  …                                   │
└──────────────────────────────────────┘
```

每行：词 + 当前状态胶囊 + 所属文章 + 出现次数 + 最近学习时间。

### 6.3 筛选（4 档）

| 档 | 判据（词状态机） |
|----|----------------|
| 全部 | 不筛 |
| 待掌握 | `stage ∈ {seen, recognized}` 或 `leech = true` |
| 学习中 | `stage = owned` |
| 已掌握 | `stage = graduated` |

### 6.4 单词详情（点行展开）

```
┌──────────────────────────────────────┐
│  atmosphere          [已毕业]  [重学]  │
│  /ˈætməsfɪər/  n. 大气层；氛围        │
├──────────────────────────────────────┤
│  📖 原文语境                          │
│  "The atmosphere protects life       │
│   from harmful radiation."           │
│  —— 《地球与生命》第 1 卷 · 第 3 句    │
│  [▶ 播放这句]                         │
├──────────────────────────────────────┤
│  📝 例句 / 其他语境                    │
│  "The meeting was held in a relaxed  │
│   atmosphere."                        │
├──────────────────────────────────────┤
│  学习状态：已见面 → 文中可辨 → 义项直连  │
│  接触 5 次 · ② 答对 3 次 · 无错误      │
└──────────────────────────────────────┘
```

**原文语境永远排在第一位**，例句其次 —— 这是「单词在文章中被掌握」这条理念在 UI 上的直接体现。

### 6.5 与文章的联动

- 词详情里的「▶ 播放这句」→ 跳到首页 → 自动进入对应文章的任务模式 → 定位到该句并播放
- 单词本不提供「脱离文章的背词模式」（无卡片翻转、无拼写测试）—— 那是 1.0 的事，3.0 不做

---

## 7. 随身听

### 7.1 定位与融合方式

> **视觉和整体页面结构 → 情景训练**
> **播放条交互 → 影子跟读**

具体：
- 页面骨架 = 情景训练的 `#lsPanel` 全屏浮窗风格（居中大卡片 + 玻璃底）
- 播放控制 = 影子跟读的播放条交互（点句即读 / 单句循环 2·3·5 遍 / AB / 倍速 / 书签 / 位置条）

### 7.2 播放卡片（默认态）

```
┌──────────────────────────────────┐
│      ┌──────────────────┐        │
│      │   当前文章封面区   │        │  ← 篇名 + 卷名 + 一句正在播的原文
│      │   正在播…          │        │
│      └──────────────────┘        │
│                                  │
│   ◄ 上一篇    ▶ / ⏸    下一篇 ►   │  ← 播放控制（影子跟读逻辑）
│                                  │
│   ────────●────────────         │  ← 句级位置条（可拖）
│   第 12 / 339 句 · 还剩 23 句    │
│                                  │
│   循环 3 遍 · AB · 1x · 书签     │  ← 影子跟读的四个弹层按钮
│                                  │
│      [ 点击展开全文阅读 ]          │
└──────────────────────────────────┘
```

> **2026-09-25（E/F，用户「随身听页面 UI 不如主站精致」）**：`/app/` 随身听页重做版式与卡片 ——
> 页面宽度对齐首页（`#appView` 全宽，不再锁 560 窄列）；**桌面横向两栏**（左封面卡：圆盘 + serif 篇名
> + 卷名 + 正在播句；右控制区），平板居中 620、手机单列；卡片用首页同款实底 + 精修阴影，封面卡
> 用 `accent-soft→白` 浅渐变 + 状态圆盘，「展开全文阅读」用锁定主按钮渐变。播放控制/位置条/四个
> 附加按钮/交互逻辑均不变（仍复用影子跟读那套）。

### 7.3 展开全屏阅读

点卡片任意处 → 展开全屏阅读弹窗：

- 正文 = 影子跟读的常规模式阅读页（`#art` 段→句结构 + 行内词义 + 当前句高亮 + 自动滚动）
- 播放条 = 底部玻璃播放条（影子跟读 `.audiobar`，含上一句 / 播放 / 下一句 / 循环 / AB / 倍速 / 书签 / 快速跳转）
- 关闭 → 收回随身听播放卡片（**不是**退回首页）

完整路径：

```
随身听 → 播放文章 → 点击卡片 → 全屏阅读 → 关闭 → 返回随身听
```

### 7.4 连续播放

- 默认连播：当前句播完自动进下一句（影子跟读的 `launch` 链，**不走**任务模式的一步一停）
- 篇末自动切下一篇（六篇循环），也可以在卡片上手动 ◄ / ► 切换
- Media Session 锁屏续播 + Wake Lock（v2.0 已实现，原样搬）

### 7.5 与首页的联动

随身听播的文章 = 「最近在学的那篇」默认值；用户切了篇之后，首页卡片上的「最近学习」也会跟着更新 —— 实现取 `max(sents[i].lastReadAt, listen.last[篇])`（`app/app.js:142-146`），即「最近一次精读」与「最近一次收听」里更晚的那个，**不再是单一 `lastContactAt`**（收听不推 contact，见 §7.6）。

### 7.6 随身听与任务模式**分开记账**（2026-09-24 定）

**两套数各算各的，不混：**

| 来源 | 记什么 | 进不进熟练度 | 界面读它的地方 |
|------|--------|-------------|--------------|
| **任务模式**（① 通读 / ② 答题） | **精读次数**（每篇一个累计值） | ✅ 进（§9.2 的 25% 那一项） | 任务条、首页卡片、学习数据页 |
| **随身听** | **收听数据**（每篇听了多少句 / 多长时间） | ❌ 不进 | 只在「学习数据」页有独立一块 |

- 任务模式 / 自由精读仍走引擎的 `contact` 事件（**一天一句只记一次接触**是 §9.1 的引擎口径，动不了、也不会虚高）；**收听是唯一例外：收听不推 `contact`**。
  - 为什么收听必须绕开引擎：`plan-engine.js:142` 里 `contact` 必然同时写词状态与 `sents[].lastReadAt`；而通读完成度 `everRead` 读的正是 `lastReadAt > 0`（`app/app.js:109`）。收听一旦推 `contact`，就会把「通读完成度 ×40%」一起抬上去 —— 与上表「收听不进熟练度」物理互斥。所以收听只能记在引擎之外的独立收听账。
  - 实现落点：`app/index.html` 播放完成回调里，随身听只调 `TASK.listenTick()`，只有非随身听的自由播放才 `TASK.onSentenceComplete()`（`app/index.html:4210-4221`）；任务模式的精读次数由手动按钮走 `taskSentenceFinished` 计数（`app/index.html:8262-8267`）。
- 但「**精读次数**」只数任务模式里的完成（`taskSentenceFinished`），随身听的一律不往里算
- 于是「口袋里挂着连播」刷不出精读次数、也刷不动熟练度 —— 想提熟练度只能坐下来精读

> v2.0 的老口径是「自由模式播放也 `recordRep`」，两套混在一起、还导致毕业门槛能被口袋连播刷穿；3.0 拆开。

---

## 8. 我的

### 8.1 只放这些（点底部「我的」一行 → 向上弹的浮窗）

| 区块 | 内容 |
|------|------|
| 头像 + 昵称 | 登录后显示昵称；游客显示「登录」按钮 |
| 账号信息 | 邮箱 / 登录状态 / 登录表单 / 退出按钮 |
| 学习摘要 | 累计天数 / 连续天数 / 已毕业词数（3 个数，比首页更简） |
| 当前状态 | 「正在学《XX》· 第 N 天」 |
| 数据管理 | 导出备份 / 导入恢复（沿用 `exportBackup`/`importBackup`，含 `ielts.shadow.v2`） |
| **主题** | 深色 / 浅色切换（iOS 拨杆） |
| **口音** | 全局英式 / 美式切换 |
| **音色** | 朗读音色选择（自定义玻璃浮层，分组 + 星级 + 试听） |
| **计划设置** | 每天分钟数 / 新词开关 / **重置学习计划**（2026-09-25 加；日界固定凌晨 4 点） |

> **2026-09-24 用户改口径**：「我的」不再是切页，而是侧栏底部**一条分割线 + 一行**（未登录「登录」/ 已登录头像+账号），
> 点它向上弹浮窗（§2.2）。主题（深浅色）、**口音切换**、**音色选择**都收进这个浮窗 —— 文章内的 `.r-badges` 不再放口音/音色
> （任务模式里要换口音/音色就先回一级页面开「我的」；沉浸式阅读页没有这两个入口）。

> **M5 落实**（2026-09-24）：上表九块全部在位并加锁（账号 / 学习摘要 / 当前状态 / 数据管理 /
> 主题 / 口音 / 音色 / 计划设置）。**数据管理健壮化**：导入先校验（坏 JSON 根、错 app、无可恢复数据一律拒绝）
> 再落盘，落盘任一步失败整份回滚（不半截覆盖）；备份格式仍是 v2.0 的 `{app:'ielts-shadow',backupVer:1,data}`，
> 覆盖 `ielts-*` + `ielts.shadow.v2` + `ielts.app3.article`。**账号**登录表单走 `<form>`：点「登录」与输入框回车
> 是同一条路。**无障碍**：浮窗 `aria-modal`、打开即送焦 / 关闭归还 `#meCard`、Tab 在浮窗内收敛、控件触摸目标 ≥44px。
> §8.2 的负向约束由 `playwright-tests/journeys/app3/me5.spec.ts` 锁住。

### 8.2 明确不放的

使用方法、性能与错误、帮助中心、产品说明、与核心学习无关的任何功能入口。

---

## 9. 数据模型与进度体系

### 9.1 底座不变

沿用 v2.0 的 `ielts.shadow.v2`（`ROOT2`）+ `shadow/js/plan-engine.js`：

- **词状态机**：`seen → recognized → owned → graduated`（+ `leech` 重点词通道）
- **事件流**：`contact / quiz / stage / relearn / dayplan` append-only，重放派生一切
- **间隔排程**：`wordInterval(reps)` 按接触次数排 `due`，毕业词 14 天回池保温
- **日账**：`daily[day]` 的 `sentDone / quizDone / correct / promote / minutes`

3.0 只加一个维度：**按篇统计**。

### 9.2 文章熟练度（新派生字段）

```
熟练度(article) =
    通读完成度 × 40%      ← min(该篇「每句至少读过一次」的句数 / 全篇句数, 1)
  + ② 正确率 × 35%       ← 该篇 quiz 正确数 / 该篇 quiz 总数（该篇还没有 quiz 记录时这一项记 0）
  + 精读重复度 × 25%     ← min(该篇「精读次数」/ (全篇句数 × 2), 1)
                          ⚠️「精读次数」只数**任务模式**里的完成；随身听不计（§7.6）
```

> 「× 2」意思是：每句平均精读 2 遍就算这一项拉满 —— 再多放不额外加分（避免刷次数）。
> 熟练度 ≥ 80% 且已学完 → 卡片显示 `熟练` 徽章。
> **三个权重是草案值**，上线后按六篇的真实分布再调（§14 第 2 条已确认「先按这组跑」）。

### 9.3 按篇队列（assemble 的驱动变化）

| | v2.0 | 3.0 |
|---|------|-----|
| 队列来源 | 全局排（六篇混排，三池 + 分钟预算） | **按篇排**：进入第 N 篇的任务模式时，assemble 只在这一篇的句子里排 |
| `assemble` 接口 | `assemble(state, {now, todayMinutes, rate, secNew, secReview, wordsOf, totalSents, boundaryHour, plan})` | **加一个 `scope`**：`assemble(state, {…, scope: Set<全局句号>})` —— 只从 scope 里挑句。**这是本次唯一的引擎改动**（`plan-engine.js` 纯加参数，不带 scope 时行为与 v2.0 完全一致，老测试不受影响） |
| `plan.newPerDay` | 按全局词数折算 | 按当前篇的词数折算（同样靠 scope 收窄） |
| 保温 / 到期复习 | 全局混排 | 只在 scope 内排（别的篇的到期词**不进**今天的队列，留到点进那篇时再排） |
| 事件流 | 不变 | 不变（contact / quiz 照推，引擎重放不区分篇） |
| 毕业判据 | 不变（12 次接触 + 1 次 ② 答对） | 不变 |

> ⚠️ 早前草稿写的「引擎不改、改调用方」**不成立**：`assemble` 的候选集来自 `state.words` 全量，
> 调用方没法在外面收窄而不破坏分钟预算（在外面 filter 会把预算算空）。所以老老实实加 `scope`。
>
> **为什么改**：v2.0 的全局队列适合「跟着排程走」的被动学习；3.0 的核心是「用户选一篇文章、把它学透」，按篇排让用户点进一篇文章时看到的是**这篇自己的**进度 n/N，而不是全局排程里混着六篇的 n/N。
> 代价：断更后欠的句子不再「顺延到全局队列」，而是「留在那篇文章里」—— 想起来哪篇没学完就点哪篇，这是 3.0 更鼓励的行为。

### 9.4 首页卡片的数据来源（全从 ROOT2 派生，不新增存储字段）

⚠️ **注意**：`sentDone` / `quizDone` 是**日账里的全局计数**，不能拿来判「某一篇学到哪了」。
按篇要用下面这三个派生量（都由 `state` 现算，不写盘）：

| 派生量 | 算法 |
|--------|------|
| `everRead(篇)` | 这一篇的句子里 `state.sents[i].lastReadAt > 0` 的条数（读「有没有碰过」，不是「今天读没读」） |
| `quizOf(篇)` | 事件流里 `type='quiz'` 且该句属于这一篇的条数 |
| `quizOkOf(篇)` | 同上，`ok=true` 的条数 |

| 卡片数据 | 计算 |
|---------|------|
| 熟练度 | §9.2 公式 |
| 当前阶段 | `everRead = 0` → `未开始`；`everRead > 0` 且 `quizOf = 0` → `通读中`；`everRead = 全篇句数` 且 `quizOf = 0` → `② 可答题`；`quizOf > 0` 且未满一遍 → `② 做题`；通读满 + ② 满一遍 → `已学完`；熟练度 ≥ 80% → `熟练` |
| 已毕业 X / 全篇 Y | `countStagesOf(篇)`（§9.5） |
| 最近学习 | 这一篇 `state.sents` 的 `max(lastReadAt)` 转相对时间 |

### 9.5 要新增的三张派生索引（实施时的清单）

现有代码里**没有**按篇统计的能力，这三样必须新建（都放 `plan-engine.js`，纯函数、可单测）：

| 名称 | 作用 | 为什么必须新建 |
|------|------|--------------|
| `wordArticle(word)` | 词 → 它出现在哪几篇 | `VOCAB` 只有词条，没有篇章归属；句子里的 `[[词:形式]]` 标记才是真来源 |
| `countStagesOf(article)` | 按篇的词状态计数 | 现有 `countStages()` 只扫全局 |
| `articleScope(article)` | 该篇的全局句号集合 | 按篇队列要喂给 `assemble`（§9.3） |

> 三个都是「句子 → 篇章」的同一个映射派生出来的，实现时一次建好、共用。

---

## 10. 视觉与交互规格

### 10.1 设计令牌（全部继承，不新造）

> **2026-09-24（第三轮，用户拍板）主按钮配色回退**：退回「深墨字」之前那一版 —— 渐变 `135deg #2bd4a4→#0fae7e`
> （轻薄荷）+ 白字/白图标 + 通透光晕 `0 3px 10px rgba(16,180,135,.25)`；三站（`/`、`/shadow/`、`/app/`）同值。
> 撤销第二轮的 `#2e9c76→#0f7c5a`（第二轮其余改动，如 `/app/` 左侧栏 `#f6f6f4`，保留）。
>
> **⚠️ 知情取舍（务必保留）**：白字压最亮端 `#2bd4a4` 实测只有 **1.90:1**（最暗端 `#0fae7e` = 2.85:1），
> **已知低于 WCAG AA 正文 4.5（纯图标也要 3:1）**。这是用户 2026-09-24 看过实测数字后**亲自选定**的品牌色，
> **知情接受**。因此门禁**不再按对比度判达标**，改为**锁定色值的回归锁**
> （`playwright-tests/journeys/app3/palette.spec.ts` 的 `LOCKED_GRAD` + `journeys/shadow/cta-and-popups.spec.ts`
> 的 `scanGradLock`，只防手滑改浅/改深/改字色）；第二轮的 `WHITE_MIN` 3:1 底线与「合成回归」探针已删除。
> 非主按钮（选中胶囊等）仍用深墨 `--cta-ink-dark`，按 AA 由实底胶囊用例管。

| 类别 | 值（已存在于两个应用中） |
|------|----------------------|
| 主色 | `#0c9c74`（边框/悬停/强调）/ `#10b487`（亮 accent）/ soft `#e0f5ec` / **主按钮渐变 `135deg #2bd4a4→#0fae7e`**（轻薄荷，白字实测 1.90~2.85:1，⚠️ 已知低于 AA，用户 2026-09-24 知情接受；门禁为色值回归锁，见上） |
| CTA 文字 | `--cta-ink #fff`（主按钮白字；选中态药丸等**非主按钮**用 `--cta-ink-dark #04231b`，因为它们的底色是更亮的 `--task-new`，白字只有 2.66:1，深墨 6.26:1） |
| 错误 | coral `#e07a5f` |
| 提示 | gold `#f2b73f` / note `#c08a2e` |
| 背景 | 一级页面内容区 `#f8fdfa`（主站薄荷底）/ 左侧栏 `#f6f6f4`（偏灰的白，2026-09-24 二轮；竖向分隔线 `#e9e9e6`）/ **任务模式（沉浸式阅读）暖白 `#faf8f4`** / 深色 `#1c1a17`（暖黑） |
| 圆角 | 10 / 12 / 16 / 18px |
| 阴影 | `0 2px 12px` / `0 12px 40px` |
| 玻璃 | `blur(22px) saturate(170%)` + `rgba(252,250,246,.92)`（浮层用；文档流卡片只半透明不模糊，性能约定） |
| UI 字体 | PingFang SC / -apple-system 栈 |
| 正文/故事 | Georgia / Songti SC serif |
| 图标 | Remix Icon 4.x |
| 过渡 | `.25s` / `.15s` + `prefers-reduced-motion` 全压 |

### 10.2 断点

| 档 | 宽度 | 导航 | 内容区 |
|----|------|------|--------|
| 桌面 | ≥ 1024px | 左侧栏 220px | 3 列文章卡 |
| 平板 | 701–1023px | 左侧栏 180px（图标+文字收窄） | 2 列 |
| 手机 | ≤ 700px | 底部 TabBar（4 项 + 我的） | 1 列 |

> **2026-09-25（F，用户「首页适配还行，其他三个页面稀烂」）**：四个一级页统一到**首页的宽度口径** ——
> 内容占 `#appView` 全宽（不再各自锁 `stats/words=880`、`listen=560`），栅格/卡片间距/圆角/字号层级
> 跟着首页对齐：学习数据与单词本卡片圆角 16、间距 12、桌面单词本 2 列（平板/手机单列）；随身听见 §7.2。
> 文章内头部/正文内容框仍按 §4.3 的 `min(1084px, calc(100% - 36px))` 口径（`<2px` 锁在 `leftovers.spec.ts`）。

### 10.3 交互范式（已趋同的沿用）

| 范式 | 来源 |
|------|------|
| 点句即读 + `.playing` 句高亮 | 影子跟读 |
| 底部玻璃播放条 morph（收起成胶囊 / 展开全功能） | 影子跟读 |
| 任务条一条通栏、三态换内容不藏控件 | 影子跟读（v2.0 定稿） |
| 边缘竖标签抽屉（词库 / 目标词速查） | 两个应用都有 |
| 点词弹释义卡（`openPop`） | 两个应用都有 |
| `n/N` 数字唯一性（同一个数只在一个地方说） | v2.0 口径 |
| 触摸目标 ≥ 44px | v2.0 口径 |
| 深浅色完整 token 覆盖 | 两个应用都有（统一到影子跟读的暖色系） |

### 10.4 非功能需求（不是可选项，验收要查）

| 项 | 口径 | 来源 |
|----|------|------|
| 加载 | 正文异步渲完前**不能出现「空状态」误判**（v2.0 的 `x.i < sents.length` 静默过滤坑） | v2.0 教训 |
| 错误 | 数据 JSON 拉不到 → 明确报错 + 重试，不许白屏 | 主站现有 404 处理 |
| 离线 | 已访问过的资源走 Cache Storage；事件流是 append-only，离线照记、联网补传 | 主站 SW + v2.0 `queueEventUpload` |
| 无障碍 | 触摸目标 ≥ 44px、键盘可达（Tab 顺序合理）、`aria-live` 播报答题对错、`prefers-reduced-motion` 全压 | v2.0 §10 + 本轮审计的 a11y 缺口（`.qz-note` 缺 `aria-live`，3.0 要补） |
| 性能 | 首屏 < 2s（有缓存秒开）；文章卡片不引图表库（§5.6） | 主站口径 |
| 安全 | 无硬编码密钥；`config.js` 由构建期写、不进仓库 | AGENTS.md |

---

## 11. 不做什么

### 11.1 页面不做的

| 不做 | 理由 |
|------|------|
| 使用方法 / 帮助中心 / 关于产品 / 性能与错误 | 与核心学习流程无关 |
| 单独的「设置」一级页面 | 旋钮收进「我的」浮窗 |
| **脱离文章**的背词形态（卡片翻转 / 拼写测试 / 无语境刷题） | 3.0 核心是文章语境中习得。② 的四选一**保留**，但它长在文章里、锚在具体句子上（§4.4） |
| 社交 / 排行榜 / 打卡分享 | 单人学习产品，不做社区 |
| AI 对话 / 写作批改 | 超出当前范围 |

### 11.2 功能不做的

| 不做 | 理由 |
|------|------|
| 自由选篇组合成「自定义课程」 | 六篇就是六篇，不拆不组 |
| 篇章内句子级别「跳过不学」 | 每句都要过一遍，② 出题的底线 |
| 导入自定义文章 | 内容是核心资产，不做 UGC |
| 离线整包下载 | PWA + Cache Storage 够用（v2.0 已有） |

### 11.3 与现有两个应用的关系（2026-09-24 定稿：**并存，不替换**）

**决定**：旧版「情景训练」（`/`）与「影子跟读」（`/shadow/`）**都保留**，3.0 做**新站点**（`/app/`）。

因此上一版 PRD 里那份「3.0 会丢掉什么」的取舍表**作废** —— 什么都不丢：

| 现有内容 | 规模 | 处置 |
|---------|------|------|
| 22 章主题故事 | 135 篇 | **原样保留**在 `/`（情景训练） |
| 词库《词汇真经》 | 3674 条 | **原样保留**在 `/` |
| 例句/补充词 | 1510 条 sup | **原样保留**；其中适合的例句可以**复制**进 3.0 单词本的「例句 / 其他语境」（§6.4），但不动 `sup.json` 本身 |
| 6 篇跟读文章 + 3245 目标词 | 1833 句 | 3.0 的**主内容**，从 `/shadow/data/` 复制一份到 `/app/data/`（保持 `SHADOW_DATA_VER` 口径） |

> **门牌号已定（§14.1 第 10 条）**：3.0 = `ielts-vocab.bond/app/`，与 `/`、`/shadow/` 同一栋房子、同一个大门（登录共用）、同一本账本（进度共用）。
>
> 取舍的代价从「丢内容」变成「维护面变大」：三套入口、三份内容源。
> 缓解：3.0 **只复制**跟读那套数据（sections/vocab），**不引用** `/` 的故事数据；
> 数据同步仍以 `shadow/data/` 为唯一真值源，3.0 的 `app/data/` 由构建脚本复制（不手改）。

---

## 12. 迁移与兼容

### 12.1 用户数据（3.0 与旧版**同源共用**）

⚠️ `/app/`、`/`、`/shadow/` 是**同一个 origin**，`localStorage` 是**同一份**。所以：

| 场景 | 处理 |
|------|------|
| 影子跟读老用户 | 3.0 读同一个 `ielts.shadow.v2`，事件流重放后词状态 / 日账 / streak / 书签全在 → **天然无缝**，不需要迁移代码 |
| 同一个浏览器开两个标签页（3.0 + 影子跟读） | ⚠️ 两边都会写同一个 key，**后写的覆盖先写的**。不做锁（单人使用，成本不划算），只在文档与 `VERSION` 里写明 |
| 主站老用户（`learning_data` 表 / 22 章故事进度） | **与 3.0 无关**：3.0 不读主站数据，主站也不读 3.0 的。两套进度各自独立 |
| v1.x 用户（只有 `ielts-task-plan` / `ielts-task-progress`） | 走 v2.0 的迁移路径（`ShadowPlan.migrate`），3.0 不再单独做 |
| 老计划里残留的 `plan.boundaryHour`（2026-09-25 前建的） | **读时忽略、不迁移、不崩**：3.0 的 `bdHour()` 恒返 4，所有日界的读取都走它；`planConfig().boundary` 恒为 4，新计划不再写该字段。旧字段留着也不会被读（同源共享的 `/shadow/` 若再用会自行写回，互不干扰） |

### 12.2 部署（3.0 = 新路径，旧版一行不动）

| 步骤 | 内容 |
|----|------|
| 1 | **开分支开发**（AGENTS.md 纪律：`agent/<name>-3.0-<date>`，不直接动 `main`）。`/` 与 `/shadow/` 全程不动 |
| 2 | 新建 `/app/` 目录：`app/index.html`（+ 需要时的 `app/app.js` / `app/app.css`）；**全部加进 `scripts/build_site.mjs` 的 `FILES` 白名单**，否则公网 404 且构建自检直接失败 |
| 3 | 数据：把 `shadow/data/{sections,vocab,chapters}.json` **复制**成 `app/data/`（构建期复制，不手改）；`SHADOW_DATA_VER` 跟着 `shadow/` 那份走 |
| 4 | 本地全量 E2E 绿 + `validate_data.py` PASSED（AGENTS.md 的「完工前 3 件事」） |
| 5 | 合并到 `main` → EdgeOne 自动部署。线上验证：`/` 与 `/shadow/` 未受影响、`/app/` 是 3.0 |
| 6 | 切 tag `v3.0.0`，更新 `VERSION` 文件 |

**回滚预案**

| 情况 | 动作 |
|------|------|
| 3.0 有问题，先别让人进 | **把入口摘掉就行** —— 3.0 是新路径，旧版 `/` 和 `/shadow/` 压根没变。要么 `revert` 掉 3.0 那个 merge，要么在 EdgeOne 上把 `/app/` 下线 |
| 只想下线 3.0 的某一屏 | 每屏都是 hash 路由（`#/home` `#/stats`…），改路由表 + 重新部署即可 |
| 数据层出问题 | 与 v2.0 同一 key、同一 schema，**不需要数据回滚**；真出问题用「导出备份」先存一份再修 |

> ⚠️ 不能写「`git checkout v2.0` + 重新部署」：那是 detached HEAD，EdgeOne 只监听 `main`，根本不会部署。

---

## 13. 验收清单

### 13.1 功能验收（每条都要有 E2E 锁）

- [ ] 首页六张卡片，状态四档（未开始 / 学习中 / 已学完 / 熟练）视觉正确
- [ ] 点卡片 → 进入该篇任务模式，底部任务条 `① 通读 0/N`（N = 这一篇的句数，不是全局队列数）
- [ ] 任务模式两步制（① 通读 → ② 挖空）完整走通，收工态显示「今天完成」
- [ ] **② 挖空长在正文里**：空是 `.sent` 内联元素，**不存在**底部题卡这个容器
- [ ] **点空弹浮窗**：四选一在浮窗里；选完空里填上词（对绿 / 错红 + 正确答案），错的就地摊辨析表
- [ ] **「下一题」定位到下一个未答的空**（滚动居中 + 自动弹浮窗）；答完最后一个 → 小结
- [ ] ② 的「回看句子」**已移除**、「这题有问题」在浮窗里、键盘 1–4 / `→` / Esc 都对
- [ ] **入口 1**：今天额度读满（不要求本篇读完）→ 遍间小结卡出现「开始答题」，点了进 ② 且正文立刻挖空；任务条推进键此时写「开始答题」、点了直达 ②，不再写「下一句」
- [ ] **入口 1（M1 既有路径）**：① 走完整篇队列 → 同样端出小结「开始答题」→ ②（两条触发路并存）
- [ ] **入口 2**：首页卡片对「通读已完成」**或**「今天额度读满且今天读的正是这一篇」出现「答题」按钮，点了直达该篇 ②（不必先跑 ①）；数据页「今天」块与「待加强」指向同一入口
- [ ] 收工态「看词本」按钮 → 跳单词本页
- [ ] **没计划也能进任务模式**：任务条位置是空状态 + 「去设置」入口，点了跳配置（不是白屏/无反应）
- [ ] **随身听与精读分开记**：随身听刷再多也不涨「精读次数」、不动熟练度；学习数据页有独立的随身听一块
- [ ] **部署隔离**：`/`（情景训练）与 `/shadow/`（影子跟读）在 3.0 上线后**逐字节未变**
- [ ] 学习数据页四块数据正确（累计天数 / 文章进度 / 单词掌握 / 趋势）
- [ ] 未开始用户 → 学习数据页给一句话 + 打开「我的」的按钮（不内嵌设置屏）
- [ ] 单词本筛选四档（全部 / 待掌握 / 学习中 / 已掌握）计数正确
- [ ] 单词详情「▶ 播放这句」→ 跳首页 → 进任务模式 → 定位该句并播放
- [ ] 随身听：播放卡片 → 点卡片展开全屏阅读 → 关闭 → 返回播放卡片
- [ ] 随身听播放条 = 影子跟读的交互（点句即读 / 循环 / AB / 倍速 / 书签 / 位置条）
- [ ] 随身听篇末自动切下一篇
- [ ] 我的页面：登录 / 登出 / 导出 / 导入 / 深浅色 / 三个计划旋钮
- [ ] v2.0 用户数据升级后词状态 / streak / 日账不丢
- [ ] 深色模式全页面覆盖

### 13.2 视觉验收

- [ ] 左侧导航 4 项 + 底部「我的」一行（桌面）/ 底部 TabBar（4 项 + 我的，手机），无多余入口
- [ ] 文章卡片间距 / 圆角 / 玻璃效果与情景训练一致
- [ ] 任务条与 v2.0 影子跟读的任务条同款（一条浮空玻璃卡）
- [ ] 随身听卡片结构参考情景训练的 `#lsPanel`（居中大卡 + 玻璃底）
- [ ] 全局配色统一到影子跟读的暖底薄荷绿系（`#faf8f4` / `#0c9c74` / `#10b487`）

### 13.3 数据验收

- [ ] 文章熟练度公式（§9.2）在六篇文章上各算一遍，数值合理（0–100%）
- [ ] 按篇队列：进入第 N 篇后，`todayPlan(true).queue` 只含这一篇的句子
- [ ] `countStages()` 按篇过滤后，六篇的已毕业数之和 = 全局已毕业数
- [ ] v2.0 的 E2E 全量（157 条）在 3.0 合并后全绿或有意废弃

---

---

## 14. 决议记录与待确认

### 14.1 已拍板（2026-09-24，用户原话 → 落进本文哪一节）

| # | 决定 | 落点 |
|---|------|------|
| 1 | 旧版「情景训练」和「影子跟读」**都保留**，3.0 **做新站点** | §2.5 部署路径 / §11.3 / §12 |
| 2 | 熟练度权重（40 / 35 / 25，门槛 80%）**先按这组跑**，上线后按真实分布调 | §9.2 |
| 3 | **随身听的数据单独算数**；任务模式的算**精读次数**（两者分开记账） | §7.6 / §9.2 |
| 4 | **没计划也能进任务模式**，空状态给跳转入口 → 点了跳配置 | §2.3 |
| 5 | 手机导航：**底部 TabBar**（4 项 + 我的） | §2.1 / §10.2 |
| 6 | 深色切换放**顶栏** | §8.1 |
| 7 | 点词释义卡：**保留影子跟读那套**（辨析 / 同义词 / 词伙 / 段落小注） | §10.3 |
| 8 | ② = **「挖空选词」+「浮窗选择」，四选一**（不改引擎） | §4.4 |
| 9 | 六篇顺序**保持 1–6**（编号 = 章节号，跨章句号体系依赖它） | §9.3 / 附录 A |
| 10 | **3.0 放在同一栋房子的新房间**：`ielts-vocab.bond/app/`（不是另买域名） | §2.5 / §11.3 / §12 |
| 11 | **开发先交最小闭环**：先做「首页选文章 → 进去学完一篇」，数据页 / 单词本 / 随身听 / 我的先放着 | §14.2 |
| 12 | **任务模式里点句 = 一步一停**（不是自由阅读） | §4.5 |
| 13 | **3.0 与旧版不互通**（不在页面上互相带路） | §2.4 |
| 14 | 主站 3674 词书的例句**先不进** 3.0 单词本 | §6.4 |

### 14.2 交付顺序（已定：最小闭环优先）

| 阶段 | 交付物 | 用户能做什么 |
|------|--------|------------|
| **M1 · 最小闭环（第一个交给你看的东西）** | 外壳（左侧导航 4 项 + 底部「我的」一行，先只有首页能用）+ 首页六张卡片 + 文章任务模式（① 通读一步一停 → ② 文内挖空浮窗答题 → 收工） + 「没计划」的空状态与跳配置 | **真的学完一篇文章**，进度存档 |
| M2 | 学习数据页（四问四块 + 单词掌握 + 随身听空态 + 待加强） | 看见自己学得怎么样 |
| M3 | 单词本（列表 + 四档筛选 + 词详情原文语境） | 按文章查词 |
| M4 | 随身听（播放卡 + 展开全屏阅读 + 独立收听数据） | 磨耳朵 |
| M5 | 我的（账号 / 导出导入 / 摘要） | 管理账号与数据 |
| M6 | 收口：PWA、无障碍、深色全覆盖、全量 E2E | —— |

> **M1 的验收就一句话：点开一篇文章，能从头学到收工，进度存得住、刷新不丢。**
> M1 出来先给你试，你试过没问题我再往下做 —— 不把五个页面一次堆完再让你第一次上手。

> **M3 落实**（2026-09-24）：单词本（列表 + 四档筛选 + 词详情原文语境 + 词详情→该篇播放联动）已交付。
> M2「待加强」的「去复习」现落到 `#/words/todo`（「待掌握」筛选视图）。

> **M4 落实**（2026-09-24）：随身听（播放卡默认态 + 展开全屏阅读 + 连播/篇末切篇 + 独立收听账 +
> 学习数据页真数据 + 首页联动）已交付。**§7.6 分开记账**：收听不推 contact（否则经 `lastReadAt`
> 抬高 `everRead`，见 §7.6 的原因）、不进精读次数/熟练度，只进 3.0 私有的收听账
> （`ielts.app3.article` 的 `listen` 段，不新建 key、不给共享 `ielts.shadow.v2` 加字段）。
> **已知代价（标注，留待下一批）**：这份按篇收听账住在 `LS_ARTICLE`，**不进云同步** —— 换设备会丢。
> 本次只如实记录，不改架构。

> **M5 落实**（2026-09-24）：「我的」（账号 / 导出导入 / 学习摘要 / 当前状态 + 主题/口音/音色/计划设置）
> 已交付；导入校验与失败回滚、浮窗无障碍（焦点管理 + `aria-modal` + Tab 收敛）与触摸目标 ≥44px、
> 手机端底部抽屉形态均有 E2E 锁（`playwright-tests/journeys/app3/me5.spec.ts`）。
> **§8.2 负向约束**同文件锁住。
>
> **重置学习计划**（2026-09-25，用户补做）：与旧的「清空重来」（`resetPlan`，进度连根清掉）分开，
> 新增 `resetLearningPlan` —— 二次确认写明「会重置：分钟数 / 新词开关 / 开始日期 / 今天排程；不会动：
> 已学进度 / 单词状态 / 连续天数 / 学习数据」。重置后经 `pendingReplan` 重建设置时 `initPlan` **只换计划、
> 保留事件流与词状态**（§9 是核心资产）。锁见 `me5.spec.ts` 的「重置学习计划」。

> **M6 落实**（2026-09-25）：收口完成。**无障碍**：`@axe-core/playwright` 对 `/app/` 全页/浮层
> （4 路由 × 浅深 + 我的浮窗 + 任务模式 + ② 浮窗）扫描 **0 违规**（含 best-practice）；修掉
> 对比度 token 化、`.sent` 嵌套交互、`role=switch` 上的非法 `aria-pressed`、标题跳级、常驻 dialog
> 未 hidden；头部/任务条控件有效触区 ≥44px（`a11y.spec.ts` 回归锁）。**深色全覆盖**：`--iel-*`
> 别名在 `body.dark` 重声明、`html:has(body.dark)` 兜底基色、逐页浅深截图核对。**PWA**：真实
> `/app/sw.js` + 离线兜底页（无网无缓存不白屏）。**N1**：续读条不再盖底部控件。**全量 E2E**：
> `--repeat-each=3` 882/882、连续全量 294 全绿。主按钮配色**未动**（仍是用户知情的浅薄荷 + 白字）。
>
> **M6 a11y 诚实化补丁**（2026-09-24 复审）：旧 `a11y.spec.ts` 只断言 `violations===''`，无视
> `incomplete` —— 首页「未开始」卡片整卡 `opacity:.62` 把文字压到 3.1:1（AA 要 4.5）却被掩成
> 0 违规。现已：① 去掉整卡透明度，改只虚化装饰（浅色四档文字实测 4.62–5.68:1）；② 门禁把
> `color-contrast` 的 incomplete 也纳入判定，仅显式登记的「axe 算不了背景」清单可放行，并加了
> 一条注入低对比度元素必红的自检；③ 覆盖面从首页/任务①扩到 学习数据/单词本/随身听/我的浮窗/
> ② 浮窗。见 `playwright-tests/journeys/app3/a11y.spec.ts`。

---

## 附录 A · 六篇文章基础数据

| # | 标题 | 段 | 句 | 词条数 | 卷数 |
|---|------|----|----|--------|------|
| 1 | 地球与生命 | 58 | 339 | 571 | 4 卷 |
| 2 | 校园与文化 | 52 | 312 | 615 | 2 卷 |
| 3 | 衣食住行 | 40 | 232 | 458 | 2 卷 |
| 4 | 社会与规则 | 66 | 394 | 526 | 2 卷 |
| 5 | 历史与发明 | 36 | 215 | 424 | 2 卷 |
| 6 | 身体与时间 | 56 | 341 | 671 | 2 卷 |
| | **合计** | **308** | **1833** | **3245**（跨章去重） | |

## 附录 B · 术语对照

| 3.0 术语 | v2.0 代码对应 | 说明 |
|---------|-------------|------|
| 文章任务模式 | `enterTaskMode()` + `#taskBar` + 文内挖空 | 任务条与两步制逻辑原封不动；② 的承载 UI 换掉（§4.4） |
| 熟练度 | 新派生字段（§9.2 公式） | 首页卡片 + 学习数据页共用 |
| 按篇队列 | `ShadowPlan.assemble` 新增 `scope` 参数 | **唯一的引擎改动**；不带 `scope` 时行为与 v2.0 逐字一致（§9.3） |
| 按篇统计 | 新增 `wordArticle` / `countStagesOf` / `articleScope` | 现有 `countStages()` 只统计全局（§9.5） |
| 随身听 | `#lsPanel` 视觉骨架 + `.audiobar` 播放交互 | 两者融合 |
| 单词本 | `renderBookView()` 扩展 | 加文章关联 + 原文语境 |
| 我的 | 主站 `#me` 视图 + 影子跟读的备份/计划控件 | 合并 |
