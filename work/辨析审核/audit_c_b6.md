# 门禁 2 审核 · 批次 6 / 切片 41+42（6_05 + 6_06，worklist_idx 198–207，全部是三成员组）

审核者独立执行：只读 `shadow/data/sections.json`、`shadow/data/vocab.json`、两份切片包与两份草稿；只写本文件。
未跑 `tools/land_compare.py`（任何模式），未做任何 git 写操作，未改草稿或数据。
被审文件 mtime（自证审的是哪一版）：`work/辨析草稿/6_05.md` 2026-09-21 09:15:48（85 879 B）、`work/辨析草稿/6_06.md` 2026-09-21 09:09:04（91 327 B）。

- **组数 10 / BLOCK 0 / MISMATCH 4 / 通过 6**（MISMATCH = 198、201、202、207，全是"存在性断言／计数"那一族，不动内容骨架）
- 门禁 1 我自己重跑（不信作者自报）：`python3 tools/check_compare_draft.py` 对两份草稿 **都 PASSED（EXIT=0）** —— 但这只覆盖 code span，见下面 C 行的口径。

## 自算底账（全部我自己重算，未采信草稿「附／复核索引」里任何一个数）

| 项 | 我算出来的 | 与草稿自报的关系 |
|---|---|---|
| 全书句数 | 1833；六章实测 339 / 312 / 232 / 394 / 215 / 341；偏移 `[0,339,651,883,1277,1492]` | 与 BRIEF §二 一致；6_06 头写的「章3 66 段 394 句」我实测 66 段 394 句 ✓ |
| A 逐字符 | 58 行「该段原文」（6_05 28 行 / 6_06 30 行），去 `[[词头:表面]]`、去 `**` 后英文与 `sentZh` **双栏**脚本比对 → **58/58 全对，0 行不符**（含 721 `Natural`、718 「装点」这种译文比原文多字／大写靠标记撑住的格子） | 草稿未自报行数，我补上 |
| 撇号（单列） | 58 行里唯一带撇号的是 1218 `in Lin's hands`（6_06 组3 原文表＋items/diff 共 3 处引用；另 1272 `Lin's face`）——数据与草稿**都是半角 `'`**；数据侧 U+2019 只出现在 42/88/281/666/697/799/1540/1792 这 8 句，本片一律未引 | 无撇号风险 |
| B 句号 | 独立重建索引后逐组回算 21 个句号（6_05 11 个、6_06 10 个）：段内序号、成员 `[[词头:…]]` 位置、共现段全部复现，与两份切片包 `sents` 逐个一致 | **但 6_05 第 448 行自称"5 组 12 个句号"，实为 11 个**（636/637/638/718/719/852/853/878/879/880/881）→ 见组 198–202 汇总行的必修 |
| C 可 grep | 两份草稿共 1056 条 code span：其中 **363 条是字段名／路径／键值式串**（`cmp`、`paraZh`、`subheads[3][44]`、`all_synonyms: false`…）不计入，得 **693 条真英文引用串**：**632 条逐字命中**（在去标记后的 sections 原文或 vocab 的 m/ex/exZh/note/cmp 里）；未命中 61 条 = **10 条省略号缩略**（逐条回原句都能补全）＋ **38 条明写"本书 0 处／不这样写"的断言**（去重 35 条，反向 grep：**35/35 确实 0 命中，没有一条虚报"没有"**）＋ 13 条 `all_synonyms: true/false` 串（回 worklist 逐组复算，**10/10 与 worklist 标注一致**）。非 code span 的英文串（表格里、正文裸串）另扫一遍：正证据 0 条查不到 | 草稿自报"PASSED（0 条查不到）"方向正确，但它没算过"未命中里有多少是真否定行"——这个我替它算了 |
| D 宽度 | 20 条受上限约束的行（10 默认行 + 10 总结句）全部用 `tools/width_rule.width` 复量：**20/20 与标称一字不差**（默认行 37.0/40.0/40.0/39.0/36.5 ＋ 38.5/38.5/39.0/38.5/39.0；总结句 25.0/28.0/28.5/27.0/27.0 ＋ 25.0/22.5/23.0/20.0/23.0）；另 30 个 `core` 实测 5.0–10.0，全在 ≤12 内 | 无虚报 |
| E 义项照抄 | 按卡 `m` 切档（剥词性前缀、去括号、按 `；`/`、` 拆）后逐槽扫：**30 个 core / 10 条默认行 / 10 条总结句 0 命中**；10 张表头各命中 1–2 次，**全部是 §五.11 的豁免形状**（199/201/202/204/205/206/207 引用义项＋当场否定，且我把每条否定回数据验实为真；198/203/207 是引用＋验实） | 无 BLOCK |
| F 列数 | 10 张 items 表 30 行成员：列数 **7/7 与表头齐平**（批次 5 的"6 列错位"这一族本片 0 处）；diff 表全 5 列、原文表全 3 列、复核索引全 8 列；词性格一律只写词性、卡上第二档都落在 `sense` 格 → 无"词性＋义项挤一格" | 6_06 第 471 行自报"15 张表已逐行数过 `\|`"，我复核成立 |
| slug | 线上 178 张已落地表的 `cmp.group` 全量列出，与本片 10 个新组 slug（`-`.join(members)）逐一比对：**无一同名** → 不存在"静默覆盖旧表"这一类最严重事故 | 见「需要他拍」第 1 条的载体问题 |

---

## 组 198 · hop / jump / leap（6_05 组1，段 [1,49]）

- A 原文表：**OK** —— 6 行（635–640）英文＋中文逐字对得上；只粗三成员，635 `dive`、637 `plunge`、638 `spring`、639 `bounce`、640 `camp`/`picnic` 虽挂了词头都没粗 ✓（§五.16）。
- B：**OK** —— 636/637/638 ＝段 49 第 1/2/3 句，`[[jump]]`/`[[leap]]`/`[[hop]]` 实测在句内；分档复算：hop 标 1（638）＋未标 2（302 `hopped`、602 `hip-hop` 内含）✓；jump 标 2（636、1536）＋未标 4（53、290、306、921）✓；leap 标 1（637）＋未标 0 ✓；`hops/hopping/leaps/leaped/leapt/leaping` 全书 0 处（含卡片，词边界复验）✓；`paraZh` 段 49、`subheads[1][27]`/`[1][0]` 两条标题逐字 ✓。
- C：正证据逐条查到（两卡 `ex`/`exZh`、302、290、306、53、921、1536、602、`jumping kangaroo` ＝290 同源、别卡 `ex` 旁证 7 句）；**旁证计数我逐张卡扫过：jump 被 brook/hurdle/fence/parachute/daring/frog/kangaroo 各 1 句 ＝ 7 次 ✓；"hop 在别卡 ex 里 0 次"✓（唯一命中的是 hip-hop 卡自己那句，草稿已点名豁免）；leap 在别的卡 0 次 ✓**。否定行复验：`jump the queue`／`leap at the chance`／`a leap of faith`／`hitchhik` 全书 0 处 ✓。
- D：默认行 标称 37.0 / 实测 **37.0**；总结句 25.0 / **25.0** → 过。三个 `core` 实测 7.0/6.0/7.0 ≤12 ✓。
- E：**OK** —— 表头「worklist 说三词共同义项是『跳跃』，本段三处确实都在跳」是**肯定式引用**，我回 `sentZh` 验实（636 跳上跳下／637 蹦得老高／638 单脚跳）→ §六 名副其实，放行；`core`、默认行、总结句 0 命中 `m` 词。
- F：**OK（两处必须改，都在"全书唯一"式断言上）** —— 见下。**另**：草稿 line 82 引 hip-hop 卡写作「`n. 街舞；嘻哈文化`」，卡上实为 `n. 街舞；嘻哈文化；adj. 嘻哈的`（第三档被吞，建议级）。
  1. **L60（diff「句子的力气花在哪」/leap/一句话区别）："本书唯一给跳法配了程度词的就是 leap high" —— 不成立。** 本书卡侧另有一处程度词配跳法：`The kangaroo jumps high in the Australian wild.`（kangaroo 卡 `ex`），**草稿自己在 L81 出处清单就引了这句**，属同文自相矛盾。最小修法：限定成"本书**课文**唯一给跳法配程度词的是 637 `leap high`（卡侧 jump 也有一次 `jumps high`，见 kangaroo 卡 ex）"。
  2. **L64（diff「中文落点」/三个词/一句话区别）："本书译文从没把三词并成同一个中文词" —— 说过头。** 该行「常见搭配」栏自己引的三条 `exZh` 里，jump「跳过了水坑」与 leap「跳过了篱笆」**动词部分完全相同**；课文侧 302 hop 译「在扶栏边跳」与 921 jump 译「跳下来」也共用「跳」。最小修法：把范围收到本段——"本段三处译文没并成同一个中文词（卡 `exZh` 里 jump 与 leap 都作「跳过了 X」，那是卡侧、不在本段）"。
- **判定：需作者改（MISMATCH，2 格，都是把"本段/课文"的分布写成"本书"）＋ 文件头 1 处计数**：L448「5 组 12 个句号」→ 实为 11 个。其余可落地。

## 组 199 · adorn / embellish / ornament（6_05 组2，段 [2,11]）

- A：**OK** —— 6 行（717–722）双栏逐字对得上；719 一句里 ornament＋embellish 都粗、plain/festival 挂了词头但没粗 ✓。
- B：**OK** —— 718＝段11 第 1 句、719＝第 2 句；`[[adorn]]`、`[[ornament]]`、`[[embellish]]` 实测在句内；三词标 1＋未标 0 ✓；`adorned/embellished/ornaments` 只在各自卡 `ex`、`adorning/adornment/embellishes/embellishment/ornamental` 全书 0 处 ✓；`paraZh`、`subheads[2][0]` 逐字 ✓。
- C：正证据全查到（三卡 `ex`/`exZh`/词伙、516 `decorate jars with flowers`、717/720/721/722 背景句）。否定行逐条复验：`adorn oneself with jewellery`／`adornment`／`ornamental` 全书 0 ✓；「装饰」全书课文译文 1 处＝516、落 `decorate` ✓；「佩戴」「使美观」两档课文译文 0 处 ✓；`dress` 无卡 ✓；三卡 `cmp` 全空 ✓；**别卡 `ex`/`note` 里 adorn/embellish/ornament 各 0 次 ✓**（我按词边界逐卡扫）。
- D：默认行 标称 40.0 / 实测 **40.0**（贴顶，见下）；总结句 28.0 / **28.0**；core 8.0/7.0/7.0 ≤12 ✓。
- E：**OK** —— 表头「worklist 说三词共同义项是『装饰』，可本书课文译文一次没用过这个词」＝引用＋当场否定，且否定为真 ✓；`core`（往那样东西上装点／让素色的变好看／那枚小金饰本身）、默认行、总结句 0 命中 `m` 词。
- F：**OK** —— 7 列齐；`gold ornament` ＝719 主动并条；embellish 卡 `note` 空串、adorn/ornament 只有词伙段都如实写；"embellish 与 ornament 全部书证压在 719 一句"与实测一致；「不写的」栏（讲故事添油加醋那一档）如实标"卡 `m` 只给一档"。一处口径不统一（建议级，不改不拦）：L153 说「三卡 `m` 与**两张** `exZh` 都写『装饰』」，L174 又说「三卡 `exZh` 倒是都用『装饰』」——实测三张 `exZh` 全含「装饰」（ornament 是「彩色的装饰品」），按 174 统一。
- **判定：通过（可落地）**。提醒：默认行正好 40.0，落地时加任何字必超。

## 组 200 · delicious / tasty / yummy（6_05 组3，段 [2,34]）

- A：**OK** —— 6 行（849–854）双栏逐字 ✓；只粗三成员，851 mild/spicy、853 thirsty、854 spice/sauce 未粗 ✓。
- B：**OK** —— 852＝段34 第 3 句（delicious、yummy 同句两个标记实测都在）、853＝第 4 句 ✓；三词标 1＋未标 0 ✓；比较级词形全书 0 ✓；`paraZh` 段34、`subheads[2][0]` 逐字 ✓。
- C：正证据全查到（三卡 `ex`/`exZh`、chef 卡 `ex`、`simply yummy` 词伙＝852）；否定行复验：「美味」全书课文译文 **0 处** ✓、「可口」只在卡 `m` ✓、「好吃」1 处＝852 ✓、`tastier/tastiest/yummier` 0 ✓、`deliciously/tastefully/mouth-watering/yummy mummy` 0 ✓、tasty/yummy 在别的卡 `ex`/`note` 里 0 次 ✓、delicious 别卡只用过 1 次（chef）✓。
- D：默认行 标称 40.0 / 实测 **40.0**（贴顶）；总结句 28.5 / **28.5**；core 7.0/6.0/7.0 ✓。
- E：**OK** —— 义项词「美味的／可口的」在 core/默认行/总结句/表头 **全部 0 命中**（本片唯一一组四槽彻底干净）；表头走"引用＋否定"（「这个词本书课文译文一次都没用过」）✓ 且为真。
- F：**OK** —— 载体报备我实测：`tasty` 卡 `cmp.group` 实测就是 **`tasty-yummy`**、`at` 实测就是 **[2,34]**，与草稿写的一致；那张表成员确实只有 tasty/yummy、**通篇没出现 delicious**（我在其 cmp JSON 里 grep `delicious` → 0 命中）✓；新组 slug `delicious-tasty-yummy` 与线上 178 张表 **不同名**（不会被静默覆盖，但也不能挂回 tasty）✓。列数 7/7 齐；中文造景对得上（"主厨给客人做的那顿晚饭"＝chef 卡 ex；"又热又好的那碗汤"＝tasty 卡 ex）。
- **判定：通过（可落地）**。提醒：默认行正好 40.0。

## 组 201 · exterior / external / outside（6_05 组4，段 [2,39]）

- A：**OK** —— 6 行（877–882）双栏逐字 ✓；878 只粗 outside（同句 outward 没粗）、879 粗 exterior/external（dust 没粗）、880/881 归组 5 一律没粗 ✓（§五.16 教科书式执行）。
- B：**OK** —— 878/879＝段39 第 1/2 句；outside **标 13 处**的号我逐个回算：878、942、976、1080、1092、1095、1110、1147、1151、1270、1496、1521、1758 —— 与草稿**完全同一串**；段内坐标（942 章3段9第5、1080 章3段32第5、1270 章3段64第0、1496 章5段0第4、1521 章5段4第5、1758 章5段44第2）逐个实测对得上；exterior/external 标 1＋未标 0 ✓；1308 `outsider` 无卡、不算 outside ✓；`exteriors/outsides` 0 ✓。
- C：正证据全查到（三卡 `ex`/`exZh`/三串词伙、`external events` 跨卡同串=event 卡 ✓、别卡 `ex` 旁证恰好 5 句 downpour/suburb/landfill/throng/celebrity ✓）；否定行复验：「外部」全书课文译文 **0 处** ✓、`the outside`／`outside of`／`an exterior`／`exteriors`／`outside in`／`from the outside` 全书 0 ✓、exterior 在别卡 `ex`/`note` 0 次 ✓。
- D：默认行 39.0 / **39.0**；总结句 27.0 / **27.0**；core 5.0/6.0/6.0 ✓。
- E：**OK** —— 表头「也从没把任何一个译作卡上的『外部』」＝引用＋否定，实测为真；`core`（朝外那一面／外面的那层灰／门口那块地方）、默认行、总结句 0 命中 `m` 词。逐格回材料（本组是"外/内"两组里最容易写成词典排比的一张，实测没写成）：「哪一层外」三格分别押在 879 的 rails、879 的 dust＋三串词伙、878/942/1270/1496 的位置词上，每格都有可 grep 的原文，没有"侧重…而…则…"式对仗。
- F：**载体报备实测为真**：`exterior` 卡 `cmp.group` ＝ **`exterior-outward`**、`at` ＝ **[2,39]**；`external`/`outside` 两卡 `cmp` 均空 ✓；新组 slug `exterior-external-outside` 与线上无同名 ✓。7 列齐。两处要改：
  1. **L316＋L348：1147 漏归档，"共 6 处都不接"应为 7 处。** 实测 outside 带标记 13 处里"后面什么都不接"的是 1080、1095、1110、1147、1151、1496、1758 —— **7 处**；草稿把 1147（`wrote a warm slogan on the wooden door outside`）既没列进"带名词"那格、也没算进"不接"那格，L348 于是写成"共 6 处"。最小修法：L316 的"后面什么都不接"清单补 1147，L348 改"7 处"。
  2. L290 措辞：「exterior、external 在别的卡 `ex`/`note` 里各 0 次」——同卡同栏的**同义词栏**其实各列了对方一次（这正是本组 `all_synonyms: true` 的判据，L344 自己也写了）。建议改成"在别的卡的 `ex` 与词伙段里各 0 次"（同义词栏另计）。
- **判定：需作者改（MISMATCH 1 处实质：1147 漏档／6→7；另 1 处措辞）**，骨架与全部书证不用动。

## 组 202 · inner / interior / internal（6_05 组5，段 [2,39]）

- A：**OK** —— 4 行（879–882）双栏逐字 ✓（879/882 只当背景，成员词按本组粗，外词不粗 ✓）。
- B：**OK** —— 880＝段39 第 3 句（inner 与 interior 同句、实测都在）、881＝第 4 句（internal）✓；inner **标 4 处**（880、1562、1634、1764）＋未标 0 ✓，interior/internal 标 1＋未标 0 ✓，`internals/interiors` 0 ✓；1562/1634/1764 的坐标（章5 段11第4／段23第4／段45第2）实测对得上。
- C：正证据全查到（三卡 `ex`/`exZh`、三栏同义词、`inner itch`＝1562 / `inner organ`＝1634 跨卡同源、`internal corner`＝881 同源）；否定行复验：「内部」全书课文译文**恰好 2 处＝406、1634** ✓、「国内」0 处 ✓、`interior design`／`inner self`／`internally` 0 ✓、interior/internal 在别卡 `ex`/`note` 0 次 ✓、`inside` 无卡 ✓（且三栏确实都列了 inside ✓）。
- D：默认行 36.5 / **36.5**；总结句 27.0 / **27.0**；core 6.0/7.0/8.0 ✓。
- E：**OK** —— 表头「卡上那个『内部』在本段一次没出现」＝引用＋否定 ✓；四槽里 `core`（靠里的那一间／翻新的那一整片／贴在零件与角落前）、默认行、总结句 0 命中；本组同样逐格回了材料，没写成"内部的／里面的／里面的"排比。
- F：列数 7/7 齐；同源如实（三处都点名）。**一处必须改**：
  - **L407＋L421：把 `in-house`（406）称作"别的词头"—— 它不是词头。** 实测：406 的标记只有 `[[accounting]]` 与 `[[audit]]`，`in-house` **没打标记**，`vocab.json` 里**也没有 `in-house` 这个键**。这正是 §五.15 拦的那类（批次 4 的 `influence` 同款：下游照这句话去 `vocab.json` 找就撞空）。最小修法：改成"另一处落的是 406 里**未打标记、也没有卡**的 `in-house`"，并在 L421 那行末尾的括注里去掉"别的词头"。（"「内部」只 2 处 = 1634 + 406"这条断言本身我复验为真，不用动。）
  - 附带两处建议级：L407 与 L431 说 880/881「都落『里面的』」——881 的译文是「里面每一个角落」（无「的」），措辞收成「都落『里面』」；L374 说 interor/internal "在别的卡 ex/note 里 0 次"，同组 201 那处措辞问题（同义词栏各列了对方）。
- **判定：需作者改（MISMATCH 1 处：in-house 被写成"词头"）**，其余可落地。

## 组 203 · occupation / profession / vocation（6_06 组1，段 [3,53]）

- A：**OK** —— 6 行（1198–1203）双栏逐字 ✓；1200 粗 profession/occupation、1201 只粗 vocation（同句 saw/labour/dawn/night 挂了词头都没粗）✓。
- B：**OK** —— 1200＝段53 第 2 句、1201＝第 3 句；三词标 1＋未标 0 ✓；`paraZh` 段53、`subheads[3][44]` 逐字 ✓（实测 [3,53] 在 worklist 里出现 2 次：idx 106 clerk-personnel ＋ 203，与草稿"同段已有一张表、挂在 clerk 卡上"一致 ✓）。
- C：正证据全查到（三卡 `m`/`ex`/`exZh`/三栏同义词、1199/1202/1203 背景句、`honest occupation`＝1200、`true vocation`＝1201 同源）。否定行逐条复验，**全部为真**：`occupations/vocations` 0、`professions` 只在 profession 卡词伙 `pursue professions` 1 次（课文 0）、`professional` 恰好 athlete＋present 两卡词伙、`vocational` 恰好 institution 卡词伙、`occupy/occupied` 全书 0、`occupational` 0、`an occupation in banking`／`the teaching profession`／`professional development`／`vocational training` 0 ✓、`job`/`work` 无卡 ✓、三卡 `cmp` 全空 ✓。
- D：默认行 38.5 / **38.5**；总结句 25.0 / **25.0**；core 7.0/8.0/8.0 ✓。
- E：**OK** —— 表头「worklist 说三词共同义项是『职业』，本书课文译文确实把 1200 后半句译成了『正经职业』」＝肯定式引用，我实测为真（「职业」全书课文译文**只有 1200 一处**；草稿补的"其余 6 次全在 vocab.json：三卡 m 各 1、profession/vocation 的 exZh 各 1、embark 的 exZh 1"我逐张卡数过，**正好 6 次**✓）；`core`（养活全家那一份／开店够不上那一行／他自己认下的天职）、默认行、总结句 0 命中 `m` 词（「天职」不是 `m` 词，是 1201 的译文，实测「天职」全书课文译文 1 处＝1201 ✓）。
- F：**OK** —— 7 列齐；「占领」档如实（occupation 卡 `ex` 确实没演示本组义项，「占领」全书课文译文 0 处 ✓）；同源如实（两条词伙各并条，profession 侧多出的 2 条卡上文本明写"课文没用过"）；中文造景逐条回译（1200「专业行当／正经职业」、1201「天职」全对）。
- **判定：通过（可落地）**。作者提请拍板（occupation 卡 `ex` 走「占领」档）→ **不退、也不必再拍**：这就是 §五.6"档位错／书证在别处"的标准处置，草稿三件齐（标题说轻、义项只留 `sense`、数据边界点名"哪一档本书 0 处"），且"被否 / 被认 / 被抬成天职"这条分工线全部押在 1200–1201 两句可 grep 的原文上；"三侧各 1 处课文"按 §五.7 不是退回理由。

## 组 204 · dismiss / fire / layoff（6_06 组2，段 [3,54]）

- A：**OK** —— 6 行（1204–1209）双栏逐字 ✓；1206 只粗 fire、1207 粗 layoffs/dismiss（hire/discharge/resign/flour/sack/retire 都挂了词头都没粗）✓；表下如实报备 1207 表面形式是复数 `layoffs`、1204 是 `resumes`、1209 是 `sacks` ✓。
- B：**OK** —— 1206＝段54 第 2 句、1207＝第 3 句；**fire 标 3 处（34、1005、1206）＋未标 0** 我实测一致（248 那句是 `[[bush fire:bush fire]]`，草稿按"另一个词头的表面形式"排除 ✓，`bush fire` 确有一张 `phrase.` 卡 ✓）；dismiss/layoff 标 1＋未标 0 ✓；`fired/fires/firing` 课文 0（`fired` 只在 cannon 卡 ex ✓）、`dismissed` 只在 dismiss 自己卡 ✓、`layoffs` 只在 1207 ✓；[3,54] 在 worklist 220 组里只出现这一次 ✓。
- C：正证据全查到；否定行复验全部为真：**「解雇」全书课文译文 0 处 ✓、「开除」也 0 处 ✓**、辞退＝1206 一处、裁员/赶走＝1207 一处、`be/was/got fired`／`lay off`／`laid off`／`dismiss from`／`dismissal`／`fire sb for sth` 全书 0 ✓、"「活动停止期间」查不到任何句子" ✓、"三卡 `note` 都没有「同义词：」段" ✓、"fire 卡词伙 `fire anyone` 就是 1206、layoff 卡词伙 `sudden layoff` 就是 1207" ✓。
- D：默认行 38.5 / **38.5**；总结句 22.5 / **22.5**；core 6.0/6.0/7.0 ✓。
- E：**OK** —— 表头「worklist 给的共同义项『解雇』这三个字，本书三处译文一处都没用上」＝引用＋否定，实测为真；`core`（不赶生病那位／雨天不辞退人／吓帮手的那一阵）、默认行、总结句 0 命中 `m` 词——**"雨天不辞退人"用的是 1206 的译文而不是卡上的「解雇」，正是 §五.4 要的形状**。
- F：**OK** —— 7 列齐（`fire` 词性格写"v.（卡上另挂 n.）"、`解雇；射击；火`留在 sense 格，没有挤列）；同源如实（两条词伙各并条，dismiss 卡 `note` 空如实写"它在本组义项上只有 1207 那一条文本"）；中文造景对得上（dismiss 卡 ex 的"没人丢饭碗"、fire 卡 ex 的"那堆跟人无关"都实测为真）。一处建议级：L157 引 dismiss 卡 `exZh` 作「他不理会她的建议。」，卡上是「他不理会她的建议，走开了。」（截半，出处清单 L168 是全的）。
- **判定：通过（可落地）**。作者提请拍板（义项三字全书译文 0 处＋两句卡 ex 不演示义项）→ **不退、不必拍**：§五.6 的处置（标题说轻、义项关进 `sense`、数据边界明写"本书没把这三词并成同一个中文"）三件全做了；硬线"layoff 是名词当主语吓人 vs fire/dismiss 动词后面接人"押在 1206/1207 各 1 句可 grep 的原文上；卡 `note` 空、各 1 条文本按 §五.7/§五.14 都不是降级理由。

## 组 205 · agreement / protocol / treaty（6_06 组3，段 [3,56]）

- A：**OK** —— 6 行（1216–1221）双栏逐字 ✓；1218 粗 treaty/agreement（同句 paper 挂了词头没粗 ✓）、1219 只粗 protocol（charter 没粗 ✓）。
- B：**OK** —— 1218＝段56 第 2 句、1219＝第 3 句；三词标 1＋未标 0 ✓；`treaties/protocols/agreements` 全书 0 ✓；`agreed` 课文 8 处（758/1035/1185/1188/1216/1238/1245/1253）＋`agreeing` 1 处（1021）＋`agree` 无卡 —— 三条我逐个复验全对 ✓；[3,56] 在 worklist 出现 3 次（idx 109、110、205），与草稿"同段已有两张表（collaborate-cooperate 在 collaborate 卡、norm-standard 在 norm 卡）"实测一致 ✓。
- C：正证据全查到（三卡 `ex`/`exZh`、三串词伙全部＝1218/1219 原话、别卡文本恰好 bilateral＋joint 两条、`The Magna Carta…` 在 charter 卡 ✓、`shop charter`＝1219 ✓）。否定行复验：**「协议」全书课文译文只有 1 处＝1021，落的是 `bilateral deal`** ✓、「条约」0 ✓、「议定书」0 ✓、「规矩」恰好 9 处（含 1219）✓、「外交」1 处＝372 落 `diplomat` ✓、`in agreement`／`sign a treaty`／`a treaty between two countries` 0 ✓、"372 与 protocol 无关"实测为真（372 无 protocol 标记）✓。
- D：默认行 39.0 / **39.0**；总结句 23.0 / **23.0**；core 7.0/7.0/7.0 ✓。
- E：**OK** —— 表头「worklist 给的共同义项『协议』，本书这三处译文一处都没用上」＝引用＋否定，实测为真（1218「口头约定／纸约」、1219「送货规矩」）；`core`（太松的那句老话／签了字的那张纸／司机留着的那套）、默认行、总结句 0 命中 `m` 词。
- F：**OK** —— 7 列齐；`all_synonyms: false` 与"protocol/treaty 两卡 `ex` 只差词头和开头的 The"这条同形如实写进数据边界（§五.14 三件齐：同形照写、false 有据、各算一侧）✓；同源如实（三条词伙全并条、charter 的 `shop charter` 也点名同源）；**任务书点名的"不许升级成正式程度不同"这一格，草稿不但没升级、还反过来用 bilateral/joint/own ex 三条证否**（"这一格分不开这三个词"），是本片处理最干净的一处。一处建议级：L246 说 agreement 的 `exZh` 是「一份分担费用的协议」，卡上原文是「他们签署了一份分担费用的协议。」（起头省略，非错）。
- **判定：通过（可落地）**。作者提请拍板（「协议」一处没落上、全书译文 1 处落 deal）→ **不退、不必拍**：§五.6 名不副实型的标准处置，且可 grep 的分工线有两条都在本段（`compared to` 前后一先一后、`differed from` 只有 protocol 配）。

## 组 206 · cheat / deceive / fraud（6_06 组4，段 [3,62]）

- A：**OK** —— 6 行（1258–1263）双栏逐字 ✓；1259 只粗 deceive、1260 粗 cheat/fraud（同句 quiet 挂了词头没粗 ✓）。
- B：**OK** —— 1259＝段62 第 1 句、1260＝第 2 句；cheat 标 1＋**未标 3（473、1539、1547）** 我实测完全一致 ✓、deceive/fraud 标 1＋未标 0 ✓；`deceives/deceiving/deceiver/frauds/fraudulent` 全书 0、`deceived` 只在 deceive 卡 ✓；[3,62] 在 worklist 只出现 1 次 ✓。
- C：正证据全查到（三卡 `ex`/`exZh`/词伙、attempt 卡 `ex`、473/1539/1547 三句原文与译文逐字 ✓）。否定行复验：**「欺骗」全书课文译文 0 处 ✓**、蒙骗 0 ✓、欺诈 0 ✓、**「骗局」全书仅 1 处＝1260 ✓**、「作弊」恰好 3 处＝473/1539/1547 且那三句标记各在 presume/indignity/convict ✓、`cheat sheet`／`commit fraud`／`tax fraud`／`a fraud`／`cheat him out of` 0 ✓、三卡 `note` 都只有词伙段 ✓。
- D：默认行 38.5 / **38.5**；总结句 20.0 / **20.0**；core 5.0/5.0/5.0 ✓。
- E：**OK** —— 表头「worklist 的共同义项『欺骗』这三个字全书课文译文 0 处」＝引用＋否定 ✓；义项词只进 `sense` 格，`core`（往奶里掺水／专挑老朋友／藏在笑后面）、默认行（用的是译文里真有的「骗老朋友／掺水奶」）、总结句 0 命中。
- F：**OK** —— 7 列齐；"cheat 那一档不许升级成『本书没有考试用法』"写成"带标记 1 处＋未标 3 处，两档各在一头" ✓ 正是 §五.6 结尾要求的写法；三侧 2/2/3 条计数与实测一致；同源如实（`deceive old friends`＝1259 并条）。建议级：L353 说 1262 的「小聪明」是 `clever`，实测该句 `[[clever]]`＋`[[trick]]` 两个词头一起译成「小聪明」。
- **判定：通过（可落地）**。

## 组 207 · demand / request / require（6_06 组5，段 [3,64]）

- A：**OK** —— 6 行（1270–1275）双栏逐字 ✓；1274 粗 demand/request、1275 只粗 require（petition 没粗 ✓）。
- B：**OK** —— 1274＝段64 第 4 句、1275＝第 5 句、1276＝段65 第 0 句（"末段只剩 1276 一句"实测为真：章3 共 66 段，段 65 只有 1 句）✓；三词标 1＋未标 0 ✓；`demands` 只住卡片词伙（demand/mental/compete 三张）、`requests/required/requiring` 全书 0、`requires` 只在 require 卡 ✓；[3,64] 只出现 1 次 ✓。
- C：正证据全查到（三卡 `ex`/`exZh`/词伙、workforce/bilateral/joint… 别卡文本、1623/1276/1221 旁证句）。否定行复验：**「要求」全书课文译文恰好 1 处＝1274 ✓**、「请求」2 处（1274、1623，1623 的 `asked` 无卡 ✓）、「需求」0 处 ✓、「需要」18 处且 1275 不在其中 ✓、`make a demand`／`at your request`／`request sb to do`／`supply and demand`／`service request`／`requisition` 0 处 ✓、`need/want/ask/agree` 无卡 ✓、三卡 `cmp` 全空 ✓、`mental demands` 跨卡同串 ✓。**一处虚报（必须改）**：见 F-1。
- D：默认行 39.0 / **39.0**；总结句 23.0 / **23.0**；core 10.0/10.0/6.0 ≤12 ✓（草稿自报"core 6–10 字"实测成立）。
- E：**OK** —— 表头「worklist 的共同义项『要求』全书课文译文只有 1 处，正好落在 1274 的 demand 上，request 拿到的是『请求』，require 那句干脆译成了『花不了多少钱』」＝引用＋逐侧验实（三条我都 grep 到）；`core`、默认行、总结句 0 命中 `m` 词。
- F：**OK 一处必须改**
  1. **L388＋L442：`requirements` 不是"全书只在 criteria 卡的同义词栏里"。** 实测卡片侧有**两处**：criteria 卡 `note` 同义词栏，以及 **appearance 卡 `note` 词伙 `meet appearance requirements`**（课文 0 处 ✓）。最小修法：两处都改成"全书 2 处：criteria 卡同义词栏、appearance 卡词伙 `meet appearance requirements`；课文 0 处"。（`requirement` 单数确实全书 0，别一起改掉。）
  2. 「数据边界」诚实度：L448 把 `all_synonyms: false` 的机械判据拆成"demand↔require 互列、request↔require 互列、demand↔request 谁都没列"——我逐张卡扫过 `note`，**三条全对**；这是本片最标准的 §五.14 写法。
  3. 建议级：L400 `demand` 的 `scene` 写"卡 ex 是干净的水人人都缺"——卡 `ex`/`exZh` 只说「对清洁水的需求正在增加」，"人人都缺"是外加的画面（该格是 landing 槽，建议改成"干净水的缺口在涨"）。
- **判定：需作者改（MISMATCH 1 处＝requirements 计数；另 1 处建议级造景）**，内容骨架不用动。

---

## 汇总

| 组 | worklist_idx | 判定 | 必修（具体到格子，均我亲自核过） |
|---|---|---|---|
| 198 hop/jump/leap | 198 | **MISMATCH** | L60「本书唯一给跳法配程度词」→限定"课文"（kangaroo 卡 ex `jumps high` 是反例，L81 自引）；L64「译文从没把三词并成同一个中文词」→限到本段；L448「12 个句号」→11 |
| 199 adorn/embellish/ornament | 199 | 通过 | —（提醒：默认行 40.0 贴顶；L153「两张 exZh」与 L174 不一致，建议级） |
| 200 delicious/tasty/yummy | 200 | 通过 | —（提醒：默认行 40.0 贴顶） |
| 201 exterior/external/outside | 201 | **MISMATCH** | L316 补 1147、L348「共 6 处」→7 处；L290 措辞（同义词栏各列对方一次） |
| 202 inner/interior/internal | 202 | **MISMATCH** | L407＋L421 把 `in-house` 写成"别的词头"→"未打标记、也没有卡"；（建议）L407/L431「里面的」→「里面」 |
| 203 occupation/profession/vocation | 203 | 通过 | —（提请拍板 1：不退、不必拍） |
| 204 dismiss/fire/layoff | 204 | 通过 | —（提请拍板 2：不退、不必拍；建议补全 dismiss 的 `exZh`） |
| 205 agreement/protocol/treaty | 205 | 通过 | —（提请拍板 3：不退、不必拍） |
| 206 cheat/deceive/fraud | 206 | 通过 | —（建议级：L353「小聪明」是 clever＋trick 两词头一起译） |
| 207 demand/request/require | 207 | **MISMATCH** | L388＋L442 `requirements` 全书 2 处（漏 appearance 卡词伙）；（建议）L400 scene「人人都缺」 |

**两条横向结论（写给下一批，不必再逐组问）**
1. 本片把 §五.15 那族错误压到极低：**55 条"本书 0 处／不这样写"式断言去重 35 条，反向 grep 35/35 全部为真，无一条虚报"没有"**；栽的 4 处全是同一方向的过头话——把"本段/课文"的分布写成"本书"，或把没卡的普通词写成"词头"、把 2 处写成"只在 1 处"。**判据固定为：句子里出现"唯一／从没／只在／全书／别的词头"这五个词形时，必须连卡片范围一起扫，并且回 `vocab.json` 确认键是否存在。**
2. 「≤40 = 手机上两行以内」这条口径本片守住：20 条宽度标称与实测一字不差，其中 6_05 组2（adorn）与组3（delicious）的默认行**正好 40.0 顶格**——按 `width_rule` 的加法，**再加任何一个字符（一个汉字 +1.0、半个字母 +0.5）就是 41.0，超上限＝BLOCK**；落地时这两行一个字都动不得，要动只能等量换字。口径本身既不放 45 也不压 25。

**需要他拍的最多 3 条**
1. **载体（只有这一条是真问题）**：新组 200/201 与线上表共用成员——`tasty` 已带 `tasty-yummy`（`at`=[2,34]，与新组同段）、`exterior` 已带 `exterior-outward`（`at`=[2,39]，与新组 201、202 同段）。我实测：**10 个新组 slug 与线上 178 张表无一同名，不会静默覆盖**；但一个词头只能带一张表，所以要拍"组 200 挂 `delicious` 还是 `yummy`、组 201 挂 `external` 还是 `outside`"。草稿建议前者，内容不需要为它改一个字。另要确认：挂段 39 末一次要多出 3 张卡（entrance-entry、exterior-outward、201、202），挂段 34 末 2 张——这是 §五.9 说过的渲染层事项，不是内容问题。
2. **6_06 那 3 条提请拍板，我建议全部撤下**：三条事实我逐条 grep 复验为真（occupation 卡 `ex` 走「占领」、"解雇"三字全书课文译文 0 处、"协议"一处没落上），但它们正好落在 §五.6／§五.7 已经定死的处置范围内，草稿也都按 (b) 档出了 diff 表且分工线全部可 grep——按批次 5 写死的判据属"照写、不再上报"。留着只会在每次合并时重复占用他 3 分钟。
3. **4 处 MISMATCH 谁来改**：全部是单格措辞／计数，我这边边界是只读、没动草稿。要么派回原作者一次性按上表改 6 格（10 分钟量级，不涉内容骨架、不涉任何书证），要么我在门禁 3 里按这份清单代改——但要他先点头，因为我改出来的字不会再过一次门禁 1 的作者自检。
