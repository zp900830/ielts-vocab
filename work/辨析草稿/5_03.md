# 辨析卡草稿 · 批次 5 切片 30（5 组 / worklist_idx 146–150）

> 小结：切片 `work/compare_slices/slice30.json` 的 5 组（worklist_idx 146 / 147 / 148 / 149 / 150）全部写完；门禁 `python3 tools/check_compare_draft.py work/辨析草稿/5_03.md` 结果见本行末：PASSED（0 条查不到）。宽度全部用 `tools/width_rule.py` 那把尺实测，标称值逐组见各组小标题。
> 提请拍板 4 处：**组 1**（individual / single）共享义项「单独」两侧都不落地（1466 的 individual 是名词"每人"、1464 的 single 译「同一个」），两词各只有 1 处书证；**组 2**（committee / council）义项「委员会」在本段只落在 committee 那侧（council 译「镇议会」），另有一处未打标记的 `street committee`（课文 1215）；**组 3**（glance / glimpse）两卡上**已经带着一份 `cmp` 四词表**（look-verbs：glance / glimpse / peep / peer，挂在同一句区），本卡与它同挂段 1 末、内容重叠度最高，要不要替换或合并由人定；**组 5**（curse / swear）两卡上也**已经带着一份 `cmp`**（curse-swear，`at` 就写着 [5,2]，与本组同段同句），是最直接的重复。组 4（oath / vow）义项两侧都成立、不必拍板，只是 oath 卡 `note` 是空的（无词伙段）已按 §三.8 明说。
> 未动任何产品代码 / 数据文件（`shadow/data/vocab.json`、`shadow/data/sections.json` 只读未改），未碰别人的草稿与 `work/辨析审核/`，未跑 `tools/land_compare.py`，未做任何 git 写操作。

---

## 组1 · individual / single —— 单独 ｜ `all_synonyms`: false ｜ 段 [4,31] ｜ 句 1464 / 1466

**共现位置**：1 段 2 句 —— 第 4 章段 31（全局 1462–1467），成员句 1464（single）与 1466（individual）中间只隔 1465 一句，同一段在说"全街一条心、每人各管一摊"。**主锚点 = 章4 段31**（`paraZh`：拄拐小波也提议，全街鼓掌目标一致。`subheads[4][18]` 起为「下篇·发明比赛」，本段在其下）。卡挂段 31 末；组 2（committee / council）在段 33 末，与本组不同段。
主锚点 = 章4 段31

**这一段在讲什么**：发明比赛那队人的收尾——1462 拄拐的小波还敢提方案，1463 说他们跟这条街的关系甜，1464 说队里没为吵架散伙、每人守着同一个目标，1465 说梅独自管图表、别人管各自的罐子，1466 说每人记笔记（连北街那位疲惫的通勤叔叔也写），1467 收在全街鼓掌。段内 1465 另有 `sole`（独自）、`respective`（各自的）两个"各"字头的词，按硬约束 2 不进本组表，只作背景。

**该段原文（章4 段31）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1462 | Small Bo, still dependent on crutches, dared to propose a safer spout. | 拄着双拐的小波，走路还得靠那副拐，却敢提一个更安全的水嘴方案。 |
| 1463 | Their close engagement with the street felt sweet, longer than a short honeymoon. | 他们和街道的密切关系很甜，比短暂蜜月还长。 |
| 1464 | No row caused divorce in the team; each kept a **single** shared aim. | 团队没因吵闹离婚，大家守着同一个目标。 |
| 1465 | Mei took sole care of charts, and others kept respective jars in line. | 梅独自管图表，其他人管好各自的罐子。 |
| 1466 | Each **individual** wrote notes, even the tired commuter uncle from North Street. | 每人记笔记，连北街疲惫的通勤叔叔也写。 |
| 1467 | The whole neighbourhood clapped, even the stern boss from the tea shop. | 整街鼓掌，连茶铺严厉的老板也拍手。 |

individual 全书带标记 1 处（1466）、未打标记 0 处；single 全书带标记 1 处（1464）、未打标记 0 处（口径 13：两档分开报，两词都是"标记 1 + 未标 0"）。

### 表头一句（title）

> 隔一句的两个"各"：1466 说的是"每一个人"各写各的笔记，1464 说的是目标"只有一个"——worklist 给的共同义项"单独"，本段两处都没站上去：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| individual | /ˌɪndɪˈvɪdʒuəl/ | n. | n. 个人；adj. 个别的；单独的 | 一个一个的人 | 1466 每人记笔记，连北街那位疲惫的通勤叔叔也写；卡 ex 里每人都有权自由发言 | `Each individual wrote notes`（课文 1466） |
| single | /ˈsɪŋɡl/ | adj. | adj. 单一的；单独的；单身的 | 就一个，不是两个 | 1464 全队守着同一个目标；卡 ex 里那张公交票 | `a single shared aim`（课文 1464） |

### 默认那一行（实测宽度 38.0 / ≤40）

```
individual = 每人（each individual），single = 只一个（a single shared aim）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 它在句子里站哪一格 | individual | `Each individual wrote notes`（1466，后面直接带动词 wrote）、`Each individual has the right to speak freely.`（卡 ex） | 当主语，指"人"，自己会做事 | 本书两处 individual 都是名词中心语，前面站 Each、后面跟动词——它是"人" |
| 它在句子里站哪一格 | single | `a single shared aim`（1464，后面是名词 aim）、`a single ticket`（卡 ex，后面是名词 ticket） | 放在名词前面，说明那样东西只有一个 | 本书两处 single 都是定语，自己从不担任动作；本书没给过 single 当主语的写法 |
| 卡上词伙给的是一类什么东西 | individual | `well-rounded individuals`、`those individuals in need`、`college-educated individuals`（卡词伙，本书课文一条都没用到） | 说"某一类人"，还常带修饰语 | 三条词伙说的全是**人**、且全是复数写法 `individuals`；这正对上 1466 的"每人" |
| 卡上词伙给的是一类什么东西 | single | `remain single`（卡词伙，本书课文 0 处） | 说一个人的婚姻状态 | 它唯一的词伙落在"单身"上，跟"人"这件事隔了一层：本书没有一处 `individuals` 指婚姻状态，也没有一处 single 当主语 |
| 前面站什么限定词 | individual | 本书 2 条文本（课文 1466 + 卡 ex）**都是 Each 开头**：`Each individual` | 跟 Each 搭配当主语 | 这是本组最硬的一格：卡 ex 与课文同一开头，措辞可 grep |
| 前面站什么限定词 | single | `a single shared aim`（1464，跟 a）、`a single ticket for the bus`（卡 ex，跟 a） | 跟 a 搭配放名词前 | 本书两处都是"冠词 a + single + 名词"，跟 individual 那两处各走一路 |
| 中文落点 | 两个词 | 1466 译「每人记笔记」／1464 译「大家守着同一个目标」（`sentZh`） | 汉译英时先问中文说的是"每个人"还是"只有一个" | 本书译文自己就没把这两个词并成一个中文词：individual 走「每人」、single 走「同一个」 |

### 总结句（实测宽度 14.0 / ≤30）

> 每人各记一份，目标只有一个。

### 出处清单

- `No row caused divorce in the team; each kept a single shared aim`、`a single shared aim`、`each kept a single shared aim`、译文「团队没因吵闹离婚，大家守着同一个目标」 —— 课文第 1464 句（段 [4,31] 第 2 句，`single` 带标记）及 `sentZh`；single 侧第 1 条书内文本。
- `Each individual wrote notes, even the tired commuter uncle from North Street`、`Each individual wrote notes`、`Each individual`、`the tired commuter uncle from North Street`、译文「每人记笔记，连北街疲惫的通勤叔叔也写」 —— 课文第 1466 句（段 [4,31] 第 4 句，`individual` 带标记）及 `sentZh`；individual 侧第 1 条书内文本。
- `He bought a single ticket for the bus.` / 他买了一张单程公交车票。 —— single 卡 `ex` / `exZh`（表里写作 `a single ticket`、`a single ticket for the bus`，均为其连续子串）。
- `remain single` —— single 卡 `note` 词伙段（本书课文 0 处，只作卡上现成块列出）。
- `Each individual has the right to speak freely.` / 每个人都有权自由发言。 —— individual 卡 `ex` / `exZh`（表里写作 `Each individual has the right to speak freely`，为其连续子串）。它与课文 1466 同一开头 `Each individual`，但**是另一句话**，按口径 8 不算同源、各算一侧一条文本。
- `well-rounded individuals`、`those individuals in need`、`college-educated individuals` —— individual 卡 `note` 词伙段三条（本书课文 0 处，逐条已回查：课文里既没有 `well-rounded` 也没有 `college-educated`，`in need` 那 1 处命中是 `brain needs` 的假阳性，不算）。
- `Mei took sole care of charts, and others kept respective jars in line`、`sole care`、`respective jars`、译文「梅独自管图表，其他人管好各自的罐子」 —— 课文第 1465 句（本段背景：同段还有 sole「独自」、respective「各自的」两个"各"字头的词，按硬约束 2 不进本组表）。
- `Small Bo, still dependent on crutches, dared to propose a safer spout`、`Their close engagement with the street felt sweet, longer than a short honeymoon`、`The whole neighbourhood clapped, even the stern boss from the tea shop` —— 课文第 1462 / 1463 / 1467 句（本段其余句，只作表头背景）。
- 义项「n. 个人；adj. 个别的；单独的」「adj. 单一的；单独的；单身的」 —— 两卡 `m`；「拄拐小波也提议，全街鼓掌目标一致」—— 段 31 `paraZh`；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，但共享义项「单独」两侧都不成立（要拍板）

- 两卡互不列同义词（individual `note` 只有词伙段、无同义词栏；single `note` 只有词伙段、无同义词栏），worklist 标 `all_synonyms: false` 有据；义项交集「单独」来自两卡 `m` 里都写了「单独的」这一档。
- **口径 6 处理**：那一档在本书**没有一处落地**——1466 的 individual 是名词、译文「每人」；1464 的 single 是定语、译文「同一个」（不是"独自一人"）。所以标题与默认行只说"这一段各领哪样东西"（每人 / 只一个），「单独」只留在 `sense` 槽，不宣称"本段两词都指单独"。另要写清：individual 卡 `m` 的 **adj. 档（个别的；单独的）本书 0 处**（唯一那处是名词），single 卡 `m` 的「单身的」档本书也 0 处（只有卡上词伙 `remain single`）。
- **同源合并（口径 8）**：两词各 2 条独立文本（课文 1 + 卡 ex 1），词伙 individual 3 条 / single 1 条全为卡上现成块、本书未用。卡 ex 与课文 1466 只差谓语，同形不同句，已各算一条。
- **计数结论、不是禁令**："individual 当主语、single 只当定语"来自 **2 条 vs 2 条**的分布（本书样本就这么大）。英文里 `a single person`、`single individual` 这类写法本书没出现过，也不代表不行——表里措辞一律停在"本书这几处"。
- **不写的**：「individual 强调个体性、single 强调数量为一」（卡 `note` 无语域标注，本书给不出证据，写了就是把中文义项复述一遍）；`individual effort`、`single parent`、`single-minded`、`in single figures`、`the individual vs the mass` 这类本书 0 处的串一律不列。
- **提请拍板点**：本组两处书证都**不**站在 worklist 那个共同义项上（组 1 与批次 3 组 1 的 feast 不同：feast 至少还有"宴席"这一档在书里成立，本组是两侧各自的第二、第三档都没落地）。我的判断：仍值得出卡——"Each + individual 做主语 / a + single 做定语"这一格是卡上现成、可 grep 的硬分工，正好是他要的"哪样东西"层面。若他要求辨析必须两词撞同一义项，本组只能退回义项表。

---

## 组2 · committee / council —— 委员会 ｜ `all_synonyms`: false ｜ 段 [4,33] ｜ 句 1478

**共现位置**：1 段 1 句 —— 第 4 章段 33（全局 1474–1479），两个词挤在同一句 1478 的前后两半，由 `and` 串起来（前半开会、后半借桌椅）。**主锚点 = 章4 段33**（`paraZh`：戏剧喝彩加官方支持，选出代表继续冲刺。`subheads[4][18]` 起为「下篇·发明比赛」，本段在其下）。卡挂段 33 末；组 1 在段 31 末，两组不同段。
主锚点 = 章4 段33

**这一段在讲什么**：发明比赛得到各方支持——1475 戏剧社的女主角和女演员大声喝彩，1476 说严厉的大臣没来但退休的指挥官寄了字条，1477 说集市的主任一直支持学生作品，1478 给两个机构：集市的 committee 开了两次会、镇里的 council 借出桌椅，1479 选兰做代表、有南街的水专家指导。整段是"谁来捧场、谁来撑腰"的名目清单。

**该段原文（章4 段33，成员句 + 前后各一句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1477 | The kind director of the fair stayed a firm proponent of school builds. | 集市的和善主任一直支持学生作品。 |
| 1478 | The fair **committee** met twice, and the town **council** lent chairs and tables. | 集市委员会开了两次会，镇议会借出桌椅。 |
| 1479 | They chose Lan as delegate, guided by a water specialist from South Street. | 他们选兰做代表，有南街的水专家指导。 |

committee 全书带标记 1 处（1478）＋未打标记另有 1 处（课文 1215 `the street committee`，译「街道小组」）；council 全书带标记 1 处（1478）、未打标记 0 处（口径 13：两档分开列，别并成一个数）。

### 表头一句（title）

> 一句里两个办事的机构，各管一件事：集市那个 committee 开了两次会，镇里那个 council 把桌椅借出来——worklist 说共同义项是"委员会"，可本书译文给 council 的中文是「镇议会」：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| committee | /kəˈmɪti/ | n. | n. 委员会 | 为一件事凑起来的那拨人 | 1478 集市那拨开了两次会；1215 街道那个小组拦不住吵嚷（未打标记）；卡 ex 里它每周一开会 | `The fair committee met twice`（课文 1478） |
| council | /ˈkaʊns(ə)l/ | n. | n. 理事会；委员会；议会 | 镇里市里那层机构 | 1478 镇里出借桌椅；卡 ex 市政府定回收新规 | `the town council lent chairs and tables`（课文 1478） |

### 默认那一行（实测宽度 35.0 / ≤40）

```
committee = 开了两次会（met twice），council = 借出桌椅（lent chairs）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 同句里它干了什么 | committee | `The fair committee met twice`（1478，谓语是 meet 的过去式 met）、`The committee meets every Monday.`（卡 ex，又是 meet） | 说一拨人**开会**这件事 | 本书给它的两个动词都是"开会"（met / meets），一次都没给它别的行为 |
| 同句里它干了什么 | council | `the town council lent chairs and tables`（1478，谓语是 lend；`lent chairs and tables` 是同句后半） | 说机构**给东西、定规矩** | 本书它那 1 处课文的谓语是"借出"，卡 ex 那处是"定了新规定"（`made a new rule about recycling`）——两处都不是开会 |
| 它前面挂的是哪一路名字 | committee | `The fair committee`（1478，集市）、`the street committee`（1215，未打标记，街道） | 跟着**一场活动 / 一条街**这种小范围 | 本书两处 committee 的定语都是小场面：集市、街道 |
| 它前面挂的是哪一路名字 | council | `the town council`（1478 = 卡词伙）、`The city council`（卡 ex，市政府） | 跟着**镇 / 市**这种行政单位 | 本书 council 两处挂的都是地名行政单位（town / city），比 committee 那两处高一层 |
| 中文译成了哪个词 | 两个词 | committee：1478「集市委员会」、1215「街道小组」（`sentZh`）；council：1478「镇议会」、卡 ex「市政府理事会」 | 汉译英时先问中文给的是"委员会"还是"议会 / 理事会 / 小组" | 共同义项「委员会」在本书只对 committee 成立；council 的 2 处中文没有一处写作「委员会」 |
| 卡上给几档、有没有同义词栏 | committee | 卡 `m` 只「委员会」一档；`note` 只有词伙段 `fair committee`，**没有同义词栏** | 它只管这一件事 | 它卡上不提 council，所以 worklist 的 `all_synonyms: false` 有据（两栏没互列） |
| 卡上给几档、有没有同义词栏 | council | 卡 `m` 三档「理事会；委员会；议会」；`note` 同义词栏 `authority, government, ministry, official`（栏里**没有 committee**）、词伙 `town council` | 它还得兼"理事会 / 议会" | authority 卡 `note` 同义词栏里回列了 council（两卡互列的是 council↔authority 这一对，与 committee 无关）；栏里的 government、ministry 在 `vocab.json` 里没有卡，按硬约束 2 不进表 |
| 加 s 怎么写 | 两个词 | 本书 2 处 committee、1 处 council 全是单数写法（课文里并没有 `committees`、`councils` 这两种形式）；卡 ex 给 committee 配的动词是单数（`meets`） | 主谓一致 | 本书没给过这两个词的复数形态，落地时别把"集合名词在英式英语里可当复数"当本书结论 |

### 总结句（实测宽度 15.0 / ≤30）

> 一个机构在开会，一个在借桌椅。

### 出处清单

- `The fair committee met twice, and the town council lent chairs and tables`、`The fair committee met twice`、`The fair committee`、`met twice`、`the town council`、`lent chairs and tables`、`lent chairs`、`the town council lent chairs and tables`、译文「集市委员会开了两次会，镇议会借出桌椅」 —— 课文第 1478 句（段 [4,33] 第 4 句，两词都带标记）及 `sentZh`。**两词在本书唯一的同段书证就是这一句。**
- `No sanction from the street committee could stop the shouting, so friends tried to coordinate a calm talk`、`the street committee`、译文「街道小组的处罚拦不住吵嚷，朋友们只好想法子张罗一次平静的谈话」 —— 课文第 1215 句（章3 段55；该句 committee **未打标记**，按 §五.13 单列），committee 侧第 2 处书内文本。
- `The committee meets every Monday.` / 委员会每周一开会。 —— committee 卡 `ex` / `exZh`（表里写作 `The committee meets every Monday`，为其连续子串；本书课文没有这样写过，`every Monday` 在课文 0 处）。
- `fair committee` —— committee 卡 `note` 词伙段，与课文 1478 **同源**（口径 8，不另计）。
- `The city council made a new rule about recycling.` / 市政府理事会制定了关于回收的新规定。 —— council 卡 `ex` / `exZh`（表里写作 `The city council`、`made a new rule about recycling`，均为其连续子串）。
- `town council` —— council 卡 `note` 词伙段，与课文 1478 **同源**（口径 8，不另计）。
- `authority, government, ministry, official` —— council 卡 `note` 同义词栏；authority 卡 `note` 同义词栏为「同义词：council, government, ministry, official」（回列 council）。committee 卡 `note` 无同义词栏。
- `The kind director of the fair stayed a firm proponent of school builds`、`They chose Lan as delegate, guided by a water specialist from South Street` —— 课文第 1477 / 1479 句（本段其余有卡的词，只作表头背景：这一段还有 director、proponent、delegate、specialist 几个"人物 / 职务"词，均未与本组并成一组，按硬约束 2 不进表）。
- 义项「n. 委员会」「n. 理事会；委员会；议会」 —— 两卡 `m`；「戏剧喝彩加官方支持，选出代表继续冲刺」—— 段 33 `paraZh`；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，但「委员会」这一档本书只给 committee 用（要拍板）

- 两卡互不列同义词（committee 卡连词伙之外的同义词栏都没有；council 的同义词栏列的是 authority / government / ministry / official，不含 committee），worklist 标 `all_synonyms: false` 有据；义项交集「委员会」来自两卡 `m`（council 卡 `m` 第二档正是「委员会」），所以 worklist 不算标错。
- **口径 6 处理**：本书 council 的 2 处中文是「镇议会」（1478）和「市政府理事会」（卡 ex），**没有一处把 council 译成"委员会"**；committee 侧 1478 译「委员会」成立、1215 又译成「街道小组」。所以标题与默认行只说"这两个机构各干了什么"，「委员会」留在 `sense` 槽，不宣称"本段两词都指委员会"。
- **同源合并（口径 8）**：committee 卡词伙 `fair committee` = 课文 1478、council 卡词伙 `town council` = 课文 1478，都不另计。独立文本：committee 侧 **3 条**（1478 + 1215 未标 + 卡 ex）＋词伙 1（同源）；council 侧 **2 条**（1478 + 卡 ex）＋词伙 1（同源）。council 侧另有一档（「理事会」）只在卡 ex 的中文里，本组未展开。
- **"开会归 committee"是计数结论、不是禁令**：它来自 committee 侧 2 条 meet 用例（课文 1 + 卡 ex 1）对 council 侧 0 条。council 全书只 1 处课文，样本小到不能再小，表里措辞一律停在"本书这几处"。
- **不写的**：「council 是民选的地方机构、committee 是任命的办事小组」（两卡 `note` 无语域标注，本书给不出证据）、「committee 作集合名词后动词可用复数」（本书唯一一处配的是 `meets`）；`student council`、`board of directors`、`committee member`、`appoint a committee`、`local authority` 这类本书 0 处的串一律不列。
- **提请拍板点**：本组的共同义项只在 committee 一侧成立（council 两处中文都另有落点）。我的判断是照现表落地——本组的硬货不依赖那个义项名：**committee 前面挂活动与街区（fair / street）、council 前面挂行政单位（town / city）**，两条都在课文原句上、可 grep。若他要求"共享义项必须在共现段成立"，本组属义项半落地型，报此备查。

---

## 组3 · glance / glimpse —— 一瞥 ｜ `all_synonyms`: false ｜ 段 [5,1] ｜ 句 1498 / 1499

**共现位置**：1 段 2 句 —— 第 5 章段 1（全局 1498–1503），成员句 1498（glimpse）与 1499（glance）**相邻两句、同一个赶公交的清晨**（先撞见公交，再低头看表）。**主锚点 = 章5 段1**（`paraZh`：赶公交上学，利奥立志今天准时努力。`subheads[5][0]`：上篇·备考的一天，本段在其下）。卡挂段 1 末；组 4（oath / vow）在 1502、1503 那两句、同挂本段末，本组只讲"看一眼"，"立誓"那格交给组 4（口径 9：两张卡各写各的切入角）。
主锚点 = 章5 段1

**这一段在讲什么**：赶公交那几分钟——1498 阳光刺眼但利奥瞥见了公交，1499 他飞快地看一眼手表、又透过树丛朝长路偷看，1500 目光稳在路面上、努力穿过小雨看，1501 他对着繁星沉思、注视着夜空，1502 立下誓言、又默默许了个庄重的誓约，1503 许下承诺、吹口哨的人轻声哼唱。整段前四句是"看"的一族词排队上场。

**该段原文（章5 段1，成员两句 + 后一句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1498 | The low sun sends a sharp glare but Leo gets a quick **glimpse** of the bus. | 低矮的阳光有些刺眼，但利奥很快瞥见了公交车。 |
| 1499 | Leo takes a quick **glance** at his watch and has a peep down the long road through the trees. | 利奥飞快地看了一眼手表，又透过树丛朝长路那头偷看了一下。 |
| 1500 | His steady gaze stays on the road while he tries to peer through light morning rain. | 他目光稳定地看着路面，努力透过清晨小雨凝视。 |

glance 全书带标记 1 处（1499）、未打标记 0 处；glimpse 全书带标记 1 处（1498）、未打标记 0 处（口径 13 分档报）。两卡的 `note` 字段**都不存在**（既没有同义词栏也没有词伙段），所以本组的搭配证据只有"课文 1 句 + 卡 `ex` 1 句"这么窄（§三.8 按要求明说；口径 7：薄是常态）。

### 表头一句（title）

> 挨着的两句，两个词前面挂的都是 `a quick`，可本书中文给它们的落点并不一样：1498 印「瞥见」、1499 印「看了一眼」——一个是阳光刺眼时**撞见**了公交，一个是自己低头**去看**手表，区别不在快慢，在谁先动手：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| glance | /ɡlæns/ | n. | n. 一瞥；扫视 | 自己抬头去看一眼 | 1499 看一眼手表（赶公交怕迟到）；卡 ex 里她看一眼钟 | `takes a quick glance at his watch`（课文 1499） |
| glimpse | /ɡlɪmps/ | n. | n. 一瞥 | 撞见、瞥见 | 1498 刺眼阳光里瞥见公交；卡 ex 里猫跑掉前瞥见它 | `gets a quick glimpse of the bus`（课文 1498） |

### 默认那一行（实测宽度 37.0 / ≤40）

```
glance = 抬眼看表（a quick glance），glimpse = 撞见公交（a quick glimpse）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 谁先动手 | glance | `Leo takes a quick glance at his watch`（1499）、`She took a glance at the clock.`（卡 ex） | 人主动去看的：怕迟到、想看几点 | 本书 2 处 glance 的宾语都是**不会跑的东西**（手表、钟），是你去看它 |
| 谁先动手 | glimpse | `Leo gets a quick glimpse of the bus`（1498，同句前半是 `The low sun sends a sharp glare`——光正碍事）、`I caught a glimpse of the cat before it ran away.`（卡 ex，猫要跑） | 没看清却看见了：对象在动、条件不利 | 本书 2 处 glimpse 的宾语都是**会动的东西**（公交、猫），是它撞进你眼里 |
| 跟哪个动词、哪个介词 | glance | `takes a quick glance at`（1499）、`took a glance at the clock`（卡 ex）——本书 2 处全是 **take + at** | 说"看了一眼某物" | 本组最硬的一格：本书两处形状完全平行（`a quick` + 名词 + 介词），换的只有那个动词和那个介词 |
| 跟哪个动词、哪个介词 | glimpse | `gets a quick glimpse of`（1498）、`caught a glimpse of the cat`（卡 ex）——本书 2 处全是 **get / catch + of** | 说"瞥见了某物" | 它的动词是"得到、撞上"那一类（get / catch），不是"做"（take）；介词是 of 不是 at |
| 卡上给的词性用上了吗 | glance | 卡 `m` 只「n. 一瞥；扫视」一档；本书 1 处正是名词（`a quick glance`） | 本书只用它的名词档 | 两卡 `m` 都只列 n.，而卡上那份四词表把它们的 `pos` 写成 v./n.——动词写法本书这两句都没出现 |
| 卡上给的词性用上了吗 | glimpse | 卡 `m` 只「n. 一瞥」一档；本书 1 处正是名词（`a quick glimpse`） | 同上 | 它的 `m` 连"瞥见"这个中文都没写，只写「一瞥」——"瞥见"那层意思本书是靠在 `of` 后面挂会动的东西来给的 |
| 中文落点 | 两个词 | 课文：1498 译「很快瞥见了公交车」／1499 译「飞快地看了一眼手表」（`sentZh`）；卡 `exZh`：glance 译「瞥了一眼钟」／glimpse 译「瞥了一眼那只猫」 | 汉译英时先问中文是"（主动）看一眼"还是"瞥见" | 本书课文把两个词分开了（瞥见 / 看了一眼），两张卡的例句译文却把它们并成了同一个「瞥了一眼」——**卡上的中文不许当区分依据**，要区分得回到 take…at 与 get…of |

### 总结句（实测宽度 17.5 / ≤30）

> glance 是你去看，glimpse 是它出现。

### 出处清单

- `The low sun sends a sharp glare but Leo gets a quick glimpse of the bus`、`Leo gets a quick glimpse of the bus`、`gets a quick glimpse of the bus`、`a quick glimpse`、`a sharp glare`、`The low sun sends a sharp glare`、译文「低矮的阳光有些刺眼，但利奥很快瞥见了公交车」 —— 课文第 1498 句（段 [5,1] 第 0 句，`glimpse` 带标记）及 `sentZh`；`sharp glare` 同见 glare 卡 `note` 词伙段（与 1498 **同源**，口径 8 不另计）。glimpse 侧第 1 条书内文本。
- `Leo takes a quick glance at his watch and has a peep down the long road through the trees`、`Leo takes a quick glance at his watch`、`takes a quick glance at his watch`、`a quick glance`、`has a peep down the long road`、译文「利奥飞快地看了一眼手表，又透过树丛朝长路那头偷看了一下」 —— 课文第 1499 句（段 [5,1] 第 1 句，`glance` 带标记）及 `sentZh`。glance 侧第 1 条书内文本。同句另有 `peep`（peep 卡有卡），按硬约束 2 不进成员。
- `She took a glance at the clock.` / 她瞥了一眼钟。 —— glance 卡 `ex` / `exZh`（表里写作 `took a glance at the clock`，为其连续子串）。
- `I caught a glimpse of the cat before it ran away.` / 我在它跑掉之前瞥了一眼那只猫。 —— glimpse 卡 `ex` / `exZh`（表里写作 `caught a glimpse of the cat`，为其连续子串）。
- `His steady gaze stays on the road while he tries to peer through light morning rain` —— 课文第 1500 句（本段背景：同段还有 gaze、peer，均未与本组并成一组，不进表）。
- `a sharp glare`、`glance` 与 `glimpse` 卡上那份四词表 —— glare 卡 `note` 词伙段；两卡 `cmp`（`type`: compare，`group`: look-verbs，成员 glance / glimpse / peep / peer，两卡上是同一份内容的两份拷贝）。那份表里 `pos` 写作 v./n.、glimpse 的 `sense` 写作「瞥见」，与两卡 `m`（只 n.、只「一瞥」）不一致 —— 报备在本组「数据边界」。
- 义项「n. 一瞥；扫视」「n. 一瞥」 —— 两卡 `m`；「赶公交上学，利奥立志今天准时努力」—— 段 1 `paraZh`；「上篇·备考的一天」—— `subheads[5][0]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，但两侧都薄、且卡上已有一份四词表在讲同一件事（要拍板）

- 两卡 `note` 字段都不存在（连"同义词："栏都没有），worklist 标 `all_synonyms: false` 只是机械位的结果，不构成退回理由（口径 14 同理）；义项交集「一瞥」来自两卡 `m`，本段两处**都站在这档上**（都译成瞥见 / 看一眼），所以本组不属义项名不副实型。
- **证据强度（§三.8 要求明说）**：glance 侧独立文本 **2 条**（课文 1499 + 卡 ex）、glimpse 侧独立文本 **2 条**（课文 1498 + 卡 ex），**两卡都没有词伙段可引**，所以表里的搭配列全是课文与卡 ex 的原样片段，没一条是"卡上现成词伙"。同源方面：`sharp glare` = glare 卡词伙与课文 1498 同源，只算 1 条，另计在 glare 名下。
- **档位报备（口径 6 的"档位错"变体）**：卡上那份四词表把两词标成 v./n.，本书 1498、1499 两处走的都是**名词档**（`a quick glance` / `a quick glimpse`）；动词写法（glanced / glimpsed 那类）本书这两句没出现，本组表里因此一律不写。
- **与既有四词表重复（提请拍板的核心）**：`glance` 与 `glimpse` 两卡上已经各挂着一份 `cmp`（group `look-verbs`，含 peep / peer 共 4 词），它的 `summary` 里已经写了"glance 是主动快看，glimpse 是被动瞥见"。本卡与那份表**结论方向相同**，但本卡的三条区别是那份表没有的、且可 grep：**take…at 对 get/catch…of 的框架**、**宾语一个不会跑一个会跑（表 / 钟 vs 公交 / 猫）**、**卡 ex 译文把两词并成同一个「瞥了一眼」**。请他定：本卡是替换卡上那份 `cmp`、还是与它并存（口径 9 允许同段多卡，但"一词只能带一张表"是既有规则，两卡同时挂在段 1 末会撞）。
- **那份旧表的拷贝其实挂在三张卡上（补报）**：带 look-verbs 那份 `cmp` 的不只是 glance / glimpse 两张，**peep 卡上也带着一份同表**；我把三份逐字段比过（读 `shadow/data/vocab.json` 三张卡的 `cmp`），**三份拷贝唯一的差别就是 peep 那份额外带 `at` = [5, 1]**，glance 与 glimpse 那两份都没有 `at` 字段、其余内容三张全等。也就是说旧表现在页面上确实就钉在章5 段1 末，但走的是哪条路要说清：三份子表 `group` 同名、渲染只认词表里排最前那一份（`vocab.json` 键序我逐个查过：glimpse 1636、glance 1637、peep 1638），而 glimpse 那份没有 at，于是退回"第一个 ≥2 个成员被标出的段"——全书第一个满足的正是章5 段1（1498–1500 连着标了 glimpse / glance / peep / peer）；peep 那份带的 [5, 1] 指的也是同一段。"替换还是并存"要怎么落、要不要给新表也补一个 at 把顺序钉住，都属卡数据改动，不在本草稿权限内，只报备到这里。
- **音标按卡 `p` 字段原样搬**：glance 卡 `p` 与 `us` 同为 /ɡlæns/、`uk` 另标 /ɡlɑːns/，glimpse 卡 `p` 与 `uk`、`us` 三处一致 /ɡlɪmps/ —— 英美两读不是本组的区别，别写进表里当钩子。
- **计数结论、不是禁令**："glimpse 的宾语会动"来自本书 **2 条 vs 2 条**，样本极小；`a glimpse of hope` 这类抽象用法本书没有，也不代表不能这么用，表里措辞一律停在"本书这两处"。
- **不写的**：「glance 常含不屑／冷淡的一瞥、glimpse 常用于 of＋抽象」（两卡无语域标注，本书给不出证据）；`at first glance`、`glance over`、`glance through`、`a fleeting glimpse`、`catch a glimpse of`（本书卡上是 `caught`，没有这条原形词伙）一类本书 0 处的串一律不列。

---

## 组4 · oath / vow —— 誓言 ｜ `all_synonyms`: false ｜ 段 [5,1] ｜ 句 1502

**共现位置**：1 段 1 句 —— 第 5 章段 1（全局 1498–1503），两个词挤在同一句 1502 的前后两半，由 `and` 串起来（前半 oath 被"立"、后半 vow 被"许"）。**主锚点 = 章5 段1**（`paraZh`：赶公交上学，利奥立志今天准时努力。`subheads[5][0]`：上篇·备考的一天）。卡挂段 1 末；组 3（glance / glimpse）在 1498、1499 两句、同挂本段末 —— 本组只管 1502、1503 这两句的"立誓"，别再回去讲"看一眼"（口径 9）。
主锚点 = 章5 段1

**这一段在讲什么**：1502 之前三句都在看（见组 3），1501 转到他对着繁星沉思、注视着夜空，1502 一句两半都是立下誓约（前半被动"was taken"、后半"made silently"），1503 再补一个 pledge（许下承诺）＋吹口哨的人轻声哼唱。**中文译句给 1502 补了主语「他」**，英文原句里没写谁。

**该段原文（章5 段1，成员句 + 前后各一句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1501 | He contemplated the stars and stared at the night sky. | 他沉思着天上的繁星，注视着夜空。 |
| 1502 | An **oath** was taken and a solemn **vow** made silently. | 他立下誓言，又默默许下庄重的誓约。 |
| 1503 | A pledge was given and the whistler sang softly. | 他许下承诺，吹口哨的人轻声哼唱。 |

oath 全书带标记 1 处（1502）、未打标记 0 处；vow 全书带标记 1 处（1502）、未打标记另有 2 处（都是动词写法 `vowed`：课文 119、课文 491；口径 13 分档列）。注意别按子串数：课文 545 那个 `vowel`（元音）不算 vow 的用例。

### 表头一句（title）

> 一句两半、两个"誓"，本书给它们的动词不一样：oath 是被 take 起来的，vow 是被 make 出来的、还加了 silently——worklist 说共同义项是"誓言"，这一档两处都成立，要分的是这两条路：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| oath | /əʊθ/ | n. | n. 誓言 | 立出来的那句誓 | 1502 那句被动的"誓言已立下"；卡 ex 里他发誓要说真话 | `An oath was taken`（课文 1502） |
| vow | /vaʊ/ | n. / v. | n. 誓言；v. 发誓 | 心里许下的那一约 | 1502 那个 solemn、默默许的；119 利奥对着倒下的树立志别再砍；491 朋友们立志去解旧怕 | `a solemn vow made silently`（课文 1502） |

### 默认那一行（实测宽度 34.0 / ≤40）

```
oath = 立出来的誓（took an oath），vow = 默默许的约（made silently）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 它配哪个动词 | oath | `An oath was taken`（1502，被动里的 take）、`He took an oath to tell the truth.`（卡 ex，主动里的 take） | 说"立"一个誓 | 本书 oath 侧 2 条文本的动词全是 take，一次都没换过 |
| 它配哪个动词 | vow | `a solemn vow made silently`（1502，make 的过去分词）、`He made a vow to quit smoking.`（卡 ex） | 说"许"一个约 | 本书 vow 侧名词用法的动词全是 make——同一句里两半各配一个动词，正好互不交叉（take 归 oath、make 归 vow），是本组最硬的一格 |
| 誓的内容怎么接出来 | oath | `an oath to tell the truth`（卡 ex，说的是**真话**，对象是别人） | 对着别人交代一件事 | 本书唯一一处 oath + to do 在卡 ex 上，说的是"说真话"这一类交代 |
| 誓的内容怎么接出来 | vow | `a vow to quit smoking`（卡 ex，戒的是自己的烟）、`Leo vowed never to deforest the land for profit`（119，未打标记）、`friends vowed to resolve old fears`（491，未打标记） | 对着自己定一件事；还能直接当动词用 | vow 侧 4 条文本里有 3 条是"人 + vow + to do"，且主语都是具体的人（Leo / friends / He）；本书 oath 那处却是没人称的被动 |
| 卡上给几档、note 有没有东西 | oath | 卡 `m` 只「n. 誓言」一档；卡 `note` 是**空串**（既无同义词栏、也无词伙段） | 它只能当名词用 | oath 侧全部证据只有 2 条（课文 1 + 卡 ex 1），而且卡上一条现成词伙都拿不出来（§三.8 明说，不硬凑） |
| 卡上给几档、note 有没有东西 | vow | 卡 `m` 两档「n. 誓言；v. 发誓」；`note` 词伙段 `solemn vow`（与 1502 **同源**，口径 8 不另计）；无同义词栏 | 名词、动词都能用 | 它比 oath 多的正是动词那一档，而本书**真用了**那档 2 次（119、491 的 vowed，都没打标记）；worklist 的 `all_synonyms: false` 有据：两栏没互列 |
| 中文落点 | 两个词 | 1502 译「他立下誓言，又默默许下庄重的誓约」：oath → 「誓言」、vow → 「誓约」；两卡 `exZh` 却都译成「他发誓…」（「他发誓要说出真相」／「他发誓戒烟」） | 汉译英时先问中文是"誓言"还是"誓约"，还是把"发誓"当动词用 | 课文译文把两个词分了手（誓言 / 誓约），卡上译文把它们合成一个「发誓」——想区分就靠 take 与 make 那两个动词 |

### 总结句（实测宽度 16.5 / ≤30）

> oath 只是名词，vow 还能当动词用。

### 出处清单

- `An oath was taken and a solemn vow made silently`、`An oath was taken`、`a solemn vow made silently`、`made silently`、`a solemn vow`、译文「他立下誓言，又默默许下庄重的誓约」 —— 课文第 1502 句（段 [5,1] 第 4 句，两词都带标记）及 `sentZh`。表里 `an oath to tell the truth` 一类框架另计在卡 ex 名下。**两词在本书唯一的同段书证就是这一句。**
- `He took an oath to tell the truth.` / 他发誓要说出真相。 —— oath 卡 `ex` / `exZh`（表里写作 `took an oath`、`He took an oath to tell the truth`、`an oath to tell the truth`，均为其连续子串）。oath 卡 `note` 是空串，**卡上无词伙可引**（§三.8）。
- `He made a vow to quit smoking.` / 他发誓戒烟。 —— vow 卡 `ex` / `exZh`（表里写作 `made a vow`、`He made a vow to quit smoking`、`a vow to quit smoking`，均为其连续子串）。
- `solemn vow` —— vow 卡 `note` 词伙段，与课文 1502 **同源**（口径 8，不另计）。
- `Seeing the fallen trees, Leo vowed never to deforest the land for profit`、`Leo vowed never to deforest the land for profit`、`Leo vowed`、译文「看到倒下的树，他发誓再也不为赚钱而砍伐这片林地。」 —— 课文第 119 句（章0 段20；该句 vowed **未打标记**，按 §五.13 单列），`sentZh` 原文照引。
- `Calm assurance began to manifest as friends vowed to resolve old fears`、`friends vowed to resolve old fears`、译文「平静自信开始显现，朋友们发誓化解旧恐惧。」 —— 课文第 491 句（章1 段25；未打标记，按 §五.13 单列），`sentZh` 原文照引。这两处 vowed 的中文都是「发誓」，与两卡 `exZh` 的「他发誓…」同一个中文词。
- `A pledge was given and the whistler sang softly`、`He made a pledge to help the poor.` —— 课文第 1503 句；pledge 卡 `ex`（只作背景：本段"承诺"另有一个词，且它的 `ex` 与 vow 卡 `ex` 同一个 make…to 框架；pledge 不是本组成员，按硬约束 2 不进表）。
- `He contemplated the stars and stared at the night sky` —— 课文第 1501 句（本段背景，只说明 1502 前面站着的是"对着夜空沉思"）。
- 义项「n. 誓言」「n. 誓言；v. 发誓」 —— 两卡 `m`；「赶公交上学，利奥立志今天准时努力」—— 段 1 `paraZh`；「上篇·备考的一天」—— `subheads[5][0]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，oath 侧只有 2 条、卡上连词伙都没有

- 两卡互不列同义词（oath `note` 空串；vow `note` 只有词伙段），worklist 标 `all_synonyms: false` 有据；义项交集「誓言」在本段两处**都成立**（1502 两半各是一个誓的名词），所以本组不属义项名不副实型，也不属档位错那型。
- **证据强度（§三.8）**：oath 侧独立文本 **2 条**（课文 1502 + 卡 ex 那一句），卡上无词伙段可引；vow 侧独立文本 **4 条**（课文 1502 + 未打标记的 119、491 + 卡 ex），词伙 `solemn vow` 与 1502 同源不另计。**4 对 2 是本片厚薄差距最大的一组**（组 1、组 3、组 5 都是 2 对 2），但按口径 7 这是常态、不必退。
- **"vow 能当动词"这条有 2 处书内文本**（119、491），但两处都**没打 `[[vow:…]]` 标记**：按口径 13 已分档写清，落地时别把它说成"课文标过的用法"；也别据此声称"oath 不能当动词"（oath 卡 `m` 只给 n.，本书也确实 0 处，但那是卡上没给档、不是本书禁过）。
- **音标按卡 `p` 字段原样搬**：oath 卡 `p` 与 `uk` 同为 /əʊθ/、`us` 另标 /oʊθ/；vow 三处一致 /vaʊ/ —— 英美两读不是本组的区别。
- **复数与形式**：本书 `oaths`、`vows` 两种复数写法都没有；1502 那句 vow 用的是单数。课文 545 的 `vowel` 与 vow 无关（子串撞车，计数时已排除）。
- **不写的**：「oath 偏法律／庭上的宣誓、vow 偏宗教／婚誓」（两卡 `note` 无语域标注；oath 卡 ex 那句 `to tell the truth` 是唯一线索，但本书没写它在法庭）；`swear an oath`（swear 是组 5 的成员，本书没给这个词伙）、`take a vow of silence`、`wedding vows`、`parliamentary oath`、`under oath` 这类本书没有的串一律不列。
- 无需拍板：本组共享义项在两处书证上都成立，唯一要报备的是 oath 侧空 `note`（词伙拿不出来）与 vow 侧两处未打标记的动词写法，均已在表里按分档写明。

---

## 组5 · curse / swear —— 咒骂 ｜ `all_synonyms`: false ｜ 段 [5,2] ｜ 句 1509

**共现位置**：1 段 1 句 —— 第 5 章段 2（全局 1504–1509），两个词挤在同一句 1509 的前后两半，共用主语 `Leo` 与同一个 `will not`（前半直接骂、后半要拐一个 at）。**主锚点 = 章5 段2**（`paraZh`：一路波折后利奥赶到学校，学会冷静有礼。`subheads[5][0]`：上篇·备考的一天，本段在其下）。卡挂段 2 末；组 3、组 4 在段 1 末（不同段），本组不回去讲"看一眼"与"立誓"（口径 9）。
主锚点 = 章5 段2

**这一段在讲什么**：迟到那一路——1504 利奥要回妈妈的话、通知朋友公交晚了，1505 他清楚说出想法、冷静解释延误，1506 两个男孩大声争吵但司机制止了纠纷，1507 争论之后他加快脚步赶校门，1508 和善的门卫催他跑、没因迟到责骂他，1509 收在两句"不会骂"：不咒骂坏运气、不对着堵车骂人。整段是"生了气也不失礼"的行为清单。

**该段原文（章5 段2，成员句 + 前后各一句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1507 | After the short argument Leo must hasten on foot to reach the school gate. | 短暂的争论后，利奥必须加快脚步赶往校门。 |
| 1508 | A kind guard seems to urge him to run and does not scold him for lateness. | 一位和善的门卫似乎催他快跑，没有因迟到责骂他。 |
| 1509 | Leo will not **curse** the bad luck and will not **swear** at the heavy traffic. | 利奥不会咒骂坏运气，也不会对着拥堵的交通骂骂咧咧。 |

curse 全书带标记 1 处（1509，动词）、未打标记 0 处；swear 全书带标记 1 处（1509，动词）、未打标记 0 处（`swore`、`swears` 这两种写法课文里都没有；口径 13 分档报）。两卡 `note` 字段**都不存在**（无同义词栏、无词伙段），所以本组能引的搭配只有课文那一句和两卡各自的 `ex`。

### 表头一句（title）

> 同一句里两个"不骂"，主语一样、`will not` 一样，差别全在动词后面那个 at：curse 后面直接跟"坏运气"，swear 要拐一个 `at` 才接到"堵车"——worklist 的共同义项"咒骂"两处都成立，可本书给它们的中文不一样：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| curse | /kɜːrs/ | v. | v. 咒骂；n. 诅咒 | 把气撒在看不见的东西上 | 1509 他不骂那回坏运气；卡 ex 里老人低声嘟囔的一句诅咒 | `curse the bad luck`（课文 1509） |
| swear | /swer/ | v. | v. 发誓；咒骂 | 冲着眼前那件烦心事爆粗 | 1509 他不冲着堵车骂；卡 ex 里他发誓没偷钱 | `swear at the heavy traffic`（课文 1509） |

### 默认那一行（实测宽度 35.0 / ≤40）

```
curse = 骂坏运气（curse the bad luck），swear = 对着堵车骂（swear at）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 骂的对象看得见吗 | curse | `will not curse the bad luck`（1509，宾语是坏运气） | 气撒在**看不见、摸不着**的那样东西上 | 本书 curse 那半句的宾语是运气——你没法指着它骂 |
| 骂的对象看得见吗 | swear | `will not swear at the heavy traffic`（1509，at 后面是堵车） | 气撒在**眼前堵着的那件烦心事**上 | 本书 swear 那半句的宾语是交通：车就在那儿不动，他才骂 |
| 中间要不要一个介词 | curse | 本书唯一一处**不带介词**：`curse the bad luck`；卡 ex 给的是名词用法（`muttered a curse under his breath`） | 直接跟宾语 | 本组最硬的一格：同一句里 curse 后面是名词、swear 后面必须先放 at |
| 中间要不要一个介词 | swear | 本书唯一一处带 at：`swear at the heavy traffic`；卡 ex 那句什么介词都不带、后面直接跟他说的那件事（`He swore he didn't steal the money.`） | 骂某样东西要 `swear at`；说"发誓…"另走一条路 | 它的 at 在本书接的是**物**（traffic）；卡上那份旧表写的是"swear at sb = 骂某人"，本书没给过人当宾语的例子 |
| 卡上另一档用上了吗 | curse | 卡 `m` 第二档「n. 诅咒」——本书 1 处走的是动词档，名词档只在卡 ex（`The old man muttered a curse under his breath.`），课文里没有 | 想让"那句脏话"当东西摆出来时用它 | 本书没给 curse 的名词用例，读到一个光秃秃的 curse 先判它是动词 |
| 卡上另一档用上了吗 | swear | 卡 `m` 第一档「v. 发誓」——本书 1 处走的是第二档「咒骂」，发誓档只在卡 ex（`He swore he didn't steal the money.`），课文里没有 | 想说"我保证"时它跳出来 | 本书"发誓"这件事在段 1 的 1502 用的是 oath 与 vow，一次都没交给 swear |
| 中文落点 | 两个词 | 1509 译「利奥不会咒骂坏运气，也不会对着拥堵的交通骂骂咧咧」（`sentZh`）；卡 `exZh`：curse 译「低声嘟囔了一句诅咒」／swear 译「他发誓他没有偷钱」 | 汉译英时先问中文是"咒骂"还是"骂骂咧咧"，还是"发誓" | 本书译文已经替他们分了：curse 走「咒骂」，swear 走「骂骂咧咧」（一个词就点出"对着外面嚷"这层），两卡 ex 的中文则各走"诅咒"与"发誓" |

### 总结句（实测宽度 21.5 / ≤30）

> curse 后面直接跟对象，swear 骂的时候带 at。

### 出处清单

- `Leo will not curse the bad luck and will not swear at the heavy traffic`、`Leo will not curse the bad luck`、`curse the bad luck`、`the bad luck`、`will not swear at the heavy traffic`、`swear at the heavy traffic`、`the heavy traffic`、译文「利奥不会咒骂坏运气，也不会对着拥堵的交通骂骂咧咧」 —— 课文第 1509 句（段 [5,2] 第 5 句，两词都带标记）及 `sentZh`。**两词在本书唯一的同段书证就是这一句，也是各自唯一的一处。**
- `The old man muttered a curse under his breath.` / 老人低声嘟囔了一句诅咒。 —— curse 卡 `ex` / `exZh`（表里写作 `muttered a curse under his breath`，为其连续子串）。curse 卡 `note` 字段不存在，**卡上无词伙可引**（§三.8 明说）。
- `He swore he didn't steal the money.` / 他发誓他没有偷钱。 —— swear 卡 `ex` / `exZh`。swear 卡 `note` 字段不存在，**卡上无词伙可引**（§三.8 明说）。
- `heavy traffic` —— traffic 卡 `note` 词伙段（与课文 1509 **同源**，口径 8 不另计；traffic 卡 `ex` 另有一句 `The traffic on the highway was very heavy this morning.`，只作旁证，traffic 不是本组成员）。
- `A kind guard seems to urge him to run and does not scold him for lateness`、`After the short argument Leo must hasten on foot to reach the school gate` —— 课文第 1508 / 1507 句（本段背景：同段另有 scold「责骂」、argument、quarrel、dispute 几个"吵／骂"词，均未与本组并成一组，按硬约束 2 不进表）。
- `An oath was taken and a solemn vow made silently` —— 课文第 1502 句（组 4 的成员句，本组只用来报备："发誓"这一档本书交给 oath / vow，没交给 swear）。
- 两卡上那份 `cmp`（`group`: curse-swear，`at` 写作 [5, 2]）逐字引：title「同句里两个词都和“骂”有关，但侧重点不同：」；items 里 curse 的 `core` 作「语气重，带恶意和愤怒，希望对方遭殃」、`eg` 作 `curse the bad luck`，swear 的 `core` 作「日常爆粗口发泄；也可表示郑重发誓」、`eg` 作 `swear at the heavy traffic`；diff 三行 label 是「语气」「对象」「其他含义」；summary 里写着「swear at sb = 骂某人」「swear to do = 发誓做」。 —— 两卡 `cmp` 字段（同一份内容的两份拷贝），本组「数据边界」逐条回查了它。
- 义项「v. 咒骂；n. 诅咒」「v. 发誓；咒骂」 —— 两卡 `m`；「一路波折后利奥赶到学校，学会冷静有礼」—— 段 2 `paraZh`；「上篇·备考的一天」—— `subheads[5][0]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡，但卡上已经挂着一张本组的表（重复度全片最高，要拍板）

- 两卡互不列同义词（两卡连 `note` 字段都没有），worklist 标 `all_synonyms: false` 有据；义项交集「咒骂」在本段两处**都成立**（译文「咒骂」／「骂骂咧咧」都是骂），所以本组不属义项名不副实型。
- **重复情况要说清**：`curse` 与 `swear` 两卡上各挂着一份 `cmp`，`group` 就叫 curse-swear，而且它的 `at` 字段写的正是 [5, 2] —— 与本组**同段同句**。那份旧表已占掉"语气轻重""对象是人是物"两格，本卡的增量是三格它没有的：**本书这一句里 curse 零介词、swear 带 at**（框架，可 grep）、**两卡 `ex` 各走另一档**（curse 的名词档 / swear 的发誓档，本书都 0 处）、**译文自己把两词分成「咒骂」与「骂骂咧咧」**。
- **顺手回查那份旧表的两条断言（口径 15）**：① 它写「swear at sb = 骂某人」——本书唯一一处 `swear at` 后面接的是 traffic（物），没有一例 sb；② 它写「swear to do = 发誓做」——课文里 `swear to` 这一串 0 处（swear 全书只 1509 一处，且接的是 at），卡 ex 那句走的是另一条路：swore 后面直接跟他说出来的那件事（宾语从句，exZh 逐字印「他发誓他没有偷钱」），一个 to 都没有。这两条在**课文与两卡 `ex` 里都没有书证**（唯一的出处就是那份 `cmp` 自己写的），落地时别把它们当课文结论；是否删改属产品数据改动，不在本草稿权限内，只报备。
- **同源合并（口径 8）**：traffic 卡词伙 `heavy traffic` = 课文 1509，不另计。两侧独立文本各 **2 条**（课文 1 + 卡 ex 1），与组 1、组 3 并列本片最薄，按口径 7 属常态；两卡 `note` 都缺失，所以"常见搭配"一列只能放课文片段与卡 ex 原句，没有一条卡上现成词伙。
- **音标按卡 `p` 字段原样搬**：curse 卡 `p` 与 `us` 同为 /kɜːrs/、`uk` 另标 /kɜːs/；swear 卡 `p` 与 `us` 同为 /swer/、`uk` 另标 /sweə/ —— 英美两读不是本组的区别。
- **计数结论、不是禁令**：本书 1 处 vs 1 处撑不起"语气轻重"这条（旧表那条是靠语感写的）——本卡因此**不写褒贬／轻重**，只写框架与宾语看得见看不见。
- **不写的**：`curse words`、`a curse on sb`、`bless or curse`、`swear by`、`swear at sb`、`bad language`、`profanity` 这类本书没有的串一律不列；「swear 还能当名词指誓言」（卡 `m` 只给 v.，与组 4 的 vow / oath 分工不同，本书没有这一用法）。
- **提请拍板点**：本组与卡上既有 `cmp`（curse-swear，已声明挂 [5,2]）**是同一段同一句的同一对词**，是全片唯一一处"新卡与旧表正面重叠"。请他定：新卡替换旧 `cmp`、还是旧 `cmp` 保留而本卡不出（组 3 的 glance / glimpse 是同型但旧表面向四词、范围更大）。我按现表出卡，未动任何卡数据。

---

## 附：本切片复核索引（给门禁 / 抽查用）

| 组 | worklist_idx | words | senses | `all_synonyms` | paras | sents | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 146 | individual / single | 单独 | false | [4,31] | 1464,1466 | 出卡但**义项两侧都不落地**：1466 的 individual 是名词「每人」、1464 的 single 译「同一个」；硬区别 = Each + individual 做主语 / a + single 做定语；两侧独立文本各 2 条 |
| 2 | 147 | committee / council | 委员会 | false | [4,33] | 1478 | 出卡但**「委员会」只在 committee 侧成立**（council 两处中文是「镇议会」「市政府理事会」）；硬区别 = meet 归 committee、town/city 归 council；committee 另有 1 处未打标记（课文 1215「街道小组」） |
| 3 | 148 | glance / glimpse | 一瞥 | false | [5,1] | 1498,1499 | 出卡，义项两侧成立；**两卡已各带一份四词 `cmp`（look-verbs）**，与本卡同段末 → 要拍板；硬区别 = take…at（表／钟，不会跑）vs get/catch…of（公交／猫，会跑）；两卡 `note` 均无 |
| 4 | 149 | oath / vow | 誓言 | false | [5,1] | 1502 | 出卡，义项两侧成立；硬区别 = take 归 oath、make 归 vow（本句两半各配一个）+ vow 另有动词档且本书 2 处未打标记（119、491）；oath 卡 `note` 是空串 |
| 5 | 150 | curse / swear | 咒骂 | false | [5,2] | 1509 | 出卡，义项两侧成立；**两卡已各带一份 `cmp`（curse-swear，`at` 就写 [5,2]）＝同段同句重叠，全片唯一** → 要拍板；硬区别 = curse 零介词 vs swear 带 at；旧表两条（swear at sb / swear to do）本书 0 书证，已报备 |

- 全局句号口径按章偏移 `[0, 339, 651, 883, 1277, 1492]`（全书 1833 句）自算：章4 段31 起 1462（1464、1466 为段内第 2、4 句）、段33 起 1474（1478 为段内第 4 句）、章5 段1 起 1498（1498、1499、1502 为段内第 0、1、4 句）、段2 起 1504（1509 为段内第 5 句）—— 5 组 7 个句号与切片包 `sents` 逐个对得上。
- 挂段位置：组 1 → 章4 段31 末；组 2 → 章4 段33 末；组 3、组 4 → 章5 段1 末（同段两张，切入角不重叠：组 3 只讲 1498–1500 的"看"，组 4 只讲 1501–1503 的"立誓"）；组 5 → 章5 段2 末。按 worklist_idx 升序排。
- 本片 5 组的宽度标称值全部用 `tools/width_rule.py` 的同一把尺量出（汉字与全角标点各 1、其余 0.5）：默认行 38.0 / 35.0 / 37.0 / 34.0 / 35.0，总结句 14.0 / 15.0 / 17.5 / 16.5 / 21.5，无一条超上限。
- 章5、章4 的 `paraZh` 均可作背景（口径 12 点名的坏数据是**章3 段 22–43**，本片 5 组不落在那 22 段里）；本片各组表头一律只引课文真有的句子，主题栏逐字附注、不扩写。
- 本文件不引用任何在 `shadow/data/vocab.json`、`shadow/data/sections.json` 里查不到的英文串；门禁命令与结果见文件头小结（`python3 tools/check_compare_draft.py work/辨析草稿/5_03.md`）。
- 落地成卡对象时：`title` = 各组「表头一句」，`summary` 的「简单记」= 各组「默认那一行」，`diff` 各行 `label` = 「维度」列，键名用成员词头。

