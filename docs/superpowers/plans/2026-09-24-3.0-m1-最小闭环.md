# 3.0 M1（最小闭环）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `ielts-vocab.bond/app/` 交出一个能被真人用起来的最小闭环：首页选文章 → 进文章任务模式 → ① 通读（一步一停）→ ② 文内挖空浮窗答题 → 收工，进度存档且刷新不丢。

**Architecture:** `/app/index.html` 是 `shadow/index.html` 的**分叉**（fork）—— 把影子跟读那套运行时（播放链 / 任务模式 / 出题引擎 / 词库抽屉 / 云同步）整段搬过来，换掉外壳（左侧导航 + hash 路由），换掉 ② 的承载 UI（底部题卡 → 文内挖空 + 浮窗）。**引擎与数据不 fork**：`/app/` 直接 `<script src="/shadow/js/plan-engine.js">` 并读 `/shadow/data/*.json`，只有一份真值源。`/`（情景训练）与 `/shadow/` 全程一行不动。

**Tech Stack:** 零依赖单文件（原生 HTML/CSS/JS）+ `shadow/js/plan-engine.js`（共享引擎）+ Playwright E2E。

**Spec:** `docs/superpowers/specs/2026-09-24-3.0-article-first-PRD.md`

## Global Constraints

- **`/` 与 `/shadow/` 一行都不能改**（除引擎那一处向后兼容的加参）。改完必须 `git diff --stat` 确认没碰到它们。
- 引擎改动**只允许加参数**：`assemble(state, opts)` 不带 `scope` 时行为必须与 v2.0 逐字一致 —— 影子跟读那 157 条 E2E 是这条的锁，改完必须全绿。
- **localStorage key 不变**：继续用 `ielts.shadow.v2`（与影子跟读共用，老用户进度天然续上）。不许改 schema。
- 所有新增的、运行时会被抓的文件，必须同时加进 `scripts/build_site.mjs` 的 `FILES` 白名单。
- 文案：全中文界面；`n/N` 这类数字只在一个地方说（PRD §10.3）；触摸目标 ≥ 44px。
- 每个任务收尾都要 `cd playwright-tests && npx playwright test <相关 spec>` 绿 + `python3 scripts/validate_data.py` PASSED。
- 分支纪律：`agent/<name>-3.0-m1-<date>`，不直接动 `main`（AGENTS.md）。

---

## File Structure

| 文件 | 职责 | 新建/改动 |
|------|------|----------|
| `shadow/js/plan-engine.js` | **共享引擎**。本次只加：`assemble` 的 `scope` 参数 + 三个按篇派生函数 | 改（向后兼容） |
| `app/index.html` | 3.0 外壳：左侧导航 + 5 个一级页面的容器 + 任务模式容器 + CSS | 新建（fork 自 shadow） |
| `app/app.js` | 3.0 的页面渲染与路由（首页 / 学习数据 / 单词本 / 随身听 / 我的 —— M1 只有首页 + 任务模式有内容） | 新建 |
| `shadow/index.html` | **一行不动**（运行时逻辑照抄进 app/index.html） | 不动 |
| `playwright-tests/journeys/app3/` | 3.0 的 E2E（新目录） | 新建 |
| `playwright-tests/playwright.config.ts` | 加一个指向 `/app/` 的项目（或复用 baseURL + 路径） | 改 |
| `scripts/build_site.mjs` | `FILES` 白名单加 `app/index.html`、`app/app.js` | 改 |

**为什么 `app/app.js` 而不是全塞进 index.html**：fork 后光运行时就有 6700 行，外壳与 5 个页面再塞进去会破 10000 行（PRD §2.5 的自定红线）。拆成「index.html = 外壳 + CSS」「app.js = 页面与路由」两个文件，各自还能一口气读完。

---

## Task 1: 引擎按篇化（`scope` + 三张派生索引）

**Files:**
- Modify: `shadow/js/plan-engine.js`（加 `assemble` 的 `scope`；加 `wordArticle` / `countStagesOf` / `articleScope`）
- Test: `playwright-tests/journeys/shadow/plan-engine.spec.ts`（追加一个 describe）

**Interfaces:**
- Consumes: 现有 `assemble(state, opts)`、`SECTIONS`（由调用方通过 `wordsOf` / `totalSents` 间接提供 —— ⚠️ 引擎本身不认识 `SECTIONS`，篇章归属必须由调用方传进来）
- Produces:
  - `assemble(state, {…, scope})` —— `scope: Set<number> | null`，给定时只从这些全局句号里挑句；`null`/省略 = 全量（v2.0 行为）
  - `ShadowPlan.articleScope(sections, article)` → `Set<number>`（该篇的全局句号）
  - `ShadowPlan.wordArticle(sections, word)` → `number[]`（词出现在哪几篇，按篇号升序）
  - `ShadowPlan.countStagesOf(state, scope, wordsOf)` → `{seen,recognized,owned,graduated,leech}`（只在 scope 内的句子里统计词；**没有 `fresh`** —— 引擎不知道目标词总数）

- [ ] **Step 1: 写失败测试**

在 `playwright-tests/journeys/shadow/plan-engine.spec.ts` 末尾追加：

```ts
test.describe('按篇化：scope 与三张派生索引（3.0 M1）', () => {
  test('不带 scope 时 assemble 与 v2.0 逐字一致', async ({ page }) => {
    const got = await page.evaluate(() => {
      const st = ShadowPlan.emptyState();
      // 造 3 句、每句 1 个词都读到过 —— 只比「带/不带 scope」的相对差
      for (let i = 0; i < 3; i++) st.words['w' + i] = ShadowPlan.newWord();
      const base = { now: Date.now(), todayMinutes: 15, rate: 1, secNew: 25, secReview: 8,
                     wordsOf: (i: number) => ['w' + i], totalSents: 100, boundaryHour: 4,
                     plan: { todayMinutes: 15, boundaryHour: 4, pausedNew: false } };
      const a = ShadowPlan.assemble(st, base);
      const b = ShadowPlan.assemble(st, Object.assign({}, base, { scope: null }));
      return { qa: a.queue.map((x: { i: number }) => x.i), qb: b.queue.map((x: { i: number }) => x.i) };
    });
    expect(got.qa).toEqual(got.qb);
  });

  test('scope 收窄后只排该篇的句', async ({ page }) => {
    const got = await page.evaluate(() => {
      const st = ShadowPlan.emptyState();
      const scope = ShadowPlan.articleScope(SECTIONS, 0);
      const inScope = Array.from(scope);
      const out = ShadowPlan.assemble(st, { now: Date.now(), todayMinutes: 60, rate: 1,
        secNew: 25, secReview: 8, wordsOf: (i: number) => SECTIONS.flatMap((s: any) =>
          s.paragraphs.flat().map((_: any, k: number) => k)).slice(i, i + 1),
        totalSents: 1833, boundaryHour: 4,
        plan: { todayMinutes: 60, boundaryHour: 4, pausedNew: false }, scope });
      return { inScope: inScope.length, queued: out.queue.map((x: { i: number }) => x.i) };
    });
    expect(got.queued.length, '收窄后一句都没排 = 这条什么都没测').toBeGreaterThan(0);
    expect(got.queued.every((i: number) => i < got.inScope), '排出了 scope 外的句').toBe(true);
  });

  test('wordArticle / countStagesOf 与全局对得上', async ({ page }) => {
    const got = await page.evaluate(() => {
      const w = 'volcano';
      const arts = ShadowPlan.wordArticle(SECTIONS, w);
      const st = ShadowPlan.emptyState();
      st.words[w] = ShadowPlan.newWord();
      st.words[w].stage = 'graduated';
      const one = ShadowPlan.countStagesOf(st, ShadowPlan.articleScope(SECTIONS, arts[0]));
      let sum = 0;
      for (let a = 0; a < SECTIONS.length; a++) sum += ShadowPlan.countStagesOf(st, ShadowPlan.articleScope(SECTIONS, a)).graduated;
      return { arts, oneGrad: one.graduated, sum };
    });
    expect(got.arts.length, '一个目标词应当至少归属一篇').toBeGreaterThan(0);
    expect(got.oneGrad).toBe(1);
    expect(got.sum, '六篇已毕业数之和必须等于全局那 1 个').toBe(1);
  });
});
```

- [ ] **Step 2: 跑测试确认红**

Run: `cd playwright-tests && npx playwright test journeys/shadow/plan-engine.spec.ts -g "按篇化"`
Expected: FAIL —— `ShadowPlan.articleScope is not a function`

- [ ] **Step 3: 最小实现（加在 `plan-engine.js` 的 `return`/导出之前）**

```js
  /* ---- 3.0 按篇化：scope + 三张派生索引 ----
     引擎不认识 SECTIONS（它是纯函数模块），所以篇章归属一律由调用方把 sections 传进来。 */

  // 该篇的全局句号集合。sections[].paragraphs[].length 就是每段的句数，与 index.html 的
  // computeChapterStart / chapterSentCount 同一套口径（跨章句号 = 前面所有篇的句数之和 + 本句在篇内的序号）。
  function articleScope(sections, article) {
    const out = new Set();
    if (!Array.isArray(sections) || !sections[article]) return out;
    let base = 0;
    for (let a = 0; a < article; a++) {
      for (const p of sections[a].paragraphs) base += p.length;
    }
    let n = 0;
    for (const p of sections[article].paragraphs) n += p.length;
    for (let k = 0; k < n; k++) out.add(base + k);
    return out;
  }

  // 词 → 出现在哪几篇。真来源是课文里的 [[词:形式]] 标记，sections[].paragraphs[][] 里存的就是原始串。
  function wordArticle(sections, word) {
    const hits = new Set();
    if (!Array.isArray(sections) || !word) return [];
    const needle = '[[' + word + ':';
    for (let a = 0; a < sections.length; a++) {
      for (const p of sections[a].paragraphs) {
        for (const raw of p) {
          if (typeof raw === 'string' && raw.indexOf(needle) >= 0) { hits.add(a); break; }
        }
      }
    }
    return Array.from(hits).sort((x, y) => x - y);
  }

  // 只在 scope 内的句子里统计词（scope 为 null/省略 → 全量）。
  // ⚠️ 引擎里**没有** countStages —— 那个函数长在 shadow/index.html 里（它还要 ALL_TARGET_WORDS
  //    才能算 fresh）。所以这里从零数，**别去调 countStages**，否则 ReferenceError。
  //    也不返回 fresh（引擎不知道目标词总数，fresh 由调用方拿总数减）。
  function countStagesOf(state, scope, wordsOf) {
    const c = { seen: 0, recognized: 0, owned: 0, graduated: 0, leech: 0 };
    const ws = (state && state.words) || {};
    let keys;
    if (!scope || typeof wordsOf !== 'function') {
      keys = Object.keys(ws);
    } else {
      const inScope = new Set();
      scope.forEach(i => wordsOf(i).forEach(w => inScope.add(w)));
      keys = Object.keys(ws).filter(k => inScope.has(k));
    }
    keys.forEach(k => {
      const x = ws[k];
      c[x.stage] = (c[x.stage] || 0) + 1;
      if (x.leech) c.leech++;
    });
    return c;
  }
```

`assemble` 内部选句的地方（三池装句子那几处）统一加一道闸：

```js
  // 3.0：按篇排。scope 给定时只从这些全局句号里挑；省略 = 全量（v2.0 行为，逐字不变）。
  const scope = (o.scope instanceof Set) ? o.scope : null;
  const inScope = (i) => !scope || scope.has(i);
```

然后每个 `push({ i })` / `queue.push` 前判 `if (!inScope(i)) continue;`。

导出：在文件末尾的 `return { … }` 里加 `articleScope, wordArticle, countStagesOf`。

- [ ] **Step 4: 跑测试确认绿**

Run: `cd playwright-tests && npx playwright test journeys/shadow/plan-engine.spec.ts -g "按篇化"` → PASS
Run: `cd playwright-tests && npx playwright test journeys/shadow/plan-engine.spec.ts` → 全绿（旧行为没被碰）

- [ ] **Step 5: 全量回归（引擎是共享的，必须证没影响影子跟读）**

Run: `cd playwright-tests && npm test`
Expected: 157 passed

- [ ] **Step 6: 提交**

```bash
git add shadow/js/plan-engine.js playwright-tests/journeys/shadow/plan-engine.spec.ts
git commit -m "feat(engine): assemble 加 scope + 三张按篇派生索引（3.0 M1，向后兼容）"
```

---

## Task 2: `/app/` 骨架（fork + 外壳 + 路由 + 白名单）

**Files:**
- Create: `app/index.html`（fork 自 `shadow/index.html` 的运行时，换外壳）
- Create: `app/app.js`
- Modify: `scripts/build_site.mjs`（`FILES` 加两条）
- Test: `playwright-tests/journeys/app3/shell.spec.ts`

**Interfaces:**
- Consumes: `/shadow/js/plan-engine.js`、`/shadow/data/*.json`（共享，不复制）
- Produces: `window.APP3`（路由与页面渲染的命名空间）；`#appShell`（左侧导航 + `#appView` 内容区）

- [ ] **Step 1: 写失败测试**

```ts
// playwright-tests/journeys/app3/shell.spec.ts
import { test, expect } from '../../fixtures';
test.describe('3.0 外壳', () => {
  test('左侧导航 5 项，只有首页有内容，旧版两个入口未受影响', async ({ page, baseURL }) => {
    await page.goto(`${baseURL}/app/index.html`);
    await expect(page.locator('.sidenav .nav-item')).toHaveCount(5);
    expect(await page.locator('.sidenav .nav-item').allInnerTexts())
      .toEqual(['首页', '学习数据', '单词本', '随身听', '我的']);
    // 深链
    await page.goto(`${baseURL}/app/index.html#/stats`);
    await expect(page.locator('.sidenav .nav-item.on')).toHaveText('学习数据');
  });
});
```

- [ ] **Step 2: 跑测试确认红** → 404 / 选择器不存在

Run: `cd playwright-tests && npx playwright test journeys/app3/shell.spec.ts`
Expected: FAIL（`/app/index.html` 404）

- [ ] **Step 3: fork 与改造**

```bash
mkdir -p app
cp shadow/index.html app/index.html      # 起点：整段运行时照搬
```

然后对 `app/index.html` 做**五处**改动（每处都给出锚点，照着改）：

1. **`<head>` 里加 app.js**：在 `<script src="js/plan-engine.js"></script>` 之后加
   ```html
   <script src="/shadow/js/plan-engine.js"></script>
   <script defer src="app.js"></script>
   ```
   （把原来相对路径的 `js/plan-engine.js` 改成 `/shadow/js/plan-engine.js` —— 共享同一份引擎，绝不复制）
2. **数据路径**：把 `fetch('data/sections.json'` / `'data/vocab.json'` / `'data/chapters.json'` 三处改成 `/shadow/data/…`（共享同一份数据，绝不复制）
3. **删掉旧外壳**：`<header class="topbar">`、`<footer class="audiobar">`、`<div class="layout">`（含 `#panel` 词库抽屉）**整段删掉**，换成：
   ```html
   <body>
     <div id="appShell">
       <nav class="sidenav" aria-label="主导航">
         <div class="nav-brand">雅思背单词 <span>3.0</span></div>
         <button class="nav-item on" data-route="home">首页</button>
         <button class="nav-item" data-route="stats">学习数据</button>
         <button class="nav-item" data-route="words">单词本</button>
         <button class="nav-item" data-route="listen">随身听</button>
         <button class="nav-item" data-route="me">我的</button>
       </nav>
       <main id="appView" role="main"></main>
     </div>
   </body>
   ```
4. **改 `body` 的 class 机制**：删掉 `body.reading` / `body.scrolled` 那套顶栏收缩的 CSS 与 JS（新外壳没有顶栏），保留 `.task-mode` / `.dark`。
5. **加外壳 CSS**（`app/index.html` 的 `<style>` 末尾）：
   ```css
   #appShell { display: flex; min-height: 100vh; }
   .sidenav { flex: none; width: 220px; padding: 16px 12px; border-right: 1px solid var(--border);
     display: flex; flex-direction: column; gap: 4px; position: sticky; top: 0; height: 100vh; }
   .nav-brand { font-weight: 700; padding: 6px 10px 14px; color: var(--accent-text); }
   .nav-item { min-height: 44px; text-align: left; padding: 0 12px; border: none; border-radius: 12px;
     background: transparent; font-size: 14px; color: var(--text); cursor: pointer; }
   .nav-item.on { background: var(--accent-soft, rgba(16,180,135,.07)); color: var(--accent-text); font-weight: 600; }
   #appView { flex: 1; min-width: 0; padding: 20px 24px 88px; }
   @media (max-width: 700px) {
     #appShell { flex-direction: column-reverse; }
     .sidenav { width: auto; height: auto; position: fixed; left: 0; right: 0; bottom: 0; z-index: 300;
       flex-direction: row; border-right: none; border-top: 1px solid var(--border);
       padding: 6px; padding-bottom: calc(6px + env(safe-area-inset-bottom, 0px)); }
     .nav-brand { display: none; }
     .nav-item { flex: 1; text-align: center; font-size: 11px; }
     #appView { padding: 16px 16px 88px; }
   }
   ```

**`app/app.js`**（M1 只有首页与任务模式有内容）：

```js
/* 3.0 外壳路由。M1 只有 #/home 与文章任务模式有内容，其余四屏先给「建设中」占位。 */
(function () {
  const ROUTES = ['home', 'stats', 'words', 'listen', 'me'];
  let cur = 'home';
  function route() {
    const h = (location.hash || '#/home').replace(/^#\//, '').split('/')[0];
    cur = ROUTES.includes(h) ? h : 'home';
    document.querySelectorAll('.sidenav .nav-item').forEach(b =>
      b.classList.toggle('on', b.dataset.route === cur));
    const view = document.getElementById('appView');
    if (cur === 'home' && window.APP3 && window.APP3.renderHome) return window.APP3.renderHome(view);
    if (cur === 'home') { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }
    view.innerHTML = '<div class="app-todo"><h3>' +
      ({ stats: '学习数据', words: '单词本', listen: '随身听', me: '我的' })[cur] +
      '</h3><p class="sm">这一屏在 3.0 的后续里程碑里交付。</p></div>';
  }
  document.addEventListener('click', (e) => {
    const b = e.target.closest('.nav-item');
    if (b) { location.hash = '#/' + b.dataset.route; }
  });
  window.addEventListener('hashchange', route);
  window.APP3 = Object.assign(window.APP3 || {}, { route, current: () => cur });
  document.addEventListener('DOMContentLoaded', route);
  route();
})();
```

- [ ] **Step 4: 加进构建白名单**

`scripts/build_site.mjs` 的 `FILES` 数组加：

```js
  'app/index.html',
  'app/app.js',
```

- [ ] **Step 5: 跑测试确认绿** → `npx playwright test journeys/app3/shell.spec.ts` PASS
- [ ] **Step 6: 证旧版没被碰**

Run: `cd playwright-tests && npx playwright test journeys/shadow/shell-nav.spec.ts journeys/smoke.spec.ts` → 全绿
Run: `git diff --stat` → 确认 `index.html` / `shadow/index.html` **不在改动列表里**

- [ ] **Step 7: 提交**

```bash
git add app/index.html app/app.js scripts/build_site.mjs playwright-tests/journeys/app3/shell.spec.ts
git commit -m "feat(app3): /app/ 外壳（左侧导航 5 项 + hash 路由），引擎与数据共享 /shadow/ 不复制"
```

---

## Task 3: 首页六张文章卡片

**Files:**
- Modify: `app/app.js`（`renderHome`）
- Test: `playwright-tests/journeys/app3/home.spec.ts`

**Interfaces:**
- Consumes: `ShadowPlan.articleScope` / `countStagesOf` / `wordArticle`（Task 1）、`SECTIONS`、`ROOT2`
- Produces: `window.APP3.renderHome(el)`、`window.APP3.articleStat(articleIndex)` → `{progress, stage, grad, total, lastAt}`

- [ ] **Step 1: 写失败测试**

```ts
// playwright-tests/journeys/app3/home.spec.ts
import { test, expect } from '../../fixtures';
test.describe('3.0 首页', () => {
  test('六张卡片 + 四档状态 + 卡片数据', async ({ page, baseURL }) => {
    await page.goto(`${baseURL}/app/index.html#/home`);
    await expect(page.locator('.art-card')).toHaveCount(6);
    await expect(page.locator('.art-card .a-title').first()).toHaveText('地球与生命');
    // 全新用户：六张全是「未开始」
    expect(await page.locator('.art-card[data-stage="todo"]').count()).toBe(6);
    // 造点进度 → 第一篇变「通读中」，且熟练度 > 0
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const st = TASK.state();
      const s = ShadowPlan.articleScope(SECTIONS, 0);
      Array.from(s).slice(0, 20).forEach((i: number) => (st.sents[i] = { lastReadAt: Date.now() }));
    });
    await page.reload();
    const c0 = page.locator('.art-card').first();
    await expect(c0).toHaveAttribute('data-stage', 'reading');
    expect(Number(await c0.locator('.a-pct').innerText().then(t => t.replace('%', '')))).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: 跑测试确认红** → `.art-card` 不存在

- [ ] **Step 3: 实现 `renderHome`**

```js
  function rel(ts) {                       // 相对时间，复用影子跟读口径
    if (!ts) return '还没学过';
    const d = Math.floor((Date.now() - ts) / 864e5);
    return d <= 0 ? '今天' : d === 1 ? '昨天' : d + ' 天前';
  }
  function articleStat(a) {
    const st = ROOT2.state, scope = ShadowPlan.articleScope(SECTIONS, a);
    const total = scope.size;
    let ever = 0;
    scope.forEach((i) => { const s = st.sents[i]; if (s && s.lastReadAt > 0) ever++; });
    const c = ShadowPlan.countStagesOf(st, scope, wordsOfSent);
    // ② 正确率 / 精读次数：走事件流（M1 先只算通读完成度，② 与精读在 Task 5/6 补）
    const progress = total ? Math.round(Math.min(ever / total, 1) * 40) : 0;   // 只算 40% 那项
    const stage = ever === 0 ? 'todo' : (ever >= total ? 'read' : 'reading');
    let lastAt = 0; scope.forEach((i) => { const s = st.sents[i]; if (s && s.lastReadAt > lastAt) lastAt = s.lastReadAt; });
    return { progress, stage, grad: c.graduated, total, lastAt, wordsOfArticle: scope.size };
  }
  function renderHome(view) {
    const cards = SECTIONS.map((s, a) => {
      const x = articleStat(a);
      return `<button class="art-card" data-a="${a}" data-stage="${x.stage}">
        <div class="a-title">${esc(s.title)}</div>
        <div class="a-en">${esc((TIT_EN[s.title] || '').replace(/^\s*·\s*/, ''))}</div>
        <div class="a-bar"><i style="width:${x.progress}%"></i></div>
        <div class="a-meta"><span class="a-pct">${x.progress}%</span>
          <span>已毕业 ${x.grad} / ${x.wordsOfArticle} 词</span>
          <span>${rel(x.lastAt)}</span></div>
      </button>`;
    }).join('');
    view.innerHTML = `<div class="home-banner" id="homeBanner"></div><div class="art-grid">${cards}</div>`;
    window.APP3.renderBanner(document.getElementById('homeBanner'));
  }
  window.APP3 = Object.assign(window.APP3 || {}, { renderHome, articleStat });
```

+ CSS（`app/index.html`）：

```css
   .art-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px; }
   .art-card { text-align: left; padding: 14px 16px; border: 1px solid var(--border); border-radius: 16px;
     background: var(--card); cursor: pointer; }
   .art-card[data-stage="todo"] { opacity: .62; }
   .art-card .a-title { font-size: 17px; font-weight: 700; }
   .art-card .a-en { font-size: 11.5px; color: var(--muted); margin: 2px 0 10px; }
   .art-card .a-bar { height: 6px; border-radius: 3px; background: rgba(11,84,64,.14); overflow: hidden; }
   .art-card .a-bar i { display: block; height: 100%; background: linear-gradient(90deg, #2bd4a4, #0fae7e); }
   .art-card .a-meta { display: flex; justify-content: space-between; gap: 8px; margin-top: 8px;
     font-size: 11px; color: var(--muted); }
   .art-card .a-pct { font-weight: 700; color: var(--accent-text); }
   @media (max-width: 1023px) { .art-grid { grid-template-columns: repeat(2, 1fr); } }
   @media (max-width: 700px) { .art-grid { grid-template-columns: 1fr; } }
```

- [ ] **Step 4: 跑测试确认绿** → PASS
- [ ] **Step 5: 提交** `feat(app3): 首页六张文章卡片（熟练度 / 阶段 / 单词 / 最近学习）`

---

## Task 4: 顶部「今天该做什么」+ 没计划的空状态

**Files:**
- Modify: `app/app.js`（`renderBanner`）
- Test: `playwright-tests/journeys/app3/home.spec.ts`（追加）

**Interfaces:**
- Produces: `window.APP3.renderBanner(el)`
- 依赖：`ROOT2.plan`（有/无计划）、`planFromTest()`（无 —— 用 `TASK.state()` 判）

- [ ] **Step 1: 写失败测试**

```ts
  test('没计划 → 横幅给「去设置」；有计划 → 给「继续学」', async ({ page, baseURL }) => {
    await page.goto(`${baseURL}/app/index.html#/home`);
    await page.evaluate(() => localStorage.removeItem('ielts.shadow.v2'));
    await page.reload();
    await expect(page.locator('#homeBanner .b-go')).toContainText('设置');
    // 点它 → 出设置屏（复用影子跟读的 renderSetup 容器）
    await page.locator('#homeBanner .b-go').click();
    await expect(page.locator('#setupSheet .ps-start')).toBeVisible();
    await page.locator('#setupSheet .ps-opt').nth(2).click();      // 15 分钟
    await page.locator('#setupSheet .ps-start').click();
    await expect(page.locator('#homeBanner .b-go')).toContainText('继续学');
  });
```

- [ ] **Step 2: 跑确认红**
- [ ] **Step 3: 实现**（横幅三分支：没计划 / 有计划没做完 / 做完；设置屏用一个 `#setupSheet` 容器套影子跟读现成的 `renderSetup`）
- [ ] **Step 4: 跑确认绿**
- [ ] **Step 5: 提交** `feat(app3): 首页横幅（今天该做什么）+ 没计划的设置入口`

---

## Task 5: 进入文章任务模式（按篇队列 + ① 通读）

**Files:**
- Modify: `app/app.js`（`openArticle`）
- Test: `playwright-tests/journeys/app3/task.spec.ts`

**Interfaces:**
- Produces: `window.APP3.openArticle(a)` —— 切到任务模式视图 + 按篇 assemble
- 依赖：Task 1 的 `scope`

- [ ] **Step 1: 写失败测试**

```ts
  test('点卡片 → 任务模式，n/N 是这一篇的句数（不是全局）', async ({ page, baseURL }) => {
    await page.goto(`${baseURL}/app/index.html#/home`);
    await page.evaluate(() => { TASK.resetV2(); TASK.initPlan(15); });
    await page.reload();
    await page.locator('.art-card').first().click();
    await expect(page.locator('#taskBar')).toBeVisible();
    const n = await page.evaluate(() => Number(document.getElementById('tbTitle').textContent.match(/\/\s*(\d+)/)![1]));
    const art1 = await page.evaluate(() => ShadowPlan.articleScope(SECTIONS, 0).size);
    expect(n, '任务条的 N 必须是第一篇的句数').toBe(art1);
  });
```

- [ ] **Step 2: 跑确认红**
- [ ] **Step 3: 实现**（`openArticle` → `currentArticle = a`；`todayPlan(force)` 调 `assemble` 时传 `scope: ShadowPlan.articleScope(SECTIONS, a)`；`syncQueueFromPlan` 与 `buildQueue` 都按 scope 收窄；正文只渲这一篇）
- [ ] **Step 4: 跑确认绿** + 全量回归（`npm test` 157）
- [ ] **Step 5: 提交** `feat(app3): 进文章任务模式（按篇队列 + ① 通读一步一停）`

---

## Task 6: ② 文内挖空 + 浮窗选择

**Files:**
- Modify: `app/index.html`（`.qz-blank` 的内联渲染 CSS + 浮窗 CSS）
- Modify: `app/app.js`（`renderQuiz` 改成「文内挖空 + 浮窗」）
- Test: `playwright-tests/journeys/app3/task.spec.ts`（追加）

**Interfaces:**
- Consumes: 现有 `buildQuiz` / `quizList` / `answerQuiz`（引擎不动）
- Produces: `window.APP3.maskArticle()` / `openBlank(bi)` / `nextBlank()`

- [ ] **Step 1: 写失败测试**

```ts
  test('② 挖空长在正文里，点空弹浮窗，下一题定位到下一个空', async ({ page, baseURL }) => {
    await page.goto(`${baseURL}/app/index.html#/home`);
    await page.evaluate(() => { TASK.resetV2(); TASK.initPlan(15); TASK.enterTaskMode(); TASK.setPass(2); });
    // 空是正文里的内联元素，且**不存在**底部题卡
    expect(await page.locator('#appView .sent .qz-blank').count()).toBeGreaterThan(0);
    expect(await page.locator('#taskCard').count(), '底部题卡必须不存在').toBe(0);
    await page.locator('.qz-blank').first().click();
    await expect(page.locator('#blankPop .qz-opt')).toHaveCount(4);
    await page.locator('#blankPop .qz-opt').first().click();
    await expect(page.locator('.qz-blank').first()).toHaveClass(/qa-done/);
    await expect(page.locator('#blankPop')).toBeHidden();
    const before = await page.evaluate(() => window.APP3.currentBlank());
    await page.locator('#tbNext').click();
    expect(await page.evaluate(() => window.APP3.currentBlank())).not.toBe(before);
  });
```

- [ ] **Step 2: 跑确认红**
- [ ] **Step 3: 实现**
  1. ② 态渲染：正文 `render()` 时，对 `quizList()` 里每个 item 的句子，把目标词位置换成 `<span class="qz-blank" data-bi="${bi}">`
  2. 点空 → `#blankPop`（复用 `.prog-tip` 玻璃配方）锚在空上方，内容 = 4 个 `.qz-opt` + 「这题有问题」
  3. 选完 → `answerQuiz(opt)` → 空里填词（`.qa-ok` 绿 / `.qa-no` 红 + `<i class="ans">正确答案</i>`）；答错就地摊 `revealNote` 辨析表
  4. `#tbNext` 在 ② 态 = `nextBlank()`（滚到下一个未答的空 + 弹浮窗；没有 = 小结）
  5. 删掉 `renderQuiz` 里所有写 `#taskCard` 的分支（并删 `#taskCard` 这个节点）
- [ ] **Step 4: 跑确认绿**
- [ ] **Step 5: 提交** `feat(app3): ② 文内挖空 + 浮窗四选一（底部题卡作废，引擎未动）`

---

## Task 7: 收工态 + 双入口 + 存档验证

**Files:**
- Modify: `app/app.js`（收工态按钮、入口 2）
- Test: `playwright-tests/journeys/app3/task.spec.ts`（追加）

- [ ] **Step 1: 写失败测试**

```ts
  test('两条入口 + 收工态 + 刷新不丢', async ({ page, baseURL }) => {
    await page.goto(`${baseURL}/app/index.html#/home`);
    // 入口 1：① 走完 → 小结卡有「开始答题」
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15); TASK.enterTaskMode();
      TASK.todayPlan(true).queue.forEach((q: { i: number }) => TASK.readDone(q.i));
      TASK.setPass(1); TASK.advance();
    });
    await expect(page.locator('.pass-summary .go')).toContainText('答题');
    // 入口 2：首页卡片对「通读已完成」的篇目出现「答题」按钮
    await page.goto(`${baseURL}/app/index.html#/home`);
    await expect(page.locator('.art-card[data-stage="read"] .a-quiz')).toHaveCount(1);
    // 收工态
    await page.evaluate(() => { TASK.setPass(2); /* 答完所有题 */ for (let g=0;g<300;g++){const q=TASK.currentQuiz(); if(!q)break; TASK.answerQuiz(q.answer); TASK.nextQuiz();} });
    await expect(page.locator('#tbNext')).toContainText('看词本');
    // 刷新不丢
    const before = await page.evaluate(() => TASK.state().daily);
    await page.reload();
    expect(await page.evaluate(() => TASK.state().daily)).toEqual(before);
  });
```

- [ ] **Step 2: 跑确认红**
- [ ] **Step 3: 实现**（收工态「看词本」先跳 `#/words` 占位屏（M3 才做真词本）；首页卡片在 `data-stage="read"` 时多一颗 `.a-quiz` 直达 ②）
- [ ] **Step 4: 跑确认绿**
- [ ] **Step 5: 提交** `feat(app3): 收工态 + 两条答题入口 + 存档回归`

---

## Task 8: 部署 `/app/` + 线上冒烟 + 收口

- [ ] **Step 1:** `cd playwright-tests && npm test`（157 + 新增 app3 全绿）
- [ ] **Step 2:** `python3 scripts/validate_data.py` → `Validation PASSED`
- [ ] **Step 3:** `node scripts/build_site.mjs` 本地构建，确认 `dist/app/index.html`、`dist/app/app.js` 都在，且构建自检不报缺文件
- [ ] **Step 4:** 合并到 `main` → 等 EdgeOne 部署
- [ ] **Step 5:** 线上冒烟（curl 三个路径的 HTTP 200 + 字节数）：
  - `https://ielts-vocab.bond/` → 与本地 `index.html` 字节一致（**旧版没被碰**）
  - `https://ielts-vocab.bond/shadow/` → 与本地 `shadow/index.html` 字节一致
  - `https://ielts-vocab.bond/app/` → 200，且含 `#appShell`
- [ ] **Step 6:** 切 tag `v3.0.0-m1`，更新 `VERSION` 文件（记 M1 内容与验证）
- [ ] **Step 7:** 告诉用户：「M1 上线了，地址 `ielts-vocab.bond/app/`，请试：点一篇文章 → 从头学到收工 → 刷新看进度在不在」

---

## Self-Review

**1. Spec coverage（M1 范围内的 PRD 条款）**

| PRD 条款 | 落在哪个 Task |
|---------|-------------|
| §2.1/§2.2 左侧导航 5 项 + 三档断点 | T2 |
| §2.3 没计划也能进 + 空状态跳配置 | T4 |
| §2.5 部署 `/app/` + 白名单 + 引擎/数据共享 | T2 / T8 |
| §3.1–3.5 首页六卡 + 四档状态 + 卡片数据 + 顶部横幅 | T3 / T4 |
| §4.1–4.2 任务模式结构 + 复用 v2.0 逻辑 | T5 |
| §4.4 ② 文内挖空 + 浮窗 + 两条入口 | T6 / T7 |
| §4.5 点句 = 一步一停 | T5（沿用 v2.0，不改） |
| §9.2 熟练度（M1 只算 40% 那项） | T3（② 与精读两项在 T6/T7 补） |
| §9.3 按篇队列（`scope`） | T1 / T5 |
| §9.4/§9.5 三张派生索引 | T1 |

**M1 明确不做**（留给 M2–M5）：学习数据页四块、单词本、随身听、我的、PWA、无障碍全覆盖、深色全覆盖。

**2. Placeholder scan**：全文无 TBD/TODO；每个代码步骤都给了可粘贴的代码或精确锚点（T2 的 fork 给了 5 处锚点，T5/T6 给了要改的函数名与阈值）。

**self-review 抓到并修掉的一处**：初稿让 `countStagesOf` 去调 `countStages(state)` —— 那个函数
**不在引擎里**（在 `shadow/index.html`，且还要 `ALL_TARGET_WORDS` 才能算 `fresh`），照抄会 ReferenceError。
已改成从零数、且不返回 `fresh`。

**3. Type consistency**：
- `ShadowPlan.articleScope(sections, article)` / `wordArticle(sections, word)` / `countStagesOf(state, scope, wordsOf)` —— T1 定义，T3/T5 使用，签名一致。
- `window.APP3.renderHome` / `articleStat` / `renderBanner` / `openArticle` / `currentBlank` —— T2/T3/T4/T5/T6 逐处定义并使用，名字一致。
- `#blankPop` / `.qz-blank` / `.qa-done` / `.art-card[data-stage]` —— T3/T6 里选择器与实现一致。
