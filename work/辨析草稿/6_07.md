# 辨析卡草稿 · 批次 6 切片 43（5 组 / worklist_idx 208–212，全是三成员组）

> 小结：切片 `work/compare_slices/slice43.json` 的 5 组（worklist_idx 208 / 209 / 210 / 211 / 212）全部为三词组，逐组写完；门禁 `python3 tools/check_compare_draft.py work/辨析草稿/6_07.md` 结果见本行末：**PASSED（0 条查不到）**。宽度全部用 `tools/width_rule.py` 那把尺实测，标称值＝实测值，逐组见各组小标题。
> 提请拍板 0 组。报备三处（都按既有口径自处，不需逐组等答复）：**组 1** worklist 义项「揭露」在 `sections.json` 全文 0 处（口径 6 的名不副实／档位型，标题按"这一段三个词各领哪样东西"写、义项只留 `sense` 槽）；**组 2** `compute` 词头卡上已经挂着一份批次 5 落地的 compute／estimate 表（已 grep 核实，一个词头只能带一张表，落地载体大概率换到 `calculate` 或 `count`，本组内容不为此改动）；**组 3** 同段的 `turn` 那处（1428）走的不是 worklist 那个义项、是拧阀门，且 `convert`、`turn` 两卡 `note` 整栏为空。
> 未动任何产品代码 / 数据文件（`shadow/data/vocab.json`、`shadow/data/sections.json` 只读未改），未碰别人的草稿（6_01 至 6_06、5 系列各份）与 `work/compare_slices/`、`work/辨析审核/`，未跑 `tools/land_compare.py`（任何模式），未做任何 git 写操作。

---

## 组1 · expose / reveal / uncover —— 揭露 ｜ `all_synonyms`: false ｜ 段 [4,18] ｜ 句 1388 / 1389（三成员）

**共现位置**：1 段 2 句 —— 第 5 章（章索引 4）段 18（全局 1384–1389），三个成员分布在**紧挨着的两句**里：1388 站着 reveal（同句还有 disclose）、1389 一句里站着 uncover 与 expose（由 `and` 并列，前半亮点、后半短处）。**主锚点 = 章4 段18**（`paraZh`：小镇理工学院请机修工带学生为清水街道创新，立志参赛。`subheads[4][18]`：下篇·发明比赛）。卡挂段 18 末。
主锚点 = 章4 段18

**这一段在讲什么**：1384 说镇上理工学院请了一位和善的机修工指导学生，1385 说他们要真进步、要为干净街道创新，1386 说没人指望院子里做的小玩意带来大突破，1387 说职员讲了专利、教学生怎么安全地发明实用东西，1388 说团队不提前说计划、要等到集市上才说，1389 说他们希望评委能把亮点找出来、而别把短处冷着摊开（全由本段句子本身撑）。

**该段原文（章4 段18）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1384 | Our small town polytechnic asked a kind mechanic to guide school pupils today. | 镇上小理工学院请了一位和善的机修工来指导学生。 |
| 1385 | They wanted real advance and hoped pupils could innovate for clean streets here. | 他们想要真正的进步，希望学生为干净街道创新。 |
| 1386 | Nobody expected a big breakthrough from a tiny gizmo made in a yard. | 没人想到院子里做的小发明会有大突破。 |
| 1387 | A clerk explained patents and told pupils how to invent useful things safely. | 职员讲解了专利，告诉学生如何安全地发明实用东西。 |
| 1388 | The team would not disclose plans early but would **reveal** them at the fair. | 团队不提前透露计划，要在集市上揭晓。 |
| 1389 | They hoped judges might **uncover** bright ideas and never **expose** weak points coldly. | 他们希望评委发现亮点，绝不冷漠地暴露短处。 |

按 §五.13 两档分开：**expose** 全书带 `[[词头:…]]` 标记 1 处（1389）＋ 未打标记 0 处；**reveal** 带标记 1 处（1388）＋ 未打标记 0 处；**uncover** 带标记 1 处（1389）＋ 未打标记 0 处（三词各按"原形+s/es/ed/ing"词形扫全书 1833 句，命中句数都是 1）。同段的 disclose（1388，带标记 1 处）不是本组成员，只作表头背景（硬约束 2）。

### 表头一句（title）

> 这一段三个词都没在被"揭露"什么（本书译文从头到尾没写过「揭露」这两个字，见下方核对）：1388 是团队自己决定哪天说（集市上才说），1389 前半是评委自己去把亮点找出来，后半是别把短处冷着摊到眼前——worklist 给的共同义项只留在各卡 `m` 里，本组的分工落在"谁动的手"与"摊出来的是哪样东西"：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| expose | /ɪkˈspəʊz/ | v. | 使遭受；暴露；揭露 | 把害人的东西摊到跟前 | 1389 评委不该冷着摊出来的那处短处；卡上没穿外套挨冻的那群孩子；卡上三条词伙全是"被摊到……跟前"（干扰、暴力画面、家暴） | `never expose weak points coldly`（课文 1389） |
| reveal | /rɪˈviːl/ | v. | 揭露；泄露；显露 | 到那个场合才说出口 | 1388 团队不提前说、留着到集市上说的计划；卡上那封信说出来的他的秘密；卡上现成的 facts | `would reveal them at the fair`（课文 1388） |
| uncover | /ʌnˈkʌvər/ | v. | 揭开；揭露 | 由别人自己翻出来 | 1389 希望评委找出来的那些亮点；卡上侦探翻出来的真相 | `uncover bright ideas`（课文 1389） |

### 默认那一行（实测宽度 39.5 / ≤40）

```
expose = 短处被摊开（weak points），reveal = 集市上才公布，uncover = 评委找亮点
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 摊出来的是哪样东西 | expose | `expose weak points`（1389）、`were exposed to cold weather`（卡 ex）、`be exposed to distractions`、`be exposed to violent images`、`be exposed to domestic violence`（三条词伙，本书课文 0 处，已 grep） | 说一样会伤着人的东西被摊到跟前 | 本组里只有它拿到的宾语**全是要躲开的东西**：短处、没外套的冷风、干扰、暴力画面、家暴 |
| 摊出来的是哪样东西 | reveal | `reveal them at the fair`（1388，them 回指 plans）、`revealed his secret`（卡 ex）、`reveal facts`（卡词伙，本书课文 0 处） | 说计划、秘密、事实被说出口 | 它摊出来的是"本来压在下面的东西"——计划、秘密、facts，书里没写它是好事还是坏事 |
| 摊出来的是哪样东西 | uncover | `uncover bright ideas`（1389；卡词伙同串、同源）、`uncovered the truth`（卡 ex） | 说亮点、真相被翻出来 | 它这两处摊出来的都是好东西（亮点、真相），跟 expose 那侧正好反着 |
| 谁动的手、什么时候 | expose | `never expose weak points coldly`（1389 后半，否定式）、`The children were exposed to cold weather without coats.`（卡 ex，主语是挨冻的孩子） | 一处是"别这样干"、一处是"已经这样了" | 本书给它的两处都不痛快：一处被 never 拦住、一处是孩子挨冻 |
| 谁动的手、什么时候 | reveal | `The team would not disclose plans early but would reveal them at the fair.`（1388，主语是团队自己） | 说话的人就是藏着东西的那一方，且挑好日子 | 三词里只有它带着"到某个时候才说"的时间位（early 对 at the fair） |
| 谁动的手、什么时候 | uncover | `They hoped judges might uncover bright ideas`（1389，主语是评委、前面挂着 might） | 动手的是外人，能不能翻出来句子里没定 | 它是三词里唯一"找的人不是自己"的一处：亮点由评委去翻，团队只能在旁边盼 |
| 句子里的框架 | expose | 卡 `ex` 与三条词伙一律 `be exposed to`（被动 + to）；本书课文那处（1389）却是主动 + 直接宾语 | 说谁被摊在什么东西跟前 | 它是本组唯一有"被动 + to"框架的词，卡上那 4 条文本（1 句 ex + 3 条词伙）全走这个骨架，课文那 1 处反而不走 |
| 句子里的框架 | reveal | `reveal them at the fair`（1388，动词 + 代词 + 地点）、`reveal facts`（词伙，动词 + 复数名词） | 动词后面直接摊东西 | 本书这几处它后面从不出现 to：说出口的东西紧跟在动词后 |
| 句子里的框架 | uncover | `uncover bright ideas`（1389，动词 + 复数名词）、`uncovered the truth`（卡 ex，动词 + the truth） | 动词后面直接摊东西 | 它与 reveal 同框架，本组的区别不在句法而在上面两行（宾语好坏、谁动手） |
| 卡上圈子里有谁 | expose | 「同义词：disclose, display」＋词伙三条 | 站在 disclose 一堆里 | 它这一栏**没列 reveal、也没列 uncover** —— 整组 `all_synonyms: false` 就卡在这一栏上 |
| 卡上圈子里有谁 | reveal | 「同义词：disclose, display, expose」＋词伙 `reveal facts` | 栏里点名了本组的 expose | 它单向认 expose；expose 没认回来（上一行） |
| 卡上圈子里有谁 | uncover | 「同义词：disclose, display, expose」＋词伙 `uncover bright ideas` | 与 reveal 卡同一组名单 | reveal 与 uncover 两栏字面完全一样（disclose, display, expose），差别只在词伙那条 |
| 中文落点 | 三个词 | reveal：1388「要在集市上揭晓」；uncover：1389「希望评委发现亮点」；expose：1389「绝不冷漠地暴露短处」（`sentZh`） | 汉译英时先问中文给的是"说出口""找出来"还是"摊在眼前" | 本书译文三处给了三个不同的中文（揭晓／发现／暴露），共同义项那两个字一次都没用上；另有一处「暴露」在 1432，英文用的却是 showed |

### 总结句（实测宽度 26.0 / ≤30）

> reveal 是当众说，uncover 是自己找，expose 是摊短处。

### 出处清单

- `The team would not disclose plans early but would reveal them at the fair`、`reveal them at the fair`、`at the fair`、译文「团队不提前透露计划，要在集市上揭晓」 —— 课文第 1388 句（章4 段18 第 4 句）及 `sentZh`。本组 reveal 的唯一课文书证。
- `They hoped judges might uncover bright ideas and never expose weak points coldly`、`uncover bright ideas`、`bright ideas`、`expose weak points`、`weak points`、`never expose weak points coldly`、译文「他们希望评委发现亮点，绝不冷漠地暴露短处」 —— 课文第 1389 句（章4 段18 第 5 句）及 `sentZh`。uncover 与 expose 各在本句取到唯一一处书证（**同一句、两格，各算一侧**）。
- `The letter revealed his secret.` / 这封信揭露了他的秘密。 —— reveal 卡 `ex` / `exZh`（表里写作 `revealed his secret`，为其连续子串）。
- `reveal facts` —— reveal 卡 `note` 词伙段（本书课文 0 处，已 grep）。
- `The detective uncovered the truth.` / 侦探揭开了真相。 —— uncover 卡 `ex` / `exZh`（表里写作 `uncovered the truth`，为其连续子串）。
- `uncover bright ideas` —— uncover 卡 `note` 词伙段，与课文 1389 **同源**（按口径 8 合并算 1 条）。
- `The children were exposed to cold weather without coats.` / 孩子们在没有外套的情况下遭受了寒冷天气。 —— expose 卡 `ex` / `exZh`（表里写作 `were exposed to cold weather`，为其连续子串）。
- `be exposed to distractions`、`be exposed to violent images`、`be exposed to domestic violence` —— expose 卡 `note` 词伙段三条（本书课文 0 处，已 grep；三串互不相同、不与课文同源，各算一条卡上文本）。
- 「同义词：disclose, display, expose」（reveal、uncover 两卡）／「同义词：disclose, display」（expose 卡） —— 三卡 `note` 同义词栏。已逐个 grep `vocab.json`：`disclose` 有卡（词伙 `disclose plans` 与课文 1388 同源；它的同义词栏是 expose、reveal、divulge），**display 与 divulge 都没有词头卡** —— 按硬约束 2 都不进成员表。
- `One small mistake showed a thin flaw in the lower left seam`、译文「小失误暴露了左下接缝的细裂纹」 —— 课文第 1432 句（章4 段26 第 1 句）；本组只为说明"本书另一处「暴露」的英文是 showed"，show 不是本组成员、不进表。
- 义项「使遭受；暴露；揭露」「揭露；泄露；显露」「揭开；揭露」 —— 三卡 `m`（只待在 `sense` 槽）；「小镇理工学院请机修工带学生为清水街道创新，立志参赛」—— 段 18 `paraZh`；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡；共同义项名不副实（口径 6 型），按既有口径自处

- **义项核对（§五.15，已逐条 grep）**：worklist 的共同义项「揭露」在 `shadow/data/sections.json` 全文 **0 处**（`sentZh`、`paraZh` 都扫过）；本书给这三处的译文分别是「揭晓」「发现」「暴露」。三张卡 `m` 里都有「揭露」二字，说明它是卡上的义项、不是课文里的用法 → 按口径 6 处理：标题改写成"这一段三个词各领哪样东西"、表头**引用并当场否掉**（§五.11 放行形状），「揭露」只留在 `sense` 槽，不进默认行 / `core` / 总结句。**这不是"本书没有这个词"**——三个词各有一处课文书证，上表逐条给了。
- **另一档**：expose 卡 `m` 第一档「使遭受」的课文位置 0 处（本书唯一课文处 1389 走的是"摊开短处"），该档的书证在卡 `ex`（孩子挨冻那句，`exZh` 正是「遭受了寒冷天气」）；reveal `m` 第三档「显露」本书课文 0 处（1541 那处译「显露偏见」的英文是 shows，不是 reveal，已核对）。
- **同源合并（口径 8）**：uncover 卡词伙 `uncover bright ideas` 就是课文 1389（算 1 条）→ uncover 侧独立文本 **2 条**（课文 1 + 卡 ex 1），是本切片最薄的那一档（组 2 的 calculate／compute、组 3 的 convert、组 4 三侧同为 2 条）；reveal 侧 **3 条**（课文 1388 + 卡 ex + 词伙 1，词伙课文 0 处）；expose 侧 **5 条**（课文 1389 + 卡 ex + 卡词伙 3，三条词伙课文 0 处、互不同源）。按口径 7：薄是常态，照写不编。
- **`all_synonyms: false` 有据**：reveal、uncover 两栏都列了 expose，expose 那栏只列 disclose、display —— 单向，不可能两两互列 → false 不是"三词不同义"，1389 一句里 uncover 与 expose 并排就是反证。
- **计数结论、不是禁令**："expose 的宾语全是要躲的东西"到本书这 5 条为止（样本小），落地措辞停在"本书给它的这几处"，不许升级成"expose 只能接坏事"。
- **不写的**：expose 的新闻体"揭露丑闻"、reveal 的名词用法（卡 `m` 只给 v. 一档，本书也没有 the reveal）、uncover 的字面"掀开盖子"、`reveal itself`、`expose somebody to danger`、`uncover a plot` 这类两份数据里 0 处的写法一律不列。
- 无需拍板：口径 6 的处置是定死的，本组按它写完并在上面把档位证据摆清；三侧都有课文书证，不构成退回。

---

## 组2 · calculate / compute / count —— 计算 ｜ `all_synonyms`: false ｜ 段 [4,20] ｜ 句 1398 / 1399（三成员）

**共现位置**：1 段 2 句 —— 第 5 章（章索引 4）段 20（全局 1396–1401），成员句 1398（**calculate 与 compute 挤在同一句，由 `and` 并列**：前半省下的水滴、后半纸上的费用）与 1399（count 与 estimate 并列，count 在前半）。**主锚点 = 章4 段20**（`paraZh`：工具量具齐备，学生测量计算，请老师评估改进。`subheads[4][18]`：下篇·发明比赛）。卡挂段 20 末。
主锚点 = 章4 段20

**口径 9 报备（同段已挂四张表）**：段 20 段末现有 instrument／tool、compute／estimate、estimate／evaluate、assess／gauge 四张已落地的表。本组**不重复**它们取过的角（compute／estimate 那张走的是"已经花掉的账 vs 还没到手的省头"）；本组只走一条新轴：**能不能一件一件数出来**——顺带把 calculate 与 compute 在本书里其实分不开这件事写清。同句的 estimate 归那张表，不进本组成员。

**这一段在讲什么**：1396 说锋利的仪器和手工具都锁在木箱里，1397 说那把黄铜量规帮学生每天稳稳地测浑水，1398 说他们晚饭后在纸上算省下的水滴和费用，1399 说他们能一只只数清空的罐子、再对整条街的省头给个准头，1400 说老师来评估作品、带着微笑看图纸，1401 说有个备用配件白送、还会留下一样能拌进花园土的副产品（全由本段句子本身撑）。

**该段原文（章4 段20）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1396 | Each sharp instrument and hand tool stayed in a locked wooden box. | 每件锋利的仪器和手工具都放在上锁的木箱里。 |
| 1397 | A brass gauge helped pupils measure muddy water with steady hands daily. | 那把黄铜量规，帮学生们每天都稳稳地测那些浑水。 |
| 1398 | They would **calculate** drops saved and **compute** costs on paper after supper. | 他们晚饭后在纸上计算省下的水滴和费用。 |
| 1399 | They could **count** jars emptied and estimate savings for the whole street. | 他们数清空的罐子，估算整条街的节水量。 |
| 1400 | Teachers came to assess the build and evaluate drawings with kind smiles. | 老师们来评估作品，带着微笑评价图纸。 |
| 1401 | A spare accessory came free, with a clean byproduct for garden soil. | 有个备用的配件是白送的，还会留下一样干净的副产品，可以拌进花园的土里。 |

按 §五.13 两档分开：**calculate** 带标记 1 处（1398）＋ 未打标记 0 处；**compute** 带标记 1 处（1398）＋ 未打标记 0 处；**count** 带标记 2 处（1089、1399）＋ **未打标记另有 11 处**（253、285、643、778、856、905、909、1118、1183、1189、1632，全书 1833 句按 count/counts/counted/counting 扫，共 13 句）。count 是本组唯一的厚侧；它另被 cashier 卡 `ex` 用掉 1 处（已 grep 各卡 `ex`/`exZh`/`note`/`m` 栏，compute 在两卡之外的栏位 0 处引用）。

### 表头一句（title）

> 1398 一句里两个动词各领一样东西：calculate 接的是省下的水滴、compute 接的是晚饭后纸上的费用，1399 的 count 接的是一只一只清空的罐子——worklist 给的共同义项在本书译文里被并成了一个词（1398 两个动词共译「计算」，已 grep 全书 `sentZh`「计算」只这一处），三个词真正的分工不在"算"这个字，而在**对象能不能一件一件对上号**：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| calculate | /ˈkælkjuleɪt/ | v. | 计算；推测 | 动笔算出来的那个量 | 1398 晚饭后纸上算的省下的水滴；卡上那笔被很快算出的总成本；卡上还牵着 assess、measure 那一堆"量"词 | `calculate drops saved`（课文 1398） |
| compute | /kəmˈpjuːt/ | v. | 估算；计算 | 纸上那笔账 | 1398 同句后半那笔费用（on paper after supper）；卡上那笔被很快算出的总账 | `compute costs on paper`（课文 1398） |
| count | /kaʊnt/ | v.；n. | 计算；看作；n. 计数 | 一件一件对上号 | 1399 一只只清空的罐子；1089 父亲在尘土上画线数的受访者；卡上桌上那堆苹果；cashier 卡里店员数的那把钱；253 那句"每个小成果都算数" | `count jars emptied`（课文 1399） |

### 默认那一行（实测宽度 38.5 / ≤40）

```
calculate = 水滴（calculate drops），compute = 纸上的账，count = 数清空的罐子
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 对象能不能一件件点出来 | count | `count jars emptied`（1399）、`to count each survey respondent`（1089，父亲在尘土上画线）、`counts the apples on the table`（卡 ex）、`counted the money`（cashier 卡 ex）、`counted every shilling`（1189） | 数离散个件：罐子、受访者、苹果、一先令一枚 | 本书 13 处 count 里有 **8 处**宾语前挂着 each / every（285、643、778、856、1089、1118、1189、1632），另外几处也全是能指着点的东西 |
| 对象能不能一件件点出来 | calculate | `calculate drops saved`（1398；卡词伙 `calculate drops` 与之同源）、`calculates the total cost`（卡 ex） | 算一个总量：水滴合计、总成本 | 它接的 drops、the total cost 都是**合起来的量**，本书这几处一处 each/every 都没有 |
| 对象能不能一件件点出来 | compute | `compute costs on paper`（1398；卡词伙 `compute costs` 与之同源）、`computes the total cost`（卡 ex） | 算一笔账，且本书明写了在纸上 | 它的两处宾语（costs / the total cost）全是钱；"纸上"这个状语是本组唯一写明动笔的一处 |
| calculate 与 compute 分得开吗 | calculate | 卡 `ex` `He calculates the total cost quickly.` | 与 compute 卡 ex 只差词头 | **本书给不出这条区别**：两卡 `ex` 句子完全相同（只换动词头）、1398 译文把两词并成一个词、两词各只有 1 处课文书证——这一格是缺证，不硬造 |
| calculate 与 compute 分得开吗 | compute | 卡 `ex` `He computes the total cost quickly.`、词伙 `compute costs` | 同上 | 能写出来的差别只剩栏位：calculate 有同义词栏（assess、measure），compute 没有；且 compute 在两卡之外的栏位 0 处被引用（已 grep），calculate 被 measure、estimate、assess 三张卡点名 |
| 卡上另一档用上了吗 | calculate | 卡 `m` 第二档「推测」——本书 0 处（已 grep：calculate 词形全书只 1398 一处） | 说"我寻思他会来" | 这一档只有卡上有、课文没有，表里不写它的搭配 |
| 卡上另一档用上了吗 | compute | 卡 `m` 第一档「估算」——本书两处译文都没落成「估」（1398 译「计算」、卡 `exZh` 译「算出」；`sentZh` 里「估算」二字全书只跟着 1399 的 estimate） | 想说"先给个准头"它最容易被选中 | 它的名义首档与本书实译差一档，所以 `core` 里不写那个"估"字（同段 compute／estimate 那张表已记过一次，本组不重复展开） |
| 卡上另一档用上了吗 | count | 卡 `m`「看作」一档本书 0 处（已 grep count as / counted as 均 0）；n.「计数」一档 0 处（课文 13 处全是动词位）；卡 `note` **整栏为空**（既无同义词也无词伙） | 说"把他当自己人"、说"一个计数" | 按 §五.1 明说：count 侧的搭配证据全来自课文 13 处与卡 `ex`，卡上给不出词伙；它的两档中文本书一次都没用过 |
| 本组里谁还能不带宾语 | count | `Every small outcome counts, and kind acts create a real impact`（253，译「每个小成果都算数」）、`regular turnover was counted on Sundays`（1183，译「定期在周日结算营业额」） | 不带东西、只说"这算不算数"；以及结账 | 只有 count 在本书被这样用（253 后面什么都不接）；calculate / compute 两处书证都必带宾语 |
| 本组里谁还能不带宾语 | calculate | 本书 2 条文本（1398、卡 ex）全部动词 + 宾语 | —— | 这一格只有 count 有货，落地时别写成"另两个不能不带宾语"，只能说"本书没给过" |
| 中文落点 | 三个词 | calculate：1398「计算」／卡 `exZh`「快速计算出总成本」；compute：1398「计算」／卡 `exZh`「很快算出总成本」；count：1399「数」／卡 `exZh`「计算桌子上的苹果数量」（`sentZh`、`exZh`） | 汉译英时先问中文是"数一数"还是"算一算" | 本书译文在 1398 把两词并成一个词、在 1399 给 count 换了个"数"字；反倒在 count 的卡 `exZh` 上又用回「计算」——中文这一格自己也不齐 |

### 总结句（实测宽度 23.5 / ≤30）

> count 数得清个数，calculate 和 compute 动笔算。

### 出处清单

- `They would calculate drops saved and compute costs on paper after supper`、`calculate drops saved`、`compute costs on paper`、`drops saved`、`on paper after supper`、译文「他们晚饭后在纸上计算省下的水滴和费用」 —— 课文第 1398 句（章4 段20 第 2 句）及 `sentZh`。**calculate 与 compute 的唯一课文书证，同句两格、各算一侧。**
- `They could count jars emptied and estimate savings for the whole street`、`count jars emptied`、译文「他们数清空的罐子」 —— 课文第 1399 句（章4 段20 第 3 句）及 `sentZh`。同句的 estimate 归段末 compute／estimate 那张表，不进本组。
- `Uncle sang an old song while Father drew lines in the dust to count each survey respondent`、`to count each survey respondent` —— 课文第 1089 句（章3 段34 第 1 句；本组 count 的第 2 处带标记书证）及 `sentZh`「父亲在尘土上画线，逐个数着调查里的受访者」。
- `Every small outcome counts, and kind acts create a real impact` —— 课文第 253 句（章0 段43）；`They counted each buzzing insect near the flowers and moved a pink worm off the path`（285）、`We kept a steady pace and counted each small step with care`（643）、`Grandma wiped one stone pillar after another, counting each tall column aloud`（778）、`Grandma counted each fresh ingredient, adding a herbal health supplement for the elders`（856）、`Later a long freight train stood in a yard, and we sat there counting its cars`（905）、`Father read our itinerary out loud in the shop, counting the trains we still had to take`（909）、`Strange how quiet a morning ride home stays while Father counts every way strangers assist us`（1118）、`Every transaction was written down and regular turnover was counted on Sundays`（1183）、`He counted every shilling carefully and polished the old copper jars for decoration`（1189）、`Then the doctor pressed her soft abdomen and counted each lower rib one by one`（1632） —— count 未打标记的 11 处（§五.13 单列）。
- `He calculates the total cost quickly.` / 他快速计算出总成本。 —— calculate 卡 `ex` / `exZh`（表里写作 `calculates the total cost`，为其连续子串）。
- `He computes the total cost quickly.` / 他很快算出总成本。 —— compute 卡 `ex` / `exZh`（表里写作 `computes the total cost`，为其连续子串）。**与上一句只差词头**（见数据边界 §五.14 条）。
- `He counts the apples on the table.` / 他计算桌子上的苹果数量。 —— count 卡 `ex` / `exZh`（表里写作 `counts the apples on the table`，为其连续子串）。
- `calculate drops` —— calculate 卡 `note` 词伙段（与课文 1398 **同源**，按口径 8 算 1 条）；`compute costs` —— compute 卡 `note` 词伙段（同样与 1398 **同源**，算 1 条）；count 卡 `note` 整栏为空，无词伙可引。
- 「同义词：assess, measure」（calculate 卡）／compute 卡只有词伙段、无同义词栏／count 卡 `note` 整栏为空 —— 三栏不可能互列 → worklist 标 `all_synonyms: false` 有据。另 measure、estimate、assess 三张卡的同义词栏各回列 calculate（跨卡单向点名，已 grep），本组不把它们拉进成员表（硬约束 2）。
- `The cashier counted the money at the end of the day.` / 收银员在一天结束时数了钱。 —— cashier 卡 `ex`（表里写作 `counted the money`，为其连续子串）：count 侧"别的卡在用它"的 1 条独立文本。
- 义项「计算；推测」「估算；计算」「计算；看作；n. 计数」 —— 三卡 `m`（只待在 `sense` 槽）；「工具量具齐备，学生测量计算，请老师评估改进」—— 段 20 `paraZh`（本段主题栏与 1398 那句对得上，本组仍只按句子本身写）；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡；calculate ↔ compute 那一格是缺证，硬区别只有一条

- **本组唯一站得住的硬轴 = 对象能不能一件件点出来**：count 侧全书 13 处（8 处宾语前挂 each / every）对 calculate / compute 侧各 2 条文本（合起来的量）。这一条可 grep、可落地。
- **calculate 与 compute 谁跟谁都没有本书能给的区别**（明写缺证）：两卡 `ex` 只差词头、1398 一处课文里两词并列而译文并成一个词、两词各只有 1 处课文书证。表里那一行只报备"分不开"，**不**用词源、语域、"compute 更书面"之类补洞（三卡 `note` 都没有语域标注，本书给不出证据）。
- **§五.14 形状，三件齐**（calculate ↔ compute）：① 两卡例句只差词头这一事实照写，是本书最强的"可换"物证；② 两栏没互列（compute 卡无同义词栏、calculate 栏只列 assess/measure）故 `all_synonyms: false` 有据；③ 同形两句**各算一侧书证**，不并成一条。不再提请拍板。
- **同源合并（口径 8）**：`calculate drops` = 课文 1398、`compute costs` = 课文 1398，两条词伙都不另计 → calculate 侧独立文本 2 条（课文 + 卡 ex）、compute 侧 2 条（课文 + 卡 ex）、count 侧 15 条（课文 13 + 卡 ex + cashier 卡 ex）。
- **compute 这张卡已经带着一份表（报备，不改内容）**：已 grep `shadow/data/vocab.json`，全书 181 张 `cmp` 里 compute 名下挂着批次 5 落地的 compute／estimate（挂段 [4,20]）。一个词头只能带一张表，本组落地时载体大概率要换到 `calculate` 或 `count`（`count` 名下现无 `cmp`，`calculate` 名下也没有）。本组成员仍按切片给的 `words` 写，未为躲载体改内容。
- **不写的**：「calculate 偏人工／compute 偏机器」（两卡 `note` 都没这样标，本书也不写计算机语境）、`calculate the area`、`compute the difference`、`count on somebody`、`a head count` 这类两份数据 0 处的写法一律不列；count 卡 `m` 的「看作」「计数」两档本书 0 处，只在此报备、不造句。
- 无需拍板：共同义项「计算」在 1398 的译文里对两个动词都成立、1399 的 count 由译文换成「数」——这一落点差异已写进表里"中文落点"行；薄的一侧（calculate、compute 各 2 条）按口径 7 如实写。

---

## 组3 · convert / transform / turn —— 转变 ｜ `all_synonyms`: false ｜ 段 [4,25] ｜ 句 1426 / 1428（三成员）

**共现位置**：1 段 2 句 —— 第 5 章（章索引 4）段 25（全局 1426–1431），成员句 1426（**transform 与 convert 挤在同一句，由 `and` 并列、宾语还是同一桶沟水**）与 1428（turn，隔一句、同一伙人）。**主锚点 = 章4 段25**（`paraZh`：调水调阀找滴水，读数变准，节水见效。`subheads[4][18]`：下篇·发明比赛）。卡挂段 25 末。
主锚点 = 章4 段25

**这一段在讲什么**：1426 说他们能把沟里的水对付过来、引去浇花坛，1427 说他们没动屋顶那道水道、只把罐子挪了挪去接雨，1428 说黄昏时全组去拧阀门、给口渴的根调水，1429 说他们调小螺丝、用粉笔标出滴水点，1430 说三天平静的试验之后读数又准又精，1431 说每个装对的配件都省下水、每处改好的错误都教人细心（全由本段句子本身撑）。

**该段原文（章4 段25）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1426 | They could **transform** ditch water and **convert** it for flower beds. | 他们能转化沟水，供花坛使用。 |
| 1427 | They did not alter the roof path but did shift jars to gather rain. | 他们没改屋顶水道，只挪罐子收集雨水。 |
| 1428 | At dusk the crew would **turn** valves and adapt flows for thirsty roots. | 黄昏时全组转动阀门，为口渴的根调水。 |
| 1429 | They would adjust small screws and pinpoint drips with chalk marks. | 他们调小螺丝，用粉笔标出滴水点。 |
| 1430 | The readings grew accurate and precise after three calm trial days. | 三天平静试验后读数又准又精。 |
| 1431 | Each correct fitting saved water, and each fixed error taught care. | 每个正确的配件都节水，每处改好的错误都教人细心。 |

按 §五.13 两档分开：**transform** 带标记 1 处（1426）＋ 未打标记 0 处；**convert** 带标记 1 处（1426）＋ 未打标记 0 处；**turn** 带标记 1 处（1428）＋ **未打标记另有 19 处**（24、73、103、117、402、428、892、912、928、964、985、1024、1074、1103、1379、1611、1743、1808、1813；按 turn/turns/turned/turning 扫全书 1833 句共 20 句，turnip、turnover 一类另词头已排除）。turn 侧另被 knob、blade、bend 三张卡的 `ex` 各用掉 1 处（已 grep 各卡栏位；convert、transform 在两卡之外 0 处被引用）。

### 表头一句（title）

> 1426 一句里两个词在弄同一桶沟水（transform 在前、convert 接一个 it），1428 的 turn 干的却是另一桩活：它拧的是阀门，拧完阀门还是阀门——worklist 给三词的那个共同义项，本书译文一次都没用过（已 grep `sections.json` 全文：「转变」「转换」「变换」各 0 处），turn 这一处走的更不是这一档；它这一档的本书证据在别段（未打标记的 5 处 `turned … into`），本段这条明写缺证：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| convert | /kənˈvɜːrt/ | v. | 转换；转变；变换 | 给它改个用途 | 1426 那桶沟水被 it 接住、引去花坛；卡上旧纸张改做成的新本子 | `convert it for flower beds`（课文 1426） |
| transform | /trænsˈfɔːrm/ | v. | 转变 | 整个换了副面孔 | 1426 前半被对付的沟水；卡上新老师接手之后那个活起来的班；卡上整条天际线 | `transform ditch water`（课文 1426） |
| turn | /tɜːrn/ | v.；n. | 转动；转变；n. 轮到的机会 | 本段那一下是拧 | 1428 黄昏全组去拧的那批阀门；卡上离开前要关掉的那盏灯；892／1074／1743／1808／1813 五处 `turned … into`（未打标记）；24 那句轮流睡觉 | `turn valves`（课文 1428） |

### 默认那一行（实测宽度 38.0 / ≤40）

```
transform = 全班换模样，convert = 引到花坛（for flower beds），turn = 拧阀门
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段它在弄什么 | transform | `transform ditch water`（1426 前半，直接带宾语）、`transformed the class into a lively group`（卡 ex）、`transform skylines`（卡词伙，本书课文 0 处，已 grep） | 说一大片东西整个换了副面孔 | 本段它后面只挂着一个 ditch water，改完是什么句中没说；卡上那两处才说（一个班活起来、整条天际线） |
| 本段它在弄什么 | convert | `convert it for flower beds`（1426 后半，it 回指前半的沟水）、`converts old paper into new notebooks`（卡 ex） | 说一样东西被安排去干别的 | 它是本组唯一在课文里带着"去做什么"的一处：for flower beds——水还是水，去处变了 |
| 本段它在弄什么 | turn | `turn valves`（1428；同句还有 adapt flows）、`turned the knob to adjust the volume`（knob 卡 ex）、`The wind turns the blade of the fan.`（blade 卡 ex） | 手上那一下：拧、扳、拨 | 本段它根本没换任何东西：阀门拧完还是阀门，变的只是水的流量（同句的 adapt flows） |
| 换样那一格靠什么框架 | transform | 本书课文 0 处带 into（1426 只到宾语就收）；卡 ex 才带 `into a lively group` | 写"变成什么"时靠卡上那种 into | 课文里它这一格是空的：本组这一格只有卡上 1 句撑 |
| 换样那一格靠什么框架 | convert | 课文 `convert it for flower beds`（for 接去处）；卡 ex `into new notebooks`（into 接成品） | 接 for 说用途、接 into 说成品 | 它两个框架各 1 处、一处课文一处卡：本组唯一"用途与成品都写过"的一侧 |
| 换样那一格靠什么框架 | turn | `turned it into a talk about city traffic`（892）、`turned the old slum school into a story I still remember`（1074）、`turning mundane grey afternoons into games for kids`（1743）、`slothful afternoons slowly turned into brisk walks`（1808）、`turning care into a glad anniversary of new life`（1813） | 说一样东西变成另一样 | **本书真正在"换样"这一格上反复用的是 turn + into（5 处，全未打标记）**，比 convert、transform 两侧加起来都多；只是本段那处不是它 |
| 场面大小 | transform | `transformed the class`（卡 ex，一整个班）、`transform skylines`（卡词伙，一整条天际线） | 大场面：整个班、整片楼线 | 课文那处只是一桶沟水；卡上两处才是"一群／一片"（整个班、整条天际线），本组再没有第二处这么大的对象 |
| 场面大小 | convert | `converts old paper into new notebooks`（卡 ex）、`convert it for flower beds`（1426） | 材料与水的改道 | 它两处都是具体的物料（纸、沟水），而且都给了去处 |
| 场面大小 | turn | `Night patrols turned to friendly guards watching sleepy village lanes`（1379）、`until scores turned to cold decimals`（428）、`The small room turned uncomfortably humid as wet clothing dried by the heater`（73）、`the bank turns soft and muddy`（103） | 抽象的东西在变（夜巡、分数、房间、河岸） | 本组里只有它的"换样"能落在看不见的东西上：懒午后变成快走、看病变成周年（1808、1813），别两个词本书都没给过这种宾语 |
| 卡上栏位与另一档 | convert | 卡 `note` **整栏为空**（既无同义词也无词伙），卡上文本只有 `ex` 一句 | —— | 按 §五.1 明说：convert 侧的搭配证据只有课文 1 处 + 卡 ex 1 句，卡上给不出词伙；这是本切片最薄的一档（2 条） |
| 卡上栏位与另一档 | transform | 卡 `note` 只有词伙段一条 `transform skylines`，无同义词栏 | 说一座城的外形被改了 | 这条词伙本书课文 0 处（已 grep），只作卡上现成块列出，不算课文书证 |
| 卡上栏位与另一档 | turn | 卡 `note` **整栏为空**；卡 `ex` `turn off the light`（译「关掉灯」）——`m` 三档里没有单列"关"这一档；`m` 的 n. 一档「轮到的机会」本书在 24（`took turns sleeping`，未打标记）用过 | 关灯、轮流做同一件事 | 它是本组唯一"卡上义项与卡上例句对不齐"的一处；也是唯一名词档在课文里真出现过的（24，未标） |
| 中文落点 | 三个词 | 1426「他们能转化沟水，供花坛使用」（一个「转化」对前半的 transform，后半的 convert 只落到「供……使用」，中文里没再出现"转"字）；1428「转动阀门」；892「索性把它说成」、1074「讲成」、1743／1808／1813「变成」（`sentZh`） | 汉译英时先问中文是"改用途"、"整个换副面孔"还是"变成另一样" | 本书译文在 1426 一处都没把两个英文动词分开译；「转变」「转换」「变换」三个词全书 0 处（已 grep `sections.json` 全文），1426 那唯一一处「转化」同时管着两个动词 |

### 总结句（实测宽度 26.5 / ≤30）

> transform 换模样，convert 改用途，turn 本段在拧阀门。

### 出处清单

- `They could transform ditch water and convert it for flower beds`、`transform ditch water`、`convert it for flower beds`、`for flower beds`、译文「他们能转化沟水，供花坛使用」 —— 课文第 1426 句（章4 段25 第 0 句）及 `sentZh`。**transform 与 convert 的唯一课文书证，同句两格、各算一侧。**
- `At dusk the crew would turn valves and adapt flows for thirsty roots`、`turn valves`、译文「黄昏时全组转动阀门，为口渴的根调水」 —— 课文第 1428 句（章4 段25 第 2 句）及 `sentZh`。turn 全书唯一带标记的一处，走的不是 worklist 那一档。
- `He converts old paper into new notebooks.` / 他把旧纸张转换成新笔记本。 —— convert 卡 `ex` / `exZh`（表里写作 `converts old paper into new notebooks`，为其连续子串）。卡 `note` 整栏为空，无词伙段可引。
- `The new teacher transformed the class into a lively group.` / 这位新老师把班级变成了一个充满活力的团体。 —— transform 卡 `ex` / `exZh`（表里写作 `transformed the class into a lively group`、`transformed the class`，均为其连续子串）。
- `transform skylines` —— transform 卡 `note` 词伙段（本书课文 0 处，已 grep）。
- `Please turn off the light before you leave.` / 请在你离开前关掉灯。 —— turn 卡 `ex` / `exZh`（表里写作 `turn off the light`，为其连续子串）。卡 `note` 整栏为空。
- turn 未打标记的 5 处"换样 + into"：`The host went on about votes, and Father turned it into a talk about city traffic`（892）、`Not far from home, Father turned the old slum school into a story I still remember`（1074）、`His paper birds looked delicate and bright, turning mundane grey afternoons into games for kids`（1743）、`Loud horns felt obtrusive near the clinic, while slothful afternoons slowly turned into brisk walks`（1808）、`Lin's yearly check-up matched his birthday week, turning care into a glad anniversary of new life`（1813）。
- turn 未打标记的"换样 + to" 3 处：`No one knew the full extent of exhaustion until scores turned to cold decimals`（428）、`Night patrols turned to friendly guards watching sleepy village lanes`（1379）、`In the second country we had just reached, our talk under the lamps turned to a company and its president`（1024）。
- turn 其余未打标记的实物处：`The team moved their tents to open ground at a low altitude and took turns sleeping`（24，名词档）、`The small room turned uncomfortably humid as wet clothing dried by the heater`（73）、`After storms the bank turns soft and muddy, so she moulds the damp clay into small bricks to show her class`（103）、`Then a dramatic change in the wind turned the sky from blue to black over the silent wilderness`（117）、`After a quick sketch of his room, he turned the music volume down and reviewed a hard new subject about city rivers`（402）、`We turned down a narrow lane where two dogs slept in the sun`（912）、`Over tea one morning, Father pointed at a pedicab crossing the yard gate and waved until it turned the corner`（928）、`A propeller on the screen turned so slowly that I watched it for minutes`（964）、`On the train Father worked out the money due for our tickets, then turned back to the rivers beyond`（985）、`When the ride home turned quiet again, Father put his whole viewpoint into three short lines`（1103）、`At last Leo turns sideways to smile at the moon and then sleeps with hope`（1611）。
- `He turned the knob to adjust the volume.`（knob 卡 ex）、`The wind turns the blade of the fan.`（blade 卡 ex）、`The car turned at the bend in the road.`（bend 卡 ex） —— turn 侧"别的卡在用它"的 3 条独立文本（三处都是手上／街上那一下，没有一处是"换样"）。
- 「同义词栏」核对（§五.15，已 grep）：convert 卡 `note` 整栏为空、turn 卡 `note` 整栏为空、transform 卡只有词伙段无同义词栏 → 三栏不可能互列 → worklist 标 `all_synonyms: false` 有据；不是"三词不同义"，1426 同句并列就是反证。
- 义项「转换；转变；变换」「转变」「转动；转变；n. 轮到的机会」 —— 三卡 `m`（只待在 `sense` 槽）；「调水调阀找滴水，读数变准，节水见效」—— 段 25 `paraZh`；「下篇·发明比赛」—— `subheads[4][18]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡；本组只有两格站得住，turn 那一档按口径 6 自处

- **口径 6 的"档位错"型（照写、把话说轻）**：worklist 的共同义项取自三卡 `m` 的交集，本段两处站得住（1426 前半、后半），第三处（1428 的 turn）不在这个档上——它拧的是阀门。**这一档 turn 的书证在别段**（未打标记 5 处 turned … into ＋ 3 处 turned … to，已逐条列在出处清单），本组不许它升级成"turn 没有这一档"，也不许把本段那处说成这一档。共同义项原词只留 `sense` 槽，标题按 §五.11 引用＋当场否掉。
- **同段还有 alter / shift（1427）在改东西**（没改屋顶水道、只挪罐子），它们不是本组成员（硬约束 2），只在"这一段在讲什么"里当背景，不进表。
- **本组站得住的只有两格**：① 1426 同句并列、宾语同一桶水（transform / convert 是本书给的最强"可换"物证，译文只用了一个「转化」管两处英文动词）；② 真正在"换样"这一格上被反复使用的是 turn + into（5 处），不是 convert / transform（各 1 处课文）。**"transform 比 convert 改动更大"这一条只在卡上两句例句里成立**（一个班、一条天际线 vs 一叠纸、一桶水），课文里没有第二处能撑它 —— 表里那行"场面大小"就是按这个边界写的，不许落地成通用规则。
- **同源合并（口径 8）**：transform 侧 3 条（课文 1426 + 卡 ex + 词伙 `transform skylines`，词伙课文 0 处）、convert 侧 2 条（课文 1426 + 卡 ex，卡无栏位）、turn 侧 24 条（课文 20：标 1 + 未标 19；卡 ex 1；knob／blade／bend 三卡 ex 3）。三侧厚薄差一个量级，按口径 7 如实写：convert 是本切片最薄的一档（2 条，与组 1 uncover、组 4 三侧同档），turn 是本切片最厚的一侧（但厚在全是未打标记的别的档）。
- **不写的**：「convert 只用于用途／格式、transform 只用于外观」（三卡 `note` 都无语域标注，本书给不出这种二分）、`turn into a habit`、`transform into`（课文 0 处）、`conversion`、`transformation`（两个名词形本书 0 处，也没卡）、`turn round`、`turnip`／`turnover`（另词头，不算 turn 的书证）一律不列。
- 无需拍板：口径 6 已定死处置；三侧各有课文书证，不构成退回。

---

## 组4 · desire / itch / yearn —— 渴望 ｜ `all_synonyms`: false ｜ 段 [5,11] ｜ 句 1560 / 1561 / 1562（三成员）

**共现位置**：1 段 3 句 —— 第 6 章（章索引 5）段 11（全局 1558–1563），三个成员**各占紧挨着的一句**：1560（desire，同句还有 aspire）、1561（yearn）、1562（itch，前面挂着 inner、后面跟着 drives）。**主锚点 = 章5 段11**（`paraZh`：利奥立志高远，用计划安慰家人。`subheads[5][0]`：上篇·备考的一天）。卡挂段 11 末。
主锚点 = 章5 段11

**这一段在讲什么**：1558 说利奥要承担额外的听力训练、训练里规定了每天的时间上限，1559 说好成绩能说服爸爸、安慰家里担心的人，1560 说他学着把志向放高、管住对游戏和手机的那股冲动，1561 说他像是朝着广博知识去的、还常抬出过去的伟大例子，1562 说心里那份对进步的坐不住推着他一次次去做难的听力任务，1563 说他每晚努力、慢慢完成每周计划（全由本段句子本身撑）。

**该段原文（章5 段11）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1558 | Leo will undertake extra listening drills that clearly stipulate daily time limits. | 利奥会承担额外的听力训练，训练明确规定每日时限。 |
| 1559 | Good scores can convince his father and reassure his worried family at home. | 好成绩能说服爸爸，安慰家里担心的亲人。 |
| 1560 | Leo learns to aspire high and control wild **desire** for games and phones. | 利奥学着志向高远，控制对游戏和手机的狂热渴望。 |
| 1561 | He seems to **yearn** for broad knowledge and often invoke great past examples. | 他似乎渴望广博知识，常引用过去的伟大例子。 |
| 1562 | An inner **itch** for progress drives each new attempt at difficult listening tasks. | 内心对进步的渴望，推动他每一次去做较难的听力任务。 |
| 1563 | Leo will strive each evening and slowly fulfil his weekly study plan. | 利奥每晚都会努力，慢慢完成每周学习计划。 |

按 §五.13 两档分开：**desire** 带标记 1 处（1560）＋ 未打标记 0 处；**yearn** 带标记 1 处（1561）＋ 未打标记 0 处；**itch** 带标记 1 处（1562）＋ 未打标记 0 处（三词各按全部词形扫全书 1833 句，命中都只有那一句；1716 那处译「他也渴望……」的英文是 `grew keen`，`desirable` 是另一个词头，均不算本组书证）。三词在**别的卡栏位里的引用都是 0 处**（已 grep `ex`/`exZh`/`note`/`m`），本组三侧独立文本各只有 2 条。

### 表头一句（title）

> 挨着三句写的都是利奥心里那股劲儿，可三句里它的身份不同：1560 它是**要被管住的那样东西**（control wild desire）、1561 它只是他心里朝着的方向（后面挂 for broad knowledge）、1562 它是**推他去做题的那一下**（drives each new attempt）——worklist 的共同义项三处译文都落上了（三个「渴望」），区别不在中文，在词性、后面挂什么、以及谁管着谁：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| desire | /dɪˈzaɪər/ | n.；v. | 欲望；v. 渴望 | 要被管住的那样东西 | 1560 里被 control 的那股（对游戏和手机）；卡上他想环游世界的那一份；别的卡一次都没提它 | `control wild desire for games and phones`（课文 1560） |
| itch | /ɪtʃ/ | n.；v. | 渴望；痒；v. （使）痒 | 心里坐不住那一下 | 1562 前面挂着 inner、后面 drives 的那个；卡上蚊子咬的那一处（身上那一档，课文 0 处） | `An inner itch for progress`（课文 1562） |
| yearn | /jɜːrn/ | v. | 渴望；渴求 | 心里朝着某样去 | 1561 他似乎朝着广博知识去的那一下；卡上他想要的那种更好的日子 | `yearn for broad knowledge`（课文 1561） |

### 默认那一行（实测宽度 37.5 / ≤40）

```
desire = 要管住那股（wild desire），yearn = 想更好的日子，itch = 心里那一下
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 它在本段是敌是动力 | desire | `control wild desire for games and phones`（1560，它是 control 的宾语）、`a strong desire to travel the world`（卡 ex，felt 的宾语） | 说那样得管住、得压住的东西 | 本组三处里只有它是**被别人管的那个**：句子的动词是 control，不是它 |
| 它在本段是敌是动力 | itch | `An inner itch for progress drives each new attempt at difficult listening tasks`（1562，它是主语、后面跟着 drives） | 说那一下推着你去动手 | 它是本组唯一自己站出来当主语的一处，而且谓语是 drives——它管人，不是人管它 |
| 它在本段是敌是动力 | yearn | `He seems to yearn for broad knowledge`（1561，它是 seems to 后面的动词）、`He yearns for a better life.`（卡 ex） | 说人心里朝着某样东西去 | 它既不推人也不被管：本书两处它前面都只挂着一个"想"（1561 的 seems to、卡上的 He yearns），句子到心里就停了 |
| 后面挂什么框架 | desire | `desire for games and phones`（1560，for 接东西）、`a strong desire to travel the world`（卡 ex，to 接动作） | for 接物件、to 接要做的事 | 本组唯一两套框架都有书证的一个，也是唯一能接 `to + 动词` 的（另两个本书只有 for） |
| 后面挂什么框架 | itch | `an inner itch for progress`（1562，for 接抽象名词） | 接一个抽象名词：进步 | 本书只给它 1 处 for、没有 to；1562 那处 inner 已经把它钉在"心里"，不是皮肤 |
| 后面挂什么框架 | yearn | `yearn for broad knowledge`（1561）、`yearns for a better life`（卡 ex） | 动词后面直接挂 for | 本书 2 处全走 for，一处例外都没有（它是三词里唯一的纯动词，框架最死） |
| 词性与它站的格 | desire | 卡 `m` 给了 n. 与 v. 两档；本书 2 处文本全在名词位（1560 宾语、卡 ex 宾语）；**v. 一档本书 0 处**（已 grep） | 名词：那样被管、被 felt 的东西 | 它卡上的动词档在本书没落地过——读到 desire 先按名词处理（itch 的动词档同样空，见下一行） |
| 词性与它站的格 | itch | 卡 `m` 三档（n. 渴望；n. 痒；v.（使）痒）；本书课文那处走的是「渴望」档（1562，带标记、名词位）；身上那一档只有卡 `ex` 1 处（蚊子咬）；**v.（使）痒一档本书 0 处**（已 grep：课文里 itch 只 1562 一处、且在名词位，itchy 全书 0 处） | 名词：心里那一下，或身上那一下 | 逐处核下来：worklist 那一档在本书**真出现了、走的是名词档**；动词两档都空 |
| 词性与它站的格 | yearn | 卡 `m` 只 v. 一档；本书 2 处全在动词位（1561 seems to yearn、卡 ex He yearns） | 动词：只能人在做这件事 | 三词里唯一的纯动词，卡上没给名词档，本书也没把它当东西用过 |
| 卡上栏位与词伙 | desire | 「词伙：control wild desire」一条（与课文 1560 **同源**，算 1 条），**无同义词栏** | 说那股要压住的劲 | 它看着有出处，其实卡与课文是同一处；独立文本 2 条（课文 + 卡 ex） |
| 卡上栏位与词伙 | itch | 「词伙：inner itch」一条（与课文 1562 **同源**，算 1 条），**无同义词栏**；卡 `ex` 那句 `caused an itchy on my arm`（走身上那一档，卡上写的词形是 itchy） | 说心里那一下（课文）／身上那一下（卡） | 它是本组最特殊的一侧：卡上唯一一条词伙就是课文那处比喻，可卡的例句却回到了蚊子咬 |
| 卡上栏位与词伙 | yearn | 卡 `note` **整栏为空**（既无同义词也无词伙），卡上文本只有 `ex` 一句 | —— | 按 §五.1 明说：yearn 侧的搭配证据只有课文 1 处 + 卡 ex 1 句，卡上给不出词伙；本组三侧各 2 条，是本切片唯一三侧齐薄的一组 |
| 中文落点 | 三个词 | 1560「狂热渴望」／1561「渴望广博知识」／1562「内心对进步的渴望」（`sentZh`） | 汉译英时先问中文给的是"要管住的东西"还是"心里推着你" | 本书译文在三处用了同一个「渴望」——三词在中文里零区别；而全书 `sentZh` 的「渴望」共 6 处，另外 3 处（1324、1716、1736）英文分别是 will、keen、eager，本组这三个词并不是"渴望"的唯一说法 |

### 总结句（实测宽度 22.0 / ≤30）

> desire 要管住，yearn 心里想，itch 推你去试。

### 出处清单

- `Leo learns to aspire high and control wild desire for games and phones`、`control wild desire`、`wild desire`、`desire for games and phones`、译文「利奥学着志向高远，控制对游戏和手机的狂热渴望」 —— 课文第 1560 句（章5 段11 第 2 句）及 `sentZh`。desire 全书唯一书证；`control wild desire` 同见 desire 卡 `note` 词伙段（**同源**，按口径 8 算 1 条）。同句的 aspire 不是本组成员，只作表头背景（硬约束 2）。
- `He seems to yearn for broad knowledge and often invoke great past examples`、`yearn for broad knowledge`、译文「他似乎渴望广博知识，常引用过去的伟大例子」 —— 课文第 1561 句（章5 段11 第 3 句）及 `sentZh`。yearn 全书唯一课文书证。
- `An inner itch for progress drives each new attempt at difficult listening tasks`、`An inner itch for progress`、`inner itch`、`drives each new attempt`、译文「内心对进步的渴望，推动他每一次去做较难的听力任务」 —— 课文第 1562 句（章5 段11 第 4 句）及 `sentZh`；`inner itch` 同见 itch 卡 `note` 词伙段（**同源**，算 1 条）。
- `He felt a strong desire to travel the world.` / 他有一种强烈的环游世界的欲望。 —— desire 卡 `ex` / `exZh`（表里写作 `a strong desire to travel the world`、`strong desire`，均为其连续子串）。
- `He yearns for a better life.` / 他渴望更好的生活。 —— yearn 卡 `ex` / `exZh`（表里写作 `yearns for a better life`，为其连续子串）。卡 `note` 整栏为空。
- `The mosquito bite caused an itchy on my arm.` / 蚊子叮咬让我的手臂发痒。 —— itch 卡 `ex` / `exZh`（表里写作 `caused an itchy on my arm`、`caused an itchy`，均为其连续子串；卡上这一处走的是身上那档、词形照卡上原文引，未改写）。
- 「词伙：control wild desire」（desire 卡）／「词伙：inner itch」（itch 卡）／yearn 卡 `note` 整栏为空 —— 三卡 `note` **都没有同义词栏** → 不可能互列 → worklist 标 `all_synonyms: false` 有据；不是"三词不同义"，三处译文都落在同一个「渴望」上就是反证。
- `The news was terrible yet the will for peace was terrific in hearts`、译文「但心中对和平的渴望却极其强烈」（1324）、`Slow progress felt desirable to him, and he grew keen to learn new skills each bright morning`、译文「他也渴望每个明朗早晨学点新本领」（1716）、`He wrote a letter of gratitude to Anna, saying he felt eager to start a new learning year`、译文「说渴望开启新的学习之年」（1736） —— 全书 `sentZh` 里「渴望」共 6 处，这三处英文用的是 will / keen / eager；三词都不是本组成员（硬约束 2），本条只为说明"中文一个词、英文好几个来源"。
- 义项「欲望；v. 渴望」「渴望；痒；v. （使）痒」「渴望；渴求」 —— 三卡 `m`（只待在 `sense` 槽）；「利奥立志高远，用计划安慰家人」—— 段 11 `paraZh`；「上篇·备考的一天」—— `subheads[5][0]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡；三侧各 2 条独立文本，区别全在"谁管着谁"与框架

- **共享义项核对**：本组**不属**口径 6 的名不副实型 —— 1560、1561、1562 三处 `sentZh` 都写着「渴望」，worklist 的义项三侧都成立（已 grep 全书「渴望」6 处、本组占 3 处）。按 §五.4，这个共同义项原词仍只待在 `sense` 槽，默认行 / `core` / 总结句一律换成画面钩子。
- **itch 那一档逐处核过（本切片单独提醒要求）**：它卡上 `m` 首档「渴望」**在本书真出现了** —— 1562 一处、带标记、**名词档**（`An inner itch for progress`），且卡上唯一那条词伙 `inner itch` 就是这一处（同源，不另计）；n.「痒」档只有卡 `ex` 1 处（蚊子咬，本书课文 0 处）；v.「（使）痒」档本书 0 处（课文里 itch 只 1562 那一个名词位，itchy 全书 0 处，已 grep）。三档里 1 档落地、1 档只在卡上、1 档空 —— 表里"词性与它站的格"那行按这个写，没升级成任何禁令。
- **同源合并（口径 8）**：desire 侧 2 条（1560 + 卡 ex；词伙 1 条与 1560 同源，不另计）、itch 侧 2 条（1562 + 卡 ex；词伙同源）、yearn 侧 2 条（1561 + 卡 ex；卡无栏位）→ **本切片唯一三侧齐薄的一组**，按口径 7 照写不编，"卡上栏位与词伙"整行明说了薄在哪。
- **本组站得住的硬区别有两格**：① 谁管着谁（1560 desire 是 control 的宾语 / 1562 itch 是 drives 的主语 / 1561 yearn 前挂 seems to）——三处书证各一条、正好一格一个；② 框架（desire 有 for 与 to 两套，yearn、itch 本书只有 for）。别的一律不写。
- **不写的**：「itch 比 desire 口语」「yearn 偏书面语域」（三卡 `note` 都没有语域标注，本书给不出证据）、`itch for a seat`、`yearn after`、`sexual desire`、`a burning desire`、`itch to do sth` 这类两份数据 0 处的写法一律不列（desire 卡 ex 的 `to travel` 是名词后面的不定式，不等于本书写过 `desire to do` 的动词用法）。
- 无需拍板：义项三侧都成立、itch 的三档已逐处核清；薄的部分按口径 7 如实落地。

---

## 组5 · cope / dispose / tackle —— 处理 ｜ `all_synonyms`: false ｜ 段 [5,12] ｜ 句 1567 / 1568（三成员）

**共现位置**：1 段 2 句 —— 第 6 章（章索引 5）段 12（全局 1564–1569），成员句 1567（tackle）与 1568（**cope 与 dispose 挤在同一句，由 `and` 并列：前半压力、后半烦恼**）。**主锚点 = 章5 段12**（`paraZh`：启动复习循环，利奥认真攻克听说读写。`subheads[5][0]`：上篇·备考的一天）。卡挂段 12 末。
主锚点 = 章5 段12

**这一段在讲什么**：1564 说难的一周就在眼前、利奥保持镇定、开始按主题把卡片归类，1565 说"开始"就是带着目标把一天启动，1566 说他不会占别人便宜、而是去走新的路子，1567 说只要肯出力就能迎面接住每一个挑战，1568 说他学会了跟压力相处、并把烦恼清走，1569 说他的做法诚实、什么都没漏（全由本段句子本身撑）。

**该段原文（章5 段12）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1564 | A hard week looms ahead, but Leo keeps calm and begins to sort his cards by topic. | 艰难的一周近在眼前，但利奥保持镇定，开始按主题整理自己的卡片。 |
| 1565 | To commence is to launch the day with purpose. | 所谓开始，就是带着目标启动新的一天。 |
| 1566 | He would not exploit others but explore new paths. | 他不会占别人的便宜，而是去探索新的道路。 |
| 1567 | If you exert effort, you **tackle** each challenge directly. | 只要肯出力，你就能正面处理每一个挑战。 |
| 1568 | He learned to **cope** with stress and **dispose** of worry. | 他学会了应对压力，并处理掉烦恼。 |
| 1569 | His conduct was honest and nothing was left to omit. | 他的行为诚实，什么都没被漏掉。 |

按 §五.13 两档分开：**cope** 带标记 1 处（1568）＋ 未打标记 0 处；**dispose** 带标记 1 处（1568）＋ 未打标记 0 处；**tackle** 带标记 1 处（1567）＋ 未打标记 0 处（三词各按 cope/copes/coped/coping、dispose/disposes/disposed、tackle/tackles/tackled 的全部词形扫全书 1833 句，命中各只有那一句；1564 的 sort、1566 的 explore 是另两个词头，不算本组书证）。别的卡栏位里：cope 被 mount 卡的词伙用掉 1 次（`cope with mounting pressure`，跨卡同串），dispose、tackle 在两卡之外 **0 处**引用（已 grep 各卡 `ex`/`exZh`/`note`/`m` 栏）。

### 表头一句（title）

> 1568 一句里两个动词被一个 `and` 拴在一起，管的两样东西却都在心里，接法完全不同：cope 后面挂 with stress（压力还在那儿，他跟它相处）、dispose 后面挂 of worry（烦恼是被清走的那样）；1567 的 tackle 什么介词都不挂，直接接 each challenge——worklist 的共同义项在译文里出现两次（1567、1568），cope 那处却换成了另一个字，三词的分工就是这三套框架：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| cope | /kəʊp/ | v. | 应付；处理 | 那东西还在肩上 | 1568 他学会了跟它相处的那件事；卡上他靠每天散步压住的；卡上还挂着越堆越高的那堆压力 | `cope with stress`（课文 1568） |
| dispose | /dɪˈspəʊz/ | v. | 处置；处理 | 把它清走 | 1568 被 and 清掉的那一样；卡上那堆被弄走的旧书；卡词伙里的废物 | `dispose of worry`（课文 1568） |
| tackle | /ˈtæk(ə)l/ | v. | 处理 | 迎面接住它 | 1567 肯出力就正面撞上的那一个个挑战；卡上明天要办的那件事；卡词伙里的污染 | `tackle each challenge directly`（课文 1567） |

### 默认那一行（实测宽度 36.5 / ≤40）

```
cope = 压力还留着（with stress），dispose = 把烦恼清走，tackle = 正面接招
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 后面挂什么（本组最硬的一格） | cope | `cope with stress`（1568；卡 ex `copes with stress by walking every day` 与词伙同串）、`cope with mounting pressure`（卡词伙与 mount 卡词伙同串，本书课文 0 处） | 只走 with：跟那样东西共处 | 本组唯一后面必挂 with 的一个，本书 3 处文本一处例外都没有 |
| 后面挂什么（本组最硬的一格） | dispose | `dispose of worry`（1568）、`disposed of the old books`（卡 ex）、`dispose of waste`（卡词伙，本书课文 0 处） | 只走 of：of 后面就是被弄走的那样 | 它是本组唯一带 of 的——of 后面的东西在句子里就"没了"（烦恼、旧书、废物） |
| 后面挂什么（本组最硬的一格） | tackle | `tackle each challenge directly`（1567）、`tackle the problem tomorrow`（卡 ex）、`tackle pollution`（卡词伙，本书课文 0 处） | 零介词：动词后面直接上宾语 | 三套框架里唯一不挂介词的：出手就是接住那件事本身 |
| 那东西还在不在 | cope | `He learned to cope with stress`（1568）、`He copes with stress by walking every day.`（卡 ex，靠每天散步压住） | 东西还在，人学着与它共处 | 本书给它的两条都是长跑：压力不消失，他只是每天走路跟它相处 |
| 那东西还在不在 | dispose | `dispose of worry`（1568）、`He disposed of the old books.`（卡 ex） | 东西被清走、扔掉 | 本组只有它能"让那样东西不见"：烦恼没了、一摞旧书没了 |
| 那东西还在不在 | tackle | `If you exert effort, you tackle each challenge directly`（1567，同句前半是 exert effort） | 迎面撞上去，事情还在眼前 | 它既不共处也不清走：本书那处靠同句的"肯出力"正面对上 each challenge |
| 接的是哪类东西 | cope | stress（1568、卡 ex）、mounting pressure（卡词伙） | 心里的负担，看不见但赖着不走 | 本书给它的宾语全是心里那点重量：压力、越堆越高的压力 |
| 接的是哪类东西 | dispose | worry（1568）、the old books（卡 ex）、waste（卡词伙） | 抽象的烦恼与要扔的实物都能接 | 本组唯一一处 of 后接实物旧货（一摞书）在它这侧——抽象与实物通吃 |
| 接的是哪类东西 | tackle | each challenge（1567）、the problem（卡 ex）、pollution（卡词伙） | 难题、公共议题这类要办的事 | 它接的全是"该有人动手办"的东西：挑战、问题、污染 |
| 卡上圈子里有谁 | cope | 「词伙：cope with mounting pressure、cope with stress」，**无同义词栏** | 跟压力相处 | 它这一栏根本没有同义词可列 → 三卡不可能互列 → worklist 标 `all_synonyms: false` 有据 |
| 卡上圈子里有谁 | dispose | 「同义词：address, handle, deal with」＋词伙 `dispose of waste` | 处理、对付一堆词 | 栏里没点 tackle、也没点 cope；`address`、`deal with` 在两份数据里**没有词头卡**（已 grep），`handle` 有卡但不是本组成员（硬约束 2） |
| 卡上圈子里有谁 | tackle | 「同义词：address, confront」＋词伙 `tackle pollution` | 正面迎上一堆词 | 它与 dispose 唯一的交点是都列了 `address`（一个没卡的词）；`confront` 也没有卡（已 grep）——三侧的互不点名才是 false 的物证 |
| 卡上另一档用上了吗 | cope | 卡 `m` 首档「应付」本书 0 处（已 grep `sections.json` 全文「应付」0）；1568 译文给的是「应对」 | 想说"应付得来" | 名义首档在本书没落地过，本书只把它译成"跟压力相处"那一类 |
| 卡上另一档用上了吗 | dispose | 卡 `m` 首档「处置」本书 0 处（已 grep `sections.json` 全文「处置」0 处） | 说处置垃圾、处置嫌疑人 | 卡上词伙 `dispose of waste` 才是这一档的形状，但课文 0 处（只在卡上） |
| 卡上另一档用上了吗 | tackle | 卡 `m` 只一档「处理」，没有第二档 | —— | 三卡里只有它没给备选档，所以表里也不替它编一档 |
| 中文落点 | 三个词 | 1567「正面处理每一个挑战」／1568「应对压力」＋「处理掉烦恼」（`sentZh`） | 汉译英时先问中文是"跟它相处""弄走它"还是"迎面接住" | 本书 `sentZh` 里「处理」一共 2 处，**两处都在这半段里**（1567 归 tackle、1568 归 dispose），而 dispose 那处多了一个「掉」字——那正是 of 的"弄走"；cope 那处一个"处"字都没用 |

### 总结句（实测宽度 21.0 / ≤30）

> cope 扛着，dispose 清走，tackle 迎面接住。

### 出处清单

- `He learned to cope with stress and dispose of worry`、`cope with stress`、`dispose of worry`、`with stress`、`of worry`、译文「他学会了应对压力，并处理掉烦恼」 —— 课文第 1568 句（章5 段12 第 4 句）及 `sentZh`。**cope 与 dispose 的唯一课文书证，同句两格、各算一侧**（本组最强的一条物证：本书自己把两个词并在一个 `and` 两边）。
- `If you exert effort, you tackle each challenge directly`、`tackle each challenge directly`、`tackle each challenge`、`exert effort`、译文「只要肯出力，你就能正面处理每一个挑战」 —— 课文第 1567 句（章5 段12 第 3 句）及 `sentZh`。tackle 全书唯一书证。
- `He copes with stress by walking every day.` / 他通过每天散步来应对压力。 —— cope 卡 `ex` / `exZh`（表里写作 `copes with stress by walking every day`、`by walking every day`，均为其连续子串）。
- `cope with mounting pressure`、`cope with stress` —— cope 卡 `note` 词伙段两条：`cope with stress` 与课文 1568 **同源**（同时也是卡 `ex` 的连续子串，三处同串只算 1 条）；`cope with mounting pressure` 同见 mount 卡词伙段（跨卡同串，算 1 条），本书课文 0 处（已 grep）。
- `He disposed of the old books.` / 他处理了那些旧书。 —— dispose 卡 `ex` / `exZh`（表里写作 `disposed of the old books`、`of the old books`，均为其连续子串）。
- `dispose of waste` —— dispose 卡 `note` 词伙段（本书课文 0 处，已 grep）。
- `He will tackle the problem tomorrow.` / 他明天会处理这个问题。 —— tackle 卡 `ex` / `exZh`（表里写作 `tackle the problem tomorrow`、`tackle the problem`，均为其连续子串）。
- `tackle pollution` —— tackle 卡 `note` 词伙段（本书课文 0 处，已 grep；pollution 有卡、但它是别的组的成员，不进本组表）。
- 「同义词：address, handle, deal with」（dispose 卡）／「同义词：address, confront」（tackle 卡）／cope 卡 `note` 无同义词栏 —— 已逐个 grep `vocab.json`：`address`、`confront`、`deal with` **都没有词头卡**（`handle` 有卡），按硬约束 2 一律不进成员表。三栏没一处互列本组成员 → `all_synonyms: false` 有据。
- 义项「应付；处理」「处置；处理」「处理」 —— 三卡 `m`（只待在 `sense` 槽）；「启动复习循环，利奥认真攻克听说读写」—— 段 12 `paraZh`；「上篇·备考的一天」—— `subheads[5][0]`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 数据边界与判定：能出卡；本组有本书少见的"三套框架并排"物证，但三侧都薄

- **共享义项核对**：worklist 的「处理」在本书译文里两处成立（1567 tackle、1568 dispose，`sentZh` 全书「处理」就这 2 处），cope 那处译文换成了「应对」（`sentZh` 里「应对」1 处，就是 1568；另有一处在章0 段33 的 `paraZh`，与本组无关）→ **不属口径 6 的名不副实型**，但"三词在中文里并不是同一个词"这一格已如实写进"中文落点"。按 §五.4，「处理」「应付」「处置」三个 `m` 原词只待在 `sense` 槽。
- **本组最硬的一格是介词框架**：with / of / 零介词，三档共 9 条文本（课文 2 + 卡 ex 3 + 卡词伙 4）无一例外 —— 这是本切片形状最整齐的一格，但仍是"本书这几处"的分布，不许落地成"cope 只能接 with"。
- **同源合并（口径 8）**：cope 侧独立文本 **3 条**（1568、卡 ex、词伙 mounting pressure 那条课文 0 处但与 mount 卡同串 → 算 1 条）、dispose 侧 **3 条**（1568、卡 ex、词伙 dispose of waste 课文 0 处）、tackle 侧 **3 条**（1567、卡 ex、词伙 tackle pollution 课文 0 处）→ 三侧齐平、各 3 条，其中课文各只 1 处。按口径 7 照写：薄是常态，本组不靠编语义层加厚。
- **不写的**：「cope 偏心理、tackle 偏事务、dispose 偏垃圾」（三卡 `note` 无语域标注，本书这三处虽然正好各占一类，样本只 1 处，不外推）、「dispose 的 n. 名词档 disposal」（卡 `m` 只给 v.，本书也没有 disposal）、`cope with it`、`tackle a task`、`dispose of the body` 这类两份数据 0 处的写法一律不列。
- 无需拍板：三侧各有 1 处课文书证、框架区别可 grep 且整齐；`all_synonyms: false` 有据。

---

## 附：本切片复核索引（给门禁 / 抽查用）

| 组 | worklist_idx | words | senses | `all_synonyms` | paras | sents | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 208 | expose / reveal / uncover | 揭露 | false | [4,18] | 1388,1389 | 出卡：义项名不副实（「揭露」全书 0 处，三处译文是 揭晓／发现／暴露）→ 口径 6 自处；硬区别 = 摊出来的是好东西还是害人的东西（reveal plans・uncover bright ideas 对 expose weak points）+ expose 的 `be exposed to` 框架；uncover 侧 2 条独立文本（本切片最薄的一档） |
| 2 | 209 | calculate / compute / count | 计算 | false | [4,20] | 1398,1399 | 出卡：唯一硬轴 = 对象能否一件件点出来（count 全书 13 处、8 处宾语挂 each/every，对 calculate・compute 各 2 条"合起来的量"）；calculate↔compute 明写缺证（两卡 `ex` 只差词头 = §五.14 形状，各算一侧）；**compute 名下已挂批次 5 的 compute／estimate 表，落地载体建议换 count 或 calculate** |
| 3 | 210 | convert / transform / turn | 转变 | false | [4,25] | 1426,1428 | 出卡：本段 turn 不在此档（1428 拧的是阀门）→ 口径 6 的"档位错"、书证在别段（未标 turned … into 5 处）；硬区别 = 1426 同句并列同一桶水（可换物证）＋ 本书"换样"那一格真正反复用的是 turn + into；convert、turn 两卡 `note` 整栏为空已明说 |
| 4 | 211 | desire / itch / yearn | 渴望 | false | [5,11] | 1560,1561,1562 | 出卡：义项三侧都成立（三处 `sentZh` 全写「渴望」）；硬区别 = 谁管着谁（desire 被 control／itch 作 drives 的主语／yearn 前挂 seems to）＋ 框架（desire 有 for・to，另两个只有 for）；**itch 的「渴望」档逐处核过：真出现、走名词档（1562），v.（使）痒一档本书 0 处**；三侧各 2 条独立文本（本切片唯一三侧齐薄） |
| 5 | 212 | cope / dispose / tackle | 处理 | false | [5,12] | 1567,1568 | 出卡：硬区别 = 三套框架（cope with ／ dispose of ／ tackle 零介词，9 条文本无例外）+ 1568 同句并列就是本书物证；中文落点：全书 `sentZh`「处理」2 处都在这两句里、cope 那处译「应对」；三卡不互列故 false 有据；三侧各 3 条独立文本（课文各只 1 处） |

- **全局句号口径**按章偏移 `[0, 339, 651, 883, 1277, 1492]`（全书 1833 句）自算并与切片包逐个核对：章4 段18 起 1384（成员句 1388／1389 ✓）、段20 起 1396（1398／1399 ✓）、段25 起 1426（1426／1428 ✓）；章5 段11 起 1558（1560／1561／1562 ✓）、段12 起 1564（1567／1568 ✓）。五组 `sents` 条目共 11 个、去重 11 个，与 `slice43.json` 全部对得上，无一需要重算。
- **挂段位置**：组 1 → 章4 段18 末；组 2 → 段20 末；组 3 → 段25 末；组 4 → 章5 段11 末；组 5 → 段12 末。按 worklist_idx 升序排（208→212）。**口径 9 报备**：段 20 段末已挂四张表（instrument／tool、compute／estimate、estimate／evaluate、assess／gauge），本切片落地上去就是第五张，故组 2 换了轴（见该组开头）。
- **载体冲突（只这一处需下游处理）**：已 grep `shadow/data/vocab.json` 全部 181 张 `cmp` —— 本切片 15 个成员词里只有 **compute** 名下已有表（批次 5 的 compute／estimate，挂 [4,20]）；calculate、count、expose、reveal、uncover、convert、transform、turn、desire、itch、yearn、cope、dispose、tackle 名下均无 `cmp`。组 2 内容按成员原样写、未为此改动，落地时载体换成 calculate 或 count 即可（两者都是本组成员、且名下都没有表）。
- **章4／章5 段主题栏已复核无章3 的坏数据形状**（判据 `paraZh[i+22] == paraZh[i] + 尾缀「 灯下团聚，欢语不断。」`：章3 命中 22 段、章4 命中 0、章5 命中 0；坏数组在章索引 3，未动它）。本文件五组「这一段在讲什么」全部只由 `paragraphs` 真句撑；`paraZh` 一律标「段主题栏」，其中组 2 那段主题栏写的「学生测量计算」正好被本组句子撑住（1397 measure、1398 calculate），仍只当背景引用。
- **宽度标称值全部用 `tools/width_rule.py` 同一把尺实测**（汉字与全角标点各 1、其余 0.5）：默认行 39.5 / 38.5 / 38.0 / 37.5 / 36.5（上限 40），总结句 26.0 / 23.5 / 26.5 / 22.0 / 21.0（上限 30），`core` 15 格实测最大 10（上限 12）；无一条超上限，写完后逐组回量过一遍，标称＝实测。
- **表格列数已逐张数过 `|`**：15 张表（该段原文 3 格 × 5、成员 items 7 格 × 5、差异 diff 5 格 × 5）每表各行列数齐平，无整排左移、无 `eg` 槽空行。
- **本文件不引用任何在 `shadow/data/vocab.json`、`shadow/data/sections.json` 里查不到的英文串**；所有"本书没有／0 处／某词没有卡"式存在性断言均自己 grep 后再写（§五.15），集中在各组「数据边界」与出处清单里，抽查点：display／divulge 无卡、address／confront／deal with 无卡（handle 有卡）、desirable 与 desire 是两个词头、turnip／turnover 与 turn 无关、全书「揭露」「转变」「应付」「处置」「推测」各 0 处、三档 itch 的落点。门禁命令与结果见文件头小结（`python3 tools/check_compare_draft.py work/辨析草稿/6_07.md`）。
- **落地成卡对象时**：`title` = 各组「表头一句」，`summary` 的「简单记」= 各组「默认那一行」，`diff` 各行 `dim` = 「维度」列、`label` 用「词／三个词」，键名用成员词头；`items` 表 7 格与 `sense` / `core` / `scene` / `eg` 一一对应，`at` 取各组「主锚点」那一行。
