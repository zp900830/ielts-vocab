# 门禁 2 审核 · 批次 3 切片 10 + 11（`work/辨析草稿/3_01.md` 5 组 + `work/辨析草稿/3_02.md` 5 组）

组数 10 / BLOCK 1 / MISMATCH 5 / 通过（OK）4

> **审核方式（全部亲手复算，未采信草稿自报数字）**
> - 自建全局句索引：读 `shadow/data/sections.json` 逐章逐段逐句编号，实测各章起始全局号 = `{0:0, 1:339, 2:651, 3:883, 4:1277, 5:1492}`，与 BRIEF 的章偏移**完全一致**，全书 1833 句 ✓。
> - **A 行用机器逐字比**：写脚本把两份草稿「该段原文」表里每一行的英文（去掉 `**` 粗体与 `[[词头:表面]]` 标记后）、中文，与 `sections.json` 对应 `章/段/句` 做**字节级相等比较**，共 52 行。结果：**51 行逐字相符，1 行不符**（见组 61）。
> - **C 行用机器全量扫**：抽出两份草稿全部反引号内英文串（去掉字段名/命令/路径后，10 组共 **308 条证据串**），逐条在 `sections.json`（按表面形式）与 `vocab.json`（`m`/`ex`/`exZh`/`note` 原文）里不区分大小写检索。**无一条例外被当正面证据用却查不到**；查无的串全部落在否定行，且逐条实测确为 0 处（例外见组 55 / 组 56 两条 false-negative）。
> - **D 行全部用 `tools/width_rule.py` 的 `width()` 复量**，20 个标称值（10 条默认行 + 10 条总结句）**与实测逐一相等，无一条超上限**。
> - 全程只读：`shadow/data/*`、`shadow/index.html`、`scripts/`、`tools/`、测试目录、别人的草稿与 `work/辨析审核/` 其它文件均未改；未跑 `tools/land_compare.py`（避免写预览文件）；未做任何 git 写操作。

---

## 组 52 · feast / festival（3_01 组1）

A 原文表：**OK（逐字相符）**。表 6 行（575–580）字节级比对全中；`paraZh`「等待微笑、甜美消遣填满温暖夜晚，美好期待化作现实。」与 `subheads[1][27]`「下篇·文化娱乐周」逐字命中。
B 句号与共现：**OK**。自算章1 段39 起 575（339 + 前 39 段 236 句），579 为段内第 4 句，与切片包 `sents=[579]` 一致；两成员确以 `[[festival:festival]]` / `[[feast:feast]]` 同现于 579 ✓。处数复算：feast 标记 2 处 = 579、835 ✓；festival 标记 4 处 = 579、654、719、1127 ✓（另 1142 `warm village festivals` 未打标记，草稿写的是"标 4 处"，措辞成立）；654 = 章2 段0、719 = 章2 段11、1127 = 章3 段40，章段标注全对。
C 可 grep：**OK**。本组 43 条英文串，实测命中 **37** 条（五句课文 + 译文 + 两卡 `ex`/`note` + lantern·ethnic·official·jewish·turkey 五张旁证卡），查无 5 条（`harvest festival`、`food festival`、`feast day`、`the Feast of ...`、`feast of lights`）全在「不写的」否定行，实测课文 0 处 ✓，记为否定行放行。**1 条槽位标错**：出处清单写「`paper lanterns` 同见 lantern 卡词伙」，实测该卡词伙是**单数** `paper lantern`，复数形只在该卡 `ex` 里 —— 串本身查得到（同一张卡），只是槽位写错。
D 宽度：默认行草稿 38.0 / 实测 **38.0**；总结句草稿 25.5 / 实测 **25.5**。均在限内，标称即实测（未压低未抬高）。
E 义项照抄：**OK**。「盛宴／宴会／节日」只在 `sense` 槽；`core`、默认行均无义项词；表头「worklist 说共同义项是"节日"，可本段真站在这档上的只有 festival」是 §五.11 的"引用 + 当场否定"句式，放行。diff「中文落点」行引的是 `sentZh`（盛宴／办席／节日盛装…），逐条实测与译文一致，不是复述义项。
F 同源诚实：**OK**。feast 卡词伙 `sweet feast` = 课文 579、jewish 卡词伙 `jewish festival` = 课文 1127，两处同源都在「证据强度」点名合并，未算成两条独立出处；"festival 4 处里 3 处当定语"实测为真（579 mood / 654 outfit / 719 dress 定语，1127 中心语），"feast 0 处定语"亦真，且明写为计数结论非禁令。
**结论级：OK（可落地）**。作者拍板项（义项「节日」只在 festival 侧兑现）→ 我的判断：**处理正确、不必退回**。该义项确实只来自 feast 卡 `m` 自带第三档（实测 `m` = n. 盛宴；宴会；节日），草稿按 §五.6 把标题与默认行改成"各领哪样东西"，并给出可 grep 的硬区别（定语 3 处 vs 0 处），正是他要的层面。最小改动：把那条 `paper lanterns` 的出处写成 `lantern.ex`。

---

## 组 53 · imitate / mimic（3_01 组2）

A 原文表：**OK（逐字相符）**。表 6 行（581–586）字节级全中（含 583「喜戏」、585「第一次上台的芭蕾演员」等易错处）；`paraZh`、`subheads[1][27]` 命中。
B 句号与共现：**OK + 1 处失实**。自算段40 起 581，581 为段内第 0 句，与 `sents=[581]` 一致；两成员同挤 581、共用主语 `Kids` ✓。**处数失实**：草稿写「mimic 全书标 1 处（581）——**两词在本书只出现这一次**」。前半（标记 1 处）为真，后半为**假**：课文 **227**（章0 段38 第 5 句）`Their corner display mimics a wet rainforest and looks like a tiny jungle.`（译「他们那一角的展示**模仿**了潮湿的雨林」）是 `mimic` 的第二处书内文本（未打标记）。imitate 确为全书 1 处 ✓。
C 可 grep：**OK**。本组 24 条英文串，实测命中 **20** 条（581 全句 + 3 片段、两卡 `ex`、两卡 `note` 同义词栏与词伙、`mimic new sounds` 跨 sound 卡、582 全句），查无 3 条；其中 `new sounds`（课文 0 处）、`imitate sb in sth`、`mimic sb's manner`、`a good mimic` 均在否定行且实测 0 ✓。但**归属因漏 227 而偏低**：`mimics` 被只归给 mimic 卡 `ex`，实际课文 227 另有一处书内 `mimics`（比卡上例句更硬的证据）。
D 宽度：默认行草稿 38.0 / 实测 **38.0**；总结句草稿 17.0 / 实测 **17.0**。合规。
E 义项照抄：**OK**。「模仿／仿效／模拟」只在 `sense` 槽；表头用「学」（来自 `sentZh`），`core`（"照着谁学一套做法" / "学着像、还学出声"）与默认行均未照抄。
F 同源诚实：**MISMATCH**。`mimic new sounds`（mimic 卡与 sound 卡同串）已按 §五.8 合并算 1 条 ✓；两卡 `ex` 判"句子不同、不算同源" ✓ 成立（实测只差一个动词、`exZh` 逐字符相同）。问题在**厚度低报**：「本组全部独立英文文本只有 5 条」「两词在本书只出现这一次」漏 227，实为 6 条 / mimic 书内 2 处。低报不产生假卡（227 只会加强"mimic 学已有东西"这条线），但数字与"只出现这一次"是错的。
**结论级：MISMATCH（需作者改，不必退回）**。最小清单：①「两词在本书只出现这一次」→「两词**标记**书证各 1 处且同挤 581；mimic 另有未打标记 1 处 = 课文 227」；②处数 5 条 → 6 条；③「加 s 怎么写」行补 227 的书内 `mimics`。
作者拍板项（"卡上零区别"型要不要只出义项表）→ 我的判断：**不必只出义项表**。硬货实测在：`imitate unlawful behaviour` 在 imitate 卡 `note`、`mimic new sounds` 在 mimic 卡 `note`；两卡 `ex` 确实只差动词、`exZh` 一字不差；diff 没编第三层语义。按 §五.7 薄不是退回理由，改上面三处数字即可落地。

---

## 组 54 · mimic / simulate（3_01 组3）

A 原文表：**OK（逐字相符）**。表 3 行（581/582/583）字节级全中。
B 句号与共现：**OK + 同上失实**。581（段内 0）、582（段内 1）自算与 `sents=[581,582]` 一致 ✓；simulate 确全书 1 处 ✓；「mimic 全书标 1 处…两词在本书**各只 1 处**」后半仍为假（课文 227）。
C 可 grep：**OK**。本组 31 条英文串，实测命中 **27** 条（含 `her mother's voice`、`The child mimics`、`real traffic conditions`、`the small circus show`、`at dusk`）；查无的 `simulated data`、`simulation`、`mimicry` 均在否定行且实测 0 ✓。
D 宽度：默认行草稿 38.0 / 实测 **38.0**；总结句草稿 23.5 / 实测 **23.5**。合规。
E 义项照抄：**OK**。共同义项「模拟」在表头是"引用 + 当场否定"（"只在 simulate 这一侧有书证"），§五.11 豁免；`core`（"有原件，照着像" / "没到场，造出来"）与默认行无义项词。
F 同源诚实：**MISMATCH**。`simulate rain sounds` = 课文 582 已点名同源合并 ✓、`mimic new sounds` 跨卡只算 1 条 ✓，两侧 2 条 / 3 条的算术与自家口径一致。但漏 227 把两行结论带偏：①「谁当主语 | mimic：本书 + 卡 ex 两处主语都是人（孩子）」—— 227 主语是 `Their corner display`（非人）；② simulate 行「卡 ex 那句主语是 `The game`——本组唯一一处非人主语，**这是 simulate 独有的一格**」—— 按全书口径这一格**不独有**（mimic 在 227 同样是非人主语，且 227 译「模仿」正落在"照着现成的学"这一侧）。这是"把本书这几句恰好这么写"升级成排他结论的雏形，属 AUDIT_BRIEF §1 第 4 类，落地前必须改。
**结论级：MISMATCH（需作者改，不必退回）**。最小清单：①处数行补 227；②「谁当主语」两行按 227 改写（mimic 侧 = 人说、陈列也说；simulate 侧删"独有"或明确限定为"本组已登记 3 条里"）；③「有原件 / 没到场」主线不塌，反而多 227 一处支持（宾语"雨林"是现成真实东西），可把它列为 mimic 侧第 4 条。

---

## 组 55 · film / movie（3_01 组4）

A 原文表：**OK（逐字相符）**。表 4 行（587–590）字节级全中。说明性文字"成员句 + 前后各两句"与实际列出的（前 1 句 + 后 2 句）不符 —— 不是表格行错，记在 D 之外的文字瑕疵。
B 句号与共现：**OK**。段41 起 587（339 + 前 41 段 248 句），588 为段内第 1 句 ✓ 与 `sents=[588]` 一致；film 标记 2 处 = 588、1136（章3 段42 第 1 句 ✓）；movie 标记 1 处 = 588，且 `\bmovie` 在全书（含未标记）课文里也只有 588 一句 ✓。
C 可 grep：**OK，但 1 条否定行不成立**。本组 56 条英文串，实测命中 **50** 条：课文 588/1136/587、film 卡 `ex`/`note`、movie 卡 `ex`/`note`、17 张别的卡的 `ex`（duration、critic、actress、role、indifferent、apathetic、arouse、horrible、tame、objective、opinion、x-rated、violence、controversial、terror、epic、high-definition）、motion 卡词伙、soviet 卡词伙、tragedy / animation 卡词伙。"十几张卡"实测 = 除 movie 自己外 **18 张**卡的 `ex/exZh/note` 提到 movie ✓。**错处**：「不写的」把 `box office` 列为"本书 0 处"，实测 `box office` 就写在 office 卡 `note` 词伙段里（`词伙：physical office space, box office, travel to the central office`）—— 数据里真有的块，"0 处"是假阴性。另实测"film 卡第二档"薄薄的一层"本书 0 处"为**真**（全书 `\bfilm` 只有 588、1136，都是"影片"）✓。
D 宽度：默认行草稿 37.5 / 实测 **37.5**；总结句草稿 19.5 / 实测 **19.5**。合规。
E 义项照抄：**OK（带 1 条备注）**。`core`（"片名与行当那一头" / "你去看的那一场"）与默认行都换掉了「电影」。表头「一句里两个"电影"前后排队」是**肯定式**引用义项词：严格照 §五.4 字面会被拦，但按 §五.11 的执行标准（拦的是"宣称本段两词都指 X 而该段实际不撞"）—— 588 一句里 film 译「短片」、movie 译「长电影」，该段确实两词都指电影，**不构成假宣称** → 放行（与组 59、组 60 同型，本审核对三处一致处理）。
F 同源诚实：**OK**。soviet 卡词伙 `old soviet film` = 课文 1136 已合并、`soft tragedy` / `bright animation` 与 587 同源亦点名不另计 ✓；"movie 侧 11 条"拆成 4 + 5 + 2 实数吻合，表里只列 4 条、第 5 条（apathetic）在出处清单单列报备 ✓；未据同源文本宣称"两处书证方向一致"。
**结论级：MISMATCH（需作者改 1 处）**：`box office` 不能当"本书 0 处"用 —— 删掉这个例子，或改写成"本书只有 office 卡词伙里有 `box office`，与本组无关"。作者自判"无需拍板"→ 我核 588 译文两侧都落在电影类，成立；本组不属义项名不副实型 ✓。

---

## 组 56 · artist / painter（3_01 组5）

A 原文表：**OK（逐字相符）**。表 4 行（588–591）字节级全中；说明"成员两句 + 前后各一句"与实际一致 ✓。
B 句号与共现：**OK**。589（段内 2）、590（段内 3）与 `sents=[589,590]` 一致 ✓；artist 标记 1 处 = 589、painter 标记 1 处 = 590，且 `\bartist` / `\bpainter` 全书含未标记也只有这两处 ✓；596 确挂 `[[portrait]]` + `[[painting]]` ✓、597 用 `draw` ✓、598 用 `depict` ✓（报备行"597 那句 `Lily began to draw clouds above`"逐字命中）。
C 可 grep：**OK**。本组 35 条英文串，实测命中 **30** 条（589/590/587/591、artist 卡 `ex`、painter 卡 `ex`、`young artist` / `calm painter` / `wise craftsman` 三张卡词伙、craftsman 卡 `ex`）。查无的 `a house painter`、`wall painting`、`artist's studio`、`portrait painter` 均在否定行且实测 0 ✓。1 条措辞偏松：`the arts` 被列进"本书没有"，实测课文 341 有 `At the arts academy …`（"艺术学院"里的定语，不是作者想排除的"the arts = 艺术界"那一格）—— 不构成假证据，建议改成"本书只有 `the arts academy` 这一处含 the arts 字样"。
D 宽度：默认行草稿 36.5 / 实测 **36.5**；总结句草稿 20.5 / 实测 **20.5**。合规。
E 义项照抄：**OK**。「画家」在表头是"引用 + 当场否定"（"本段真译成「画家」的只有 painter 那一侧"），豁免；`core`（"手底下能把花弄活的那位" / "拿画笔的那位（还得出卖力气）"）与默认行都没出现「画家／艺术家／油漆工」；「油漆工」「艺术家」只在 `sense` 槽与 diff 表"卡 `m` 第几档"的引用位（实测 artist `m` = n. 艺术家；画家、painter `m` = n. 画家；油漆工 ✓）。
F 同源诚实：**OK**。三条同源（`young artist` = 589、`calm painter` = 590、`wise craftsman` = 590）全部点名合并；两侧各 2 条独立文本的算术与实测一致；未把同源算两条、也未据同源宣称"方向一致"。最硬那格（两卡 `ex` 动词交叉：artist 用 painted、painter 用 draws）实测为真。
**结论级：OK（可落地）**。作者拍板项（589 的 artist 译「艺术家」，画家档只在卡 `ex` 成立）→ 我的判断：**不必退回**。与组 52 的 feast 不同，artist 侧「画家」在卡 `ex` 里真在画画（`The artist painted a beautiful landscape.` 实测存在），且本组区别完全不依赖那个义项名（paint/draw 交叉 + young/calm 两路修饰 + 590 有帮手），三条均可 grep。

---

## 组 57 · opt / select（3_02 组57）

A 原文表：**OK（逐字相符）**。表 6 行（593–598）字节级全中；`paraZh`「展览收集与画像描绘。」、章名「校园与文化」与章 `zh`「新生学期与文化娱乐周，两卷读完。」逐字命中。
B 句号与共现：**OK**。段42 起 593、共 6 句（593–598）✓；594（段内 1）、595（段内 2）与 `sents=[594,595]` 一致 ✓；select 确以 `[[select:select]]` 在 594、opt 以 `[[opt:opt]]` 在 595 ✓。处数复算：opt 标记 1 处 ✓、select 标记 1 处 ✓，且 `\bselect\w*` 全书只有 594 与 456（`selective`，另一个词头），无漏计 → 草稿"全书 1 现"两条都成立 ✓（与组 53 的 mimic 不同，本组数字干净）。
C 可 grep：**OK**。本组 26 条英文串，实测命中 **22** 条（594/595 全句与片段、opt 卡 `ex`、select 卡 `ex` + 词伙、criteria 卡 `ex` 的 `selection`）。查无的 `opt for`、`opt for something`、`selected` 均在否定行，实测课文 + 卡上全 0 ✓。跨卡 `select` 归属亦核清：只有 choose 卡与 opt 卡的**同义词栏**点 select、criteria 卡是名词形 `selection` —— 草稿把 criteria 那条明写为"不当 opt / select 的动词书证" ✓ 诚实。
D 宽度：默认行草稿 39.5 / 实测 **39.5**（贴上限但未超，符合 §五.10"不抬不压"）；总结句草稿 26.5 / 实测 **26.5**。core 汉字数 9 / 8 亦实测相符。
E 义项照抄：**OK**。「选择／决定／挑选／精心挑选的」只在 `sense` 槽；`core`（"自己点头去做那件事" / "一堆里拿哪个出来"）与默认行无义项词；表头是"两词都算 + 当场分头"，非假宣称 ✓。
F 同源诚实：**OK**。select 卡词伙 `select smooth stones` = 课文 594 已点名合并（"合并后 select 侧实际只有 2 条独立文本"）✓；"全部 2 处都接 to 动词 / 都接名词"明写为 2 处 vs 2 处的计数结论、并举出别种语料可能有的写法 ✓；opt 卡 `note` **确无词伙段**（实测 note = `同义词：select`）✓ 已明说，没硬凑。
**结论级：MISMATCH（只需改 1 个单元格）**：diff 表「中文各走哪一路」行的"一句话区别"结尾写「本书译文把 select 处理成「挑」、把 opt 处理成「自愿选择」——**两条路都没写到"选"字**」—— 这半句与 `sentZh` 自相矛盾：草稿自己刚引的 595 译文就是「可以自愿**选择**画画」（实测 595 `sentZh` = 孩子们可以自愿选择画画，然后用手机给花拍照。）。最小修法：删掉"两条路都没写到『选』字"这半句，或改成"select 走「挑」、opt 走「自愿 + 选择」，一个报动作一个报意愿"。其余可直接落地。本组无拍板项。

---

## 组 58 · depict / describe（3_02 组58）

A 原文表：**OK（逐字相符）**。表 5 行（594–598）字节级全中；`paraZh` 与章名命中 ✓。
B 句号与共现：**OK，且本行是两份草稿里数得最干净的一组**。598 = 段42 第 5 句 ✓ 与 `sents=[598]` 一致；两成员同挤 598（`[[depict:depict]]` + `[[describe:describe]]`）✓。处数逐条实测：describe **标记 6 处 = 598、891、906、951、1071、1131** ✓ 与草稿完全一致；**未打标记 2 处 = 947、1127** ✓ 也一致（947 heads 只有 sunlight/harbour、1127 只有 jewish/festival）；951 原文标记确为 `[[describe:described]]` ✓ 逐字命中；depict 标记 1 处 = 598 ✓（全书 `\bdepict\w*` 也只有这一处）。各句"章N 段M 第 K 句"抽查 8/8 全对（891 = 章3 段1 第 2 句、906 = 段3 第 5 句、947 = 段10 第 4 句、951 = 段11 第 2 句、1071 = 段31 第 2 句、1127 = 段40 第 4 句、1131 = 段41 第 2 句）。
C 可 grep：**OK**。本组 35 条英文串，实测命中 **32** 条；`illustrate` 卡 `note` 同义词栏确点 depict、其 `m` 确有「给书籍、杂志等加插图」档 ✓；`explain` 卡 `note` 同义词栏确点 describe ✓（跨卡提及这一类草稿主动报备了 ✓）；describe 卡 `note` **实测为空串** ✓ 草稿"零词伙、零同义词栏"为真。查无的 `describe X as Y`、`depict sth in a painting` 均在否定行、实测 0 ✓。**1 条漏计**：`adverb` 卡 `ex` = `He speaks slowly, using an adverb to describe how he talks.` 也是 describe 的一条卡面文本，草稿"describe 侧课文 8 处 + 自己卡 ex 1 条"未列它（按本组 competition / movie 侧"别的卡 ex 也算"的自家口径，应算第 10 条）。
D 宽度：默认行草稿 37.5 / 实测 **37.5**；总结句草稿 22.0 / 实测 **22.0**。合规；core 汉字数 8 / 6 实测相符。
E 义项照抄：**OK**。表头「worklist 说共同义项是"描述"，可这一段 depict 走的是"画"那一档，本书 depict 两处都没有译成"描述"」= 引用 + 当场否定，§五.11 豁免；`core`（"画面上有那样东西" / "用话讲给你听"）与默认行均无「描述／描绘」；「描述」只在 `sense` 槽。
F 同源诚实：**MISMATCH（1 个数字虚高 1）**。`depict birds`（卡词伙）= 课文 598 已点名合并 ✓，"depict 侧 2 条独立文本"算术对 ✓；"4 处带尾巴"逐条实测命中（598 `in easy words`、891 `in my own words`、906 `from memory`、卡 ex `in one sentence`）✓；否定行「「描述」这两个字在本书只跟着 describe；depict 一次都没这样译过」实测**成立**（全书译文含「描述」只有 598 与 adverb 卡 `exZh`，两处译的都是 describe；含「描绘」的课文译文 0 处）✓。**唯一错处**：diff 行「本书 describe 的 8 处课文里 **7 处**的说话人是父亲」。实测按"describe 这个动作的发起人"算是 **6 处**（906、947、951、1071、1127、1131）；891 的发起人明确是"我"（原文 `let me describe it in my own words`，译文「又让**我**用自己的话讲一遍」），父亲在那句只是 compared 的主语 —— 草稿自己在同一张表里就把 891 写作「let me describe…」。「7 处」只有换成"讲故事人"这个更松的口径才成立，而正文用的是"说话人"。
**结论级：MISMATCH（需作者改，不必退回）**。最小清单：①「7 处说话人是父亲」→「6 处 describe 的动作发起人是父亲（891 是「我」讲，父亲是 compare 的主语）」，或把"说话人"改成"讲故事人"并括注 891；②「证据强度」里 describe 侧文本数补 `adverb.ex` 一条（并入现有 9 条 → 10 条，尾巴数不变）。本组无被推翻的语义。
作者拍板项（义项「描述」名不副实）→ 我的判断：**照写、不退回，处置正确**。实测 depict 卡 `m` = v. 描绘；描述（交集确实来自第二档）、598 译文「画下小鸟」、卡 `exZh`「这幅画描绘了一个宁静的村庄」—— 本书没把 depict 用在"用话说"上，草稿把话说轻 + 义项留在 `sense` 槽，正是 §五.6 的标准处置。它另提的"顺手给 describe 卡补词伙"属 `shadow/data/` 变更，本审核不动、也不计入草稿缺陷。

---

## 组 59 · famous / well-known（3_02 组59）

A 原文表：**OK（逐字相符）**。表 6 行（611–616）字节级全中；`paraZh`「市中心友好比赛与志愿者。」命中。
B 句号与共现：**OK**。段45 起 611、共 6 句（611–616）✓；615（段内 4）= `[[famous:famous]]`、616（段内 5）= `[[well-known:well-known]]` ✓ 与 `sents=[615,616]` 一致。处数：famous 标记 2 处 = 615、1028 ✓（1028 = 章3 段24 第 1 句 ✓，且 `\bfamous` 全书无第三处）；well-known 标记 1 处 = 616 ✓（`\bwell.known` 全书也只有 616）✓。
C 可 grep：**OK**。本组 22 条英文串，实测命中 **19** 条（615/616/1028 三句全句与片段、两卡 `ex`、famous 卡 2 条词伙、cuisine / dynasty / parliament 三张卡的 `ex`/`note`）；`He is a famous singer.` 与 `He is a well-known singer.` 实测只差一词、两条 `exZh` 同为「他是一位著名的歌手。」✓ 逐字符相同，草稿"本书自己就认定可换"的物证成立。查无的 `famous for being`、`a well-known fact` 均在否定行、实测 0 ✓。跨卡复核：`well-known` 在别的卡只出现在 familiar / famous 两张卡的**同义词栏**（无用法句）→ "well-known 侧 2 条"成立 ✓。
D 宽度：默认行草稿 33.0 / 实测 **33.0**（只括号一侧英文例，符合 §五.10"宁可只留一侧"）；总结句草稿 24.5 / 实测 **24.5**。合规；core 汉字数 6 / 7 实测相符。
E 义项照抄：**OK**。「著名／出名／众所周知」只在 `sense` 槽；`core`（"全城都知道他" / "说名字大家点头"）与默认行无义项词；表头对「著名」是肯定式引用，但 615 译「看着像名人」、616 译「那位有名的老师」该段确实两词都指"有名" → 不构成 §五.11 拦的假宣称（与组 55、组 60 一致放行）。
F 同源诚实：**OK**。famous 侧"7 条"拆法实测成立（课文 2 + 卡 ex 1 + 卡词伙 2 + cuisine/dynasty 两条 `for` 架子 = 7），且 `famous parliament` 词伙与 1028 同源已点名不另计 ✓；"本书两处 `for` 架子都挂在 famous 上"实测为真（全库无 `well-known for`）✓；未据同源文本宣称两处方向一致。
**结论级：OK（可落地）**。作者拍板项（两卡 `ex` 同形、区别全在 famous 一侧，well-known 侧仅 2 条）→ 我的判断：**薄不是问题，照写**（§五.7）。本组的硬区别实测都站得住：`for` 框架 2 vs 0、表语位 615 有 / well-known 2 处全在名词前、卡上现成块 2 vs 0，四条均可 grep。唯一提醒（不算退回理由）：默认行「well-known = 只站在名词前」是无限定词的通用口气，而作者在「不写的」里已拒绝把"前面必须 a"写成规则 —— 落地时若想稳一点，可在这一格里补"本书"两字；这是口味层，不改变本组结论。

---

## 组 60 · competition / tournament（3_02 组60）

A 原文表：**OK（逐字相符）**。表 6 行（611–616）与组 59 同段、逐字节全中（两张卡的表同段各列一次，属 §五.9 允许的"同段两张"，不算同源重复计数）。
B 句号与共现：**OK**。611 = 段45 第 0 句 ✓ 与 `sents=[611]` 一致；`[[competition:competition]]` + `[[tournament:tournament]]` 确在同一句、被 `felt kinder than` 摆在天平两头 ✓。处数：competition 标记 1 处（611）+ 未标记 1 处（370 = 章1 段5 第 1 句 ✓）✓；tournament 标记 1 处（611）、全书 `\btournament\b` 也只有 611 ✓。独立文本数复算：competition **10 条**（课文 2 + 自己卡 `ex`1/词伙 3 + represent / unite / spur 三张卡 `ex` + survive 卡词伙）✓ 逐条实测命中；tournament **3 条**（611 + 自己卡 `ex` + champion 卡 `ex`）✓，别的卡提到 tournament 的确实只有 champion 一张 ✓。contest 卡只在同义词栏点 competition，草稿明写"不进成员、只报备"✓ 未混计。
C 可 grep：**OK**。本组 42 条英文串，实测命中 **39** 条，含 370 全句（译文「拿到演讲比赛的决赛资格」为 `sentZh` 连续子串 ✓）、611 全句、六张卡 `ex`、五条 competition 词伙。查无的 `enter a competition`、`tournament bracket` 均在否定行、实测 0 ✓。"本书 competition 那 10 处里查不到一处配 won / champion"实测**成立**（`won` 只出现在 contest 卡 `ex` 的 spelling contest 上，不属本组 10 条）✓；"tournament 3 处里 2 处配 won / champion"✓。
D 宽度：默认行草稿 37.5 / 实测 **37.5**；总结句草稿 28.5 / 实测 **28.5**。合规；core 汉字数 7 / 8 实测相符。
E 义项照抄：**OK**。「比赛／锦标赛／竞争」只在 `sense` 槽与 diff 表"卡 `m` 第几档"的引用位（实测 competition `m` = n. 竞争；比赛、tournament `m` = n. 锦标赛；比赛 ✓，"卡 `m` 第一档「锦标赛」正是 611 的译文"亦实测为真：611 `sentZh` 给 tournament 译「锦标赛」✓）；`core`（"抢位置的那股劲" / "一轮轮打完发冠军"）与默认行无义项词。表头「两个"比赛"被摆在天平两头」为肯定式，611 确两词都是比赛 → 不构成假宣称。
F 同源诚实：**OK**。`grand tournament` = 611 同源已点名合并（所以 tournament 侧只算 3 条而非 4 条）✓；`survive competition` 只取词伙一条、同卡另一条明写"本组不取"✓；"3 处 vs 10 处"全部标成计数结论、并显式拒绝写成"不可数／可数"大结论 ✓。
**结论级：OK（可落地）**。本组无拍板项（作者未提请）；我复核作者写的"共享义项这次名副其实"实测成立（611 两侧都落在比赛类），不属于 §五.6 型。

---

## 组 61 · mat / rug（3_02 组61）

A 原文表：**不符 1 行 → BLOCK**。表 6 行里 5 行字节级全中，**第 666 行英文抄错一个标点字符**：
- 草稿：`His baby's blue blanket …`（半角 `'` U+0027）
- 数据：`His baby’s blue blanket …`（弯引号 `’` U+2019）
该行属「该段原文」表，按 AUDIT_BRIEF A 行"标点、大小写、单复数、时态都算不符 = **BLOCK**"，必须改回数据原字符再落地（这张表是落地时写进运行数据的源文本，字符被换 = 静默改 `sections.json`）。同组中文行 666 逐字相符，其余 5 行英文/中文全中；`paraZh`「洗枕头擦污渍、毛巾擦干夹标签，居家整理细致入微。」与章 2《衣食住行》`zh` 逐字命中 ✓。
B 句号与共现：**OK**。段2 起 663（651 + 前 2 段 12 句）、共 6 句（663–668）✓；664 = 段内第 1 句 ✓ 与 `sents=[664]` 一致；`[[rug:rug]]` + `[[mat:mat]]` 确在同一句 664 ✓。处数复算：mat 标记 1 处（664）+ 未标记 2 处（618 = 章1 段46 第 1 句 ✓、633 = 章1 段48 第 4 句 ✓）✓ 完全对得上；rug 标记 1 处（664）、全书 `\brugs?\b` 也只有 664 一处 ✓。各句"章N 段M 第 K 句"抽查 6/6 全对（含 663 / 665 = 段2 第 0 / 2 句）。
C 可 grep：**OK**。本组 36 条英文串，实测命中 **32** 条（664/663/665/618/633 全句与片段、mat 卡与 rug 卡的 `ex`/`note`、oriental 卡 `ex`、mattress / carpet / pad / cushion 四张卡的词伙）。特别复核的三条否定行**全部实测成立**：`bath mat`、`yoga mat`、`a rug in the hallway` 在课文与卡上均 0 处 ✓；"本书 carpet 唯一一处"实测 `\bcarpets?\b` 全书只有 663 一处 ✓；618 段 `paraZh`「晨练瑜伽与毯上拉伸。」逐字命中 ✓。
D 宽度：默认行草稿 37.0 / 实测 **37.0**；总结句草稿 17.0 / 实测 **17.0**。合规；core 汉字数 7 / 6 实测相符（顿号未计入，符合"只数汉字"的自报口径）。
E 义项照抄：**OK**。表头「worklist 给的共同义项"垫子"在本段只由 mat 一侧兑现，rug 那侧本书译的是「地毯」」= 引用 + 当场否定，§五.11 豁免；`core`（"门口那块、图干净" / "屋里铺的、能坐"）与默认行均无「垫子／地毯／席子」；「席子」「垫子」两档只出现在 `sense` 槽与 diff 表"卡 `m` 第二档"的引用位（实测 mat `m` = n. 垫子；席子、rug `m` = n. 小地毯；垫子 ✓，且"664 译「门口垫子」、卡 `ex` 译「垫子」"、"664 译「地毯」、卡 `exZh` 译「地毯」"四条译文实测逐字为真 ✓）。
F 同源诚实：**OK**。mat 卡词伙 `door mat` = 664、rug 卡词伙 `flowered rug` = 664，两条同源都点名合并（所以两侧 4 条 / 3 条的算术对）✓；mattress / carpet / pad / cushion 四张卡的词伙与 663、665 同源，出处清单明写"全部与课文同源，且都不进本组成员"、"数出处时也别重复计" ✓；"本书唯一两处复数形都归 mat"实测为真（618、633 都是 `mats`，rug 三处全单数）✓；"mat 在本书没被图案词修饰过"标注为计数结论非禁令 ✓。
**结论级：BLOCK（只卡 A 行一个字符，不是内容问题）**。必须改的最小清单：把 666 行英文的 `baby's` 改回数据原字符 `baby’s`（U+2019），一行一处。改完本组即可落地 —— B/C/D/E/F 五行全过。
作者拍板项（「垫子」只 mat 一侧兑现 + 同段挤着 carpet / pad / cushion / mattress 四张"垫子"档的卡）→ 我的判断：**处置正确、不必退回**，且这是两份草稿里"同段邻居"处理得最干净的一组：六个候选词全部明写"只报备、不进表"，符合 §五.2 与 §五.9。改那一个字符即可。

---

## 汇总与排序

| 组 | idx | 草稿 | 结论级 | A | B | C | D | E | F | 必须改的最小清单 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 52 | 3_01 | **OK** | ✓ | ✓ | ✓ 1 槽位标错 | ✓ | ✓ | ✓ | （可选）`paper lanterns` 出处改 `lantern.ex` |
| 2 | 53 | 3_01 | **MISMATCH** | ✓ | ✗ 处数 | ✓ | ✓ | ✓ | ✗ 低报 | 补课文 227：处数 5→6、删"只出现这一次"、`mimics` 行加书内证据 |
| 3 | 54 | 3_01 | **MISMATCH** | ✓ | ✗ 处数 | ✓ | ✓ | ✓ | ✗ 排他结论 | 补 227：改「谁当主语」两行、删 simulate「非人主语独有」 |
| 4 | 55 | 3_01 | **MISMATCH** | ✓ | ✓ | ✗ 假阴性 | ✓ | ✓ | ✓ | `box office` 不是"本书 0 处"（在 office 卡词伙）；表格说明"前后各两句"改"前 1 后 2" |
| 5 | 56 | 3_01 | **OK** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | （可选）`the arts` 那条否定改精确 |
| 57 | 57 | 3_02 | **MISMATCH** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 删/改「两条路都没写到"选"字」（595 译文就是「自愿选择」） |
| 58 | 58 | 3_02 | **MISMATCH** | ✓ | ✓ | ✗ 漏 1 条 | ✓ | ✓ | ✗ 数字虚高 1 | 「7 处说话人是父亲」→ 6 处（891 是「我」讲）；describe 侧补 `adverb.ex` |
| 59 | 59 | 3_02 | **OK** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 无 |
| 60 | 60 | 3_02 | **OK** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 无 |
| 61 | 61 | 3_02 | **BLOCK** | ✗ 666 弯引号 | ✓ | ✓ | ✓ | ✓ | ✓ | 666 行 `baby's` → 数据原字符 `baby’s`（U+2019），一处 |

**最严重的一条**：组 61 · A 行原文表 666 英文标点被改成半角撇号（数据是 U+2019）—— 全表 52 行里唯一一处字节级不符，属 A 行定义的字面 BLOCK；修法是一个字符，不涉及内容重写，但必须在落地（把这张表写进运行数据）之前改掉，否则会静默改动 `sections.json` 原文。

**三处"提请拍板"的总判断**：10 组里作者共提请 6 处拍板，我的结论全部是**不退回、按现表落地**（组 52、53、54、56、58、59、61 的义项/厚度问题都已按 §五.6/§五.7 正确处理）；真正需要人拍板的只有一个流程问题：组 53 / 54 的"某成员全书出现几次"要不要把**未打标记**的课文用法计入（3_02 的组 58/60/61 是计了的，两份草稿口径不一致，建议统一定成"标记数 + 未标记数分开写"，本次 5 处结论都只需按这个口径补字，不必推翻任何一组的区别）。
