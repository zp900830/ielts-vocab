# 辨析卡 drafting · 批次 1A（5 组定口味）

> 只做内容草稿，未动任何产品代码/数据文件。5 组已写完；卡上每一条英文串（共 283 个 code span）跑过机器终检，**0 条在 vocab.json / sections.json 里查不到**。5 组我判**都不必退回**，但组 2、组 5 的"共享义项"和组 3 的证据厚度各有一处要你拍板，见各节末尾。
>
> **2026-09-20 门禁 2 修复轮（作者侧）**：按 `work/辨析审核/audit_batch1.md` 逐组改掉本文件被点名的 4 条 BLOCK 与全部 MISMATCH —— BLOCK：组 1 / 组 2 / 组 3（= worklist_idx 0/1/2）表头照抄卡 `m` 义项词、组 5（idx 4）「3 条独立文本」数重；MISMATCH：组 1、组 2 同源未点名，组 3 出处条数与"只挂在 endanger 上"的措辞，组 4（idx 3）撇号字形，组 5 的 `mild ailment` 出处号 + "8 处"方向数。表头那一档随后按 BRIEF §五.11 的裁决回调了一次：组 1、组 3 改回"引用共同译文 + 当场否定"，组 2 属灰区保持摘掉（见口径第 8 条）。逐条对应关系与实测宽度见 `work/辨析审核/FIX_batch1.md`。

## 0. 我用了哪些字段 / 口径（10 行内）

1. **卡片字段**：`shadow/data/vocab.json`[词头] 的 `m`（义项）、`note`（字符串时按「同义词：」「词伙：」两段拆读）、`ex`/`exZh`；`dawn`/`deteriorate`/`jeopardise` 的 note 里没有词伙段，我在每节「证据强度」里明说。
2. **对比型卡的目标形状**照 `docs/PRD.md` §7.2 与现存样例 `vocab.json` 的 `curse`/`swear`/`peep`（`note.type==='compare'`，含 `title` / `items[{w,p,pos,sense,core,eg|scene}]` / `diff[{label,…}]` / `summary`）；我的「默认那一行」= 样例里 summary 末尾「简单记：…」那半句，「差异维度表」= `diff`。
3. **段落原文**：`shadow/data/sections.json`[章]`.paragraphs[段][句]`（含 `[[词头:表面形式]]` 标记，引用时只留表面形式）+ `sentZh` 同位译句 + `paraZh` 段主题 + `subheads` 小标题。
4. **全局句号口径**：章累计句数偏移 = `[0, 339, 651, 883, 1277, 1492]`，总 1833 句；我按 `章偏移 + 段前句数 + 段内序号` 自算，结果与 `work/compare_groups_worklist.json` 的 `sents` 完全一致（0 基），本文所有「第 N 句」都用这个全局 0 基号。
5. **共现段判定**：一段里两个成员**都**以 `[[词头:…]]` 出现才算共现，自算结果与 worklist 的 `paras` 一致（dawn/sunrise 3 段、flat/smooth 2 段、其余 1 段）。
6. **英文可回溯**：卡上所有英文串（搭配与例句）我都跑了 grep 终检——课文按表面形式、卡片按 `ex`/`note` 原文，`grep -i`，见各节「出处清单」末行的核对结果；查不到的一律没写。
7. **长度口径**（与 `scripts/validate_data.py._width` / `tools/audit_ledger.py` 同一把尺，2026-09-20 收紧版）：**汉字与全角标点各记 1**，其余（拉丁字母、空格、`=`、半角括号）每个计 0.5；默认行 ≤40、总结句 ≤30。本文件 5 组的标称值就是这把尺的实测值，修复后复量：默认行 38.0 / 38.0 / 39.0 / 36.0 / 39.5，总结句 28.5 / 24.0 / 20.0 / 26.0 / 22.0，全部在限内。
8. **义项照抄检查**：成员 `m` 里的中文词（如「平坦的」「危及」「温和的」）不单独充当区别，只出现在 `sense` 槽（那本来就是义项位），`core`/默认行一律换掉。表头这一档按 BRIEF §五.11（门禁 2 的拍板项 2 已裁决：**引用共同译文 + 当场否定 = 放行**）分三种处置：**组 1、组 3** 已改回引用「黎明」「危及」并紧跟否定的原写法（那正是他点头过的形状）；**组 2** 属灰区、保持摘掉——原句「两个词真撞在"平"上时」只是拿义项词当叙述起点、没有当场否定它，同一句里「租来的那间公寓」还是拿 `flat` 的义项词直接当钩子（§五.11 放行的只有"当靶子 + 否定"这一种）；**组 4、组 5** 的表头本来就没引 `m` 原词（门禁 2 对这两组 E 项判的是 OK），未动。逐条理由见 `work/辨析审核/FIX_batch1.md`。
9. 未采用：`work/confusable_groups.json`、PDF 同义替换表、何琼等号表——按 PRD §5.10，它们不喂辨析。

---

## 组 1 · dawn / sunrise（共享义项：黎明）

**共现位置**：3 段 —— 章0 段20（全局 114–119，成员句 114 / 118）、章1 段43（599–604，成员句 600 / 604）、章3 段0（883–888，成员句 883 / 886）。卡挂段末，**主锚点取章0 段20**（第一段，两个词都是该段的显学词）。

**该段原文（章0 段20，`paraZh`：峡谷日出宫殿传说，荒野沙漠立誓护林。）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 114 | Leo photographed the splendid **sunrise** above the quiet lake. | 利奥拍下宁静湖面壮丽的日出。 |
| 115 | Later he visited a grand palace built by ancient kings. | 后来参观古代国王建的宏伟宫殿。 |
| 116 | The vast canyon looked truly magnificent as the guide told a very interesting legend. | 向导讲起一个有趣的传说，眼前深邃的峡谷显得格外壮丽。 |
| 117 | Then a dramatic change in the wind turned the sky from blue to black over the silent wilderness. | 接着风突然变了，寂静荒野上方的天空由蓝一下子转黑。 |
| 118 | At **dawn** they crossed a hot dry desert of low prickly bushes. | 拂晓穿过多刺灌木丛生的炎热干旱沙漠。 |
| 119 | Seeing the fallen trees, Leo vowed never to deforest the land for profit. | 看到倒下的树，他发誓再也不为赚钱而砍伐这片林地。 |

另两段共现（同一批句子，供表头改写用）：600 `…classical strings at dawn` / 604 `A shy solo rose over a sweet soft melody at sunrise`（`paraZh`：周日清晨多种歌曲风格）；883 `At dawn Father spread out the old sea chart…` / 886 `At sunrise we boarded the train…`（上篇·跨国列车，出发日）。

### 表头一句（title）

> 这一段里 `sunrise` 是他举起相机拍的那个东西，`dawn` 是他们出发的那个小时——都译成"黎明"，用法不是一回事：

### 默认显示的那一行（实测宽度 38.0 / ≤40）

```
dawn = 出发的那个钟点（at dawn），sunrise = 早起看的那道景（golden sunrise）
```

### 差异维度表

| 维度 | dawn | sunrise |
|---|---|---|
| 常见搭配（全取自卡 note 或课文原句） | `at dawn`（118 / 600 / 883）、`each dawn`（356）、`from dawn to dark night`（1201）；卡 ex `The birds sing at dawn` | `golden sunrise`（卡词伙 + 125）、`splendid sunrise`（卡词伙 + 114，另见 splendid 卡词伙）、`a calm lake at sunrise`（1824） |
| 用在哪 | 给动作标时间：几点起、几点出发、干到天亮 | 描写景象：湖上、大麦地上方、天空那一片光和色 |
| 一句话区别 | 只用来标时间，本书 16 处（按 `[[dawn:…]]` 标记计）没有一次是"被看的东西" | 既能当时间状语（`at sunrise` 886 / 1111 / 1126），又能当名词宾语被看、被拍、被描述 |
| 语法槽位 | 16 处基本都是时间点：`at dawn`、`each dawn`（356）、`from dawn to dark night`（1201）；最宽的一步也只是 1264 的 `the new dawn rule`（作定语，不算时间状语）。**没有一句把 dawn 写成被看见的景物**（本书查不到 see/watch + dawn 的组合） | 能带冠词、能被形容词修饰、能作 `watch / photograph / see` 的宾语（114、125、卡 ex `We woke up early to see the sunrise`） |
| 考试里怎么用 | 阅读/听力里 `at dawn = at sunrise` 是同义替换，别当新信息（对照 883 / 886 两句结构完全平行，都是"天亮做某事"） | 写作口语描述自然景象时才有它：`watch the golden sunrise`（125）；反过来只报时间点时两者都可用 |

### 总结句（实测宽度 28.5 / ≤30）

> 天刚亮那个点：dawn 是出发的钟点，sunrise 是看的那轮太阳。

### 出处清单（每条 → 来源）

| 卡上用到的英文串 | 出处 |
|---|---|
| `at dawn` | 卡 `dawn`.ex `The birds sing at dawn.` + 课文 118 / 600 / 883 / 823 / 882 / 1000 / 1120 / 1598 / 1818 |
| `each dawn` | 课文 356（`trained with the elite runners each dawn`） |
| `from dawn to dark night` | 课文 1201 |
| `The birds sing at dawn.` | 卡 `dawn`.ex / exZh |
| `golden sunrise` | 卡 `sunrise`.note 词伙 + 课文 125（`to watch the golden sunrise spread over the barley`）——**两处同源**（卡上词伙抄的就是 125 那句），计数只算 1 条 |
| `splendid sunrise` | 卡 `sunrise`.note 词伙、卡 `splendid`.note 词伙 + 课文 114——**三处同源**（两张卡的词伙都是 114 那句），计数只算 1 条 |
| `a calm lake at sunrise` | 课文 1824 |
| `We woke up early to see the sunrise.` | 卡 `sunrise`.ex / exZh |
| `at sunrise`（作时间点） | 课文 886 / 1111 / 1126（三句同为 `At sunrise we boarded the train`） |
| `watch the golden sunrise` | 课文 125 |
| grep 终检 | 上列 10 条串全部在 `vocab.json` 或 `sections.json` 原文内可回溯（`grep -i`），0 条落空 |

**证据强度**：课文 dawn 16 句、sunrise 8 句，双卡都有可用文本（`dawn` 卡 note 为空，但它的全部搭配都来自 `ex` 与课文，不影响成表）。**不退回**，这组数据最厚。
- **16 这个数的口径**：按 `[[dawn:…]]` 标记统计 = 16 句（与 worklist 一致）。若改按表面文本再放宽一句，967「…embark again at dawn」也带 at dawn 但词头没被标记，**没算进 16**；本组所有句数都是标记口径。
- **同源合并（BRIEF §8）**：卡 `sunrise`.note 的 `golden sunrise` ↔ 课文 125、`splendid sunrise` ↔ 课文 114 都是"卡上词伙抄课文那一句"，各算 1 条独立文本，出处清单已就地标注。合并后 sunrise 侧的独立文本 = 125、114、1824、886/1111/1126、卡 ex 共 6 处，方向仍一致（都落在"被看/被拍的景象"或"时间点"两档上）；**没有**把同源重复处当成两处独立书证来撑结论。
**待你拍的口径**：`dawn` 卡 `note` 是空的——若这张对比卡要落在 `dawn` 的 note 上，等于给它凭空挂内容；建议同批把 `at dawn` 补成 `dawn` 卡的词伙段（出处就是课文 118 和它自己的 ex），再上对比卡。

---

## 组 2 · flat / smooth（worklist 标称共享义项：平坦）

**共现位置**：2 段 —— 章0 段10（全局 53–58，成员句 55 / 56）、章2 段8（699–704，成员句 699 / 701 / 703）。主锚点取章0 段10。

**先报一个口径问题（重要，不是内容问题）**：这两段里 `flat` 一次都没用"平坦"。55 `They rented a small dry flat…` 走的是名词"公寓"，699 `An old flat iron…` 走的是"扁的"。而 `smooth` 在这两段都是"摸着不硌"（56 石地板、701 灰浆、703 布料）。真正的"平坦 vs 平滑"碰撞在**不同句**：971 `Flat land ran past the window`（平原）对 594 `select smooth stones by the lake`。也就是说 worklist 给的 `senses:["平坦"]` 是从两张卡的 `m` 交集来的，不是从共现段的实际用法来的。
→ **建议**：卡仍挂章0 段10 末，但表头按"这一段各用哪个义项"讲，"平坦"这一档放在差异维度表里补，别在标题里宣称本段两词都指平坦。（若你要求"辨析必须撞同一个义项"，这组就该退回单语境，证据就是上面这两句。）

**该段原文（章0 段10，`paraZh`：沿溪流进山谷，浅水处不受表象迷惑，石屋落脚。）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 53 | They jumped quickly over a narrow brook and followed a winding stream through tall pines. | 他们跃过一条窄窄的小溪，沿着蜿蜒的溪流穿过高大的松林。 |
| 54 | The water was clear and shallow, yet the guide warned against superficial judgments about safety. | 水清且浅，但向导警告别对安全做肤浅判断。 |
| 55 | They rented a small dry **flat** at the end of the path. | 他们在步道尽头租下一间干燥的小公寓。 |
| 56 | Its stone floor **felt** surprisingly **smooth** beneath their tired bare feet. | 石地板踩在他们疲惫的光脚底下，出奇地光滑。 |
| 57 | The outer walls stayed **rough** and dark with moss. | 外墙依旧粗糙，覆满深色苔藓。 |
| 58 | Behind the house a dry sandy path climbed toward the far hills. | 屋后一条干燥沙土路爬向远山。 |

（57 是 `rough` 卡词伙 `stayed rough` 的原句，正好是 56 的反义词，段内自带对照。）

### 表头一句（title）

> 这一段 `flat` 是他们租下来的那处落脚地，`smooth` 是光脚踩出来的手感——两个词真要撞在一起时，一个说陡不陡、一个说硌不硌：

### 默认显示的那一行（实测宽度 38.0 / ≤40）

```
flat = 说形状不陡不鼓（flat land），smooth = 说手感不硌不涩（smooth stones）
```

### 差异维度表

| 维度 | flat | smooth |
|---|---|---|
| 常见搭配（卡 note / 课文原句） | `dry flat`（卡词伙 + 55）、`flat land`（971）、`flat tyre`（tyre 卡词伙 + 934）、`flat iron`（iron 卡词伙 + 699）、`flat seat cushion`（665）、`a small flat in London`（卡 ex） | `felt surprisingly smooth`（卡词伙 + 56）、`select smooth stones`（select 卡词伙 + 594）、`shave smooth`（shave 卡词伙 + 670 `were shaved smooth`）、`smooth intonation`（intonation 卡词伙 + 541）、`smooth wall plaster`（701） |
| 用在哪 | 地形轮廓、物件截面（扁/瘪）、英式"公寓" | 表面质地（地板、石头、灰浆、布料），再引申到"顺"（语调 541、动作 1534 `Smooth moves`） |
| 一句话区别 | 眼睛看得出来没有起伏、没有坡度，可以用尺子量 | 手摸得出来不硌不涩，课文每次都给触觉动词：56 `felt`、703 `feels rough… feels smooth` |
| 词性与反义 | 本书 6 次全是形容词或名词（公寓），没有"把……摁平"的动词用法；反义方向是"鼓/陡/圆"（665 瘪的对应 soft feather pad） | 卡 `m` 带 `v. 使平滑`，670 就是被动用法 `were shaved smooth`；反义直接是 `rough`（703 同句对举、57 同段） |
| 考试里怎么用 | 租房/住房场景听力：`flat`=公寓，与 `apartment` 互替换（两卡同义词互指；apartment 卡词伙 `crowded apartment`） | describe an object / 手工、材料类话题：701、703 那整段就是"上手摸材料"的语料；说语调、说动作流畅也用它 |

### 总结句（实测宽度 24.0 / ≤30）

> 看得见的不起伏用 flat，摸得着的不粗糙用 smooth。

### 出处清单

| 卡上用到的英文串 | 出处 |
|---|---|
| `dry flat` | 卡 `flat`.note 词伙 + 课文 55（`a small dry flat`）——**两处同源**（卡上词伙就是 55 那句里的短语），计数只算 1 条 |
| `flat land` | 课文 971（原句首字母大写 `Flat land ran past the window`） |
| `flat tyre` | 卡 `tyre`.note 词伙 + 课文 934（`A flat tyre slowed the truck`） |
| `flat iron` | 卡 `iron`.note 词伙 + 课文 699 |
| `flat seat cushion` | 课文 665（`each flat seat cushion`） |
| `a small flat in London` | 卡 `flat`.ex |
| `felt surprisingly smooth` | 卡 `smooth`.note 词伙 + 课文 56——**两处同源**（卡上词伙就是 56 那句里的短语），计数只算 1 条 |
| `select smooth stones` | 卡 `select`.note 词伙 + 课文 594 |
| `shave smooth` / `were shaved smooth` | 卡 `shave`.note 词伙 + 课文 670 |
| `smooth intonation` | 卡 `intonation`.note 词伙 + 课文 541 |
| `smooth wall plaster` | 课文 701 |
| `Smooth moves` | 课文 1534 |
| `crowded apartment` | 卡 `apartment`.note 词伙 |
| `stayed rough` | 卡 `rough`.note 词伙 + 课文 57 |
| grep 终检 | 14 条全部回原文可找到，0 条落空 |

**证据强度**：`flat` 6 句、`smooth` 7 句，双卡都有词伙，且 `smooth` 的搭配还有 3 张别的卡互指（select / shave / intonation 的词伙里都写着 smooth），密度够。**不退回**。
- **同源口径（BRIEF §8）**：本组出处清单里凡是写成"卡词伙 + 课文号"的行，卡上那条词伙与课文那一句是**同一处文本**（`dry flat`↔55、`felt surprisingly smooth`↔56 已就地标注；`flat tyre`↔934、`flat iron`↔699、`select smooth stones`↔594、`were shaved smooth`↔670、`smooth intonation`↔541、`stayed rough`↔57 同理），数独立证据时按**课文出现次数**算，不把"卡上一条 + 课文一条"记成两条。
- "平坦"这一档不在本段：表头已改成"这一段各用哪个义项"的说法（`flat`=租来的落脚处、`smooth`=脚底的手感），worklist 的 `senses:["平坦"]` 只留在 `sense` 槽，"平坦 vs 平滑"的真碰撞写在差异表里（971 对 594），标题不宣称本段两词都指平坦。

---

## 组 3 · endanger / jeopardise（共享义项：危及）

**共现位置**：1 段 1 句 —— 章0 段2（全局 10–14），两个词挤在同一句 11 里（worklist `sents` 只有 `[11]`，即同句共现，属 PRD 说的 3.7% 那一类，卡挂段2 末）。

**该段原文（章0 段2，`paraZh`：厄尔尼诺与受伤森林、温室、彩虹，小波捡石测磁。）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 10 | Leo once had only a small mishap when he dropped his compass on the stone stairs. | 利奥只在石阶上掉过指南针，出过一次小意外。 |
| 11 | Climbing loose cliffs would **endanger** lives and **jeopardise** the whole school outing. | 攀爬松动的峭壁会危及生命，也会危及整次出游。 |
| 12 | A destructive storm, worsened by El Nino, broke trees along the hill road. | 一场被厄尔尼诺加重的破坏性风暴刮倒了山间小路两旁的树。 |
| 13 | Ms. Lin called the rainbow after the rain a beautiful natural phenomenon. | 林老师说，雨后的彩虹是一种美丽的大自然现象。 |
| 14 | Bo picked up a shiny pebble and tested it with his little red magnet. | 小波捡起一颗发亮的鹅卵石，用红色小磁铁试了试。 |

11 这句本身就是最好的教材：同一个主语、同一个 would，后面换着说两遍"危及"——不是两个强度，是**同句换词避复 + 宾语换了类型**（lives → the whole school outing）。

### 表头一句（title）

> 同一句先 `endanger` 命、再 `jeopardise` 整次出游，两个"危及"接的不是同一种东西：

### 默认显示的那一行（实测宽度 39.0 / ≤40）

```
endanger = 让活物遭殃（endangered species），jeopardise = 让事情黄掉（outing）
```

### 差异维度表

| 维度 | endanger | jeopardise |
|---|---|---|
| 常见搭配（卡 note / 课文原句） | `endangered species`、`endanger health`（卡词伙）、`endanger lives`（11）、`Pollution can endanger animals`（卡 ex） | `jeopardise the whole school outing`（11）、`Smoking can jeopardise your health`（卡 ex）；卡上**零词伙** |
| 用在哪 | 环境/动物/健康话题——污染、猎杀、栖息地没了（卡 ex 开头就是 Pollution） | 一件事的成败：行程、计划、机会、前途（本书唯一一次就是 school outing） |
| 一句话区别 | 宾语是**有命有身子的东西**：lives / animals / species / health | 宾语是**已经握在手里、可能丢掉的抽象成果**：the whole school outing |
| 正式度 | 数据里没有任何标记，两词同句并列，强度看不出高低——**所以表里不写"谁更正式"** | 同左；唯一确定的差异是拼写（见下一行） |
| 考试里怎么用 | 写作 Task 2 环境段直接上 `endangered species`，它是卡上现成的可背块 | 想表达"把整件事搭进去"就套 11 的句型 `jeopardise the whole school outing`；`-ise/-ize` 注意见出处清单第 8 行 |

### 总结句（实测宽度 20.0 / ≤30）

> endanger 伤的是命，jeopardise 黄的是事。

### 出处清单

| 卡上用到的英文串 | 出处 |
|---|---|
| `endangered species` | 卡 `endanger`.note 词伙 |
| `endanger health` | 卡 `endanger`.note 词伙 |
| `endanger lives` | 课文 11（`would endanger lives`） |
| `Pollution can endanger animals.` | 卡 `endanger`.ex / exZh |
| `jeopardise the whole school outing` | 课文 11（`and jeopardise the whole school outing`） |
| `Smoking can jeopardise your health.` | 卡 `jeopardise`.ex / exZh |
| 两词互列同义词 | 卡 `endanger`.note 同义词含 jeopardize；卡 `jeopardise`.note 同义词含 endanger（按 PRD §5.10，同义对子允许一起讲） |
| 拼写不一致（待你定） | 词头与卡 ex 用 `jeopardise`（英式 -ise），`endanger` 卡同义词栏写 `jeopardize`（美式 -ize）——两条都在数据里，不是我加的 |
| grep 终检 | 上列 **6 条引文串**全部回原文可找到，0 条落空；末两行（两词互列同义词 / 拼写不一致）是说明行、不是引文，不计算在条数里 |

**证据强度：薄，但我判它撑得起**。课文各 1 次、合计就 1 句，`jeopardise` 卡上没有词伙段——所以这张表的"宾语类型"那一行只有 **1 句课文 + 1 句卡 ex** 两条支撑，且 **health 撞车**（`endanger health` 与 `jeopardise your health` 都接 health），说明"活物 vs 事情"这条线在 health 上越界。
**不退回的理由**：区别点不是靠强度、不是靠正式度（这两条我都否掉了，因为无据），而是靠**本书唯一那次出现的宾语**+**`endangered species` 这块在本组两个词里只挂在 `endanger` 一侧**（`species` 卡的词伙里也写着它，但 species 不是本组成员），二者都可 grep。
**要落地前建议补的数据**：给 `jeopardise` 补 1–2 条有出处的词伙（现在 note 只有同义词，表里那一格会空），否则这组的实用价值全押在 11 那一句上。

---

## 组 4 · degrade / deteriorate（共享义项：退化）

**共现位置**：1 段 —— 章0 段8（全局 42–46），成员句 43（deteriorate）/ 44（degrade），相邻两句、同一段主题（极地天气）。卡挂段8 末。

**该段原文（章0 段8，`paraZh`：极地熊与倾斜地轴，天气恶化，升级帐篷。）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 42 | A hungry polar bear prowled nearby while the guide explained Earth’s tilted axis. | 一只饥饿的北极熊在附近游荡，向导则在一旁讲解倾斜的地轴。 |
| 43 | The thin springtime air began to **deteriorate**, and the wind would aggravate their frostbite. | 单薄的春季空气开始恶化，寒风会加重他们的冻伤。 |
| 44 | Old tents **degrade** fast in such cold, so the team upgrades them with thick warm linings. | 旧帐篷在严寒里坏得很快，所以小队会给它们加上厚厚保暖的内衬。 |
| 45 | Sea winds erode the cliffs day and night. | 海风日夜侵蚀着悬崖。 |
| 46 | Tired of the ice, everyone dreamed of sailing home across the warm Mediterranean. | 大家受够了冰雪，梦想着穿过温暖的地中海回家。 |

44 是本组的关键：`degrade fast` 后面**同句**接 `the team upgrades them`——本书自己就把 degrade 摆在 upgrade 的反义位置上，"掉档"这个意象不用我编。

### 表头一句（title）

> 这一段 `deteriorate` 说的是空气天气自己在变糟，`degrade` 说的是帐篷这件东西被消耗掉档：

### 默认显示的那一行（实测宽度 36.0 / ≤40）

```
deteriorate = 情况自己越来越糟，degrade = 物件被用得掉档（degrade fast）
```

### 差异维度表

| 维度 | degrade | deteriorate |
|---|---|---|
| 主语是谁（卡 ex / 课文原句） | 具体实物：`Old tents degrade fast`（44）、`The soil degrades over time without proper care`（卡 ex） | 状况类：`The weather will deteriorate tomorrow`（卡 ex）、`The thin springtime air began to deteriorate`（43） |
| 怎么个坏法 | 被外部磨掉一档，方向是"降级"——同句反义就是 `upgrades`（44） | 没人动手也自己往下滑，`began to deteriorate` 之后还叠加 `the wind would aggravate`（43） |
| 搭配可得性 | 卡有词伙 `degrade fast`，课文 44 就是它的原句 | 卡 note 为空，全部证据只有 ex 一句 + 课文 43 一句 |
| 中文义项给的差别 | 卡 `m` 只写「v. 退化」一个词，本书没给"贬低人"那档 | 卡 `m` 写「恶化；变坏；退化」三档，都比 degrade 宽 |
| 考试里怎么用 | 写土壤/材料被消耗：`The soil degrades over time`（卡 ex）、`tents degrade fast`（44） | 写天气、病情、形势转坏：`The weather will deteriorate`（卡 ex）、`air began to deteriorate`（43） |

### 总结句（实测宽度 26.0 / ≤30）

> deteriorate 是自己越来越糟，degrade 是被磨得掉了档。

### 出处清单

| 卡上用到的英文串 | 出处 |
|---|---|
| `degrade fast` | 卡 `degrade`.note 词伙 + 课文 44（`Old tents degrade fast in such cold`） |
| `The soil degrades over time without proper care.` | 卡 `degrade`.ex / exZh |
| `upgrades them`（degrade 的反义对举） | 课文 44 |
| `air began to deteriorate` | 课文 43 |
| `The weather will deteriorate tomorrow.` | 卡 `deteriorate`.ex / exZh |
| `aggravate their frostbite`（43 后半，说明"继续恶化"） | 课文 43 |
| grep 终检 | 6 条全部回原文可找到，0 条落空 |

**证据强度**：各 1 句课文 + 各 1 句卡 ex，共 4 条独立文本，比组 3 厚（degrade 另有卡词伙 degrade fast 与课文重合）。**不退回**，并按要求把区别写成"主语类型 + 掉档方向"这种他能用的东西。
**明写的两条边界**：① 常见讲法"deteriorate 不及物、degrade 可带宾语"，本书 4 条例证**全是及物性相同的用法**（`tents degrade fast`、`soil degrades`、`air began to deteriorate`、`weather will deteriorate` 都没宾语），所以这一档我在数据里找不到例子，表里不写；② degrade 的"贬低人格"义在卡 `m` 里没有，也没写。要加就得先给出处。

---

## 组 5 · mild / moderate（worklist 标称共享义项：温和）

**共现位置**：1 段 —— 章0 段12（全局 65–71），成员句 66（mild）/ 67（moderate），相邻两句；`subheads` = 第二卷·归来——风暴过后是家园。卡挂段12 末。

**该段原文（章0 段12，`paraZh`：气象站温和午后，暖气配适量晚餐，谈热带与干旱。）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 65 | The path down led them straight to a little wooden shack. | 下山的路把他们直接带到一间小木棚。 |
| 66 | At the little station they studied basic meteorology on a surprisingly **mild** afternoon. | 小站里他们在意外温和的午后学基础气象学。 |
| 67 | The old electric heating kept the room warm while they ate a **moderate** dinner. | 旧电暖气让房间暖和，吃着适量的晚餐。 |
| 68 | Each of them wore a thick thermal coat against the night cold. | 人人穿厚保暖外套抵御夜寒。 |
| 69 | They compared this cold with the wet heat of the distant tropics. | 把这里的冷和远方热带的湿热比较。 |
| 70 | One researcher had once worked in an arid desert where nothing grows. | 一位研究员曾在寸草不生的干旱沙漠工作过。 |
| 71 | Outdoors, the evening soil stayed soft and moist after light rain. | 屋外小雨过后，傍晚土壤依然松软潮湿。 |

**又一个口径问题（和组 2 同类）**：两卡 `m` 的交集是"温和"，但**本书里 `moderate` 从没修饰过天气或人**——它唯一那次出现（67）修饰的是一顿晚饭的量。所以"温和"这个共同义项是词卡给的，不是语料给的；这段真正撞在一起的是"都不极端"，而不是"温和"。表头按后者写，才和段文对得上。

### 表头一句（title）

> 这一段两个词都在说"不到极端"：`mild` 说的是那个午后的劲儿，`moderate` 说的是那顿晚饭的量。

### 默认显示的那一行（实测宽度 39.5 / ≤40）

```
mild = 不冲鼻不刺激（mild detergent），moderate = 量不超标（a moderate dinner）
```

### 差异维度表

| 维度 | mild | moderate |
|---|---|---|
| 常见搭配（卡 note / 课文原句） | `mild afternoon`、`surprisingly mild`（卡词伙 + 66）、`mild detergent`（detergent 卡词伙 + 658）、`mild fruit flavour`（850）、`mild and spicy`（851）、`mild fever / mild flu / mild arthritis / mild hypertension / mild infection / mild fatigue`（各自卡的词伙；课文里的病症用例落在 1656–1681）、`mild ailment`（**只在卡 ailment 词伙上**，课文里它出现在 1806，1656–1692 区间没有这一条） | `a moderate dinner`（67）、`a moderate amount of food`（卡 ex）、`moderate intensity activities`（卡词伙，intensity 卡同词伙） |
| 用在哪 | 天气（没想象中冷）、病症（不重）、味道与清洁剂（不冲不刺激）、性情（301 那只温顺的鸽子） | 吃多少、练多猛——凡事先有个刻度，它指中间那一格 |
| 一句话区别 | 预期里该猛的东西被调低：850 同句反义摆着 `not the sharp sour taste`，66 前面挂 `surprisingly` 就是因为"这海拔本该冷" | 数量/强度不上两头：67 与"kept the room warm"同句并列，说的是刚好够用、不多 |
| 能不能修饰天气 | 能，且是卡 ex 的第一句：`The weather is mild today.` | 本书 0 例（moderate 全书只出现 1 次，宾语是 dinner）——所以"温和的天气"只能说 mild |
| 考试里怎么用 | 健康类阅读里它是"轻微"不是"温和"：1656–1681 连着 **7 处**（`a mild illness`、`mild hypertension`、`mild arthritis`…），译错方向就丢分。同一批句子里只有 1692 的 `mild teas` 本书译文是"温和的茶饮"——那说的是口味，别拿它当病症那一档 | 运动与饮食建议：`moderate intensity activities`（卡词伙）、`a moderate amount of food`（卡 ex）——口语 Part 1 谈 habits 直接可套 |

### 总结句（实测宽度 22.0 / ≤30）

> mild 管"劲儿大不大"，moderate 管"量多不多"。

### 出处清单

| 卡上用到的英文串 | 出处 |
|---|---|
| `mild afternoon` / `surprisingly mild` | 卡 `mild`.note 词伙 + 课文 66（`on a surprisingly mild afternoon`） |
| `The weather is mild today.` | 卡 `mild`.ex / exZh |
| `mild detergent` | 卡 `detergent`.note 词伙 + 课文 658（`with a mild detergent`） |
| `mild fruit flavour` | 课文 850 |
| `mild and spicy` | 课文 851 |
| `not the sharp sour taste`（同句反义） | 课文 850 |
| `a grey mild pigeon` | 课文 301（性情温顺这一档；注：这是课文里恰好相邻的描写短语，不建议单独当词伙背） |
| `a mild illness` / `mild infection` / `mild hypertension` / `mild arthritis` / `mild flu` / `mild fever` / `Mild fatigue` / `mild teas` | 课文 1656 / 1657 / 1662 / 1666 / 1668 / 1669 / 1681 / 1692；除 `a mild illness`、`mild teas` 外，同名搭配同时也是 infection / hypertension / arthritis / flu / fever / fatigue 各卡的词伙。前 7 处本书译文都走"轻微"，只有 1692 的 `mild teas` 译"温和的茶饮" |
| `mild ailment` | 卡 `ailment`.note 词伙；课文里它落在 **1806**（`Anna learned to rest when a mild ailment made her eyes water`，该处 mild 未被词头标记），所以它属于 ailment 卡的词伙，不在上面 1656–1692 那一串里 |
| `a moderate dinner` | 课文 67（`they ate a moderate dinner`） |
| `He ate a moderate amount of food.` | 卡 `moderate`.ex / exZh |
| `moderate intensity activities` | 卡 `moderate`.note 词伙 + 卡 `intensity`.note 词伙 |
| grep 终检 | 上列 12 行引文串全部回原文可找到，0 条落空 |

**证据强度**：`mild` 课文 13 句（按 `[[mild:…]]` 标记计）+ 卡词伙 + 7 张健康类卡（infection / hypertension / arthritis / flu / fever / fatigue / ailment）的词伙互指，厚。
`moderate` 课文只 1 句（67），另有卡 ex 一句、词伙一条 —— 而 `moderate`.note 与 `intensity`.note 抄的是**同一条字符串** `moderate intensity activities`（两张卡各写一遍），按 BRIEF §8 只能算 1 条。所以 moderate 侧的独立文本是 **2 条**（卡 ex + 这一条词伙），**旧稿写的"3 条独立文本"是数重了，已改**；2 条的方向倒是一致（ex 说吃的量、词伙说练的强度，都是"居中那一格"），与课文 67 的 `a moderate dinner` 同向。
mild 侧同源点名（同一处文本合并算 1 条）：卡词伙 `mild afternoon`、`surprisingly mild` ↔ 课文 66；卡 `detergent` 词伙 `mild detergent` ↔ 课文 658；7 张健康卡上那条"mild + 病名"的词伙各是不同字符串，且除 `mild ailment`（课文在 1806）外都各有一句课文与之同形 —— 词伙与课文那句也按同源算，不重复计数。
**不退回**——只是 moderate 那一列比 mild 短，落地时别为了对齐去造搭配。
