# 2026-09-19 线上配置操作手册（三件事，都在控制台点，我在仓库里做不了）

> 面向：只有你一个人能操作腾讯云 / Supabase 控制台。
> 按顺序做，每步后面都有「怎么确认成功」。做完不需要改任何代码。
> 术语说明：EdgeOne Pages 已改名 **EdgeOne Makers**，控制台里两个名字可能混着出现，是同一个东西。

---

## 现在线上是什么状态（本轮改动落地后）

> ⚠️ 本节是 14:42 之前拍的状态快照，第 1、2 行已经过时。**15:45 的控制台复核与两条纠正见文末「15:45 复核」，以那一节为准。**

| 现象 | 原因 | 本轮改完后 |
|---|---|---|
| `shadow/config.js` 返回 404 | 该文件被 gitignore，仓库里没有；EdgeOne 的静态文件**读不到环境变量**，只有构建命令能读 | 构建脚本会生成它。**但凭证是空的**，要你在第 1 步填 |
| `docs/`、`tools/`、`playwright-tests/`、6 个 `sections.json.backup-*` 能公网下载 | 仓库根就是发布根 | 输出目录改成 `dist/` 后自动全部 404（已本地实测） |
| 4 张表里 `user_data` 没有任何建表脚本和 RLS 策略 | 建表 SQL 全在另一个项目里，且从没包含这张表 | 第 3 步粘一段 SQL |

---

## 第 1 步：填 Supabase 的两个环境变量

**去哪：** 腾讯云 EdgeOne 控制台 → 你的项目列表 → 点开 `ielts-vocab` → **项目设置** → **环境管理**。
里面固定有两行：**生产**（对应 main 分支）和 **预览**（其它分支）。点「生产」这一行的 **编辑** → 找到 **环境变量**。

**加两条：**

| 变量名 | 变量值 | 去哪拿值 |
|---|---|---|
| `SUPABASE_URL` | `https://nhqevdkjqzgnyjbijwso.supabase.co` | Supabase 控制台 → 项目 → Project Settings → API → Project URL |
| `SUPABASE_KEY` | Project **anon** public key 那一整串 | 同一页，**publishable/anon key**，不要复制 service_role key |

要点：
- 单个变量值上限 **500 字节**。anon key 约 300 字符，一条放得下；**别**把 URL 和 key 拼在一起。
- 生效范围文档里自相矛盾（一处写「按环境」，一处写「作用于整个项目」）。看到下拉框就两个环境都填，省事。
- **改环境变量不会自动重新部署**，只作用于"下一次部署"。所以填完必须做第 2 步。
- 如果你不想把 key 粘进控制台：更干净的做法是改用 GitHub Actions 构建（EdgeOne 官方支持），把它当 Actions secret。本轮不做，留给你选。

**确认：** 环境变量列表里两条都在，值显示为掩码。

---

## 第 2 步：让构建跑起来（框架预设 / 输出目录 / 构建命令）

我已经在仓库根放了 `edgeone.json`：

```json
{ "buildCommand": "node scripts/build_site.mjs",
  "installCommand": "true",
  "outputDirectory": "dist" }
```

**去哪核对：** 项目设置 → **构建部署配置**，确认四项是：

| 字段 | 应为 |
|---|---|
| 框架预设 | 其它 / 静态（不要选 Next.js 之类） |
| 根目录 | `./` |
| 构建命令 | `node scripts/build_site.mjs` |
| 安装命令 | `true`（仓库根没有 package.json，必须显式设，否则平台可能自己去 npm install） |
| 输出目录 | `dist` |

如果控制台里这几项**显示的还是旧值**，说明 `edgeone.json` 没能覆盖控制台设置 —— 就在界面上手动改成上表的值并保存。
（**安装命令这一步别省**：如果安装命令跑了 `npm install` 而输出目录又是 `/`，`node_modules` 会整包变成公网文件，还会撑爆 2 万文件/5 GiB 限制。）

**触发一次部署：** **只有推 `main` 的自动部署会把生产版本切过去**（15:45 实测）。
控制台里手动的「新建部署」会构建成功、会把产物上传、`项目概览`的「最近更新」也会变成它 ——
但 `ielts-vocab.bond` 仍然服上一个 release，所以**别指望点它来让配置生效**，见文末「15:45 复核」第 3 条。
或者直接推一个提交（main 分支会自动部署）。

**确认（三条都要过）：**

```bash
curl -s -o /dev/null -w "config %{http_code}\n" "https://ielts-vocab.bond/shadow/config.js?cb=$(date +%s)"
curl -s -o /dev/null -w "docs   %{http_code}\n" "https://ielts-vocab.bond/docs/"
curl -s "https://ielts-vocab.bond/shadow/index.html?cb=$(date +%s)" | grep -o 'SHADOW_DATA_VER = "[a-f0-9]*"'
```

期望：`config 200`、`docs 404`、版本戳是当时最新的那个。
（15:45 实测：戳已随课文改动变成 `9a6df72d2c`。它是由 `sections.json` 算出来的，每次改课文都会变，
**别把任何具体值当常量抄**。另外 `config 200` 不足以说明成功 —— 要把内容 `curl` 出来看两个凭证是不是空串，
空串就是构建没吃到环境变量，正确值应长成 `SUPABASE_URL: "https://…supabase.co"`。）

再在浏览器里开跟读站 → 登录一次 → 播一句 → 回 Supabase 控制台 → Table Editor → `user_data`，应能看到你自己那一行，`marks`/`pos` 有内容。**这一步才算云同步真的通了。**

**如果部署红了：** 进部署详情看构建日志（新版把每一步分开显示）。两种最可能：
- 找不到 `node` 或版本太老 → 项目设置里有个 **Node.js 版本** 下拉，选 20/22/24 任一。
- 构建脚本自己报错 → 日志会写「白名单漏了页面引用的文件：xxx」，把那一行发我，我补白名单。
- 部署失败时**站点仍停留在上一个版本**，不会白屏。

> 顺带一提：`netlify.toml` 和 `_redirects` 这两个文件 EdgeOne 完全不认（官方迁移文档要求改写进 `edgeone.json`），它们是死的；我不删是怕你以为我动了别的东西，你想清就清。

---

## 第 3 步：开 RLS（4 张表）

**去哪：** Supabase 控制台 → 项目 → 左侧 **SQL EDITOR** → New query → 把仓库里这段**整份**粘进去 → Run：

```
sql/2026-09-19-user-data-rls.sql
```

这段是幂等的（可反复执行），做四件事：
1. 建 `user_data` 表（`user_id` 主键 + `marks` + `pos` + `updated_at`）—— **这张表目前没有任何地方建它**，而跟读站每天在往里写；
2. 开 RLS，并给「只能读写自己那一行」三条策略；
3. 末尾两条 SELECT 打印四张表现状 —— 你直接把结果截图发我；
4. 一段**注释掉**的可选项：`learning_data` 的现有策略额外要求「profiles 里有我且 role 正常」，一旦注册触发器丢失，用户连自己的学习数据都读不到且前端静默失败。单人使用下这个条件没有安全收益。**默认为不动**，要放宽再取消注释执行。

**确认：** 结果里四张表 `已开rls = true`；策略数为 `user_data` 3、`profiles` ≥1、`learning_data` 1、`app_settings` **0**（这张有意零策略，只有 service_role 能通过 Edge Function 访问）。

顺手在 Supabase 里再确认一件事（我没权限看）：**Authentication → Policies/URL Configuration** 里有没有把 `ielts-vocab.bond` 列进 Redirect URLs。没加的话，邮箱登录链接会跳错地方。

---

## 做完这三步之后还剩什么

- 你的书签/续读位会从「只存在手机浏览器里」变成真的能跨端；清缓存不再归零。
- 上一批留的那个隐患也顺手修了：课文里插入句子会让本地进度序号错位，现在有了顺移账本 + 门禁 + 端到端测试。
- 还没排队的：首屏 2.9MB 瘦身、PWA 离线、URL 路由、两套语音引擎合并、14 处 `alert()`、87 处空 `catch`、已读/未读分母塌缩、阅读站反向排查。

## 我替你做过的事，你可以复核

```bash
git log --oneline -6
node scripts/build_site.mjs            # 本地重建 dist/，会打印清单和缺变量告警
python3 scripts/validate_data.py       # 数据门禁（含新的第 14/15 条）
cd playwright-tests && npm test        # 27 条端到端
```

> 本地跑测试前留意：`playwright-tests` 会自己起两个静态服务，端口 **8931**（shadow）和 **8932**（仓库根，给主站/admin 用）。
> 这两个端口被别的进程占着时它会直接复用 —— 我这次就被自己起的 8932 服务坑过一轮，主站 4 个测试假红。
> 复跑前先 `lsof -nP -iTCP:8931 -iTCP:8932 -sTCP:LISTEN` 看一眼。

---

## 15:45 控制台复核（第 1、2 步已落地，附证据；结论与上面正文有冲突处以本节为准）

1. **第 1 步做完了。** 两条变量在「项目设置 → 环境变量」表里，值显示为掩码，生效范围一栏是「全部环境」；
   生产环境的「编辑环境」弹窗里同样能看到这两条，key 与本地 `config.js` 那份一致（208 字符，`eyJhbGci` 开头，两段点号）。
   注意控制台里那个「新增环境变量」弹窗：变量值用脚本注入 DOM 的方式**不会被 React 收到**（保存键一直是灰的），
   要么真打字，要么人工 ⌘V —— 模拟按键不触发系统粘贴，剪贴板读取权限也被站点了。
2. **第 2 步的配置项做完了。** 控制台原本显示的就是旧值（输出目录 `/`、编译命令与安装命令「未设置」），
   已手动改成 `Other` / `./` / `dist` / `node scripts/build_site.mjs` / `true` 并保存。
   也就是说 `edgeone.json` 确实会覆盖构建行为（14:42 那次就是按它构建的），但**它不回填控制台表单**，
   所以控制台显示旧值不代表没生效 —— 判据是构建日志里的 `[StaticAssetsBuilder] BuildScript:` 和 `[builder] InstallCommand:`。
3. **手动的「新建部署」不切生产版本。** 本次 `dpft2prrewyv`（15:33 触发，20s 成功，日志三步全绿）：
   - 它的**预览地址**（`ielts-vocab-dpft2prrewyv.edgeone.cool`，要先访问一次拿 `eo_token` cookie，直接 curl 会 302）里
     `config.js` **带着真实凭证** → 构建读得到环境变量，第 1 步的值有效。
   - 但 `ielts-vocab.bond` 十几分钟后仍返回 14:42 那个 release 的字节：所有文件 `Last-Modified: 06:42:09 GMT`，
     `--resolve` 到两个真实边缘节点 IP 结果一致，且 `EO-Cache-Status: Cache Miss` + `Age: 0` ——
     **不是本地代理、不是边缘缓存，是生产版本指针没动。**
   - 对照组：14:41 那次是推 commit 的自动部署，bond 二十几秒就切了。
   - 所以：**填完环境变量不用再做别的，下一次推 `main` 时凭证会自动写进 `config.js`；不要为了验证去造空提交。**
4. **白名单已生效**（是 14:42 那次带来的，不是本次）：`/docs/`、`/tools/`、`/playwright-tests/`、
   备份 `sections.json.backup-*` 全部 404；`/index.html`、`/shadow/index.html`、`/admin/index.html`、`/404.html` 200。
5. **`ielts-vocab.bond` 不在这个项目的「域名管理」页里** —— 那儿只有 `ielts-vocab-o4rle2ym.edgeone.cool`（关联环境列 `-`），
   生产环境的「编辑环境」里域名一栏是「暂无域名」。bond 的 CNAME 是 `ielts-vocab.bond.pages.dnsoe8.com`（老 Pages 后缀）。
   它到底挂在哪儿、为什么只跟自动部署走，**没查清**；它是否就是第 3 条的原因，也还没排除。
6. 「工具集 → 清除缓存」报「当前账号下暂无站点」，管不到 Pages/Makers 域名，别用它。
7. Node.js 版本控制台 22.11.0，构建机实际切到 `v22.21.1`；构建脚本零依赖，能跑（本地 13 文件 / 4.26 MB，与线上日志逐项一致）。

### 第 3 步已于 15:55 执行（SQL 与仓库文件 sha256 一致：`628a8414…33faa`）

跑之前先核过：编辑器内容与 `sql/2026-09-19-user-data-rls.sql` 逐字节相同（3214 字符 / 16 条语句）。
Supabase 弹了「包含破坏性操作」的确认框（被 `drop policy if exists` / `revoke all` 关键字触发的通用提醒），
已确认这三条只作用在 `user_data` 上、且紧接着重建同名策略。

跑完在 **Database → Policies** 页核对（结果表格是 canvas，读不到文本，所以用这一页而不是 SQL 结果面板）：

| 表 | RLS | 策略 | 手册预期 |
|---|---|---|---|
| `user_data` | 已开 | **4 条**：`owner all`(ALL) + `read own shadow data`(SELECT) + `write own shadow data`(INSERT) + `update own shadow data`(UPDATE) | 3 条 |
| `profiles` | 已开 | 1 条 `read own profile`(SELECT) | ≥1 ✓ |
| `learning_data` | 已开 | 1 条 `own row only`(ALL) | 1 ✓ |
| `app_settings` | 已开 | 0 条 | 0 ✓ |

两点与手册预期不同，都查清了：

1. **多出的 `owner all` 不是这次建的**，定义是 `to authenticated using (auth.uid() = user_id) with check (auth.uid() = user_id)`，
   和这次三条同一个语义（只能碰自己那一行），没有放宽。它存在说明 **`user_data` 表和它的列在这次之前就有人建好了**
   （列：`user_id` uuid 主键+外键+非空、`marks`/`pos`/`updated_at` 非空 —— 与前端 `upsert` 逐列对得上），
   而且表里**已经有 2 行**。所以「表不存在」这个前提只对了一半：缺的从来是脚本和凭证，不是表。
   这次真正改变的是：`enable row level security` + `revoke all ... from anon`（界面因此显示 `API DISABLED`）。
2. **Auth 的 Redirect URLs 不用加。** `SITE_URL` 已经是 `https://ielts-vocab.bond`，`URI_ALLOW_LIST` 是空串；
   全仓库 `grep redirectTo|redirect_to|emailConfirm` **0 命中** —— 前端从不传自定义跳转地址，GoTrue 只会回到 `SITE_URL`。
   查法：`GET https://api.supabase.com/platform/auth/<ref>/config`，字段是大写的 `SITE_URL` / `URI_ALLOW_LIST`
   （小写的 `site_url` / `redirect_urls` 取不到，会以为没配）。

**仍未通的一件事**：`bond` 上的 `config.js` 里两个凭证还是空串，所以「浏览器登录 → 播一句 → `user_data` 出现自己那行」
这条端到端还没法验。按第 3 条的结论，它会在下一次推 `main` 时自动修好。

### 16:15 用部署预览地址把端到端验完了 —— 链路是通的

不用等推 `main`：那次部署的预览域名带着真凭证，在它上面登录 + 加书签即可验证全链路。
证据（都取自预览页面自己发出的请求，用 `localStorage` 里的 session 回读那一行）：

- `POST /auth/v1/token?grant_type=password` → **200**（登录通）
- `GET /rest/v1/user_data?select=marks,pos,updated_at&user_id=eq.<uid>` → **200**，1 行（RLS 读自己那行通过）
- `POST /rest/v1/user_data?on_conflict=user_id` ×4 → **全 2xx**，无 401/403（RLS 的 INSERT/UPDATE 策略通过）
- 回读：`updated_at = 2026-09-19T08:15:24Z`（北京 16:15:24，就是刚才那一下），`marks` 10 条，最新一条时间戳为刚才

**所以：构建注入 → 环境变量 → 登录 → RLS 策略 → 写库，整条已经成立；`bond` 缺的只有「生产版本切换」这一下。**

做这个测试前先读了 `shadow/index.html:3659` 的 `cloudPullMarks`：登录只做「云端 ∪ 本地」合并，
且合并期间 `_markSyncing = true` 会挡住推送，推送只在改动书签后 debounce 2s 才发（`shadow/index.html:3639`）。
**所以换一个空 localStorage 的域名登录不会把云端书签写成空** —— 这条以后可以放心用（合并结果上限 50 条，云端超过 50 条时才需要另想）。
