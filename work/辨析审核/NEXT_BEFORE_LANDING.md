# 落地前剩余清单（52 组：批次 1 十组 + 批次 2 四十二组）

批次 2 现状（2026-09-20 我复跑，非代理自报）：42 组宽度机检 **0 条超限**、
`check_compare_draft.py` 九份 **PASSED**。三路修复代理各自另交 3 条分歧（见各 FIX 文件末节）。

按顺序做，做完一步勾一步：

1. **长表→宽表**（唯一的硬阻塞）。批次 2 的差异表是 `| 维度 | 词 | 常见搭配 | 用在哪 | 一句话区别 |`
   长表，落地器只吃批次 1A 那种 `| 维度 | 词1 | 词2 |` 宽表。
   `tools/normalize_draft_shape.py` 的这一步**有 bug 不许 --write**：它就地改行却按旧跨度遍历，
   索引漂移，会把「出处清单」误认成差异表。修法＝倒序处理各组 + 只在「差异维度表」小节内找表；
   改完必须先看 dry-run 报告里「组数 N → N」和每组行数对不对，再 --write。
2. **4 处表头「这一句」**残留，门禁 3 第 16 条会拦（卡挂段末、没有"这一句"可指）：
   `2_04` 2 处、`2_08` 1 处、`2_09` 1 处。改成「这一段」或「NNN 同句里」。
   （批次 2 另 6 处已由修复代理清掉。）
3. **回退一处越界改动**：`2_06` 组 37 门禁 2 判 OK，修复代理为留余量把括号例子换成
   `(at the end)`。审核说 OK 就不该动 —— 只回这一处，见 `FIX_2_05_07.md` 分歧 1。
4. 批次 1（1A/1B）补测与 FIX_batch1.md —— 代理在跑，落地前必须齐。
5. `python3 tools/land_compare.py`（dry）→ 看冲突清单（`cite` 一词条带两张表）→ `--write`
   → `scripts/validate_data.py` → `cd playwright-tests && npm test`。
6. 新增一批机检：把"每个组都被量到"写死（打印 量到 N / 应有 M）。
   这一轮两次踩到同一个坑：1B 和 2_04 用 `## 3.` 编号，找组的脚本静默跳过，
   于是"全绿"里有 3 组从没被量过。**漏测比超限危险，因为它不报错。**


## 2026-09-20 23:00 落地后补记（52 组已进 vocab.json，cmp 字段）

已完成：落地器 4 处形状修复 → 52 组 0 错误 → `--write` → validate PASSED（57 张卡）→ 82 E2E 全绿
→ 390px 真机视口看过渲染（维度小标题成块、单侧维度整行合并、无 markdown 死字、无空列）。

**一处要他拍板的实测发现（我没动内容）**：段末默认行**全部渲染成两行**。
容器实测宽 334px、字号 13px、行高 22.1px → 一行只装得下约 25 个「汉字当量」，
而我们一路校准的上限是 40。也就是说 ≤40 这条尺从批次 1A 起就没对应到"一行读完"，
52 条 + 旧 5 条无一例外都是两行。三选一：
① 接受两行（把 PRD §7.2 那句"一行"改成"两行内"，什么都不用重做）；
② 上限压到 ~25，52 条默认行重压一轮（一轮代理活，内容要删）；
③ 折中：字号/行高不动，把默认行改成"词 = 钩子"两行版式（第一行一个词），
   等于承认两行但让它断得整齐。
我倾向 ①（他现在每天看的就是这个高度，两行没挡住读），但这条是口味，不替他定。

> **2026-09-20 深夜 他拍板：①「接受选项 1，更新限制为两行内」** —— 上限 40/30 一个数字都不动，
> 改的是"≤40 对应什么渲染"这句说明。已同步的 5 处（都是文字，非机检逻辑）：
> `tools/width_rule.py` 文件头 + `LINE_CAP` 注释、`scripts/validate_data.py` 第 16 条报错文案
> （原来写"手机上会折行"，现在写"超过两行"，并把 `_width` 从抄本改成 import `width_rule`）、
> `tools/audit_ledger.py`（同样删掉抄本改 import —— 这把尺原来有 4 份抄本，现在只剩 `tools/width_rule.py` 一处）、
> `work/compare_slices/BRIEF.md` §五.10（起草侧照这个写）、`work/compare_slices/AUDIT_BRIEF.md` D 行（审核侧照这个判）、
> `docs/PRD.md` §5.10 一句话文体行、`docs/2026-09-20-学习计划与任务模式重设计-PRD.md` §8.3 判据 2 / §8.5 显示位 / 附录 D。
> 上面 ① 里那句"PRD §7.2"是我写错了节号 —— §7.2 是数据 schema，文体口径在 §5.10（老 PRD）与 §8.3（新 PRD）。
> **对后续 4 批的实际影响**：默认行按"两行读完、不拖第三行"写，别再为了凑单行砍钩子；
> 也**不许**反过来拿"反正两行"当理由往 40 上加字 —— 上限不松这条没变。

**顺手记两条口径决定**（写进台账，别再逐组上报）：
· 2_06 组 3（worklist 37）那处"报告判 OK、修复代理为留余量换了括号例子"**不回退**：
  两个写法（morning summary / at the end）都是同一条卡 ex 里的原文，宽度 40.0 压线是事实，
  换完 37.5 有余量、语义没变，且报告原话就是"建议减字留余量"。已在 FIX_2_05_07.md 点名。
· 1A 组 2 表头与 1B 组 6/7 的引号**不再改回**：改回去只统一了标点、不动任何一条默认行，
  收益是零、风险是把门禁 2 判过 E BLOCK 的"照抄义项"形状再摆回标题。
  1A 组 1/组 3 那两处按 §五.11 改回是另一回事（那里有"当场否定"）。

## 2026-09-21 批次 3（42 组 / slice10–18）全流程记录

回路照旧：9 个起草代理并行 → 4 个独立审核代理并行 → 4 个修复代理并行 → 落地。
落盘物：草稿 `work/辨析草稿/3_01..09.md`、审核 `audit_{a,b,c,d}_b3.md`、修复台账 `FIX_3_{a,b,c,d}.md`。

**门禁 2 战果**：42 组 = BLOCK 1 / MISMATCH 16 / OK 25。BLOCK 只有一处、且是字节级：
`3_02` 的「该段原文」表把数据里的弯撇号 `baby’s` 抄成了半角 `baby's` —— 原文表是照抄运行数据的，
字符被换等于静默改 `sections.json`。16 条 MISMATCH 集中在三件事上：
① **虚报"本书没有"**（`pepper grains`@847、`sacks`@1209、`sitting room`@1712、`box office` 在 office 卡词伙里、
`seat belt` 在 compel/oblige 卡 ex 里）—— 门禁 1 结构上抓不到否定行里的假否定，三轮批次每次都栽在这；
② **计数口径混着写**（"全书只出现一次"其实另有未打标记的一处）；③ **把段主题栏当段落内容**（见下）。

**新查实的课文数据缺陷（要他拍板，我没动数据）**：`sections.json` 章3 的 `paraZh` 是坏数组 ——
段 22–43 的主题栏 = 段 0–21 原样重抄一遍再挂尾缀「 灯下团聚，欢语不断。」
（实测 `paraZh[i+22] == paraZh[i] + 尾缀`，i=0…21 全中；其余 5 章零重复）。
后果：那 22 段的主题栏写的具体事件（买热汤／孩子挥手／已是黄昏）在本段句子里根本不存在，
`3_08` 有 3 组照它写表头，全部被审核判 MISMATCH 并已按句子重写。
口径已写进 `BRIEF.md` §五.12（主题栏不是出处）+ §五.13（计数分「带标记 / 未标记」两档），
下游批次 4–6 不用再逐组上报。**但数据本身要不要修（把章3 上篇那 22 条主题栏换成真按段写的），是另一件事**：
它不影响卡片、只影响"段主题"这个界面元素，改它要他点头。

**门禁 3 撞出来的一条工具漏口（已堵）**：`land_compare.py --check` 报 0 错误、`validate_data.py` 却拦下两组
表头用「这一句／这句」指代（`elegance` 3_03、`entrance` 3_06）—— 同一规则此前只在门禁 3 里，落地器不管，
于是每次都是"写进 vocab.json 才发现"。已把同一条正则加进 `land_compare.py`（硬错误），并就地改掉两条草稿的表头
（改成「725 同句里…」「877 两个词都成立」，只动指代词，没动任何一条默认行）。

**落地后状态**：辨析卡 57 → **99 张**；`validate_data.py` **PASSED**；`land_compare --check` 18 份草稿 / 84 组、
错误 0、载体冲突 0（`declare` 同属三组那位，落地器自动换到 proclaim/announce/affirm，未改内容）；
`SHADOW_DATA_VER` → `d7cc81a7f8`（由落地器写进 `shadow/index.html`）。

## 2026-09-21 批次 4（42 组 / slice19–27）全流程记录

回路同批次 3：9 起草 → 4 独立审核 → 4 修复 → 落地。**门禁 2 战果：BLOCK 3 / MISMATCH 10 / OK 29。**
- BLOCK 三处全是"照抄数据抄错／义项词当钩子"：组 96 默认行把 aid 卡 `m` 的原词「援助」当了钩子中心语（E 项）、
  组 109 原文表把 `regulations` 抄成单数、组 119 声称"strike 本书没有一次当动词"而 `struck` 在 lightning／midnight
  两卡 `ex` 里就是动词 —— 第三处正是门禁 2 自己提的那族"虚报缺失"，被反向抓了回来。
- 审核顺带查出**门禁 1 的一处真缺陷**（不是草稿问题）：标记正则 `[[^]:]+` 不排除 `"` 与换行，会把段落数组开头的字面
  `[[` 当标记起点、一路吞到本章第一句里第一个真标记，**后果是每章第一句（全局 0／339／651／883／1277／1492）
  整句永远查不到**，作者被逼成"不整句引用"。已修（两处 re.sub 合并成 `sec_variants()`，validate 第 16 条 import 它，
  少一份抄本）；回归：标记数 4072 不变、29 份草稿全 PASSED、整句可查率 1827/1833 → **1833/1833**。
- 我新加的"表头不许用『这一句／这句』"硬拦在落地前抓到 **5 组**（4_03 组3、4_05 组2/组3、4_09 组1/组2），
  上一批这类是写进 vocab.json 才被门禁 3 发现。改法一律换成"同句"或点名句号，不动任何默认行。
- 口径新增两条并写进 BRIEF：**§五.14**（worklist 的 `all_synonyms` 与"两卡例句只差词头"打架时按现表出卡、
  同形写进「数据边界」当合法物证、不再逐组提请拍板 —— 定义只是"同义词栏互不互列"的机械位，结构上反映不了例句形状）、
  **§五.15**（任何"本书没有／0 引用／各有自己的卡"式存在性断言必须自己 grep 过 —— 反例可查不到，
  但"有关于有无"的那句话本身必须查得到；`influence` 根本没有卡就是这一族的最新样本）。§五.6 另补"档位错"一例。

## 2026-09-21 批次 5（42 组 / slice28–36）全流程记录

回路同上：9 起草 → 4 独立审核 → 4 修复 → 落地。**门禁 2 战果：BLOCK 4 / MISMATCH 12 / OK 26。**
落地 **40 组**，另 **2 组挂起待他拍**（见下"两件没做的事"）。

- **本片最值得记的一类：items 表整排左移**。组 162 的 `lean`、组 165 的 `invalid` 与 `patient` 三行**只有 6 列** ——
  词性格被「n. 病人；病弱者；adj. 无效的」这种"词性＋义项"混写占掉，后面每格都错一位：`sense` 槽拿到中文钩子、
  `eg` 槽空。门禁 1 只查英文串查不查槽位，**只有人眼按表头数列才看得见**；落地器按槽取数，会直接产出一张
  没有例句、`sense` 写着中文画面的卡。已按报告拆成 7 列，并让修复代理用 `land_compare.cells()` 同口径复算全文档列数。
- **中文造景是门禁 1 的盲区**（新样本）：`patient` 的 `core`「诊室里**挂号**的那位」——"挂号"两份数据 0 处，
  而它整句英文侧全查得到。这类只能靠审核读中文。改成了有出处的「被围着的那位」（nurse/doctor/physician/hospital 四条卡 `ex`）。
- 老毛病继续：`recovery`「本书 0 处」虚报（它有自己的卡、课文 1701 一处）；义项原词漏进默认行两处（「放大」「估价」）；
  附录复述清单与组内标称不一致（`5_08` 的 21.0 应为 19.0）。**计数改一处要连着改的地方**：`5_07` 的 7→8 一共 4 处引用点。
- **审核反过来纠正了审核自己**：audit_b 的专题一说旧 look-verbs 表挂段 1 末是"peep 走 `at` 命中 [5,1]"，
  修复代理复算 `cmpIndex()` 后指出**成因不同**：同 group 只认 `Object.keys(VOCAB)` 里第一份（glimpse 1636 < glance 1637 < peep 1638），
  生效的恰是**没有 `at`** 的那份，于是走"≥2 成员被标出的段"回退 —— 回退结果仍是 [5,1]。**结论对、机制错**，
  草稿按机制写。这条对下次判断"旧表到底显示在哪"有用：别按 `at` 猜，按键序猜。

### 两件没做的事（要他拍，落地器已不会静默处理）

1. **组 148 glance / glimpse**：两个成员的卡位都被线上旧 look-verbs 表占着（peep 也带一份），
   落地器老实报冲突 → 这组**没落**。要么撤旧表，要么承认"一词只能带一张表"这条限制卡住了新内容。
2. **组 150 curse / swear**：比 ① 更危险 —— 新表 slug `curse-swear` 与线上旧 `cmp.group` **一模一样**，
   落地器把它当"自己上一轮落的"放行，于是**原地静默覆盖**，而且只覆盖载体那一张（curse）；
   swear 卡上那份旧拷贝留着，**点开 swear 词卡看到的还是旧表**，而旧表里「swear at sb＝骂某人」全书 0 处
   `at`+人、「swear to do＝发誓做」整库唯一出处就是那份旧表自己（自证循环）。
   这组同样**没落**。修法二选一：先手动清/改 curse、swear 两卡的旧 `cmp` 再落新表（干净）；
   或把标题成员顺序换成 `swear / curse` 让冲突检测报出来（绕过检测，不推荐）。

### 本片为这两件事给落地器加的两道闸（已回跑 126 组已落地草稿，0 误报）

- `--skip <slug>`：显式挂起某组，打印"已排除"；拼错 slug 会打 `!! 本次草稿里没有这个 slug`。
  有了它，一批里卡住 1 组不再等于整批 40 组落不了。
- **`WARN ⚠覆盖`**：slug 撞上线上已有表但**内容不同**时点名报警，且这类提醒**不参与 20 行截断**
  （以前会被 85 条"eg 还有第二条书证"的噪音挤掉，等于没报）。
- 另：BRIEF **§五.16** 定死「该段原文表只粗本组成员」—— 批次 5 为这条来回改了三轮（1719 的 `surprising` 挂了词头
  但不属本组；1716/1718 的 `grew` 既没卡也没标记却加着粗）。这张表不落地，只影响人复查，但口径漂移会一直返工。

**落地后状态**：辨析卡 141 → **181 张**（178 个 group，多出的 3 份拷贝是 look-verbs×3 与 curse-swear×2 这两组旧表）；
`validate_data.py` **PASSED**；`npm test` **82 全绿**；`SHADOW_DATA_VER` → `d0292d39a7`。
门禁 3 新增 3 条**非阻断告警**："辨析卡 glance／glimpse／peep 的 summary 里没有「简单记：」那半句 → 段末默认行只能整段显示（会超宽）" ——
也是那三张旧表遗留，和上面 ① 是同一件"旧表要不要清"的事。
