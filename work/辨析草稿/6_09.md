# 辨析卡草稿 · 批次 6 切片 45（2 组 / worklist_idx 218–219）

> 小结：切片 `work/compare_slices/slice45.json` 的 2 组（worklist_idx 218 / 219）**全部写完，两组都是四成员组**（此前 219 组里最多三成员）；门禁 `python3 tools/check_compare_draft.py work/辨析草稿/6_09.md` → **PASSED（全部英文串可回溯，0 条查不到）**；另用同一套 units 逻辑做了"不跳过否定行"的严格复查，剩下 20 条查不到的全部是「不写的」段与 §五.13 计数行里有意举的反例（reduction / dwindling / diminishing / peer at / gazing / peering / contemplation / stares / `gaze into the distance` 那一族），逐条回两份数据 grep 确认全书 0 命中。宽度全部用 `tools/width_rule.py` 那把尺实测：组 1 默认行 39.5、总结句 28.0；组 2 默认行 38.0、总结句 29.0。
> 提请拍板 2 处（都是"载体被旧表占了"这一族，内容本身不必退回）：**组 1**（decrease / diminish / dwindle / reduce）成员 decrease 卡上已挂 `decrease-reduce` 一张（`at`＝[3,47]，就是本组同一个段），本组只能落到 diminish / dwindle / reduce 三张空卡上；**组 2**（contemplate / gaze / peer / stare）四张卡都没有 `cmp`，但 peer 是线上那份手写旧表 `look-verbs`（挂在 glance / glimpse / peep 三张卡上、`at` 那份在 peep＝[5,1]，也正是本组同一个段）里的第四个 item，旧表已把 glance / glimpse / peep 的对照讲完、还顺手交代了 peer 的名词档，本组一律不重讲。
> 未动任何产品代码 / 数据文件（`shadow/data/vocab.json`、`shadow/data/sections.json`、`shadow/index.html` 只读未改，章3 段22–43 那个坏主题栏没碰），未碰别人的草稿（6_01–6_08、5_01–5_09 那些 md）与 `work/辨析审核/`，未跑 `tools/land_compare.py`（干跑与自查两种模式都没跑），未做任何 git 写操作。

---

## 组1 · decrease / diminish / dwindle / reduce —— 减少 ｜ `all_synonyms`: false ｜ 段 [3,47] ｜ 句 1162 / 1163 / 1164 / 1165

**共现位置**：1 段 4 句 —— 章3（社会与规则）段 47（全局 1162–1167），四个成员**各占一句、连成四句**：1162 dwindle（存下的硬币）、1163 decrease（生意那一段）、1164 diminish（每天来买货的人）、1165 reduce（后屋的浪费）。**主锚点 = 章3 段47**（`paraZh`：生意下滑后他省吃俭用，想办法稳住客流。——本段六句都撑得住；`subheads[3][44]`：下篇·街角小店）。卡挂段 47 末。
主锚点 = 章3 段47

> **同段已有三张表**（口径 9）：段 47 段末现在挂着 `increase-increment`（at 在 increase）、`growth-increase`（at 在 growth）、`decrease-reduce`（at 在 decrease）三张，本组是第四张。旧的那张 `decrease-reduce` 已经把 decrease / reduce 的"名词位 vs 及物动词位""挂不挂 `in`""句子里有没有人动手""中文给的是'下滑'还是'减少'"四格讲完了 —— 本组不复述这条线，重心全在旧表没碰过的 dwindle（1162）与 diminish（1164）身上：它两补的是**掉的东西是数得出来的还是摸不着的**、**句子带不带"一天天"这个过程**两格。

**这一段在讲什么**：小店的账本往下走的那几天——他盼过物价回落，可存下来的硬币一天天少（1162）；为了撑过生意的那一截下滑，他缩小摊子、晚上早早关门（1163）；希望在往下落，每天来买货的人也像越来越少（1164）；他手上能动的只有两头：后屋的浪费往下压、货架上的鲜果往上加（1165）；客人每多一点点都像寒冬后的暖意生长（1166）；成本还在往上爬，所以他不敢把店面扩到隔壁（1167）。（段主题栏「生意下滑后他省吃俭用，想办法稳住客流」与本段句子相合；章3 段22–43 那批坏主题栏与本段无关。）

**该段原文（章3 段47，全段 6 句）**

| 全局句 | 英文原句（只粗本组四个成员） | 中文译句 |
|---|---|---|
| 1162 | He had hoped for price deflation but saw his stored coins **dwindle** day by day. | 他盼着物价回落，可存下的硬币一天天变少。 |
| 1163 | To weather the **decrease** in business he had to downsize, closing earlier in the evening. | 为了撑过生意的下滑，他只好缩小摊子，晚上早早关门。 |
| 1164 | He watched his hopes decline as the daily stream of customers seemed to **diminish**. | 他看着自己的希望在心里一点点落下去，每天来买货的人也像越来越少。 |
| 1165 | He tried to **reduce** waste in the back room and increase fresh fruit on the shelves. | 他努力减少后屋的浪费，在货架上多摆新鲜水果。 |
| 1166 | Every tiny increment in visitors felt like warm growth after the long cold spell. | 客人每多一点点，都像寒冬过后的暖意生长。 |
| 1167 | Costs continued to escalate so he dared not expand the shop to the next door. | 成本不断攀升，他不敢把店面扩到隔壁。 |

**计数（§五.13 两档分开列）**：四个成员书内都是**带标记 1 处 ＋ 未打标记 0 处** —— dwindle 1162、decrease 1163、diminish 1164、reduce 1165。变形另算一档：`dwindles` 只在 dwindle 卡 `ex` 露过 1 次、`reduces` 只在 emission 卡 `ex` 露过 1 次，两档分开；过去式与名词形（reduced / decreased / diminished / dwindled / reduction）在课文与卡面都查不到，0 处。卡侧厚薄极不均：reduce 被 4 张别的卡的 `ex` 当干活动词、被 7 张卡的词伙借用；dwindle 卡 `note` 整条为空（既无词伙段也无同义词栏）。

### 表头一句（title）

> 连着四句、四个"少"字，掉的东西却一类一个样：1162 是数得出来的硬币，1163 是账上落下来的那一截（它还是个名词），1164 是摸不着的客流和希望，1165 是他自己伸手去压的浪费——worklist 给的共同义项「减少」这四个汉字本书译文里只落在 1165 那一处，另外三处各写成了「变少」「下滑」「越来越少」：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| decrease | /dɪˈkriːs/ | n. / v. | n. 减少；v. 减少；降低 | 账上落下来的那截 | 生意那一段往下走（1163）；上个月降雨量（卡 `ex`）；卡词伙那一条课文没用到 | `the decrease in business`（课文 1163） |
| diminish | /dɪˈmɪnɪʃ/ | v. | v. 减少；削弱；减损 | 摸不着的那样东西淡下去 | 每天来买货的人（1164）；午夜后的噪音（卡 `ex`）；动机（卡词伙） | `the daily stream of customers seemed to diminish`（课文 1164） |
| dwindle | /ˈdwɪnd(ə)l/ | v. | v. 缩小；减少 | 一天一天往下漏 | 他存下的硬币（1162）；夏天池塘里的水（卡 `ex`） | `his stored coins dwindle day by day`（课文 1162） |
| reduce | /rɪˈduːs/ | v. | v. 减少；降低；减量 | 他伸手压下去的那样东西 | 后屋的浪费（1165）；债、无聊、负担、焦虑（卡词伙 + 别的卡词伙） | `reduce waste in the back room`（课文 1165） |

### 默认那一行（实测宽度 39.5 / ≤40）

```
decrease = 那截数，diminish = 噪音变弱，dwindle = 硬币一天天少，reduce = 他动手
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 掉下去的是哪类东西 | decrease | `the decrease in business`（1163）、`a decrease in rainfall`（卡 `ex`） | 说"少了多少"那件事本身 | 它管的是一个**数**，落在生意、降雨这类能记账的东西上 |
| 掉下去的是哪类东西 | diminish | `the daily stream of customers seemed to diminish`（1164）、`The noise will diminish after midnight.`（卡 `ex`）、`diminish motivation`（卡词伙） | 说一样摸不着的东西淡了：客流、希望、噪音、动机 | 本书 2 处 + 卡上 2 条，宾语位置**没有一条是数得出来的存货** |
| 掉下去的是哪类东西 | dwindle | `his stored coins dwindle day by day`（1162）、`The water in the pond dwindles in summer.`（卡 `ex`） | 说一堆原本摆在眼前的存货：硬币、塘里的水 | 它两处都是**能指着说"就这些"**的东西，本组只有它带实物 |
| 掉下去的是哪类东西 | reduce | `reduce waste in the back room`（1165）、`reduce debt`、`reduce boredom`（卡词伙）、`reduce the burden`（burden 卡词伙）、`reduce anxiety`（anxiety 卡词伙） | 说他手里那样东西：浪费、债、无聊、负担、焦虑 | 它是本组唯一一条**动词后面直接拖宾语**的（不像 decrease 那样把东西摆到 `in` 后面；`reduce waste in our daily life` 里那个 in 接的是"在哪儿"），旧表已把这一格讲透，本表只补一句：它词伙的对象全是要人去处理的东西，不是自己走掉的存货 |
| 句子带不带"一天天"这个过程 | decrease | 1163 全句只给结果：`To weather the decrease in business he had to downsize`；卡 `ex` 也只给结果 `There was a decrease in rainfall last month.` | 事后翻账本时用它 | 本组四句里只有它这一处**既没写过程也没写打算**，那一截已经掉完了，句子只留着结果让人去撑 |
| 句子带不带"一天天"这个过程 | diminish | 1164 用的是 `seemed to diminish`（像是越来越少）—— 主语那条 `stream of customers` 本身是"流水"，`He watched his hopes decline` 同句并列 | 说不清是真少了还是感觉少了 | 它带的是**观察**（watched / seemed），本书没有一处给出确切数目 |
| 句子带不带"一天天"这个过程 | dwindle | 1162 全句 `his stored coins dwindle day by day` —— `day by day` 就挂在它后面 | 一天一天漏，看得见进度 | 本组唯一一处带**进度字样**的书证就是它（`day by day`），落地时这一格别让它替别人背书 |
| 句子带不带"一天天"这个过程 | reduce | `He tried to reduce waste in the back room`（1165）、`We need to reduce waste in our daily life.`（卡 `ex`） | 说有人着手去处理 | 它带的是**尝试**（tried to）而不是时长；本组四句里只有它前半句写着"想办但还没办成" |
| 这一处中文各落在哪个字上 | decrease | 1163 译「生意的下滑」（`sentZh`） | 中文先给"下滑"这个名词时想到它 | 本组四句译文四个样：下滑 / 变少 / 越来越少 / 减少，四张卡都没共用同一个中文词 |
| 这一处中文各落在哪个字上 | diminish | 1164 译「每天来买货的人也像越来越少」 | 中文用"越来越…"时对应它 | 「减少」这两个字本书全段只给了 reduce；diminish 那处译文里连"少"字都带的是"越来越" |
| 这一处中文各落在哪个字上 | dwindle | 1162 译「存下的硬币一天天变少」 | 中文说"一天天变少"时对应它 | 它的中文和它的英文一样都带过程（一天天），是本组中英最贴的一对 |
| 这一处中文各落在哪个字上 | reduce | 1165 译「他努力减少后屋的浪费」——「减少」二字本书译文只在这里出现过（另一处是章1 句 437，与本组无关） | 中文直接写"减少"时 | 它是本组唯一一处**卡上义项与课文译文用同一个词**的，其余三处都得换字 |

### 总结句（实测宽度 28.0 / ≤30）

> decrease 记那个数，reduce 记那双手，其余两个记怎么没的。

### 出处清单

- `He had hoped for price deflation but saw his stored coins dwindle day by day`、`his stored coins dwindle day by day`、`dwindle day by day`、`day by day`、`stored coins`、译文「他盼着物价回落，可存下的硬币一天天变少」 —— 课文第 1162 句（章3 段47 第 0 句）及 `sentZh`。dwindle 书内唯一一处带标记书证。
- `To weather the decrease in business he had to downsize, closing earlier in the evening`、`the decrease in business`、`To weather the decrease in business`、`he had to downsize`、`closing earlier in the evening`、译文「为了撑过生意的下滑，他只好缩小摊子，晚上早早关门」 —— 课文第 1163 句（第 1 句）及 `sentZh`。decrease 书内唯一一处带标记书证（旧表 `decrease-reduce` 用的也是这一句）。
- `He watched his hopes decline as the daily stream of customers seemed to diminish`、`the daily stream of customers seemed to diminish`、`seemed to diminish`、`He watched his hopes decline`、`stream of customers`、译文「他看着自己的希望在心里一点点落下去，每天来买货的人也像越来越少」 —— 课文第 1164 句（第 2 句）及 `sentZh`。diminish 书内唯一一处带标记书证；同句的 decline 是另一张卡（`m` 写的是「v. 下降；n. 衰退」），按硬约束 2 不进成员。
- `He tried to reduce waste in the back room and increase fresh fruit on the shelves`、`reduce waste in the back room`、`He tried to reduce waste`、`in the back room`、`fresh fruit on the shelves`、译文「他努力减少后屋的浪费，在货架上多摆新鲜水果」 —— 课文第 1165 句（第 3 句）及 `sentZh`。reduce 书内唯一一处带标记书证（旧表用的也是这一句）。
- `The water in the pond dwindles in summer.` / 夏天时，池塘里的水逐渐减少。 —— dwindle 卡 `ex` / `exZh`（表里写作 `dwindles in summer`、`in the pond`，均为其连续子串）。该卡 `note` 为空，无同义词栏、无词伙段。
- `The noise will diminish after midnight.` / 午夜后噪音会减少。 —— diminish 卡 `ex` / `exZh`（表里写作 `after midnight` 为其连续子串）。
- `diminish motivation` —— diminish 卡 `note` 词伙段（本书课文 0 处，只作卡上现成块列出）。
- `There was a decrease in rainfall last month.` / 上个月降雨量减少了。 —— decrease 卡 `ex` / `exZh`（表里写作 `a decrease in rainfall`、`There was`）。
- `decrease life satisfaction` —— decrease 卡 `note` 词伙段（本书课文 0 处；它是 decrease 卡上唯一的动词框架）。
- `We need to reduce waste in our daily life.` / 我们需要减少日常生活中的浪费。 —— reduce 卡 `ex` / `exZh`（`reduce waste` 与课文 1165 同串但是两句不同的话，按 §五.8 各算 1 条）。
- `reduce debt`、`reduce waste`、`reduce boredom` —— reduce 卡 `note` 词伙段（本书课文只有 `reduce waste` 那一处真出现过）。
- `reduce the burden`（burden 卡词伙）／`reduce anxiety`（anxiety 卡词伙）／`reduce operating costs`（operate 卡词伙）／`reduce traffic accidents`（accident 卡词伙） —— 4 张别的卡的词伙段，本表只引其中 2 条；`reduce debt` 又见 debt 卡词伙（与 reduce 卡同串，不另计）。
- `The government is working to reduce illiteracy in rural areas.`（illiteracy 卡 `ex`） —— 报备 reduce 在卡侧被借用作干活动词的一例；reuse / campaign / emission 三张卡的 `ex` 也各借了一次，本组表里不重复列。
- `Every tiny increment in visitors felt like warm growth after the long cold spell`（1166）、`Costs continued to escalate so he dared not expand the shop to the next door`（1167） —— 本段其余两句，只作背景；increment / growth / escalate / expand 都不是本组成员（硬约束 2），也不粗。
- 「同义词：cut, decline, diminish, reduce, deprive」（decrease 卡）／「同义词：cut, decrease, decline」（diminish 卡）／「同义词：alleviate, cut, decline, decrease, deprive」（reduce 卡） —— 三卡 `note` 同义词栏原文；dwindle 卡 `note` 为空。cut / alleviate / deprive / decline 按硬约束 2 不进成员。
- 义项「n. 减少；v. 减少；降低」「v. 减少；削弱；减损」「v. 缩小；减少」「v. 减少；降低；减量」 —— 四卡 `m`；「生意下滑后他省吃俭用，想办法稳住客流」—— 段 47 `paraZh`；「下篇·街角小店」—— `subheads[3][44]`。
- 门禁核对：见文件头小结（本组所有反引号内英文串已跑 `tools/check_compare_draft.py`）。

### 数据边界与判定：能出卡，但本组是"接在旧表后面补两格"

- **载体（本组第一条要报备的事实）**：`shadow/data/vocab.json` 里成员 decrease 卡已经挂着 `cmp`，`group` 是 `decrease-reduce`、`at` 是 [3, 47] —— 正是本组的主锚点段；reduce 与 diminish、dwindle 三张卡都**没有** `cmp`（grep 过全库：带 `decrease-reduce` 这张表的卡只有 decrease 一张，reduce 只是它 `items` 里的第二个 w）。所以本组落地时 `at` 那份拷贝只能落在 diminish / dwindle / reduce 三张空卡之一，落在 decrease 卡上就成了"一段末两张表挤在同一张卡"。**选谁当载体是真人要拍的事**，本组内容不因载体改一个字。
- **同段已有三张表（§五.9）**：段 47 段末现有 `increase-increment`、`growth-increase`、`decrease-reduce` 三张（`at` 各在 increase / growth / decrease 上）。本组是第四张，切入角按 §五.9 各写各的：旧表只讲 decrease / reduce 的句法位，本表讲**掉的东西是哪一类**与**带不带过程**，decrease / reduce 两员在本表里只以新角出现。
- **`all_synonyms: false` 有据**：判定式是"组内每一对都要在彼此的同义词栏里互列"。本组两处不满足 —— ① dwindle 卡 `note` 整条为空，谁也互列不上；② diminish 栏只有 `cut, decrease, decline`、reduce 栏只有 `alleviate, cut, decline, decrease, deprive`，**diminish 与 reduce 两栏都没写对方**。decrease 与 diminish、decrease 与 reduce 两对确实互列（旧表据此标的是 true），所以 false 只是被那一对和空卡拖下来的，不表示"不同义"，按 §五.14 不构成退回理由。
- **共享义项「减少」名不副实的一半（§五.6）**：worklist 那个「减少」取自四卡 `m` 的交集，而本书这一段把它译成四个样 —— 「变少」（1162）／「下滑」（1163）／「越来越少」（1164）／「减少」（1165）。「减少」这两个字**全书译文里只出现 2 处**（章1 句 437「友情减少了心理摩擦」、本组 1165），本段只落实在 reduce 一侧。所以标题只说"各掉哪一类东西"，不宣称"本段四词都表减少"，「减少」及其余义项原词（降低 / 削弱 / 减损 / 缩小 / 减量）一律待在 `sense` 槽，core、默认行、总结句里都不出现。
- **同源合并（§五.8）后的独立文本**：dwindle 侧 = 课文 1 + 卡 `ex` 1 = **2 条**（它卡 `note` 为空，是四侧最薄的一族）；diminish 侧 = 课文 1 + 卡 `ex` 1 + 卡词伙 1 = 3 条；decrease 侧 = 课文 1 + 卡 `ex` 1 + 卡词伙 1 = 3 条；reduce 侧 = 课文 1 + 自己卡 `ex` 1 + 自己卡词伙 3（其中 `reduce waste` 与卡 `ex`、课文 1165 是同串不同句，三处并到 1 条来读）+ 别的卡借用的 10 条（`ex` 4 条：reuse / campaign / emission / illiteracy；词伙 7 条：burden / debt / aggressive / diversity / anxiety / operate / accident，其中 debt 那条与自己卡词伙同串、不另计）。四侧课文各只 1 处，是本切片最"平"的一组：厚薄差全在卡侧，不在课文。
- **计数结论、不是禁令**：「dwindle 只数得出来的存货」「diminish 不接数得出来的宾语」来自 2 条与 3 条的分布，样本极小；能写出的否定句只有这几条，且都逐字 grep 过两份数据：本书没有一处 `diminish` 后面直接接名词（课文那一处是 `seemed to diminish` 这种无宾语写法，卡上唯一的宾语是词伙 `diminish motivation`）、没有一处 `dwindle` 把掉的东西摆在 `in` 后面（它唯一带 in 的是 `dwindles in summer`，那个 in 接的是时间）、没有一处 reduce 站在 the / a 后面当那个数（旧表已声明过这条，本表沿用不另证）、`reduction` / `dwindling` / `diminishing` 全书 0 处。**不许**把这些升级成"diminish 不能接宾语"这类通则。
- **不写的**：「decrease 偏书面、reduce 偏口语」（四卡 `note` 无语域标注）、「dwindle 只能用于人以外的东西」（它两处宾语确实都不是人，但 2 条撑不住这条规则）、「diminish 还有'贬低某人地位'这档」（卡 `m` 只到「削弱；减损」）、`reduce by 30%`、`on the decrease`、`dwindle away`、`diminish to nothing`、`reduced to tears` 这类本书没有的写法一律不列，也不造句。
- 提请拍板（本组 1 条）：**载体**。decrease 卡位已被 `decrease-reduce` 占了 `at`＝[3,47]，本组要落地就得换到 diminish / dwindle / reduce 三张空卡之一（三张都空、都合规）；换到哪一张、以及段 47 末一次挂四张表的排布，是渲染侧的事，本组内容已按"不复述旧表"写好。

---

## 组2 · contemplate / gaze / peer / stare —— 凝视 ｜ `all_synonyms`: false ｜ 段 [5,1] ｜ 句 1500 / 1501

**共现位置**：1 段 2 句 —— 章5（身体与时间）段 1（全局 1498–1503），四个成员**挤在相邻两句里、两句各两个**：1500 是 gaze（名词，他那道目光）与 peer（动词，往小雨那头看），1501 是 contemplate 与 stare（两个动词，一个对着星星、一个对着夜空）。**主锚点 = 章5 段1**（`paraZh`：赶公交上学，利奥立志今天准时努力。——本段六句撑得住：1498–1501 是赶公交那一路，1502–1503 是"立志"那两句；`subheads[5][0]`：上篇·备考的一天）。卡挂段 1 末。
主锚点 = 章5 段1

> **同段已有两张表**（口径 9）：段 1 段末现在挂着 `look-verbs`（`at` 在 peep）与 `oath-vow`（`at` 在 oath）两张，本组是第三张。`look-verbs` 那张的四个 item 是 glance / glimpse / peep / **peer** —— 它已经把"快看 vs 瞥见 vs 偷看 vs 费力辨认"这条线讲完，还顺手在 summary 里交代了 peer 的名词档（同辈、`peer pressure`）。**本组一律不重讲那四个词的对照，也不重讲 peer 的"同辈"**：本组只讲自己这四个词，peer 只出现在"它后面挂 `through`"这一格里。

**这一段在讲什么**：利奥等公交那几分钟和前一天夜里——低矮的阳光刺眼，可他很快瞥见了公交车（1498）；他飞快看一眼手表、又透过树丛朝长路那头偷看（1499）；他的目光稳稳停在路面上，同时努力透过清晨的小雨往前看（1500）；再往前一句是头一天夜里他对着繁星想事情、对着夜空看得出神（1501）；然后是立下誓言与庄重的誓约（1502）、许下承诺与吹口哨的人轻声哼唱（1503）。（段主题栏「赶公交上学，利奥立志今天准时努力」与本段句子相合；章3 段22–43 那批坏主题栏与本段无关。）

**该段原文（章5 段1，全段 6 句）**

| 全局句 | 英文原句（只粗本组四个成员） | 中文译句 |
|---|---|---|
| 1498 | The low sun sends a sharp glare but Leo gets a quick glimpse of the bus. | 低矮的阳光有些刺眼，但利奥很快瞥见了公交车。 |
| 1499 | Leo takes a quick glance at his watch and has a peep down the long road through the trees. | 利奥飞快地看了一眼手表，又透过树丛朝长路那头偷看了一下。 |
| 1500 | His steady **gaze** stays on the road while he tries to **peer** through light morning rain. | 他目光稳定地看着路面，努力透过清晨小雨凝视。 |
| 1501 | He **contemplated** the stars and **stared** at the night sky. | 他沉思着天上的繁星，注视着夜空。 |
| 1502 | An oath was taken and a solemn vow made silently. | 他立下誓言，又默默许下庄重的誓约。 |
| 1503 | A pledge was given and the whistler sang softly. | 他许下承诺，吹口哨的人轻声哼唱。 |

**计数（§五.13 两档分开列）**：四个成员书内都是**带标记 1 处 ＋ 未打标记 0 处** —— gaze 1500、peer 1500、contemplate 1501、stare 1501（1501 里 surface 是 `contemplated` / `stared`，词头仍登记为 contemplate / stare）。除这四外，1833 句里再没有一处这四个词的未标写法；`stares` / `gazing` / `peering` / `contemplation` 这类变形在课文与卡面都查不到，0 处。卡侧极不均：stare 与 contemplate 两张卡的 `note` 都是空的（连词伙段都没有），peer 卡上 `ex` 与词伙**两条全在"同辈"那一档**，它"看"这一档全书只有 1500 一处。

### 表头一句（title）

> 这一段四个词都算"看"，可本书译文里「凝视」这两个字只给了 1500 的 peer 一处：同句的 gaze 被译成"目光稳定地看着路面"、还是个名词；1501 那半句给 contemplate 的是「沉思着天上的繁星」、给 stare 的是「注视着夜空」——worklist 那个共同义项「凝视」是四张卡 `m` 的交集，本书一处都没按这四个汉字写齐过：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| contemplate | /ˈkɑːntəmpleɪt/ | v. | v. 考虑；凝视；思考…的可能性 | 心里在掂量那样东西 | 1501 那晚的繁星；卡 `ex` 里那件辞职的事 | `He contemplated the stars and stared at the night sky`（课文 1501） |
| gaze | /ɡeɪz/ | n. / v. | n. 凝视；v. 注视 | 停在那儿的一道目光 | 1500 停在路面上的那道目光；卡 `ex` 落在日落上；卡词伙是屏幕 | `His steady gaze stays on the road`（课文 1500） |
| peer | /pɪr/ | v. / n. | v. 凝视；n. 同辈；同等的人 | 隔着雨往前找 | 1500 清晨那层小雨；卡上两条都是同辈那档 | `tries to peer through light morning rain`（课文 1500） |
| stare | /ster/ | v. | v. 凝视；盯着看 | 忘了收的那一眼 | 1501 整片夜空；卡 `ex` 整晚对着月亮 | `stared at the night sky`（课文 1501） |

### 默认那一行（实测宽度 38.0 / ≤40）

```
contemplate = 心里掂量，gaze = 停住的目光，peer = 透过小雨看，stare = 看直了
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段它是名词还是动词 | contemplate | `He contemplated the stars`（1501，后面直接拖 the stars） | 说人对着那样东西想了很久 | 本段中文给它的是「沉思着天上的繁星」——眼睛那层几乎没写，全在脑子里 |
| 本段它是名词还是动词 | gaze | `His steady gaze stays on the road`（1500，前面有 His、后面挂 stays） | 说"那道目光"停在哪儿 | 本组四张卡里**只有它在本段是名词**（`His steady gaze`），换成动词就得整句改写 |
| 本段它是名词还是动词 | peer | `he tries to peer through light morning rain`（1500，跟在 tries to 后面） | 说雨里、雾里往前看 | 它是本段唯一被"努力"托着的（`tries to`），中文给了全段唯一一处「凝视」 |
| 本段它是名词还是动词 | stare | `stared at the night sky`（1501）、`He stared at the moon all night.`（卡 `ex`） | 说人对着一样东西看住了 | 本段中文给它的是「注视着夜空」，四句译文四个样，谁都没跟谁撞字 |
| 眼睛后面挂什么 | contemplate | 两处都不挂介词，后面直接接东西：`contemplated the stars`（1501）、`He contemplates quitting his job.`（卡 `ex`，接的是 quitting） | 后面接"想的那件事" | 本组只有它**两处都不带任何介词**；书里没有一处 contemplate at / on |
| 眼睛后面挂什么 | gaze | 本段挂 on：`gaze stays on the road`（1500）；卡 `ex` 挂 fixed on：`His gaze fixed on the sunset.`；卡词伙挂 at：`gaze at screens` | 名词时挂 on，作动词时挂 at | 它是本组唯一"两档各有各的介词"的：on 那两处本书真用了，at 那一条只在卡上 |
| 眼睛后面挂什么 | peer | 本书唯一一处挂 through：`peer through light morning rain`（1500） | 后面接"隔着什么东西看" | 本书没有一处 `peer at`（全库 0 命中）——这一格只有 through 一条书证，落地时别替它补 at |
| 眼睛后面挂什么 | stare | 两处都挂 at：`stared at the night sky`（1501）、`He stared at the moon all night.`（卡 `ex`） | 后面接"盯住的那样东西" | 本组最规整的一侧：课文与卡上同一个框架，只差宾语是夜空还是月亮 |
| 卡上另一档在本书哪里活着 | contemplate | 卡 `m` 第一档「考虑」真活着：`He contemplates quitting his job.` / 他正在考虑辞去工作（卡 `ex` / `exZh`）；`note` 整条为空，无词伙段可引 | 想说"在盘算一件事"时用它 | 它卡上三档，本书课文那一处（1501）中文落在「沉思」上，"考虑"这档只活在卡上那一句里 |
| 卡上另一档在本书哪里活着 | gaze | 词伙 `gaze at screens`（卡 `note`，本书课文 0 处）；卡 `ex` 走的是名词档 | 想说"盯屏幕太久"时用它 | 本组唯一带词伙段的一侧，可那条本书一次都没用过，属"卡上现成块" |
| 卡上另一档在本书哪里活着 | peer | 卡 `ex` `He feels comfortable talking to his peers.` 与词伙 `peer pressure` **两条都在同辈档**；同串又见 pressure 卡（`peer pressures`）、negative 卡（`negative peer pressure`）、conform 卡（`conform to peer behaviour`） | 想说"同龄人、同伴压力"时用它 | 它"看"这一档书证全书只 1 处（1500），卡面反倒全在同辈那档 —— 同辈这条旧表已交代过，本表不复述 |
| 卡上另一档在本书哪里活着 | stare | 卡 `note` 整条为空（既无词伙段也无同义词栏），书证只有 1501 那处 + 卡 `ex` 那一句 | 想说"盯得人不自在"时用它 | 本组最薄的一侧之一：全部书证 2 条，且两条都是"整晚/夜空"这种长时间写法 |

### 总结句（实测宽度 29.0 / ≤30）

> contemplate 在掂量，gaze 停住，peer 透过雨，stare 看直了。

### 出处清单

- `His steady gaze stays on the road while he tries to peer through light morning rain`、`His steady gaze stays on the road`、`steady gaze`、`gaze stays on the road`、`stays on the road`、`he tries to peer through light morning rain`、`tries to peer through light morning rain`、`peer through light morning rain`、`through light morning rain`、译文「他目光稳定地看着路面，努力透过清晨小雨凝视」 —— 课文第 1500 句（章5 段1 第 2 句）及 `sentZh`。gaze 与 peer 各自书内唯一一处带标记书证；同句的 steady 是另一张卡，不进成员、不粗。
- `He contemplated the stars and stared at the night sky`、`He contemplated the stars`、`contemplated the stars`、`stared at the night sky`、`the night sky`、译文「他沉思着天上的繁星，注视着夜空」 —— 课文第 1501 句（第 3 句）及 `sentZh`。contemplate 与 stare 各自书内唯一一处带标记书证；同句的 night 也打了词头标记，但它不属本组，按 §五.16 不粗、不进成员。
- `His gaze fixed on the sunset.` / 他的凝视固定在日落上。 —— gaze 卡 `ex` / `exZh`（表里写作 `fixed on`、`on the sunset` 均为其连续子串）。这条与课文 1500 **不是同一条文本**（两句都写作 `His ... gaze ... on ...` 的框架，但宾语一处是 road、一处是 sunset），各算一侧书证、不并成一条。
- `gaze at screens` —— gaze 卡 `note` 词伙段（本书课文 0 处，只作卡上现成块列出）。
- `He feels comfortable talking to his peers.` / 他觉得和同辈交谈很自在。 —— peer 卡 `ex` / `exZh`（表里写作 `talking to his peers` 为其连续子串）。
- `peer pressure` —— peer 卡 `note` 词伙段；同串另登记在 pressure 卡 `note`（`peer pressure`、`peer pressures`、`economic pressure`）、negative 卡 `note`（`negative peer pressure`）、conform 卡 `note`（`conform to peer behaviour`）—— 跨卡同串，按 §五.8 算 1 组文本。
- `He contemplates quitting his job.` / 他正在考虑辞去工作。 —— contemplate 卡 `ex` / `exZh`（表里写作 `quitting his job` 为其连续子串）。该卡 `note` 为空。
- `He stared at the moon all night.` / 他整晚凝视着月亮。 —— stare 卡 `ex` / `exZh`（表里写作 `stared at the moon`、`all night` 均为其连续子串）。该卡 `note` 为空。
- `The low sun sends a sharp glare but Leo gets a quick glimpse of the bus`（1498）、`Leo takes a quick glance at his watch and has a peep down the long road through the trees`（1499） —— 只作背景：本段前两句挂的是旧表 `look-verbs` 的四个成员（glance / glimpse / peep）与 glare 那张卡，本组不进成员、不粗（硬约束 2、§五.16）。
- `An oath was taken and a solemn vow made silently`（1502）、`A pledge was given and the whistler sang softly`（1503） —— 本段其余两句，同段另有一张 `oath-vow` 表（at 在 oath），本组不碰。
- 义项「v. 考虑；凝视；思考…的可能性」「n. 凝视；v. 注视」「v. 凝视；n. 同辈；同等的人」「v. 凝视；盯着看」 —— 四卡 `m`；「赶公交上学，利奥立志今天准时努力。」—— 段 1 `paraZh`；「上篇·备考的一天」—— `subheads[5][0]`。
- 门禁核对：见文件头小结（本组所有反引号内英文串已跑 `tools/check_compare_draft.py`）。

### 数据边界与判定：能出卡，但四张卡里三张的"看"只有一处书证

- **逐词 grep 结果（本组第一条要报备的事实，四张卡一张一张报）**：
  - `contemplate` —— 卡上**没有** `cmp`（空卡位，可当载体）。
  - `gaze` —— 卡上**没有** `cmp`（空卡位，可当载体）。
  - `peer` —— 卡上**没有** `cmp`（空卡位，可当载体）；但它是旧表 `look-verbs` 的第四个 item（那张的 items 是 glance / glimpse / peep / peer），而旧表**没有**拷到 peer 卡上，所以 peer 卡的卡页现在什么都不显示。它被旧表"占用"的只是内容层（"费力辨认"这一格 + summary 里的同辈档），不是卡位。
  - `stare` —— 卡上**没有** `cmp`（空卡位，可当载体）。
  - 全库 `look-verbs` 这张表只挂在 **glance / glimpse / peep 三张卡**上，带 `at` 的那一份**只有 1 份、在 peep**（`at`＝[5, 1]，正是本组主锚点段；glance / glimpse 两份拷贝都没有 `at`）。所以本组四张卡卡位全空，`at` 落在谁身上都行 —— **换不换、换给谁是真人要拍的事**，本组内容没为躲载体改一个字。
  - **旧表既有形状报备（本组不改、也无权改）**：全库 181 张 `cmp` 里，`summary` 带「简单记：」的是 178 张，**唯一不带的那 3 张就是 `look-verbs` 的三份拷贝**（glance / glimpse / peep 各一份，内容是同一条）；items 全无 `eg` 槽的表也正好是这 3 张（只有 `scene`）。段末那一行按 `shadow/index.html` 的 `compareLine` 逻辑取 `summary` 里「简单记：」之后那半句，取不到就回落到 `title` —— 所以旧表段末现在显示的是「这一段几个词都和"看"有关，但"看"法不同：」，而不是一个"A = …，B = …"式钩子；它 summary 全文按 `tools/width_rule.py` 那把尺量是 69.0（展开后那一行是总结句上限 30 的两倍多）。这三条都是旧表自己的事，写在这里只说明一件事：本组那张表带「简单记：」、带 `eg`，与旧表在同一段落末并排时形状不一致。
- **`all_synonyms: false` 有据**：判定式是"组内每一对都要在彼此的同义词栏里互列"。本组四张卡的 `note` 里**一条同义词栏都没有**（gaze / peer 只有词伙段，contemplate / stare 的 `note` 整条为空），谈不上互列，false 完全成立。也就是说这一组本书没给过任何"卡上声明可换"的物证，四者的区别全靠课文那两句的句法位置撑 —— 这与 §五.14 说的"同形打架"是另一种形状（没有同形、也没有互列）。
- **共享义项「凝视」名不副实（§五.6 全套处置）**：「凝视」是四卡 `m` 的交集，但**全书课文译文里「凝视」只出现 1 处**（1500，落的正是 peer 那半句）；同一句的 gaze 译成"目光稳定地看着路面"、1501 的 contemplate 译「沉思着天上的繁星」、stare 译「注视着夜空」。卡面另一头，「凝视」只出现在四张卡的 `m` 与 gaze / stare 两条 `exZh` 里。所以：标题只说"这一段四个词各领哪样看"，不宣称"本段四词都表凝视"；「凝视」及其余义项原词（注视 / 盯着看 / 考虑 / 同辈 / 同等的人 / 思考…的可能性）一律待在 `sense` 槽，core、默认行、总结句里都不出现。表头里那两个引号（「沉思着天上的繁星」「注视着夜空」）是**逐字引本书译文**，不是给词下定义 —— 按 §五.11 引用＋否掉那一路走。
- **同源合并（§五.8）后的独立文本**：contemplate 侧 = 课文 1 + 卡 `ex` 1 = 2 条（且卡 `ex` 走的还是另一档）；gaze 侧 = 课文 1 + 卡 `ex` 1 + 卡词伙 1 = 3 条；peer 侧"看"这档 = **课文 1 条**（卡 `ex`、卡词伙两条全在同辈档，不并进来算）；stare 侧 = 课文 1 + 卡 `ex` 1 = 2 条。本组是本切片最薄的一组：4 个成员、全组独立英文文本一共 8 条，其中一侧只有 1 条。**证据薄不退回**（§五.7），但落地时"peer 只有一处书证"这一句要照抄，别让它去替 glance / glimpse / peep 背书。
- **计数结论、不是禁令**：能写出的否定句都逐字 grep 过两份数据 —— 本书没有一处 `peer at`、没有一处 contemplate 后接介词（两处一个直接接 the stars、一个接 quitting）、gaze 在本段与卡 `ex` 两处真句子里都是名词（唯一的动词形状是词伙 `gaze at screens`，课文 0 处）、四词的过去式与进行形在课文里 0 处（1501 的 `contemplated` / `stared` 是打了标记的表面形式，按 §五.13 归入"带标记"那一档）。**不许**把这些写成"peer 不能接 at""gaze 不能作动词"这类通则；本书给的就这两句。
- **不写的**：「stare 是没礼貌地盯、gaze 是含着情地看」（四卡 `note` 无语域／褒贬标注，本书给不出证据，虽然两条 `ex` 一个看月亮一个看屏幕，都读不出礼貌不礼貌）、「contemplate 还指沉思状」（卡 `m` 只到「思考…的可能性」）、`gaze into the distance`、`stare in disbelief`、`peer at sb through`、`lost in contemplation`、`contemplative` 这类本书没有的写法与词形一律不列，也不造句；旧表讲过的 glance / glimpse / peep 三词对照不在本表出现（硬约束 2 ＋ §五.9）。
- 提请拍板（本组 1 条）：**同段第三张表的排布**。段 1 末将同时挂 `look-verbs`（旧，无「简单记：」、summary 69.0 超宽、items 无 `eg`）、`oath-vow` 与本组这张（新，默认行 38.0）。三段并排时手机上一屏可能被"辨析"栏占满；旧表那三处形状问题不在本组权限内，本组不改它，只把事实写在这里给真人看一眼。

---

## 附：本切片复核索引（给门禁 / 抽查用）

| 组 | worklist_idx | words | senses | `all_synonyms` | paras | sents | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 218 | decrease / diminish / dwindle / reduce | 减少 | false | [3,47] | 1162,1163,1164,1165 | 出卡：四侧课文各 1 处（都是带标记 1 + 未标 0），新讲的两格 = 掉的是存货还是强度（dwindle 硬币 / 塘水 vs diminish 客流 / 噪音 / 希望）＋ 句子带不带 `day by day` 这个过程；false 有据（dwindle 卡 `note` 空 + diminish 与 reduce 两栏没互列）；「减少」二字本段只落实在 reduce 一侧（§五.6 已把话说轻）；载体报备 = decrease 卡已被旧表 `decrease-reduce` 占了 `at`＝[3,47]，本组须落 diminish / dwindle / reduce |
| 2 | 219 | contemplate / gaze / peer / stare | 凝视 | false | [5,1] | 1500,1501 | 出卡：四张卡卡位全空（`look-verbs` 只挂 glance / glimpse / peep，`at` 那份在 peep＝[5,1]）；peer 只是旧表第四个 item、卡页不显示，本组不重讲 glance / glimpse / peep 对照也不重讲 peer 同辈档；硬区别 = 本段 gaze 是唯一的名词（`His steady gaze`）+ 四词各挂一个框架（不挂介词 / on / through / at）；「凝视」全书译文只 1 处（1500，peer 那半句）→ §五.6 把话说轻 |

- 全局句号口径按章偏移 `[0, 339, 651, 883, 1277, 1492]`（全书 1833 句）自算：章3 段47 起 1162（本段 6 句 1162–1167）、章5 段1 起 1498（段0 占 1492–1497，本段 6 句 1498–1503）；两组共 6 个句号（1162 / 1163 / 1164 / 1165 / 1500 / 1501）与切片包 `sents` 逐个对得上。
- 挂段位置：组 1 → 章3 段47 末（该段末已有 3 张：increase-increment / growth-increase / decrease-reduce）；组 2 → 章5 段1 末（该段末已有 2 张：look-verbs / oath-vow）。两组不同段、组内成员全在同一段，无 §五.9 之外的冲突。
- 本片 2 组的宽度标称值全部用 `tools/width_rule.py` 的同一把尺量出（汉字与全角标点各 1、其余 0.5）：组 1 默认行 39.5、总结句 28.0；组 2 默认行 38.0、总结句 29.0 —— 无一条超上限。四成员组的默认行按"两行读完"写：组 1 四个成员名就吃掉 14.5 当量、组 2 吃掉 12.0，钩子一律压到 3–6 字，没有删成员、没有超线。
- §五.12 报备：组 1 在章3 段47（不在坏掉的 22–43 那批里），组 2 在章5 段1 —— 两处 `paraZh` 都与本段句子互相印证，只当背景用，凡主题栏写到而句子不含的（如"省吃俭用""立志今天准时"）都没当成出处。
- 本文件不引用任何在 `shadow/data/vocab.json`、`shadow/data/sections.json` 里查不到的英文串；门禁命令与结果见文件头小结（`python3 tools/check_compare_draft.py work/辨析草稿/6_09.md`）。
- 落地成卡对象时：`title` = 各组「表头一句」，`summary` 的「简单记」= 各组「默认那一行」，`diff` 各行 `label` = 「维度」列，键名用成员词头（组 1 四个键 `decrease` / `diminish` / `dwindle` / `reduce`，组 2 四个键 `contemplate` / `gaze` / `peer` / `stare`，顺序照切片包 `members`）。
