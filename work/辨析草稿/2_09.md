# 辨析卡草稿 · 批次 2 / 切片 09（worklist idx 50、51）

**小结（3 行）**
1. 本片实得 **2 组**（切片包 `work/compare_slices/slice09.json` 与 `manifest.json` 都只列 idx 50、51；任务书里写的"3 组"与包不符，我按包做）。
2. 门禁 `python3 tools/check_compare_draft.py work/辨析草稿/2_09.md`：**PASSED（全部英文串可回溯）**；另跑了一遍严格自查（把门禁放行的否定行也拆开查），除文件路径 / 全局号数组外 0 条落空。门禁 2（`work/辨析审核/audit_d.md`）两组 0 BLOCK / 2 处 MISMATCH，本文件已按其结论校订：组 50 的 expect core 标称汉字数（9）→（10）、组 51 撤回"该改成 all_synonyms: true"的改标签建议（详见 `work/辨析审核/FIX_2_04_08_09.md`），内容结论未动。
3. 报备 2 项（按 BRIEF §5.7 证据薄不构成退回，门禁 2 亦判两组可落地，不再提请拍板）：① anticipate 全书只 1 次、卡上零词伙；② pastime / recreation 各 1 次，两组证据都属"最薄一档"，我把差异全压在可 grep 的框架与限定词上，没有编语义差。

**口径（照 BRIEF §2、§3）**
- 全局句号 = 章偏移 `[0, 339, 651, 883, 1277, 1492]` + 段前句数 + 段内序号，0 基。自算结果：组 50 = 574（同句共现）、组 51 = 575 / 576，与切片包 `sents` 一致。每组「**主锚点 = 章N 段M**」一行取自该组「该段原文」表所在段（= 切片 `paras` 第一个共现段），落地时按 [章, 段] 填挂段位置；同段多张按 worklist_idx 升序排（BRIEF §5.9）。
- 宽度换算沿用批次 1A 的实测口径：汉字与全角标点各记 1，其余字符（字母/空格/`=`/半角括号）各记 0.5（用 1A 那 5 条默认行 + 3 条总结句反推校准，8 条全部对上），与 `scripts/validate_data.py` 里的宽度函数同一把尺（BRIEF §5.10）。items 的 core 是另一档：按 1B 口径只数汉字（≤12），标称值即汉字数。默认那一行 = 落地 summary 里的「简单记」半句，总结句 = summary 的第二句。
- 数据只认 `shadow/data/vocab.json` 与 `shadow/data/sections.json`；未采用 `work/confusable_groups.json`、PDF 同义替换表、何琼等号表。

---

## 组 50 · anticipate / expect —— 预期 ｜ `all_synonyms: true` ｜ 段 [1, 38] ｜ 句 574（同句共现）

**共现位置**：1 段 1 句 —— 章 1《校园与文化》段 38（全局 569–574），两个词挤在同一句 **574** 里（切片包 `sents` 就一个号 `[574]`，属同句共现）。**主锚点 = 章1 段38**（唯一共现段，取自下方「该段原文」表所在段），卡挂段末。

**该段原文（章 1 段 38，`paraZh`：传播好消息、预见晴天、期待快乐与朋友，积极心态迎接周五欢聚。）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 569 | Our kind correspondent met the quick messenger by the tall tree. | 我们那位好心的通讯员，在大树旁遇上了一个跑得飞快的送信人。 |
| 570 | The tired editor helped the fast typist finish pages before lunch. | 累坏了的编辑帮着打字员，在午饭前赶完了那几页稿纸。 |
| 571 | Each printed handout flew with a bright leaflet across the room. | 每张讲义和明亮的小传单飞过房间。 |
| 572 | The loud propaganda faded as the friends chose to publish only true stories they had checked themselves. | 喧嚣的宣传声渐渐淡去，朋友们选择只发表自己核实过的真实故事。 |
| 573 | Good news was helped disseminate and sunny days foreseen with smiles. | 好消息被传播，晴天被预见，微笑相伴。 |
| 574 | Pleasure was **anticipated** for the Friday fair, and every friend was **expected** to come. | 大家早就盼着星期五的那场游园会，同时也盼着那些最要好的朋友。 |

574 是本组唯一、也最值钱的一条证据：同一个句子、两套被动框架，前半 `anticipated for + 游园会`，后半 `every friend was expected to come`——中文译文把两边都写成"盼着"，英文接的东西完全不同。

### 表头一句（title）

> 这一句先 `anticipated` 游园会那点子高兴事，再 `expected` 朋友到场，两个"预期"接的不是同一种东西：

### 默认那一行（实测宽度 39.5 / ≤40）

```
anticipate = 先想到有事（anticipate rain），expect = 等人到（expected to come）
```

### 成员（items）

| 词 | 音标 | 词性 | sense（卡上 `m`） | core（≤12 汉字｜实测） | scene | eg（含出处） |
|---|---|---|---|---|---|---|
| anticipate | /ænˈtɪsɪpeɪt/ | v. | 预料；预期 | 事先想到有那件事（8） | 星期五的游园会还没开，高兴事先想到 | `Pleasure was anticipated for the Friday fair`（课文 574） |
| expect | /ɪkˈspekt/ | v. | 期待；期望；预期 | 等着谁或哪样东西到位（10） | 朋友该来、礼物该到；还有"预期成果"那种表上话 | `every friend was expected to come`（课文 574） |

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 574 里各自接什么 | anticipate | `anticipated for`、`Pleasure was anticipated for the Friday fair`（课文 574） | 说"有哪件事在前头"，为哪一场活动用 for 引出来 | 它接的是**一件事**（pleasure / rain / the Friday fair），本库唯一一次就带 for |
| 574 里各自接什么 | expect | `every friend was expected to come`、`expected to come`（课文 574）、`I expect a gift on my birthday.`（卡 ex） | 说"谁该到场、哪样东西该到手"，人后面再跟 to 动词 | 它能接**一个人**：574 后半的主语就是 every friend；卡 ex 接的是 a gift |
| 能不能站到名词前面 | expect | `follow expected behaviour`、`achieve expected learning outcomes`（卡词伙；后一条 outcome 卡也写着同一串，另有 `learning outcomes`） | 写"预期表现""预期学习成果"这种考核/论文口径时，它是形容词那一格 | 卡上有成条的 expected + 名词搭配，本组两词里只有 expect 有这一格 |
| 能不能站到名词前面 | anticipate | 卡上 note 只有同义词段（`conjecture, expect, predict`），零词伙；课文里 `anticipated` 那一次是被动动词（574） | 想写"预期的＋名词"时，本库拿不出 anticipate 的书证 | 这一格是空的，不是我漏写——见「证据强度」 |
| 中文落点 | 两个词 | 574 译"大家早就**盼着**游园会，同时也**盼着**那些最要好的朋友"（`sentZh`） | 汉译英时中文都是一个"盼"字，得自己判接的是事还是人 | 译文看不出差别，框架差只能从英文侧记：for 那件事 ／ to come 那个人 |

### 总结句（实测宽度 23.0 / ≤30）

> anticipate 后面站的是事，expect 后面站的是人。

### 出处清单

| 卡上用到的英文串 | 出处 |
|---|---|
| `Pleasure was anticipated for the Friday fair` | 课文 574（原文标为 `[[anticipate:anticipated]]`） |
| `anticipated for` | 课文 574 的连续子串 |
| `anticipated` | 课文 574（全库仅此 1 处） |
| `every friend was expected to come`、`expected to come` | 课文 574（原文标为 `[[expect:expected]]`） |
| `I anticipate rain tomorrow.` | 卡 `anticipate`.ex / exZh（`anticipate rain` 为其连续子串） |
| `I expect a gift on my birthday.` | 卡 `expect`.ex / exZh |
| `follow expected behaviour` | 卡 `expect`.note 词伙 |
| `achieve expected learning outcomes` | 卡 `expect`.note 词伙 + 卡 `outcome`.note 词伙 |
| `learning outcomes` | 卡 `outcome`.note 词伙 |
| `conjecture, expect, predict` | 卡 `anticipate`.note 同义词栏原文 |
| `Nobody expected a big breakthrough from a tiny gizmo made in a yard.` | 课文 1386（章 4 段 18；该句 expected 未打标记，见下） |
| grep 终检 | 上列 11 条串逐条回 `vocab.json` / `sections.json` 原文核对，0 条落空；门禁同结果 |

### 证据强度（本组最薄的地方，逐条说明）

- 课文侧：`anticipate` 全书 1 次（574），`expect` 标记 1 次（574）+ 未标记 1 次（1386，章 4 的 words 表里没有 expect，所以那句只是普通用词、没加 `[[expect:…]]`）。
- 卡侧：`anticipate` 卡 `note` **没有词伙段**（只有同义词栏 conjecture, expect, predict），所以表里 anticipate 那一列的"搭配"只能来自它自己的 ex 和课文 574 两处，我照实写了，没替它编第二条。
- 全库检索支撑的两条"不写"：搜不到 anticipate 修饰名词的形容词形用法；也搜不到 `expect` 接 for 活动的那条框架——所以表里只写"574 那一次带 for"，没写成规则。
- 义项照抄检查：卡 `m` 的「预料」「期待」「期望」「预期」只出现在 `sense` 槽；默认行与 core 换成了"先想到有事／等人到"这种场景说法。
- 同段邻居 573 的 `foresee`（卡 m「预见；预知」，卡 ex `I cannot foresee what will happen tomorrow.`）也在"预期"这一带，但它不在切片给的成员里，按 §5.2 不进表。
- `all_synonyms: true` 的处理：两卡同义词栏互点（anticipate 卡列 expect；expect 卡列 anticipate，predict 卡两张都列），词性同为 v.，所以区别只落在**框架（for 事 ／ 人 + to 动词）**与**能不能当形容词**上，不写强度、不写正式度——两张卡都没有语域标注，无出处。

### 报备（不提请拍板）

1. 本组证据是"同句一次 + 卡 ex 各一句"，比批次 1A 的组 3（endanger/jeopardise）还薄一档；我按 BRIEF §5.3 硬写了框架级辨析。按 §5.7「证据薄不是退回理由」（实测 216/220 组最薄一侧都只 1 处书证），本组照写、不撤，门禁 2 亦判可落地。
2. anticipate 卡上零词伙——若这张对比卡要落在 anticipate 的 note 上，等于给它凭空挂内容；建议落地时顺手把 `anticipated for the Friday fair`、`anticipate rain` 两条补成它的词伙段（出处就是 574 与它自己的 ex，属 `shadow/data/` 变更，走正常流程 + `validate_data.py`，不在本草稿范围内动）。

---

## 组 51 · pastime / recreation —— 消遣 ｜ `all_synonyms: false` ｜ 段 [1, 39] ｜ 句 575 / 576（相邻两句）

**共现位置**：1 段 2 句 —— 章 1《校园与文化》段 39（全局 575–580），成员句 575（pastime）/ 576（recreation），紧挨着的两句、同一段主题。**主锚点 = 章1 段39**（唯一共现段，取自下方「该段原文」表所在段），卡挂段末。

**这一段在讲什么**：这是周四讨论（563）、周五游园会（574）之后接上来的那段放松——575 把消遣一项一项数出来填满夜晚，576 把消遣当成歌声在忙碌一天之后带来的那点安静；同段 579 一起笔已经是 Saturday，段 40 就是周六的演出。

**该段原文（章 1 段 39，`paraZh`：等待微笑、甜美消遣填满温暖夜晚，美好期待化作现实。）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 575 | Smiles were awaited as each sweet **pastime** filled the warm evening. | 微笑被等待，每一项甜美的消遣填满了温暖的夜晚。 |
| 576 | Songs entertain kids and bring quiet recreation after busy days. | 歌声让孩子们开心，也在忙碌一天之后带来一点安静的消遣。 |
| 577 | Tales amuse friends while idle gossip about others stays far from us. | 讲故事让朋友们开心，而背后嚼舌的闲话离我们远远的。 |
| 578 | No low rumour breaks our warm consensus to help each other. | 低级谣言打不破我们互助的温暖共识。 |
| 579 | Saturday woke with a bright festival mood and a sweet feast at noon. | 星期六一起床就带着节日的明快气氛，中午还有一场甜甜的盛宴。 |
| 580 | Our short program needed one quick rehearsal before lunch today. | 我们那个小节目，得在今天午饭前抓紧排一次。 |

（575、576 一句一个成员词，`sentZh` 却把两边都译成"消遣"——差别全在英文那侧的限定词和动词上。）

### 表头一句（title）

> 这一段"消遣"出现了两回：575 把它一项一项数出来，576 把它当成歌声带来的那一块，接法完全不同：

### 默认那一行（实测宽度 39.0 / ≤40）

```
pastime = 一件件玩（sweet pastime），recreation = 忙后的闲（quiet recreation）
```

### 成员（items）

| 词 | 音标 | 词性 | sense（卡上 `m`） | core（≤12 汉字｜实测） | scene | eg（含出处） |
|---|---|---|---|---|---|---|
| pastime | /ˈpɑːstaɪm/（英）/ˈpæstaɪm/（美） | n. 可数（课文里被 each 数着） | 消遣 | 一项项数得出的玩（8） | 温暖的那个晚上，一项一项的活动把时间填满 | `each sweet pastime filled the warm evening`（课文 575） |
| recreation | /ˌrekriˈeɪʃ(ə)n/ | n.（课文里零冠词，跟在 bring 后） | 娱乐；消遣；休闲活动 | 忙完才有的那段放松（9） | 忙碌一天收工以后，歌声带来的那点安静 | `bring quiet recreation after busy days`（课文 576） |

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 前面站什么限定词 | pastime | `each sweet pastime`（课文 575 + 卡词伙 `sweet pastime`）、`his favorite pastime`（卡 ex） | 说具体的某几样爱好：下棋、读书、打球，一件一件列 | 575 用 each 修饰它、译文给"每一项"——它在本书里是数得着的一件件 |
| 前面站什么限定词 | recreation | `bring quiet recreation`（卡词伙）、`recreation after busy days`（课文 576）、`his favorite recreation`（卡 ex） | 说"忙完之后那段放松"，一整块，不拆成项 | 576 前面既不挂 a 也不挂 each，译文给"一点"——它在这段是被带来的一大块 |
| 谁在句子里干活 | pastime | `each sweet pastime filled the warm evening`（575，作主语） | 它是干活那个：自己把一晚上填满 | 575 里消遣是做动作的那个，recreation 在本书那一次只是被歌声带过来的东西 |
| 谁在句子里干活 | recreation | `Songs entertain kids and bring quiet recreation after busy days`（576，作宾语） | 它是被带来的那个：主语是 songs | 它站在 bring 后面当宾语：谁给它、什么时候给（after busy days）都写清了 |
| 卡给的义项宽窄 | 两个词 | pastime 卡 `m` 只有「消遣」一档；recreation 卡 `m` 给「娱乐；消遣；休闲活动」三档 | 汉译英先看中文想说的是"一样爱好"还是"休闲这回事" | recreation 覆盖更宽：pastime 卡同义词栏点名 `recreation`，recreation 卡同义词栏回头写的是 `amusement, entertainment`，没点 pastime |

### 总结句（实测宽度 23.5 / ≤30）

> pastime 数得出一件件，recreation 是一大块休闲。

### 出处清单

| 卡上用到的英文串 | 出处 |
|---|---|
| `each sweet pastime` | 课文 575（原文标为 `[[pastime:pastime]]`） |
| `sweet pastime` | 卡 `pastime`.note 词伙 + 课文 575 |
| `each sweet pastime filled the warm evening` | 课文 575 连续子串 |
| `Reading books is his favorite pastime.` | 卡 `pastime`.ex / exZh（`his favorite pastime` 为其连续子串） |
| `bring quiet recreation` | 卡 `recreation`.note 词伙（两条词伙之一，另一条 `quiet recreation`） |
| `Songs entertain kids and bring quiet recreation after busy days` | 课文 576（原文标为 `[[recreation:recreation]]`） |
| `recreation after busy days` | 课文 576 连续子串 |
| `Reading books is his favorite recreation.` | 卡 `recreation`.ex / exZh（`his favorite recreation` 为其连续子串） |
| `amusement, entertainment` | 卡 `recreation`.note 同义词栏原文 |
| `recreation` | 卡 `pastime`.note 同义词栏原文（pastime 只列了这一个同义词） |
| `entertain kids`、`amuse friends` | 卡 `entertain`.note 词伙 / 卡 `amuse`.note 词伙，与课文 576 / 577 同句（本组不进表，只作段内边界，见下） |
| grep 终检 | 上列 11 条串逐条回 `vocab.json` / `sections.json` 原文核对，0 条落空 |

### 证据强度（本组最薄的地方，逐条说明）

- 课文侧：`pastime` 全书 1 次（575）、`recreation` 全书 1 次（576）——两组都是"一次一档"，本表的限定词与主宾位置就是从这两句里抠出来的。
- 卡侧：两张卡都有词伙段（`sweet pastime` / `quiet recreation`、`bring quiet recreation`），且词伙与课文那两句重合，所以"甜的一件件"对"安静的一大块"这一档不是我对称造出来的。
- 两卡 ex 只差结尾一个词（`his favorite pastime` 对 `his favorite recreation`），说明在这个所有格框架里它们确实可换；worklist 给 all_synonyms: false 也**没有标错** —— 该标签按"双向互点"算（pastime 卡同义词栏列 recreation，recreation 卡同义词栏列 amusement, entertainment，没回指 pastime），本组属单向指点，所以 false 是算法正确输出，详见下方报备第 2 条。
- 全库检索支撑的三条"不写"：本书查不到 pastime 的复数与 a pastime；也查不到 a recreation / recreations / the recreation；`amusement`、`entertainment`、`leisure` 在 `vocab.json` 里都没有卡，按 §5.2 不进表，只留在同义词栏说明里。
- 义项照抄检查：卡 `m` 的「消遣」「娱乐」「休闲活动」只待在 `sense` 槽与"卡给的义项宽窄"这一档的说明里（那一档就是在说义项宽窄，属边界说明）；默认行与 core 用的是"一件件玩／忙后的闲"这种场景说法。
- 同段邻居 576 的 `entertain`、577 的 `amuse`（都有卡、都有标记，义项"娱乐"）与"消遣"这一带相邻，但不在本组成员里，不进表。

### 报备（不提请拍板）

1. 各 1 次、共 2 句：可数/不可数这一档只有 `each sweet pastime` 与 `bring quiet recreation` 两条书证，落地时别把它当规则背——它是这两句的写法。按 BRIEF §5.7，证据薄不构成退回，门禁 2 判本组内容可落地。
2. **撤回原稿"这一组的 false 该改 true"的建议**（门禁 2 专项核实①）：本组是**单向同义指点** —— pastime 卡同义词栏点名 recreation，recreation 卡同义词栏回头写的是 amusement, entertainment，没点 pastime；而 all_synonyms 的算法要求**双向互点**才记 true，所以 `all_synonyms: false` 是正确输出，不该改成 true。本卡内容按"可换但覆盖宽窄不同"出表，与标签无关，也不受影响。补充一句给落地侧：真正决定"能不能出选择题"的闸门是 confusable_groups.py 里那条**单向 OR** 的同义词黑名单（pastime → recreation 单向就已命中，这一对今天就已经被挡掉了），下游**不要**拿 all_synonyms 当选项闸门用。

---

## 门禁与自检记录

- `python3 tools/check_compare_draft.py work/辨析草稿/2_09.md` → `OK 2_09.md`，末行 `门禁 1: PASSED（全部英文串可回溯）`，退出码 0。
- 严格自查（脚本按 `／、；` 拆条、连否定行一起查）：除文件路径、全局号数组、无卡词 `leisure`（明写"没有卡"）外，0 条查无出处。
- 未改任何产品代码 / 数据文件 / 测试；本片只产出本文件。
- 门禁 2 后校订（依 `work/辨析审核/audit_d.md`）：① 组 50 的 expect core 标称汉字数（9）→（10），内容一字未动；② 组 51 撤回"false 该改 true"的改标签建议，改为"单向指点、false 正确"的报备；③ 两组的 `主锚点 = 章N 段M` 一行、`### 默认那一行（实测宽度 X / ≤40）` + 代码围栏、`### 总结句（实测宽度 X / ≤30）` + 一行引用统一成机检可量格式，`python3 tools/audit_ledger.py` 两组 0 条超限（默认行 39.5 / 39.0，总结句 23.0 / 23.5）。逐条记录见 `work/辨析审核/FIX_2_04_08_09.md`。
