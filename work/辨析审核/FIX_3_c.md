# 修复记录 FIX_3_c · 批次 3 切片 05 + 06 改稿台账

- 依据报告：`work/辨析审核/audit_c_b3.md`（10 组 / BLOCK 0 / MISMATCH 3 / 通过 7）。
- 改稿对象：仅 `work/辨析草稿/3_05.md`、`work/辨析草稿/3_06.md`（本记录除外）。未跑 `tools/land_compare.py`，未做任何 git 写操作，未碰 `shadow/data/*`、`scripts/`、`tools/`、别人的草稿与审核报告。
- 宽度均用 `python3 -c "import sys;sys.path.insert(0,'tools');from width_rule import width;print(width('…'))"` 复量，未手算。本文件 20 个标称值（10 默认行 + 10 总结句）改稿前后逐个复量一致，无一条超限；下表"改后宽度"列的"不受约束"指该行是正文/表格行，不属默认行（≤40）或总结句（≤30）两个受约束槽。
- 改后门禁 1：`OK 3_05.md` / `OK 3_06.md`，均 **PASSED**（各自单跑 + 合跑三次同结果）。

| 报告编号 | 组 | 改前写法（摘） | 改后写法（摘） | 改后宽度 | 现在过不过 |
|---|---|---|---|---|---|
| idx 73 组 2 · 判定 1 | 3_05 组2 living room / parlour | 「`front room`、`sitting room` 这类本书没有的近义词不列」 | `front room` 实测 0 处不列；`sitting room` **本书有 1 处**（课文 1712 `clear laughter filled the small sunny sitting room`，译「会客室」，句内标记只打在 `merry`/`laughter` 上、本书无此卡），不属本组共现段故不并列进成员表，改为就地报备 | 不受约束（正文行）；该组默认行 38.5、总结句 29.0 未动 | 过（虚报已消，实串可 grep，§五.13 分档已标） |
| idx 73 连带 | 3_05 附录否定行清单 | 把 `sitting room` 列进"本书没有／0 处"的反例名单 | 名单里换成 `front room`，并新增一句点名 `sitting room`(1712)、`pepper grains`(847)、`flour sacks`(1209) **本书都有**、已改成实情、不属否定行 | 不受约束 | 过（附录与正文不再互相打脸） |
| idx 76 组 5 · 必须改 1 | 3_05 组5 corn / grain（diff「卡上给不给现成块」grain 行末列） | 「本书课文里这一档 0 处用过」 | 「这一档课文里就有 1 处：847 `the black pepper grains`（译「胡椒粒」），只是那里 `grain` 未打词条标记（标记打在 `pepper` 上）→ 卡词伙与 847 是同一处文本，同源合并不另计」 | 不受约束（表格行）；该组默认行 37.5、总结句 21.5 未动 | 过（要渲染给用户的那句错信息已删） |
| idx 76 组 5 · 必须改 2 | 3_05 组5 出处清单 `pepper grains` 行 | 「本书课文里 0 处这样用过，是与课文不同源的独立一条」 | 「**与课文第 847 句同一处文本**（章2 段33 第 4 句，全句 + 译文照引，标记打在 pepper/ginger/root 上），按 §五.8 同源合并，不另计为新文本」 | 不受约束 | 过（同源改判为同源，实串逐字回原文核过） |
| idx 76 组 5 · 必须改 3 | 3_05 组5「不写的」第 2 条 | 「grain 的『颗粒』档除卡上 `pepper grains` 外本书 0 处，不当本段证据」 | 「『颗粒』档本书课文只 847 一处（未打词条标记；卡词伙 `pepper grains` 就是那一句），它不在本组共现段，所以只当'还挂着另一档'的说明、不当本段区别证据」 | 不受约束 | 过（0 处虚报已删，判据方向不变） |
| idx 76 组 5 · 必须改 4a | 3_05 组5 diff「同句里它是什么形状」grain 行末列 | 「本书给 `sack` 卡的那条词伙是 `flour sack`，与这句不同源」 | 「`sack` 在课文里两处各 1（831 的 `heavy grain sacks` 未打标记、1209 的 `the flour sacks` 打了 `[[sack:sacks]]`），卡词伙 `flour sack` 与 1209 同源、与 831 这半句不同源」 | 不受约束 | 过 |
| idx 76 组 5 · 必须改 4b | 3_05 组5 出处清单 sack 那行 | 「本书课文里 `sacks` 只出现在 831 的 `heavy grain sacks`，卡上给的是 `flour sack`，两处不同源」 | 「`sacks` 在课文里 2 处：831（未打标记）+ 1209 `the flour sacks`（章3 段54 第 5 句，全句与译文照引，标记打在 `sack:sacks`）；卡词伙 `flour sack` 与 1209 同源不另计，与 831 不同源，只当'容器'格旁证」 | 不受约束 | 过（§五.8 / §五.13 双齐） |
| idx 76 连带 | 3_05 组5 段原文表下「注」 | 「corn 全书带标记只 831 这 1 句；grain 全书带标记 2 句（831、1305）」 | 按 §五.13 两档分开列：corn 带标记 1 处（831）＋未打标记 0 处；grain 带标记 2 处（831、1305）＋未打标记 1 处（847 的 `grains`） | 不受约束 | 过 |
| idx 76 连带 | 3_05 组5「共现位置」段 | 「grain 另在 1305（章4 段4）出现 1 次」 | 「grain 共现段外另在 1305（带标记）与 847（颗粒档、未打标记）各 1 次，两处都不是共现段」 | 不受约束 | 过 |
| idx 76 连带 | 3_05 组5「同源合并后独立文本数」 | grain = 831 + 1305 + 卡 ex + 卡词伙 `pepper grains` = 4 条 | grain = 831 + 1305 + 847（未打标记）+ 卡 ex = 4 条（卡词伙与 847 同一处文本、不另计）——补一句"总数不因这条修正而增减"，与报告 F 项结论一致 | 不受约束 | 过（账仍 4 条，来源换成真出处） |
| idx 76 连带 | 3_05 组5 diff「别的卡上它出现在哪一头」grain 行末列 | 「唯一一条课文外的书证在饥荒段」 | 「共现段外那条带标记的课文书证在饥荒段（另有一处 847 属颗粒档、未打标记，见本表末行）」，"本书从没把 corn 写进被抢这一头"原样保留 | 不受约束 | 过（不与新登记的 847 冲突） |
| idx 76 连带 | 3_05 附录「5 组共现段各不相同」行 | 「组 5 的 grain 另在 1305（章4 段4）各出现 1 次，两处都不是共现段」 | 补 847（章2 段33，`grains` 未打标记），三处均标各自口径 | 不受约束 | 过 |
| idx 81 组 5 · 必须改 1 | 3_06 组5 lorry / truck 出处清单 | 「truck 其余 **6** 处课文」（自列 933/934/994/1231/1245/1254/1356 = 7 句） | 「truck 其余 **7** 处课文（全书带标记 8 处减本组 921）」，句子清单未动 | 不受约束；默认行 38.0、总结句 25.0 未动 | 过（计数与自列句子数一致） |
| idx 81 组 5 · 必须改 2 | 3_06 组5 表头一句 | 「一个在场内被人从**驾驶室**跳下来」（本书 0 处 cab/驾驶室） | 「一个在场内被人从卡车上跳下来」（= 921 原文 `jumped down from his truck` 与其译文） | 表头不受 ≤40 约束；实测该组默认行 38.0 / 总结句 25.0 未变 | 过（无据部件已删） |
| idx 81 连带 | 3_06 组5 段原文表下计数行 + 附录复核索引第 5 行 | 「truck 全书标记 8 处、lorry 全书只有 922 这 1 处」 | 按 §五.13 分开列：truck 带标记 8 处 ＋ 未打标记 1 处（25 `from old trucks`）；lorry 带标记 1 处（922）＋ 未打标记 0 处；复核索引行注明"均为带标记口径" | 不受约束 | 过（两档未并成一个数） |
| idx 79 组 3 · 报告口径条 | 3_06 组3 entrance / entry 计数（表下注、diff 前两行、数据边界、复核索引） | 「entrance 全书标记 1 处；entry 课文标记也只有 877 这 1 处，但 456 还有一处没打标记的 `entry test`」；diff 里写「本书唯一一处课文 entry」「本书唯一一处 entrance」 | 全部改成 §五.13 形状：entrance 带标记 1 处（877）＋未打标记 0 处；entry 带标记 1 处（877）＋未打标记 1 处（456 `no harsh selective entry test`，该句标记只打在 `prerequisite`/`selective` 上）。按报告结论**保留** 456 当 entry 的"准入"档书证，并注明"456 只算准入档、不算门那一档"，成员仍是切片给的两词（§五.2 合规） | 该组默认行 35.5、总结句 23.5 未动 | 过（合规保留 + 计数口径改齐） |
| 未改（有意） | 3_05 组3 complex / mall | 报告建议（非必须）：790 除 `supermarket`、`booth` 外还有 `night` 一张卡，该句少列一个 | **未动**：报告判 OK、不计 MISMATCH，且用户明令"报告说 OK 的组不要顺手改" | — | 不影响门禁（3_05 仍 PASSED） |
| 未改（禁止项） | 3_05 组3 | 报告提请拍板 ② 的"给 complex 名词档补词伙"门槛 | **未补**：本书唯一可补的 `stone shopping complex` 与 789 同源，补了撞 §五.8；「提请拍板 ②」原样保留，未新增退回门槛 | — | 过（按报告"支持出卡、不必退"） |
| 未改（禁止项） | 5 处提请拍板（3_05 组1/组3/组4/组5、3_06 组2/组4） | 作者自列的待拍板段与"退点在哪"的自述 | **全部原样保留**：报告逐条结论是"支持出卡、不必退"，未据此删门槛或另发明门槛 | 各相关默认行/总结句均未动 | 过 |
| 未改（有意） | 3_06 组2 注（"与本组两词互列同义词"→"与 exterior 互列"）、3_06 组4（`paraZh` 引用截断补全、「全书就 908 一处」补"（按标记口径）"） | 报告标为"建议（非必须）/非阻断修正" | **未动**：这两组判定 OK，按用户令不顺手改；如需一并精确化请由报告或主控点名后再改 | — | 不影响门禁（3_06 仍 PASSED） |
