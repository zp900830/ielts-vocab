# 任务模式底栏第二版（2026-09-22 晚，他看图口述）

- 依据：他 2026-09-22 两张截图 + 原话（本文 §1 逐条抄录，实现按最后一句为准）
- 上游：`docs/superpowers/specs/2026-09-22-任务模式走查.md`（同日白天的全量走查）、`spark-output/bench/影子跟读任务模式.md`（竞品拆解）
- 本文是「底栏」这条的唯一口径来源；与 `2026-09-22-任务模式两步制.md` 冲突时，**底栏以本文为准，流程仍以两步制为准**。

## 1. 他这次说的（逐条）

| # | 原话 | 落成什么 |
| --- | --- | --- |
| 1 | 「高亮的样式使用和常规模式一样就行。不需要额外加描边这种样式」 | 删掉 `.sent.task-current` 那一圈 2px 绿描边，任务模式的高亮就是常规模式的浅绿底纹 |
| 2 | 「（关键句变高亮）还有 bug，只能跟着下一句走，上一句没反应」 | 见 §2 —— 这是本轮唯一的 blocker |
| 3 | 「文字双行展示位于任务条左侧（核心信息在上、辅助信息下）」 | `.tb-text` 两行：`.tb-title` 核心、`.tb-line2` 辅助 |
| 4 | 「所有按钮位于任务条右侧」 | `.tb-btns` 靠右；播放那一组（循环/AB/倍速/书签/▶）也在这排里 |
| 5 | 「关闭按钮位于任务条右上方（悬浮后显示 退出任务模式，点击后提示是否要退出）」 | `.tb-close` 绝对定位右上角；`title`/`aria-label` 保留；点击进 `data-state="exit"` 问一句 |
| 6 | 「样式和播放条一致 glass 风格，整体 UI 都优化一下」 | 复用 `.audiobar` 那套 Liquid Glass 配方（同渐变、同 blur、同内高光、同 22px 圆角、同左右收边） |
| 7 | 两个数「显示」 | ② 做题态补「这批还剩几分钟」；全站补「本次已用多久」 |

**改口记录**：同一天里底栏口径变了两次（先「一条通栏、一行按钮、退出在最左」，再「文字左 / 按钮右 / 关闭右上」）。按他最后一句重做，`task-flow.spec.ts` 那条几何锁同步改写，不留旧口径的断言。

## 2. blocker：两套「当前句」标记必然不同步

描边那套（`.sent.task-current`）只由 `goToNextTask()` 移动，而「上一句」这颗按钮直接调全局 `step(-1)` —— 它不认识这个类。后果不止是"看着不动"：

1. 屏幕上同时有两句被标成当前（描边停在旧句、常规底纹跟到新句）；
2. 队列位置 `taskPos` 也停在旧句，于是**再按「下一句」，记的是那句并没在读的句子的进度**。

所以修法不是"给 `step(-1)` 补一行同步"，而是**取消第二套标记**：「我在哪一句」全站只有 `.sent.playing` 一个真值（它本来就跟着 `playFrom` → `paint` 走，上一句/下一句/键盘/自动链全都经过它）。`taskPos` 则改由新的 `TASK.prev()` 在翻句后对齐到屏幕上这一句。

## 3. 底栏结构

```
#taskBar（玻璃卡，浮在底部：左右 24px 收边、22px 圆角、bottom 12px + 安全区）
├── .tb-close#tbExit          绝对定位右上角，只有图标，title=「退出任务模式」
└── .tb-body（宽屏一行：文字左 / 按钮右；≤700px 折成两行：文字在上 / 按钮在下）
    ├── .tb-text
    │   ├── .tb-title#tbTitle  核心行：① 通读 64/141 · 今天 15 分钟
    │   └── .tb-line2          辅助行：这批还剩 ~32分5秒 · 本次 8分钟 〔全部过完约 2027 年 9 月〕
    └── .tb-btns               播放组 → 上一句 → 再来 → 下一句（从右往左重要度递减）
```

状态机沿用「一条栏多种态，只换内容不藏控件」：`idle | read | quiz | done | resume | **exit**`。新增的 `exit` 态不弹层、不新开弹窗——正文换成问句，按钮排换成「继续做 / 退出」。

## 4. 两个数怎么算（不许编）

- **本次已用多久**：`sessionStart`（进任务模式那刻）到现在，每 15 秒自己走一次表。**前 20 秒不显示** —— 刚点进来就写「本次 0秒」看着像坏了。
- **② 还剩几分钟**：`quizStart` + 已答题数算出**他自己刚才的每题秒数**，再乘剩余题数。**一题没答完时只报「还剩 N 题」，不给分钟数** —— 一个凭空来的数比没有数更坏（这条是「数字不许编」口径的延续）。
- 顺手抓到：① 态那行「这批还剩」以前写成 `secNew() / rate`，而 `secNew()` 本身已经除过倍速 —— **倍速被除了两次**（1.5× 时把剩余时间少报一半）。改成 `secNew()` 一次。这是同一天在引擎里抓到的 `rate` 平方坑的第二个现场。

## 5. 明确没做的

- ② 态辅助行原来的提示语（「选出填回空格的那个词」/「答错不倒退，今天之内还会再见到它」）让位给剩余时间 —— 四选一没什么要教的，而「还要多久收工」正是这一步唯一想知道的数。答错的安慰话仍在做题卡里。
- 窄屏（≤560px）「再来 / 下一句」仍退成纯图标：按钮排自己一行也放不下带文字的九颗（实测过）。名字由 `title`/`aria-label` 说。
- 退出确认**不做**挽留：文案只报「今天的进度已经记下了，退出不会欠下什么」，零惩罚口径不变（竞品拆解里这是相对多邻国/Anki 的差异化资产）。

## 6. 验证

- 锁：`task-flow.spec.ts` 底栏几何锁按新口径改写 + 新增 4 条（高亮单一真值与上一句对称、点 ✕ 先问一句、② 剩余时间来自真实速度、底栏与播放条同款玻璃）。
- 探针：`node work/task_mode_audit.mjs`（半透明玻璃比白底更吃对比度，改完必须复量，别只信纸面值）。

---

## 追加（同夜第三次改口）：条上只留三个数

他看着成品说：「底部按钮上方的那些文字一堆，都没耐心看。是否可以结构化的展示，信息有主次。」
数了一下，条上同时报 **5 件事**（读到哪 / 今天预算 / 还剩多久 / 本次多久 / 全书何时过完），全是同一种灰、同一号字。

给他三个选项（只留三个 / 全留但拉开层级 / 只留进度其余点开），他选**「只留三个」**。落成：

| 层 | 内容 | 样式 |
| --- | --- | --- |
| 主行 | `① 通读 9/45` + 一条 3px 进度条 | 13.5px / 700，进度条吃掉剩余宽度 |
| 辅行左 | `还剩 ~15分钟` | 11px / 中灰 —— **唯一一个能立刻行动的数** |
| 辅行右 | `本次 6分35秒` | 10.5px / 更浅，`margin-left:auto` 顶到右端 |

撤掉的两件：**「今天 15 分钟」**（预算，设置屏 / 计划页 / 今日面板都有）和**「全部过完约 2028 年 3 月」**
（`#tbEta` 整个节点删掉，`paintTaskEta` 一起删 —— 「四处同源」从此是**三处**）。
进度条替掉"已完成 x%"这类废话字：同一行左边本来就是 `n/N`，画出来比再说一遍短。

② 态进度条跟的是**做题进度**（`quizCursor / quizN`），① 态跟的是**句子进度** —— 主行标题各说各的计数器，
条就在它正下方，读法不歧义。续读条（`data-state="resume"`）也补了一次填条，否则会出现「已读 2/24 但条是空的」。

**措辞跟着收**：`这批还剩 ~` → `还剩 ~`（条上只剩一个时间数，不必再强调"这批"）。

---

## 附：探针改口径之后又抓到的四条（2026-09-22 深夜第二轮）

跑 `node work/task_mode_audit.mjs` 复量玻璃底对比度时发现：**上一轮那份报告本身在骗人**，
先把探针修对，才轮到产品。

### 先修探针（三条都是它自己的错）

1. **深色模式从来没被测过。** 探针找的是 `#themeBtn` / `[onclick*="dark"]`，
   真开关是 `#btnDark` + `onclick="toggleDark()"`（大写 D，选择器大小写敏感）→ 点了个空，
   报告里那半节「深色对比度」量的其实还是亮色。**假覆盖比假红更坏**：它让人以为查过了。
   现在改成点 `#btnDark` 并**断言 `body.dark` 真的挂上**，挂不上就报 P1「这一节没测到」。
2. **半透明玻璃把对比度量乐观了。** 旧写法一路向上找第一个不透明 `background-color`，
   等于把玻璃那几层 `.65/.25/.15` 当不存在。改成按由外向内把所有半透明层叠回底色，
   渐变再取最不亮那一档；判据也从「字号 != 11px」换成 WCAG 真正的分档（大号字 3:1 / 其余 4.5:1）。
3. **「进度填条 0px」是假红。** 一句没读的时候填条本来就该是空的。加上「已经读了 N>0 句」这个前提。

### 探针修对之后，抓到两条真的（都是本轮我自己引入的，已修）

| 问题 | 实测 | 根因 | 修法 |
| --- | --- | --- | --- |
| 「本次多久」小字看不清 | 3.67:1（10.5px，要求 4.5:1） | 我给它挑了 `#79857e`，比左边 `.tb-sub` 的 `#56625b` 浅 | 压到 `#667069` = **4.92:1**，仍比左边浅一档，主次没丢 |
| 深色下 ✕ 几乎看不见 | 2.2:1 | **特异度撞车**：`#taskBar .tb-close{color:#4a554f}` 带 id = (1,1,0)，压过 `body.dark .tb-close` 的 (0,2,1) → 深色规则根本没生效 | 深色那两条补上 `#taskBar`。旁边播放按钮的深色规则早就带着 id，只有我新加的这条漏了 |

两条都复量过：修完之后 390 与 1280 两档、亮/暗两态都不再报这两项。

### 两条**没修**的：确认是早就在的，不该混进这一轮

1. **主按钮白字压绿底 = 1.9:1**（P1）。`linear-gradient(135deg,#2bd4a4,#0fae7e)` + `#fff`
   是全应用的主 CTA 配方（`--grad`），「开始今日任务 / 下一题 / 继续任务 / 下一句」都是它，
   上一轮就已经在线上。手改等于改全站所有主按钮的视觉，**要他拍板**，不做顺手修。
   算术复核过：#2bd4a4 相对亮度 0.503，白字 (1.05)/(0.553)=1.90 —— 探针没算错。
2. **A-B / 循环 / 倍速 弹层在 390px 上溢出视口**（P2）。弹层 244px 宽、`left:50% + translateX(-50%)`，
   没有任何视口收边。**常规模式也溢出**（实测右边超出 39px：弹层右沿 429 vs 视口 390），
   任务模式只是把它从"超出右边"挪成了"超出左边 34px"。修法是让弹层改挂到条子上（窄屏 `position:static` + 左右 12px 收边），
   但那要同时动 3 个弹层 × 2 条栏 × 窄/宽屏，**风险面比这一轮整个 UI 改动还大**，单开一轮做。

### 全量从 13 分钟涨到 26.4 分钟，查了：不是底栏自激

`work/taskbar_loop_probe.mjs`（新）：不包装闭包函数（数不到），改数它**写出去的 DOM**——
MutationObserver 盯 `--task-bar-h` 的值真的变了没。结果：

| 静默 6 秒 | 翻 10 句 | 高度来回改号 | 帧数 |
| --- | --- | --- | --- |
| 写 0 次 | 写 0 次 | 0 次（单帧最大 0px） | 360 帧 / 6 秒 = 稳定 60fps |

底栏高度在 390px 上恒为 102.6px。**这个项目最坑的那类"读布局→改 class→重启过渡→无限循环"排除了。**
26.4 分钟没有归因（要跑一次基线全量才能定性，值 25 分钟），先记着别当"已经查清"。

### 全量结果与那条红

`1 failed / 115 passed (26.4m)` —— 红的是 `smoke.spec.ts` 的 admin 页 `page.goto(…:8932)` 超时。
**与本轮无关**（没动 `root-server.ts` / `smoke.spec.ts` / `app-review-queue.spec.ts`），
机器放空后 `--workers=1` 单跑 **2 passed (1.0m)**。机制这次查清了：两个 spec 共用 8932、各自 beforeAll/afterAll 管一次生死，
先跑完的那个把自己的服务关掉 / 第 29 行 `pkill` 直接把对方刚起的杀掉。已写进记忆，单独一轮再修。

---

# 同夜第四轮：①绿底白字对比度 ②弹层出屏幕（他 2026-09-22 拍板：单开一轮修完再合 main）

上一轮这两条是"发现但没顺手改"的，他把口径下达得很短：①「所有绿色主按钮的白字压绿底，实测对比度只有 1.9……改它等于改全站主按钮」②「A-B／循环／倍速那三个小弹层在手机上会伸到屏幕外……修它要同时动三个弹层、两条栏、宽窄屏」。本轮两件都修，并按字面范围把主应用一起改了（绿色那颗是全站同一支渐变）。

## ① 换字色，不换底色

量到的事实：白字压渐变亮端 `#2bd4a4` = **1.90:1**（AA 正文 4.5:1、纯图标 3:1）。要白字过线，底色亮度得压到 **0.183 以下** —— `#2bd4a4` 是 0.503，一路压到 `#0a6b50` 才够，那就不是这支薄荷绿了。所以换字不换底：

| 绿底 | 白字（改前） | `--cta-ink` `#04231b`（改后） | 线 |
| --- | --- | --- | --- |
| `#2bd4a4` 渐变亮端 | 1.90 | **8.76** | 4.5 |
| `#0fae7e` 渐变暗端（最差那一端） | 2.85 | **5.85** | 4.5 |
| `#10b487` = `--task-new` / 深色 `--accent` | 2.66 | **6.26** | 4.5 |
| `#0c9c74` 浅色 `--accent` | 3.49 | **4.78** | 4.5 |

命中范围：**跟读站 16 条规则 + 主应用 10 条规则 = 26 处清零**（开始今日任务、下一句、继续任务、下一题、过关卡片、计划选项选中态、播放键、A-B 生效态、书签面板 A/B、随身听 FAB、侧栏图标……）。绿的亮度一颗没动 → 「一行只许有一颗绿主按钮」那套层级原样保留（深色模式 `--grad` 与浅色同值，所以字色也共用一个变量）。

**门禁三道，缺一不可：**
1. **源码级**（`cta-and-popups.spec.ts` Case 1）：扫三份站点 `<style>` 里"底色是绿 且 字色是白"的声明块 → 0 命中。屏幕级扫描只能量到此刻在屏的控件，做题态／A-B 生效态／弹层要先钻进去才显形，这道不依赖渲染。
2. **实页级**（Case 2/3）：整页叠回半透明玻璃层后逐个数，且**要求"捞到的站点数 > 0"** —— 一条都没捞到不是"全过"，是没量到。
3. **规格级**：`#tbNext` 的 `color` 必须是 `rgb(4,35,27)` 且 `background-image` 里仍有 `43,212,164` —— 把"不许把绿压暗来凑数"写进断言。

## ② 弹层以整条栏为锚

根因不是"没做适配"，是**锚点选错了**：菜单 `left:50%; translateX(-50%)` 以那颗约 40px 宽的小按钮为中心，菜单宽 80~244px，所以只要按钮中心离屏幕边小于半个菜单宽就必然出屏。

改法：窄屏（≤700px）把 `.loop-wrap / .rate-wrap / .ab-wrap` 改成 `position:static` —— 绝对定位的包含块上移到 fixed 的 `.audiobar` / `.task-bar` 本体，菜单居中于整条栏，`bottom:100% + 8px` 仍是"栏上方 8px"。390px 上两侧各留 (栏宽-菜单宽)/2。

| 弹层 | 改前 390px | 改后 390px | 1280px |
| --- | --- | --- | --- |
| 倍速（常规模式） | 右溢 **18.8** | 0 | 逐位不变 |
| A-B 选段（常规模式） | 右溢 **39.0** | 0 | 逐位不变 |
| A-B 选段（任务模式） | 左溢 **33.5** | 0 | 逐位不变 |
| 循环 / 书签 | 本来就在屏内 | 0 | 逐位不变 |

顺手一处副作用收掉：wrap 变 static 后，那根"hover 桥"也跟着以整条栏为基准，会从 44px 涨成通栏宽的一条隐形横条，把压在栏上方那 12px 的正文点击吃掉 → 显式收回 44px 并正对自己的按钮。

## 本轮我自己犯的三条（都改在流程里，不只是改在代码里）

1. **闸门一开始根本不响**：源码级扫描要求文件里有 `<style>` 标签，而自检夹具是 `.css` → 返回 0 命中，看起来"三份站点都干净"。**靠夹具的"必须恰好抓到 2 条"这一断言才暴露**。教训：新门禁必须喂一份已知有缺陷的输入看到它响，且"不依赖渲染的扫描"要连夹具文件类型一起验。
2. **判"面板开着"用错了属性**：`.today-panel` 常驻 DOM，靠 `.open` class 显形（`opacity`+`pointer-events`），我拿 `display !== 'none'` 判 → 每个页面都被判成"模态关不掉"。
3. **改完反而"没站点可量"的假绿**：实页扫描原来要求字色是浅色（`lum>0.55`）才算一个站点，白字换成深墨后站点数直接归 0 —— 报"越界 0"看着像通过，其实什么都没量。改成"绿底 + 有字/有图标"就量，无论字色，改前改后是同一批站点（27 处 → 0 处）。

## 没量到的两档 / 没做的两族

- **深色模式那两档没量到**：探针点 `#btnDark` 后 `body.dark` 没挂上（那两档实际是浅色的重复），所以深色只有"数值 + 源码级"两层覆盖（见上表：4.78 / 6.26 都过线），没有像素级证据。
- **A/B 角标（3.49:1）和 `#loopCount`（9px 小徽标）没动**：它们是状态标记不是按钮，按字面范围之外，等他拍板。
- **绿色文字压白底那一族没动**（`.btn` 12px 的 `#0c9c74` 字 = 3.49:1，`.loop-menu button.active` 同理）：这是"彩字压白底"，跟他下达的"白字压绿底"是两族问题，改它同样等于改全站，需要单独一轮。
