# 辨析卡草稿 · 批次 4 切片 26（5 组 / worklist_idx 129–133）

> 小结：切片 `work/compare_slices/slice26.json` 的 5 组（worklist_idx 129 / 130 / 131 / 132 / 133）全部写完；门禁 `python3 tools/check_compare_draft.py work/辨析草稿/4_08.md` 结果见本行末：PASSED（0 条查不到）。宽度全部用 `tools/width_rule.py` 那把尺实测，标称值逐组见各组小标题。
> 提请拍板 2 处：**组 1**（enemy / foe）foe 卡 `ex` 就是课文 1332 那一句、`exZh` 也与课文译文同句，foe 侧独立文本只有 2 条（enemy 侧 10 条），5:1 的薄侧要不要留；**组 2**（strategy / tactic）共享义项「策略」在本段一处都没落地（1334 译文给的是「战略」与「战术」），按口径 6 说轻。另：组 4 义项「坟墓」两侧都成立，不必拍板。
> **两项原上报的拍板请求已按新口径就地结案，不再等人拍**：① **组 3**（happen / occur）两卡 `ex` 是同一句只换动词、`exZh` 一字未改（与批次 3 组 2 同形，那组标的是 `true`）—— 按 §五.14 把这一同形事实写进「数据边界」当合法物证（两句各算一侧书证），并补"两卡『同义词：』栏没互列、故 worklist 的 `false` 有据"，不退回义项表、不改 worklist；② **组 5**（disturb / interrupt）的邻居词 interfere 按硬约束 2 留在成员表外（成员集合由切片包 `words` 钉死，与它有没有卡无关），本组只报"同义词栏单向互列"这一行；组 5 的义项半边（1364 的 interrupt 本书译「打断」、卡 `exZh` 也译「打断」）仍按口径 6 在原处如实报备。
> 未动任何产品代码 / 数据文件（`shadow/index.html`、`shadow/data/vocab.json`、`shadow/data/sections.json` 只读未改），未碰别人的草稿与 `work/辨析审核/`，未跑 `tools/land_compare.py`，未做任何 git 写操作。

---

## 组1 · enemy / foe —— 敌人 ｜ `all_synonyms`: true ｜ 段 [4,9] ｜ 句 1332 / 1333

**共现位置**：1 段 2 句 —— 第 4 章段 9（全局 1332–1337），成员句 1332（foe）与 1333（enemy）**相邻两句、说的是同一件事**（先说老对手学会客气比，再说曾经的竞争者把老敌人称作密友）。**主锚点 = 章4 段9**（`paraZh`：老敌变挚友，巧谋蓝图与劝告化为助人力量。属第四章·历史与发明——上篇·纪念馆）。卡挂段 9 末；组 2（strategy / tactic）同在本段末，本组只讲"对面那批人"，"定策略"那格交给组 2。
主锚点 = 章4 段9

**这一段在讲什么**（只按段里真有的句子写）：1332 说老对手学会了友好地比，不让 rival 那股骄傲毁了春耕的活；1333 说一位曾经的 competitor 现在管自己的老 enemy 叫珍爱的密友；1334–1335 转去说 strategy / tactics 与 clever manoeuvre、爷爷的 blueprint；1336–1337 是爷爷的警告与护士给饿着的孩子订面包（段 9 `paraZh` 的"巧谋蓝图与劝告化为助人力量"覆盖的是 1334–1337，与本组无关）。

**该段原文（章4 段9，成员两句 + 后两句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1332 | Old **foes** learned to **contend** kindly without letting **rival** pride **spoil** **spring** work. | 老对手学会友好竞争，不让对手式的骄傲毁掉春耕。 |
| 1333 | An ex-**competitor** now calls his old **enemy** a cherished close friend. | 一位曾经的竞争者如今称老敌人为珍爱的密友。 |
| 1334 | Good **strategy** needs wiser **tactics** than hasty guns and **bitter** cries. | 好的战略需要比仓促枪炮和苦涩哭声更明智的战术。 |
| 1335 | Each **clever** **manoeuvre** followed Grandpa's simple **blueprint** for safe return home. | 每次巧妙的调动都遵循爷爷安全回家的简单蓝图。 |

foe 全书带 `[[词头:…]]` 标记 2 处（1332、1609）、未打标记 0 处；enemy 全书带标记 1 处（1333）、未打标记 0 处——但 enemy 另在 **8 张别的卡的 `ex`** 里被当"敌人"用（raid / cannon / spear / traitor / spy / scout / strangle / defend），foe 的卡面 0 张（§五.13 分档：书内标记数与卡侧引用数分开列）。

### 表头一句（title）

> 挨着的两句，一句说一**批**老对手学会了客气比，一句说一个人的老敌人被当面称作密友——worklist 给的共同义项"敌人"落在 1333 的 enemy 上；1332 的 foe 本书译的是「老对手」，它真译成「敌人」的那处在别章：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| enemy | /ˈenəmi/ | n. | 敌人；敌军 | 打仗时对面那个 | 1333 那个被叫作密友的老敌人；卡 ex 里战场上跟士兵对打的那位；8 张别的卡 ex 里的营地、大炮、长矛 | `his old enemy`（课文 1333） |
| foe | /fəʊ/（英）/foʊ/（美） | n. | 敌人；对手 | 老对头，会讲和 | 1332 那批学会客气比的老对手；1609 与 friends 排在一起被分辨的那头 | `Old foes learned to contend kindly`（课文 1332） |

### 默认那一行（实测宽度 38.0 / ≤40）

```
enemy = 打仗那阵营（the enemy camp），foe = 老对手学会客气（contend kindly）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段它站在句子里哪一头 | foe | `Old foes learned to contend kindly`（1332，复数、当主语）、`rival pride`（同句，rival 卡的词伙） | 说"一整批老对头"，而且是主动学会客气的那批 | 本段 foe 是主语、是复数，句子里它是被夸的对象 |
| 本段它站在句子里哪一头 | enemy | `his old enemy`（1333，单数、带 his、站在 calls 后面）、`a cherished close friend`（同句后半） | 说"某个人的那一个敌人" | 本段 enemy 是宾语、是单数，前面必定站一个限定词（his） |
| 本书把它派给哪种场合 | foe | `distinguish friends from foes`（1609，与 friends 对举，译「把朋友与敌人辨别清楚」）、`foes learned to contend kindly`（1332 同句场景） | 讲"分清敌我""老对头和好"这类句子 | 本书给 foe 的 2 处都不动刀枪：处在学分辨、处在学会客气 |
| 本书把它派给哪种场合 | enemy | `the enemy camp`（raid / spy 两卡 ex）、`fired the cannon at the enemy`（cannon 卡 ex）、`threw a spear at the enemy`（spear 卡 ex）、`helping the enemy`（traitor 卡 ex）、`the enemy's movement`（scout 卡 ex）、`strangle his enemy with a rope`（strangle 卡 ex）、`defend their country from enemies`（defend 卡 ex）、`his enemy in the war`（卡 ex） | 开炮、投矛、摸营地、抓叛徒那一类句子 | 8 张别的卡的 ex 都拿 enemy 当"对面那个 / 那群"，foe 一张都没有——这是本组最硬的一格 |
| 卡 `m` 的第二档真用上了吗 | enemy | 第二档「敌军」：`the enemy camp`（raid 卡 ex，译「敌人的营地」）、`the enemy camp`（spy 卡 ex，译「敌军营地」）、`the enemy's movement`（scout 卡 ex） | 集合用法：对面那一支 | 这一档 3 处书证全在别的卡上，本组课文那 1 处（1333）走的是"一个人" |
| 卡 `m` 的第二档真用上了吗 | foe | 第二档「对手」：1332 的译文正是「老对手」；同段紧邻的 1333 还挂着 competitor 卡（译「竞争者」） | 比赛、竞争里对面那位 | foe 本段走的是「对手」档，worklist 那个"敌人"在本段没落上（落的是 1609 那处） |
| 单复数与限定词怎么写 | foe | 本书 2 处**全是复数写法** `foes`（1332、1609）；卡上词伙记的却是单数 `old foe` | 说"一批对头" | 卡词伙与课文在数上对不齐：词伙给单数、课文两处都给复数 |
| 单复数与限定词怎么写 | enemy | 本书 10 处（课文 1 + 卡 ex 9）里 9 处单数（`his old enemy`、`the enemy`、`the enemy camp`），只有 `from enemies`（defend 卡 ex）1 处是复数；卡上词伙 `old enemy` = 课文 1333 | 说"某一个敌人" | enemy 在本书基本以"限定词 + 单数"出场， foe 基本以复数出场 |

### 总结句（实测宽度 22.0 / ≤30）

> enemy 那边有营地和枪炮，foe 这边只有老朋友。

### 出处清单

- `Old foes learned to contend kindly without letting rival pride spoil spring work`、`Old foes learned to contend kindly`、`old foes learned`、`foes learned to contend kindly`、`contend kindly`、`rival pride`、译文「老对手学会友好竞争，不让对手式的骄傲毁掉春耕」 —— 课文第 1332 句（段 [4,9] 第 0 句）及 `sentZh`；`rival pride` 同见 rival 卡 `note` 词伙段（与 1332 同源）。**foe 卡 `ex` 就是这一整句**（见下条）。
- `An ex-competitor now calls his old enemy a cherished close friend`、`his old enemy`、`old enemy`、`a cherished close friend`、译文「一位曾经的竞争者如今称老敌人为珍爱的密友」 —— 课文第 1333 句（段 [4,9] 第 1 句）及 `sentZh`；`old enemy` 同见 enemy 卡 `note` 词伙段（与 1333 同源）。
- `Old foes learned to contend kindly without letting rival pride spoil spring work.` / 老对手学会友好竞争，不让对手式的骄傲毁掉春耕。 —— foe 卡 `ex` / `exZh`，与课文 1332 **同一句、同一译文**（口径 8：同源，不另计）。
- `He learned to differentiate truth from lies and distinguish friends from foes`、`distinguish friends from foes`、`friends from foes`、译文「他学会了把真理与谎言区分开，把朋友与敌人辨别清楚」 —— 课文第 1609 句（章5 段19 第 3 句）及 `sentZh`；foe 侧第 2 处书内文本（带标记）。
- `The soldier fought against his enemy in the war.` / 士兵在战争中与他的敌人作战。 —— enemy 卡 `ex` / `exZh`（表里写作 `his enemy in the war`，为其连续子串）。
- `The soldiers raided the enemy camp at night.`（raid 卡 ex，译「士兵们在夜间突袭了敌人的营地」）、`The soldier was spying on the enemy camp at night.`（spy 卡 ex，译「秘密侦察敌军营地」） —— 表里 `the enemy camp` 的两处出处。
- `The soldiers fired the cannon at the enemy.`（cannon 卡 ex）、`The warrior threw a spear at the enemy.`（spear 卡 ex）、`He was called a traitor for helping the enemy.`（traitor 卡 ex）、`The scout watched the enemy's movement quietly.`（scout 卡 ex）、`He tried to strangle his enemy with a rope.`（strangle 卡 ex）、`The soldiers defend their country from enemies.`（defend 卡 ex） —— enemy 侧其余 6 条卡 ex；`the enemy's movement` 为其连续子串。
- `Good strategy needs wiser tactics than hasty guns and bitter cries`、`Each clever manoeuvre followed Grandpa's simple blueprint for safe return home` —— 课文第 1334 / 1335 句（同段后两句，只作"本段还站着哪几张卡"的背景，不进成员，与组 2 不重复展开）。
- 「同义词：foe, opponent, adversary」（enemy 卡）／「同义词：enemy」（foe 卡） —— 两卡 `note` 同义词栏，**互列对方**；opponent、adversary 在 `vocab.json` 里没有卡，按硬约束 2 不进表当成员。
- 义项「敌人；敌军」「敌人；对手」 —— 两卡 `m`；「老敌变挚友，巧谋蓝图与劝告化为助人力量」—— 段 9 `paraZh`（本组只取其中"老敌变挚友"部分，后半说的是 1334–1337）。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 证据强度与数据边界：能出卡，但两卡例句同源、foe 侧极薄（要拍板）

- 两卡 `note` 同义词栏**互列**（enemy 栏里有 foe，foe 栏里有 enemy），`m` 交集是「敌人」→ worklist 标 `all_synonyms: true` 有据；按硬约束 3 照写，区别落在**搭配对象 + 单复数与限定词框架 + 卡 `m` 第二档**三格。
- **独立文本条数（口径 8 同源合并后）**：foe 侧 **2 条**（课文 1332 + 课文 1609）——卡 `ex` 与 1332 同句同译、卡词伙 `old foe` 也指向 1332，都不另计；enemy 侧 **10 条**（课文 1333 + enemy 卡 ex + 8 张别的卡 ex），卡词伙 `old enemy` 与 1333 同源不另计。10 : 2 是本片最悬殊的一侧。
- **口径 6 处理**：worklist 义项「敌人」在 1333 的 enemy 上成立，1332 的 foe 本书译的是「老对手」；foe 真被译成「敌人」的那处是 1609（"distinguish friends from foes"），不在本段。所以标题与默认行只说"这一段各领哪样东西"，「敌人」留在 `sense` 槽。
- **"复数归 foe、单数归 enemy"是本书这 12 处的计数结论，不是规矩**：foe 卡词伙自己写的是单数 `old foe`，enemy 侧也有 `from enemies` 一处复数（defend 卡 ex），表里已按"本书 2 处全是复数／本书 10 处里 9 处单数"这种写法落地。
- **不写的**：「foe 是书面／古语、enemy 是普通说法」（两卡 `note` 只有同义词与词伙，无语域标注，本书给不出证据）、「foe 还能指棋类或体育里的对手」（1332 那句有 rival、1333 有 competitor，但那是另两张卡的词，不是 foe 的用法标注）、`a private foe`、`mortal enemy`、`the enemy of my enemy` 这类本书 0 处的写法一律不列。
- **提请拍板点**：foe 卡 `ex` 与课文 1332 一字不差、`exZh` 也与 `sentZh` 同句，本书 foe 侧独立文本只有 2 条，全部硬区别里只有"enemy 被 8 张别的卡的 ex 拿去当敌人"这一格是厚的。若他认为这种 5:1 的组该降级为义项小卡，这组是本片第一个候选；否则建议按现表落地。

---

## 组2 · strategy / tactic —— 策略 ｜ `all_synonyms`: true ｜ 段 [4,9] ｜ 句 1334

**共现位置**：1 段 1 句 —— 第 4 章段 9（全局 1332–1337），两个词挤在同一句 1334 的前后两半（`Good strategy needs` 在前、`wiser tactics` 是它要的那个）。**主锚点 = 章4 段9**（`paraZh`：老敌变挚友，巧谋蓝图与劝告化为助人力量。属第四章·历史与发明——上篇·纪念馆）。卡挂段 9 末；组 1（enemy / foe）同在本段末（1332 / 1333），本组只讲"定哪一套、改哪一招"，不再回去讲对面那批人。
主锚点 = 章4 段9

**这一段在讲什么**（只按段里真有的句子写）：1334 是本组唯一一句——好的 strategy 需要比"仓促的枪炮"和"苦涩的哭声"更明智的 tactics；紧接着 1335 说每一次 clever manoeuvre 都照着爷爷那张 simple blueprint 走（这两句一起构成本组的语境，本书把"战略—战术—调动—蓝图"排成了一串）。

**该段原文（章4 段9，成员句 + 紧邻两句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1333 | An ex-**competitor** now calls his old **enemy** a cherished close friend. | 一位曾经的竞争者如今称老敌人为珍爱的密友。 |
| 1334 | Good **strategy** needs wiser **tactics** than hasty guns and **bitter** cries. | 好的战略需要比仓促枪炮和苦涩哭声更明智的战术。 |
| 1335 | Each **clever** **manoeuvre** followed Grandpa's simple **blueprint** for safe return home. | 每次巧妙的调动都遵循爷爷安全回家的简单蓝图。 |

strategy 全书带 `[[词头:…]]` 标记 1 处（1334）、未打标记 0 处；tactic 全书带标记 1 处（1334，**那一处写的是复数 tactics**）、未打标记 0 处。卡侧：两卡互相把对方列进了自己 `note` 的同义词栏（strategy 栏 `同义词：tactic`、tactic 栏 `同义词：strategy`），**除这两栏外全书没有第三张卡用到它们** —— 本组两侧都是最薄的一档（口径 7：薄是常态，本节照写）。

### 表头一句（title）

> 一句里两个词是"上下手"的关系：strategy 是开口缺招的那一个、tactics 是它点名要的那一招——worklist 给的共同义项"策略"在本段一处都没落地（本书译文写的是「战略」和「战术」）：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| strategy | /ˈstrætədʒi/ | n. | 策略；战略 | 事先定好的那一套 | 团队为赢比赛定的那套；本段那句里缺招的正是它 | `Good strategy needs wiser tactics`（课文 1334） |
| tactic | /ˈtæktɪk/ | n. | 战术；策略 | 临场改的那一招 | 教练为了赢比赛改掉的那招；本段它被战略点名要 | `wiser tactics than hasty guns`（课文 1334） |

### 默认那一行（实测宽度 39.5 / ≤40）

```
strategy = 先定一套（good strategy），tactic = 临场改一招（changed his tactic）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段那一句里谁缺谁 | strategy | `Good strategy needs wiser tactics`（1334，它当那句的主语）、`good strategy`（卡词伙，就是课文那半句） | 说"整套打算够不够明智" | 本段它是提要求的那个：它 needs 一样东西 |
| 本段那一句里谁缺谁 | tactic | `needs wiser tactics`（1334，它是 needs 的宾语）、`wiser tactics than hasty guns`（同句，被拿来跟枪炮比） | 说"具体使的那一招" | 本段它是被要的那个：战略嫌它不够 wiser |
| 卡 `ex` 给它哪个动词、谁在做 | strategy | `The team made a smart strategy to win the game.`（卡 ex，团队 made） | 一套东西被**定**出来 | 卡给它的动词是 make，做事的是 team（一整队） |
| 卡 `ex` 给它哪个动词、谁在做 | tactic | `The coach changed his tactic to win the game.`（卡 ex，教练 changed） | 一招东西被**改**掉 | 卡给它的动词是 change，做事的是 coach（场边一个人）——定归定，改归改 |
| 前面站的修饰语 | strategy | `good strategy`（卡词伙 = 1334）、`a smart strategy`（卡 ex） | 夸这套打算本身好不好 | 本书给它配的都是**原级**：good、smart |
| 前面站的修饰语 | tactic | `wiser tactic`（卡词伙）、1334 原文是 `wiser tactics than hasty guns and bitter cries` | 夸这一招比另一招高明 | 本书给它配的是**比较级** wiser，还带 than —— 它一出场就在跟别的手法比 |
| 数怎么写 | strategy | 1334 是 `Good strategy needs`（前面不带 a the，单数形）；卡 ex 才带 a：`a smart strategy` | 说"这一类打算"时 | 本书课文那处是零冠词单数写法，复数 strategies 本书没有 |
| 数怎么写 | tactic | 卡上词伙记的是单数 `wiser tactic`，课文 1334 那处写的是复数 `tactics`（词头标的是 tactic） | 说"好几招"时 | 两卡的词伙都记单数，课文只给了 tactic 复数——这一格是本书自己的一处对不齐 |

### 总结句（实测宽度 22.5 / ≤30）

> 一句里 strategy 缺招，tactic 是被要的那一招。

### 出处清单

- `Good strategy needs wiser tactics than hasty guns and bitter cries`、`Good strategy needs wiser tactics`、`needs wiser tactics`、`wiser tactics than hasty guns`、`hasty guns`、`bitter cries`、译文「好的战略需要比仓促枪炮和苦涩哭声更明智的战术」 —— 课文第 1334 句（段 [4,9] 第 2 句）及 `sentZh`。这是两词在本书**唯一的同段书证，也是各自的唯一一处**。
- `good strategy` —— strategy 卡 `note` 词伙段（与 1334 同源，不另计）。
- `wiser tactic` —— tactic 卡 `note` 词伙段（单数写法只在卡上；课文那处是 `wiser tactics`）。
- `The team made a smart strategy to win the game.` / 这个团队制定了一项聪明的策略来赢得比赛。 —— strategy 卡 `ex` / `exZh`（表里写作 `made a smart strategy`、`a smart strategy`，均为其连续子串）。
- `The coach changed his tactic to win the game.` / 教练改变了战术以赢得比赛。 —— tactic 卡 `ex` / `exZh`（表里写作 `changed his tactic`，为其连续子串）。两条 `ex` 的尾巴 `to win the game` 一字不差。
- `Each clever manoeuvre followed Grandpa's simple blueprint for safe return home`、`clever manoeuvre`、`simple blueprint`、译文「每次巧妙的调动都遵循爷爷安全回家的简单蓝图」 —— 课文第 1335 句（段 [4,9] 第 3 句）及 `sentZh`；两条词伙同见 manoeuvre / blueprint 卡 `note` 词伙段（与 1335 同源）。**这两个词没与本组并成一组，按硬约束 2 不进成员表**，只作本组语境的邻居。
- `An ex-competitor now calls his old enemy a cherished close friend` —— 课文第 1333 句（组 1 成员句，只作本组前一句背景）。
- 「同义词：tactic」（strategy 卡）／「同义词：strategy」（tactic 卡） —— 两卡 `note` 同义词栏，**互列对方**。
- 义项「策略；战略」「战术；策略」 —— 两卡 `m`；「老敌变挚友，巧谋蓝图与劝告化为助人力量」—— 段 9 `paraZh`（本组只取其中"巧谋蓝图"那一半，且那一半写的是 1335 的 manoeuvre / blueprint）。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 证据强度与数据边界：能出卡，但共享义项「策略」在本书一处都没落地（要拍板）

- 两卡 `note` 同义词栏**互列**（strategy 栏里有 tactic，tactic 栏里有 strategy），`m` 交集是「策略」→ worklist 标 `all_synonyms: true` 有据；按硬约束 3 照写，区别落在**搭配对象（谁 needs 谁）+ 卡 ex 的动词与主语 + 修饰语级别 + 单复数写法**四格。
- **口径 6 处理（义项名不副实）**：worklist 那个共同义项「策略」来自两卡 `m` 的交集，但本书译文一处都没这样写——1334 把 strategy 译成「战略」、把 tactics 译成「战术」；两卡 `ex` 的 `exZh` 倒是都用了「策略」二字（strategy 卡）与「战术」（tactic 卡）。也就是说**本书没把 tactic 译成"策略"用过**。所以标题与默认行只说"这一段各领哪样东西"，「策略」留在 `sense` 槽。
- **同源合并（口径 8）**：strategy 卡词伙 `good strategy` = 课文 1334；tactic 卡词伙 `wiser tactic` 指向的也是 1334 那半句（只是数不同）。两侧独立文本各 **2 条**（课文 1334 + 卡 ex 1），是本片最对称也最薄的一组；卡侧文本除本卡 `ex` 外只有互相那两栏同义词（strategy 栏 `同义词：tactic`、tactic 栏 `同义词：strategy`，逐张扫 vocab 的 `ex` / `note` 后确认：除这两栏外没有第三张卡提到它们）。
- **卡上第二档**：strategy 卡 `m` 的「战略」正是 1334 的译文落点，「策略」档只在卡 `ex` 的中文里出现；tactic 卡 `m` 的「策略」档本书 0 处（见上）。
- **不写的**：「strategy 是长远全局、tactic 是具体一着」（两卡 `note` 只有同义词与词伙，无语义标注；本组表里"上下手"那一格靠的是 1334 那一句的**句内关系**，只算本书这 1 处）、「strategy 更宏观」这类层级断言、`military strategy`、`political tactics`、`delaying tactic`、`strategic plan` 本书 0 处一律不列（`strategic`、`tactical` 两个形容词形在全书数据里 0 见）。
- **提请拍板点**：本组两词的全部书证就是**同一句 1334**，且共享义项「策略」在本书译文里对不上任何一处。若他要求"共现必须至少两处独立书证"或"共享义项须在共现段成立"，这组是本片第二个候选退回项。我的判断：值得留——"卡 ex 一个用 make、一个用 change；一个主语是 team、一个是 coach"这 4 个词都是可 grep 的真区别，落在口径要求的"搭配对象"上，不依赖那个义项名。

---

## 组3 · happen / occur —— 发生 ｜ `all_synonyms`: false ｜ 段 [4,10] ｜ 句 1343

**共现位置**：1 段 1 句 —— 第 4 章段 10（全局 1338–1343），两个词挤在同一句 1343 的前后两半，由 `where` 串起来（前半给巨变、后半给善行）。**主锚点 = 章4 段10**（`paraZh`：真力不在武力而在援手，日常善行带来巨变。属第四章·历史与发明——上篇·纪念馆）。本段是本片唯一一处"1 句 2 词"，卡挂段 10 末。
主锚点 = 章4 段10

**这一段在讲什么**（只按段里真有的句子写）：1338–1341 说平静的抗议驳斥谣言、真正的力量不在残酷的武力而在温柔相助的手、热食补身子、柔歌减折磨；1342 说每个小 event 里有值得大人安静想的东西；1343 收尾——善行每天在学校周围 occur 的地方，巨变就能 happen。

**该段原文（章4 段10，成员句 + 前两句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1341 | Soft songs **can** **mitigate** torment and bring **comfort** to lonely widows. | 柔和的歌能减轻折磨，给孤独的寡妇带来安慰。 |
| 1342 | Each small **event** holds some **matter** **worthy** of **quiet** adult thought. | 每个小事件都包含值得成年人安静思考的事情。 |
| 1343 | Great change **can** **happen** where kind deeds **occur** daily around schools. | 善行每天在学校周围发生的地方，就会有巨大变化。 |

happen 全书带 `[[词头:…]]` 标记 1 处（1343）、未打标记 0 处；occur 全书带标记 1 处（1343）、未打标记 0 处。差在卡侧：happen 另在 **9 张别的卡的 `ex`** 里出场（crash / murder / strike / attack / extinction / simultaneous / formation / conjunction / foresee），occur 在别的卡里 **0 引用**（§五.13 分档：书内标记数 1:1，卡侧引用数 9:0）。

### 表头一句（title）

> 一句里两个"发生"各干一件事：前半那句巨变后面什么都没挂，后半那句善行挂着 daily 和 around schools——两卡 `ex` 是同一句事故只换了动词（本书给的"可换"物证，见「数据边界」），所以这一组的区别只能落在状语上：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| happen | /ˈhæpən/ | v. | 发生 | 那一回、那一天撞上来 | 昨天那起事故、十字路口的撞车、清晨对基地的袭击、恐龙那回灭绝 | `Great change can happen`（课文 1343） |
| occur | /əˈkɜːr/ | v. | 发生；出现 | 隔一阵就来一轮 | 善行每天在学校周围那一轮 | `kind deeds occur daily`（课文 1343） |

### 默认那一行（实测宽度 38.5 / ≤40）

```
happen = 说哪一回（The accident happened），occur = 说多久一次（occur daily）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段那一句里它后面挂什么 | happen | `Great change can happen`（1343，后面直接是 `where` 从句，自身不带状语） | 说"会有那么一回事" | 本段它光站着，时间、频率一个都没给 |
| 本段那一句里它后面挂什么 | occur | `kind deeds occur daily around schools`（1343）、`occur daily`（卡词伙，就是这一处） | 说"多久来一轮" | 本段它后面挂了 daily + around schools 两样东西，本书唯一一条词伙也正是这个 |
| 本书这几处它的主语是哪样事 | happen | `The accident happened yesterday.`（卡 ex）、`The car crash happened at the intersection.`（crash 卡 ex）、`The murder happened last night in the park.`（murder 卡 ex）、`The strike on the base happened at dawn.`（strike 卡 ex）、`The attack happened at night.`（attack 卡 ex）、`The extinction of dinosaurs happened millions of years ago.`（extinction 卡 ex） | 事故、袭击、灭绝这类一次性的事 | 本书 happen 的 11 条文本里 6 条主语是事故／袭击／灭绝那一类；另 5 条是 change / events / formation / rain and thunder / what |
| 本书这几处它的主语是哪样事 | occur | `kind deeds occur daily around schools`（1343）、`The accident occurred yesterday.`（卡 ex） | 本书只给了它两样：善行、事故 | 课文那处它接的是**善行**、而且天天来；卡 ex 那处又接了事故——两处都不是"一次性"写法 |
| 它后面挂哪类状语 | happen | `yesterday`、`at the intersection`、`last night in the park`、`at dawn`、`at night`、`millions of years ago`、`simultaneously`、`in conjunction`、`when water vapor cools`、`tomorrow`（`I cannot foresee what will happen tomorrow.`，foresee 卡 ex） | 说什么时候、在哪儿、跟什么同时 | 本书 happen 的 11 处里 10 处带时间／地点／方式状语，还独有"进 what 从句"那一处（`what will happen tomorrow`） |
| 它后面挂哪类状语 | occur | `daily around schools`（1343）、`yesterday`（卡 ex） | 说频率、说范围 | 本书 occur 只有 2 条，1 条给频率（daily）、1 条跟 happen 卡 ex 完全同形（只换动词）；本书没有 `occurs` / `occurring` 这类写法 |
| 卡上给几档、note 给不给东西 | happen | 卡 `m` 只「发生」一档；`note` 是**空串**——零词伙、零同义词栏 | 只这一件事 | 它是本片唯一一张 note 全空的成员卡：happen 侧的全部证据都来自课文 1343 + 卡 ex + 那 9 张别的卡的 ex |
| 卡上给几档、note 给不给东西 | occur | 卡 `m` 两档「发生；出现」；`note` 只有词伙段 `occur daily`，**没有同义词栏** | 它还得兼"出现"那一档 | 「出现」那一档本书 0 处（1343 译的是「发生」），本库给不出 occur 表"出现"的例子 |

### 总结句（实测宽度 17.5 / ≤30）

> happen 说哪一回，occur 说多久一次。

### 出处清单

- `Great change can happen where kind deeds occur daily around schools`、`Great change can happen`、`kind deeds occur daily`、`occur daily around schools`、`daily around schools`、译文「善行每天在学校周围发生的地方，就会有巨大变化」 —— 课文第 1343 句（段 [4,10] 第 5 句）及 `sentZh`。这是两词在本书唯一的同段书证，也是各自**唯一一处带标记的课文文本**。
- `The accident happened yesterday.` / 事故昨天发生了。 —— happen 卡 `ex` / `exZh`（表里写作 `The accident happened`、`happened yesterday`、`yesterday`，均为其连续子串）。
- `The accident occurred yesterday.` / 事故昨天发生了。 —— occur 卡 `ex` / `exZh`（表里写作 `occurred yesterday`，为其连续子串）。两条 `ex` **只差中间那个动词**，`exZh` 一字未改。
- `occur daily` —— occur 卡 `note` 词伙段（与课文 1343 **同源**，不另计）。
- `The car crash happened at the intersection.`（crash 卡 ex）／`The murder happened last night in the park.`（murder 卡 ex）／`The strike on the base happened at dawn.`（strike 卡 ex）／`The attack happened at night.`（attack 卡 ex）／`The extinction of dinosaurs happened millions of years ago.`（extinction 卡 ex）／`The two events happened simultaneously.`（simultaneous 卡 ex）／`The formation of clouds happens when water vapor cools.`（formation 卡 ex）／`The rain and the thunder happened in conjunction.`（conjunction 卡 ex）／`I cannot foresee what will happen tomorrow.`（foresee 卡 ex） —— happen 侧其余 9 条卡 ex，表里的状语逐条取自这些句子。
- `Each small event holds some matter worthy of quiet adult thought`、译文「每个小事件都包含值得成年人安静思考的事情」 —— 课文第 1342 句（段 [4,10] 第 4 句）及 `sentZh`；event、matter 各有自己的卡，未与本组并成一组，按硬约束 2 不进成员，只说明本段"事件"一词本书用的是 event。
- `Soft songs can mitigate torment and bring comfort to lonely widows` —— 课文第 1341 句（本段前一句，只作背景）。
- 义项「发生」「发生；出现」 —— 两卡 `m`；happen 卡 `note` 为空串、occur 卡 `note` 只有词伙段（两卡互不列同义词）；「真力不在武力而在援手，日常善行带来巨变」—— 段 10 `paraZh`。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 证据强度与数据边界：能出卡，两卡 `ex` 只差动词这一同形事实按 §五.14 记为合法物证（不再提请拍板）

- **同形这一事实照写（§五.14①）**：happen 卡 `ex` `The accident happened yesterday.` 与 occur 卡 `ex` `The accident occurred yesterday.` 是同一句、`exZh`「事故昨天发生了。」一字未改，只换中间那个动词（与批次 3 组 2 的 imitate / mimic 同形，那组 worklist 标的是 `true`）。这是本书能给的**最强"可换"物证**，按 §五.14 记进本节当合法物证：这两句**各算一侧书证**（happen 侧算进上面那 11 条、occur 侧算进 2 条），不并成一条，也不据此否认同形。
- **两栏没互列，故 false 有据（§五.14②）**：`all_synonyms` 只是"两卡的『同义词：』栏有没有互列"这个机械位（定义在 `tools/confusable_groups.py:301`），结构上反映不了例句的形状。happen 的 `note` 是空串（压根没有同义词栏）、occur 的 `note` 只有词伙段 `occur daily`，两栏没互列 → worklist 标 `false` 是**有据的机械结果**，既不代表"本书证明它俩不同义"，也不构成退回理由。本组按现表出卡，**不再逐组提请拍板**（§五.14 已把这个形状定死）。
- **本组不宣称"共同义项在本段不成立"**：「发生」在两词身上都成立（卡 ex 两处都译「发生了」）；反过来说，本书译文在 1343 把「发生」三个字只给了 occur，happen 那半句被折成「就会有巨大变化」。这一格如实写在"中文落点"里。
- **同源合并（口径 8）**：occur 卡词伙 `occur daily` 就是课文 1343，不另计 → occur 侧独立文本 **2 条**（课文 + 卡 ex）；happen 侧独立文本 **11 条**（课文 1 + 卡 ex 1 + 别的卡 ex 9）。11 : 2 是本片最悬殊的一侧（厚的一侧全靠别的卡的例句）。
- **"坏事配 happen、善行配 occur"只算本书这 11 处的分布，不是词本身的规矩**——occur 卡 ex 那句的主语就是 accident，正好把这条打平，所以表里措辞停在"本书 happen 的 11 条里 6 条"。同理，`happened` 8 处 / `happens` 1 处 / `happen` 2 处 vs `occurred` 1 处 / `occur` 1 处，也是本书形态清单；本书没有 occurs / occurring / occurrence 三种写法（`strategies`、`strategic`、`tactical` 之类同理，见组 2）。
- **不写的**：「occur 比 happen 正式／书面」（两卡 `note` 无语域标注，happen 卡 note 甚至全空）、「happen 只能接事、occur 能接人」（本书 13 条文本里 occur 从接过人，但样本只有 2 条，不足以立规矩）、「occur to sb 使想起」（本书 0 处）；`It so happens that ...`、`as happens`、`occur frequently` 本书 0 处一律不列。
- **本组无待拍板项**：原先报的"元数据自相矛盾（卡面说可换、worklist 说不同义）"已由 §五.14 定死处置 —— 按现表出卡、同形写进上面第一条当合法物证、不改 worklist 的 `false`、也不退回义项表，不再逐组上报。留在数据边界里的只有一条**措辞锁**：本组的真区别（时间状语 vs 频率状语）建立在**课文那唯一 1 句**的不对称上——happen 那半句恰好没带状语，这属于本书写法而非词的分工，落地措辞要停在"本书这一处"。

---

## 组4 · grave / tomb —— 坟墓 ｜ `all_synonyms`: false ｜ 段 [4,12] ｜ 句 1350 / 1351

**共现位置**：1 段 2 句 —— 第 4 章段 12（全局 1350–1355），成员句 1350（grave）与 1351（tomb）**紧挨着两句、同一处墓园**。**主锚点 = 章4 段12**（`paraZh`：墓园寂静海陆安眠，石阶之上和平在心中生长。属第四章·历史与发明——上篇·纪念馆）。卡挂段 12 末；本组是本片唯一一处两词各占一句、且两侧厚度相同的组。
主锚点 = 章4 段12

**这一段在讲什么**（只按段里真有的句子写）：1350 说一场寂静的葬礼在白杨后面、一座长青苔的墓旁边结束；1351 说一座朴素的 tomb 立得远远的，远离那片硝烟旧地的喧闹；1352–1354 转到老海事地图上的 naval routes、沉睡的 fleet 与在绿松旁歇着的 troop；1355 收在慢行者沿石阶前行、和平在心里 progress。

**该段原文（章4 段11 末句 1349 + 段12 前三句 1350–1352；成员两句 + 前后各一句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1349 | Each lost **casualty** asks us to **bury** pride along with rusty shells. | 每一位死伤者都提醒我们把骄傲和生锈炮弹一起埋葬。 |
| 1350 | One silent **funeral** closed near a mossy **grave** behind white poplars. | 白杨后苔藓墓旁，一场寂静的葬礼结束。 |
| 1351 | A **plain** **tomb** stands far from noisy **hell** of smoky old fields. | 朴素的坟墓远离硝烟旧地的喧闹地狱。 |
| 1352 | Old **maritime** maps show **naval** routes once used by worried sailors. | 古老的海事地图显示了忧心水手曾用的海军航线。 |

grave 全书带 `[[词头:…]]` 标记 1 处（1350）、未打标记 0 处；tomb 全书带标记 1 处（1351）、未打标记 0 处。两词在别的卡的 `ex` / `note` 里都 **0 引用**（`graves` 那个字符串是 engrave 卡的词，不算 grave）。两侧独立文本各 2 条。段归属按 §五.12 那把尺复核过：表里 1349 是**章4 段11 的末句**（段11 起 1344、共 6 句），只作前情；1350–1352 才属**章4 段12**（起 1350），本组两张卡挂段 12 末。

### 表头一句（title）

> 挨着的两句各给一座墓：1350 那座是被靠近的那个点——葬礼在它旁边收尾；1351 那座是自己立着的——它站得远离那片硝烟地。两个词本书译的都是坟墓，区别全在句子给的动作上：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| grave | /ɡreɪv/ | n.（卡 `m` 另给了 adj. 一档） | 坟墓；严重的 | 白杨后那处、能走到旁边 | 1350 白杨后面那座长青苔的；卡 ex 里祖父的那座，人们去探视 | `a mossy grave`（课文 1350） |
| tomb | /tuːm/ | n. | 坟墓；冢 | 立着的一座、里面装东西 | 1351 那座素面的、离战场远远的；卡 ex 里那座装满文物的古冢 | `A plain tomb stands`（课文 1351） |

### 默认那一行（实测宽度 39.5 / ≤40）

```
grave = 葬礼刚散的那旁边（a mossy grave），tomb = 素面立着（plain tomb stands）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段它站在句子里哪一头 | grave | `closed near a mossy grave`（1350，在 `near` 后面，是葬礼靠过去的那个点）、`behind white poplars`（同句给它的方位） | 说"走到它旁边"、"它在哪排树后面" | 本段它是被靠近的对象，句子里干活的是那场 funeral |
| 本段它站在句子里哪一头 | tomb | `A plain tomb stands far from noisy hell`（1351，它当主语、动词是 stands） | 说"那座东西立在那儿、离得很远" | 本段它自己就是主语，句子给它的动作是"立着"，还给了它一个"远离"的方向 |
| 本书让它挨着谁 | grave | `silent funeral`（1350，funeral 卡词伙就是这一处）、`a mossy grave`（青苔） | 跟"办丧事"同框 | 本书给 grave 的邻居是一场葬礼和几棵白杨——它旁边在办事 |
| 本书让它挨着谁 | tomb | `plain tomb`（1351，tomb 卡词伙就是这一处；同句 plain 也挂了自己的卡）、`noisy hell of smoky old fields`（同句它远离的地方） | 跟"打过仗的地方"对举 | 本书给 tomb 的邻居是战场那头的喧闹——它被摆在对立面，离硝烟远远的 |
| 卡 `ex` 让它接什么 | grave | `They visited the grave of their grandfather.`（卡 ex）／`the grave of their grandfather` | 说"这是谁的墓" | 它的 `of` 后面接**人**；funeral 卡 ex 也是同一位长辈（`He went to his grandfather's funeral.`）——卡上 grave 通向人名 |
| 卡 `ex` 让它接什么 | tomb | `The ancient tomb was filled with artifacts.`（卡 ex）／`was filled with artifacts`、`The ancient tomb` | 说"这座墓里有什么" | 它的句子接的是**东西**（文物）和一个年纪修饰（ancient）；本书没给过 tomb 后接 of + 人名这种写法 |
| 卡上给几档、note 给不给东西 | grave | 卡 `m` 两档：「坟墓」+ `adj.` 严重的；`note` 是**空串**——零词伙、零同义词栏 | 读到 grave 要先判是名词还是那个形容词 | 本组两卡里薄的一侧：grave 只有课文 + 卡 ex 这 2 条，"严重的"一档本书 0 处 |
| 卡上给几档、note 给不给东西 | tomb | 卡 `m` 两档「坟墓；冢」；`note` 词伙 `plain tomb`（与 1351 同源） | 说大土堆那种冢 | 它的第二档「冢」本书也 0 处，但卡上至少留了一条现成词伙；tomb 没有形容词档，不会像 grave 那样跳出来当"严重的" |

### 总结句（实测宽度 19.5 / ≤30）

> grave 旁边刚办完葬礼，tomb 里装着文物。

### 出处清单

- `One silent funeral closed near a mossy grave behind white poplars`、`closed near a mossy grave`、`a mossy grave`、`mossy grave`、`behind white poplars`、`One silent funeral closed`、译文「白杨后苔藓墓旁，一场寂静的葬礼结束」 —— 课文第 1350 句（段 [4,12] 第 0 句）及 `sentZh`；`silent funeral` 同见 funeral 卡 `note` 词伙段（与 1350 **同源**）。
- `A plain tomb stands far from noisy hell of smoky old fields`、`A plain tomb stands`、`plain tomb stands`、`plain tomb`、`stands far from noisy hell`、`noisy hell`、`of smoky old fields`、译文「朴素的坟墓远离硝烟旧地的喧闹地狱」 —— 课文第 1351 句（段 [4,12] 第 1 句）及 `sentZh`；`plain tomb` 同见 tomb 卡 `note` 词伙段（与 1351 **同源**）。
- `They visited the grave of their grandfather.` / 他们参观了祖父的坟墓。 —— grave 卡 `ex` / `exZh`（表里写作 `the grave of their grandfather`，为其连续子串）。
- `The ancient tomb was filled with artifacts.` / 这座古老的坟墓里充满了文物。 —— tomb 卡 `ex` / `exZh`（表里写作 `was filled with artifacts`、`filled with artifacts`、`The ancient tomb`，均为其连续子串）。
- `He went to his grandfather's funeral.` / 他去参加了他祖父的葬礼。 —— funeral 卡 `ex` / `exZh`（表里写作 `He went to his grandfather`，为其连续子串）。它说明卡上 grave 与 funeral 挂在同一位长辈身上，不进成员。
- `Each lost casualty asks us to bury pride along with rusty shells`、`bury pride`、译文「每一位死伤者都提醒我们把骄傲和生锈炮弹一起埋葬」 —— 课文第 1349 句（章4 段11 末句）及 `sentZh`；`bury pride` 同见 bury 卡 `note` 词伙段（与 1349 同源）。bury、casualty 各有自己的卡，未与本组并成一组，按硬约束 2 不进成员。
- `They bury the dead in the cemetery.` / 他们把死者埋葬在墓地里。 —— bury 卡 `ex` / `exZh`（本组只用来报备：本书"墓地"这个处所词出现在 bury 卡 ex 上，写作 `cemetery`， grave / tomb 两卡都没有这一档）。
- `Old maritime maps show naval routes once used by worried sailors`、`A quiet fleet sleeps while a tired troop rests beside green pines`、`Slow walkers proceed along stone steps while peace will progress in hearts` —— 课文第 1352 / 1353 / 1355 句（本段其余有卡的词，只作表头背景，不进成员）。
- 义项「坟墓；严重的」「坟墓；冢」 —— 两卡 `m`；grave 卡 `note` 为空串、tomb 卡 `note` 只有词伙段（两卡互不列同义词，也都没有同义词栏）；「墓园寂静海陆安眠，石阶之上和平在心中生长」—— 段 12 `paraZh`（本组只取"墓园寂静"部分，海陆安眠与石阶说的是 1352–1355）。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 证据强度与数据边界：能出卡，两侧各 2 条、共享义项两边都成立

- 两卡 `note` 都没有同义词栏（grave 的是空串），worklist 标 `all_synonyms: false` → 属"中文义项撞车"型近义，撞车撞在两卡 `m` 的第一档都写「坟墓」上。按硬约束 3 的思路把区别落在**句内位置 + 邻居 + 卡 ex 接人还是接物 + 卡上另一档**。
- **共享义项在本段成立**（不像组 2 / 组 5 要说轻）：1350 译文「苔藓墓」、1351 译文「坟墓」，两处都是坟墓档；本书没有一处把 grave 当形容词"严重的"用过。
- **同源合并（口径 8）**：funeral 卡词伙 `silent funeral` = 课文 1350、tomb 卡词伙 `plain tomb` = 课文 1351、bury 卡词伙 `bury pride` = 课文 1349，三条都不另计 → grave 侧独立文本 **2 条**（课文 + 卡 ex）、tomb 侧 **2 条**（课文 + 卡 ex）。本片唯一一处两侧一样薄且对齐的组。
- **"grave 接人、tomb 接物"这条只算了 2 条 vs 2 条**：来自两卡 `ex` 各 1 句（`the grave of their grandfather` / `was filled with artifacts`），本书没有第三处；tomb 后接 of 人名、grave 后接 filled with 东西这两种写法本书两处都没给过，落地时别把它写成规矩。
- **不写的**：「tomb 是石室／贵族大墓、grave 是土坑」（两卡 `note` 无语域与规模标注，本书"ancient + artifacts"只 1 句，撑不起这条定义）、「grave 作动词表"铭记"」（engrave 卡才有那档，本书 grave 卡 `m` 只有 n. 与 adj.）、`family grave`、`graveyard`、`above ground`、`tombstone` 本书 0 处一律不列。
- 无需拍板：本组义项成立、两侧厚度一致，只是绝对条数薄（各 2 条）。

---

## 组5 · disturb / interrupt —— 打扰 ｜ `all_synonyms`: false ｜ 段 [4,14] ｜ 句 1363 / 1364

**共现位置**：1 段 2 句 —— 第 4 章段 14（全局 1362–1367），成员句 1363（disturb）与 1364（interrupt）**相邻两句、同一件主题**（上一句是吵到心里，下一句是吵断手上的游戏）。**主锚点 = 章4 段14**（`paraZh`：止息争吵干扰，退守妥协让出和平之路。属第四章·历史与发明——上篇·纪念馆）。卡挂段 14 末。
主锚点 = 章4 段14

**这一段在讲什么**（只按段里真有的句子写）：1362 说那种 harmful 的玩笑带坏新人、毁了安静的晚课；1363 说大声的争吵 disturb 掉困倦的心，而自夸的喊叫去 interfere with 课程；1364 说迟来的钟声 interrupt 了游戏，但好孩子不从 duty 上 retreat；1365–1367 是干叶收缩、疲惫的脚步 withdraw、老人靠 compromise  withstand 寒风、最后 surrender 刀具放白鸽。

**该段原文（章4 段14，成员两句 + 前一句）**

| 全局句 | 英文原句 | 中文译句 |
|---|---|---|
| 1362 | Such **harmful** jokes **mislead** newcomers and **spoil** **quiet** evening classes. | 这种有害的玩笑误导新人，破坏安静的晚课。 |
| 1363 | Loud quarrels **disturb** sleepy minds while boastful shouts **interfere** with lessons. | 大声争吵打扰困倦的心，自夸的喊叫干扰课程。 |
| 1364 | Late bells **interrupt** games but kind kids never **retreat** from **duty**. | 迟来的钟声打断游戏，但好孩子从不从职责上退缩。 |

disturb 全书带 `[[词头:…]]` 标记 1 处（1363）、未打标记 0 处；interrupt 全书带标记 1 处（1364）、未打标记 0 处。卡侧引用（逐张扫 vocab 各卡 `note` 后计数，§五.15）：disturb 被 **affect、impact、interrupt 三张卡**的同义词栏收进去（affect / impact 收在"影响"那一档、interrupt 收在"打断"那一档），interrupt 除自己那张卡外本书 0 引用。本段"打扰"这一档实际挤了三个词（disturb 1363 / interfere 1363 同句 / interrupt 1364），interfere 有自己的卡（`m` 是 `v. 干扰`）、未与本组并成一组，按硬约束 2 不进成员 —— 成员集合由切片包 `words` 钉死，与邻居词有没有卡无关。

### 表头一句（title）

> 挨着的两句都在说吵：上一句吵的是心里那点困劲儿，下一句吵的是正玩着的那局——worklist 给的共同义项"打扰"只落在 1363 的 disturb 上，1364 的 interrupt 本书译的是「打断」：

### 成员（items）

| 词 | 音标 | 词性 | `sense`（卡上 `m`） | `core`（≤12 字） | `scene` | `eg`（含出处） |
|---|---|---|---|---|---|---|
| disturb | /dɪˈstɜːrb/ | v. | 打扰；妨碍 | 吵到正安静待着的那位 | 1363 那群大声的争吵吵到困倦的心；卡 ex 里读书读到一半被吵的那位 | `disturb sleepy minds`（课文 1363） |
| interrupt | /ˌɪntəˈrʌpt/ | v. | 打断；打扰；打岔 | 掐断正在进行的那件 | 1364 那阵迟来的钟声掐掉了游戏；卡 ex 里说话说到一半被插的那位 | `Late bells interrupt games`（课文 1364） |

### 默认那一行（实测宽度 38.5 / ≤40）

```
disturb = 吵困倦的心（sleepy minds），interrupt = 打断那局（interrupt games）
```

### 差异维度表（diff）

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 课文原句） | 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 本段它后面接的是哪样东西 | disturb | `Loud quarrels disturb sleepy minds`（1363，宾语是 minds）、`disturb sleepy minds`（卡词伙，就是这一处） | 吵的是一颗正待着的心、一个安静状态 | 本书给它的是"人心"，那东西本来在困、在静，被吵完还是在那儿 |
| 本段它后面接的是哪样东西 | interrupt | `Late bells interrupt games`（1364，宾语是 games）、`interrupt games`（卡词伙，就是这一处） | 吵的是一件正在进行的事 | 本书给它的是"那局游戏"，被掐断的就是那件事本身 |
| 卡 `ex` 里那半句 while 在干什么 | disturb | `Please don't disturb me while I'm reading.`（卡 ex，`disturb me while I'm reading`）、`don't disturb me while I'm reading`（祈使 + 否定） | 有人在安静地读 | 两卡 ex 框架一模一样（动词 + me + while 从句），它那条从句里是**读书**，而且是开口求人别吵 |
| 卡 `ex` 里那半句 while 在干什么 | interrupt | `He interrupted me while I was speaking.`（卡 ex，`interrupted me while I was speaking`） | 有人在说话说到一半 | 它那条从句里是**说话**——一个正往前走的进程，被切一下就断了；卡 ex 是陈述句、过去时 |
| 同义词栏往哪边列（单向） | disturb | 卡 `note`「同义词：affect, impact, influence」——三个都是"影响"词，栏里**没有 interrupt**；反过来 affect 卡、impact 卡、interrupt 卡三张各自把 disturb 收进自己的栏（我逐张扫过 `note`，见出处清单） | 本书把 disturb 归到"起作用"那一堆 | 它自己不认 interrupt 是同义词，它认的是影响 |
| 同义词栏往哪边列（单向） | interrupt | 卡 `note`「同义词：disrupt, disturb, interfere with」——栏里**有 disturb**，还有一条带介词的 `interfere with` | 本书把 interrupt 归到"搅进去"那一堆 | 单向承认：interrupt 收 disturb，disturb 不收 interrupt（这就是 worklist 标 false 的物证）。disrupt 在 `vocab.json` 里没卡，按硬约束 2 不进表 |
| 本段那一句用哪个词管"干扰课程" | 两个词 | 1363 后半句写的是 `boastful shouts interfere with lessons`（`interfere with`，本书用的正是 interrupt 卡栏里那个块），不是 `interrupt lessons`；disturb 与 interrupt 都直接带宾语、不挂介词 | 想说"吵到课"时 | 本书给"课"配的词是 interfere（必须 with），interrupt 被排到下一句去管游戏；晚课（1362 `quiet evening classes`）那边本书用的又是 spoil |

### 总结句（实测宽度 22.0 / ≤30）

> disturb 吵安静待着的，interrupt 切断正做的。

### 出处清单

- `Loud quarrels disturb sleepy minds while boastful shouts interfere with lessons`、`Loud quarrels disturb`、`disturb sleepy minds`、`sleepy minds`、`boastful shouts interfere with lessons`、`interfere with lessons`、`boastful shouts interfere`、译文「大声争吵打扰困倦的心，自夸的喊叫干扰课程」 —— 课文第 1363 句（段 [4,14] 第 1 句）及 `sentZh`；`disturb sleepy minds` 同见 disturb 卡 `note` 词伙段（与 1363 **同源**）。interfere 有自己的卡（`v. 干扰`），未与本组并成一组，按硬约束 2 不进成员。
- `Late bells interrupt games but kind kids never retreat from duty`、`Late bells interrupt games`、`interrupt games`、`kind kids never retreat from duty`、`retreat from duty`、译文「迟来的钟声打断游戏，但好孩子从不从职责上退缩」 —— 课文第 1364 句（段 [4,14] 第 2 句）及 `sentZh`；`interrupt games` 同见 interrupt 卡 `note` 词伙段（与 1364 **同源**）。retreat、duty 各有自己的卡，不进成员。
- `Please don't disturb me while I'm reading.` / 我读书时请不要打扰我。 —— disturb 卡 `ex` / `exZh`（表里写作 `disturb me`、`disturb me while I'm reading`、`don't disturb me while I'm reading`、`while I'm reading`，均为其连续子串）。
- `He interrupted me while I was speaking.` / 我在说话时他打断了我。 —— interrupt 卡 `ex` / `exZh`（表里写作 `interrupted me`、`interrupted me while I was speaking`、`while I was speaking`，均为其连续子串）。
- `Such harmful jokes mislead newcomers and spoil quiet evening classes`、`quiet evening classes`、`spoil quiet evening classes`、译文「这种有害的玩笑误导新人，破坏安静的晚课」 —— 课文第 1362 句（段 [4,14] 第 0 句）及 `sentZh`（本组只用它说明"课"在本书另有 spoil 配着）。
- `Don't interfere when I am working.` / 我工作时不要打扰。 —— interfere 卡 `ex` / `exZh`（只作"干扰"这一档的第二处出处，不进成员；该卡 `note` 为空串）。
- 「同义词：affect, impact, influence」（disturb 卡）／「同义词：disrupt, disturb, interfere with」（interrupt 卡） —— 两卡 `note` 同义词栏，**单向互列**；`同义词：disturb, impact, influence` 见 affect 卡 `note`、`同义词：affect, disturb, effect, influence` 见 impact 卡 `note`（跨卡登记：这两处加上面 interrupt 卡那一栏，把 disturb 收进自己栏的一共三张卡）。**词头存在性自查（§五.15：逐条 grep 过 `shadow/data/vocab.json` 全部 3245 个词头）** —— `affect`、`impact`、`interfere` 各有自己的卡；`influence` 与 `disrupt` **都没有卡**，它们只是别的卡栏里/句里的一个词：`influence` 出现在 dominate / affect / disturb / impact 四张卡的同义词栏里，另外在 radius 卡 `ex` `The new policy has a wide radius of influence.` 与 propaganda 卡 `ex` `The government used propaganda to influence people's opinions.` 两句里作句内词；词头里只有一个形近词 `influx`（`n. 涌入`，与本组无关），下游照卡找 `influence` 会撞空。`disrupt` 只作为 interrupt 卡同义词栏里的一个词条、外加 marine / ecosystem / wildlife 三张卡的词伙 `disrupt marine ecosystems`、`disrupt wildlife` 出现（全在生态语境）。课文 1833 句里 influence / disrupt 均 0 处。**这三样邻居词一律不进成员表，理由与它们有没有卡无关**：成员集合由切片包 `words` 钉死（硬约束 2），邻居词只作背景与"谁认谁"那一格的引用，扩员／换员是 worklist 生成层的事。
- 义项「打扰；妨碍」「打断；打扰；打岔」 —— 两卡 `m`；「止息争吵干扰，退守妥协让出和平之路」—— 段 14 `paraZh`（本组只取其中"止息争吵干扰"部分，退守妥协说的是 1365–1367）。
- 门禁核对：本组所有反引号内英文串已跑 `tools/check_compare_draft.py`，0 条查不到。

### 证据强度与数据边界：能出卡，但共同义项「打扰」只落在 disturb 一侧（口径 6 的标准报备，非待拍板项）

- 两卡同义词栏**单向**（interrupt 栏里写着 disturb；disturb 栏里全是"影响"那三个词、没有 interrupt），worklist 标 `all_synonyms: false` 与卡面一致 → 属"中文义项撞车 + 一方承认一方不承认"型。按硬约束 3 把区别落在**宾语类型 + 卡 ex 的 while 从句 + 同义词栏方向 + 介词框架**四格。
- **口径 6 处理**：交集「打扰」在 1363 的 disturb 上成立（译文正是「打扰」），在 1364 的 interrupt 上不成立——本书译文写的是「打断」，interrupt 卡自己那条 `exZh` 也写「打断了我」。也就是说**本书没把 interrupt 译成"打扰"用过**（它"打扰"这一档只在卡 `m` 上，1364 走的是"打断"档）。标题与默认行都只说"这一段各管哪样东西"、「打扰」留在 `sense` 槽。
- **同源合并（口径 8）**：disturb 卡词伙 `disturb sleepy minds` = 课文 1363、interrupt 卡词伙 `interrupt games` = 课文 1364，两条都不另计 → 两侧独立文本各 **2 条**（课文 1 + 卡 ex 1）。disturb 侧另有 3 处跨卡登记（affect 卡、impact 卡、interrupt 卡这三张卡的同义词栏里出现 disturb，只是词表条目、不是句子，不计条数）。
- **"两卡 ex 同框架"是本组最硬的一格**：`v. + me + while + 从句` 两词都能用，唯一的区别在从句里那个人正在干什么（reading / speaking）。这条是 2 条 vs 2 条的计数结论，别写成"interrupt 只能接进行时"。
- **不写的**：「disturb 语气比 interrupt 轻／重」（两卡 `note` 无语域与程度标注）、「interrupt 只能用于说话、disturb 只能用于安静场合」（本书 1364 的宾语是 games、1363 的是 minds，各 1 处，撑不起这种分工）、「disturb 还有"妨碍"档」（卡 `m` 第二档，本书 0 处书证，只报备不造句）、`disturb the peace`、`interruption`、`put someone off` 这类本书没有的写法一律不列。
- **义项半边按口径 6 报备（不是待拍板项）**：本组共享义项「打扰」在共现段只成立一半（1364 译「打断」），是口径 6 的标准情形，我按现表处理、不退回，也不必他另开口径。
- **邻居词 interfere 不拉进本组（硬约束 2／§五.14 已定，不再提请拍板）**：本段 1363 一句里 disturb 与 interfere 并排、interrupt 卡同义词栏又明写 `interfere with`，三词同档；但成员集合由切片包 `words` 钉死，interfere 虽有卡（`m` 是 `v. 干扰`）却没与本组并成一组 —— 扩员／换员是 worklist 生成层的事，草稿一律不自行并组。interfere 的处置保持现在这个形状：只在"本段那句用哪个词管干扰课程"那一格逐字引书证（`boastful shouts interfere with lessons`），并标明"有卡但未并组、不进成员、不计条数"。本组真正要报的那一行是**同义词栏单向**：interrupt 栏收了 disturb，disturb 栏只收"影响"那三个词、没收 interrupt —— 这一格是这张表最有信息量的地方，不是缺陷。

---

## 附：本切片复核索引（给门禁 / 抽查用）

| 组 | worklist_idx | words | senses | `all_synonyms` | paras | sents | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 129 | enemy / foe | 敌人 | true | [4,9] | 1332,1333 | 出卡但**foe 侧极薄**：foe 卡 `ex` 就是课文 1332（同句同译），foe 独立文本 2 条 vs enemy 10 条（含 8 张别的卡 ex）；硬区别 = enemy 被派给开炮／摸营地那类句子、foe 只出现在"老对头讲和"与"分清敌我"；本段 foe 译「老对手」，「敌人」档落在 1609 |
| 2 | 130 | strategy / tactic | 策略 | true | [4,9] | 1334 | 出卡但**共享义项「策略」本书一处都没落地**（1334 译「战略」/「战术」）：两词全部书证就是同一句 1334；硬区别 = 句中谁 needs 谁 + 卡 ex 一个 make（team）一个 change（coach）+ 修饰语 good/smart 对 wiser + tactic 在课文里是复数 tactics |
| 3 | 131 | happen / occur | 发生 | false | [4,10] | 1343 | 出卡，义项两侧都成立（两卡 exZh 都是「事故昨天发生了。」）；**两卡 ex 同一句只换动词 = §五.14 的合法物证，写进「数据边界」、不再提请拍板**（false 有据：两栏没互列，happen 的 note 是空串）。硬区别 = 本书 happen 11 条里 10 条带时间/地点状语、独有进 what 从句那一处；occur 2 条、卡词伙 `occur daily` 是频率 |
| 4 | 132 | grave / tomb | 坟墓 | false | [4,12] | 1350,1351 | 出卡，义项两侧都成立（1350「墓」/1351「坟墓」）：两侧各 2 条独立文本、最对称也最薄；硬区别 = 本段一个被靠近（closed near a mossy grave）一个自己立着（A plain tomb stands）+ 卡 ex 一个接人名一个接文物 + grave 卡 `note` 全空且另带 `adj. 严重的` 一档 |
| 5 | 133 | disturb / interrupt | 打扰 | false | [4,14] | 1363,1364 | 出卡但**「打扰」只落在 disturb 一侧**（1364 与 interrupt 卡 ex 的中文都是「打断」）；同义词栏**单向**（interrupt 收 disturb，disturb 只收"影响"三词）；硬区别 = 宾语是心还是那局游戏 + 两卡 ex 同框架只差 reading / speaking + 本段"干扰课程"本书用的是 `interfere with`。**interfere 不拉进组**（硬约束 2：成员集合由切片包 `words` 钉死；influence / disrupt 也没有词头卡，只作栏内词条），只报"同义词栏单向"这一行，不再提请拍板 |

- 全局句号口径按章偏移 `[0, 339, 651, 883, 1277, 1492]`（全书 1833 句）自算：章4 段 9 起 1332、段 10 起 1338、段 12 起 1350、段 14 起 1362（各段 6 句）——5 组 8 个句号与切片包 `sents`（1332 / 1333 / 1334 / 1343 / 1350 / 1351 / 1363 / 1364）逐个对得上；旁证 1609 属章5 段19 第 3 句（组 1 的 foe 侧第 2 处）。
- 挂段位置：组 1、组 2 → 章4 段9 末（同段两张，切入角不同：组 1 讲对面那批人，组 2 讲定哪一套、改哪一招）；组 3 → 段10 末；组 4 → 段12 末；组 5 → 段14 末。按 worklist_idx 升序排。
- §五.12 自查：本片 4 个共现段（章4 段 9 / 10 / 12 / 14）的 `paraZh` 只当背景引用，且逐组写明"本组只取其中哪半句"，所有"这一段在讲什么"均由 `paragraphs` 里真有的句子支撑；章3 段 22–43 那个坏掉的主题栏本片没用到、也没去改它。
- 本片 5 组的宽度标称值全部用 `tools/width_rule.py` 的同一把尺量出（汉字与全角标点各 1、其余 0.5）：默认行 38.0 / 39.5 / 38.5 / 39.5 / 38.5，总结句 22.0 / 22.5 / 17.5 / 19.5 / 22.0，无一条超上限。
- 本文件不引用任何在 `shadow/data/vocab.json`、`shadow/data/sections.json` 里查不到的英文串；"不写的"清单里那些本书没有的写法一律按反例列出并当场否定。门禁命令与结果见文件头小结（`python3 tools/check_compare_draft.py work/辨析草稿/4_08.md` → PASSED）。
- 落地成卡对象时：`title` = 各组「表头一句」，`summary` 的「简单记」= 各组「默认那一行」，`diff` 各行 `label` = 「维度」列，键名用成员词头。
