# 影子跟读 · 移动端排查修复报告

**时间**: 2026-09-11　**对象**: `雅思影子跟读.html`（6 篇 / 1678 句 / 3219 标注词）

---

## 一、排查范围

对整个项目进行全量代码审查，重点聚焦移动端（≤700px）的交互体验、触摸操作、布局适配和性能问题。涵盖 CSS 响应式、JS 事件处理、手势支持、安全区域等维度。

---

## 二、修复清单（共 13 项）

### Bug 修复（P0）

| # | 问题 | 严重程度 | 修复方案 |
|---|---|---|---|
| 1 | **移动端词库抽屉遮罩点击无法关闭**：原代码用 `e.target === .layout` 精确匹配，但 `::after` 伪元素在部分浏览器上 target 不一致 | 高 | 改用 `asideEl.contains(e.target)` 反向排除法——点击不在 aside 内即关闭 |
| 2 | **抽屉打开时背景仍可滚动**：无 body scroll lock，用户在词库列表滑动时会带动正文滚动 | 高 | `togglePanel()` 中根据开关状态设置 `document.body.style.overflow = 'hidden'` |
| 3 | **进度条触摸坐标兼容性**：`paintSeekPreview` 和 `seekToEvent` 使用 `e.clientX`，触摸拖动时可能取到 undefined | 中 | 兼容 `e.touches[0].clientX`，触摸拖动不再漂移 |
| 4 | **body 默认 class="panel-open"**：桌面端首次加载即展开侧栏，非预期行为 | 中 | 移除 body 上的 `panel-open` 默认类 |

### 体验优化（P1）

| # | 问题 | 修复方案 |
|---|---|---|
| 5 | **进度条触摸区域过小**（11px）：手指难以精确拖动 | 移动端 seekWrap/seekTrack 高度增至 22px，hit-test 伪元素扩大至 -12px/-20px |
| 6 | **播放条按钮触摸目标不足**（min-height 26px）：低于 Apple HIG 推荐的 44px | 增至 34px + padding 加大，接近最佳触摸区 |
| 7 | **倍速菜单在移动端可能溢出视口**：从底部向上展开，靠近屏幕底部时被裁切 | 移动端覆写 `bottom` 和 `min-width`，确保不越界 |
| 8 | **顶栏右侧按钮无滚动提示**：水平溢出时用户不知道还有更多控件 | 添加 `mask-image` 右边缘 8% 透明渐变，暗示可横滑 |
| 9 | **词库抽屉打开时 edgeTab 仍可见**：与抽屉重叠，易误触 | `.layout.panel-open #edgeTab { opacity: 0; pointer-events: none }` |
| 10 | **无下滑关闭手势**：只能点 ✕ 或遮罩关闭抽屉 | 注入 touchstart/touchend 监听，下滑 >60px 自动关闭 |
| 11 | **下拉可能触发页面刷新**：移动端 overscroll 传导到浏览器 | `overscroll-behavior-y: contain` 阻断传导 |
| 12 | **云登录弹窗在移动端定位不佳**：固定 top:64px 可能被顶栏遮挡 | 改为 `left/right: 12px; width: auto; top: 52px` 自适应 |
| 13 | **语音选择器触摸触发滚动**：下拉操作与 topbar 横向滚动冲突 | `touch-action: manipulation` 防止浏览器误判手势 |

---

## 三、技术细节

### Fix 1 — 抽屉遮罩关闭（核心 Bug）

**原代码**：
```javascript
if (e.target.closest('.layout') && e.target === document.querySelector('.layout.panel-open')) {
  togglePanel();
}
```

**问题**：`::after` 伪元素的事件 target 在不同浏览器上行为不一致。Safari/Chrome 可能返回 `.layout`，但某些 WebView 返回子元素。

**修复后**：
```javascript
if (window.matchMedia('(max-width: 700px)').matches && document.querySelector('.layout.panel-open')) {
  const asideEl = document.getElementById('panel');
  if (asideEl && !asideEl.contains(e.target) && !e.target.closest('#edgeTab')) {
    togglePanel();
  }
}
```

### Fix 2 — Body Scroll Lock

```javascript
function togglePanel() {
  const layout = document.querySelector('.layout');
  const mobile = window.matchMedia('(max-width: 700px)').matches;
  if (mobile) {
    const opening = !layout.classList.contains('panel-open');
    layout.classList.toggle('panel-open');
    document.body.style.overflow = opening ? 'hidden' : '';
  } else {
    document.body.classList.toggle('panel-open');
  }
}
```

### Fix 3 — 触摸坐标兼容

```javascript
// Before
const x = e.clientX - r.left;
// After
const cx = e.clientX !== undefined ? e.clientX : (e.touches && e.touches[0] ? e.touches[0].clientX : 0);
const x = cx - r.left;
```

### Fix 5 — 进度条触摸区域

```css
/* 移动端 */
#seekWrap { height: 22px; }    /* 原 11px */
#seekTrack { height: 22px; }   /* 原 11px */
#seekRail, #seekFill { top: 9px; }  /* 原 top: 0 */
```

### Fix 8 — 下滑关闭手势

```javascript
panel.addEventListener('touchend', e => {
  if (!dragging) return;
  dragging = false;
  const dy = (e.changedTouches[0] || {}).clientY - startY;
  if (dy > 60) togglePanel();
}, { passive: true });
```

---

## 四、回归校验

- 所有 CSS 媒体查询保持 `max-width: 700px` 断点一致
- 桌面端行为不受影响（所有新增逻辑均有 `matchMedia` 守卫）
- `panel-open` 类的两种用法（`body.panel-open` 桌面 / `layout.panel-open` 移动）保持独立
- 进度条拖动的 pointer 事件兼容 `clientX` 和 `touches[0].clientX`
- 语音合成、书签、跳转、云端登录等核心功能代码未改动

---

## 五、备份

修复前文件已备份至：`雅思影子跟读.backup-20260911.html`
