# 辨析卡草稿 · 批次 2 / 切片 06（5 组）

> 3 行小结：本文件写了 **5 组**（worklist 下标 35–39，全部落在第 1 章《校园与文化》·上篇「新生学期」的段 21、段 22）。
> 门禁 `python3 tools/check_compare_draft.py work/辨析草稿/2_06.md` → **PASSED**（结果同见文件末行；5 个成员词全书各只出现 1 次，所以每格搭配都回原文核过，没凑一条）。
> 提请拍板 3 件：① 组 1（cite / reference）共享义项「提及」在课文唯一那处共现里不成立，我按词性重写了表头，要不要退回单语境你定；② 组 3（abstract / summary）abstract 侧"摘要"这一档只有课文 470 一条证据（卡 ex 走的是形容词义），偏薄；③ 组 1/2/3 同挂段 21 末、组 4/5 同挂段 22 末，"同段末排几张卡、什么顺序"切片包没给口径。

> **门禁 2 修复轮（报告 `work/辨析审核/audit_c.md`，本文件 = worklist 组 35–39）**：① 同源计数按口径 8 全部合并订正 —— 组 35 两侧各 3→**2 条**、组 36 各 3→**2 条**、组 37 summary 侧 4→**2 条**、组 38 detect 侧→**3 条**、组 39 postulate 侧 3→**2 条**（presume 侧同步写明 2 条）；② 组 38 默认行的「测出」换成「早早查出来的」（E-BLOCK：`测出` 是 detect 卡 `m` 第一档原词），表格里「书里另一处 detect」改成「另一处在 sensor 卡 ex（不是课文）」；③ 组 39 边界句撤回「切片另有组 195 / 217 收它们」里对 `speculate` / `predict` / `assume` 的误报（反查 worklist 全 220 组：这三词一组都没被收；组 1 的 `elicit`/`extract`/`assume`、组 4 的 `speculate`/`predict` 同此订正）；④ 组 37 默认行为留余量把（morning summary）换成同一条卡 ex 里的（at the end），40.0→**37.5**；⑤ 组 38 表头「这一句里」→「476 同句里」。宽度：5 组默认行 38.5 / 39.0 / 37.5 / 39.0 / 39.5，总结句 24.5 / 23.5 / 24.5 / 23.0 / 23.0，与拍定口径实测逐条一致。逐条对照见 `work/辨析审核/FIX_2_05_07.md`。

**本文件口径**（照批次 1A/1B）
- 组成员严格等于切片给的 2 个词，别的目标词（`elicit` / `extract` / `assume` / `suppose` / `hypothesis` / `discern` / `sensor`）只在出处里出现、不进表。
- 全局句号 = 章偏移 `[0, 339, 651, 883, 1277, 1492]` + 段前句数 + 段内序号，自算结果与切片 `sents` 逐条对齐（468 / 469 / 470 / 471 / 473 / 474 / 476）。
- 宽度实测只有一把尺（2026-09-20 拍定，= `scripts/validate_data.py._width`）：**汉字与全角标点各记 1，其余（拉丁字母、空格、半角括号）记 0.5**；默认行 ≤40、总结句 ≤30。本文件标的就是这把尺的数（与批次 1A 同一口径），逐行已用 `tools/audit_ledger.py` 复量，标称 = 实测。
- 每个成员在**全书只出现 1 次**（已逐词全库核过，见各组「证据强度」），所以表里宁可空一格也不造搭配。

---

## 组 1 · cite / reference —— 提及 ｜ `all_synonyms: false` ｜ 段 [1,21] ｜ 句 468（同句共现）

**共现位置**：1 段 1 句 —— 第 1 章段 21（全局 467–472），两个词挤在同一句 468。卡挂段 21 末，**主锚点 = 章1 段21**（`paraZh`：整理引用搭建知识框架。`subheads[1][0]`：上篇·新生学期）。

**该段原文（章1 段21）**

| 全局句 | 英文原句（成员词加粗）| 中文译句 |
|---|---|---|
| 467 | Each member shared an honest opinion and tried to introduce one helpful method. | 每人分享诚实观点，并介绍一种有用方法。 |
| 468 | They kept one useful **reference** book and learned how to **cite** sources correctly. | 他们留了一本有用参考书，学会正确引用来源。 |
| 469 | Good questions could elicit fresh thoughts and one perfect quote for the wall. | 好问题能引出新想法和墙上完美引言。 |
| 470 | Sam learned to extract key lines and write a short clear abstract daily. | 山姆学会抽取关键句，每天写简短清晰摘要。 |
| 471 | Mei wrote a brief morning summary but refused to simply assume an easy win. | 梅写了晨间简要总结，但不轻易假设轻松获胜。 |
| 472 | Snow began to fall gently over silent rooftops near midnight. | 快半夜时，雪开始静静地落在寂静的屋顶上。 |

**先报一个口径问题（要你拍板）**：切片给的共享义项是「提及」，两张卡 `m` 里也确实都有这两个字（cite = `v. 引用；提及`，reference = `n. 参考；提及`）。但**全书唯一那处共现（468）里两个词都不是"提及"**：`cite sources` 译「引用来源」、`reference book` 译「参考书」。也就是说「提及」是词卡交集给的、不是语料给的。
→ 建议：卡仍挂段 21 末，但表头按「一个是你伸手去引的动作、一个是桌上那本书」讲（词性差），把「提及」这档收进差异维度表第 1 行说明，**不在标题里宣称本段两词都指"提及"**。若你要求"辨析必须撞同一个义项"，这组按规格退回单语境，证据就是上面这句 468。

**表头一句（title）**
> 这一段在学整理引用，两个"提及"一个是你伸手去引的那个动作、一个是桌上备着查的那本书：

**默认那一行（summary 的「简单记」｜实测宽度 38.5 / ≤40）**
```
cite = 伸手去引（cite sources），reference = 桌上那本参考书（reference book）
```

**成员（items）**

| 词 | 音标 | 词性 | sense（卡上 `m`）| core（≤12 字）| scene | eg（含出处）|
|---|---|---|---|---|---|---|
| cite | /saɪt/ | 卡 `m` 只给动词一档 | 提及（引用）| 把来源点名出来 | 学会正确引用来源那一课 | `learned how to cite sources correctly`（课文 468）|
| reference | /ˈrefrəns/ | 卡 `m` 只给名词一档 | 提及（参考）| 备着查的那本书 | 小组里留一本有用的书当查阅本 | `one useful reference book`（课文 468）|

**差异维度表（diff）**

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 卡 ex / 课文原句）| 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 「提及」这档谁能独立当动作 | cite | `cite sources`（卡词伙，课文 468 就是它）、`He cited a book in his essay.`（卡 ex）| 说「他提到／引了哪本书」，词本身就把动作干完 | cite 卡 `m` 是动词，后面直接顶宾语，不带介词 |
| 「提及」这档谁能独立当动作 | reference | `Please use this book for reference.`（卡 ex）、`reference book`（卡词伙 + 课文 468）| 它只能在名字位：`for reference` 后面、或挤在 book 前当定语 | 要写成动作得借 cite 卡同义词栏里那个 `refer to`，reference 自己动不起来 |
| 同义词栏给的方向 | cite | 卡 note 同义词：quote, refer to, mention（三条都是动词性说法）| 想换词时它有一排动词可换 | cite 卡有同义词栏，可以顺着换 |
| 同义词栏给的方向 | reference | 卡 note 只有词伙一段，没有同义词栏 | 换词没处查，只能回课文 | reference 这一侧证据比 cite 薄，表里不给它凑同义词 |
| 中文落点 | 两个词 | 468 译「学会正确引用来源」／「一本有用参考书」（`sentZh`）| 汉译英时先问：中文"提到"要的是动作还是那本书 | 本书译文自己就把 cite 处理成「引用」、reference 处理成「参考」，两个都没译成「提及」 |

**总结句（实测宽度 24.5 / ≤30）**
> cite 是动作，reference 是名词，本书只给了它一本书

**出处清单**
- `cite sources` —— cite 卡 `note` 词伙段；同一短语见课文第 468 句（`learned how to cite sources correctly`）。
- `He cited a book in his essay.` / 他在文章中引用了一本书。 —— cite 卡 `ex` / `exZh`。
- cite 卡 `note` 同义词段：`quote, refer to, mention`。
- `reference book` —— reference 卡 `note` 词伙段。
- `one useful reference book`、`learned how to cite sources correctly` —— 课文第 468 句（段 [1,21] 第 2 句）；译文「一本有用参考书」「正确引用来源」同句 `sentZh`。
- `Please use this book for reference.` / 请使用这本书作为参考。 —— reference 卡 `ex` / `exZh`（表中写作 `for reference`，为其连续子串）。
- 义项「引用；提及」「参考；提及」 —— 两卡 `m`；「整理引用搭建知识框架」—— 段 21 `paraZh`。

**证据强度与边界**
- 两个词**全书各只出现 1 次**，且就是同一句 468；reference 卡 `note` **没有同义词栏**（只有词伙 `reference book`）。同源合并后（口径 8）：**reference 侧 2 条独立文本** = 课文 468（＝卡词伙 `reference book`，同一处）+ 卡 ex 1 句；**cite 侧同理 2 条** = 课文 468（＝卡词伙 `cite sources`，同一处）+ 卡 ex 1 句。前一版按「卡词伙 1 条 + 卡 ex 1 句 + 课文 1 句」各记 3 条，是把同一处文本数了两遍，已订正。
- **不写的**：`citation`、`cited in`（`vocab.json` 无 `citation` 卡、课文无此串）；「reference 比 cite 正式」——两卡 `note` 均无语域标注；reference 的"提及"义在本书 0 例（唯一的 468 走"参考书"），所以表里只说"这一档独立当不了动作"，不宣称它没这个义项。
- 同句 468 之外的目标词 `elicit`（469）、`extract`（470）、`assume`（471）都有卡，但本批次 worklist 全 220 组里没有一组收它们（逐个反查过），按硬约束 2 一律不并进本表。

---

## 组 2 · cite / quote —— 引用 ｜ `all_synonyms: true` ｜ 段 [1,21] ｜ 句 468 / 469

**共现位置**：1 段 2 句 —— 第 1 章段 21（全局 467–472），成员句 468（cite）与 469（quote）**相邻两句**。卡挂段 21 末，**主锚点 = 章1 段21**（`paraZh`：整理引用搭建知识框架。）。与组 1 同段，本卡表头必须写成"引的对象差在哪"，别和组 1 的"动作 vs 书"撞一句话。

**该段原文（章1 段21，与组 1 同段，只列成员两句 + 前后各一句）**

| 全局句 | 英文原句（成员词加粗）| 中文译句 |
|---|---|---|
| 467 | Each member shared an honest opinion and tried to introduce one helpful method. | 每人分享诚实观点，并介绍一种有用方法。 |
| 468 | They kept one useful reference book and learned how to **cite** sources correctly. | 他们留了一本有用参考书，学会正确引用来源。 |
| 469 | Good questions could elicit fresh thoughts and one perfect **quote** for the wall. | 好问题能引出新想法和墙上完美引言。 |
| 470 | Sam learned to extract key lines and write a short clear abstract daily. | 山姆学会抽取关键句，每天写简短清晰摘要。 |

**表头一句（title）**
> 紧挨着的两句，一句在讲引用来源的规矩、一句在挑墙上要贴的那一句：两个"引用"接的不是同一种东西：

**默认那一行（summary 的「简单记」｜实测宽度 39.0 / ≤40）**
```
cite = 后面直接跟来源（cite sources），quote = 把那句原话搬来（perfect quote）
```

**成员（items）**

| 词 | 音标 | 词性 | sense（卡上 `m`）| core（≤12 字）| scene | eg（含出处）|
|---|---|---|---|---|---|---|
| cite | /saɪt/ | 卡 `m` 只给动词一档 | 引用 | 把从哪儿来的点名 | 学规矩那一课：来源要引对 | `learned how to cite sources correctly`（课文 468）|
| quote | /kwəʊt/ | 卡 `m` 给名词 + 动词两档（引文／引用；另有「报价」一档本书没用）| 引用 | 搬过来的那一句 | 好问题勾出一句能贴上墙的话 | `one perfect quote for the wall`（课文 469）|

**差异维度表（diff）**

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 卡 ex / 课文原句）| 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 引的是死物还是人话 | cite | `cite sources`（卡词伙，课文 468 即它）、`He cited a book in his essay.`（卡 ex）| 后面挂来源、一本书 | 它带的宾语是**出处本身**：sources / a book |
| 引的是死物还是人话 | quote | `He quoted Shakespeare in his speech.`（卡 ex）、`perfect quote`（卡词伙）、`one perfect quote for the wall`（课文 469）| 后面挂一个人、一句原话 | 它带的是**谁说过的那句**：Shakespeare、贴上墙的那一句 |
| 能不能当名字数出来 | cite | 卡 `m` 只有动词一档；课文 468 也是 `how to cite sources`（跟在 to 后）| 只能当动作使 | cite 在本书没有名词形可用 |
| 能不能当名字数出来 | quote | 卡 `m` 第一档就是名词；课文 469 写 `one perfect quote`（one + 形容词 + 它）| 能说"一句引言"，能被数、能被挑 | quote 天生可数：469 那句里它就是被 elicit 出来的那个东西 |
| 整组同义落在哪一档 | 两个词 | 两卡 `note` 同义词栏互指（cite 卡写 quote；quote 卡写 cite），课文 468／469 一动作一名词 | 汉译英先判中文"引用"是动作还是"一句引文" | 卡上互列同义词只对到**动词那一档**；本书唯一那处 quote 用的是名词，两词换不了 |

**总结句（实测宽度 23.5 / ≤30）**
> cite 后面直接跟来源，quote 是能贴上墙的那句原话

**出处清单**
- `cite sources`、`He cited a book in his essay.` / 他在文章中引用了一本书。 —— cite 卡 `note` 词伙段 / `ex` / `exZh`。
- `learned how to cite sources correctly`、译文「学会正确引用来源」 —— 课文第 468 句（段 [1,21] 第 2 句）及 `sentZh`。
- `perfect quote` —— quote 卡 `note` 词伙段。
- `one perfect quote for the wall`、`Good questions could elicit fresh thoughts and one perfect quote for the wall`、译文「墙上完美引言」 —— 课文第 469 句（段 [1,21] 第 3 句）及 `sentZh`。
- `elicit fresh thoughts` —— elicit 卡 `note` 词伙段（与课文 469 同一处）；表中只作 469 那句的上下文，不进组员。
- `He quoted Shakespeare in his speech.` / 他在演讲中引用了莎士比亚的话。 —— quote 卡 `ex` / `exZh`。
- 两卡同义词栏互指 —— cite 卡 `note` 含 quote；quote 卡 `note` 同义词：cite（切片 `all_synonyms: true` 即由此而来）。
- 义项「引用；提及」「引文；报价；引用」 —— 两卡 `m`；「整理引用搭建知识框架」—— 段 21 `paraZh`。

**证据强度与边界**
- cite 全书 1 次（468）、quote 全书 1 次（469）。卡词伙与课文是同一处文本（`cite sources`＝468、`perfect quote` 是 469 `one perfect quote for the wall` 的子串），按口径 8 同源合并 → **独立文本各 2 条**（课文那句 + 卡 ex 那句），前一版记的「各 3 条」已订正；比组 1 并不更厚。
- **不写的**：「quote 比 cite 口语／cite 比 quote 学术」——两卡 `note` 无语域标注，本书也无第二处语料，无据；quote 卡 `m` 的「报价」一档本书 0 例（无 price / cost 同现），不进表；`citation`、`in quotation marks` 之类的串在两份数据里都查不到，不写。
- 与组 1 的分工：组 1 讲 cite vs reference（动作 / 那本书），本组讲 cite vs quote（引出处 / 搬原话），两组同段但表头与默认行不重复，落地时段 21 末要能排开。

---

## 组 3 · abstract / summary —— 摘要 ｜ `all_synonyms: false` ｜ 段 [1,21] ｜ 句 470 / 471

**共现位置**：1 段 2 句 —— 第 1 章段 21（全局 467–472），成员句 470（abstract）与 471（summary）**相邻两句、同一句式**（都是"某人学会／写了某段东西"）。卡挂段 21 末，**主锚点 = 章1 段21**（`paraZh`：整理引用搭建知识框架。）。段 21 已排组 1、组 2 两张卡，本组是第三张 —— 表头只讲"怎么 produced 出来的"，别再回去讲引用。

**该段原文（章1 段21，成员两句 + 前后各一句）**

| 全局句 | 英文原句（成员词加粗）| 中文译句 |
|---|---|---|
| 469 | Good questions could elicit fresh thoughts and one perfect quote for the wall. | 好问题能引出新想法和墙上完美引言。 |
| 470 | Sam learned to extract key lines and write a short clear **abstract** daily. | 山姆学会抽取关键句，每天写简短清晰摘要。 |
| 471 | Mei wrote a brief morning **summary** but refused to simply assume an easy win. | 梅写了晨间简要总结，但不轻易假设轻松获胜。 |
| 472 | Snow began to fall gently over silent rooftops near midnight. | 快半夜时，雪开始静静地落在寂静的屋顶上。 |

**表头一句（title）**
> 挨着的两句，一个是先抽关键句、写下来亮在最前面的一段，一个是事情讲完了在最后再说一遍：

**默认那一行（summary 的「简单记」｜实测宽度 37.5 / ≤40）**
```
abstract = 先抽关键句（clear abstract），summary = 最后说一遍（at the end）
```

**成员（items）**

| 词 | 音标 | 词性 | sense（卡上 `m`）| core（≤12 字）| scene | eg（含出处）|
|---|---|---|---|---|---|---|
| abstract | /ˈæbstrækt/ | 卡 `m` 两档：名词 + 抽象的 | 摘要（n. 摘要；概要）| 抽关键句攒成的一段 | 山姆每天写完的那一小段 | `write a short clear abstract daily`（课文 470）|
| summary | /ˈsʌməri/ | 卡 `m` 只给名词一档 | 摘要（n. 总结；摘要）| 收个尾再说一遍 | 一早课上那段简要总结 | `a brief morning summary`（课文 471）|

**差异维度表（diff）**

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 卡 ex / 课文原句）| 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 它是先抽还是后收 | abstract | `extract key lines`（extract 卡词伙，与 470 同句）、`write a short clear abstract daily`（课文 470）| 一堆东西摆在前面，它是在**前面**亮出来的那一小段 | 470 的次序就是它的定义：先抽关键句，再写 abstract |
| 它是先抽还是后收 | summary | `He gave a summary of the story at the end.`（卡 ex，`at the end`）、`a brief morning summary`（课文 471）| 事情讲完了再来一遍，它排在**后面** | 卡 ex 把 at the end 写死了：summary 是收尾那道手续 |
| 除了"那段东西"还管别的吗 | abstract | 卡 `m` 第二档是形容词；卡 ex `The painting shows an abstract shape.`、词伙 `illustrate abstract theories`（theory 卡、illustrate 卡同条）| 夸画、夸理论不具体时，它站在名词前 | abstract 一张卡带两档，summary 卡上只有名词一档 |
| 除了"那段东西"还管别的吗 | summary | 卡 `m` 只给「总结；摘要」；卡词伙 `brief morning summary` / `morning summary` 里它前面挂的是**时间**（morning）| 说某天某课收的尾：morning summary | summary 没有形容词档，打扮它的形容词只出现在它前面 |
| 中文落点 | 两个词 | 470 译「简短清晰摘要」／471 译「晨间简要总结」（`sentZh`）| 汉译英先看中文给的是「摘要」还是「总结」 | 本书译文自己已经分家：abstract → 摘要，summary → 总结 |

**总结句（实测宽度 24.5 / ≤30）**
> abstract 还能形容画和理论，summary 只管收尾说一遍

**出处清单**
- `write a short clear abstract daily`、`a short clear abstract`、`clear abstract`、译文「每天写简短清晰摘要」 —— 课文第 470 句（段 [1,21] 第 4 句）及 `sentZh`。
- `extract key lines` —— extract 卡 `note` 词伙段（与课文 470 同一处）；本表只作"先抽后写"的顺序证据，不进组员。
- `illustrate abstract theories` —— abstract 卡 `note` 词伙段（theory 卡、illustrate 卡同条词伙互指）。
- `The painting shows an abstract shape.` / 这幅画展示了一个抽象的形状。 —— abstract 卡 `ex` / `exZh`（表中写作 `an abstract shape`，为其连续子串）。
- `a brief morning summary`、`Mei wrote a brief morning summary`、译文「晨间简要总结」 —— 课文第 471 句（段 [1,21] 第 5 句）及 `sentZh`。
- `brief morning summary`、`morning summary` —— summary 卡 `note` 词伙段。
- `He gave a summary of the story at the end.` / 他在结尾对故事做了总结。 —— summary 卡 `ex` / `exZh`（表中写作 `a summary of the story`、`at the end`，均为连续子串）。
- 义项「摘要；概要；抽象的」「总结；摘要」 —— 两卡 `m`；「整理引用搭建知识框架」—— 段 21 `paraZh`。

**证据强度与边界（这组要报备一处）**
- abstract 全书 1 次（470）、summary 全书 1 次（471）。**abstract 卡上唯一那句例句走的是形容词义**，名词"摘要"这一档在两份数据里只有课文 470 一处 + 卡 `m` 一个字面 —— 也就是共享义项「摘要」的 abstract 侧证据只有一条。表里因此只把 abstract 的名词用法钉在"先抽后写"这个顺序上（470 同句的 `extract key lines` 是它唯一的现场证据），不给它凑额外搭配。
- summary 侧：`brief morning summary`（卡词伙）就是课文 471 那半句、`morning summary` 又是它的前缀 —— 三条指同一处文本，加卡 ex 那句才是 **2 条独立文本**（前一版记的「四条文本方向一致」已按口径 8 订正）。就这 2 条的方向仍然一致：卡 ex 那句写死了 `at the end`（收尾），课文那句给了 brief（简短）+ morning（时间），所以"收尾"这一格不是靠条数堆出来的。
- **不写的**：`abstract of a paper`、`write a summary of`（本书与卡上均无此串）；「abstract 用于学术论文、summary 用于日常」——两卡 `note` 没有语域标注，课文 470／471 也没给出论文语境（该段只到"引用来源"为止）；`syllabus` 卡同义词栏里那条 summary（`curriculum, outline, summary`）属"教学大纲 vs 总结"另一条线，`outline` 在 `vocab.json` 无卡，不进表。

---

## 组 4 · detect / perceive —— 察觉 ｜ `all_synonyms: false` ｜ 段 [1,22] ｜ 句 476（同句共现）

**共现位置**：1 段 1 句 —— 第 1 章段 22（全局 473–478），两个词挤在同一句 476，由同一个主语 `Sharp eyes` 管着。卡挂段 22 末，**主锚点 = 章1 段22**（`paraZh`：大胆假设细心观察保持清醒。`subheads[1][0]`：上篇·新生学期）。组 5（postulate / presume）同在本段，排在这张前面。

**该段原文（章1 段22，成员句 476；整段列出供表头改写）**

| 全局句 | 英文原句（成员词加粗）| 中文译句 |
|---|---|---|
| 473 | Teachers would never presume cheating nor idly suppose a fiasco without verification. | 老师们绝不会凭空调定有人作弊，也不会不作核实就猜要出大乱子。 |
| 474 | Lin put forward one brave new hypothesis and one quiet simple postulate for her friends to test. | 林提出了一个大胆的新假说，还有一个安安静静、简单清楚的公设，让朋友们去检验。 |
| 475 | Freshmen loved to speculate about scores but none could truly predict results. | 新生爱猜测分数，却无人能真正预测结果。 |
| 476 | Sharp eyes could **perceive** small changes and **detect** rising stress early. | 眼尖的人能感觉到细小的变化，早早察觉正在升高的压力。 |
| 477 | Anna could discern tired smiles and stayed fully conscious of time passing. | 安娜能分辨出那些强撑的笑，也始终清楚时间在一点点过去。 |
| 478 | Seniors reckon late effort useful and deem steady sleep equally important. | 学长认为深夜努力有用，视稳定睡眠同等重要。 |

**表头一句（title）**
> 476 同句里"眼尖的人"一口气干了两件事：把细小的变化看出来、把正在升上来的压力早一步查出来——两个"察觉"接的不是同一种东西：

**默认那一行（summary 的「简单记」｜实测宽度 39.0 / ≤40）**
```
detect = 早早查出来的（rising stress），perceive = 看出小差别（small changes）
```

**成员（items）**

| 词 | 音标 | 词性 | sense（卡上 `m`）| core（≤12 字）| scene | eg（含出处）|
|---|---|---|---|---|---|---|
| detect | /dɪˈtekt/ | 卡 `m` 只给动词一档（v. 测出；察觉）| 察觉 | 把没摆到明处的找出来 | 机器人拿传感器去测墙；压力刚抬头就被查出来 | `detect rising stress early`（课文 476）|
| perceive | /pərˈsiːv/ | 卡 `m` 只给动词一档（v. 感知；察觉）| 察觉 | 感官先捕到 | 眼尖的人看到旁边那些小变化 | `perceive small changes`（课文 476）|

**差异维度表（diff）**

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 卡 ex / 课文原句）| 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 先记这条：这一档不用选 | detect | `I can detect a change in her mood.`（卡 ex）、`detect rising stress`（卡词伙，课文 476 即它）| 察觉某人情绪变了、某事在升温 | 两张卡的例句几乎同一句，宾语都是"某人情绪的变化"，这层意思**两者都对** |
| 先记这条：这一档不用选 | perceive | `I perceive a change in his mood.`（卡 ex）、`perceive small changes`（卡词伙，课文 476 即它）| 同上 | 卡 ex 一对比就看出来：detect 的 `a change in her mood` 换成 perceive 也成立 |
| 同义词栏把方向掰开 | detect | 卡 note 同义词：find, discover, notice（都是"找出、查见"那一族）| 东西藏在那儿，你去查、去测 | 课文里 detect 只有 476 一处；另一次主语是机器，在 sensor 卡 ex（不是课文）—— `The robot uses a sensor to detect walls.` |
| 同义词栏把方向掰开 | perceive | 卡 note 同义词：appreciate, ascertain, comprehend（都是"心里明白"那一族）| 感官与认知层面先捕到，不带仪器味 | perceive 全书只 476 那 1 次，主语是人（`Sharp eyes`），书里从没让机器 perceive 过 |
| 476 里两个宾语分工 | detect | `detect rising stress early`（课文 476）| 接"正在升、还没摆到明处"的那种量 | 它后面是**趋势**：stress 在 rising，还要 early 才显出本事 |
| 476 里两个宾语分工 | perceive | `perceive small changes`（课文 476）| 接"小到你不经意就漏掉"的差别 | 它后面是**细节**：changes 只 small，靠眼尖 |

**总结句（实测宽度 23.0 / ≤30）**
> detect 找出没摆到明处的，perceive 是感官先捕到

**出处清单**
- `detect rising stress`、`detect rising stress early`、`Sharp eyes could perceive small changes and detect rising stress early`、译文「眼尖的人能感觉到细小的变化，早早察觉正在升高的压力」 —— detect 卡 `note` 词伙段 + 课文第 476 句（段 [1,22] 第 4 句）及 `sentZh`。
- `perceive small changes` —— perceive 卡 `note` 词伙段（与课文 476 同一处）。
- `I can detect a change in her mood.` / 我能察觉到她情绪的变化。 —— detect 卡 `ex` / `exZh`（表中写作 `a change in her mood`，为其连续子串）。
- `I perceive a change in his mood.` / 我察觉到他的情绪有变化。 —— perceive 卡 `ex` / `exZh`。
- detect 卡 `note` 同义词段 `find, discover, notice`；perceive 卡 `note` 同义词段 `appreciate, ascertain, comprehend`。
- `The robot uses a sensor to detect walls.`、`to detect walls` —— sensor 卡 `ex`（课文里 sensor 侧另有一条实物：第 94 句 `mount a small detector on the boulder`「把小探测器安装在巨石上」，只作"测出"这一档的旁证，`detector` 在 `vocab.json` 无卡，不进表）。
- 义项「测出；察觉」「感知；察觉」 —— 两卡 `m`；「大胆假设细心观察保持清醒」—— 段 22 `paraZh`。

**证据强度与边界**
- perceive **全书只出现 1 次**（476）。detect 侧按口径 8 合并同源后是 **3 条独立文本**：课文 476（卡词伙 `detect rising stress` 就是这半句，同一处）+ detect 卡 ex 1 句 + sensor 卡 ex 1 句（前一版把卡词伙与课文分成两条记，已订正）。所以本组的差异全钉在"476 那两个宾语的分工 + 两张卡各自的同义词栏"上，都是可 grep 的东西。
- **不写的**：`detect a problem`、`perceive sth as`、detect/perceive 的"语域正式度"对比 —— 两份数据里都没有这些串或标注；同段 477 的 `discern tired smiles`（discern 有卡、义项「辨明」）是第三个"看出来"，切片没把它并进本组，不进表；475 的 `speculate` / `predict` 都有卡，但本批次 worklist 全 220 组里没有一组收它们（逐个反查过），同样不并进来。
- 两处 `a change in her mood` 与 `a change in his mood` 几乎同句 —— 这是本书给现成的"这一档可互换"证据，写在表第 1 行，免得他以为凡察觉必须二选一。

---

## 组 5 · postulate / presume —— 假定 ｜ `all_synonyms: false` ｜ 段 [1,22] ｜ 句 473 / 474

**共现位置**：1 段 2 句 —— 第 1 章段 22（全局 473–478），成员句 473（presume）与 474（postulate）**相邻两句**，同一主题（大胆假设 vs 细心观察）。卡挂段 22 末，**主锚点 = 章1 段22**（`paraZh`：大胆假设细心观察保持清醒。）。组 4（detect / perceive，476）同在本段。

**该段原文（章1 段22，成员两句 + 后一句）**

| 全局句 | 英文原句（成员词加粗）| 中文译句 |
|---|---|---|
| 473 | Teachers would never **presume** cheating nor idly suppose a fiasco without verification. | 老师们绝不会凭空调定有人作弊，也不会不作核实就猜要出大乱子。 |
| 474 | Lin put forward one brave new hypothesis and one quiet simple **postulate** for her friends to test. | 林提出了一个大胆的新假说，还有一个安安静静、简单清楚的公设，让朋友们去检验。 |
| 475 | Freshmen loved to speculate about scores but none could truly predict results. | 新生爱猜测分数，却无人能真正预测结果。 |
| 476 | Sharp eyes could perceive small changes and detect rising stress early. | 眼尖的人能感觉到细小的变化，早早察觉正在升高的压力。 |

**表头一句（title）**
> 紧挨的两句，一句在说老师心里不乱下结论，一句说林把自己摆出来的那条交给朋友去验——两个"假定"一个是私下的、一个是摊开来的：

**默认那一行（summary 的「简单记」｜实测宽度 39.5 / ≤40）**
```
presume = 先认了（never presume），postulate = 摊出来给人验（simple postulate）
```

**成员（items）**

| 词 | 音标 | 词性 | sense（卡上 `m`）| core（≤12 字）| scene | eg（含出处）|
|---|---|---|---|---|---|---|
| presume | /prɪˈzuːm/ | 卡 `m` 只给动词一档 | 假定（v. 假设；假定；设想）| 没核实就先当真 | 老师不凭空调定谁作弊；我当他明早到 | `would never presume cheating`（课文 473）|
| postulate | /ˈpɑːstʃəleɪt/ | 卡 `m` 两档：名词「假设」+ 动词「假定」 | 假定 | 摊开来待人验的那条 | 林提完假说，再摆一条简单的让朋友验 | `one quiet simple postulate for her friends to test`（课文 474）|

**差异维度表（diff）**

| 维度 | 词 | 常见搭配（全部来自卡上词伙 / 卡 ex / 课文原句）| 用在哪 | 一句话区别 |
|---|---|---|---|---|
| 念头放在哪儿 | presume | `never presume`（卡词伙，课文 473 就是 `would never presume cheating`）、`I presume he will arrive tomorrow.`（卡 ex）| 说话人自己心里先认下的那件事 | 它待在**心里**：473 同句还补了 `without verification`，正说明它不需要人来验 |
| 念头放在哪儿 | postulate | `quiet simple postulate`、`simple postulate`（卡词伙，与 474 同一处）、`put forward one brave new hypothesis and one quiet simple postulate`（课文 474）| 摆到桌面上、写在纸上的一条 | 它待在**桌面上**：474 句尾就是它的定义 —— `for her friends to test` |
| 句子里有没有别人 | presume | 卡 ex 只剩"我"和"他明天到"；课文 473 是老师们自己不去认定 | 单方面的判断 | 本书两处 presume 都没有第三个人接手 |
| 句子里有没有别人 | postulate | 卡 ex `He postulates that all humans are equal.`、课文 474 的 `her friends to test` | 提出来给同行验 | postulate 天生带一个"谁来验"的下家，这是它和 presume 最硬的一格 |
| 前面挂什么词 | presume | `never presume`（副词在前）、`presume cheating`（后面直接接事情）| 说"别乱假定"时 never 顶在前面；假定某事时宾语紧跟 | presume 卡 `m` 只有动词一档 |
| 前面挂什么词 | postulate | `one quiet simple postulate`（one + 两个形容词在前）、`He postulates that…`（动词档后接 that）| 说"一条公设"时它站名词位，前面全挤着形容词 | postulate 卡 `m` 有名词一档，本书 474 用的正是这一档；卡上词伙也都是形容词 + 名词的形状 |
| 中文落点 | 两个词 | 473 译「凭空调定」／474 译「公设」（`sentZh`）| 汉译英先判：是自己心里认了，还是要摆出来给人验 | 本书译文已经分家：presume → 「凭空……定」，postulate → 「公设」 |

**总结句（实测宽度 23.0 / ≤30）**
> presume 是心里先认了，postulate 是摆出来给人验

**出处清单**
- `never presume`、`would never presume cheating`、`presume cheating`、`without verification`、译文「绝不会凭空调定有人作弊」「不作核实」 —— presume 卡 `note` 词伙段 + 课文第 473 句（段 [1,22] 第 1 句）及 `sentZh`。
- `I presume he will arrive tomorrow.` / 我假设他明天会到。 —— presume 卡 `ex` / `exZh`。
- `quiet simple postulate`、`simple postulate` —— postulate 卡 `note` 词伙段（两条其实指同一处，即课文 474）。
- `one quiet simple postulate`、`put forward one brave new hypothesis and one quiet simple postulate`、`for her friends to test`、译文「安安静静、简单清楚的公设，让朋友们去检验」 —— 课文第 474 句（段 [1,22] 第 2 句）及 `sentZh`。
- `He postulates that all humans are equal.` / 他假定所有人都是平等的。 —— postulate 卡 `ex` / `exZh`。
- 义项「假设；假定；设想」「假设；假定」 —— 两卡 `m`；「大胆假设细心观察保持清醒」—— 段 22 `paraZh`。

**证据强度与边界**
- presume 全书 1 次（473）、postulate 全书 1 次（474），各配卡 ex 1 句。postulate 卡 `note` **没有同义词栏**（只有词伙 `quiet simple postulate` / `simple postulate`，两条都指课文 474 那一处），所以按口径 8 同源合并后：**postulate 侧 2 条独立文本** = 课文 474（＝两条卡词伙）+ 卡 ex 1 句（前一版记成「课文 1 句 + 卡词伙 1 处 + 卡 ex 1 句」= 3 条，已订正）；presume 侧同理 **2 条** = 课文 473（卡词伙 `never presume` 就是这半句）+ 卡 ex 1 句。
- 本组最硬的一格是"有没有下家"（474 句尾的 `for her friends to test` 对 473 的 `without verification`），两处都在同一句里，不靠语感。
- **不写的**：「postulate 是学术用语、presume 是日常用语」——两卡 `note` 无语域标注；`presume that…` 的从句模板除卡 ex 那一句外本书无第二处；同段的 `suppose`（473）、`hypothesis`（474）、`speculate` / `predict`（475）与 471 的 `assume` 都有卡，但归属要查清再写：`suppose` 与 `hypothesis` 确实被别的组收走了（组 195 = deem / reckon / suppose，组 217 = hypothesis / postulate / presume / suppose 四词一组）；而 `speculate`、`predict`、`assume` 在本批次 worklist 全 220 组里**一组都没被收**（逐个反查过，前一版写「另有组 195 / 217 收它们」是误报，已订正）。两种归属结论都一样：按硬约束 2 与 P10，成员只能是切片给的 2 个词，本表一律不并进来。
- 与组 4 的分工：本组和组 4 同段（473／474 与 476），落地时段 22 末要放两张卡，位置得排开。

---

## 附：切片 06 复核索引

| 组 | words | senses | all_synonyms | paras | sents | 判定 |
|---|---|---|---|---|---|---|
| 1 | cite / reference | 提及 | false | [1,21] | 468 | 出卡，但共享义项「提及」在 468 不成立（引用 vs 参考书），表头已改按词性讲 —— **待拍板** |
| 2 | cite / quote | 引用 | true | [1,21] | 468,469 | 出卡；同义只对到动词那一档，本书那处 quote 是名词，区别硬 |
| 3 | abstract / summary | 摘要 | false | [1,21] | 470,471 | 出卡；abstract 名词"摘要"侧只有课文 470 一条证据 —— **报备偏薄** |
| 4 | detect / perceive | 察觉 | false | [1,22] | 476 | 出卡；卡 ex 两句几乎同句（可互换那档已写进表第 1 行），差异落在同义词族 + 宾语分工 |
| 5 | postulate / presume | 假定 | false | [1,22] | 473,474 | 出卡，5 组里区别最干净（有没有下家接手）；postulate 卡无同义词栏 |

**挂段冲突（要你定口径）**：组 1／2／3 都挂章1 段 21 末，组 4／5 都挂章1 段 22 末。本文件已把 5 张卡的表头与默认行写成互不重复的切入角，但"同一段末排几张、按什么顺序排"是产品侧口径，切片包里没有依据，未擅自决定。

**门禁**：`python3 tools/check_compare_draft.py work/辨析草稿/2_06.md` → **PASSED（全部英文串可回溯，0 条查不到）**。

