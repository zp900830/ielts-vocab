# 3.0 M3（单词本页）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `/app/` 交出一屏「单词本」一级页（`#/words`）：全部目标词按文章关联列出 + 四档筛选（全部 / 待掌握 / 学习中 / 已掌握）+ 点行展开词详情（原文语境永远排第一）+ 词详情「▶ 播放这句」跳回该篇任务模式并定位播放；M2「待加强」的「去复习」真正落到「待掌握」筛选视图。

**Architecture:** 只动 3.0 外壳（`app/app.js` + `app/index.html`）。数据全部从既有 `SECTIONS` / `VOCAB` / `ROOT2.state` 派生（§9.4），**不新增存储字段**；「词 → 文章 / 出现次数 / 句子」索引由句子里的 `[[词:形式]]` 标记现建一次、缓存（§9.5 的 `wordArticle` 同源）。`#/words` 路由 M1 起是占位屏，M3 把 `route()` 的 words 分支接到 `APP3.renderWords`，并支持 `#/words/<filter>` 深链。列表默认只渲 60 行、点「加载更多」增量追加（**不一次塞 3245 个 DOM 节点**）。`/`、`/shadow/`、`shadow/js/plan-engine.js` 一行不动；`data/vocab.json` 只读。

**Tech Stack:** 零依赖原生 HTML/CSS/JS（无框架）+ Playwright E2E。

**Spec:** `docs/superpowers/specs/2026-09-24-3.0-article-first-PRD.md`（§6 / §2.4 / §9.4 / §9.5 / §10.1–10.4 / §13.1 / §14.2）

## Global Constraints

- **允许改**：`app/**`、`playwright-tests/**`、`docs/**`。**不许进 diff**：`index.html`、`shadow/**`、`shadow/js/plan-engine.js`、`config.js`、`data/vocab.json`（只读它，不许改）。
- **不新增存储字段**：单词本所有数字从 `TASK.state()` / `TASK.sentWordsOf()` / `VOCAB` / `SECTIONS` 现算（§9.4）。不写 `localStorage`。
- **词表宇宙**：文章里出现过（`[[词:形式]]` 标记）的**全部目标词**（真实数据 3245 条）；未接触过的词 stage 视作 `fresh`，显示「未见面」，只在「全部」档出现（§6.3 四档不含「未见」）。
- **性能**：默认筛「全部」时空表可能 3245 行 → 首屏只渲 60 行，`加载更多` 每次追加 60 行（增量渲染，理由见 File Structure）。**禁止一次 `innerHTML` 塞全部行**。
- **不新增浮层/页面**（§2.4 护栏）：入口只有侧栏「单词本」一项；词详情是**行内展开**（点行展开），不是新浮层、不是新页面；「▶ 播放这句」只是复用既有的任务模式。
- 视觉**继承**既有令牌（`--card` / `--border` / `--accent-soft` / `--accent-text` / `--grad` / `--note` / `--hl`…），**不新造色**；一级页面用主站薄荷配色 + 白底卡片；**深色全覆盖**（`body.dark`）。
- 无障碍（§10.4）：语义化 `h1`、筛选是真正的 `button`（`aria-pressed`）、词行是 `button`（`aria-expanded`）、可聚焦可键盘（Enter/Space 原生）、触摸目标 ≥44px。
- **`plan-engine.js` 只读**：只用 `ShadowPlan.articleScope` / `newWord`；不改引擎。`data/vocab.json` 只读。
- 分支纪律：已在 `agent/opencode-3.0-m3-0924`，**每个任务一个 commit、不 push、不改历史**。
- 每个任务收尾：`cd playwright-tests && npx playwright test journeys/app3/words.spec.ts` 绿。收口任务再跑全量（基线 **238**）、`python3 scripts/validate_data.py`、`node scripts/build_site.mjs`。

---

## File Structure

| 文件 | 职责 | 新建/改动 |
|------|------|----------|
| `app/app.js` | 单词本页的派生与渲染（`renderWords` / `wordIndex` / `filterWords` / `rowHtml` / `wordDetailHtml` / `toggleWordRow` / `playSentence`）+ `route()` 加 words 分支与筛选段 + 事件代理（筛选 / 加载更多 / 展开 / 播放 / 重学）+ M2「去复习」落点 | 改 |
| `app/index.html` | 单词本 CSS（列表 / 筛选 / 详情 / 深色 / 响应式）+ TASK 新增 `playSentence` 导出（`openArticle` 支持 `playGlobal`） | 改 |
| `playwright-tests/journeys/app3/words.spec.ts` | M3 的 E2E 锁 | 新建 |
| `docs/superpowers/specs/2026-09-24-3.0-article-first-PRD.md` | §2.4 补 M3 入口说明 + §14.2 进度标注 | 改 |
| `scripts/build_site.mjs` | **不动**（M3 只改已有的 `app/index.html` + `app/app.js`，都已在白名单；不新增运行时文件） | 不动 |

**为什么全部塞进 `app/app.js` 而不新建文件**：单词本是纯派生 + 渲染，没有独立子系统；`app/app.js` 现 609 行，M3 追加约 220 行仍在可读范围。新建文件会碰构建白名单，收益为零（沿用 M2 的判断）。

**为什么「增量渲染（加载更多）」而不是真虚拟列表**：真虚拟列表要接管滚动、回收 DOM，行高不定（详情展开会变高）时容易滚动跳动，且对屏幕阅读器不友好。本页最多 3245 行、行内容轻，分页增量（每批 60）实现简单、可测（DOM 行数 < 总数）、无障碍（已渲的行始终在 DOM 里，`Ctrl+F` 也能找），对 3245 条足够。理论最坏情况：用户真的一路点到 3245 行，DOM 会到 3245 —— 但那已经是「用户主动把整本翻完」，与首屏卡顿无关；真要极致再上虚拟化，属 M6 收口范围。

---

## Task 1: `#/words` 路由 + 词索引 + 列表行 + 增量渲染（性能）

**Files:**
- Modify: `app/app.js`（`route()` words 分支；新增 `renderWords` / `wordIndex` / `wordIndexOrder` / `rowHtml` / `filterWords` / `countBucket` / `inFilter` / `sentPos` / `volNo`）
- Modify: `app/index.html`（单词本 CSS：`.words-page` / `.wb-filters` / `.wb-list` / `.wb-row` / `.wb-more`）
- Test: `playwright-tests/journeys/app3/words.spec.ts`（新建，含夹具）

**Interfaces:**
- Consumes: `SECTIONS`、`VOCAB`、`TASK.state()`、`TASK.sentWordsOf(i)`、`ShadowPlan.articleScope`、`esc`、`rel`
- Produces: `window.APP3.renderWords(view, filter)`

- [ ] **Step 1: 写失败测试**（新建 `words.spec.ts`）

```ts
// playwright-tests/journeys/app3/words.spec.ts
// 3.0 M3：单词本页（PRD §6）。服务器归 global-setup.ts 起停（仓库根 8932）；
// /app/ 在仓库根，所以和 shell/home/stats 一样用 E2E_ROOT_URL，不走 baseURL。
import { test, expect } from '../../fixtures';
import { waitShadowReady } from '../../utils/app-ready';

declare const TASK: {
  hasPlan: boolean;
  resetV2(): void;
  initPlan(minutes: number): void;
  readDone(i: number): void;
  relearn(w: string): void;
  state(): {
    daily: Record<string, unknown>;
    words: Record<string, { stage: string; reps: number; ok3: number; err: number; leech: boolean; lastContactAt?: number }>;
    sents: Record<number, { lastReadAt: number }>;
  };
  article: number | null;
};
declare const ShadowPlan: {
  articleScope(sections: unknown, a: number): Set<number>;
  newWord(): Record<string, unknown>;
};
declare const SECTIONS: Array<{ title: string }>;
declare const VOCAB: Record<string, { p?: string; m?: string; uk?: string; us?: string; ex?: string; exZh?: string }>;
declare const APP3: {
  route(): void;
  current(): string;
  playSentence(a: number, gi: number): void;
};
declare const dataReady: boolean;

const rootUrl = process.env.E2E_ROOT_URL || '';

/* 两篇小课文 + 3 个已知目标词，够断言「文章关联 / 卷 / 出现次数 / 原文语境 / 联动」。
   句号：篇 0 = 0,1,2；篇 1 = 3,4。atmosphere 在篇 0 出现 2 次（第 0、2 句）。 */
const WORDS = (() => {
  const mk = (title: string, sub: string, lines: string[]) => ({
    title, zh: title, subheads: [sub],
    paragraphs: [lines],
    sentZh: [lines.map((_, i) => `第 ${i + 1} 句译文。`)],
    paraZh: [''],
  });
  const sections = [
    mk('地球与生命', '第一卷·远行', [
      'The [[atmosphere:atmosphere]] protects life.',
      'We need [[oxygen:oxygen]] to live.',
      'The [[atmosphere:atmosphere]] also keeps us warm.',
    ]),
    mk('校园与文化', '上篇·新生学期', [
      'Students [[oxygen:oxygen]] study in the [[library:library]].',
      'The [[library:library]] is quiet.',
    ]),
  ];
  const vocab = {
    atmosphere: { p: 'ˈætməsfɪr', m: 'n. 大气；气氛；氛围', uk: 'ˈætməsfɪə', us: 'ˈætməsfɪr',
      ex: 'The meeting was held in a relaxed atmosphere.', exZh: '会议在轻松的气氛中举行。', note: '词伙：lively atmosphere' },
    oxygen: { p: 'ˈɒksɪdʒən', m: 'n. 氧气', ex: 'Oxygen is essential for life.', exZh: '氧气是生命所必需的。' },
    library: { p: 'ˈlaɪbrəri', m: 'n. 图书馆', ex: 'She borrowed a book from the library.', exZh: '她从图书馆借了一本书。' },
  };
  return {
    'sections.json': JSON.stringify(sections),
    'vocab.json': JSON.stringify(vocab),
    'chapters.json': '[]',
  };
})();

/* 大词表：一篇 120 句、每句 1 个独有目标词 —— 专门锁「不能一次渲染全部」。 */
const BIG = (() => {
  const lines = Array.from({ length: 120 }, (_, i) => `Sentence ${i} about [[w${i}:w${i}]].`);
  const sections = [{ title: '大地', zh: '大地', subheads: ['第一卷'], paragraphs: [lines],
    sentZh: [lines.map((_, i) => `第 ${i + 1} 句。`)], paraZh: [''] }];
  const vocab: Record<string, { m: string }> = {};
  for (let i = 0; i < 120; i++) vocab['w' + i] = { m: 'n. 词' + i };
  return { 'sections.json': JSON.stringify(sections), 'vocab.json': JSON.stringify(vocab), 'chapters.json': '[]' };
})();

async function stubData(page: import('@playwright/test').Page, payloads: Record<string, string>) {
  for (const [name, body] of Object.entries(payloads)) {
    await page.route(`**/shadow/data/${name}*`, (r) =>
      r.fulfill({ status: 200, contentType: 'application/json', body }));
  }
}

test.describe('3.0 单词本页（M3，PRD §6）', () => {
  test('列出全部目标词，行含状态/文章/卷/出现次数；未见面也在「全部」里', async ({ page }) => {
    await stubData(page, WORDS);
    await page.goto(`${rootUrl}/app/index.html#/words`);
    await waitShadowReady(page);

    await expect(page.locator('.words-page > h1')).toHaveText('单词本');
    const words = (await page.locator('.wb-row .wr-word').allInnerTexts()).sort();
    expect(words).toEqual(['atmosphere', 'library', 'oxygen']);
    // 未接触过 → 未见面胶囊
    await expect(page.locator('.wb-row[data-w="atmosphere"] .wr-pill')).toHaveText('未见面');
    // 文章关联：atmosphere 在《地球与生命》第 1 卷出现 2 次
    const at = page.locator('.wb-row[data-w="atmosphere"] .wr-meta');
    await expect(at).toContainText('地球与生命');
    await expect(at).toContainText('第 1 卷');
    await expect(at).toContainText('出现 2 次');
    // oxygen 横跨两篇：主篇仍是第一篇，出现次数是全局合计（篇 0 一次 + 篇 1 一次 = 2）
    await expect(page.locator('.wb-row[data-w="oxygen"] .wr-meta')).toContainText('出现 2 次');
  });

  test('长列表增量渲染：初始只渲一批，点「加载更多」才追加（不能一次渲全部）', async ({ page }) => {
    await stubData(page, BIG);
    await page.goto(`${rootUrl}/app/index.html#/words`);
    await waitShadowReady(page);

    const total = await page.evaluate(() => Object.keys(VOCAB).length);
    expect(total).toBe(120);
    const first = await page.locator('.wb-row').count();
    expect(first, '初始要有一批').toBeGreaterThan(0);
    expect(first, '不能一次渲染全部 120 行').toBeLessThan(total);
    // 第一行真的是 w0，且不是全部
    await expect(page.locator('.wb-row .wr-word').first()).toHaveText('w0');

    await page.locator('.wb-more').click();
    await expect.poll(() => page.locator('.wb-row').count(), '加载更多要真的追加').toBeGreaterThan(first);
  });
});
```

- [ ] **Step 2: 跑测试确认红**

Run: `cd playwright-tests && npx playwright test journeys/app3/words.spec.ts`
Expected: FAIL —— `#/words` 当前是 `.app-todo` 占位屏，`.words-page` / `.wb-row` 都不存在。

- [ ] **Step 3: 实现**

`app/app.js` 顶部（`let _hlArticle = null;` 之后）加单词本常量与缓存：

```js
  /* ---- 3.0 单词本（M3，PRD §6）---- */
  const WB_FILTERS = ['all', 'todo', 'learning', 'mastered'];
  const WB_FILTER_LABEL = { all: '全部', todo: '待掌握', learning: '学习中', mastered: '已掌握' };
  const WORD_STAGE_LABEL = { fresh: '未见面', seen: '已见面', recognized: '文中可辨',
                             owned: '义项直连', graduated: '已毕业' };
  const W_BATCH = 60;              // 增量渲染每批行数
  let _wordIndex = null, _wordIndexOrder = null, _wordIndexFor = null;
  let _wordsShown = W_BATCH, _wordsFilter = null;
```

`route()` 里，把「取 hash 首段」改成同时取筛选段，并在 stats 分支后插入 words 分支：

```js
  function route() {
    const raw = (location.hash || '#/home').replace(/^#\//, '').split('/');
    const h = raw[0];
    cur = ROUTES.includes(h) ? h : 'home';
    if (cur !== 'home') _hlArticle = null;
    document.querySelectorAll('.sidenav .nav-item').forEach(b =>
      b.classList.toggle('on', b.dataset.route === cur));
    const view = document.getElementById('appView');
    if (cur === 'home' && window.APP3 && window.APP3.renderHome) { updateMeCard(); return window.APP3.renderHome(view); }
    if (cur === 'home') { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }
    if (cur === 'stats' && window.APP3 && window.APP3.renderStats) return window.APP3.renderStats(view);
    if (cur === 'stats') { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }
    if (cur === 'words' && window.APP3 && window.APP3.renderWords) return window.APP3.renderWords(view, raw[1] || 'all');
    if (cur === 'words') { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }
    const meta = TODO_META[cur];
    view.innerHTML = `<div class="app-todo"><div class="at-ico" aria-hidden="true"><i class="${meta[1]}"></i></div>` +
      `<h3>${meta[0]}</h3><p>这一屏在 3.0 的后续里程碑里交付 —— 先挑一篇文章，从头学到收尾。</p></div>`;
  }
```

在单词本实现区（`openHomeHighlight` 之后、横幅之前）加：

```js
  /* ---- 3.0 单词本（M3，PRD §6）----
     宇宙 = 文章标记里出现过的全部目标词（真实数据 3245）。索引一次建好、按 SECTIONS 缓存。 */
  function wordIndex() {
    if (_wordIndex && _wordIndexFor === SECTIONS) return _wordIndex;
    const idx = {}, order = [];
    for (let a = 0; a < SECTIONS.length; a++) {
      const scope = window.ShadowPlan.articleScope(SECTIONS, a);
      scope.forEach((gi) => {
        (TASK.sentWordsOf(gi) || []).forEach((w) => {
          let e = idx[w];
          if (!e) { e = idx[w] = { arts: [a], count: 0, sents: [] }; order.push(w); }
          else if (e.arts[e.arts.length - 1] !== a) e.arts.push(a);
          e.count++; e.sents.push(gi);
        });
      });
    }
    _wordIndex = idx; _wordIndexOrder = order; _wordIndexFor = SECTIONS;
    return idx;
  }
  function wordIndexOrder() { wordIndex(); return _wordIndexOrder; }
  // 全局句号 → {a, pi, ti}
  function sentPos(gi) {
    let n = 0;
    for (let a = 0; a < SECTIONS.length; a++) {
      const paras = SECTIONS[a].paragraphs;
      for (let p = 0; p < paras.length; p++) {
        if (gi < n + paras[p].length) return { a: a, pi: p, ti: gi - n };
        n += paras[p].length;
      }
    }
    return { a: 0, pi: 0, ti: 0 };
  }
  // 段所属「卷」号（subheads 非空处 = 每卷第一段；§6.2 的「第 N 卷」）
  function volNo(a, pi) {
    const sh = (SECTIONS[a] && SECTIONS[a].subheads) || [];
    let v = 0;
    for (let k = 0; k <= pi && k < sh.length; k++) if (sh[k]) v++;
    return Math.max(1, v);
  }
  function volStartPara(a, pi) {
    const sh = (SECTIONS[a] && SECTIONS[a].subheads) || [];
    for (let k = Math.min(pi, sh.length - 1); k >= 0; k--) if (sh[k]) return k;
    return 0;
  }
  // 句在「本卷内」的序号（§6.4 的「第 K 句」）
  function sentNoInVol(a, pi, ti) {
    const start = volStartPara(a, pi);
    let n = 0;
    for (let k = start; k < pi; k++) n += SECTIONS[a].paragraphs[k].length;
    return n + ti + 1;
  }
  function wbStatus(w, st) {
    const s = st && st.words ? st.words[w] : null;
    if (!s) return { stage: 'fresh', leech: false, s: null };
    return { stage: s.stage || 'fresh', leech: !!s.leech, s: s };
  }
  // 四档判据（§6.3，唯一出口：筛选、计数、测试断言同源）
  function inFilter(w, st, filter) {
    if (filter === 'all') return true;
    const x = wbStatus(w, st);
    if (filter === 'todo') return x.stage === 'seen' || x.stage === 'recognized' || x.leech;
    if (filter === 'learning') return x.stage === 'owned';
    if (filter === 'mastered') return x.stage === 'graduated';
    return true;
  }
  function filterWords(filter, st) {
    return wordIndexOrder().filter((w) => inFilter(w, st, filter));
  }
  function countBucket(filter, st) {
    let n = 0;
    const order = wordIndexOrder();
    for (let i = 0; i < order.length; i++) if (inFilter(order[i], st, filter)) n++;
    return n;
  }
  function rowHtml(w, st) {
    const e = wordIndex()[w];
    const pos = sentPos(e.sents[0]);
    const a = pos.a;
    const meta = `${esc(SECTIONS[a].title)} · 第 ${volNo(a, pos.pi)} 卷 · 出现 ${e.count} 次`;
    const x = wbStatus(w, st);
    const pill = x.leech ? '重点词' : (WORD_STAGE_LABEL[x.stage] || '未见面');
    const pillCls = x.leech ? ' leech' : (' s-' + x.stage);
    const relt = (x.s && x.s.lastContactAt) ? rel(x.s.lastContactAt) : '还没学过';
    return `<li class="wb-item">
      <button class="wb-row" type="button" data-w="${esc(w)}" aria-expanded="false">
        <span class="wr-head"><span class="wr-word">${esc(w)}</span><span class="wr-pill${pillCls}">${pill}</span></span>
        <span class="wr-meta">${meta}</span>
        <span class="wr-meta2">${relt}</span>
      </button>
    </li>`;
  }
  function renderWords(view, filter) {
    if (typeof dataReady === 'undefined' || !dataReady) { view.innerHTML = '<p class="sm">正在载入…</p>'; return; }
    if (WB_FILTERS.indexOf(filter) < 0) filter = 'all';
    if (_wordsFilter !== filter) { _wordsFilter = filter; _wordsShown = W_BATCH; }
    const st = (typeof TASK !== 'undefined' && TASK.state) ? TASK.state() : null;
    const list = filterWords(filter, st);
    const shown = Math.min(_wordsShown, list.length);
    const filters = WB_FILTERS.map((f) =>
      `<button class="wb-filter" type="button" data-f="${f}" aria-pressed="${f === filter ? 'true' : 'false'}">`
      + `${WB_FILTER_LABEL[f]} <span class="wf-n">${countBucket(f, st)}</span></button>`).join('');
    const rows = list.slice(0, shown).map((w) => rowHtml(w, st)).join('');
    const more = shown < list.length
      ? `<button class="wb-more" type="button">加载更多（还剩 ${list.length - shown} 个）</button>` : '';
    view.innerHTML = `<div class="words-page">
      <h1>单词本</h1>
      <div class="wb-filters" role="group" aria-label="按掌握状态筛选">${filters}</div>
      <p class="wb-hint">共 ${list.length} 个词${filter === 'all' ? '' : '（当前筛选）'} · 点词行看原文语境</p>
      <ul class="wb-list" aria-label="单词列表">${rows}</ul>
      ${more}
    </div>`;
  }
  window.APP3 = Object.assign(window.APP3, { renderWords });
```

在 `app/app.js` 底部那条主点击监听里（`const b = e.target.closest('.nav-item');` **之前**）加单词本的点击代理（此刻只接「加载更多」；筛选 / 展开 / 播放在 Task 2–4 追加）：

```js
    const wm = e.target.closest('.wb-more');
    if (wm) { _wordsShown += W_BATCH; window.APP3.route(); return; }
```

`app/index.html` 的 `<style>` 里（`.st-empty-start` 那块之后、`body.dark .st-art .sa-bar` 之前）加 CSS：

```css
  /* ============ 3.0 单词本（M3，PRD §6） ============ */
  .words-page { max-width: 880px; }
  .words-page > h1 { font-size: 22px; font-weight: 800; color: var(--text); margin: 2px 0 14px; }
  .wb-filters { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 10px; }
  .wb-filter { min-height: 44px; padding: 0 16px; border: 1px solid var(--border); border-radius: 999px;
    background: var(--card); color: var(--text); font: inherit; font-size: 13px; font-weight: 600;
    cursor: pointer; box-shadow: var(--shadow); }
  .wb-filter .wf-n { color: var(--muted); font-weight: 700; font-variant-numeric: tabular-nums; }
  .wb-filter[aria-pressed="true"] { background: var(--accent-soft); border-color: var(--accent-line); color: var(--accent-text); }
  .wb-filter[aria-pressed="true"] .wf-n { color: var(--accent-text); }
  .wb-hint { font-size: 11.5px; color: var(--muted); margin: 0 0 10px; }
  .wb-list { list-style: none; display: flex; flex-direction: column; gap: 8px; margin: 0; padding: 0; }
  .wb-row { display: block; width: 100%; min-height: 44px; text-align: left; padding: 10px 14px;
    border: 1px solid var(--border); border-radius: 14px; background: var(--card); color: inherit;
    font: inherit; cursor: pointer; box-shadow: var(--shadow); }
  .wb-row:hover { border-color: var(--accent-line); }
  .wb-item.open .wb-row { border-bottom-left-radius: 0; border-bottom-right-radius: 0; }
  .wr-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; }
  .wr-word { font-size: 15px; font-weight: 700; }
  .wr-pill { flex: none; font-size: 10.5px; font-weight: 700; line-height: 1.8; padding: 0 9px;
    border-radius: 999px; background: rgba(0,0,0,.05); color: var(--muted); }
  .wr-pill.s-seen, .wr-pill.s-recognized { background: var(--accent-soft); color: var(--accent-text); }
  .wr-pill.s-owned { background: var(--note-soft); color: var(--note); }
  .wr-pill.s-graduated { background: var(--accent); color: var(--cta-ink); }
  .wr-pill.leech { background: var(--note-soft); color: var(--note); }
  .wr-meta, .wr-meta2 { display: block; margin-top: 3px; font-size: 11px; color: var(--muted); }
  .wb-more { display: block; width: 100%; min-height: 44px; margin-top: 12px; border: 1px dashed var(--border);
    border-radius: 14px; background: var(--card); color: var(--accent-text); font: inherit; font-weight: 700;
    cursor: pointer; }
  body.dark .wb-row, body.dark .wb-filter, body.dark .wb-more { background: var(--card); border-color: var(--border); }
  body.dark .wr-pill { background: rgba(255,255,255,.06); color: var(--muted); }
  body.dark .wr-pill.s-seen, body.dark .wr-pill.s-recognized { background: var(--accent-soft); color: var(--accent-text); }
  body.dark .wr-pill.s-owned, body.dark .wr-pill.leech { background: var(--note-soft); color: var(--note); }
  body.dark .wr-pill.s-graduated { background: var(--accent); color: var(--cta-ink-dark); }
  @media (max-width: 700px) { .wb-filter { flex: 1 1 calc(50% - 8px); } }
```

- [ ] **Step 4: 跑测试确认绿**

Run: `cd playwright-tests && npx playwright test journeys/app3/words.spec.ts` → PASS

- [ ] **Step 5: 提交**

```bash
git add app/app.js app/index.html playwright-tests/journeys/app3/words.spec.ts
git commit -m "feat(app3): 单词本路由 + 词索引 + 列表 + 增量渲染（M3）"
```

---

## Task 2: 四档筛选 + 计数 + `#/words/<filter>` 深链 + M2「去复习」落到待掌握（§6.3）

**Files:**
- Modify: `app/app.js`（点击代理接 `.wb-filter`；M2 `.tip-go` 的 `leech` 改落 `#/words/todo`）
- Test: `words.spec.ts`（追加）

**Interfaces:**
- Consumes: `APP3.renderWords`（T1）
- Produces: 无新出口（筛选状态存 URL）

- [ ] **Step 1: 写失败测试**

```ts
  test('四档筛选各自真的改变结果集，且计数与 state 同源', async ({ page }) => {
    await stubData(page, WORDS);
    await page.goto(`${rootUrl}/app/index.html#/words`);
    await waitShadowReady(page);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const st = TASK.state();
      const mk = (o: Record<string, unknown>) => Object.assign(ShadowPlan.newWord(), o);
      st.words['atmosphere'] = mk({ stage: 'graduated', reps: 12, ok3: 1, leech: false });
      st.words['oxygen'] = mk({ stage: 'seen', reps: 1, ok3: 0, leech: false });
      st.words['library'] = mk({ stage: 'owned', reps: 5, ok3: 1, leech: false });
      APP3.route();
    });

    // 期望集从 state 现算（真断言，不写死字面量）
    const exp = await page.evaluate(() => {
      const st = TASK.state();
      const all = Object.keys(VOCAB);
      const bucket = (w: string) => { const s = st.words[w]; if (!s) return 'fresh'; if (s.leech) return 'leech'; return s.stage; };
      return {
        all: all.length,
        todo: all.filter((w) => { const b = bucket(w); return b === 'seen' || b === 'recognized' || b === 'leech'; }),
        learning: all.filter((w) => bucket(w) === 'owned'),
        mastered: all.filter((w) => bucket(w) === 'graduated'),
        universe: all.slice().sort(),
      };
    });
    await expect(page.locator('.wb-filter[data-f="all"] .wf-n')).toHaveText(String(exp.all));
    await expect(page.locator('.wb-filter[data-f="todo"] .wf-n')).toHaveText(String(exp.todo.length));
    await expect(page.locator('.wb-filter[data-f="learning"] .wf-n')).toHaveText(String(exp.learning.length));
    await expect(page.locator('.wb-filter[data-f="mastered"] .wf-n')).toHaveText(String(exp.mastered.length));

    const shownWords = async () => (await page.locator('.wb-row .wr-word').allInnerTexts()).sort();
    const want: Record<string, string[]> = { all: exp.universe, todo: exp.todo, learning: exp.learning, mastered: exp.mastered };
    for (const f of ['all', 'todo', 'learning', 'mastered']) {
      await page.locator(`.wb-filter[data-f="${f}"]`).click();
      await expect(page).toHaveURL(new RegExp('#/words/' + f));
      await expect(page.locator(`.wb-filter[data-f="${f}"]`)).toHaveAttribute('aria-pressed', 'true');
      expect(await shownWords(), `筛选 ${f} 的结果集`).toEqual([...want[f]].sort());
    }
    // 三档之和 = 有状态的词且不重复：todo 1 + learning 1 + mastered 1 = 3
    expect(exp.todo.length + exp.learning.length + exp.mastered.length, '夹具要真造出三档').toBe(3);
  });

  test('M2 待加强的「去复习」落到单词本「待掌握」筛选视图', async ({ page }) => {
    await stubData(page, WORDS);
    await page.goto(`${rootUrl}/app/index.html#/stats`);
    await waitShadowReady(page);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const st = TASK.state();
      st.words['oxygen'] = Object.assign(ShadowPlan.newWord(), { stage: 'seen', reps: 1, leech: true, due: Date.now() - 1000 });
      APP3.route();
    });
    await expect(page.locator('.st-tip[data-tip="leech"]')).toBeVisible();
    await page.locator('.st-tip[data-tip="leech"] .tip-go').click();
    await expect(page).toHaveURL(/#\/words\/todo/);
    await expect(page.locator('.wb-filter[data-f="todo"]')).toHaveAttribute('aria-pressed', 'true');
    expect((await page.locator('.wb-row .wr-word').allInnerTexts()).sort()).toEqual(['oxygen']);
  });
```

- [ ] **Step 2: 跑测试确认红** → 点 `.wb-filter` 不改 hash（`.wb-row` 结果不变 / URL 不是 `#/words/<f>`）

- [ ] **Step 3: 实现**

`app/app.js` 点击代理里，`const wm = e.target.closest('.wb-more');` 那块**之前**加筛选：

```js
    const wf = e.target.closest('.wb-filter');
    if (wf) { location.hash = '#/words/' + wf.dataset.f; return; }
```

M2 的「去复习」落点（同文件数据页点击代理里）：

```js
      if (kind === 'leech') { location.hash = '#/words/todo'; return; }
      if (kind === 'words') { location.hash = '#/words'; return; }
```

（原先是 `if (kind === 'leech' || kind === 'words') { location.hash = '#/words'; return; }` 一条。）

> 注意：M2 的 `stats.spec.ts` 断言 leech → `/#\/words/`，`#/words/todo` 仍匹配，不会弄红旧锁。

- [ ] **Step 4: 跑测试确认绿**

Run: `cd playwright-tests && npx playwright test journeys/app3/words.spec.ts` → PASS
Run: `cd playwright-tests && npx playwright test journeys/app3/stats.spec.ts` → 全绿（旧 leech 锁仍匹配）

- [ ] **Step 5: 提交** `feat(app3): 单词本四档筛选 + 计数 + M2「去复习」落到待掌握`

---

## Task 3: 词详情（点行展开，§6.4）

**Files:**
- Modify: `app/app.js`（`wordDetailHtml` / `sentenceHtml` / `toggleWordRow` + 点击代理接 `.wb-row` / `.wd-relearn`）
- Modify: `app/index.html`（`.wb-detail` / `.wd-*` CSS）
- Test: `words.spec.ts`（追加）

**Interfaces:**
- Consumes: `wordIndex` / `sentPos` / `volNo` / `sentNoInVol` / `wbStatus`（T1）、`VOCAB`、`TASK.relearn`
- Produces: `window.APP3` 无新出口（详情是行内 DOM）

- [ ] **Step 1: 写失败测试**

```ts
  test('点词行展开详情：原文语境在前、例句其次，含状态路径与接触/答对', async ({ page }) => {
    await stubData(page, WORDS);
    await page.goto(`${rootUrl}/app/index.html#/words`);
    await waitShadowReady(page);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const st = TASK.state();
      st.words['atmosphere'] = Object.assign(ShadowPlan.newWord(),
        { stage: 'graduated', reps: 12, ok3: 3, err: 0, leech: false, lastContactAt: Date.now() });
      APP3.route();
    });

    const row = page.locator('.wb-row[data-w="atmosphere"]');
    await expect(row).toHaveAttribute('aria-expanded', 'false');
    await row.click();
    await expect(row).toHaveAttribute('aria-expanded', 'true');
    const d = page.locator('.wb-detail');
    await expect(d).toBeVisible();
    // 音标 + 释义
    await expect(d.locator('.wd-phon')).toHaveText('/ˈætməsfɪr/');
    await expect(d.locator('.wd-mean')).toContainText('大气');
    // 原文语境：句子（目标词高亮）+ 《文章》卷/句号 + 播放
    await expect(d.locator('.wd-ctx')).toContainText('protects life');
    await expect(d.locator('.wd-ctx .wd-hl')).toHaveText('atmosphere');
    await expect(d.locator('.wd-src')).toContainText('《地球与生命》');
    await expect(d.locator('.wd-src')).toContainText('第 1 卷');
    await expect(d.locator('.wd-src')).toContainText('第 1 句');
    await expect(d.locator('.wd-play')).toBeVisible();
    // §6.4：原文语境永远排第一，例句其次
    const heads = await d.locator('.wd-block h4').allInnerTexts();
    expect(heads[0]).toContain('原文语境');
    expect(heads[1]).toContain('例句');
    await expect(d.locator('.wd-ex')).toContainText('relaxed atmosphere');
    // 学习状态：路径 + 接触/答对
    await expect(d.locator('.wd-path .wd-step.on')).toHaveText('已毕业');
    await expect(d.locator('.wd-stat')).toContainText('接触 12 次');
    await expect(d.locator('.wd-stat')).toContainText('答对 3 次');

    // 再点一次收起
    await row.click();
    await expect(row).toHaveAttribute('aria-expanded', 'false');
    await expect(page.locator('.wb-detail')).toHaveCount(0);
  });
```

- [ ] **Step 2: 跑测试确认红** → `.wb-detail` 不存在

- [ ] **Step 3: 实现**

`app/app.js`（放在 `renderWords` 附近）：

```js
  // 原文句里把目标词高亮（其它 [[词:形式]] 只留形式），其余文本转义
  function sentenceHtml(raw, word) {
    const re = /\[\[([^\]:]+):([^\]]+)\]\]/g;
    let out = '', last = 0, m;
    while ((m = re.exec(raw))) {
      out += esc(raw.slice(last, m.index));
      out += (String(m[1]).toLowerCase() === String(word).toLowerCase())
        ? '<b class="wd-hl">' + esc(m[2]) + '</b>' : esc(m[2]);
      last = re.lastIndex;
    }
    return out + esc(raw.slice(last));
  }
  function wordDetailHtml(w) {
    const e = wordIndex()[w];
    const st = (typeof TASK !== 'undefined' && TASK.state) ? TASK.state() : null;
    const x = wbStatus(w, st);
    const v = (typeof VOCAB !== 'undefined' && VOCAB[w]) || {};
    const p = String(v.p || v.us || v.uk || '').replace(/^\//, '').replace(/\/$/, '');
    const gi0 = e ? e.sents[0] : 0;
    const pos0 = sentPos(gi0);
    const a0 = pos0.a;
    const raw0 = (SECTIONS[a0] && SECTIONS[a0].paragraphs[pos0.pi] && SECTIONS[a0].paragraphs[pos0.pi][pos0.ti]) || '';
    const steps = ['seen', 'recognized', 'owned', 'graduated'].map((k) =>
      `<span class="wd-step${x.stage === k ? ' on' : ''}">${WORD_STAGE_LABEL[k]}</span>`)
      .join('<i class="wd-arrow" aria-hidden="true">→</i>');
    const ex = v.ex ? `<div class="wd-block"><h4>📝 例句 / 其他语境</h4>
        <p class="wd-ex">${esc(v.ex)}</p>${v.exZh ? `<p class="wd-exzh">${esc(v.exZh)}</p>` : ''}</div>` : '';
    return `<div class="wb-detail" role="region" aria-label="${esc(w)} 详情">
      <div class="wd-top"><span class="wd-word">${esc(w)}</span>
        <span class="wd-stage${x.leech ? ' leech' : ''}">${x.leech ? '重点词' : (WORD_STAGE_LABEL[x.stage] || '未见面')}</span>
        <button class="wd-relearn" type="button" data-relearn="${esc(w)}">重学</button></div>
      ${p ? `<div class="wd-phon">/${esc(p)}/</div>` : ''}
      <div class="wd-mean">${esc(v.m || '（词库中无此词条）')}</div>
      <div class="wd-block"><h4>📖 原文语境</h4>
        <p class="wd-ctx">${sentenceHtml(raw0, w)}</p>
        <p class="wd-src">—— 《${esc(SECTIONS[a0].title)}》第 ${volNo(a0, pos0.pi)} 卷 · 第 ${sentNoInVol(a0, pos0.pi, pos0.ti)} 句</p>
        <button class="wd-play" type="button" data-a="${a0}" data-gi="${gi0}">▶ 播放这句</button></div>
      ${ex}
      <div class="wd-block"><h4>学习状态</h4>
        <div class="wd-path">${steps}</div>
        <p class="wd-stat">接触 ${x.s ? (x.s.reps || 0) : 0} 次 · ② 答对 ${x.s ? (x.s.ok3 || 0) : 0} 次 · ${x.s && x.s.err ? '错误 ' + x.s.err + ' 次' : '无错误'}</p></div>
    </div>`;
  }
  function toggleWordRow(row) {
    const item = row.closest('.wb-item');
    if (!item) return;
    const wasOpen = item.classList.contains('open');
    const list = row.closest('.wb-list');
    if (list) list.querySelectorAll('.wb-item.open').forEach((li) => {
      li.classList.remove('open');
      const dd = li.querySelector('.wb-detail'); if (dd) dd.remove();
      const bb = li.querySelector('.wb-row'); if (bb) bb.setAttribute('aria-expanded', 'false');
    });
    if (wasOpen) return;
    item.classList.add('open');
    row.setAttribute('aria-expanded', 'true');
    const wrap = document.createElement('div');
    wrap.innerHTML = wordDetailHtml(row.dataset.w);
    item.appendChild(wrap.firstElementChild);
  }
```

点击代理里（`wm` 那块之前）加：

```js
    const wrel = e.target.closest('.wd-relearn');
    if (wrel) { try { if (TASK.relearn) TASK.relearn(wrel.dataset.relearn); } catch (err) {} window.APP3.route(); return; }
    const wrow = e.target.closest('.wb-row');
    if (wrow) { toggleWordRow(wrow); return; }
```

`app/index.html` CSS 追加（单词本 CSS 之后）：

```css
  .wb-detail { padding: 12px 14px 14px; border: 1px solid var(--border); border-top: none;
    border-bottom-left-radius: 14px; border-bottom-right-radius: 14px; background: var(--card); }
  .wd-top { display: flex; align-items: center; gap: 8px; }
  .wd-word { font-size: 17px; font-weight: 800; }
  .wd-stage { font-size: 10.5px; font-weight: 700; line-height: 1.8; padding: 0 9px; border-radius: 999px;
    background: rgba(0,0,0,.05); color: var(--muted); }
  .wd-stage.leech { background: var(--note-soft); color: var(--note); }
  .wd-relearn { margin-left: auto; flex: none; min-height: 44px; padding: 0 14px; border: 1px solid var(--border);
    border-radius: 999px; background: var(--card); color: var(--accent-text); font: inherit; font-weight: 700; cursor: pointer; }
  .wd-phon { margin-top: 6px; font-size: 12.5px; color: var(--muted); }
  .wd-mean { margin-top: 2px; font-size: 13.5px; color: var(--text); }
  .wd-block { margin-top: 12px; padding-top: 10px; border-top: 1px dashed var(--border); }
  .wd-block h4 { margin: 0 0 6px; font-size: 12.5px; color: var(--accent-text); }
  .wd-ctx { margin: 0; font-family: Georgia, 'Songti SC', serif; font-size: 14px; line-height: 1.7; color: var(--text); }
  .wd-ctx .wd-hl { color: var(--accent-text); text-decoration: underline; }
  .wd-src { margin: 6px 0 8px; font-size: 11.5px; color: var(--muted); }
  .wd-play { min-height: 44px; padding: 0 16px; border: none; border-radius: 999px; background: var(--grad);
    color: var(--cta-ink); font: inherit; font-size: 13px; font-weight: 700; cursor: pointer; box-shadow: var(--grad-shadow); }
  .wd-ex { margin: 0; font-size: 13px; color: var(--text); }
  .wd-exzh { margin: 3px 0 0; font-size: 12px; color: var(--muted); }
  .wd-path { display: flex; flex-wrap: wrap; align-items: center; gap: 6px; font-size: 12px; color: var(--muted); }
  .wd-step.on { color: var(--accent-text); font-weight: 700; }
  .wd-arrow { color: var(--muted); font-style: normal; }
  .wd-stat { margin: 8px 0 0; font-size: 12px; color: var(--muted); }
  body.dark .wb-detail { background: var(--card); border-color: var(--border); }
  body.dark .wd-stage { background: rgba(255,255,255,.06); color: var(--muted); }
  body.dark .wd-stage.leech { background: var(--note-soft); color: var(--note); }
```

- [ ] **Step 4: 跑测试确认绿**

- [ ] **Step 5: 提交** `feat(app3): 单词本词详情（点行展开，原文语境优先）`

---

## Task 4: 文章联动「▶ 播放这句」（§6.5）

**Files:**
- Modify: `app/index.html`（TASK 新增 `playSentence(a, gi)`；`openArticle` 支持 `opts.playGlobal`；导出）
- Modify: `app/app.js`（`APP3.playSentence` + 点击代理接 `.wd-play`）
- Test: `words.spec.ts`（追加）

**Interfaces:**
- Consumes: `APP3.renderWords` / `wordDetailHtml`（T1/T3）
- Produces: `TASK.playSentence(a, gi)`、`window.APP3.playSentence(a, gi)`

- [ ] **Step 1: 写失败测试**

```ts
  test('词详情「▶ 播放这句」→ 回首页并进该篇任务模式、定位该句并播放', async ({ page }) => {
    await stubData(page, WORDS);
    await page.goto(`${rootUrl}/app/index.html#/words`);
    await waitShadowReady(page);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const st = TASK.state();
      st.words['atmosphere'] = Object.assign(ShadowPlan.newWord(), { stage: 'seen', reps: 1 });
      APP3.route();
    });
    await page.locator('.wb-row[data-w="atmosphere"]').click();
    await page.locator('.wd-play').click();
    // 目标：回首页 + 进任务模式 + 该句高亮在播
    await expect(page).toHaveURL(/#\/home/);
    await expect(page.locator('body')).toHaveClass(/task-mode/);
    await expect.poll(() => page.evaluate(() => {
      const el = document.querySelector('#art .sent.playing');
      return el ? el.textContent : '';
    }), '播放的必须是含该词的原文句').toContain('atmosphere');
    expect(await page.evaluate(() => TASK.article), '进的必须是该词所属那一篇').toBe(0);
  });
```

- [ ] **Step 2: 跑测试确认红** → `TASK.playSentence` / `APP3.playSentence` 未定义（点了没反应，URL 仍 `#/words`）

- [ ] **Step 3: 实现**

`app/index.html`：
1. `openArticle(a, opts)` 里，`enterTaskMode(opts);` 之后、`if (!taskMode)` 之前插入：

```js
        /* M3 §6.5：单词本「▶ 播放这句」——进这一篇后直接落到指定全局句并起播。 */
        if (opts && opts.playGlobal != null) {
          const local = globalToLocal(opts.playGlobal);
          if (local >= 0) {
            try { playFrom(local); const el = sents[local]; if (el) el.scrollIntoView({ block: 'center' }); } catch (e) {}
          }
        }
```

2. 在 `function openArticleQuiz(a) {...}` 之后加：

```js
      // M3：单词本词详情的「▶ 播放这句」。a = 文章号，gi = 该句的全局句号。
      function playSentence(a, gi) { openArticle(a, { playGlobal: gi }); }
```

3. 公开 API 的 `return { ... openArticle, openArticleQuiz, ... }` 里，`openArticleQuiz,` 之后加 `playSentence,`。

`app/app.js`：
1. 点击代理里（`.wd-relearn` 那块之前）加：

```js
    const wplay = e.target.closest('.wd-play');
    if (wplay) { playSentence(Number(wplay.dataset.a), Number(wplay.dataset.gi)); return; }
```

2. 加 `playSentence` 并导出（放在 `openHomeHighlight` 附近）：

```js
  // §6.5：词详情 → 回首页 → 进该篇任务模式 → 定位该句并播放。
  function playSentence(a, gi) {
    if (typeof TASK === 'undefined' || !TASK.playSentence) return;
    if (location.hash !== '#/home') location.hash = '#/home';
    TASK.playSentence(a, gi);
  }
  window.APP3 = Object.assign(window.APP3, { playSentence });
```

> 顺序提醒：先设 hash（触发 `route()` 回首页），再 `TASK.playSentence` 起任务模式。任务模式用 `body.task-mode` 把 `#appShell` 收掉，所以两者不打架；`#/words` 已经是 `#/words` 时改到 `#/home` 才会触发 hashchange。

- [ ] **Step 4: 跑测试确认绿** + 回归任务模式旧锁

Run: `cd playwright-tests && npx playwright test journeys/app3/words.spec.ts` → PASS
Run: `cd playwright-tests && npx playwright test journeys/app3/task.spec.ts` → 全绿

- [ ] **Step 5: 提交** `feat(app3): 词详情「播放这句」跳回该篇任务模式并定位（§6.5）`

---

## Task 5: 收口（深色 + 无障碍 + PRD 同步 + 全量回归）

**Files:**
- Modify: `app/index.html`（如深色/a11y 有漏补）
- Modify: `docs/superpowers/specs/2026-09-24-3.0-article-first-PRD.md`（§2.4 补 M3 说明；§14.2 进度）
- Test: `words.spec.ts`（追加深色 + a11y + keyboard）

**Interfaces:** 无新增（纯收口）

- [ ] **Step 1: 写失败测试**

```ts
  test('深色模式：单词本卡片/详情不是白底，深色下可读（PRD §10.1）', async ({ page }) => {
    await stubData(page, WORDS);
    await page.goto(`${rootUrl}/app/index.html#/words`);
    await waitShadowReady(page);
    await page.evaluate(() => {
      TASK.resetV2(); TASK.initPlan(15);
      const st = TASK.state();
      st.words['atmosphere'] = Object.assign(ShadowPlan.newWord(), { stage: 'graduated', reps: 12, ok3: 1 });
      APP3.route();
    });
    await page.locator('.wb-row[data-w="atmosphere"]').click();
    const lightRow = await page.locator('.wb-row').first().evaluate((el) => getComputedStyle(el).backgroundColor);
    const lightDet = await page.locator('.wb-detail').evaluate((el) => getComputedStyle(el).backgroundColor);
    await page.evaluate(() => document.body.classList.add('dark'));
    const darkRow = await page.locator('.wb-row').first().evaluate((el) => getComputedStyle(el).backgroundColor);
    const darkDet = await page.locator('.wb-detail').evaluate((el) => getComputedStyle(el).backgroundColor);
    expect(darkRow).not.toBe(lightRow);
    expect(darkDet).not.toBe(lightDet);
    expect(darkRow).not.toBe('rgb(255, 255, 255)');
    expect(darkDet).not.toBe('rgb(255, 255, 255)');
    // 深色下正文颜色不是白底黑字那种「没覆盖」的默认
    const color = await page.locator('.wd-mean').evaluate((el) => getComputedStyle(el).color);
    expect(color).not.toBe('rgb(0, 0, 0)');
  });

  test('无障碍：h1 唯一、筛选是 button+aria-pressed、行可聚焦可键盘、触摸目标 ≥44px', async ({ page }) => {
    await stubData(page, WORDS);
    await page.goto(`${rootUrl}/app/index.html#/words`);
    await waitShadowReady(page);
    await page.evaluate(() => { TASK.resetV2(); TASK.initPlan(15); APP3.route(); });

    await expect(page.locator('.words-page > h1')).toHaveCount(1);
    await expect(page.locator('.wb-filter')).toHaveCount(4);
    for (const f of ['all', 'todo', 'learning', 'mastered']) {
      await expect(page.locator(`.wb-filter[data-f="${f}"]`)).toHaveAttribute('aria-pressed', /true|false/);
    }
    const row = page.locator('.wb-row').first();
    await expect(row).toHaveAttribute('aria-expanded', 'false');
    expect(await row.evaluate((el) => el.tagName)).toBe('BUTTON');
    for (const sel of ['.wb-row', '.wb-filter', '.wb-more']) {
      const h = await page.locator(sel).first().evaluate((el) => el.getBoundingClientRect().height);
      expect(h, `${sel} 触摸目标 ≥44px`).toBeGreaterThanOrEqual(44);
    }
    // 键盘：Tab 聚焦到行，Enter 展开
    await row.focus();
    await page.keyboard.press('Enter');
    await expect(row).toHaveAttribute('aria-expanded', 'true');
    await expect(page.locator('.wb-detail')).toBeVisible();
  });
```

- [ ] **Step 2: 跑测试确认红** → 深色那条应红（若 `.wb-row` 用 `var(--card)`，`body.dark` 会改 token，其实可能已绿；真正的漏点通常是写死浅色值）。**若两条都已绿**，说明 Task 1/3 已把深色写全 —— 仍保留锁，继续。a11y 那条若已绿属正常，但必须确认 `aria-*` 与 44px 是实打实的。

- [ ] **Step 3: 实现（有漏才补）**

按 Step 2 的红点补：若 `.wb-row`/`.wb-detail` 有写死浅色则加 `body.dark` 覆盖；若某按钮 <44px 则补 `min-height:44px`。

PRD 同步：

1. §2.4 表格后的注释区（`> **M2 落实**…` 之后）追加：

```markdown
> **M3 落实**（2026-09-24）：单词本是 §2.2 的一级页，入口**只有侧栏「单词本」一项**（`#/words`，
> 可带筛选段 `#/words/todo|learning|mastered` 做深链）；词详情是**行内展开**（`aria-expanded`），
> 「▶ 播放这句」复用既有的文章任务模式（回首页 + 进该篇 + 定位该句播放），
> **不是新浮层、也不是新页面**，所以本表无需扩列。
```

2. §14.2 表格下（`> **M1 的验收就一句话…**` 那段之后）追加：

```markdown
> **M3 落实**（2026-09-24）：单词本（列表 + 四档筛选 + 词详情原文语境 + 词详情→该篇播放联动）已交付。
> M2「待加强」的「去复习」现落到 `#/words/todo`（「待掌握」筛选视图）。
```

- [ ] **Step 4: 跑测试确认绿 + 全量回归**

Run: `cd playwright-tests && npx playwright test journeys/app3/words.spec.ts` → PASS
Run: `cd playwright-tests && npm test` → 全绿（基线 238 + M3 新增）
Run: `python3 scripts/validate_data.py` → `Validation PASSED`
Run: `node scripts/build_site.mjs` → 成功
Run: `git diff --name-only main...HEAD` → 不得出现 `index.html`、`shadow/**`、`shadow/js/plan-engine.js`、`config.js`、`data/vocab.json`

- [ ] **Step 5: 提交** `feat(app3): 单词本收口（深色 + 无障碍）+ PRD §2.4/§14.2 同步`

---

## Self-Review

**1. Spec coverage（M3 范围内的 PRD 条款）**

| PRD 条款 | 落在哪个 Task |
|---------|-------------|
| §6.1 单词始终跟文章语境绑（原文语境排第一） | T3 |
| §6.2 列表结构（词 + 状态胶囊 + 文章 + 卷 + 出现次数 + 最近学习） | T1 |
| §6.3 四档筛选（全部 / 待掌握 / 学习中 / 已掌握） | T2 |
| §6.4 词详情（点行展开）：原文语境 + 例句 + 学习状态 | T3 |
| §6.5 与文章的联动（▶ 播放这句 → 该篇任务模式 → 定位播放） | T4 |
| §9.4 全从既有 state/ROOT2 派生、不新增存储字段 | T1–T4（`wordIndex` 只读 SECTIONS/VOCAB/state） |
| §9.5 派生索引（`wordArticle` 同源） | T1 的 `wordIndex` |
| §10.1 令牌继承 / 深色 | T1 / T3 / T5 |
| §10.2 断点 | T1（`.wb-filter` 手机两列） |
| §10.4 无障碍（44px / 可聚焦 / 语义化） | T3 / T5 |
| §2.4 不新增浮层/页面 | 全局约束（行内展开 + 复用任务模式） |
| §13.1「筛选四档计数正确」「词详情播放→进任务模式定位该句」 | T2 / T4 |
| §14.2 M3 那行 | 本 plan 整体 |

**2. Placeholder scan**：全文无 TBD/TODO；每个代码步骤都给了可粘贴的代码与精确锚点。

**3. Type consistency**：
- `APP3.renderWords(view, filter)` / `playSentence(a, gi)` / `TASK.playSentence(a, gi)`/`openArticle(a, {playGlobal})` —— T1–T4 逐处定义并使用，签名一致。
- 选择器：`.words-page` / `.wb-filters` / `.wb-filter[data-f]` / `.wf-n` / `.wb-list` / `.wb-row[data-w]` / `.wr-word` / `.wr-pill` / `.wr-meta` / `.wb-more` / `.wb-detail` / `.wd-phon` / `.wd-mean` / `.wd-ctx .wd-hl` / `.wd-src` / `.wd-play` / `.wd-path .wd-step.on` / `.wd-stat` / `.wd-relearn` —— 测试与实现一致。
- 筛选 key：`all|todo|learning|mastered` 与 `WB_FILTERS`、`WB_FILTER_LABEL`、路由段、M2 `.tip-go` 落点一致。

**4. 依赖顺序**：T1（路由 + 索引 + 列表骨架）→ T2（筛选）/ T3（详情）/ T4（联动，依赖 T3 的 `.wd-play`）各自独立可验证 → T5（收口，依赖全部）。

**已知取舍（写进报告）**：
- 「词表宇宙 = 全部目标词」而非「已收录」：§6.3「全部 = 不筛」+ 性能要求（3245 条）支持这个口径；未见面词的 stage 视作 `fresh`，只在「全部」出现。
- §6.4 的「例句 / 其他语境」用 `/shadow/data/vocab.json` 里已有的 `ex`/`exZh`（共享词库自带），不引主站 3674 词书例句（§14.1 #14）；词库无 `ex` 时该块不显示（不伪造）。
- 「最近学习时间」用 `state.words[w].lastContactAt`；未接触过显示「还没学过」。
- 性能方案 = 增量渲染（每批 60）而非虚拟列表，理由见 File Structure。
