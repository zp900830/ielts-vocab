# 无障碍审计 · shadow 影子跟读站 — 2026-09-21

- 对象：`shadow/index.html`（工作区当前版，含另一会话未提交的 8 行改动）+ `shadow/js/plan-engine.js`
- 方法：axe-core 4.10.2 注入 `http://localhost:8946`（6 组扫描：移动/桌面 × 明/暗 × 弹窗/抽屉/计划屏）+ Playwright 手工键盘走查 + 触摸目标实测 + 对比度公式核对
- 本轮零改动：产品文件未动（`git status` 里的 `M shadow/index.html` 非本审计所为）；证据在 `/tmp/a11y-0921/`（临时目录，重启即失，建议尽快处理 F1/F2）
- 结论：**2 阻断、9 重要、5 次要、8 项检查过确认合规**。得分约 62/100（D）。对 VoiceOver/键盘用户，核心链路「点词查义」与「句内嵌套控件」目前是断的。

## 发现表

| # | 屏/对象 | 问题 | WCAG | 严重度 | 来源 | 定性 | 修法（一句话） |
|---|---|---|---|---|---|---|---|
| F1 | 生词弹窗 `#pop` | **键盘 Enter/Space 打不开弹窗**：`.w` 的 keydown 调 openPop 后冒泡到 `.sent` 的 `el.click()`，click 委托又把「弹窗外点击」判定为关闭——弹窗开一瞬即关（弹窗标题已被写入可证开过） | 2.1.1, 4.1.2 | 阻断 | ②人工 | 需决策（1 行） | `.w` keydown 处理末尾 `e.stopPropagation()`；或 `.sent` 处理器仅 `e.target===el` 时 click |
| F2 | 正文全部 1833 句 | **嵌套交互**：`.sent`（role=button+aria-label「朗读该句」）内嵌 `.w`、书签、A/B、辨析等按钮；外层 name 覆盖了句文本，SR 读不到句内按钮/句子本身；词库抽屉 300 张 `.item` 同型 | 4.1.2, 2.1.1 | 阻断 | ①axe（1833+300 nodes） | 需决策 | `.sent`/`.item` 去掉 role=button 与 aria-label，朗读改为句内独立按钮 |
| F3 | 今日面板 `#todayPanel` | `aria-modal="true"` 但无焦点陷阱：Tab 走过 `#psStart` 后泄漏到正文 `.w`；关闭后焦点未归还 `#btnToday` | 2.4.3, 4.1.2 | 重要 | ②人工 | 需决策（低） | 复用现成 `openModal/_trapFocus`（弹窗 #pop 已验证可用） |
| F4 | 全站明色移动态 | **对比度不达标 7225 节点**：行内查词绿 `.w` #0d9d74/bg=3.24（1091）、行内词义 `.gl` 3.49、音标 `.gl-p` 3.13（约 1840）、辨析卡词 `.nb-w` 4.14（1420）、选中态 `白字/#10b487`=2.65（ps-opt.sel、pill.on、abLive 同色） | 1.4.3 | 重要 | ①axe+计算 | 需决策 | 调深色板：绿系文本降到 #0a7d5c 一档、选中态换深底 |
| F5 | 暗色 para-zh/notebar | 暗夜段主题行 `#908a7e/#2b251a`=4.42（98+12 节点）、nb-ph 4.28——差 0.08 一档即过 | 1.4.3 | 重要 | ①axe | 需决策 | 暗色 `--muted` 提到 #9a9488 |
| F6 | 播放条 A-B 指示 `#abLive` | 开启 A-B 后 `display:flex` 可聚焦，但 `aria-hidden="true"` 且无名字——SR 用户被「隐形按钮」卡焦点 | 4.1.2, 2.4.4 | 重要 | ②人工 | safe 倾向 | 去掉 aria-hidden，加 `aria-label="退出 A-B 循环"` |
| F7 | 播放条/顶栏开关 | `btnZh/btnGloss/btnPlay` 仅 `.active` 类表状态，无 `aria-pressed`；`btnLoop/btnAB/rateCycle/markBtn/btnCloud/btnMenu` 菜单无 `aria-expanded/haspopup` | 4.1.2 | 重要 | ②人工 | safe 倾向 | 点击/切态处同步 aria-pressed、aria-expanded |
| F8 | 学习计划屏 `.ps-opt`（7 个分钟钮） | 本体是 `<button>` ✓，但选中态**只有背景色变化**（computed：透明↔rgb(16,180,135)），无 aria-pressed/checked、无 radiogroup；结果行 `#psSum`「今天大约8句·16题」无 aria-live，键盘选完无任何播报 | 1.4.1, 4.1.2, 4.1.3 | 重要 | ②人工 | 需决策 | `role=radiogroup`+`aria-checked`+✓前缀，`#psSum` 加 `aria-live=polite` |
| F9 | 词库抽屉（移动） | `aside` 无 dialog 语义；背景滚动锁了但**焦点没锁**：走过抽屉末元素（902 个可聚焦）即进入背景跳到正文链；`#edgeTab` 无 aria-expanded；关闭钮可访问名是「✕」字符 | 2.4.3, 4.1.2 | 重要 | ②人工 | 需决策 | ≤700px 时 role=dialog+trap（复用 F3 方案），edgeTab 加 aria-expanded，✕ 加 aria-label |
| F10 | 抽屉搜索结果 | 计数「共1 / 3245 词」无 aria-live，检索后 SR 无反馈 | 4.1.3 | 重要 | ②人工 | safe 倾向 | `.cnt` 加 `aria-live=polite` |
| F11 | 播放条/任务栏/菜单触摸目标 | 实测 <44px：btnPlay 65×30、上/下句 68×30、btnLoop 32×30、rateCycle 29×30、markBtn 32×30、btnMenu 24×24、抽屉✕ 27×22、tb-btn 69×35、ps-opt 64×32、edgeTab 32×60（2.5.8 的 24px 线只有 btnMenu/✕ 擦边不过） | 2.5.5(AAA), 2.5.8(AA-2.2) | 重要 | ②人工 | safe 倾向 | 移动态给 `.audiobar .btn`、`.tb-btn`、`.ps-opt` 加 `min-height:44px`，✕/btnMenu 扩 padding |
| F12 | 任务句左边条 | task-new/review/done 三态**仅颜色**区分（3px 左边框），暗态 done 条 #5c5650 对底 2.4:1（<3:1） | 1.4.1, 1.4.11 | 重要 | ②人工+计算 | 需决策 | 已学加 ✓/删号前缀文本，done 边框提亮 |
| F13 | 今日面板 | 标题层级 h1→h3 跳级（axe heading-order，全部 6 组扫描均报） | 1.3.1/2.4.6 | 次要 | ①axe | safe | 改 `<h2>` 或加 `aria-level="2"` |
| F14 | 生词弹窗「发音」钮 | 可访问名仅「发音」，未带词名（弹窗内尚可容忍） | 2.4.6 | 次要 | ②人工 | safe | aria-label 拼上当前词 |
| F15 | 音色/搜索框焦点态 | `outline:none` 后仅 border-color 变（accent-bright 对白底约 3.0:1 擦线） | 1.4.11 | 次要 | ②人工 | safe | 焦点统一走 :focus-visible outline |
| F16 | 顶栏 `btnAccent` | aria-label「切换英式美式发音」不含当前档位（明暗靠 🇬🇧/🇺🇸 emoji，label 已覆盖文本） | 4.1.2 | 次要 | ②人工 | safe | label 拼当前口音 |
| F17 | 弹窗（移动）盖播放条 | bottom sheet 与播放条矩形重叠（实测 overlapsAB=true）——modal 语义下背景本就不可达，仅提示知悉 | — | 观察 | ②人工 | 无需改 | — |

## 检查过确认合规（③）

| 项 | 证据 |
|---|---|
| 弹窗 `#pop` 对话框本体：role/aria-modal/tabindex、焦点落入首钮、Tab 环绕不外逃、Esc 关+焦点归还触发词 | probe：mousePop/mouseTabRing（4 步全在 pop 内）/mouseEsc；截图 `/tmp/a11y-0921/p1_mousepop.png` |
| 辨析折叠头 `.cmp-head`：真 button + aria-expanded 且 toggle 同步（源码 1673/1717），移动实测 352×85 | axe 6 扫描 0 报；TOUCH 数据 |
| 跳到正文 skip link 存在、桌面 Tab 首站命中 | 源码 1286 + desktopStart 序列 |
| 无禁用缩放（viewport）、`lang="zh-CN"`+title、prefers-reduced-motion、`:focus-visible` 全局 outline | 源码 5/2/107/106 |
| 无 img/video/audio；remixicon 全 `aria-hidden`；云登录/跳转/搜索输入均有 aria-label；sync-hint 有 role=status+aria-live；弹窗桌面底部边缘不裁切（JS 翻转实测 popBottom 856<900） | 源码 + probe2 desktopClip 复测 |
| 键盘计划屏可选值（Enter 选中生效、类切换正常） | probe psAfterEnter cls=ps-opt sel |

## 建议直接修的 safe 清单（等批准，预计不动布局）

1. **F6** `#abLive`：去 aria-hidden + aria-label「退出 A-B 循环」
2. **F7** 顶栏/播放条 7 个开关与菜单：补 aria-pressed / aria-expanded 同步
3. **F10** 抽屉计数 `aria-live=polite`；**F9 局部** 抽屉 ✕ 加 aria-label「收起词库」
4. **F11** 移动端 min-height:44px（播放条钮/任务栏钮/ps-opt/✕/btnMenu）——注意会微调播放条高度
5. **F13** 计划屏 h3→h2；**F14/F16** 发音钮与口音钮 label 补上下文；**F15** 输入焦点态走统一 outline
6. **F1**（一行 stopPropagation）技术上最小、但不算纯 safe（碰交互逻辑），建议随批

## 需要拍板的设计类问题

- **F2 句子/词卡语义重构**：朗读按钮和「句即按钮」二选一；这是 SR 可用性的根，改动面最大
- **F1 修法**：stopPropagation 还是 `.sent` 处理器加 target 判断
- **F4/F5/F8/F12 色彩系统**：绿色板整体调深一档？选中态加非颜色符号？
- **F3/F9 焦点陷阱**：今日面板与移动抽屉是否统一接入现成 openModal 机制（推荐，成本=接线）

## 扫描命令与统计（复现用）

```
脚本 /tmp/a11y-0921/{audit.js,probe.js,probe2.js}；服务 cd shadow && python3 -m http.server 8946
axe-core 经 shadow/.a11y-probe/ 动态 script-src 注入（绕页面 CSP），已删除
s1 移动明 3 violation 组(7225+1+1833) · s2 pop开 同构 · s3 抽屉(5+300) · s4 计划屏(2+1)
s5 移动暗(143+1+1833) · s6 桌面明(1+1833)   全部 results/probe/probe2.json + 截图 8 张
DIALOGS: []（全程零 alert，播放链未狂奔）
```
